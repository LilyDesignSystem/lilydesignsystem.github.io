<script lang="ts">
  const html: string = "<h1>Tree List Item</h1>\n\n<p>A tree list item is a single item within a TreeList hierarchy, rendered as an <code>&lt;li&gt;</code> element with <code>role=\"treeitem\"</code>. It supports keyboard focus management via <code>tabindex</code> and can represent expandable or leaf nodes in the tree structure.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;li&gt;</code> element with class <code>tree-list-item</code></li>\n<li>Spreads <code>restProps</code> onto the root element for consumer customization</li>\n</ul>\n\n<h2>Help</h2>\n\n<p>Use TreeListItem for individual entries within a TreeList. Common scenarios include file browser entries, organizational chart nodes, nested navigation items, and category trees. Items can be expandable (branches) or leaf nodes.</p>\n\n<h2>Syntax</h2>\n\n<pre><code class=\"language-html\">&lt;TreeListItem label=\"...\"&gt;Label&lt;/TreeListItem&gt;</code></pre>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;TreeNav label=\"Clinical navigation\"&gt;\n  &lt;TreeList label=\"Departments\"&gt;\n    &lt;TreeListItem tabindex=\"0\" aria-expanded=\"true\"&gt;\n      Cardiology\n      &lt;ul role=\"group\"&gt;\n        &lt;TreeListItem tabindex=\"-1\"&gt;\n          &lt;TreeLink href=\"/cardiology/outpatients\"&gt;Outpatients&lt;/TreeLink&gt;\n        &lt;/TreeListItem&gt;\n        &lt;TreeListItem tabindex=\"-1\"&gt;\n          &lt;TreeLink href=\"/cardiology/inpatients\"&gt;Inpatients&lt;/TreeLink&gt;\n        &lt;/TreeListItem&gt;\n      &lt;/ul&gt;\n    &lt;/TreeListItem&gt;\n    &lt;TreeListItem tabindex=\"-1\" aria-expanded=\"false\"&gt;\n      Oncology\n    &lt;/TreeListItem&gt;\n    &lt;TreeListItem tabindex=\"-1\"&gt;\n      &lt;TreeLink href=\"/radiology\"&gt;Radiology&lt;/TreeLink&gt;\n    &lt;/TreeListItem&gt;\n  &lt;/TreeList&gt;\n&lt;/TreeNav&gt;</code></pre>\n\n<h2>Props</h2>\n\n<table>\n<thead>\n<tr><th>Prop</th><th>Type</th><th>Default</th><th>Description</th></tr>\n</thead>\n<tbody>\n<tr><td><code>children</code></td><td><code>slot</code></td><td>(required)</td><td>Content for the tree item</td></tr>\n<tr><td><code>...restProps</code></td><td>HTML attributes</td><td></td><td>Additional attributes spread onto the <code>&lt;li&gt;</code> element (including <code>role</code>, <code>tabindex</code>, <code>aria-expanded</code>)</td></tr>\n</tbody>\n</table>\n\n<h2>Examples</h2>\n\n<p>Navigation tree:</p>\n\n<pre><code class=\"language-html\">&lt;TreeList label=\"Site navigation\"&gt;\n  &lt;TreeListItem tabindex=\"0\" aria-expanded=\"false\"&gt;\n    Products\n  &lt;/TreeListItem&gt;\n  &lt;TreeListItem tabindex=\"-1\" aria-expanded=\"false\"&gt;\n    Services\n  &lt;/TreeListItem&gt;\n  &lt;TreeListItem tabindex=\"-1\"&gt;About&lt;/TreeListItem&gt;\n&lt;/TreeList&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>ArrowDown: Moves focus to the next visible tree item (managed by parent TreeList)</li>\n<li>ArrowUp: Moves focus to the previous visible tree item (managed by parent TreeList)</li>\n<li>ArrowRight: Expands a collapsed item, or moves to first child (when applicable)</li>\n<li>ArrowLeft: Collapses an expanded item, or moves to parent (when applicable)</li>\n<li>Home: Moves focus to the first tree item (managed by parent TreeList)</li>\n<li>End: Moves focus to the last visible tree item (managed by parent TreeList)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"treeitem\"</code> -- identifies the element as an item in a tree widget</li>\n<li><code>tabindex</code> -- manages focus: <code>0</code> for the active item, <code>-1</code> for others (roving tabindex)</li>\n<li><code>aria-expanded</code> -- indicates whether an expandable item is open (<code>true</code>) or closed (<code>false</code>); omit for leaf nodes</li>\n<li><code>aria-selected</code> -- indicates whether the item is selected (optional)</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside TreeList to represent one item that may contain nested TreeList children.</li>\n<li>Use for both expandable branch nodes (with <code>aria-expanded</code>) and leaf nodes.</li>\n<li>Use with TreeLink inside the item for navigable leaf nodes.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside TreeList -- it relies on the parent for keyboard navigation and tree semantics.</li>\n<li>Do not use for flat list items -- use ContentsListItem or AccordionListItem instead.</li>\n<li>Do not use for standalone expandable content -- use Collapsible or Details instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides an <code>&lt;li&gt;</code> element with <code>role=\"treeitem\"</code> support. The consumer manages <code>tabindex</code>, <code>aria-expanded</code>, and <code>aria-selected</code> attributes via restProps, and provides all content and visual styling including indentation, icons, and expansion indicators.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.tree-list-item</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;li&gt;</code> element with class <code>tree-list-item</code></li>\n<li>Verify role=\"treeitem\"` -- identifies the element as an item in a tree widget</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Distinguish branch nodes from leaf nodes visually (e.g., folder vs. file icons, chevron indicators). Highlight the currently focused or selected item.</li>\n<li><strong>Developers</strong>: Use <code>tabindex=\"0\"</code> for the active item and <code>tabindex=\"-1\"</code> for all others (roving tabindex pattern). Omit <code>aria-expanded</code> for leaf nodes.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>TreeListItem is a child of TreeList, following the Nav/List/ListItem pattern. Expandable items can contain a nested <code>&lt;ul role=\"group\"&gt;</code> with additional TreeListItem children to create subtrees.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>tree-nav</code> — a hierarchical navigation with expandable branches</li>\n<li><code>tree-list</code> — a hierarchical list with nested expandable items</li>\n<li><code>tree-link</code> — one link in the tree</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Tree View Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/treeview/</li>\n<li>WAI-ARIA treeitem role: https://www.w3.org/TR/wai-aria-1.2/#treeitem</li>\n</ul>";
</script>

<svelte:head>
  <title>Tree List Item — Lily Design System</title>
  <meta name="description" content="A tree list item is a single item within a TreeList hierarchy, rendered as an &lt;li&gt; element with role=&quot;treeitem&quot;. It supports keyboard focus management via tabindex and can represent expandable or l..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
