<script lang="ts">
  const html: string = "<h1>Call To Action</h1>\n\n<p>A call to action (CTA) is a component designed to prompt users to take a specific action, such as navigating to a page or triggering an operation. CTAs are typically the most prominent interactive element on a page, driving user engagement toward key goals like \"Sign Up\", \"Learn More\", or \"Buy Now\".</p>\n\n<p>This component renders as an <code>&lt;a&gt;</code> element when an <code>href</code> is provided (for navigation CTAs) or as a <code>&lt;button&gt;</code> when no href is given (for action CTAs). This dual-mode approach ensures correct semantic HTML is used for each use case, benefiting both accessibility and SEO.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as <code>&lt;a&gt;</code> when <code>href</code> is provided, <code>&lt;button type=\"button\"&gt;</code> otherwise</li>\n<li>Dual-mode: link for navigation CTAs, button for action CTAs</li>\n<li><code>disabled</code> only applies in button mode (links cannot be disabled natively)</li>\n<li>Optional <code>aria-label</code> for screen reader text override when visible text is insufficient</li>\n<li>Content is provided through child elements</li>\n<li>All user-facing text comes through props and children for internationalization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>href</code>: string (optional) -- if provided, renders as a link</li>\n<li><code>label</code>: string (optional) -- <code>aria-label</code> override for screen readers</li>\n<li><code>disabled</code>: boolean (default: false) -- disables the button (button mode only)</li>\n<li><code>onclick</code>: (event: MouseEvent) =&gt; void (optional) -- click handler (button mode)</li>\n<li><code>children</code>: slot (required) -- the CTA content</li>\n<li><code>...restProps</code>: Any additional HTML attributes passed to the element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Navigation CTA linking to a sign-up page:</p>\n\n<pre><code class=\"language-html\">&lt;CallToAction href=\"/signup\"&gt;Sign Up Now&lt;/CallToAction&gt;</code></pre>\n\n<p>Action CTA triggering a workflow:</p>\n\n<pre><code class=\"language-html\">&lt;CallToAction onclick={handleAction}&gt;Get Started&lt;/CallToAction&gt;</code></pre>\n\n<p>Hero section with a primary CTA:</p>\n\n<pre><code class=\"language-html\">&lt;Hero&gt;\n  &lt;h1&gt;Build accessible apps faster&lt;/h1&gt;\n  &lt;p&gt;Lily Design System provides headless, WCAG AAA-compliant components for every framework.&lt;/p&gt;\n  &lt;CallToAction href=\"/docs/getting-started\"&gt;Get Started Free&lt;/CallToAction&gt;\n&lt;/Hero&gt;</code></pre>\n\n<p>CTA with a screen reader label override:</p>\n\n<pre><code class=\"language-html\">&lt;CallToAction href=\"/pricing\" label=\"View pricing plans for Lily Design System\"&gt;\n  View Pricing\n&lt;/CallToAction&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus the element</li>\n<li>Enter: Activate the link or button</li>\n<li>Space: Activate the button (button mode only; links do not respond to Space)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Implicit <code>link</code> role when rendered as <code>&lt;a&gt;</code></li>\n<li>Implicit <code>button</code> role when rendered as <code>&lt;button&gt;</code></li>\n<li>Optional <code>aria-label</code> -- provides screen reader text when visible text alone is insufficient</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to prominently encourage users to take a specific action, such as signing up, starting a service, or downloading an app.</li>\n<li>Use for the most prominent interactive element on a page that drives user engagement toward a key goal.</li>\n<li>Use when you need dual-mode behavior: an <code>&lt;a&gt;</code> for navigation CTAs or a <code>&lt;button&gt;</code> for action CTAs.</li>\n<li>Use in hero sections, landing pages, or marketing banners to direct the user to the primary conversion goal.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for routine navigation -- use ActionLink instead.</li>\n<li>Do not use for form submission -- use Button with <code>type=\"submit\"</code> instead.</li>\n<li>Do not use for secondary or low-priority actions -- use Button instead.</li>\n<li>Do not use more than one primary CTA per section to avoid diluting focus.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides either a semantic <code>&lt;a&gt;</code> or <code>&lt;button&gt;</code> element (based on the <code>href</code> prop), optional <code>aria-label</code>, and disabled state management, all with zero visual styling. The consumer is responsible for all CSS including prominent sizing, bold colors, padding, border-radius, hover/focus effects, and any arrow or icon decoration.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.call-to-action</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>call-to-action</code></li>\n<li>Verify Optional <code>aria-label</code> -- provides screen reader text when visible text alone is insufficient</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Limit to one primary CTA per page or section to avoid diluting focus. Use size, color, and whitespace to make it the most visually prominent element.</li>\n<li><strong>Developers</strong>: The component automatically renders as <code>&lt;a&gt;</code> when <code>href</code> is provided and <code>&lt;button&gt;</code> otherwise. The <code>disabled</code> prop only applies in button mode since links cannot be natively disabled.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>button</code> — a generic clickable button element</li>\n<li><code>action-link</code> — a hyperlink styled as an action trigger</li>\n<li><code>hero</code> — a large box or image with a title and description</li>\n<li><code>feature-card</code> — a large content card with a prominent image positioned alongside or above the text</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/HTML/Element/div\">MDN div element</a></li>\n</ul>";
</script>

<svelte:head>
  <title>Call To Action — Lily Design System</title>
  <meta name="description" content="A call to action (CTA) is a component designed to prompt users to take a specific action, such as navigating to a page or triggering an operation. CTAs are typically the most prominent interactive ..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
