<script lang="ts">
  const html: string = "<h1>Tag Group</h1>\n\n<p>TagGroup is a headless container for a collection of related tags. It uses the ARIA <code>group</code> role with an accessible label to semantically associate tag elements, making the collection understandable to screen readers and other assistive technologies.</p>\n\n<p>This component is useful for displaying categories, skills, technologies, or any set of labels that belong together. Consumers provide the individual tag elements as children and bring their own styling.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> with <code>role=\"group\"</code> to semantically group child tag elements</li>\n<li>Uses <code>aria-label</code> for an accessible name describing the group</li>\n<li>Accepts a <code>children</code> slot for rendering tag elements inside the group</li>\n<li>Spreads <code>restProps</code> onto the container for consumer customization</li>\n<li>pattern: standard props destructuring with typed props</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name for the group via <code>aria-label</code></li>\n<li><code>children</code>: slot (required) -- tag elements to render inside the group</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the container <code>&lt;div&gt;</code></li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;TagGroup label=\"Patient statuses\"&gt;\n  &lt;Tag label=\"Status: Verified\"&gt;Verified&lt;/Tag&gt;\n  &lt;Tag label=\"Status: NHS patient\"&gt;NHS patient&lt;/Tag&gt;\n  &lt;Tag label=\"Status: Review due\"&gt;Review due&lt;/Tag&gt;\n&lt;/TagGroup&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive container. Keyboard behavior depends on the child tag elements provided by the consumer.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"group\"</code> -- identifies the container as a semantic grouping of related elements</li>\n<li><code>aria-label={label}</code> -- provides an accessible name describing the purpose of the tag collection</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to display multiple tags together, such as statuses or categories applied to a single item</li>\n<li>Use within cards, table rows, or list items where an item has several attributes to show</li>\n<li>Use to visually group related status indicators with consistent spacing</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for a single tag -- render the Tag component directly</li>\n<li>Do not use for interactive chip or filter patterns -- use ToggleGroup or SegmentGroup instead</li>\n<li>Do not use for tag creation and removal -- use TagInput for editable tag lists</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component renders a <code>&lt;div&gt;</code> with <code>role=\"group\"</code> and <code>aria-label</code> to semantically associate child tag elements. It provides grouping semantics for assistive technologies. The consumer provides all visual styling including layout (flex, grid, wrap), spacing, and gap between tags.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.tag-group</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>tag-group</code></li>\n<li>Verify role=\"group\"` -- identifies the container as a semantic grouping of related elements</li>\n<li>Verify aria-label={label}` -- provides an accessible name describing the purpose of the tag collection</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use a horizontal flex-wrap layout with consistent gaps between tags. Consider a label or heading above the group to visually identify the tag category.</li>\n<li><strong>Developers</strong>: Use a descriptive <code>label</code> prop that names the collection (e.g., \"Technologies\", \"Skills\"). Pair TagGroup with Tag children for proper semantic structure.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>TagGroup uses the Group/Item composition pattern. Place Tag components as children inside TagGroup. The TagGroup provides the <code>role=\"group\"</code> container with an accessible label, while each Tag provides an individual labeled element within the group.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>tag</code> — a keyword label for categorizing content</li>\n<li><code>tag-input</code> — an input for adding and removing tags</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA group role: https://www.w3.org/TR/wai-aria-1.2/#group</li>\n</ul>";
</script>

<svelte:head>
  <title>Tag Group — Lily Design System</title>
  <meta name="description" content="TagGroup is a headless container for a collection of related tags. It uses the ARIA group role with an accessible label to semantically associate tag elements, making the collection understandable ..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
