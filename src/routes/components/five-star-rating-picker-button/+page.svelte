<script lang="ts">
  const html: string = "<h1>Five Star Rating Picker Button</h1>\n\n<p>A five-star rating picker button is an individual button within a FiveStarRatingPicker that represents one star level on a 1-5 scale. When clicked, it selects that star rating value. The component uses <code>aria-pressed</code> to indicate the currently selected state, making it accessible to screen readers and keyboard users.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as a <code>&lt;button&gt;</code> element with <code>aria-pressed</code> for toggle state</li>\n<li>Designed to be used inside a FiveStarRatingPicker component</li>\n<li>Each button represents one of five star levels (1-5)</li>\n<li>The <code>selected</code> prop controls <code>aria-pressed</code> to indicate the active selection</li>\n<li>The <code>label</code> prop provides the accessible name via <code>aria-label</code> (e.g., \"1 star\", \"3 stars\")</li>\n<li>Spreads <code>...restProps</code> onto the <code>&lt;button&gt;</code> element for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>value</code>: string (required) -- the star rating value this button represents (e.g., \"1\", \"2\", \"3\", \"4\", \"5\")</li>\n<li><code>selected</code>: boolean (default: false) -- whether this button is currently selected</li>\n<li><code>label</code>: string (required) -- accessible label for the button (e.g., \"1 star\", \"2 stars\", \"5 stars\")</li>\n<li><code>onclick</code>: callback -- handler called when the button is clicked</li>\n<li><code>...restProps</code>: any -- additional HTML attributes spread onto the <code>&lt;button&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;FiveStarRatingPicker label=\"Rate this product\"&gt;\n    &lt;FiveStarRatingPickerButton value=\"1\" label=\"1 star\" onclick={handleClick} /&gt;\n    &lt;FiveStarRatingPickerButton value=\"2\" label=\"2 stars\" onclick={handleClick} /&gt;\n    &lt;FiveStarRatingPickerButton value=\"3\" label=\"3 stars\" selected onclick={handleClick} /&gt;\n    &lt;FiveStarRatingPickerButton value=\"4\" label=\"4 stars\" onclick={handleClick} /&gt;\n    &lt;FiveStarRatingPickerButton value=\"5\" label=\"5 stars\" onclick={handleClick} /&gt;\n&lt;/FiveStarRatingPicker&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus the button</li>\n<li>Enter / Space: Activate the button to select this rating</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-pressed</code> -- indicates whether this button is the currently selected rating (<code>true</code> when selected, <code>false</code> otherwise)</li>\n<li><code>aria-label</code> -- provides the accessible name for the button (e.g., \"3 stars\")</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside FiveStarRatingPicker to represent one star level in the rating scale.</li>\n<li>Use when each star needs individual styling, icons, or interactive behavior.</li>\n<li>Use to provide custom star shapes, colors, or hover effects for each level.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside of a FiveStarRatingPicker -- it requires the parent picker for group semantics.</li>\n<li>Do not use as a standalone toggle button -- use ToggleButton instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;button&gt;</code> with <code>aria-pressed</code> and <code>aria-label</code> for accessible toggle state. The consumer provides all visual styling including star icons, colors, sizing, and pressed/unpressed visual states.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.five-star-rating-picker-button</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;button&gt;</code> element with class <code>five-star-rating-picker-button</code></li>\n<li>Verify aria-label` -- provides the accessible name for the button (e.g., \"3 stars\")</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Ensure the pressed and unpressed states are visually distinct, with sufficient color contrast for both states. Consider animation on press.</li>\n<li><strong>Developers</strong>: Always provide a descriptive <code>label</code> prop (e.g., \"3 stars\") so screen readers announce the rating level clearly.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>five-star-rating-picker</code> — a picker for selecting a 1-5 star rating using radio buttons</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Button Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/button/</li>\n</ul>";
</script>

<svelte:head>
  <title>Five Star Rating Picker Button — Lily Design System</title>
  <meta name="description" content="A five-star rating picker button is an individual button within a FiveStarRatingPicker that represents one star level on a 1-5 scale. When clicked, it selects that star rating value. The component ..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
