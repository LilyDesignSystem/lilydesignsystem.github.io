<script lang="ts">
  const html: string = "<h1>Combobox</h1>\n\n<p>A combobox combines a text input with a dropdown listbox of suggestions, providing an autocomplete or type-ahead search experience. Users type into the input to filter suggestions, then select from matching options in the dropdown. This pattern is commonly used for search fields, address inputs, tag selectors, and any scenario where users choose from a large set of options.</p>\n\n<p>The component manages the input value and dropdown open state as bindable props, so consumers can reactively filter options and control visibility. It supports full ARIA combobox semantics including <code>aria-expanded</code>, <code>aria-controls</code>, and <code>aria-autocomplete</code> for screen reader accessibility.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a wrapper <code>&lt;div&gt;</code> containing an <code>&lt;input&gt;</code> with <code>role=\"combobox\"</code> and a conditionally rendered <code>&lt;div&gt;</code> with <code>role=\"listbox\"</code>.</li>\n<li>Generates a unique <code>listboxId</code> using random string for the <code>aria-controls</code> relationship between input and listbox.</li>\n<li>The <code>value</code> prop uses two-way binding of the input text.</li>\n<li>The <code>open</code> prop uses two-way binding of dropdown visibility.</li>\n<li>The listbox is conditionally rendered with conditional rendering to hide it from the DOM when closed.</li>\n<li>Escape key closes the dropdown via an <code>onkeydown</code> handler on the input.</li>\n<li>Spreads <code>restProps</code> onto the outer wrapper <code>&lt;div&gt;</code>.</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name applied via <code>aria-label</code> on both the input and listbox.</li>\n<li><code>value</code>: string (default: <code>\"\"</code>) -- bindable current text input value.</li>\n<li><code>open</code>: boolean (default: <code>false</code>) -- bindable dropdown visibility state.</li>\n<li><code>children</code>: slot (required) -- option elements rendered inside the listbox dropdown.</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the wrapper <code>&lt;div&gt;</code>.</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Country selector with type-ahead filtering:</p>\n\n<pre><code class=\"language-html\">&lt;Combobox label=\"Select country\" value={country} open={countryOpen}&gt;\n    &lt;div role=\"option\" tabindex=\"-1\" onclick={() =&gt; { country = 'United Kingdom'; countryOpen = false; }}&gt;United Kingdom&lt;/div&gt;\n    &lt;div role=\"option\" tabindex=\"-1\" onclick={() =&gt; { country = 'United States'; countryOpen = false; }}&gt;United States&lt;/div&gt;\n    &lt;div role=\"option\" tabindex=\"-1\" onclick={() =&gt; { country = 'France'; countryOpen = false; }}&gt;France&lt;/div&gt;\n&lt;/Combobox&gt;</code></pre>\n\n<p>Medication picker with dynamic filtering:</p>\n\n<pre><code class=\"language-html\">&lt;Combobox label=\"Search medications\" value={medQuery} open={medOpen}&gt;\n    {#each filteredMedications as med}\n        &lt;div role=\"option\" tabindex=\"-1\" onclick={() =&gt; { medQuery = med.name; medOpen = false; }}&gt;\n            {med.name} ({med.dosage})\n        &lt;/div&gt;\n    {/each}\n&lt;/Combobox&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Escape: Closes the dropdown listbox.</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"combobox\"</code> -- on the <code>&lt;input&gt;</code>, identifies it as a combobox widget.</li>\n<li><code>aria-label</code> -- provides an accessible name for both the input and the listbox.</li>\n<li><code>aria-expanded</code> -- reflects the <code>open</code> state, telling screen readers whether the dropdown is visible.</li>\n<li><code>aria-controls</code> -- links the input to its associated listbox by ID.</li>\n<li><code>aria-autocomplete=\"list\"</code> -- indicates the input provides autocomplete suggestions via a list.</li>\n<li><code>role=\"listbox\"</code> -- on the dropdown container, identifies it as a list of selectable options.</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use Combobox when users need to filter and select from a large set of options by typing, such as search fields, address inputs, or tag selectors.</li>\n<li>Use Combobox when the option list is too long for a standard select dropdown and users benefit from type-ahead filtering.</li>\n<li>Use Combobox for autocomplete fields where the user types partial text and selects from matching suggestions.</li>\n<li>Use Combobox for medication or diagnosis pickers in healthcare applications where the option list may contain hundreds of entries.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use Combobox for short option lists (under 7 items) -- use RadioGroup or Select instead.</li>\n<li>Do not use Combobox for free-text entry without a predefined list of options -- use TextInput instead.</li>\n<li>Do not use Combobox for command palettes or action launchers -- use Command instead.</li>\n<li>Do not use Combobox for visible option lists that do not need a text filter -- use Listbox instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless Combobox component provides an <code>&lt;input&gt;</code> with <code>role=\"combobox\"</code>, <code>aria-expanded</code>, <code>aria-controls</code>, and <code>aria-autocomplete=\"list\"</code>, paired with a conditionally rendered <code>role=\"listbox\"</code> dropdown. The consumer provides all visual styling including input appearance, dropdown positioning, option highlighting, loading indicators, and animations.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.combobox</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>combobox</code></li>\n<li>Verify role=\"combobox\"<code> -- on the </code>&lt;input&gt;`, identifies it as a combobox widget.</li>\n<li>Verify aria-label` -- provides an accessible name for both the input and the listbox.</li>\n<li>Verify role=\"listbox\"` -- on the dropdown container, identifies it as a list of selectable options.</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Show a clear visual distinction between the input field and the dropdown list. Highlight the currently focused option and provide feedback when no results match.</li>\n<li><strong>Developers</strong>: Implement your own filtering logic by reacting to the bindable <code>value</code> prop. Provide <code>role=\"option\"</code> elements as children and manage selection state externally.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>autosuggest</code> — a text input that proposes matching options as users type</li>\n<li><code>select</code> — a dropdown select element for choosing one option</li>\n<li><code>cascader</code> — a multi-level dropdown for selecting a value from a hierarchy</li>\n<li><code>tree-select</code> — a select dropdown showing a tree of hierarchical options</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Combobox Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/combobox/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"combobox\"><input type=\"text\" role=\"combobox\" aria-expanded=\"false\" aria-label=\"Search\" /><ul role=\"listbox\" hidden><li role=\"option\">Apple</li><li role=\"option\">Banana</li></ul></div>";
  const svelteSource: string = "// In your Svelte component:\nimport Combobox from \"lily-design-system-svelte-headless/components/Combobox/Combobox.svelte\";\n\n<Combobox>\n  <!-- content -->\n</Combobox>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Combobox — Lily Design System</title>
  <meta name="description" content="A combobox combines a text input with a dropdown listbox of suggestions, providing an autocomplete or type-ahead search experience. Users type into the input to filter suggestions, then select from..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

