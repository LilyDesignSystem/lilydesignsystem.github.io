<script lang="ts">
  const html: string = "<h1>Validation List Item</h1>\n\n<p>A validation list item is one rule inside a <code>validation-list</code>, with a status of <code>pending</code>, <code>passed</code>, or <code>failed</code> exposed via a <code>data-status</code> attribute for consumer styling.</p>\n\n<p>Use it inside <code>ValidationList</code> to represent each individual validation rule that the user must satisfy.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a native <code>&lt;li&gt;</code> element with class <code>validation-list-item</code></li>\n<li><code>status</code> prop is one of <code>\"pending\"</code>, <code>\"passed\"</code>, or <code>\"failed\"</code> (default <code>\"pending\"</code>)</li>\n<li><code>status</code> is rendered as a <code>data-status</code> attribute on the <code>&lt;li&gt;</code> for consumer styling</li>\n<li><code>label</code> prop is optional — when supplied it sets <code>aria-label</code> (overriding the children text as the accessible name)</li>\n<li>Children render the rule text (e.g., \"At least 8 characters\")</li>\n<li>Pass-through attributes are forwarded to the root <code>&lt;li&gt;</code></li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>This component is part of the <code>*List</code> <code>*ListItem</code> composition pattern. It must be a child of <code>ValidationList</code>.</p>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>status</code>: <code>\"pending\" | \"passed\" | \"failed\"</code> (default: <code>\"pending\"</code>)</li>\n<li><code>label</code>: string (optional) -- aria-label override</li>\n<li><code>children</code>: slot -- rule text (e.g., \"At least 8 characters\")</li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Pending rule:</p>\n\n<pre><code class=\"language-html\">&lt;ValidationListItem status=\"pending\"&gt;At least 8 characters&lt;/ValidationListItem&gt;</code></pre>\n\n<p>Passed rule:</p>\n\n<pre><code class=\"language-html\">&lt;ValidationListItem status=\"passed\"&gt;At least one number&lt;/ValidationListItem&gt;</code></pre>\n\n<p>Failed rule with screen-reader override:</p>\n\n<pre><code class=\"language-html\">&lt;ValidationListItem status=\"failed\" label=\"Symbol requirement not met\"&gt;\n  At least one symbol\n&lt;/ValidationListItem&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>None — the item is not interactive</li>\n<li>Tab moves focus past the item</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Implicit <code>listitem</code> role from <code>&lt;li&gt;</code> element</li>\n<li><code>aria-label</code> is rendered when <code>label</code> is supplied</li>\n<li><code>data-status</code> exposes the rule state for consumer styling and consumer JS</li>\n<li>Status changes are announced through the parent <code>validation-list</code>'s <code>aria-live=\"polite\"</code></li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use as a child of <code>ValidationList</code> to represent one validation rule.</li>\n<li>Use to expose pending / passed / failed state via <code>data-status</code> for visual treatment.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside a <code>ValidationList</code>.</li>\n<li>Do not use as a generic checklist item — use <code>CheckListItem</code> instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>Renders only the <code>&lt;li&gt;</code> and its <code>data-status</code>. The status icon (dot, check, cross) and color treatment are entirely the consumer's responsibility, driven by <code>[data-status=\"pending\"]</code>, <code>[data-status=\"passed\"]</code>, and <code>[data-status=\"failed\"]</code> selectors.</p>\n\n<h2>Styles</h2>\n\n<p>Consumer CSS targets the <code>validation-list-item</code> class with <code>[data-status]</code> attribute selectors:</p>\n\n<pre><code class=\"language-css\">.validation-list-item[data-status=\"pending\"] { /* pending styles */ }\n.validation-list-item[data-status=\"passed\"] { /* passed styles */ }\n.validation-list-item[data-status=\"failed\"] { /* failed styles */ }</code></pre>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders an <code>&lt;li&gt;</code> element with class <code>validation-list-item</code></li>\n<li>Verify <code>data-status</code> defaults to <code>\"pending\"</code></li>\n<li>Verify <code>data-status</code> reflects the <code>status</code> prop</li>\n<li>Verify <code>aria-label</code> is rendered when <code>label</code> is supplied</li>\n<li>Verify children render inside the item</li>\n<li>Verify additional HTML attributes pass through to the root <code>&lt;li&gt;</code></li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use color plus an icon to signal each status — never color alone. Pending should look passive (gray dot), passed should look successful (green check), failed should look corrective (red cross).</li>\n<li><strong>Developers</strong>: Update the <code>status</code> prop reactively as the user types. The parent <code>ValidationList</code>'s <code>aria-live=\"polite\"</code> ensures the change is announced.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>validation-list</code> — a live-feedback list of input validation rules with pending, passed, and failed states</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>US Web Design System Validation: https://designsystem.digital.gov/components/validation/</li>\n<li>HTML <code>&lt;li&gt;</code>: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/li</li>\n<li>WCAG 1.4.1 Use of Color: https://www.w3.org/WAI/WCAG22/Understanding/use-of-color</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"validation-list-item\" aria-label=\"one validation rule in a validation list with a status of pending, passed, or failed\">ValidationListItem</div>";
  const svelteSource: string = "// In your Svelte component:\nimport ValidationListItem from \"lily-design-system-svelte-headless/components/ValidationListItem/ValidationListItem.svelte\";\n\n<ValidationListItem>\n  <!-- ValidationListItem content -->\n</ValidationListItem>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Validation List Item — Lily Design System</title>
  <meta name="description" content="A validation list item is one rule inside a validation-list, with a status of pending, passed, or failed exposed via a data-status attribute for consumer styling." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

