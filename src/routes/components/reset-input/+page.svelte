<script lang="ts">
  const html: string = "<h1>Reset Input</h1>\n\n<p>A reset input is a UI/UX component that provides a form reset button using the native HTML <code>&lt;input type=\"reset\"&gt;</code> element. When clicked within a form, it resets all form fields to their initial values. The component supports custom button text through the value prop and can be disabled when form resetting should be prevented.</p>\n\n<p>Reset inputs are useful in forms where users may want to clear all entered data and start over. They rely on the browser's built-in form reset behavior, which restores each field to its default value attribute.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a single <code>&lt;input type=\"reset\"&gt;</code> element for native form reset behavior</li>\n<li>The browser automatically handles resetting all sibling form fields when clicked</li>\n<li>Default button text is \"Reset\", configurable via the <code>value</code> prop</li>\n<li>Supports rest props via <code>...restProps</code> for extensibility (e.g., <code>id</code>, <code>name</code>, <code>class</code>)</li>\n<li>No ARIA attributes needed beyond the native semantics of <code>&lt;input type=\"reset\"&gt;</code></li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>value</code>: string (default: \"Reset\") -- the visible button text</li>\n<li><code>disabled</code>: boolean (default: false) -- whether the button is disabled</li>\n<li><code>...restProps</code>: spread onto the <code>&lt;input&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Form with submit and reset actions:</p>\n\n<pre><code class=\"language-html\">&lt;Form label=\"Registration\" onsubmit={handleSubmit}&gt;\n  &lt;Field label=\"Name\" required&gt;\n    &lt;TextInput label=\"Name\" value={name} required /&gt;\n  &lt;/Field&gt;\n  &lt;Field label=\"Email\" required&gt;\n    &lt;EmailInput label=\"Email\" value={email} required /&gt;\n  &lt;/Field&gt;\n  &lt;SubmitInput value=\"Register\" /&gt;\n  &lt;ResetInput value=\"Clear form\" disabled={!formDirty} /&gt;\n&lt;/Form&gt;</code></pre>\n\n<p>Simple reset with default label:</p>\n\n<pre><code class=\"language-html\">&lt;ResetInput /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Enter: activates the reset button</li>\n<li>Space: activates the reset button</li>\n</ul>\n\n<p>Note: keyboard interactions are provided natively by the <code>&lt;input type=\"reset\"&gt;</code> element.</p>\n\n<h2>ARIA</h2>\n\n<p>No additional ARIA attributes are needed. The native <code>&lt;input type=\"reset\"&gt;</code> element has an implicit <code>button</code> role and the <code>value</code> attribute serves as its accessible name.</p>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to provide a form reset button that clears all fields to their default values.</li>\n<li>Use sparingly and only when users genuinely need to start over.</li>\n<li>Use alongside SubmitInput to provide a complete set of form actions.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use as the primary action -- users rarely need to reset; use Button with <code>type=\"submit\"</code> for the main action.</li>\n<li>Do not use when data loss would be harmful -- consider a confirmation step via AlertDialog first.</li>\n<li>Do not use on forms with many fields or long workflows, as accidental resets can frustrate users.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>The ResetInput headless component provides a native <code>&lt;input type=\"reset\"&gt;</code> with configurable button text via the <code>value</code> prop and <code>disabled</code> state. The browser handles all form reset behavior natively. The consumer provides all visual styling for the button appearance.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.reset-input</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;input&gt;</code> element with class <code>reset-input</code></li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Style the reset button with less visual emphasis than the submit button to reduce accidental clicks, and position it away from the primary action.</li>\n<li><strong>Developers</strong>: Use the <code>disabled</code> prop to disable reset when the form is in its initial state (not dirty), preventing unnecessary resets.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>submit-input</code> — a button input that submits a form &lt;input type=\"submit\"&gt;</li>\n<li><code>button-input</code> — an input element of type button for form actions &lt;input type=\"button\"&gt;</li>\n<li><code>form</code> — a form element for collecting and submitting user data</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN input type=\"reset\": https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/reset</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<input class=\"reset-input\" type=\"reset\" value=\"Reset\" />";
  const svelteSource: string = "// In your Svelte component:\nimport ResetInput from \"lily-design-system-svelte-headless/components/ResetInput/ResetInput.svelte\";\n\n<ResetInput label=\"Label\" />\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Reset Input — Lily Design System</title>
  <meta name="description" content="A reset input is a UI/UX component that provides a form reset button using the native HTML &lt;input type=&quot;reset&quot;&gt; element. When clicked within a form, it resets all form fields to their initial value..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

