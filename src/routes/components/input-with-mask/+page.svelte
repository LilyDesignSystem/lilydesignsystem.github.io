<script lang="ts">
  const html: string = "<h1>Input With Mask</h1>\n\n<p>An input with mask is a <code>&lt;div&gt;</code> wrapper around a text <code>&lt;input&gt;</code> and a decorative format-mask display (e.g., <code>(___) ___-____</code>) that overlays the input to show the user where to type each character.</p>\n\n<p>Use it for structured fields such as phone numbers, dates, or document identifiers that have a predictable character pattern.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a wrapping <code>&lt;div&gt;</code> with class <code>input-with-mask</code> and <code>data-mask</code> attribute</li>\n<li><code>label</code> prop is <strong>required</strong> — it sets <code>aria-label</code> on the <code>&lt;input&gt;</code></li>\n<li><code>mask</code> prop is <strong>required</strong> — the format string displayed to the user</li>\n<li>The mask display renders as <code>&lt;span class=\"input-with-mask-display\" aria-hidden=\"true\"&gt;</code></li>\n<li>The actual input renders as <code>&lt;input class=\"input-with-mask-control\" type=\"text\"&gt;</code></li>\n<li>The mask display is decorative; the input's <code>aria-label</code> is the accessible name</li>\n<li><code>value</code>, <code>placeholder</code>, <code>disabled</code> are forwarded to the input</li>\n<li><code>onInput</code> / <code>oninput</code> callback fires on input change</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (<strong>required</strong>) -- aria-label for the input</li>\n<li><code>mask</code>: string (<strong>required</strong>) -- format mask (e.g., <code>(___) ___-____</code>)</li>\n<li><code>value</code>: string (default: \"\") -- input value</li>\n<li><code>placeholder</code>: string (optional) -- input placeholder</li>\n<li><code>disabled</code>: boolean (default: false) -- disabled state</li>\n<li><code>onInput</code> / <code>oninput</code>: callback (optional) -- input change handler</li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>US phone number mask:</p>\n\n<pre><code class=\"language-html\">&lt;InputWithMask\n  label=\"Phone number\"\n  mask=\"(___) ___-____\"\n  value={phone}\n  oninput={(e) =&gt; phone = e.target.value}\n/&gt;</code></pre>\n\n<p>Date mask:</p>\n\n<pre><code class=\"language-html\">&lt;InputWithMask\n  label=\"Date of birth\"\n  mask=\"MM/DD/YYYY\"\n  value={dob}\n/&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: focus the input</li>\n<li>All standard text-input keyboard handling is delegated to the native <code>&lt;input&gt;</code></li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>The input has the required <code>aria-label</code> from the <code>label</code> prop</li>\n<li>The mask display has <code>aria-hidden=\"true\"</code> so screen readers do not announce the placeholder pattern</li>\n<li>The wrapping <code>&lt;div&gt;</code> has a <code>data-mask</code> attribute for consumer styling</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for phone numbers, social security numbers, dates, and similar structured fields.</li>\n<li>Use when the format is predictable and showing the pattern helps users enter values correctly.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for free-form text — use a plain <code>TextInput</code> instead.</li>\n<li>Do not use for sensitive masked content (passwords) — use <code>PasswordInput</code>.</li>\n<li>Do not use as a substitute for proper input types (<code>&lt;input type=\"tel\"&gt;</code>, <code>&lt;input type=\"date\"&gt;</code>) when those are appropriate.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>Renders the structural skeleton — <code>&lt;div&gt;</code> with <code>&lt;span&gt;</code> mask display and <code>&lt;input&gt;</code> control. The visual overlay technique that aligns the mask with the input characters is entirely the consumer's responsibility.</p>\n\n<h2>Styles</h2>\n\n<p>Consumer CSS targets the <code>input-with-mask</code> class plus the inner <code>input-with-mask-display</code> and <code>input-with-mask-control</code> classes. Typical treatments use absolute positioning to overlay the mask on the input, with monospace typography so the characters align.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> with class <code>input-with-mask</code></li>\n<li>Verify the <code>data-mask</code> attribute equals the <code>mask</code> prop</li>\n<li>Verify the mask display renders inside <code>&lt;span class=\"input-with-mask-display\" aria-hidden=\"true\"&gt;</code></li>\n<li>Verify the mask display text equals the <code>mask</code> prop</li>\n<li>Verify the input has class <code>input-with-mask-control</code></li>\n<li>Verify the input has type <code>text</code></li>\n<li>Verify the input has <code>aria-label</code> equal to the <code>label</code> prop</li>\n<li>Verify the input value reflects the <code>value</code> prop</li>\n<li>Verify the input is disabled when <code>disabled</code> is true</li>\n<li>Verify the input handler fires on input</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use a monospace font for the input so the mask and the typed characters align. Choose a low-contrast color for the mask so it does not compete with the user's input.</li>\n<li><strong>Developers</strong>: The mask display is purely visual. The input value remains a plain string — do not parse the mask characters into the value. Use a separate validation component (e.g., <code>ValidationList</code>) to verify the format.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>text-input</code> — a single-line text input field &lt;input type=\"text\"&gt;</li>\n<li><code>pin-input-div</code> — a series of single-digit inputs for entering a PIN or OTP code</li>\n<li><code>input</code> — a generic HTML input element</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>US Web Design System Input Mask: https://designsystem.digital.gov/components/input-mask/</li>\n<li>HTML <code>&lt;input type=\"text\"&gt;</code>: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/text</li>\n<li>WAI-ARIA aria-hidden: https://www.w3.org/TR/wai-aria-1.2/#aria-hidden</li>\n<li>WCAG 1.3.5 Identify Input Purpose: https://www.w3.org/WAI/WCAG22/Understanding/identify-input-purpose</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"input-with-mask\" aria-label=\"an input displaying a format mask placeholder for the user to fill in\">InputWithMask</div>";
  const svelteSource: string = "// In your Svelte component:\nimport InputWithMask from \"lily-design-system-svelte-headless/components/InputWithMask/InputWithMask.svelte\";\n\n<InputWithMask>\n  <!-- content -->\n</InputWithMask>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Input With Mask — Lily Design System</title>
  <meta name="description" content="An input with mask is a &lt;div&gt; wrapper around a text &lt;input&gt; and a decorative format-mask display (e.g., (___) ___-____) that overlays the input to show the user where to type each character." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

