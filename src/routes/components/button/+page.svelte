<script lang="ts">
  const html: string = "<h1>Button</h1>\n\n<p>A button is a fundamental UI/UX component that allows users to trigger an action or event within an interface, such as submitting a form, navigating to another page, or opening a menu. Buttons are essential for guiding users through tasks, providing clear feedback on actions, and enhancing the overall user experience by offering intuitive, actionable controls.</p>\n\n<p>This headless component uses the native HTML <code>&lt;button&gt;</code> element for inherent accessibility and keyboard support. It supports toggle button semantics via the <code>pressed</code> prop, accessible label overrides, and all standard button types.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses native <code>&lt;button&gt;</code> element for inherent keyboard and accessibility support</li>\n<li>Defaults to <code>type=\"button\"</code> (not \"submit\") to prevent accidental form submissions</li>\n<li><code>aria-pressed</code> only rendered when <code>pressed</code> prop is provided (toggle button pattern)</li>\n<li><code>aria-label</code> provides screen reader override when button text alone is insufficient</li>\n<li>Native disabled attribute prevents click events automatically</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>type</code>: \"button\" | \"submit\" | \"reset\" (default: \"button\")</li>\n<li><code>disabled</code>: boolean (default: false)</li>\n<li><code>pressed</code>: boolean | undefined (default: undefined) -- toggle button state</li>\n<li><code>label</code>: string (optional) -- accessible label override</li>\n<li><code>onclick</code>: callback (optional) -- click handler</li>\n<li><code>children</code>: slot -- button content</li>\n<li><code>...restProps</code>: Any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Submit button for saving progress:</p>\n\n<pre><code class=\"language-html\">&lt;Button type=\"submit\"&gt;Save and continue&lt;/Button&gt;</code></pre>\n\n<p>Action button with disabled state:</p>\n\n<pre><code class=\"language-html\">&lt;Button onclick={handleSave} disabled={isSaving}&gt;\n  Save your progress\n&lt;/Button&gt;</code></pre>\n\n<p>Toggle button with pressed state:</p>\n\n<pre><code class=\"language-html\">&lt;Button pressed={isBold} onclick={() =&gt; isBold = !isBold}&gt;\n  Bold\n&lt;/Button&gt;</code></pre>\n\n<p>With accessible label override:</p>\n\n<pre><code class=\"language-html\">&lt;Button label=\"Close notification\" onclick={handleDismiss}&gt;\n  ✕\n&lt;/Button&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus the button</li>\n<li>Enter: Activate the button</li>\n<li>Space: Activate the button</li>\n<li>(All handled natively by <code>&lt;button&gt;</code> element)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Implicit <code>button</code> role from <code>&lt;button&gt;</code> element</li>\n<li><code>aria-pressed</code> for toggle button state (when provided)</li>\n<li><code>aria-label</code> for screen reader text override</li>\n<li><code>aria-disabled</code> from native disabled attribute</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for triggering actions such as form submission, saving progress, opening a dialog, or confirming a choice.</li>\n<li>Use a single primary button per page to give users a clear next step.</li>\n<li>Use when the action changes state or submits data -- buttons signal \"do something\", not \"go somewhere\".</li>\n<li>Use the toggle button pattern (with <code>pressed</code> prop) for features like bold/italic formatting toggles.</li>\n<li>Use when you need rich content inside the button (icons, formatted text) via children slots.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for navigation between pages -- use ActionLink or a standard anchor link instead.</li>\n<li>Do not use for linking to external websites -- use a standard anchor link.</li>\n<li>Do not use multiple primary buttons on one page -- use one primary and secondary Button variants.</li>\n<li>Do not use when you need a form-native <code>&lt;input type=\"button\"&gt;</code> -- use ButtonInput instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides a native <code>&lt;button&gt;</code> element with optional <code>aria-pressed</code> for toggle state, <code>aria-label</code> override, and standard button types, all with zero visual styling. The consumer is responsible for all CSS including colors, padding, border, border-radius, hover states, focus indicators, disabled appearance, and icon layout.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.button</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;button&gt;</code> element with class <code>button</code></li>\n<li>Verify aria-label` for screen reader text override</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Maintain a minimum 44x44px touch target. Use visual hierarchy (primary, secondary, ghost) to distinguish button importance. Ensure disabled buttons have sufficient contrast.</li>\n<li><strong>Developers</strong>: The default type is <code>\"button\"</code> (not <code>\"submit\"</code>) to prevent accidental form submissions. Only provide the <code>pressed</code> prop when implementing a toggle button pattern.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>button-group</code> — a wrapper that groups related buttons together</li>\n<li><code>button-input</code> — an input element of type button for form actions &lt;input type=\"button\"&gt;</li>\n<li><code>icon-button</code> — a button containing only an icon with a required accessible label</li>\n<li><code>split-button</code> — a button with a primary action and a dropdown for related actions</li>\n<li><code>toggle-button</code> — a button that toggles between pressed and unpressed states</li>\n<li><code>float-button</code> — a floating action button anchored to a viewport corner</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Button Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/button/</li>\n</ul>";
</script>

<svelte:head>
  <title>Button — Lily Design System</title>
  <meta name="description" content="A button is a fundamental UI/UX component that allows users to trigger an action or event within an interface, such as submitting a form, navigating to another page, or opening a menu. Buttons are ..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
