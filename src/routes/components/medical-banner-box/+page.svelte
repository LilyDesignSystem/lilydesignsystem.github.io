<script lang="ts">
  const html: string = "<h1>Medical Banner Box</h1>\n\n<p>MedicalBannerBox is a headless layout component designed to be placed inside a MedicalBanner component. It renders a <code>&lt;div&gt;</code> with <code>data-context=\"medical\"</code> that the consumer styles with flexbox horizontal layout to arrange medical banner content items side by side, such as patient details, identifiers, and action buttons.</p>\n\n<p>This component is useful for structuring medical banner content into a horizontal row, aligning patient information, clinical data, and action buttons within a medical banner message bar.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> element with class <code>medical-banner-box</code></li>\n<li>Intended to be placed inside a MedicalBanner component</li>\n<li>Uses <code>data-context=\"medical\"</code> for consumer CSS targeting</li>\n<li>Consumer applies flexbox horizontal styles via the <code>.medical-banner-box</code> CSS class</li>\n<li>Uses <code>children</code> slot for flexible content rendering</li>\n<li>Spreads <code>restProps</code> onto the <code>&lt;div&gt;</code> element for consumer extensibility</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (optional) -- accessible name applied via <code>aria-label</code> if needed</li>\n<li><code>children</code>: slot (required) -- the medical banner box content</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;div&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;MedicalBanner label=\"Patient summary\"&gt;\n  &lt;MedicalBannerBox&gt;\n    &lt;strong&gt;John Smith&lt;/strong&gt;\n    &lt;span&gt;DOB: 15/03/1985&lt;/span&gt;\n    &lt;span&gt;NHS: 123 456 7890&lt;/span&gt;\n    &lt;span&gt;Ward: Cardiology B&lt;/span&gt;\n  &lt;/MedicalBannerBox&gt;\n&lt;/MedicalBanner&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;MedicalBanner label=\"Patient record\"&gt;\n  &lt;MedicalBannerBox&gt;\n    &lt;strong&gt;Emma Taylor&lt;/strong&gt;\n    &lt;span&gt;IHI: 9012 3456 7890 1234&lt;/span&gt;\n    &lt;span&gt;DOB: 22/07/1972&lt;/span&gt;\n  &lt;/MedicalBannerBox&gt;\n  &lt;MedicalBannerBoxForDanger label=\"Allergies\"&gt;\n    Penicillin -- Anaphylaxis\n  &lt;/MedicalBannerBoxForDanger&gt;\n&lt;/MedicalBanner&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>None (passive layout element)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>No additional ARIA attributes required; the parent MedicalBanner provides the landmark role and aria-live region</li>\n<li>Optional <code>aria-label</code> via <code>label</code> prop if the box needs an accessible name</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>MedicalBannerBox is a child of MedicalBanner:</p>\n\n<pre><code>MedicalBanner\n  └── MedicalBannerBox (flexbox horizontal row)\n        ├── patient details\n        ├── clinical identifiers\n        └── action buttons</code></pre>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside MedicalBanner to arrange medical information horizontally using flexbox</li>\n<li>Use to display patient demographics, identifiers, and summary data in a single row</li>\n<li>Use to group related clinical details such as name, date of birth, and NHS number side by side</li>\n<li>Use when the MedicalBanner needs a consistent horizontal layout for its primary content</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside MedicalBanner -- use BannerBox for standard banners instead</li>\n<li>Do not use for danger-level information such as allergies -- use MedicalBannerBoxForDanger</li>\n<li>Do not use for advice-level information such as care contacts -- use MedicalBannerBoxForAdvice</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;div&gt;</code> with <code>.medical-banner-box</code> class and <code>data-context=\"medical\"</code> for consumer CSS targeting. The consumer is responsible for all flexbox styling.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.medical-banner-box</code> class for targeting. Typical consumer styles include <code>display: flex; flex-direction: row; align-items: center; gap: 1rem;</code>. No default styles are included -- this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>medical-banner-box</code></li>\n<li>Verify <code>data-context=\"medical\"</code> is present</li>\n<li>Verify it renders children content</li>\n<li>Verify pass-through attributes are applied to the <code>&lt;div&gt;</code></li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use MedicalBannerBox to create horizontal layouts within medical banners for patient details. Consider responsive behavior for narrow screens.</li>\n<li><strong>Developers</strong>: Apply <code>display: flex; flex-direction: row;</code> to <code>.medical-banner-box</code> in your CSS.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>announcement-banner</code> — a banner highlighting important messages for all users</li>\n<li><code>banner</code> — a prominent message bar across the top of a page</li>\n<li><code>banner-box</code> — a banner box that is inside a banner component, using flexbox horizontal</li>\n<li><code>government-banner</code> — a banner identifying a website as belonging to a government, with an expandable details panel</li>\n<li><code>medical-banner</code> — a prominent message bar across the top of a page, with medical information</li>\n<li><code>medical-banner-box-for-advice</code> — a medical record banner box for advice information e.g. contacts, contexts, plans, etc.</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>Parent component: MedicalBanner</li>\n<li>CSS Flexbox: https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_flexible_box_layout</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"medical-banner-box\" aria-label=\"a medical banner box that is inside a medical-banner component, using flexbox horizontal, with medical information\">MedicalBannerBox</div>";
  const svelteSource: string = "// In your Svelte component:\nimport MedicalBannerBox from \"lily-design-system-svelte-headless/components/MedicalBannerBox/MedicalBannerBox.svelte\";\n\n<MedicalBannerBox>\n  <!-- content -->\n</MedicalBannerBox>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Medical Banner Box — Lily Design System</title>
  <meta name="description" content="MedicalBannerBox is a headless layout component designed to be placed inside a MedicalBanner component. It renders a &lt;div&gt; with data-context=&quot;medical&quot; that the consumer styles with flexbox horizont..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

