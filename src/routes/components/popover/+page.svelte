<script lang="ts">
  const html: string = "<h1>Popover</h1>\n\n<p>A popover is a UI component that displays contextual information or actions in an overlay that appears near the element that triggered it. The Popover component provides a headless conditional dialog container that renders its content only when the <code>open</code> state is true, with proper dialog semantics for accessibility.</p>\n\n<p>Unlike tooltips, popovers can contain rich interactive content such as text, forms, buttons, or other controls. The component supports two-way binding on the <code>open</code> prop, allowing parent components to control visibility. The consumer is responsible for providing trigger elements and managing open/close logic.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Conditionally renders a <code>&lt;div&gt;</code> with <code>role=\"dialog\"</code> only when <code>open</code> is true</li>\n<li>Supports two-way binding on the <code>open</code> prop</li>\n<li>The <code>label</code> prop provides the dialog's accessible name via <code>aria-label</code></li>\n<li>Rest props are spread onto the <code>&lt;div&gt;</code> element</li>\n<li>The consumer must provide their own trigger element and open/close logic</li>\n<li>Content is provided via the <code>children</code> slot</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name for the popover dialog via <code>aria-label</code></li>\n<li><code>open</code>: boolean (default: false, bindable) -- controls whether the popover is visible; supports two-way binding</li>\n<li><code>children</code>: slot (required) -- the popover content</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;div&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Basic popover with a toggle trigger:</p>\n\n<pre><code class=\"language-html\">&lt;button onclick={() =&gt; showPopover = !showPopover}&gt;Toggle info&lt;/button&gt;\n&lt;Popover label=\"Additional information\" open={showPopover}&gt;\n  &lt;p&gt;Here is some contextual information.&lt;/p&gt;\n  &lt;button onclick={() =&gt; showPopover = false}&gt;Close&lt;/button&gt;\n&lt;/Popover&gt;</code></pre>\n\n<p>Popover with an inline edit form:</p>\n\n<pre><code class=\"language-html\">&lt;button onclick={() =&gt; editOpen = !editOpen}&gt;Edit name&lt;/button&gt;\n&lt;Popover label=\"Edit display name\" open={editOpen}&gt;\n  &lt;form onsubmit={(e) =&gt; { e.preventDefault(); saveName(); editOpen = false; }}&gt;\n    &lt;label for=\"display-name\"&gt;Display name&lt;/label&gt;\n    &lt;input type=\"text\" id=\"display-name\" value={name} /&gt;\n    &lt;button type=\"submit\"&gt;Save&lt;/button&gt;\n    &lt;button type=\"button\" onclick={() =&gt; editOpen = false}&gt;Cancel&lt;/button&gt;\n  &lt;/form&gt;\n&lt;/Popover&gt;</code></pre>\n\n<p>Popover for a filter panel:</p>\n\n<pre><code class=\"language-html\">&lt;button onclick={() =&gt; filterOpen = !filterOpen}&gt;Filter results&lt;/button&gt;\n&lt;Popover label=\"Filter options\" open={filterOpen}&gt;\n  &lt;fieldset&gt;\n    &lt;legend&gt;Status&lt;/legend&gt;\n    &lt;label&gt;&lt;input type=\"checkbox\" value=\"active\" /&gt; Active&lt;/label&gt;\n    &lt;label&gt;&lt;input type=\"checkbox\" value=\"archived\" /&gt; Archived&lt;/label&gt;\n  &lt;/fieldset&gt;\n  &lt;button onclick={() =&gt; { applyFilters(); filterOpen = false; }}&gt;Apply&lt;/button&gt;\n&lt;/Popover&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Escape: Consumer should implement closing the popover by setting <code>open</code> to false</li>\n<li>Tab: Should trap focus within the popover when open (consumer responsibility)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"dialog\"</code> -- identifies the popover as a dialog overlay for assistive technology</li>\n<li><code>aria-label=\"...\"</code> -- provides the accessible name for the dialog</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use Popover to display rich interactive content like forms, menus, or detailed information near a trigger element.</li>\n<li>Use Popover when the overlay content needs to persist until the user explicitly dismisses it.</li>\n<li>Use Popover for inline editing interfaces, such as editing a profile field without leaving the page.</li>\n<li>Use Popover for filter panels or mini-forms that relate to a specific trigger element.</li>\n<li>Use Popover for contextual actions that need more space than a menu item label.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for simple descriptive text on hover or focus -- use Tooltip instead.</li>\n<li>Do not use for content requiring a full modal with backdrop overlay -- use Dialog or AlertDialog instead.</li>\n<li>Do not use for preview content triggered by hover -- use HoverCard instead.</li>\n<li>Do not use for navigation menus -- use DropdownMenu or NavigationMenu instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>The Popover headless component provides a conditionally rendered <code>&lt;div&gt;</code> with <code>role=\"dialog\"</code> and <code>aria-label</code> for accessible naming. It handles open/close state via two-way binding. The consumer provides all visual styling, positioning logic, trigger elements, and focus management.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.popover</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>popover</code></li>\n<li>Verify role=\"dialog\"` -- identifies the popover as a dialog overlay for assistive technology</li>\n<li>Verify aria-label=\"...\"` -- provides the accessible name for the dialog</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Position popovers to avoid obscuring the trigger element, and ensure the popover has a clear visual boundary (border or shadow) to distinguish it from surrounding content.</li>\n<li><strong>Developers</strong>: Implement focus trapping within the popover when open, and close it on Escape key press to follow the WAI-ARIA dialog pattern.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>popup</code> — a temporary overlay that appears above page content</li>\n<li><code>hover-card</code> — a card that appears on hover over a trigger element</li>\n<li><code>tooltip</code> — a small popup showing descriptive text on hover or focus</li>\n<li><code>popconfirm-dialog</code> — a popover dialog with confirm and cancel buttons</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Dialog Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/dialog/</li>\n<li>WAI-ARIA dialog role: https://www.w3.org/TR/wai-aria-1.2/#dialog</li>\n</ul>";
</script>

<svelte:head>
  <title>Popover — Lily Design System</title>
  <meta name="description" content="A popover is a UI component that displays contextual information or actions in an overlay that appears near the element that triggered it. The Popover component provides a headless conditional dial..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
