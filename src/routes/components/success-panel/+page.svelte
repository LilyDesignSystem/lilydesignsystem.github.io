<script lang="ts">
  const html: string = "<h1>SuccessPanel</h1>\n\n<p>The dominant element on a confirmation page — a prominent panel that says, in plain language, that a multi-step task has finished, and (optionally) shows a reference number and what happens next.</p>\n\n<p>The component is built around a configurable heading (defaulting to <code>&lt;h1&gt;</code> because the confirmation panel is normally the page's main heading) and a body slot for follow-up information: confirmation reference, what to expect next, links to related services, contact details. The wrapping <code>&lt;div&gt;</code> is exposed as <code>role=\"alert\"</code> with <code>aria-live=\"polite\"</code> so screen readers announce the heading when the page loads, even if focus has not yet been moved into it.</p>\n\n<p>This is heavier than <code>Alert</code> (which is for inline status inside a longer page) and <code>Notification</code> / <code>Toast</code> (which are transient). Reach for <code>SuccessPanel</code> only at the end of a discrete task — form submission, payment, booking, account creation — when the page changes role from \"perform task\" to \"confirm task complete\".</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div role=\"alert\" aria-live=\"polite\"&gt;</code> so completion is announced to assistive technology</li>\n<li>Contains a heading (configurable level) and a message body slot</li>\n<li>Optionally renders a follow-up actions area (links/buttons) inside the panel</li>\n<li>Best placed at the top of the post-submission page, replacing the form</li>\n<li>Spreads <code>restProps</code> onto the root <code>&lt;div&gt;</code></li>\n</ul>\n\n<h2>Props</h2>\n\n<table>\n<thead>\n<tr><th>Prop</th><th>Type</th><th>Default</th><th>Description</th></tr>\n</thead>\n<tbody>\n<tr><td><code>title</code></td><td>string (required)</td><td>—</td><td>Confirmation headline</td></tr>\n<tr><td><code>headingLevel</code></td><td>1</td><td>2</td><td>3</td><td>4</td><td>5</td><td>6</td><td>1</td><td>Heading level used for <code>title</code></td></tr>\n<tr><td><code>reference</code></td><td>string</td><td>—</td><td>Optional reference number shown beneath the title</td></tr>\n<tr><td><code>children</code></td><td>slot</td><td>—</td><td>Body content (next steps, links, etc.)</td></tr>\n<tr><td><code>...restProps</code></td><td>HTML attributes</td><td>—</td><td>Spread onto the root <code>&lt;div&gt;</code></td></tr>\n</tbody>\n</table>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;SuccessPanel title=\"Application complete\" reference=\"HDJ2123F\"&gt;\n  &lt;p&gt;We have sent you a confirmation email.&lt;/p&gt;\n&lt;/SuccessPanel&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>No keyboard interactions on the panel itself</li>\n<li>Focus is moved to the panel by the consumer when navigating to the confirmation page so screen readers announce it immediately</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"alert\"</code> exposes the panel as an alert region</li>\n<li><code>aria-live=\"polite\"</code> ensures the heading and body are announced when the panel renders</li>\n<li>Heading level is configurable via the <code>headingLevel</code> prop (default <code>&lt;h1&gt;</code> — confirmation pages typically use the panel as the page's main heading)</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Confirming completion of a multi-step transaction (form submission, payment, booking)</li>\n<li>Communicating that a destructive or irreversible action succeeded</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Use <code>Alert</code> for inline success messages within a longer page</li>\n<li>Use <code>Notification</code> or <code>Toast</code> for transient confirmations</li>\n<li>Use <code>Banner</code> for non-task-confirmation announcements</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component renders semantic HTML with appropriate ARIA wiring. The consumer provides all visual styling — no CSS, animations, or layout assumptions are baked in.</p>\n\n<h2>Styles</h2>\n\n<p>The component renders with <code>.success-panel</code> as the root class. No default styles are included.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>alert</code> — inline status messaging</li>\n<li><code>notification</code> — transient confirmation</li>\n<li><code>info-state</code> — empty/error/info state composition</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li><a href=\"https://design-system.service.gov.uk/patterns/confirmation-pages/\">GOV.UK Confirmation pages pattern</a></li>\n<li><a href=\"https://www.w3.org/TR/wai-aria-1.2/#alert\">WAI-ARIA alert role</a></li>\n</ul>";
</script>

<svelte:head>
  <title>SuccessPanel — Lily Design System</title>
  <meta name="description" content="The dominant element on a confirmation page — a prominent panel that says, in plain language, that a multi-step task has finished, and (optionally) shows a reference number and what happens next." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
