<script lang="ts">
  const html: string = "<h1>Validation List</h1>\n\n<p>A validation list is a <code>&lt;ul&gt;</code> of <code>validation-list-item</code> rules that provides live feedback as the user types — typically next to a password field, a username field, or any input with multiple rules.</p>\n\n<p>Use it to give users instant, accessible confirmation as each requirement is met (or unmet) without waiting for form submission.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a native <code>&lt;ul&gt;</code> element with class <code>validation-list</code></li>\n<li><code>label</code> prop is <strong>required</strong> — sets <code>aria-label</code> describing the field the rules apply to</li>\n<li><code>aria-live=\"polite\"</code> is set on the list so status changes are announced</li>\n<li>Children are expected to be <code>validation-list-item</code> elements with status props</li>\n<li>The <code>&lt;ul&gt;</code> semantics provide list count and item position to screen readers</li>\n<li>Pass-through attributes are forwarded to the root <code>&lt;ul&gt;</code></li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>This component is part of the <code>*List</code> <code>*ListItem</code> composition pattern. Pair it with <code>ValidationListItem</code> children to render the rule set.</p>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (<strong>required</strong>) -- aria-label describing the field</li>\n<li><code>children</code>: slot -- <code>validation-list-item</code> children</li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Password requirements:</p>\n\n<pre><code class=\"language-html\">&lt;ValidationList label=\"Password requirements\"&gt;\n  &lt;ValidationListItem status={lengthOk ? \"passed\" : \"failed\"}&gt;\n    At least 8 characters\n  &lt;/ValidationListItem&gt;\n  &lt;ValidationListItem status={hasNumber ? \"passed\" : \"failed\"}&gt;\n    At least one number\n  &lt;/ValidationListItem&gt;\n  &lt;ValidationListItem status={hasSymbol ? \"passed\" : \"pending\"}&gt;\n    At least one symbol\n  &lt;/ValidationListItem&gt;\n&lt;/ValidationList&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: moves focus past the list (the list is not interactive)</li>\n<li>The list itself does not handle keyboard input</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Implicit <code>list</code> role from <code>&lt;ul&gt;</code> element</li>\n<li><code>aria-label</code> describes which input the rules apply to</li>\n<li><code>aria-live=\"polite\"</code> announces status changes as the user types</li>\n<li>Each child contributes a list item via its <code>&lt;li&gt;</code> element</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use next to inputs that have multiple validation rules.</li>\n<li>Use when users benefit from real-time feedback (passwords, usernames, custom identifiers).</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for a single rule — use <code>Hint</code> or <code>ErrorMessage</code> instead.</li>\n<li>Do not use after form submission — use <code>ErrorSummary</code> for batched errors.</li>\n<li>Do not use as a generic checklist — use <code>CheckList</code> instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>Renders the structural skeleton — <code>&lt;ul&gt;</code> with <code>aria-live=\"polite\"</code>. The status icons (pending dot, check mark, cross) and color treatment for each item state are entirely the consumer's responsibility, driven by <code>data-status</code> on each child.</p>\n\n<h2>Styles</h2>\n\n<p>Consumer CSS targets the <code>validation-list</code> class. Typical treatments remove default list bullets and apply consistent vertical spacing. Per-item visual states are styled via the inner <code>validation-list-item[data-status]</code> attribute selectors.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;ul&gt;</code> element with class <code>validation-list</code></li>\n<li>Verify <code>aria-label</code> is present and equals the <code>label</code> prop</li>\n<li>Verify <code>aria-live=\"polite\"</code> is set on the list</li>\n<li>Verify children render inside the list</li>\n<li>Verify additional HTML attributes pass through to the root <code>&lt;ul&gt;</code></li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Keep rule text short and start each rule with a verb-free statement (e.g., \"At least 8 characters\"). Use color plus an icon to signal state — never color alone.</li>\n<li><strong>Developers</strong>: Always supply <code>ValidationListItem</code> children with the appropriate <code>status</code> prop. Update the <code>status</code> reactively as the user types.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>validation-list-item</code> — one validation rule in a validation list with a status of pending, passed, or failed</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>US Web Design System Validation: https://designsystem.digital.gov/components/validation/</li>\n<li>WAI-ARIA aria-live: https://www.w3.org/TR/wai-aria-1.2/#aria-live</li>\n<li>WCAG 3.3.1 Error Identification: https://www.w3.org/WAI/WCAG22/Understanding/error-identification</li>\n</ul>";
</script>

<svelte:head>
  <title>Validation List — Lily Design System</title>
  <meta name="description" content="A validation list is a &lt;ul&gt; of validation-list-item rules that provides live feedback as the user types — typically next to a password field, a username field, or any input with multiple rules." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
