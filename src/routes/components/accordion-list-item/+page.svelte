<script lang="ts">
  const html: string = "<h1>accordion list Item</h1>\n\n<p>An accordion list item is a single expandable section using native HTML <code>&lt;details&gt;</code> / <code>&lt;summary&gt;</code> elements, providing built-in keyboard accessibility and toggle behavior. It is designed to be used inside an AccordionList within an AccordionNav container.</p>\n\n<p>The component leverages the browser's native disclosure widget to deliver accessible expand/collapse behavior without any custom JavaScript. The <code>open</code> prop is two-way bindable, allowing parent components to programmatically control and observe the expanded state.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;li&gt;</code> element with class <code>accordion-list-item</code></li>\n<li>Spreads <code>restProps</code> onto the root element for consumer customization</li>\n</ul>\n\n<h2>Help</h2>\n\n<p>Use AccordionListItem for each individual collapsible section within an AccordionList. Common scenarios include FAQ question/answer pairs, expandable settings groups, and collapsible content panels. For more granular control over the header and body, use AccordionSummary and AccordionDetails as children.</p>\n\n<h2>Syntax</h2>\n\n<pre><code class=\"language-html\">&lt;AccordionListItem summary=\"...\"&gt;\n  &lt;!-- expandable content --&gt;\n&lt;/AccordionListItem&gt;</code></pre>\n\n<h2>Usage</h2>\n\n<p>Simple with summary prop:</p>\n\n<pre><code class=\"language-html\">&lt;AccordionListItem summary=\"What is your return policy?\"&gt;\n  &lt;p&gt;You can return items within 30 days of purchase for a full refund.&lt;/p&gt;\n&lt;/AccordionListItem&gt;</code></pre>\n\n<p>With explicit summary and details children:</p>\n\n<pre><code class=\"language-html\">&lt;AccordionListItem&gt;\n  &lt;AccordionSummary&gt;Shipping information&lt;/AccordionSummary&gt;\n  &lt;AccordionDetails&gt;\n    &lt;p&gt;Free standard shipping on orders over $50. Express shipping available for $9.99.&lt;/p&gt;\n  &lt;/AccordionDetails&gt;\n&lt;/AccordionListItem&gt;</code></pre>\n\n<p>With bindable open state:</p>\n\n<pre><code class=\"language-html\">&lt;AccordionListItem summary=\"Advanced options\" open={showAdvanced}&gt;\n  &lt;p&gt;Configure advanced notification preferences, API access, and data export settings.&lt;/p&gt;\n&lt;/AccordionListItem&gt;</code></pre>\n\n<h2>Props</h2>\n\n<table>\n<thead>\n<tr><th>Prop</th><th>Type</th><th>Default</th><th>Description</th></tr>\n</thead>\n<tbody>\n<tr><td><code>summary</code></td><td><code>string</code></td><td>(required)</td><td>Text for the summary/header line</td></tr>\n<tr><td><code>open</code></td><td><code>boolean</code></td><td><code>false</code></td><td>Whether the item is expanded; bindable with two-way <code>open</code> binding</td></tr>\n<tr><td><code>children</code></td><td><code>slot</code></td><td>(required)</td><td>Detail content rendered when expanded</td></tr>\n<tr><td><code>...restProps</code></td><td>HTML attributes</td><td></td><td>Additional attributes passed to the <code>&lt;details&gt;</code> element</td></tr>\n</tbody>\n</table>\n\n<h2>Examples</h2>\n\n<p>FAQ item:</p>\n\n<pre><code class=\"language-html\">&lt;AccordionList&gt;\n  &lt;AccordionListItem summary=\"What is your return policy?\"&gt;\n    &lt;p&gt;You can return items within 30 days of purchase.&lt;/p&gt;\n  &lt;/AccordionListItem&gt;\n  &lt;AccordionListItem summary=\"Do you offer free shipping?\"&gt;\n    &lt;p&gt;Free shipping on orders over $50.&lt;/p&gt;\n  &lt;/AccordionListItem&gt;\n&lt;/AccordionList&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus the summary element</li>\n<li>Enter / Space: Toggle open/close (browser default for <code>&lt;details&gt;</code>)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Native <code>&lt;details&gt;</code> / <code>&lt;summary&gt;</code> provide implicit disclosure widget semantics</li>\n<li>Browser automatically manages expanded/collapsed state announcement</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside AccordionList to represent one collapsible section with a heading and expandable content.</li>\n<li>Use when you need native disclosure behavior for individual sections without custom JavaScript.</li>\n<li>Use for FAQ question/answer pairs, expandable settings groups, or categorised navigation sections.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside AccordionList -- it is designed to work within the AccordionNav composition pattern.</li>\n<li>Do not use for non-collapsible list items -- use ContentsListItem or BreadcrumbListItem instead.</li>\n<li>Do not use for top-level page sections that should always be visible -- use headings or Panel instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides native <code>&lt;details&gt;</code> / <code>&lt;summary&gt;</code> disclosure semantics, two-way bindable open state, and keyboard toggle behavior with zero visual styling. The consumer is responsible for all CSS including summary appearance, expanded/collapsed indicators, content padding, borders, and transition animations.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.accordion-list-item</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;li&gt;</code> element with class <code>accordion-list-item</code></li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Provide a clear expand/collapse indicator (chevron, plus/minus icon) next to the summary text so users understand the section is interactive.</li>\n<li><strong>Developers</strong>: Use the two-way bindable <code>open</code> prop to synchronize state when you need programmatic control, such as \"expand all\" or \"collapse all\" functionality.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>AccordionListItem follows the Nav / List / ListItem composition pattern:</p>\n\n<ul>\n<li><strong>AccordionNav</strong> -- outer <code>&lt;nav&gt;</code> container providing the landmark region.</li>\n<li><strong>AccordionList</strong> -- <code>&lt;ol&gt;</code> list grouping the collapsible items.</li>\n<li><strong>AccordionListItem</strong> -- individual <code>&lt;details&gt;</code> / <code>&lt;summary&gt;</code> sections for each expandable item.</li>\n</ul>\n\n<pre><code class=\"language-html\">&lt;AccordionNav label=\"FAQ\"&gt;\n  &lt;AccordionList&gt;\n    &lt;AccordionListItem summary=\"Question 1\"&gt;Answer 1&lt;/AccordionListItem&gt;\n  &lt;/AccordionList&gt;\n&lt;/AccordionNav&gt;</code></pre>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>accordion-nav</code> — an accordion navigation area for collapsible accordion information</li>\n<li><code>accordion-list</code> — an accordion ordered list of list item components</li>\n<li><code>accordion-link</code> — an accordion link</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>HTML details element: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/details</li>\n<li>WAI-ARIA Accordion Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/accordion/</li>\n</ul>";
</script>

<svelte:head>
  <title>accordion list Item — Lily Design System</title>
  <meta name="description" content="An accordion list item is a single expandable section using native HTML &lt;details&gt; / &lt;summary&gt; elements, providing built-in keyboard accessibility and toggle behavior. It is designed to be used insi..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
