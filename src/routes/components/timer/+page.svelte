<script lang="ts">
  const html: string = "<h1>Timer</h1>\n\n<p>A timer is a UI/UX component that displays a countdown or elapsed time value in a live-updating format. It renders as a semantic HTML <code>&lt;time&gt;</code> element with <code>role=\"timer\"</code> and <code>aria-live=\"polite\"</code> to ensure screen readers announce updates without interrupting the user. The component is commonly used for session timeouts, countdowns, stopwatches, cooking timers, exam time limits, and any scenario where users need to track the passage of time. This headless component provides the semantic structure and accessibility features while the consumer manages the actual timer logic and provides formatted display content through the children slot.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as <code>&lt;time role=\"timer\" aria-label={label} aria-live=\"polite\"&gt;</code></li>\n<li>Children slot provides the formatted timer display text</li>\n<li><code>aria-live=\"polite\"</code> ensures screen readers announce updates without interrupting</li>\n<li>Consumer manages timer logic (intervals, countdowns); this component only provides structure and accessibility</li>\n<li>Optional <code>datetime</code> attribute can be passed via restProps for machine-readable duration (e.g. <code>datetime=\"PT5M30S\"</code>)</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible label for screen readers</li>\n<li><code>children</code>: slot (required) -- formatted timer display content</li>\n<li><code>...restProps</code>: Any additional HTML attributes spread onto the <code>&lt;time&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;Timer label=\"Session timeout\" datetime=\"PT5M30S\"&gt;05:30&lt;/Timer&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;Timer label=\"Exam time remaining\" datetime=\"PT45M00S\"&gt;45:00&lt;/Timer&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;Timer label=\"Elapsed consultation time\"&gt;{formatMinutesSeconds(elapsed)}&lt;/Timer&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;Timer label=\"Cooking timer\" datetime=\"PT12M15S\"&gt;12:15&lt;/Timer&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>None -- this is an informational display, not interactive</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"timer\"</code> indicates countdown or elapsed time</li>\n<li><code>aria-label</code> from the label prop provides accessible name</li>\n<li><code>aria-live=\"polite\"</code> for non-intrusive screen reader announcements of updates</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for countdown or elapsed time display, such as session timeout, exam timer, or cooking timer.</li>\n<li>Use when screen readers need to be informed of time changes via a live region.</li>\n<li>Use with the <code>datetime</code> attribute for machine-readable ISO 8601 durations.</li>\n<li>Use when the displayed time updates dynamically via consumer-managed intervals.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for static time display -- show the time as plain text or a <code>&lt;time&gt;</code> element.</li>\n<li>Do not use for auto-triggering actions on countdown -- use TimerButton instead.</li>\n<li>Do not use when time remaining is better shown as a proportion -- use Progress or Meter instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;time&gt;</code> element with <code>role=\"timer\"</code>, <code>aria-label</code>, and <code>aria-live=\"polite\"</code> for accessible live-updating time display. The consumer manages all timer logic (intervals, countdowns) and provides formatted display content through the children slot, plus all visual styling.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.timer</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;span&gt;</code> element with class <code>timer</code></li>\n<li>Verify role=\"timer\"` indicates countdown or elapsed time</li>\n<li>Verify aria-label` from the label prop provides accessible name</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use a clear, monospaced or tabular font for the timer digits to prevent layout shifts as numbers change. Consider color changes as time runs low.</li>\n<li><strong>Developers</strong>: Provide a <code>datetime</code> attribute (e.g., <code>datetime=\"PT5M30S\"</code>) via restProps for machine-readable duration. Manage cleanup of intervals on component unmount.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>timer-button</code> — a button with a timer that will automatically click after a given amount of time</li>\n<li><code>loading</code> — a loading indicator, such as text, or image, or animation</li>\n<li><code>date-time-view</code> — see components.tsv</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA timer role: https://www.w3.org/TR/wai-aria-1.2/#timer</li>\n<li>HTML <code>&lt;time&gt;</code> element: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/time</li>\n<li>ISO 8601 Duration format: https://en.wikipedia.org/wiki/ISO_8601#Durations</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"timer\" role=\"timer\" aria-label=\"Time remaining\">05:00</div>";
  const svelteSource: string = "// In your Svelte component:\nimport Timer from \"lily-design-system-svelte-headless/components/Timer/Timer.svelte\";\n\n<Timer>\n  <!-- content -->\n</Timer>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Timer — Lily Design System</title>
  <meta name="description" content="A timer is a UI/UX component that displays a countdown or elapsed time value in a live-updating format. It renders as a semantic HTML &lt;time&gt; element with role=&quot;timer&quot; and aria-live=&quot;polite&quot; to ensu..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

