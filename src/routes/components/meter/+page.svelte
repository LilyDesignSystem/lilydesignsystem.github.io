<script lang="ts">
  const html: string = "<h1>Meter</h1>\n\n<p>Meter is a headless component that displays a scalar measurement within a known range using the native HTML <code>&lt;meter&gt;</code> element. It provides accessible labelling and supports optional threshold indicators for low, high, and optimum values.</p>\n\n<p>This component is useful for displaying disk usage, battery level, password strength, performance scores, or any bounded numeric measurement. The native <code>&lt;meter&gt;</code> element provides built-in semantics that assistive technologies can interpret, and the text content inside the element serves as a fallback for browsers that do not support the element.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a native <code>&lt;meter&gt;</code> element with the value displayed as fallback text content</li>\n<li>Uses <code>aria-label</code> for an accessible name describing what the meter measures</li>\n<li>Supports the full set of <code>&lt;meter&gt;</code> attributes: <code>value</code>, <code>min</code>, <code>max</code>, <code>low</code>, <code>high</code>, <code>optimum</code></li>\n<li>The <code>min</code> and <code>max</code> props default to 0 and 100 respectively</li>\n<li><code>low</code>, <code>high</code>, and <code>optimum</code> are optional and default to undefined</li>\n<li>Spreads <code>restProps</code> onto the meter for consumer customization</li>\n<li>pattern: standard props destructuring with typed props</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>value</code>: number (required) -- the current measured value</li>\n<li><code>min</code>: number (default: <code>0</code>) -- the minimum bound of the range</li>\n<li><code>max</code>: number (default: <code>100</code>) -- the maximum bound of the range</li>\n<li><code>low</code>: number (optional) -- the upper bound of the low range threshold</li>\n<li><code>high</code>: number (optional) -- the lower bound of the high range threshold</li>\n<li><code>optimum</code>: number (optional) -- the optimal value within the range</li>\n<li><code>label</code>: string (required) -- accessible name for the meter via <code>aria-label</code></li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;meter&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Disk usage gauge with thresholds:</p>\n\n<pre><code class=\"language-html\">&lt;Meter label=\"Disk space used\" value={72} max={100} low={25} high={75} optimum={50} /&gt;</code></pre>\n\n<p>Battery level indicator:</p>\n\n<pre><code class=\"language-html\">&lt;Meter label=\"Battery level\" value={35} min={0} max={100} low={20} high={80} optimum={100} /&gt;</code></pre>\n\n<p>Password strength meter:</p>\n\n<pre><code class=\"language-html\">&lt;Meter label=\"Password strength\" value={3} min={0} max={5} low={1} high={4} optimum={5} /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive display element with no interactive behavior.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides an accessible name describing what the meter measures</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use Meter to display a scalar measurement within a known range, such as disk usage, battery level, or password strength.</li>\n<li>Use Meter when the value has defined minimum and maximum bounds and optionally low, high, and optimum thresholds.</li>\n<li>Use Meter for compliance scores, performance ratings, or capacity indicators that represent a fixed-point measurement.</li>\n<li>Use Meter for displaying signal strength, sound volume, or any bounded numeric gauge.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use Meter for task completion or ongoing progress -- use Progress instead.</li>\n<li>Do not use Meter for indeterminate values where the range is unknown -- the range must be defined.</li>\n<li>Do not use Meter for interactive controls where the user adjusts the value -- use RangeInput or Slider instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component renders a native <code>&lt;meter&gt;</code> element with <code>aria-label</code>, supporting <code>min</code>, <code>max</code>, <code>low</code>, <code>high</code>, and <code>optimum</code> attributes for threshold semantics. The consumer provides all visual styling, including gauge colors, threshold color changes, size, and layout.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.meter</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;meter&gt;</code> element with class <code>meter</code></li>\n<li>Verify aria-label={label}` -- provides an accessible name describing what the meter measures</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use color to indicate threshold regions (e.g., green for optimum, yellow for warning, red for critical) and always include a visible text label near the meter.</li>\n<li><strong>Developers</strong>: Set <code>low</code>, <code>high</code>, and <code>optimum</code> props to leverage the browser's built-in color hinting for the <code>&lt;meter&gt;</code> element. Provide a meaningful <code>label</code> for screen readers.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>progress</code> — a horizontal progress bar showing completion</li>\n<li><code>progress-bar</code> — a horizontal progress indicator</li>\n<li><code>statistic</code> — a numeric value display with title, prefix, and suffix</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>HTML meter element: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/meter</li>\n<li>WAI-ARIA meter role: https://www.w3.org/TR/wai-aria-1.2/#meter</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<meter class=\"meter\" value=\"0.7\" min=\"0\" max=\"1\" aria-label=\"Meter\">70%</meter>";
  const svelteSource: string = "// In your Svelte component:\nimport Meter from \"lily-design-system-svelte-headless/components/Meter/Meter.svelte\";\n\n<Meter>\n  <!-- content -->\n</Meter>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Meter — Lily Design System</title>
  <meta name="description" content="Meter is a headless component that displays a scalar measurement within a known range using the native HTML &lt;meter&gt; element. It provides accessible labelling and supports optional threshold indicat..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

