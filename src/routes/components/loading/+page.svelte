<script lang="ts">
  const html: string = "<h1>Loading</h1>\n\n<p>A loading indicator communicates that content or an action is being processed, using text, an image, or an animation. Loading indicators help users understand that the system is working and they should wait.</p>\n\n<p>This headless component uses a <code>&lt;div&gt;</code> element with <code>role=\"status\"</code> and <code>aria-live=\"polite\"</code> to announce loading state changes to screen readers.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses <code>&lt;div&gt;</code> element with <code>role=\"status\"</code> for screen reader announcements</li>\n<li><code>aria-live=\"polite\"</code> announces loading state changes without interrupting</li>\n<li><code>aria-label</code> provides a text description of the loading state</li>\n<li>Content is provided through child elements (text, spinner, animation)</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (default: \"Loading\") -- accessible label for the loading state</li>\n<li><code>children</code>: slot (optional) -- loading indicator content (text, spinner, etc.)</li>\n<li><code>...restProps</code>: Any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Page-level loading overlay:</p>\n\n<pre><code class=\"language-html\">&lt;Loading label=\"Loading dashboard\"&gt;\n  &lt;span&gt;Loading dashboard...&lt;/span&gt;\n&lt;/Loading&gt;</code></pre>\n\n<p>Section loading with a spinner and descriptive text:</p>\n\n<pre><code class=\"language-html\">&lt;Loading label=\"Uploading file\"&gt;\n  &lt;ProgressSpinner label=\"Uploading\" /&gt;\n  &lt;span&gt;Uploading report.pdf...&lt;/span&gt;\n&lt;/Loading&gt;</code></pre>\n\n<p>Simple text-only loading indicator:</p>\n\n<pre><code class=\"language-html\">&lt;Loading label=\"Fetching results\"&gt;Fetching results...&lt;/Loading&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>None -- loading indicators are informational, not interactive</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"status\"</code> -- creates an ARIA live region for loading announcements</li>\n<li><code>aria-live=\"polite\"</code> -- announces changes without interrupting current speech</li>\n<li><code>aria-label</code> -- describes the loading state</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use Loading as a page-level or section-level indicator when content is being fetched, computed, or processed.</li>\n<li>Use Loading to provide feedback during asynchronous operations such as API calls or route transitions.</li>\n<li>Use Loading when the loading indicator needs to contain text, images, or animations as children.</li>\n<li>Use Loading to wrap a ProgressSpinner with additional context text for larger content areas.</li>\n<li>Use Loading for full-page overlays while initial data is being retrieved.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use Loading for inline spinners next to a button or field -- use ProgressSpinner instead.</li>\n<li>Do not use Loading for content placeholders that match the layout shape -- use Skeleton instead.</li>\n<li>Do not use Loading for instant operations that complete without noticeable delay.</li>\n<li>Do not use Loading when a progress percentage is available -- use Progress or ProgressCircle instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides <code>role=\"status\"</code> with <code>aria-live=\"polite\"</code> for accessible loading announcements, with zero visual styling. The consumer is responsible for all CSS including spinner animation, text styling, overlay, positioning, and size.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.loading</code> class for targeting. No default styles are included -- this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>loading</code></li>\n<li>Verify <code>role=\"status\"</code> is present</li>\n<li>Verify <code>aria-live=\"polite\"</code> is present</li>\n<li>Verify <code>aria-label</code> is applied</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Provide clear visual feedback that something is happening. Use consistent loading indicators across the application. Consider skeleton screens for content-heavy pages.</li>\n<li><strong>Developers</strong>: Use <code>aria-live=\"polite\"</code> to avoid interrupting screen reader users. Update the <code>label</code> prop to reflect the current loading context.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>skeleton</code> — a placeholder loading animation for content</li>\n<li><code>progress-spinner</code> — an indeterminate spinning progress indicator</li>\n<li><code>progress</code> — a horizontal progress bar showing completion</li>\n<li><code>beach-ball</code> — a decorative animated beach ball element</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/HTML/Element/div\">MDN div element</a></li>\n</ul>";
</script>

<svelte:head>
  <title>Loading — Lily Design System</title>
  <meta name="description" content="A loading indicator communicates that content or an action is being processed, using text, an image, or an animation. Loading indicators help users understand that the system is working and they sh..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
