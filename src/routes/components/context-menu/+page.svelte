<script lang="ts">
  const html: string = "<h1>Context Menu</h1>\n\n<p>A context menu provides a list of actions relevant to the current context, typically triggered by a right-click or long-press. The menu appears near the pointer and offers contextual options such as cut, copy, paste, or custom actions.</p>\n\n<p>This component manages menu visibility, keyboard navigation, and focus. When opened, focus moves to the first menu item automatically. Arrow keys navigate between items with wrapping, and Escape closes the menu. Consumers trigger opening externally (e.g., via a <code>contextmenu</code> event handler) and provide <code>role=\"menuitem\"</code> children.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> with <code>role=\"menu\"</code> and <code>aria-label</code>, conditionally shown via conditional rendering</li>\n<li>Supports two-way binding on the <code>open</code> prop</li>\n<li>reactive state tracks the menu element reference for focus management</li>\n<li><code>$effect()</code> focuses the first <code>[role=\"menuitem\"]</code> when the menu opens</li>\n<li>Keyboard handler navigates items with ArrowDown/ArrowUp (wrapping), Home/End, and Escape to close</li>\n<li>Spreads <code>restProps</code> onto the menu container</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name for the context menu via <code>aria-label</code></li>\n<li><code>open</code>: boolean (default: false) -- whether the menu is visible; bindable for two-way control</li>\n<li><code>children</code>: slot (required) -- menu item content (should include <code>role=\"menuitem\"</code> elements)</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Context menu with ContextMenuItem children:</p>\n\n<pre><code class=\"language-html\">&lt;ContextMenu label=\"Text actions\" open={isContextMenuOpen}&gt;\n    &lt;ContextMenuItem onclick={() =&gt; handleCut()}&gt;Cut&lt;/ContextMenuItem&gt;\n    &lt;ContextMenuItem onclick={() =&gt; handleCopy()}&gt;Copy&lt;/ContextMenuItem&gt;\n    &lt;ContextMenuItem onclick={() =&gt; handlePaste()}&gt;Paste&lt;/ContextMenuItem&gt;\n&lt;/ContextMenu&gt;</code></pre>\n\n<p>Triggered on right-click of a target element:</p>\n\n<pre><code class=\"language-html\">&lt;div oncontextmenu={(e) =&gt; { e.preventDefault(); isContextMenuOpen = true; }}&gt;\n    Right-click this area for options\n&lt;/div&gt;\n&lt;ContextMenu label=\"Item actions\" open={isContextMenuOpen}&gt;\n    &lt;ContextMenuItem onclick={() =&gt; handleEdit()}&gt;Edit&lt;/ContextMenuItem&gt;\n    &lt;ContextMenuItem onclick={() =&gt; handleDelete()}&gt;Delete&lt;/ContextMenuItem&gt;\n&lt;/ContextMenu&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>ArrowDown: Move focus to the next menu item (wraps to first)</li>\n<li>ArrowUp: Move focus to the previous menu item (wraps to last)</li>\n<li>Home: Move focus to the first menu item</li>\n<li>End: Move focus to the last menu item</li>\n<li>Escape: Close the menu</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"menu\"</code> -- identifies the container as a menu widget</li>\n<li><code>aria-label</code> -- provides an accessible name for the menu</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for a menu that appears on right-click or long-press.</li>\n<li>Use for contextual actions on specific items such as cut, copy, paste, or custom actions.</li>\n<li>Use when actions apply to a specific element and displaying them inline would clutter the interface.</li>\n<li>Use to provide supplementary actions alongside visible primary controls.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use as the only way to access actions -- ensure alternatives exist for accessibility.</li>\n<li>Do not use for global menus -- use Menu or DropdownMenu instead.</li>\n<li>Do not use for navigation links -- use NavigationMenu instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless ContextMenu component provides a <code>&lt;div&gt;</code> with <code>role=\"menu\"</code> and <code>aria-label</code>, automatic focus management (first item receives focus on open), keyboard navigation with ArrowUp/ArrowDown wrapping, Home/End, and Escape to close. The consumer provides all visual styling including positioning near the pointer, background, borders, shadows, and item appearance.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.context-menu</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>context-menu</code></li>\n<li>Verify role=\"menu\"` -- identifies the container as a menu widget</li>\n<li>Verify aria-label` -- provides an accessible name for the menu</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Position the menu near the trigger point and ensure it does not overflow the viewport. Group related actions with visual separators.</li>\n<li><strong>Developers</strong>: Trigger the menu by handling the <code>contextmenu</code> event on the target element and setting <code>open</code> to true. Use ContextMenuItem components as children with <code>role=\"menuitem\"</code>.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>ContextMenu follows the Menu/MenuItem composition pattern. Use ContextMenu as the container with ContextMenuItem components as children. The ContextMenu manages focus and keyboard navigation, while each ContextMenuItem renders as a <code>role=\"menuitem\"</code> element with <code>tabindex=\"-1\"</code> for roving focus.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>context-menu-item</code> — one item in a context menu</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Menu Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/menu/</li>\n</ul>";
</script>

<svelte:head>
  <title>Context Menu — Lily Design System</title>
  <meta name="description" content="A context menu provides a list of actions relevant to the current context, typically triggered by a right-click or long-press. The menu appears near the pointer and offers contextual options such a..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
