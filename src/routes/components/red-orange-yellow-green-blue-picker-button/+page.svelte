<script lang="ts">
  const html: string = "<h1>Red Orange Yellow Green Blue Picker Button</h1>\n\n<p>A five-level color status picker button is an individual button within a RedOrangeYellowGreenBluePicker that represents one of five status levels. When clicked, it selects that status value. The component uses <code>aria-pressed</code> to indicate the currently selected state and <code>data-value</code> to carry the status value, making it accessible to screen readers and enabling CSS-based styling by status color.</p>\n\n<p>The five levels provide a more granular status scale than the traditional three-level RAG: red indicates critical, orange indicates serious, yellow indicates caution, green indicates good, and blue indicates excellent or informational.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as a <code>&lt;button&gt;</code> element with <code>aria-pressed</code> for toggle state</li>\n<li>Designed to be used inside a RedOrangeYellowGreenBluePicker component</li>\n<li>Each button represents one of five status levels (red, orange, yellow, green, blue)</li>\n<li>The <code>selected</code> prop controls <code>aria-pressed</code> to indicate the active selection</li>\n<li>The <code>data-value</code> attribute carries the status value for CSS targeting</li>\n<li>The <code>label</code> prop provides the accessible name via <code>aria-label</code></li>\n<li>Spreads <code>...restProps</code> onto the <code>&lt;button&gt;</code> element for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>value</code>: string (required) -- the status value this button represents (e.g., \"red\", \"orange\", \"yellow\", \"green\", \"blue\")</li>\n<li><code>selected</code>: boolean (default: false) -- whether this button is currently selected</li>\n<li><code>label</code>: string (required) -- accessible label for the button (e.g., \"Red - Critical\", \"Green - Good\")</li>\n<li><code>onclick</code>: callback -- handler called when the button is clicked</li>\n<li><code>...restProps</code>: any -- additional HTML attributes spread onto the <code>&lt;button&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Five-level risk assessment picker:</p>\n\n<pre><code class=\"language-html\">&lt;RedOrangeYellowGreenBluePicker label=\"Risk assessment\"&gt;\n  &lt;RedOrangeYellowGreenBluePickerButton value=\"red\" label=\"Red - Critical\" onclick={handleSelect} /&gt;\n  &lt;RedOrangeYellowGreenBluePickerButton value=\"orange\" label=\"Orange - Serious\" onclick={handleSelect} /&gt;\n  &lt;RedOrangeYellowGreenBluePickerButton value=\"yellow\" label=\"Yellow - Caution\" selected onclick={handleSelect} /&gt;\n  &lt;RedOrangeYellowGreenBluePickerButton value=\"green\" label=\"Green - Good\" onclick={handleSelect} /&gt;\n  &lt;RedOrangeYellowGreenBluePickerButton value=\"blue\" label=\"Blue - Excellent\" onclick={handleSelect} /&gt;\n&lt;/RedOrangeYellowGreenBluePicker&gt;</code></pre>\n\n<p>Individual button with custom data attribute:</p>\n\n<pre><code class=\"language-html\">&lt;RedOrangeYellowGreenBluePickerButton\n  value=\"orange\"\n  label=\"Orange - Significant risk\"\n  selected\n  data-severity=\"high\"\n  onclick={handleSelect}\n/&gt;</code></pre>\n\n<p>Inside a form field for patient triage:</p>\n\n<pre><code class=\"language-html\">&lt;Field label=\"Triage level\" required&gt;\n  &lt;RedOrangeYellowGreenBluePicker label=\"Patient triage level\"&gt;\n    &lt;RedOrangeYellowGreenBluePickerButton value=\"red\" label=\"Red - Emergency\" onclick={handleSelect} /&gt;\n    &lt;RedOrangeYellowGreenBluePickerButton value=\"orange\" label=\"Orange - Urgent\" onclick={handleSelect} /&gt;\n    &lt;RedOrangeYellowGreenBluePickerButton value=\"yellow\" label=\"Yellow - Standard\" selected onclick={handleSelect} /&gt;\n    &lt;RedOrangeYellowGreenBluePickerButton value=\"green\" label=\"Green - Minor\" onclick={handleSelect} /&gt;\n    &lt;RedOrangeYellowGreenBluePickerButton value=\"blue\" label=\"Blue - Routine\" onclick={handleSelect} /&gt;\n  &lt;/RedOrangeYellowGreenBluePicker&gt;\n&lt;/Field&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus the button</li>\n<li>Enter / Space: Activate the button to select this status</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-pressed</code> -- indicates whether this button is the currently selected status (<code>true</code> when selected, <code>false</code> otherwise)</li>\n<li><code>aria-label</code> -- provides the accessible name for the button (e.g., \"Yellow - Caution\")</li>\n<li><code>data-value</code> -- carries the status value for CSS-based styling (e.g., <code>data-value=\"yellow\"</code>)</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside a RedOrangeYellowGreenBluePicker to represent one selectable five-level status option.</li>\n<li>Use when you need a button-based five-level status picker instead of a select dropdown.</li>\n<li>Use to provide individually styled buttons for each of the five colour-coded levels.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside of a RedOrangeYellowGreenBluePicker container -- the button relies on the picker for grouping and context.</li>\n<li>Do not use for standalone toggle actions -- use <a href=\"../toggle-button/\">ToggleButton</a> instead.</li>\n<li>Do not use for standalone option selection -- use <a href=\"../radio-input/\">RadioInput</a> instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>The RedOrangeYellowGreenBluePickerButton headless component provides a <code>&lt;button&gt;</code> with <code>aria-pressed</code> for toggle state, <code>aria-label</code> for accessible naming, and <code>data-value</code> for CSS targeting. The consumer provides all visual styling, including color indicators and selected-state appearance.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.red-orange-yellow-green-blue-picker-button</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;button&gt;</code> element with class <code>red-orange-yellow-green-blue-picker-button</code></li>\n<li>Verify aria-label` -- provides the accessible name for the button (e.g., \"Yellow - Caution\")</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Ensure each of the five buttons is visually distinct with both color and text, and make the selected state clearly prominent.</li>\n<li><strong>Developers</strong>: Use <code>data-value</code> for CSS selectors to apply status-specific colors, and ensure only one button has <code>aria-pressed=\"true\"</code> at a time.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>red-orange-yellow-green-blue-picker</code> — a picker for selecting a five-level color status</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Button Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/button/</li>\n</ul>";
</script>

<svelte:head>
  <title>Red Orange Yellow Green Blue Picker Button — Lily Design System</title>
  <meta name="description" content="A five-level color status picker button is an individual button within a RedOrangeYellowGreenBluePicker that represents one of five status levels. When clicked, it selects that status value. The co..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
