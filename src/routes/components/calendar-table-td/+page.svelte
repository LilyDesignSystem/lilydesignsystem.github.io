<script lang="ts">
  const html: string = "<h1>Calendar Table TD</h1>\n\n<p>A calendar board item represents a single day cell within a calendar grid, supporting <code>aria-selected</code> for the chosen date and <code>aria-current=\"date\"</code> for today. It is designed to be used inside a CalendarTable <code>&lt;table&gt;</code> / <code>&lt;tr&gt;</code> structure.</p>\n\n<p>The component uses a roving tabindex pattern where the selected cell has <code>tabindex=\"0\"</code> and all other cells have <code>tabindex=\"-1\"</code>, enabling keyboard focus management within the calendar grid. Content is typically the day number, provided through the children slot.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as a <code>&lt;td&gt;</code> element with <code>role=\"gridcell\"</code> for a single day cell in a calendar grid</li>\n<li>Supports <code>selected</code> and <code>today</code> states for visual and accessible indication</li>\n<li>Uses roving tabindex pattern: <code>tabindex=\"0\"</code> when selected, <code>-1</code> otherwise</li>\n<li><code>aria-selected</code> and <code>aria-current</code> are set conditionally (omitted when falsy via <code>|| undefined</code>)</li>\n<li>Content is provided through the children slot (typically the day number)</li>\n<li>Designed to be used inside a CalendarTable <code>&lt;table&gt;</code> / <code>&lt;tr&gt;</code> structure</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>selected</code>: boolean (default: false) -- whether this day cell is selected</li>\n<li><code>today</code>: boolean (default: false) -- whether this day cell represents today's date</li>\n<li><code>children</code>: slot (required) -- day content, typically the day number</li>\n<li><code>...restProps</code>: Any additional HTML attributes passed to the <code>&lt;td&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;CalendarTableRow&gt;\n  &lt;CalendarTableTD&gt;7&lt;/CalendarTableTD&gt;\n  &lt;CalendarTableTD today selected&gt;8&lt;/CalendarTableTD&gt;\n  &lt;CalendarTableTD&gt;9&lt;/CalendarTableTD&gt;\n&lt;/CalendarTableRow&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;!-- A disabled day outside the current month --&gt;\n&lt;CalendarTableTD aria-disabled=\"true\"&gt;31&lt;/CalendarTableTD&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus moves to the selected cell (<code>tabindex=\"0\"</code>)</li>\n<li>Arrow keys: Typically handled by the parent CalendarTable grid navigation</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"gridcell\"</code> -- identifies the cell as part of a grid</li>\n<li><code>aria-selected</code> -- set to <code>true</code> when the cell is selected; omitted otherwise</li>\n<li><code>aria-current=\"date\"</code> -- set when the cell represents today's date; omitted otherwise</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside CalendarTable to provide the <code>&lt;td&gt;</code> for each day cell in the calendar grid</li>\n<li>Use when each cell needs selection state via <code>aria-selected</code> and today indication via <code>aria-current=\"date\"</code></li>\n<li>Use with roving tabindex to enable keyboard focus management across the calendar grid</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside CalendarTable -- use TableTD or DataTableTD for other table types</li>\n<li>Do not use for column header cells -- use <code>&lt;th&gt;</code> elements inside CalendarTableHead instead</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides a <code>&lt;td&gt;</code> with <code>role=\"gridcell\"</code>, conditional <code>aria-selected</code>, conditional <code>aria-current=\"date\"</code>, and roving tabindex pattern, all with zero visual styling. The consumer is responsible for all CSS including cell sizing, selected state highlight, today indicator, hover effects, focus ring, and disabled day styling.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.calendar-table-td</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;td&gt;</code> element with class <code>calendar-table-td</code></li>\n<li>Verify role=\"gridcell\"` -- identifies the cell as part of a grid</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Make the selected date and today's date visually distinct using different background colors or outlines. Ensure a visible focus indicator for keyboard navigation.</li>\n<li><strong>Developers</strong>: The roving tabindex pattern (<code>tabindex=\"0\"</code> on selected, <code>-1</code> on others) enables the parent grid to manage focus. Ensure arrow key handlers on the grid move focus between cells.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>CalendarTableTD is part of the CalendarTable composition pattern:</p>\n\n<ul>\n<li><strong>CalendarTable</strong> -- outer <code>&lt;table&gt;</code> with <code>role=\"grid\"</code>.</li>\n<li><strong>CalendarTableHead/Body/Foot</strong> -- section groupings.</li>\n<li><strong>CalendarTableRow</strong> -- <code>&lt;tr&gt;</code> representing one week.</li>\n<li><strong>CalendarTableTD</strong> -- <code>&lt;td&gt;</code> representing one day with selection and today state.</li>\n</ul>\n\n<pre><code class=\"language-html\">&lt;CalendarTableRow&gt;\n  &lt;CalendarTableTD&gt;14&lt;/CalendarTableTD&gt;\n  &lt;CalendarTableTD today selected&gt;15&lt;/CalendarTableTD&gt;\n  &lt;CalendarTableTD&gt;16&lt;/CalendarTableTD&gt;\n&lt;/CalendarTableRow&gt;</code></pre>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>calendar-table</code> — a calendar table interactive grid for managing dates, days, etc. &lt;table&gt;</li>\n<li><code>calendar-table-body</code> — a calendar table interactive grid tbody for managing dates, days, etc. &lt;tbody&gt;</li>\n<li><code>calendar-table-foot</code> — a calendar table interactive grid tfoot for managing dates, days, etc. &lt;tfoot&gt;</li>\n<li><code>calendar-table-head</code> — a calendar table interactive grid thead for managing dates, days, etc. &lt;thead&gt;</li>\n<li><code>calendar-table-row</code> — a calendar table interactive grid row for managing dates, days, etc. &lt;tr&gt;</li>\n<li><code>calendar-table-th</code> — a calendar table interactive grid header cell for managing dates, days, etc. &lt;th&gt;</li>\n<li><code>table</code> — a table with rows and columns &lt;table&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Grid Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/grid/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<td class=\"calendar-table-td\">15</td>";
  const svelteSource: string = "// In your Svelte component:\nimport CalendarTableTD from \"lily-design-system-svelte-headless/components/CalendarTableTD/CalendarTableTD.svelte\";\n\n<CalendarTableTD>\n  <!-- CalendarTableTD content -->\n</CalendarTableTD>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Calendar Table TD — Lily Design System</title>
  <meta name="description" content="A calendar board item represents a single day cell within a calendar grid, supporting aria-selected for the chosen date and aria-current=&quot;date&quot; for today. It is designed to be used inside a Calenda..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

