<script lang="ts">
  const html: string = "<h1>Tour List Item</h1>\n\n<p>A single step within a TourGuideList guided tour. Each step represents one screen or instruction in the tour sequence. The step conditionally shows or hides based on the <code>current</code> prop, using the <code>hidden</code> attribute and <code>aria-hidden</code> for proper accessibility. Step progress information is communicated to screen readers through the accessible label.</p>\n\n<p>This component is designed to be placed inside a TourGuideList container. The consumer controls which step is current via the <code>current</code> prop, enabling flexible navigation patterns (next/previous buttons, step indicators, etc.).</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;li&gt;</code> element with class <code>tour-list-item</code></li>\n<li>Spreads <code>restProps</code> onto the root element for consumer customization</li>\n</ul>\n\n<h2>Help</h2>\n\n<p>Use TourListItem for each individual step in a TourGuideList walkthrough. Common scenarios include onboarding step content, feature highlight descriptions, and tutorial instructions. The parent TourGuideList manages the dialog overlay while each TourListItem manages its own visibility.</p>\n\n<h2>Syntax</h2>\n\n<pre><code class=\"language-html\">&lt;TourListItem label=\"...\" current={...}&gt;\n  &lt;!-- step content --&gt;\n&lt;/TourListItem&gt;</code></pre>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;TourList label=\"Feature tour\" active={showTour}&gt;\n  &lt;TourListItem label=\"Search patients\" current={step === 0} stepNumber={1} totalSteps={2}&gt;\n    &lt;p&gt;Type a patient name or NHS number into the search bar to find their record.&lt;/p&gt;\n    &lt;button onclick={() =&gt; step++}&gt;Next&lt;/button&gt;\n  &lt;/TourListItem&gt;\n  &lt;TourListItem label=\"View results\" current={step === 1} stepNumber={2} totalSteps={2}&gt;\n    &lt;p&gt;Click a result to open the full patient record.&lt;/p&gt;\n    &lt;button onclick={() =&gt; step--}&gt;Back&lt;/button&gt;\n    &lt;button onclick={() =&gt; showTour = false}&gt;Done&lt;/button&gt;\n  &lt;/TourListItem&gt;\n&lt;/TourList&gt;</code></pre>\n\n<h2>Props</h2>\n\n<table>\n<thead>\n<tr><th>Prop</th><th>Type</th><th>Default</th><th>Description</th></tr>\n</thead>\n<tbody>\n<tr><td><code>label</code></td><td><code>string</code></td><td>(required)</td><td>Accessible name for this step via <code>aria-label</code></td></tr>\n<tr><td><code>current</code></td><td><code>boolean</code></td><td><code>false</code></td><td>Whether this step is the currently visible step</td></tr>\n<tr><td><code>stepNumber</code></td><td><code>number</code></td><td>(optional)</td><td>1-based step number (e.g. 1, 2, 3)</td></tr>\n<tr><td><code>totalSteps</code></td><td><code>number</code></td><td>(optional)</td><td>Total number of steps in the tour</td></tr>\n<tr><td><code>children</code></td><td><code>slot</code></td><td>(required)</td><td>Step content</td></tr>\n<tr><td><code>...restProps</code></td><td>HTML attributes</td><td></td><td>Additional attributes spread onto the container <code>&lt;div&gt;</code></td></tr>\n</tbody>\n</table>\n\n<h2>Examples</h2>\n\n<p>Multi-step tour:</p>\n\n<pre><code class=\"language-html\">&lt;TourGuideList label=\"Getting started\" active={active}&gt;\n  &lt;TourListItem label=\"Welcome\" current={step === 0} stepNumber={1} totalSteps={3}&gt;\n    &lt;p&gt;Welcome to the app!&lt;/p&gt;\n    &lt;button onclick={() =&gt; step++}&gt;Next&lt;/button&gt;\n  &lt;/TourListItem&gt;\n  &lt;TourListItem label=\"Features\" current={step === 1} stepNumber={2} totalSteps={3}&gt;\n    &lt;p&gt;Here are the features.&lt;/p&gt;\n    &lt;button onclick={() =&gt; step--}&gt;Back&lt;/button&gt;\n    &lt;button onclick={() =&gt; step++}&gt;Next&lt;/button&gt;\n  &lt;/TourListItem&gt;\n  &lt;TourListItem label=\"Finish\" current={step === 2} stepNumber={3} totalSteps={3}&gt;\n    &lt;p&gt;You're all set!&lt;/p&gt;\n    &lt;button onclick={() =&gt; active = false}&gt;Done&lt;/button&gt;\n  &lt;/TourListItem&gt;\n&lt;/TourGuideList&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>None directly -- keyboard interactions are managed by the parent TourGuideList and by interactive elements within step content</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"group\"</code> -- groups the step content semantically</li>\n<li><code>aria-roledescription=\"step\"</code> -- identifies the element as a tour step</li>\n<li><code>aria-label</code> -- provides an accessible name, optionally including \"Step N of M\"</li>\n<li><code>aria-current=\"step\"</code> -- indicates the currently active step</li>\n<li><code>aria-hidden=\"true\"</code> -- hides inactive steps from assistive technology</li>\n<li><code>hidden</code> -- hides inactive steps from visual rendering</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside TourList to represent one step in the guided tour.</li>\n<li>Use when each step needs independent visibility control and step progress information.</li>\n<li>Use with <code>stepNumber</code> and <code>totalSteps</code> props for accessible progress labels.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside TourList -- use TimelineListItem or ContentsListItem for other list patterns.</li>\n<li>Do not use for static content sections -- use AccordionListItem or ContentsListItem instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;div&gt;</code> with <code>role=\"group\"</code>, <code>aria-roledescription=\"step\"</code>, <code>aria-label</code> (with optional step progress), <code>aria-current</code>, and <code>hidden</code>/<code>aria-hidden</code> for conditional visibility. The consumer provides all step content, navigation buttons, and all visual styling.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.tour-list-item</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;li&gt;</code> element with class <code>tour-list-item</code></li>\n<li>Verify role=\"group\"` -- groups the step content semantically</li>\n<li>Verify aria-label` -- provides an accessible name, optionally including \"Step N of M\"</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Make the current step visually prominent and clearly indicate progress (e.g., step dots, \"Step N of M\" text) so users know where they are in the tour.</li>\n<li><strong>Developers</strong>: Provide <code>stepNumber</code> and <code>totalSteps</code> props for accessible step progress labels. Use the <code>current</code> prop to control which step is visible.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>TourListItem is a child of TourGuideList, following the Guide/GuideList/GuideListItem pattern. The parent TourGuideList provides the modal dialog overlay, and each TourListItem manages its own visibility based on the <code>current</code> prop.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>tour-list</code> — an ordered list of tour guide steps</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Group Role: https://www.w3.org/TR/wai-aria-1.2/#group</li>\n<li>WAI-ARIA aria-roledescription: https://www.w3.org/TR/wai-aria-1.2/#aria-roledescription</li>\n<li>WAI-ARIA aria-current: https://www.w3.org/TR/wai-aria-1.2/#aria-current</li>\n</ul>";
</script>

<svelte:head>
  <title>Tour List Item — Lily Design System</title>
  <meta name="description" content="A single step within a TourGuideList guided tour. Each step represents one screen or instruction in the tour sequence. The step conditionally shows or hides based on the current prop, using the hid..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
