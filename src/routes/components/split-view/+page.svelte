<script lang="ts">
  const html: string = "<h1>Split View</h1>\n\n<p>A split view is a two-panel resizable layout container with a draggable divider between the panels.</p>\n\n<p>Use it for layouts that benefit from user-controlled allocation of space — file browsers next to a file editor, list/detail views, or master/detail panels — where users may want to adjust the relative size of the two panels.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses a <code>&lt;div class=\"split-view\"&gt;</code> container with <code>aria-label</code></li>\n<li><code>data-orientation</code> reflects <code>orientation</code> for consumer styling and tests</li>\n<li><code>data-split-percent</code> reflects <code>splitPercent</code> for consumer styling and tests</li>\n<li>The primary panel renders inside <code>&lt;section class=\"split-view-primary\"&gt;</code></li>\n<li>The secondary panel renders inside <code>&lt;section class=\"split-view-secondary\"&gt;</code></li>\n<li>The divider, if not customized, defaults to <code>&lt;div class=\"split-view-divider\" role=\"separator\" aria-orientation aria-valuenow aria-valuemin=\"0\" aria-valuemax=\"100\" tabindex=\"0\"&gt;</code></li>\n<li>Consumers can supply a custom divider via the <code>divider</code> slot/snippet/prop</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (<strong>required</strong>) -- aria-label for the container</li>\n<li><code>orientation</code>: <code>\"horizontal\" | \"vertical\"</code> (default <code>\"horizontal\"</code>) -- divider orientation</li>\n<li><code>primary</code>: slot/snippet/ReactNode (<strong>required</strong>) -- first panel content</li>\n<li><code>secondary</code>: slot/snippet/ReactNode (<strong>required</strong>) -- second panel content</li>\n<li><code>divider</code>: slot/snippet/ReactNode (optional) -- custom divider content</li>\n<li><code>splitPercent</code>: number 0-100 (default <code>50</code>) -- initial split position as percentage</li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;SplitView\n  label=\"Files and editor\"\n  orientation=\"horizontal\"\n  splitPercent={30}\n  primary={&lt;FileBrowser /&gt;}\n  secondary={&lt;Editor /&gt;}\n/&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Move focus to and from the divider</li>\n<li>Arrow keys: (Consumer-provided) move the divider in <code>orientation</code>-appropriate increments</li>\n<li>Home / End: (Consumer-provided) snap the divider to its minimum or maximum</li>\n<li>Enter / Space: (Consumer-provided) toggle a collapsed panel</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Container: <code>aria-label</code> (required), <code>data-orientation</code>, <code>data-split-percent</code></li>\n<li>Default divider: <code>role=\"separator\"</code>, <code>aria-orientation</code>, <code>aria-valuenow={splitPercent}</code>, <code>aria-valuemin=\"0\"</code>, <code>aria-valuemax=\"100\"</code>, <code>tabindex=\"0\"</code></li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for two-pane layouts where users benefit from controlling pane size</li>\n<li>Use for list/detail or master/detail views</li>\n<li>Use when content density justifies user-controlled space allocation</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for full page layouts — use <code>GrailLayout</code> instead</li>\n<li>Do not use for three-or-more pane layouts — compose multiple split views</li>\n<li>Do not use when the layout proportions are fixed</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>Renders the container, two <code>&lt;section&gt;</code> panels, and a focusable <code>role=\"separator\"</code> divider. Drag handling, keyboard resize logic, and visual treatment are entirely the consumer's responsibility.</p>\n\n<h2>Styles</h2>\n\n<p>Consumer CSS targets the <code>split-view</code> class plus the inner <code>split-view-primary</code>, <code>split-view-secondary</code>, and <code>split-view-divider</code> classes. Use <code>data-orientation</code> and <code>data-split-percent</code> to drive layout proportions and divider direction.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> with class <code>split-view</code></li>\n<li>Verify <code>aria-label</code> equals the <code>label</code> prop</li>\n<li>Verify <code>data-orientation</code> equals the <code>orientation</code> prop</li>\n<li>Verify <code>data-split-percent</code> equals the <code>splitPercent</code> prop</li>\n<li>Verify primary content renders inside <code>&lt;section class=\"split-view-primary\"&gt;</code></li>\n<li>Verify secondary content renders inside <code>&lt;section class=\"split-view-secondary\"&gt;</code></li>\n<li>Verify the default divider has <code>role=\"separator\"</code>, matching <code>aria-orientation</code>, <code>aria-valuenow={splitPercent}</code>, <code>aria-valuemin=\"0\"</code>, <code>aria-valuemax=\"100\"</code>, and <code>tabindex=\"0\"</code></li>\n<li>Verify a custom <code>divider</code> slot replaces the default divider when provided</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Communicate divider affordance with a hover/focus indicator; ensure it meets minimum 44×44 px target size or provide an enlarged hit area.</li>\n<li><strong>Developers</strong>: Persist <code>splitPercent</code> in user preferences so the layout survives reloads.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>splitter</code> — a draggable divider for resizing adjacent panels</li>\n<li><code>drawer</code> — a panel that slides in from the edge of the screen</li>\n<li><code>sidebar</code> — a side panel for navigation or supplementary content</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Separator: https://www.w3.org/TR/wai-aria-1.2/#separator</li>\n<li>WAI-ARIA Window Splitter Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/windowsplitter/</li>\n<li>Adobe Spectrum Split View: https://spectrum.adobe.com/page/split-view/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"split-view\" aria-label=\"a two-panel resizable layout container with a draggable divider between them\">SplitView</div>";
  const svelteSource: string = "// In your Svelte component:\nimport SplitView from \"lily-design-system-svelte-headless/components/SplitView/SplitView.svelte\";\n\n<SplitView label=\"Label\" value=\"Sample value\" />\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Split View — Lily Design System</title>
  <meta name="description" content="A split view is a two-panel resizable layout container with a draggable divider between the panels." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

