<script lang="ts">
  const html: string = "<h1>Password Input</h1>\n\n<p>PasswordInput is a headless component that renders a native <code>&lt;input type=\"password\"&gt;</code> element with accessible labelling. The input obscures the entered text with dots or asterisks, providing secure password entry.</p>\n\n<p>Use this component in login forms, registration forms, account settings, or any interface requiring secure password entry. For a more featured variant with a show/hide toggle, see PasswordInputOrTextInputDiv.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a native <code>&lt;input type=\"password\"&gt;</code> element for secure text entry</li>\n<li>Uses <code>aria-label</code> for an accessible name describing the input purpose</li>\n<li>The <code>value</code> prop uses two-way binding with an initial empty string state</li>\n<li>Includes <code>autocomplete=\"current-password\"</code> for browser password manager integration</li>\n<li>Supports <code>required</code> and <code>disabled</code> boolean states</li>\n<li>Spreads <code>restProps</code> onto the input for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name for the input via <code>aria-label</code></li>\n<li><code>value</code>: string (default: <code>\"\"</code>) -- current password value; bindable with two-way binding</li>\n<li><code>autocomplete</code>: string (default: <code>\"current-password\"</code>) -- autocomplete hint for password managers</li>\n<li><code>required</code>: boolean (default: <code>false</code>) -- whether the input is required</li>\n<li><code>disabled</code>: boolean (default: <code>false</code>) -- whether the input is disabled</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;input&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;Field label=\"Create a password\"&gt;\n  &lt;Hint&gt;Must be at least 12 characters&lt;/Hint&gt;\n  &lt;PasswordInput\n    label=\"Create a password\"\n    autocomplete=\"new-password\"\n    spellcheck=\"false\"\n  /&gt;\n&lt;/Field&gt;\n\n&lt;Field label=\"Password\"&gt;\n  &lt;PasswordInput\n    label=\"Password\"\n    autocomplete=\"current-password\"\n    spellcheck=\"false\"\n  /&gt;\n&lt;/Field&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>Standard text input keyboard interactions provided natively by <code>&lt;input type=\"password\"&gt;</code>.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides an accessible name for the password input</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use whenever you need users to create a new password or enter an existing password</li>\n<li>Use with <code>autocomplete=\"new-password\"</code> for password creation and <code>autocomplete=\"current-password\"</code> for sign-in</li>\n<li>Use with the show/hide toggle so users can verify their password before submitting</li>\n<li>Use with <code>spellcheck=\"false\"</code> and <code>autocapitalize=\"off\"</code> to prevent spell-jacking and unwanted capitalisation</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for MFA codes, one-time passwords, or PINs -- use PinInputDiv or TextInput instead</li>\n<li>Do not use for security question answers or other sensitive non-password fields -- use TextInput</li>\n<li>Do not use when you need a combined show/hide toggle with a confirmation field -- use PasswordInputOrTextInputDiv</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component renders a native <code>&lt;input type=\"password\"&gt;</code> element with <code>aria-label</code>, two-way bindable value, and <code>autocomplete</code> support. The consumer provides all visual styling including sizing, borders, and focus states.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.password-input</code> class for targeting. Common styling includes matching the visual style of other text inputs in the form.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders an <code>&lt;input&gt;</code> element with <code>type=\"password\"</code></li>\n<li>Verify <code>aria-label</code> is set from the <code>label</code> prop</li>\n<li>Verify <code>autocomplete</code> defaults to <code>\"current-password\"</code></li>\n<li>Verify <code>disabled</code> and <code>required</code> attributes propagate correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Ensure the password input matches the visual style of other form inputs. Consider using a lock icon or label to clearly indicate the field requires a password.</li>\n<li><strong>Developers</strong>: Set <code>autocomplete</code> to <code>\"new-password\"</code> for registration forms to help password managers distinguish between login and signup flows.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>password-input-or-text-input-div</code> — an input for entering a password &lt;input type=\"password\"&gt; or text &lt;input type=\"text\"&gt; with show/hide toggle</li>\n<li><code>text-input</code> — a single-line text input field &lt;input type=\"text\"&gt;</li>\n<li><code>pin-input-div</code> — a series of single-digit inputs for entering a PIN or OTP code</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN input type=\"password\": https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/password</li>\n<li>HTML autocomplete attribute: https://developer.mozilla.org/en-US/docs/Web/HTML/Attributes/autocomplete</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<label class=\"label\" for=\"demo-input\">Label</label><input class=\"password-input\" type=\"password\" id=\"demo-input\" aria-label=\"PasswordInput\" />";
  const svelteSource: string = "// In your Svelte component:\nimport PasswordInput from \"lily-design-system-svelte-headless/components/PasswordInput/PasswordInput.svelte\";\n\n<PasswordInput label=\"Label\" />\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Password Input — Lily Design System</title>
  <meta name="description" content="PasswordInput is a headless component that renders a native &lt;input type=&quot;password&quot;&gt; element with accessible labelling. The input obscures the entered text with dots or asterisks, providing secure p..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

