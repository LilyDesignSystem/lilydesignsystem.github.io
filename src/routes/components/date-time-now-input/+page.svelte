<script lang="ts">
  const html: string = "<h1>Date Time Now Input</h1>\n\n<p>DateTimeNowInput is a headless component that wraps a native <code>&lt;input type=\"date\"&gt;</code>, <code>&lt;input type=\"time\"&gt;</code>, and a <code>&lt;button type=\"button\"&gt;</code> inside a <code>&lt;div&gt;</code>. The \"Now\" button sets both inputs to the current local date and time when clicked.</p>\n\n<p>This component is useful for event logging, timestamping, incident reporting, and any scenario where users need to quickly capture the current date and time or manually enter a specific date and time.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a wrapper <code>&lt;div&gt;</code> containing a date input, a time input, and a \"Now\" button</li>\n<li>The date input is <code>&lt;input type=\"date\"&gt;</code> for selecting a date</li>\n<li>The time input is <code>&lt;input type=\"time\"&gt;</code> for selecting a time</li>\n<li>The \"Now\" button sets both date and time inputs to the current local date and time</li>\n<li>Uses <code>aria-label</code> for accessible naming on each input and the button</li>\n<li>Supports two-way binding on the <code>dateValue</code> and <code>timeValue</code> props</li>\n<li>Spreads <code>restProps</code> onto the wrapper <code>&lt;div&gt;</code> for consumer extensibility</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name for the wrapper group via <code>aria-label</code></li>\n<li><code>dateLabel</code>: string (default: \"Date\") -- accessible name for the date input via <code>aria-label</code></li>\n<li><code>timeLabel</code>: string (default: \"Time\") -- accessible name for the time input via <code>aria-label</code></li>\n<li><code>nowLabel</code>: string (default: \"Now\") -- accessible label and text for the \"Now\" button</li>\n<li><code>dateValue</code>: string (default: \"\") -- bindable date string (format: YYYY-MM-DD)</li>\n<li><code>timeValue</code>: string (default: \"\") -- bindable time string (format: HH:mm)</li>\n<li><code>required</code>: boolean (default: false) -- whether the inputs are required for form submission</li>\n<li><code>disabled</code>: boolean (default: false) -- whether the inputs and button are disabled</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the wrapper <code>&lt;div&gt;</code></li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Clinical observation timestamp with \"Now\" button:</p>\n\n<pre><code class=\"language-html\">&lt;DateTimeNowInput label=\"Observation recorded\" dateValue={obsDate} timeValue={obsTime} nowLabel=\"Record now\" required /&gt;</code></pre>\n\n<p>Incident reporting with default labels:</p>\n\n<pre><code class=\"language-html\">&lt;DateTimeNowInput label=\"Incident occurred\" dateValue={incidentDate} timeValue={incidentTime} /&gt;</code></pre>\n\n<p>Locked timestamp that has already been recorded:</p>\n\n<pre><code class=\"language-html\">&lt;DateTimeNowInput label=\"Administered at\" dateValue={adminDate} timeValue={adminTime} disabled={true} /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Moves focus between the date input, time input, and \"Now\" button</li>\n<li>Enter/Space on the \"Now\" button: Sets both inputs to the current date and time</li>\n<li>Arrow keys: Navigate within the date and time picker fields (native browser behavior)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides an accessible name for the wrapper group</li>\n<li><code>aria-label={dateLabel}</code> -- provides an accessible name for the date input</li>\n<li><code>aria-label={timeLabel}</code> -- provides an accessible name for the time input</li>\n<li><code>aria-label={nowLabel}</code> -- provides an accessible name for the \"Now\" button</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use when users need to enter a date and time with a \"Now\" button that auto-fills the current date and time.</li>\n<li>Use in clinical recording where timestamps matter, such as observation times or medication administration.</li>\n<li>Use for event logging, incident reporting, timestamping, or time tracking interfaces.</li>\n<li>Use when the user frequently needs to record the current moment quickly.</li>\n<li>Use for audit trails and activity logs that require precise date-time capture.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when only a date is needed -- use DateInput instead.</li>\n<li>Do not use when only a time is needed -- use TimeInput instead.</li>\n<li>Do not use when a single combined field is preferred and no \"Now\" button is needed -- use DateTimeLocalInput instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;div&gt;</code> wrapping a native <code>&lt;input type=\"date\"&gt;</code>, <code>&lt;input type=\"time\"&gt;</code>, and <code>&lt;button type=\"button\"&gt;</code> with accessible labeling via <code>aria-label</code> on each element, two-way value binding for date and time, and a \"Now\" button that sets both to the current local date and time. The consumer provides all visual styling, layout, and any custom validation feedback.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.date-time-now-input</code> class for targeting. No default styles are included -- this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a wrapper <code>&lt;div&gt;</code> with class <code>date-time-now-input</code></li>\n<li>Verify it contains an <code>&lt;input type=\"date\"&gt;</code> with <code>aria-label</code></li>\n<li>Verify it contains an <code>&lt;input type=\"time\"&gt;</code> with <code>aria-label</code></li>\n<li>Verify it contains a <code>&lt;button type=\"button\"&gt;</code> with <code>aria-label</code></li>\n<li>Verify clicking the \"Now\" button sets date and time to current values</li>\n<li>Verify <code>disabled</code> and <code>required</code> attributes propagate correctly</li>\n<li>Verify pass-through attributes are applied to the wrapper <code>&lt;div&gt;</code></li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Place the \"Now\" button adjacent to the inputs so users can easily discover it. Consider adding a clock icon to the button for visual clarity.</li>\n<li><strong>Developers</strong>: The \"Now\" button uses <code>new Date()</code> to get the current local date and time. The date format is <code>YYYY-MM-DD</code> and time format is <code>HH:mm</code> to match native input requirements.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>text-input</code> — a single-line text input field &lt;input type=\"text\"&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN date input: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/date</li>\n<li>MDN time input: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/time</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<label class=\"label\" for=\"demo-input\">Label</label><input class=\"date-time-now-input\" id=\"demo-input\" aria-label=\"an input for entering a date and time and \"now\" button <input type=\"date\"><input type=\"time\"><button type=\"button\">\" />";
  const svelteSource: string = "// In your Svelte component:\nimport DateTimeNowInput from \"lily-design-system-svelte-headless/components/DateTimeNowInput/DateTimeNowInput.svelte\";\n\n<DateTimeNowInput label=\"Label\" />\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Date Time Now Input — Lily Design System</title>
  <meta name="description" content="DateTimeNowInput is a headless component that wraps a native &lt;input type=&quot;date&quot;&gt;, &lt;input type=&quot;time&quot;&gt;, and a &lt;button type=&quot;button&quot;&gt; inside a &lt;div&gt;. The &quot;Now&quot; button sets both inputs to the current ..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

