<script lang="ts">
  const html: string = "<h1>Check List</h1>\n\n<p>A checklist is a semantic list container for checklist items, allowing users to track and manage tasks, options, or steps by marking items as completed or pending. Typically displayed as a list of actionable items, each with a checkbox that users can tick off as they progress, checklists are commonly used in to-do lists, forms, or onboarding processes.</p>\n\n<p>The component renders a <code>&lt;ul&gt;</code> with <code>role=\"list\"</code> and delegates item rendering to consumers, who provide <code>&lt;li&gt;</code> elements with checkboxes or other interactive controls. This headless approach lets consumers define all visual feedback such as checkmarks, color changes, or progress indicators through their own CSS.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a semantic <code>&lt;ul&gt;</code> element with explicit <code>role=\"list\"</code> to ensure list semantics are preserved across screen readers</li>\n<li>Supports an optional <code>aria-label</code> for an accessible name describing the checklist purpose</li>\n<li>Consumers provide <code>&lt;li&gt;</code> children with checkboxes or other controls</li>\n<li>Spreads <code>restProps</code> for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (default: undefined) -- accessible name for the checklist via <code>aria-label</code></li>\n<li><code>children</code>: slot (required) -- list items to render inside the <code>&lt;ul&gt;</code></li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Onboarding checklist with CheckListItem children:</p>\n\n<pre><code class=\"language-html\">&lt;CheckList label=\"Onboarding tasks\"&gt;\n    &lt;CheckListItem checked={accountCreated}&gt;Create account&lt;/CheckListItem&gt;\n    &lt;CheckListItem checked={profileSetUp}&gt;Set up profile&lt;/CheckListItem&gt;\n    &lt;CheckListItem&gt;Invite team members&lt;/CheckListItem&gt;\n&lt;/CheckList&gt;</code></pre>\n\n<p>Packing list with completed and pending items:</p>\n\n<pre><code class=\"language-html\">&lt;CheckList label=\"Packing list\"&gt;\n    &lt;CheckListItem checked={true}&gt;Passport&lt;/CheckListItem&gt;\n    &lt;CheckListItem checked={true}&gt;Boarding pass&lt;/CheckListItem&gt;\n    &lt;CheckListItem&gt;Charger&lt;/CheckListItem&gt;\n    &lt;CheckListItem&gt;Headphones&lt;/CheckListItem&gt;\n&lt;/CheckList&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive container. Keyboard interactions are determined by the consumer-provided child elements (e.g., checkboxes respond to Space).</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"list\"</code> -- explicit list role on the <code>&lt;ul&gt;</code> ensures assistive technologies treat it as a list</li>\n<li><code>aria-label</code> -- optional accessible name describing the purpose of the checklist</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for a list of items with checkboxes for tracking completion.</li>\n<li>Use for simple checklists without linked task pages, such as to-do lists or onboarding flows.</li>\n<li>Use when users need to see progress through a series of actionable items.</li>\n<li>Use for packing lists, grocery lists, or pre-flight safety checks.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for complex multi-session task tracking -- use TaskList instead.</li>\n<li>Do not use for boolean form selections that require submission -- use CheckboxGroup instead.</li>\n<li>Do not use for non-actionable informational lists -- use a standard <code>&lt;ul&gt;</code> or ContentsList instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless CheckList component provides a semantic <code>&lt;ul&gt;</code> with explicit <code>role=\"list\"</code> and optional <code>aria-label</code> for accessible identification. The consumer provides all visual styling including checkmark indicators, strikethrough effects, progress visualization, spacing, and color changes for completed items.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.check-list</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;ol&gt;</code> element with class <code>check-list</code></li>\n<li>Verify role=\"list\"<code> -- explicit list role on the </code>&lt;ul&gt;` ensures assistive technologies treat it as a list</li>\n<li>Verify aria-label` -- optional accessible name describing the purpose of the checklist</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Provide clear visual distinction between completed and pending items, such as strikethrough text or a filled checkmark. Show overall progress when appropriate.</li>\n<li><strong>Developers</strong>: Use CheckListItem components as children. Manage checkbox state externally and pass it to each item for consistent state tracking.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>CheckList follows the List/ListItem composition pattern. Use CheckList as the container with CheckListItem components as children. Each CheckListItem renders as an <code>&lt;li&gt;</code> and should contain a checkbox input and label. The CheckList manages the accessible list semantics while each CheckListItem manages its own item semantics.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>check-list-item</code> — one check list item component with a checkbox</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Listbox Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/listbox/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<ol class=\"check-list\"><li class=\"check-list-item\"><label><input type=\"checkbox\" /> Task 1</label></li><li class=\"check-list-item\"><label><input type=\"checkbox\" checked /> Task 2</label></li></ol>";
  const svelteSource: string = "// In your Svelte component:\nimport CheckList from \"lily-design-system-svelte-headless/components/CheckList/CheckList.svelte\";\n\n<CheckList>\n  <!-- list children -->\n</CheckList>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Check List — Lily Design System</title>
  <meta name="description" content="A checklist is a semantic list container for checklist items, allowing users to track and manage tasks, options, or steps by marking items as completed or pending. Typically displayed as a list of ..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

