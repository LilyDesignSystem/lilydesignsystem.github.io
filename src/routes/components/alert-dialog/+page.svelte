<script lang="ts">
  const html: string = "<h1>Alert Dialog</h1>\n\n<p>An alert dialog is a modal dialog that interrupts the user's workflow to communicate critical information and require a response. Unlike regular dialogs, alert dialogs demand immediate attention -- they are used for important warnings, confirmations of destructive actions, or error conditions that must be acknowledged before the user can continue.</p>\n\n<p>Alert dialogs are distinguished from standard dialogs by the <code>alertdialog</code> ARIA role, which signals to assistive technologies that the content requires the user's immediate attention. Common uses include confirming irreversible actions (e.g., \"Delete this item?\"), warning about unsaved changes, or displaying critical error messages that require acknowledgment.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses native HTML <code>&lt;dialog&gt;</code> element with the <code>open</code> attribute for visibility</li>\n<li><code>role=\"alertdialog\"</code> distinguishes from regular dialogs -- indicates critical content requiring acknowledgment</li>\n<li>two-way binding on the <code>open</code> prop for dialog visibility</li>\n<li>Conditionally rendered with conditional rendering to remove from DOM when closed</li>\n<li><code>aria-modal=\"true\"</code> signals modal behavior to assistive technologies</li>\n<li>Title rendered in a <code>&lt;p&gt;&lt;strong&gt;</code> element, referenced by <code>aria-labelledby</code></li>\n<li>Description rendered in a <code>&lt;p&gt;</code> element, referenced by <code>aria-describedby</code> when present</li>\n<li>Consumers are responsible for focus trapping and Escape key handling</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>open</code>: boolean (default: false, bindable) -- whether the dialog is visible</li>\n<li><code>title</code>: string (required) -- dialog heading, referenced by aria-labelledby</li>\n<li><code>description</code>: string (optional) -- dialog message, referenced by aria-describedby</li>\n<li><code>children</code>: slot -- action buttons and additional content</li>\n<li><code>...restProps</code>: Any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Destructive action confirmation:</p>\n\n<pre><code class=\"language-html\">&lt;button onclick={() =&gt; showDialog = true}&gt;Remove patient record&lt;/button&gt;\n\n&lt;AlertDialog\n  open={showDialog}\n  title=\"Remove patient record\"\n  description=\"This will permanently delete the patient record and all associated notes. This action cannot be undone.\"\n&gt;\n  &lt;button onclick={handleConfirm}&gt;Yes, remove record&lt;/button&gt;\n  &lt;button onclick={handleCancel}&gt;Cancel&lt;/button&gt;\n&lt;/AlertDialog&gt;</code></pre>\n\n<p>Unsaved changes warning:</p>\n\n<pre><code class=\"language-html\">&lt;AlertDialog\n  open={showUnsaved}\n  title=\"Unsaved changes\"\n  description=\"You have unsaved changes that will be lost if you leave this page.\"\n&gt;\n  &lt;button onclick={handleDiscard}&gt;Discard changes&lt;/button&gt;\n  &lt;button onclick={handleStay}&gt;Stay on page&lt;/button&gt;\n&lt;/AlertDialog&gt;</code></pre>\n\n<p>Simple acknowledgment without description:</p>\n\n<pre><code class=\"language-html\">&lt;AlertDialog open={showExpired} title=\"Session expired\"&gt;\n  &lt;button onclick={() =&gt; showExpired = false}&gt;OK&lt;/button&gt;\n&lt;/AlertDialog&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Escape: Closes the dialog (consumer must implement via onkeydown)</li>\n<li>Tab: Focus should be trapped within the dialog (consumer responsibility)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"alertdialog\"</code></li>\n<li><code>aria-modal=\"true\"</code></li>\n<li><code>aria-labelledby</code> references the title element</li>\n<li><code>aria-describedby</code> references the description element (when present)</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for urgent messages that require user acknowledgment before they can continue, such as \"Are you sure you want to delete this record?\"</li>\n<li>Use for destructive action confirmations where the consequence is irreversible or significant.</li>\n<li>Use when the dialog must trap focus and block interaction with the rest of the page until the user responds.</li>\n<li>Use when the ARIA <code>alertdialog</code> role is needed to signal critical content to assistive technologies.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for informational messages that do not need acknowledgment -- use Alert or Banner instead.</li>\n<li>Do not use for complex forms or multi-step workflows -- use Dialog instead.</li>\n<li>Do not use for non-modal overlays or contextual content -- use Popover or HoverCard instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides <code>role=\"alertdialog\"</code>, <code>aria-modal</code>, <code>aria-labelledby</code>, and <code>aria-describedby</code> attributes on a native <code>&lt;dialog&gt;</code> element with zero visual styling. The consumer is responsible for all CSS including backdrop overlay, dialog positioning, sizing, padding, button styles, and focus trap implementation.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.alert-dialog</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;dialog&gt;</code> element with class <code>alert-dialog</code></li>\n<li>Verify role=\"alertdialog\"</li>\n<li>Verify aria-labelledby` references the title element</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Ensure the dialog has a clear title, a concise description of the action, and distinguishable confirm/cancel buttons with appropriate visual weight (e.g., a destructive action button should appear more prominent and cautionary).</li>\n<li><strong>Developers</strong>: The consumer must implement focus trapping and Escape key handling. Set initial focus on the least destructive action button (e.g., \"Cancel\") to prevent accidental confirmations.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>dialog</code> — a modal or non-modal dialog window</li>\n<li><code>popconfirm-dialog</code> — a popover dialog with confirm and cancel buttons</li>\n<li><code>alert</code> — a status message for important information or feedback</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA alertdialog role: https://www.w3.org/TR/wai-aria-1.2/#alertdialog</li>\n<li>WAI-ARIA Alert and Message Dialogs Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/alertdialog/</li>\n</ul>";
</script>

<svelte:head>
  <title>Alert Dialog — Lily Design System</title>
  <meta name="description" content="An alert dialog is a modal dialog that interrupts the user's workflow to communicate critical information and require a response. Unlike regular dialogs, alert dialogs demand immediate attention --..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
