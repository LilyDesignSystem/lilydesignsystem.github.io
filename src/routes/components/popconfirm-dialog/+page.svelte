<script lang="ts">
  const html: string = "<h1>Popconfirm Dialog</h1>\n\n<p>A popconfirm dialog is a non-modal popover dialog with confirm and cancel buttons. It is anchored to a trigger element and uses <code>role=\"alertdialog\"</code> to announce the urgent confirmation prompt.</p>\n\n<p>Use it for inline confirmations such as delete-item, discard-changes, or remove-permission where a full modal dialog would be too disruptive.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div class=\"popconfirm-dialog\"&gt;</code> with <code>role=\"alertdialog\"</code> and <code>aria-modal=\"false\"</code></li>\n<li><code>title</code> is <strong>required</strong> and renders inside <code>&lt;h2 class=\"popconfirm-dialog-title\"&gt;</code></li>\n<li>Optional <code>description</code> renders inside <code>&lt;p class=\"popconfirm-dialog-description\"&gt;</code></li>\n<li><code>aria-labelledby</code> references the title id</li>\n<li><code>aria-describedby</code> references the description id (only when present)</li>\n<li><code>confirmLabel</code> and <code>cancelLabel</code> are <strong>required</strong></li>\n<li>Two <code>&lt;button type=\"button\"&gt;</code> elements: <code>popconfirm-dialog-cancel</code> and <code>popconfirm-dialog-confirm</code></li>\n<li>The container's <code>hidden</code> attribute reflects the <code>open</code> prop</li>\n<li>Consumer is responsible for anchoring the dialog near its trigger and managing focus</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>open</code>: boolean (default: false) -- whether the dialog is open</li>\n<li><code>title</code>: string (<strong>required</strong>) -- dialog heading</li>\n<li><code>description</code>: string (optional) -- body text</li>\n<li><code>confirmLabel</code>: string (<strong>required</strong>) -- confirm button text</li>\n<li><code>cancelLabel</code>: string (<strong>required</strong>) -- cancel button text</li>\n<li><code>onconfirm</code> / <code>onConfirm</code>: callback (optional)</li>\n<li><code>oncancel</code> / <code>onCancel</code>: callback (optional)</li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;PopconfirmDialog\n  open={isOpen}\n  title=\"Delete this item?\"\n  description=\"This action cannot be undone.\"\n  confirmLabel=\"Delete\"\n  cancelLabel=\"Cancel\"\n  onconfirm={handleDelete}\n  oncancel={handleCancel}\n/&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab / Shift+Tab: Move focus between cancel and confirm buttons</li>\n<li>Enter / Space: Activate the focused button</li>\n<li>Escape: Consumer should close the dialog and call <code>oncancel</code></li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"alertdialog\"</code> announces the urgent confirmation prompt</li>\n<li><code>aria-modal=\"false\"</code> reflects that popconfirm is not a modal</li>\n<li><code>aria-labelledby</code> references the title id</li>\n<li><code>aria-describedby</code> references the description id (when present)</li>\n<li>The container is <code>hidden</code> when <code>open</code> is false</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for inline confirmation of a destructive or non-reversible action.</li>\n<li>Use when the confirmation context is small and tightly coupled to the trigger.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for complex confirmations that need extra fields — use a Dialog or AlertDialog.</li>\n<li>Do not block the entire page — use AlertDialog for fully modal alerts.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>Renders semantic alertdialog markup with no styling. Consumer manages positioning relative to the trigger, focus management, escape handling, and the visual treatment.</p>\n\n<h2>Styles</h2>\n\n<p>Consumer CSS targets <code>popconfirm-dialog</code>, <code>popconfirm-dialog-title</code>, <code>popconfirm-dialog-description</code>, <code>popconfirm-dialog-cancel</code>, and <code>popconfirm-dialog-confirm</code>. Position the dialog adjacent to the trigger with a callout arrow if appropriate.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>popconfirm-dialog</code></li>\n<li>Verify <code>role=\"alertdialog\"</code> and <code>aria-modal=\"false\"</code> are set</li>\n<li>Verify the container is <code>hidden</code> when <code>open</code> is false</li>\n<li>Verify the title renders as an <code>&lt;h2&gt;</code> with class <code>popconfirm-dialog-title</code> and a stable id</li>\n<li>Verify <code>aria-labelledby</code> references the title id</li>\n<li>Verify <code>aria-describedby</code> references the description id only when description is provided</li>\n<li>Verify confirm and cancel buttons render with their labels and <code>type=\"button\"</code></li>\n<li>Verify confirm and cancel handlers fire on click</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Place the dialog near the trigger with a clear callout. Keep <code>title</code> short and the description specific.</li>\n<li><strong>Developers</strong>: Move focus to the cancel button (or confirm, depending on policy) when the dialog opens. Restore focus to the trigger when it closes.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>dialog</code> — a modal or non-modal dialog window</li>\n<li><code>alert-dialog</code> — a modal dialog for urgent messages requiring user acknowledgment</li>\n<li><code>popover</code> — a floating content box anchored to a trigger element</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Alert Dialog Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/alertdialog/</li>\n<li>Ant Design Popconfirm: https://ant.design/components/popconfirm</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"popconfirm-dialog\" aria-label=\"a popover dialog with confirm and cancel buttons\">PopconfirmDialog</div>";
  const svelteSource: string = "// In your Svelte component:\nimport PopconfirmDialog from \"lily-design-system-svelte-headless/components/PopconfirmDialog/PopconfirmDialog.svelte\";\n\n<PopconfirmDialog label=\"Dialog\" open>\n  <!-- dialog content -->\n</PopconfirmDialog>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Popconfirm Dialog — Lily Design System</title>
  <meta name="description" content="A popconfirm dialog is a non-modal popover dialog with confirm and cancel buttons. It is anchored to a trigger element and uses role=&quot;alertdialog&quot; to announce the urgent confirmation prompt." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

