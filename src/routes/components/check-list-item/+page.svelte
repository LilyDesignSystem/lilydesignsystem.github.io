<script lang="ts">
  const html: string = "<h1>CheckListItem</h1>\n\n<p>A check list item is a single item within a check list that includes a checkbox, allowing users to mark items as complete or incomplete. Check list items are commonly used in task lists, to-do lists, and checklists for tracking progress.</p>\n\n<p>This headless component uses a <code>&lt;li&gt;</code> element containing a checkbox input, providing inherent list semantics and accessible state management.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses <code>&lt;li&gt;</code> element for proper list semantics within a CheckList</li>\n<li>Contains a checkbox input for toggling completion state</li>\n<li><code>aria-checked</code> reflects the current checked state</li>\n<li>Label text is associated with the checkbox for accessibility</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>checked</code>: boolean (default: false) -- whether the item is checked</li>\n<li><code>disabled</code>: boolean (default: false) -- whether the checkbox is disabled</li>\n<li><code>label</code>: string (optional) -- accessible label override</li>\n<li><code>onchange</code>: callback (optional) -- change handler</li>\n<li><code>children</code>: slot -- item content</li>\n<li><code>...restProps</code>: Any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Checklist with completed, pending, and disabled items:</p>\n\n<pre><code class=\"language-html\">&lt;CheckList label=\"Daily tasks\"&gt;\n    &lt;CheckListItem checked={true}&gt;Buy groceries&lt;/CheckListItem&gt;\n    &lt;CheckListItem&gt;Clean the house&lt;/CheckListItem&gt;\n    &lt;CheckListItem disabled&gt;Optional task&lt;/CheckListItem&gt;\n&lt;/CheckList&gt;</code></pre>\n\n<p>Checklist item with change handler:</p>\n\n<pre><code class=\"language-html\">&lt;CheckListItem checked={isComplete} onchange={handleToggle}&gt;\n    Review pull request\n&lt;/CheckListItem&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus the checkbox</li>\n<li>Space: Toggle the checkbox state</li>\n<li>(Handled natively by the checkbox input)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Implicit <code>listitem</code> role from <code>&lt;li&gt;</code> element</li>\n<li><code>aria-checked</code> reflects the current state of the checkbox</li>\n<li>Label association via <code>&lt;label&gt;</code> element or <code>aria-label</code></li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside CheckList to represent one checkable item.</li>\n<li>Use when users need to mark items as complete or incomplete.</li>\n<li>Use for task tracking, progress checklists, or completion workflows.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside CheckList -- it requires a parent <code>&lt;ul&gt;</code> or <code>&lt;ol&gt;</code> context.</li>\n<li>Do not use for form checkboxes in a submission flow -- use CheckboxInput instead.</li>\n<li>Do not use for non-actionable informational list items -- use a standard <code>&lt;li&gt;</code> instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides a <code>&lt;li&gt;</code> element with an associated checkbox input and label, with zero visual styling. The consumer is responsible for all CSS including checkbox appearance, checked/unchecked states, strikethrough text, spacing, and disabled appearance.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.check-list-item</code> class for targeting. No default styles are included -- this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;li&gt;</code> element with class <code>check-list-item</code></li>\n<li>Verify the checkbox input is present and toggleable</li>\n<li>Verify <code>aria-checked</code> reflects the current state</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use visual indicators (strikethrough, opacity change) to distinguish checked from unchecked items. Maintain sufficient touch targets for the checkbox.</li>\n<li><strong>Developers</strong>: Ensure the checkbox is properly associated with its label text for screen reader accessibility.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>CheckListItem is used within a CheckList component.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>check-list</code> — an ordered list of check list item components</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/HTML/Element/li\">MDN li element</a></li>\n</ul>";
</script>

<svelte:head>
  <title>CheckListItem — Lily Design System</title>
  <meta name="description" content="A check list item is a single item within a check list that includes a checkbox, allowing users to mark items as complete or incomplete. Check list items are commonly used in task lists, to-do list..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
