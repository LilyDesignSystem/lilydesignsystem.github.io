<script lang="ts">
  const html: string = "<h1>Data Filter Form</h1>\n\n<p>A data filter component allows users to narrow down a dataset by applying criteria. It wraps filter controls in a semantic <code>&lt;form&gt;</code> element with <code>role=\"search\"</code> and accessible labelling via <code>aria-label</code>. Consumers provide their own filter inputs, submit buttons, and reset buttons as children.</p>\n\n<p>The component prevents default form submission behavior, allowing the consumer's <code>onsubmit</code> callback to handle filter application programmatically. This makes it suitable for client-side filtering, AJAX-based search, or any scenario where a full page reload is undesirable.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;form&gt;</code> element with <code>role=\"search\"</code> for assistive technology</li>\n<li>Calls <code>event.preventDefault()</code> on submit to allow consumer-controlled filter logic</li>\n<li>Passes <code>onreset</code> directly to the form for native reset behavior</li>\n<li>Spreads <code>restProps</code> onto the <code>&lt;form&gt;</code> for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name for the filter region, applied as <code>aria-label</code></li>\n<li><code>onsubmit</code>: (event: SubmitEvent) =&gt; void (default: undefined) -- callback when the filter form is submitted</li>\n<li><code>onreset</code>: (event: Event) =&gt; void (default: undefined) -- callback when the filter form is reset</li>\n<li><code>children</code>: slot (required) -- filter controls rendered inside the form</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;DataFilterForm label=\"Filter results\" onsubmit={applyFilters}&gt;\n  &lt;label&gt;Category &lt;select&gt;...&lt;/select&gt;&lt;/label&gt;\n  &lt;button type=\"submit\"&gt;Apply&lt;/button&gt;\n&lt;/DataFilterForm&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive container. Keyboard behavior is determined by the consumer-provided form controls within the children.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"search\"</code> -- identifies the form as a search landmark for assistive technology</li>\n<li><code>aria-label={label}</code> -- provides an accessible name for the search region</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use alongside DataTable to let users filter data by criteria such as date range, status, or category.</li>\n<li>Use when the dataset is large enough that filtering significantly improves findability.</li>\n<li>Use when filters should be submitted together as a group, with client-side or AJAX-based filtering rather than a full page reload.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for search across the entire site -- use SearchInput instead.</li>\n<li>Do not use for small datasets that fit on one page without filtering.</li>\n<li>Do not use for single-input search fields -- use SearchInput for a simpler pattern.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless DataFilterForm component provides a <code>&lt;form&gt;</code> with <code>role=\"search\"</code> and <code>aria-label</code> for semantic landmark identification, plus <code>preventDefault</code> on submit for consumer-controlled filter logic. The consumer provides all visual styling including filter input layout, submit/reset button appearance, spacing, and responsive behavior.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.data-filter-form</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;form&gt;</code> element with class <code>data-filter-form</code></li>\n<li>Verify role=\"search\"` -- identifies the form as a search landmark for assistive technology</li>\n<li>Verify aria-label={label}` -- provides an accessible name for the search region</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Group related filter controls visually and provide clear \"Apply\" and \"Reset\" actions. Show the active filter count or summary to indicate what is currently filtered.</li>\n<li><strong>Developers</strong>: Handle filtering in the <code>onsubmit</code> callback since default submission is prevented. Use <code>onreset</code> to clear filter state. Provide consumer form controls as children.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>form</code> — a form element for collecting and submitting user data</li>\n<li><code>data-table</code> — a data table interactive grid for displaying and sorting tabular data &lt;table&gt;</li>\n<li><code>search-input</code> — an input for entering a search query &lt;input type=\"search\"&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Landmark Roles: https://www.w3.org/WAI/ARIA/apd/practices/landmark-regions/</li>\n<li>ARIA search role: https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/Roles/search_role</li>\n</ul>";
</script>

<svelte:head>
  <title>Data Filter Form — Lily Design System</title>
  <meta name="description" content="A data filter component allows users to narrow down a dataset by applying criteria. It wraps filter controls in a semantic &lt;form&gt; element with role=&quot;search&quot; and accessible labelling via aria-label...." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
