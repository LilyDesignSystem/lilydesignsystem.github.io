<script lang="ts">
  const html: string = "<h1>TimerButton</h1>\n\n<p>A timer button is a button with a countdown timer that will automatically activate after a given amount of time. It is used for auto-advancing workflows, timed confirmations, or undo-with-timeout patterns.</p>\n\n<p>This headless component uses a <code>&lt;button&gt;</code> element with a visible countdown timer, providing both manual click activation and automatic timer-based activation.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses <code>&lt;button&gt;</code> element for semantic button behavior</li>\n<li>Countdown timer shown via <code>aria-live=\"polite\"</code> region</li>\n<li><code>aria-label</code> includes remaining time for screen readers</li>\n<li>Auto-activates when countdown reaches zero</li>\n<li>User can click to activate immediately before timer expires</li>\n<li>Timer can be cancelled by disabling or unmounting the component</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>duration</code>: number (required) -- countdown duration in seconds</li>\n<li><code>label</code>: string (required) -- accessible label describing the action</li>\n<li><code>disabled</code>: boolean (default: false) -- whether the button is disabled</li>\n<li><code>onclick</code>: callback (optional) -- handler called when activated (manually or by timer)</li>\n<li><code>children</code>: slot -- button content</li>\n<li><code>...restProps</code>: Any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;TimerButton duration={10} label=\"Undo delete\" onclick={handleUndo}&gt;\n  Undo deletion (10s remaining)\n&lt;/TimerButton&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;TimerButton duration={5} label=\"Auto-advance to next step\" onclick={handleNext}&gt;\n  Continuing to next step in 5s...\n&lt;/TimerButton&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;TimerButton duration={15} label=\"Auto-submit form\" onclick={handleSubmit}&gt;\n  Form will submit automatically in 15s\n&lt;/TimerButton&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus the button</li>\n<li>Enter: Activate the button immediately</li>\n<li>Space: Activate the button immediately</li>\n<li>Escape: Cancel the timer (optional behavior)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Implicit <code>button</code> role from <code>&lt;button&gt;</code> element</li>\n<li><code>aria-label</code> -- describes the action and remaining time</li>\n<li><code>aria-live=\"polite\"</code> region for countdown announcements</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for a button that automatically triggers after a countdown, such as auto-redirect or auto-submit after review.</li>\n<li>Use for undo-with-timeout patterns where an action auto-confirms after a delay.</li>\n<li>Use for auto-advancing wizards or slideshow controls.</li>\n<li>Use when users should have the option to click immediately or wait for the timer.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for countdown display only -- use Timer instead.</li>\n<li>Do not use for immediate actions without a delay -- use Button instead.</li>\n<li>Do not use for critical destructive actions without an undo mechanism -- use SliderButton or AlertDialog instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides a <code>&lt;button&gt;</code> element with countdown timer logic and ARIA live region for accessible time announcements, with zero visual styling. The consumer is responsible for all CSS including countdown display, progress indicator, timer animation, and button appearance.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.timer-button</code> class for targeting. No default styles are included -- this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;button&gt;</code> element with class <code>timer-button</code></li>\n<li>Verify <code>aria-label</code> is applied</li>\n<li>Verify countdown timer activates the button at zero</li>\n<li>Verify manual click activates the button immediately</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Show a visual countdown (progress bar, seconds display) so users know how much time remains. Provide a way to cancel or extend the timer.</li>\n<li><strong>Developers</strong>: Update <code>aria-label</code> with remaining time for screen reader users. Clean up timer intervals when the component unmounts or is cancelled.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>button</code> — a generic clickable button element</li>\n<li><code>timer</code> — a countdown or elapsed time display</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/HTML/Element/button\">MDN button element</a></li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<button class=\"timer-button\">Auto-click in 5s</button>";
  const svelteSource: string = "// In your Svelte component:\nimport TimerButton from \"lily-design-system-svelte-headless/components/TimerButton/TimerButton.svelte\";\n\n<TimerButton>TimerButton</TimerButton>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>TimerButton — Lily Design System</title>
  <meta name="description" content="A timer button is a button with a countdown timer that will automatically activate after a given amount of time. It is used for auto-advancing workflows, timed confirmations, or undo-with-timeout p..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

