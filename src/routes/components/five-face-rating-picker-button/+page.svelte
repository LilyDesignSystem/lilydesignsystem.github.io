<script lang="ts">
  const html: string = "<h1>Five Face Rating Picker Button</h1>\n\n<p>A five-face rating picker button is an individual button within a FiveFaceRatingPicker that represents one satisfaction level on a 1-5 scale. Each button corresponds to a face label such as \"Very bad\", \"Bad\", \"Okay\", \"Good\", or \"Very good\". When clicked, it selects that rating value. The component uses <code>aria-pressed</code> to indicate the currently selected state, making it accessible to screen readers and keyboard users.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as a <code>&lt;button&gt;</code> element with <code>aria-pressed</code> for toggle state</li>\n<li>Designed to be used inside a FiveFaceRatingPicker component</li>\n<li>Each button represents one of five satisfaction levels (1-5)</li>\n<li>The <code>selected</code> prop controls <code>aria-pressed</code> to indicate the active selection</li>\n<li>The <code>label</code> prop provides both the visible text and the accessible name via <code>aria-label</code></li>\n<li>Spreads <code>...restProps</code> onto the <code>&lt;button&gt;</code> element for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>value</code>: string (required) -- the rating value this button represents (e.g., \"1\", \"2\", \"3\", \"4\", \"5\")</li>\n<li><code>selected</code>: boolean (default: false) -- whether this button is currently selected</li>\n<li><code>label</code>: string (required) -- accessible label for the button (e.g., \"Very bad\", \"Bad\", \"Okay\", \"Good\", \"Very good\")</li>\n<li><code>onclick</code>: callback -- handler called when the button is clicked</li>\n<li><code>...restProps</code>: any -- additional HTML attributes spread onto the <code>&lt;button&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;FiveFaceRatingPicker label=\"How was your experience?\"&gt;\n    &lt;FiveFaceRatingPickerButton value=\"1\" label=\"Very bad\" onclick={handleClick} /&gt;\n    &lt;FiveFaceRatingPickerButton value=\"2\" label=\"Bad\" onclick={handleClick} /&gt;\n    &lt;FiveFaceRatingPickerButton value=\"3\" label=\"Okay\" onclick={handleClick} /&gt;\n    &lt;FiveFaceRatingPickerButton value=\"4\" label=\"Good\" selected onclick={handleClick} /&gt;\n    &lt;FiveFaceRatingPickerButton value=\"5\" label=\"Very good\" onclick={handleClick} /&gt;\n&lt;/FiveFaceRatingPicker&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus the button</li>\n<li>Enter / Space: Activate the button to select this rating</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-pressed</code> -- indicates whether this button is the currently selected rating (<code>true</code> when selected, <code>false</code> otherwise)</li>\n<li><code>aria-label</code> -- provides the accessible name for the button (e.g., \"Very good\")</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside FiveFaceRatingPicker to represent one satisfaction level in a 1-5 face rating scale.</li>\n<li>Use when each face rating option needs individual styling, icons, or interactive behavior.</li>\n<li>Use to provide custom face illustrations or emoji for each satisfaction level.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside of a FiveFaceRatingPicker -- it requires the parent picker for group semantics.</li>\n<li>Do not use as a standalone toggle button -- use ToggleButton instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;button&gt;</code> with <code>aria-pressed</code> to indicate the selected state, <code>aria-label</code> for the face description, and click handling for selection. The consumer provides all visual styling, face icons or emoji, and hover/focus effects.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.five-face-rating-picker-button</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;button&gt;</code> element with class <code>five-face-rating-picker-button</code></li>\n<li>Verify aria-label` -- provides the accessible name for the button (e.g., \"Very good\")</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Make the selected state visually distinct (e.g., highlighted border, increased size, or color change). Ensure each face button has enough tap target size for touch devices.</li>\n<li><strong>Developers</strong>: Pass the <code>selected</code> prop to control <code>aria-pressed</code>. Use the <code>onclick</code> handler to update the parent picker's value. The <code>value</code> prop carries the numeric rating for form submission.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>five-face-rating-picker</code> — a picker for selecting a 1-5 satisfaction rating using face labels</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Button Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/button/</li>\n</ul>";
</script>

<svelte:head>
  <title>Five Face Rating Picker Button — Lily Design System</title>
  <meta name="description" content="A five-face rating picker button is an individual button within a FiveFaceRatingPicker that represents one satisfaction level on a 1-5 scale. Each button corresponds to a face label such as &quot;Very b..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
