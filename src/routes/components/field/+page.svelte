<script lang="ts">
  const html: string = "<h1>Field</h1>\n\n<p>A field component wraps a form control with its label, description, and error message, providing proper labelling via <code>for</code> attribute linking and accessible error announcements. It serves as a generic form field container that works with any input type -- text inputs, selects, textareas, checkboxes, and more.</p>\n\n<p>The component auto-generates IDs for linking the label to its input and for associating description and error elements via ARIA attributes. A visual required indicator (asterisk) is rendered when <code>required</code> is true, hidden from screen readers with <code>aria-hidden</code>.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> wrapper containing a <code>&lt;label&gt;</code>, consumer-provided children, optional description <code>&lt;p&gt;</code>, and optional error <code>&lt;p&gt;</code></li>\n<li>Auto-generates a unique ID using derived value if <code>inputId</code> is not provided</li>\n<li>Derives <code>descId</code> and <code>errorId</code> from the field ID for ARIA linking</li>\n<li>The <code>&lt;label&gt;</code> uses <code>for={fieldId}</code> to link to the consumer's input element</li>\n<li>Required indicator asterisk uses <code>aria-hidden=\"true\"</code> to avoid screen reader noise</li>\n<li>Error paragraph uses <code>role=\"alert\"</code> for live announcement</li>\n<li>Sets <code>data-required</code> attribute on the wrapper div when <code>required</code> is true</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- visible label text</li>\n<li><code>description</code>: string (default: undefined) -- helper text displayed below the label</li>\n<li><code>error</code>: string (default: undefined) -- error message displayed below the input</li>\n<li><code>required</code>: boolean (default: false) -- whether the field is required, adds a visual asterisk</li>\n<li><code>inputId</code>: string (default: undefined) -- ID of the input to link the label to; auto-generated if omitted</li>\n<li><code>children</code>: slot (required) -- form control (input, select, text-area-input, etc.)</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Text input with description hint:</p>\n\n<pre><code class=\"language-html\">&lt;Field label=\"Full name\" description=\"Enter your first and last name\" required&gt;\n  &lt;TextInput label=\"Full name\" value={fullName} required /&gt;\n&lt;/Field&gt;</code></pre>\n\n<p>Email field with validation error:</p>\n\n<pre><code class=\"language-html\">&lt;Field label=\"Email address\" required error={emailError}&gt;\n  &lt;EmailInput label=\"Email address\" value={email} required /&gt;\n&lt;/Field&gt;</code></pre>\n\n<p>Optional field with helper text:</p>\n\n<pre><code class=\"language-html\">&lt;Field label=\"Notes\" description=\"Any additional information (optional)\"&gt;\n  &lt;TextAreaInput label=\"Notes\" value={notes} /&gt;\n&lt;/Field&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive container. Clicking the label focuses the associated input via the native <code>for</code> attribute behavior.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>&lt;label for={fieldId}&gt;</code> -- links the visible label to the input element</li>\n<li><code>role=\"alert\"</code> -- on the error paragraph, announces the error message to screen readers</li>\n<li><code>aria-hidden=\"true\"</code> -- on the required asterisk, prevents screen readers from reading the decorative indicator</li>\n<li><code>data-required</code> -- data attribute on the wrapper for consumer CSS styling</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to wrap a form input with its label, hint text, and error message.</li>\n<li>Use as the standard pattern for every form input inside a Form.</li>\n<li>Use when you need automatic ID generation and proper <code>&lt;label for&gt;</code> linking without manual wiring.</li>\n<li>Use with any input type: text inputs, selects, textareas, checkboxes, and more.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for groups of related inputs -- use Fieldset with a legend instead.</li>\n<li>Do not use for standalone labels without inputs.</li>\n<li>Do not use when a form control already has its own integrated label mechanism (such as DateField).</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;div&gt;</code> wrapper with a <code>&lt;label&gt;</code> linked via <code>for</code> attribute, optional description and error <code>&lt;p&gt;</code> elements with ARIA linking, a required indicator with <code>aria-hidden</code>, and <code>role=\"alert\"</code> on the error. The consumer provides all visual styling for the label, input, description, error display, and required indicator.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.field</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>field</code></li>\n<li>Verify role=\"alert\"` -- on the error paragraph, announces the error message to screen readers</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Position the label above or beside the input consistently across all fields. Use a subtle style for description text and a prominent style for error messages.</li>\n<li><strong>Developers</strong>: Pass the auto-generated or custom <code>inputId</code> to your input component's <code>id</code> attribute so the <code>&lt;label for&gt;</code> linking works. Use the <code>error</code> prop to conditionally show validation messages.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>Field composes with Label, input components (TextInput, Select, etc.), ErrorMessage, and Hint following the Form/Field/Input pattern. Field wraps a label, the consumer's input, an optional description, and an optional error message. Use inside a Form and group related Fields with Fieldset.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>form</code> — a form element for collecting and submitting user data</li>\n<li><code>fieldset</code> — a group of related form fields with a legend</li>\n<li><code>input</code> — a generic HTML input element</li>\n<li><code>label</code> — a label associated with a form input</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI Forms Tutorial: https://www.w3.org/WAI/tutorials/forms/</li>\n<li>WAI-ARIA Error Handling: https://www.w3.org/WAI/WCAG22/Techniques/aria/ARIA21</li>\n</ul>";
</script>

<svelte:head>
  <title>Field — Lily Design System</title>
  <meta name="description" content="A field component wraps a form control with its label, description, and error message, providing proper labelling via for attribute linking and accessible error announcements. It serves as a generi..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
