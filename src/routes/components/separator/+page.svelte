<script lang="ts">
  const html: string = "<h1>Separator</h1>\n\n<p>A separator is a UI/UX component that provides a visual and semantic divider between sections of content. It uses the native HTML <code>&lt;hr&gt;</code> element with an explicit <code>role=\"separator\"</code> for proper accessibility semantics. An optional label prop can be used to provide additional context about the separation for screen readers.</p>\n\n<p>Separators help organize content into logical groups and improve visual hierarchy without adding unnecessary complexity. They are commonly used between sections of a page, between groups of menu items, or between distinct areas of a form.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a single <code>&lt;hr&gt;</code> element with explicit <code>role=\"separator\"</code></li>\n<li>The <code>&lt;hr&gt;</code> element is a void element (no children or closing tag)</li>\n<li>Optional <code>aria-label</code> provides descriptive context for screen readers</li>\n<li>Supports rest props via <code>...restProps</code> for extensibility (e.g., <code>id</code>, <code>class</code>)</li>\n<li>The explicit <code>role=\"separator\"</code> reinforces the semantic meaning beyond the native <code>&lt;hr&gt;</code> role</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (default: undefined) -- optional accessible label describing the separation, applied via <code>aria-label</code></li>\n<li><code>...restProps</code>: spread onto the <code>&lt;hr&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Simple separator between content sections:</p>\n\n<pre><code class=\"language-html\">&lt;h2&gt;Introduction&lt;/h2&gt;\n&lt;p&gt;Welcome to the design system documentation.&lt;/p&gt;\n&lt;Separator /&gt;\n&lt;h2&gt;Getting Started&lt;/h2&gt;\n&lt;p&gt;Follow these steps to set up your project.&lt;/p&gt;</code></pre>\n\n<p>Labelled separator for screen reader context:</p>\n\n<pre><code class=\"language-html\">&lt;Separator label=\"End of introduction\" /&gt;</code></pre>\n\n<p>Separator between menu item groups:</p>\n\n<pre><code class=\"language-html\">&lt;Menu label=\"File actions\"&gt;\n  &lt;MenuItem&gt;New&lt;/MenuItem&gt;\n  &lt;MenuItem&gt;Open&lt;/MenuItem&gt;\n  &lt;MenuItem&gt;Save&lt;/MenuItem&gt;\n  &lt;Separator label=\"Destructive actions\" /&gt;\n  &lt;MenuItem&gt;Delete&lt;/MenuItem&gt;\n  &lt;MenuItem&gt;Archive&lt;/MenuItem&gt;\n&lt;/Menu&gt;</code></pre>\n\n<p>Separator dividing sidebar sections:</p>\n\n<pre><code class=\"language-html\">&lt;Sidebar label=\"Settings navigation\"&gt;\n  &lt;nav&gt;\n    &lt;a href=\"/settings/profile\"&gt;Profile&lt;/a&gt;\n    &lt;a href=\"/settings/account\"&gt;Account&lt;/a&gt;\n  &lt;/nav&gt;\n  &lt;Separator /&gt;\n  &lt;nav&gt;\n    &lt;a href=\"/settings/billing\"&gt;Billing&lt;/a&gt;\n    &lt;a href=\"/settings/invoices\"&gt;Invoices&lt;/a&gt;\n  &lt;/nav&gt;\n&lt;/Sidebar&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive container. The <code>&lt;hr&gt;</code> element is not focusable and does not receive keyboard input.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"separator\"</code> -- explicitly identifies the element as a separator between sections of content</li>\n<li><code>aria-label={label}</code> -- optional descriptive label for the separator, announced by screen readers when present</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to visually and semantically divide distinct sections of content, such as between page sections, form groups, or menu item groups.</li>\n<li>Use when you need assistive technologies to announce a content boundary.</li>\n<li>Use between groups of related items in a menu or list.</li>\n<li>Use to divide a sidebar into logical sections.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use purely for decorative spacing -- use CSS margins or padding instead.</li>\n<li>Do not use between every element -- only where semantic separation is meaningful.</li>\n<li>Do not use when a heading element would better introduce the new section.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component renders a semantic <code>&lt;hr&gt;</code> element with an explicit <code>role=\"separator\"</code> and optional <code>aria-label</code>. It provides the accessible separator semantics and leaves all visual styling -- thickness, color, margin, and orientation -- entirely to the consumer.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.separator</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;hr&gt;</code> element with class <code>separator</code></li>\n<li>Verify role=\"separator\"` -- explicitly identifies the element as a separator between sections of content</li>\n<li>Verify aria-label={label}` -- optional descriptive label for the separator, announced by screen readers when present</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use subtle styling that visually distinguishes sections without drawing excessive attention. Maintain consistent separator weight and color throughout the interface.</li>\n<li><strong>Developers</strong>: Provide an <code>aria-label</code> when the purpose of the separation is not obvious from the surrounding content. Avoid nesting separators inside interactive widgets.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>divider</code> — see components.tsv</li>\n<li><code>horizontal-scroller</code> — see components.tsv</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Separator Role: https://www.w3.org/TR/wai-aria-1.2/#separator</li>\n<li>MDN hr element: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/hr</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<hr class=\"separator\" />";
  const svelteSource: string = "// In your Svelte component:\nimport Separator from \"lily-design-system-svelte-headless/components/Separator/Separator.svelte\";\n\n<Separator>\n  <!-- content -->\n</Separator>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Separator — Lily Design System</title>
  <meta name="description" content="A separator is a UI/UX component that provides a visual and semantic divider between sections of content. It uses the native HTML &lt;hr&gt; element with an explicit role=&quot;separator&quot; for proper accessibi..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

