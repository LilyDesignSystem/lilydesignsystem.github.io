<script lang="ts">
  const html: string = "<h1>Expander</h1>\n\n<p>An expander shows or hides additional content with a toggle button control. It is similar to a native <code>&lt;details&gt;</code>/<code>&lt;summary&gt;</code> disclosure element but uses a <code>&lt;button&gt;</code> trigger with <code>aria-expanded</code> for full control over behavior and presentation.</p>\n\n<p>This component is useful for progressive disclosure patterns such as FAQ sections, collapsible panels, settings groups, or any interface where content should be hidden by default and revealed on demand. The expanded state is bindable, allowing parent components to programmatically control visibility.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> wrapper containing a <code>&lt;button&gt;</code> toggle and a conditional content region</li>\n<li>The button uses <code>aria-expanded</code> and <code>aria-controls</code> to communicate state to assistive technologies</li>\n<li>The content region uses <code>role=\"region\"</code> with <code>aria-label</code> matching the button label</li>\n<li>A unique <code>contentId</code> is generated using <code>Math.random()</code> for the <code>aria-controls</code>/<code>id</code> link</li>\n<li>Supports two-way binding on the <code>expanded</code> prop</li>\n<li>Content is conditionally rendered with conditional rendering, removing it from the DOM when collapsed</li>\n<li>Uses slot for the <code>children</code> prop</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- button text and accessible name for both the button and the content region</li>\n<li><code>expanded</code>: boolean (default: false) -- bindable boolean controlling whether the content is visible</li>\n<li><code>children</code>: slot (required) -- expandable content rendered when expanded is true</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the outer <code>&lt;div&gt;</code> wrapper</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;Expander summary=\"How to get your medical records\"&gt;\n  &lt;p&gt;You can see your GP records by:&lt;/p&gt;\n  &lt;ul&gt;\n    &lt;li&gt;asking for them at your GP surgery&lt;/li&gt;\n    &lt;li&gt;going online to see them if you have signed up for GP online services&lt;/li&gt;\n  &lt;/ul&gt;\n&lt;/Expander&gt;\n\n&lt;Expander summary=\"How to measure your blood glucose levels\"&gt;\n  &lt;p&gt;Testing your blood glucose levels involves a simple finger prick test\n  using a blood glucose monitor.&lt;/p&gt;\n&lt;/Expander&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;Expander label=\"Advanced settings\" expanded={showAdvanced}&gt;\n  &lt;form&gt;...&lt;/form&gt;\n&lt;/Expander&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Enter: Toggles the expander open/closed when the button has focus (native button behavior)</li>\n<li>Space: Toggles the expander open/closed when the button has focus (native button behavior)</li>\n<li>Tab: Moves focus to/from the toggle button</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-expanded={expanded}</code> -- on the button, indicates whether the controlled content is currently visible</li>\n<li><code>aria-controls={contentId}</code> -- on the button, references the id of the expandable content region</li>\n<li><code>role=\"region\"</code> -- on the content container, identifies the expandable area as a landmark region</li>\n<li><code>aria-label={label}</code> -- on the content region, provides an accessible name for the region</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to break complex, information-dense pages into expandable sections that users can open on demand.</li>\n<li>Use when the expandable content is relevant to a wide audience, not just a niche group.</li>\n<li>Use when multiple expanders on one page help users find the specific topic they need.</li>\n<li>Use only after testing whether the content works without an expander -- consider simplifying, splitting, or restructuring first.</li>\n<li>Use when you need full control over the toggle button behavior and styling beyond what native <code>&lt;details&gt;</code> provides.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when only some users need the information -- use Details for supplementary content aimed at a subset of users.</li>\n<li>Do not use for help text within forms -- use Hint or Details instead.</li>\n<li>Do not use on pages with other interactive elements like form inputs -- expanders may distract from the primary task.</li>\n<li>Do not use inside other patterns such as CareCard or WarningCallout.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;div&gt;</code> wrapper with a <code>&lt;button&gt;</code> toggle using <code>aria-expanded</code> and <code>aria-controls</code>, a conditionally rendered content <code>&lt;div&gt;</code> with <code>role=\"region\"</code>, and two-way binding on the <code>expanded</code> prop. The consumer provides all visual styling for the button, expanded content area, and any transition animations.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.expander</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>expander</code></li>\n<li>Verify role=\"region\"` -- on the content container, identifies the expandable area as a landmark region</li>\n<li>Verify aria-label={label}` -- on the content region, provides an accessible name for the region</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use a visual indicator (such as a chevron or plus/minus icon) on the toggle button to communicate expandability. Animate the expand/collapse transition for a polished feel.</li>\n<li><strong>Developers</strong>: The content region is removed from the DOM when collapsed, so any stateful children will reset. Use the bindable <code>expanded</code> prop to coordinate with parent component logic.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>details</code> — a disclosure widget that shows and hides content</li>\n<li><code>collapsible</code> — a container that can be expanded or collapsed</li>\n<li><code>accordion-list-item</code> — an accordion list item component</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Disclosure Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/disclosure/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"expander\"><button aria-expanded=\"true\">Show less</button><div>Expanded content visible here.</div></div>";
  const svelteSource: string = "// In your Svelte component:\nimport Expander from \"lily-design-system-svelte-headless/components/Expander/Expander.svelte\";\n\n<Expander>\n  <!-- content -->\n</Expander>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Expander — Lily Design System</title>
  <meta name="description" content="An expander shows or hides additional content with a toggle button control. It is similar to a native &lt;details&gt;/&lt;summary&gt; disclosure element but uses a &lt;button&gt; trigger with aria-expanded for full ..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

