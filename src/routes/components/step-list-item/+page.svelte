<script lang="ts">
  const html: string = "<h1>Step List Item</h1>\n\n<p>A step list item is one step in a step list with a status of waiting, in-progress, finished, or error. It uses a semantic <code>&lt;li&gt;</code> element and emits <code>data-status</code> so consumer CSS can render the visual indicator.</p>\n\n<h2>Composition</h2>\n\n<p>Step List Item is the child of Step List in the <code>*List</code>/<code>*ListItem</code> pattern. See StepList for full usage.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a semantic <code>&lt;li class=\"step-list-item\"&gt;</code> element</li>\n<li><code>data-status</code> reflects the <code>status</code> prop (default <code>\"waiting\"</code>)</li>\n<li><code>aria-current=\"step\"</code> is set when <code>current</code> is true</li>\n<li><code>aria-label</code> is set when the optional <code>label</code> prop is provided</li>\n<li>Children render the step's title and optional description</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>status</code>: \"waiting\" | \"in-progress\" | \"finished\" | \"error\" (default: \"waiting\") -- emitted as data-status</li>\n<li><code>current</code>: boolean (default: false) -- when true, renders aria-current=\"step\"</li>\n<li><code>label</code>: string (optional) -- aria-label override</li>\n<li><code>children</code>: slot -- step content (typically title and description)</li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;StepListItem status=\"in-progress\" current&gt;Address&lt;/StepListItem&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;StepListItem status=\"error\" label=\"Payment failed\"&gt;\n  &lt;strong&gt;Payment&lt;/strong&gt;\n  &lt;span&gt;Card declined&lt;/span&gt;\n&lt;/StepListItem&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>None at this level — keyboard behavior lives on any interactive children.</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>The <code>&lt;li&gt;</code> element provides list-item semantics</li>\n<li><code>aria-current=\"step\"</code> is set when <code>current</code> is true</li>\n<li><code>aria-label</code> is set when <code>label</code> is provided</li>\n<li><code>data-status</code> is a CSS hook for status-driven styling</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use as a child of StepList to mark a single stage.</li>\n<li>Use the <code>error</code> status to flag a step that requires correction.</li>\n<li>Use <code>current</code> for the user's active step.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside of a StepList — the <code>&lt;ol&gt;</code> parent provides correct list semantics.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>Renders a <code>&lt;li&gt;</code> with no styling. The status indicator (icon, color, connector line) and any visual layout are the consumer's responsibility.</p>\n\n<h2>Styles</h2>\n\n<p>Consumer CSS targets the <code>step-list-item</code> class. Use <code>data-status</code> to distinguish waiting/in-progress/finished/error visual states.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders an <code>&lt;li&gt;</code> element with class <code>step-list-item</code></li>\n<li>Verify <code>data-status</code> reflects the <code>status</code> prop (default <code>\"waiting\"</code>)</li>\n<li>Verify <code>aria-current=\"step\"</code> is set when <code>current</code> is true and absent when false</li>\n<li>Verify <code>aria-label</code> equals the <code>label</code> prop when provided</li>\n<li>Verify children render inside the list item</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Choose distinct visual treatments per status. Keep step labels short and the optional description specific.</li>\n<li><strong>Developers</strong>: Use the <code>error</code> status to signal a failed step. Pair <code>current</code> with the parent's <code>current</code> index for consistent ARIA and CSS state.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>step-list</code> — an ordered list of step items showing progress through a multi-step process</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA aria-current: https://www.w3.org/TR/wai-aria-1.2/#aria-current</li>\n<li>Ant Design Steps: https://ant.design/components/steps</li>\n</ul>";
</script>

<svelte:head>
  <title>Step List Item — Lily Design System</title>
  <meta name="description" content="A step list item is one step in a step list with a status of waiting, in-progress, finished, or error. It uses a semantic &lt;li&gt; element and emits data-status so consumer CSS can render the visual in..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
