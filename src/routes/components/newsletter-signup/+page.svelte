<script lang="ts">
  const html: string = "<h1>Newsletter Signup</h1>\n\n<p>A newsletter signup is a small <code>&lt;form&gt;</code> composition that collects an email address for subscription. It supports four explicit states — idle, submitting, success, and error — surfaced as <code>data-state</code> and reflected in the input/button disabled state and live-region messages.</p>\n\n<p>Use it in marketing footers, blog sidebars, splash pages, and dedicated subscription landing pages.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses a native <code>&lt;form&gt;</code> element with <code>aria-label</code> describing the form's purpose</li>\n<li><code>state</code> is exposed as <code>data-state</code> and gates <code>disabled</code> on the email input and submit button (when \"submitting\")</li>\n<li>Success message uses <code>role=\"status\"</code> + <code>aria-live=\"polite\"</code> so screen readers announce non-disruptive confirmation</li>\n<li>Error message uses <code>role=\"alert\"</code> so screen readers announce errors immediately</li>\n<li>Both messages are conditionally <code>hidden</code> based on <code>state</code></li>\n<li>All visible strings (heading, description, email label, submit label, success/error) are provided as props for i18n</li>\n<li>Submit handling is delegated to the consumer via <code>onSubmit</code> / <code>onsubmit</code></li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (<strong>required</strong>) -- aria-label for the form</li>\n<li><code>heading</code>: string (optional) -- visible heading</li>\n<li><code>description</code>: string (optional) -- body text below the heading</li>\n<li><code>emailLabel</code>: string (<strong>required</strong>) -- visible label for the email input</li>\n<li><code>emailPlaceholder</code>: string (optional)</li>\n<li><code>submitLabel</code>: string (<strong>required</strong>) -- submit button text</li>\n<li><code>state</code>: \"idle\" | \"submitting\" | \"success\" | \"error\" (default: \"idle\")</li>\n<li><code>successMessage</code>: string (optional) -- confirmation message when state is \"success\"</li>\n<li><code>errorMessage</code>: string (optional) -- error message when state is \"error\"</li>\n<li><code>onSubmit</code> / <code>onsubmit</code>: callback (optional) -- submit handler</li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Idle state with heading and description:</p>\n\n<pre><code class=\"language-html\">&lt;NewsletterSignup\n  label=\"Subscribe to the newsletter\"\n  heading=\"Stay in the loop\"\n  description=\"One email per week, no spam.\"\n  emailLabel=\"Email address\"\n  submitLabel=\"Subscribe\"\n  onsubmit={handleSubmit}\n/&gt;</code></pre>\n\n<p>Success state:</p>\n\n<pre><code class=\"language-html\">&lt;NewsletterSignup\n  label=\"Subscribe to the newsletter\"\n  emailLabel=\"Email address\"\n  submitLabel=\"Subscribe\"\n  state=\"success\"\n  successMessage=\"Thanks! Please check your inbox to confirm.\"\n/&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Move focus through the email input and submit button</li>\n<li>Enter inside the email input: Submit the form</li>\n<li>(All handled natively by <code>&lt;form&gt;</code>, <code>&lt;input&gt;</code>, and <code>&lt;button&gt;</code>)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label</code> on the <code>&lt;form&gt;</code> provides the form's accessible name</li>\n<li><code>data-state</code> reflects the current state (\"idle\" | \"submitting\" | \"success\" | \"error\")</li>\n<li>Email input and submit button are <code>disabled</code> when state is \"submitting\"</li>\n<li>Success message: <code>role=\"status\"</code> + <code>aria-live=\"polite\"</code> (non-disruptive)</li>\n<li>Error message: <code>role=\"alert\"</code> (immediately announced)</li>\n<li>Both messages use <code>hidden</code> when not in their corresponding state</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to collect newsletter subscriptions in marketing surfaces.</li>\n<li>Use whenever you need an explicit submit/loading/success/error progression for a single email field.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for multi-field signup or registration flows — use Form with multiple Field components.</li>\n<li>Do not use for in-app notification preferences — use a settings UI.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>Renders a <code>&lt;form&gt;</code> with <code>&lt;label&gt;</code> + <code>&lt;input type=\"email\"&gt;</code> + <code>&lt;button type=\"submit\"&gt;</code>, plus conditional success/error message paragraphs. Visual styling, layout direction (row/column), inline placement of messages, and submit logic are the consumer's responsibility.</p>\n\n<h2>Styles</h2>\n\n<p>Consumer CSS targets the <code>newsletter-signup</code> class on the form. Use the <code>data-state</code> attribute selector to style the form per state. Style the inner <code>newsletter-signup-success</code> and <code>newsletter-signup-error</code> classes for status messaging.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;form&gt;</code> element with class <code>newsletter-signup</code></li>\n<li>Verify <code>aria-label</code> is present and equals the <code>label</code> prop</li>\n<li>Verify <code>data-state</code> reflects the <code>state</code> prop</li>\n<li>Verify the email input and submit button are <code>disabled</code> when state is \"submitting\"</li>\n<li>Verify the success message renders with <code>role=\"status\"</code> + <code>aria-live=\"polite\"</code> and is hidden unless state is \"success\"</li>\n<li>Verify the error message renders with <code>role=\"alert\"</code> and is hidden unless state is \"error\"</li>\n<li>Verify the submit handler fires on form submission</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use a clear, single-line layout where possible. Reserve vertical space for the success and error messages so the layout does not jump.</li>\n<li><strong>Developers</strong>: Always supply <code>label</code>, <code>emailLabel</code>, and <code>submitLabel</code> (required). Do not announce success with <code>role=\"alert\"</code> — that is reserved for errors. Disable the submit while submitting to avoid duplicate calls.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>form</code> — a form element for collecting and submitting user data</li>\n<li><code>email-input</code> — an input for entering an email address &lt;input type=\"email\"&gt;</li>\n<li><code>call-to-action</code> — a prominent prompt encouraging user action</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA <code>role=\"status\"</code>: https://www.w3.org/TR/wai-aria-1.2/#status</li>\n<li>WAI-ARIA <code>role=\"alert\"</code>: https://www.w3.org/TR/wai-aria-1.2/#alert</li>\n<li>MDN <code>aria-live</code>: https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/Attributes/aria-live</li>\n<li>Mozilla Protocol Design System: https://protocol.mozilla.org/</li>\n</ul>";
</script>

<svelte:head>
  <title>Newsletter Signup — Lily Design System</title>
  <meta name="description" content="A newsletter signup is a small &lt;form&gt; composition that collects an email address for subscription. It supports four explicit states — idle, submitting, success, and error — surfaced as data-state a..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
