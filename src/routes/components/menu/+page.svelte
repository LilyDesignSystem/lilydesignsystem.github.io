<script lang="ts">
  const html: string = "<h1>Menu</h1>\n\n<p>A menu is a container for a list of actions or options, using the ARIA menu role with full keyboard navigation support. Menus are typically used for context menus, action menus triggered by buttons, and dropdown menus that present a set of commands or functions. Each item in the menu is a discrete action rather than a selectable option (for selection, use Listbox instead).</p>\n\n<p>The component manages vertical focus movement between menu items via arrow keys, with Home and End keys to jump to the first and last items. The consumer provides menu item elements as children and handles item activation externally.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> with <code>role=\"menu\"</code> and an accessible label.</li>\n<li>Uses reactive state to track the <code>menuRef</code> element reference for querying child items.</li>\n<li>Keyboard navigation queries all child elements with <code>[role='menuitem']</code> to build the navigable item list.</li>\n<li>Arrow keys wrap around: pressing ArrowDown on the last item moves focus to the first, and vice versa.</li>\n<li>Home and End keys jump to the first and last menu item respectively.</li>\n<li>Spreads <code>restProps</code> onto the container for consumer customization.</li>\n<li>This is a compound component -- pair with MenuItem for individual menu actions.</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name applied via <code>aria-label</code>.</li>\n<li><code>children</code>: slot (required) -- menu item elements to render inside the menu (should have <code>role=\"menuitem\"</code> and <code>tabindex=\"-1\"</code>).</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the container <code>&lt;div&gt;</code>.</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Actions menu with MenuItem children:</p>\n\n<pre><code class=\"language-html\">&lt;Menu label=\"Actions\"&gt;\n    &lt;MenuItem onclick={() =&gt; handleCut()}&gt;Cut&lt;/MenuItem&gt;\n    &lt;MenuItem onclick={() =&gt; handleCopy()}&gt;Copy&lt;/MenuItem&gt;\n    &lt;MenuItem onclick={() =&gt; handlePaste()}&gt;Paste&lt;/MenuItem&gt;\n&lt;/Menu&gt;</code></pre>\n\n<p>Dynamic actions menu from a list:</p>\n\n<pre><code class=\"language-html\">&lt;Menu label=\"File actions\"&gt;\n    {#each actions as action}\n        &lt;MenuItem onclick={() =&gt; handleAction(action)}&gt;\n            {action.label}\n        &lt;/MenuItem&gt;\n    {/each}\n&lt;/Menu&gt;</code></pre>\n\n<p>Menu with a disabled item:</p>\n\n<pre><code class=\"language-html\">&lt;Menu label=\"Edit\"&gt;\n    &lt;MenuItem onclick={() =&gt; handleUndo()}&gt;Undo&lt;/MenuItem&gt;\n    &lt;MenuItem aria-disabled=\"true\"&gt;Redo&lt;/MenuItem&gt;\n&lt;/Menu&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>ArrowDown: Moves focus to the next menu item, wrapping to the first item from the last.</li>\n<li>ArrowUp: Moves focus to the previous menu item, wrapping to the last item from the first.</li>\n<li>Home: Moves focus to the first menu item.</li>\n<li>End: Moves focus to the last menu item.</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"menu\"</code> -- identifies the container as a menu widget for presenting a list of actions.</li>\n<li><code>aria-label</code> -- provides an accessible name describing the purpose of the menu.</li>\n<li>Child elements should use <code>role=\"menuitem\"</code> with <code>tabindex=\"-1\"</code> for focusable menu items.</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for a list of actions or options triggered by a button, such as an actions menu on a record.</li>\n<li>Use when each item is a discrete action (e.g., Cut, Copy, Paste) rather than a selectable option.</li>\n<li>Use for action menus on table rows, card components, or toolbar overflow buttons.</li>\n<li>Use when the menu needs vertical arrow key navigation with wrapping.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for site navigation -- use NavigationMenu instead.</li>\n<li>Do not use for right-click menus -- use ContextMenu instead.</li>\n<li>Do not use for selecting one or more options from a list -- use Listbox instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides the ARIA menu role, keyboard navigation (arrow keys with wrapping, Home, End), and focus management for child menuitem elements. The consumer provides all visual styling, including menu positioning, item appearance, hover states, and animations.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.menu</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>menu</code></li>\n<li>Verify role=\"menu\"` -- identifies the container as a menu widget for presenting a list of actions.</li>\n<li>Verify aria-label` -- provides an accessible name describing the purpose of the menu.</li>\n<li>Verify Child elements should use <code>role=\"menuitem\"</code> with <code>tabindex=\"-1\"</code> for focusable menu items.</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Provide clear hover and focus indicators for menu items, and group related actions visually with separators when the menu is long.</li>\n<li><strong>Developers</strong>: Ensure each child element uses <code>role=\"menuitem\"</code> with <code>tabindex=\"-1\"</code>. Use the MenuItem component for consistent markup.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>Menu is a compound component that contains MenuItem children. Each MenuItem provides <code>role=\"menuitem\"</code> semantics, while Menu provides the <code>role=\"menu\"</code> container with keyboard navigation.</p>\n\n<pre><code>Menu (role=\"menu\") → MenuItem (role=\"menuitem\")</code></pre>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>menu-item</code> — one item in a menu</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Menu Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/menu/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"menu\" role=\"menu\"><div class=\"menu-item\" role=\"menuitem\">Action 1</div><div class=\"menu-item\" role=\"menuitem\">Action 2</div></div>";
  const svelteSource: string = "// In your Svelte component:\nimport Menu from \"lily-design-system-svelte-headless/components/Menu/Menu.svelte\";\n\n<Menu>\n  <!-- list children -->\n</Menu>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Menu — Lily Design System</title>
  <meta name="description" content="A menu is a container for a list of actions or options, using the ARIA menu role with full keyboard navigation support. Menus are typically used for context menus, action menus triggered by buttons..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

