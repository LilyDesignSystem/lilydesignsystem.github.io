<script lang="ts">
  const html: string = "<h1>Action Group</h1>\n\n<p>An action group is a row of related action buttons that can collapse to an overflow menu when horizontal space is constrained.</p>\n\n<p>Use it to surface a closely related set of bulk or context actions — formatting controls, view toggles, or shortcut commands — where the collection should be perceived as one logical unit. When space is tight, overflow items move into a menu behind a single trigger.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses a <code>&lt;div&gt;</code> with <code>role=\"group\"</code> and a required <code>aria-label</code></li>\n<li><code>overflow</code> is an optional slot/snippet/ReactNode for the overflow menu content (typically a <code>Menu</code> of hidden actions)</li>\n<li>The overflow trigger button renders only when <code>overflow</code> is provided</li>\n<li>The overflow trigger has <code>aria-haspopup=\"menu\"</code> and reflects open state via <code>aria-expanded</code></li>\n<li>The overflow menu container uses the <code>hidden</code> attribute when closed</li>\n<li>Visible action buttons are children — typically <code>Button</code>, <code>ActionBarButton</code>, or <code>ToggleButton</code> components</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (<strong>required</strong>) -- aria-label for the group</li>\n<li><code>overflowOpen</code>: boolean (default <code>false</code>) -- whether the overflow menu is open</li>\n<li><code>overflowLabel</code>: string (optional) -- aria-label for the overflow button (e.g., \"More actions\")</li>\n<li><code>onOverflowToggle</code> / <code>onoverflowtoggle</code>: callback (optional) -- overflow trigger click handler</li>\n<li><code>children</code>: slot -- the visible action buttons</li>\n<li><code>overflow</code>: slot/snippet/ReactNode (optional) -- content for the overflow menu</li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;ActionGroup\n  label=\"Text formatting\"\n  overflowOpen={open}\n  overflowLabel=\"More actions\"\n  onOverflowToggle={handleToggle}\n  overflow={&lt;Menu&gt;...&lt;/Menu&gt;}\n&gt;\n  &lt;Button label=\"Bold\"&gt;B&lt;/Button&gt;\n  &lt;Button label=\"Italic\"&gt;I&lt;/Button&gt;\n  &lt;Button label=\"Underline\"&gt;U&lt;/Button&gt;\n&lt;/ActionGroup&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Move focus into and out of the group</li>\n<li>Arrow keys: (Consumer-provided) roving tabindex among action buttons is recommended</li>\n<li>Enter / Space: Activate the focused action button or toggle the overflow menu</li>\n<li>Escape: (Consumer-provided) close the overflow menu when open</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"group\"</code> on the container</li>\n<li><code>aria-label</code> provides the accessible name (required)</li>\n<li>Overflow trigger has <code>aria-haspopup=\"menu\"</code> and <code>aria-expanded</code></li>\n<li>Overflow menu container is hidden via the <code>hidden</code> attribute when closed</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to group a small set of related actions that share context</li>\n<li>Use when overflow behavior is needed for narrow viewports</li>\n<li>Use to give a logical grouping its own accessible name</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for primary navigation — use a <code>NavigationMenu</code> or <code>TabBar</code></li>\n<li>Do not use for unrelated actions — they belong in separate groups or in a <code>ToolBar</code></li>\n<li>Do not use when only one or two actions are present and overflow is not needed</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>Action groups commonly contain <code>Button</code>, <code>ToggleButton</code>, or <code>ActionBarButton</code> children, and pair with a <code>Menu</code> for overflow content.</p>\n\n<pre><code class=\"language-html\">&lt;ActionGroup label=\"Alignment\" overflow={&lt;Menu&gt;...&lt;/Menu&gt;}&gt;\n  &lt;ToggleButton label=\"Left\"&gt;L&lt;/ToggleButton&gt;\n  &lt;ToggleButton label=\"Center\"&gt;C&lt;/ToggleButton&gt;\n  &lt;ToggleButton label=\"Right\"&gt;R&lt;/ToggleButton&gt;\n&lt;/ActionGroup&gt;</code></pre>\n\n<h2>Headless</h2>\n\n<p>Renders a <code>&lt;div role=\"group\"&gt;</code> with <code>aria-label</code> and optional overflow trigger plus overflow panel. Open/close state, positioning, animation, and visual treatment are entirely the consumer's responsibility.</p>\n\n<h2>Styles</h2>\n\n<p>Consumer CSS targets the <code>action-group</code> class. The overflow trigger uses <code>action-group-overflow-trigger</code> and the overflow menu uses <code>action-group-overflow-menu</code>. Provide a clear focus indicator on every interactive descendant.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> with <code>role=\"group\"</code> and class <code>action-group</code></li>\n<li>Verify <code>aria-label</code> equals the <code>label</code> prop</li>\n<li>Verify visible children are rendered before any overflow trigger</li>\n<li>Verify the overflow trigger renders only when <code>overflow</code> is provided</li>\n<li>Verify <code>aria-expanded</code> on the overflow trigger reflects <code>overflowOpen</code></li>\n<li>Verify the overflow panel uses <code>hidden</code> when <code>overflowOpen</code> is <code>false</code></li>\n<li>Verify <code>onOverflowToggle</code> fires when the overflow trigger is activated</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Establish a clear primary-to-secondary order; promote the most-used actions and demote infrequent actions to the overflow menu.</li>\n<li><strong>Developers</strong>: Manage <code>overflowOpen</code> state in the consumer; close it on outside click and on Escape.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>button-group</code> — a wrapper that groups related buttons together</li>\n<li><code>tool-bar</code> — a horizontal bar of tool actions</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Group: https://www.w3.org/TR/wai-aria-1.2/#group</li>\n<li>Adobe Spectrum Action Group: https://spectrum.adobe.com/page/action-group/</li>\n<li>WCAG 4.1.2 Name, Role, Value: https://www.w3.org/WAI/WCAG22/Understanding/name-role-value</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"action-group\" aria-label=\"a group of action buttons that can collapse to an overflow menu when space is constrained\">ActionGroup</div>";
  const svelteSource: string = "// In your Svelte component:\nimport ActionGroup from \"lily-design-system-svelte-headless/components/ActionGroup/ActionGroup.svelte\";\n\n<ActionGroup>\n  <!-- list children -->\n</ActionGroup>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Action Group — Lily Design System</title>
  <meta name="description" content="An action group is a row of related action buttons that can collapse to an overflow menu when horizontal space is constrained." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

