<script lang="ts">
  const html: string = "<h1>Pagination List Item</h1>\n\n<p>A pagination list item represents a single entry within a pagination navigation list. It renders as a semantic <code>&lt;li&gt;</code> element designed to be placed inside a PaginationList component (which provides the parent <code>&lt;nav&gt;</code> and <code>&lt;ul&gt;</code> structure). The item typically contains a link or button for a specific page number, previous/next controls, or an ellipsis indicator.</p>\n\n<p>Consumers are responsible for adding <code>aria-current=\"page\"</code> on the active page link and any additional ARIA attributes needed on the child elements.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as a semantic <code>&lt;li&gt;</code> element for use inside a PaginationList <code>&lt;ul&gt;</code> / <code>&lt;nav&gt;</code> structure</li>\n<li>Content is provided through the children slot, typically a link or button for a page number</li>\n<li>Passes through all additional HTML attributes via <code>...restProps</code></li>\n<li>Uses <code>slot</code> for children content</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>children</code>: slot (required) -- item content, typically a link (e.g., <code>&lt;a href=\"/page/2\"&gt;2&lt;/a&gt;</code>) or button</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>A pagination list item containing a PaginationLink for a numbered page:</p>\n\n<pre><code class=\"language-html\">&lt;PaginationListItem&gt;\n  &lt;PaginationLink href=\"/results?page=3\" label=\"Page 3\"&gt;3&lt;/PaginationLink&gt;\n&lt;/PaginationListItem&gt;</code></pre>\n\n<p>A pagination list item for a previous/next navigation link:</p>\n\n<pre><code class=\"language-html\">&lt;PaginationListItem&gt;\n  &lt;PaginationLink href=\"/conditions/diabetes/symptoms\" rel=\"prev\"&gt;\n    Previous: Symptoms\n  &lt;/PaginationLink&gt;\n&lt;/PaginationListItem&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None directly -- keyboard interaction is handled by the contained links or buttons.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Implicit <code>listitem</code> role from the semantic <code>&lt;li&gt;</code> element</li>\n<li>Consumer should set <code>aria-current=\"page\"</code> on the active page link within the item</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside PaginationList to represent one page link in the pagination sequence</li>\n<li>Use for both previous/next links and numbered page links</li>\n<li>Use when each pagination item needs semantic <code>&lt;li&gt;</code> structure within the parent list</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside of PaginationList -- it has no standalone meaning without the parent list structure</li>\n<li>Do not use for non-pagination list items -- use ContentsListItem or BreadcrumbListItem for other navigation patterns</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component renders a semantic <code>&lt;li&gt;</code> element with a children slot for page links or buttons. The consumer provides all visual styling, including link appearance, active-page highlighting, and spacing between items.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.pagination-list-item</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;li&gt;</code> element with class <code>pagination-list-item</code></li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Ensure each page item has a large enough click/touch target and consistent spacing for easy navigation.</li>\n<li><strong>Developers</strong>: Place <code>aria-current=\"page\"</code> on the link or button within the active page item, not on the <code>&lt;li&gt;</code> itself.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>PaginationListItem is the leaf component in the PaginationNav compound pattern. It is placed inside PaginationList as an <code>&lt;li&gt;</code> element.</p>\n\n<pre><code>PaginationNav → PaginationList → PaginationListItem (li) → link or button</code></pre>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>pagination-nav</code> — an ordered list of page navigation links</li>\n<li><code>pagination-list</code> — an ordered list of pagination list items</li>\n<li><code>pagination-link</code> — one pagination link in the trail</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/HTML/Element/li\">MDN li element</a></li>\n</ul>";
</script>

<svelte:head>
  <title>Pagination List Item — Lily Design System</title>
  <meta name="description" content="A pagination list item represents a single entry within a pagination navigation list. It renders as a semantic &lt;li&gt; element designed to be placed inside a PaginationList component (which provides t..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
