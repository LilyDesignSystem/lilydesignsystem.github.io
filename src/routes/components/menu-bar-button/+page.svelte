<script lang="ts">
  const html: string = "<h1>Menu Bar Button</h1>\n\n<p>A menu bar item is a single interactive item within a menu bar, using the ARIA menuitem role for keyboard navigation and screen reader support. It renders as a <code>&lt;div&gt;</code> with <code>role=\"menuitem\"</code> and is designed to be placed inside a MenuBar container with <code>role=\"menubar\"</code>.</p>\n\n<p>Focus management is delegated to the parent MenuBar, which uses roving tabindex to move focus between items with arrow keys. The item itself uses <code>tabindex=\"-1\"</code> so it is not reachable via Tab but can receive focus programmatically.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as a <code>&lt;div&gt;</code> with <code>role=\"menuitem\"</code> for ARIA menubar semantics</li>\n<li>Uses <code>tabindex=\"-1\"</code> so focus is managed programmatically by the parent MenuBar</li>\n<li>Content is provided through child elements</li>\n<li>Passes through all additional HTML attributes via <code>...restProps</code></li>\n<li>Designed to be placed inside a MenuBar container with <code>role=\"menubar\"</code></li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>children</code>: slot (required) -- content of the menu bar item</li>\n<li><code>...restProps</code>: Any additional HTML attributes passed to the <code>&lt;div&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Menu bar buttons inside a MenuBar:</p>\n\n<pre><code class=\"language-html\">&lt;MenuBar label=\"Application menu\"&gt;\n    &lt;MenuBarButton onclick={() =&gt; openFileMenu()}&gt;File&lt;/MenuBarButton&gt;\n    &lt;MenuBarButton onclick={() =&gt; openEditMenu()}&gt;Edit&lt;/MenuBarButton&gt;\n    &lt;MenuBarButton onclick={() =&gt; openViewMenu()}&gt;View&lt;/MenuBarButton&gt;\n&lt;/MenuBar&gt;</code></pre>\n\n<p>Menu bar button with submenu expansion:</p>\n\n<pre><code class=\"language-html\">&lt;MenuBarButton aria-haspopup=\"true\" aria-expanded={isFileMenuOpen}&gt;File&lt;/MenuBarButton&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Arrow keys: Focus movement is managed by the parent MenuBar</li>\n<li>Enter / Space: Activate the menu item (consumer-provided handler)</li>\n<li><code>tabindex=\"-1\"</code> enables programmatic focus from parent keyboard navigation</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"menuitem\"</code> -- identifies this as an item within a menubar</li>\n<li><code>tabindex=\"-1\"</code> -- removed from tab order; focus managed by parent MenuBar</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside MenuBar to represent one menu trigger.</li>\n<li>Use when each item needs <code>role=\"menuitem\"</code> semantics and roving tabindex focus management from the parent MenuBar.</li>\n<li>Use for top-level menu entries like File, Edit, View, or Help.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside MenuBar -- it relies on the parent for keyboard navigation.</li>\n<li>Do not use for tab selection -- use TabBarButton instead.</li>\n<li>Do not use for toolbar actions -- use ToolBarButton instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;div&gt;</code> with <code>role=\"menuitem\"</code> and <code>tabindex=\"-1\"</code> for roving focus managed by the parent MenuBar. The consumer provides all visual styling, including item appearance, hover states, active indicators, and submenu expansion affordances.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.menu-bar-button</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;button&gt;</code> element with class <code>menu-bar-button</code></li>\n<li>Verify role=\"menuitem\"` -- identifies this as an item within a menubar</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Ensure menu bar items have adequate click/touch target sizes and provide visual feedback on hover and focus.</li>\n<li><strong>Developers</strong>: Do not set <code>tabindex=\"0\"</code> on individual items; the parent MenuBar manages focus via roving tabindex. Add <code>aria-haspopup</code> and <code>aria-expanded</code> if the item opens a submenu.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>MenuBarButton is a child component of MenuBar. The parent MenuBar provides <code>role=\"menubar\"</code> with horizontal keyboard navigation, while MenuBarButton provides <code>role=\"menuitem\"</code> for each entry.</p>\n\n<pre><code>MenuBar (role=\"menubar\") → MenuBarButton (role=\"menuitem\")</code></pre>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>menu-bar</code> — a horizontal bar of menu triggers</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Menu Bar Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/menubar/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<button class=\"menu-bar-button\" role=\"menuitem\">File</button>";
  const svelteSource: string = "// In your Svelte component:\nimport MenuBarButton from \"lily-design-system-svelte-headless/components/MenuBarButton/MenuBarButton.svelte\";\n\n<MenuBarButton>MenuBarButton</MenuBarButton>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Menu Bar Button — Lily Design System</title>
  <meta name="description" content="A menu bar item is a single interactive item within a menu bar, using the ARIA menuitem role for keyboard navigation and screen reader support. It renders as a &lt;div&gt; with role=&quot;menuitem&quot; and is des..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

