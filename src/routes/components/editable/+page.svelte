<script lang="ts">
  const html: string = "<h1>Editable</h1>\n\n<p>An editable component allows inline text editing. It displays a value as plain text that can be clicked or activated via keyboard to enter edit mode, where it becomes an input field. Pressing Enter confirms the edit, and pressing Escape cancels it, reverting to the original value.</p>\n\n<p>This headless component toggles between a display state (<code>&lt;span role=\"button\"&gt;</code>) and an edit state (<code>&lt;input type=\"text\"&gt;</code>). It maintains a draft value during editing so that cancellation restores the original text. Both the <code>value</code> and <code>editing</code> state support two-way binding.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Display mode renders a <code>&lt;span&gt;</code> with <code>role=\"button\"</code> and <code>tabindex=\"0\"</code> for keyboard activation</li>\n<li>Edit mode renders an <code>&lt;input type=\"text\"&gt;</code> bound to an internal draft value</li>\n<li>Supports two-way binding for both <code>value</code> and <code>editing</code> props</li>\n<li>Uses reactive state for the internal draft value during editing</li>\n<li>Enter key in edit mode calls <code>confirm()</code> which copies draft to value</li>\n<li>Escape key in edit mode calls <code>cancel()</code> which restores draft from value</li>\n<li>Display mode responds to click, Enter, and Space for activation</li>\n<li>When <code>disabled</code> is true, the span gets <code>tabindex=\"-1\"</code> and <code>aria-disabled</code></li>\n<li>Accepts <code>...restProps</code> for forwarding additional attributes</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>value</code>: string (default: \"\") -- current text value; bindable</li>\n<li><code>label</code>: string (required) -- accessible name for the editable control, applied via <code>aria-label</code></li>\n<li><code>editing</code>: boolean (default: false) -- whether the component is in edit mode; bindable</li>\n<li><code>disabled</code>: boolean (default: false) -- whether editing is disabled</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Inline-editable project name:</p>\n\n<pre><code class=\"language-html\">&lt;Editable label=\"Project name\" value={projectName} /&gt;</code></pre>\n\n<p>Editable status text with controlled edit state:</p>\n\n<pre><code class=\"language-html\">&lt;Editable label=\"Status\" value={statusText} editing={isEditing} /&gt;</code></pre>\n\n<p>Disabled editable field for read-only users:</p>\n\n<pre><code class=\"language-html\">&lt;Editable label=\"Owner\" value={ownerName} disabled={!canEdit} /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>In display mode:</p>\n\n<ul>\n<li>Enter: Activate edit mode</li>\n<li>Space: Activate edit mode</li>\n</ul>\n\n<p>In edit mode:</p>\n\n<ul>\n<li>Enter: Confirm the edit and return to display mode</li>\n<li>Escape: Cancel the edit and return to display mode</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides an accessible name in both display and edit modes</li>\n<li><code>role=\"button\"</code> -- on the display span, indicates it is an activatable element</li>\n<li><code>aria-disabled={disabled}</code> -- on the display span when disabled, communicates the disabled state</li>\n<li><code>tabindex=\"0\"</code> / <code>tabindex=\"-1\"</code> -- controls focusability based on disabled state</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for inline text that toggles between view and edit modes on click.</li>\n<li>Use for quick edits like renaming items, updating status text, or correcting short fields.</li>\n<li>Use when navigating to a separate edit page would be disruptive to the workflow.</li>\n<li>Use in data tables or lists where individual values need in-place editing.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for complex forms with multiple fields -- use Form with Field components instead.</li>\n<li>Do not use for multi-line editing -- use TextAreaInput instead.</li>\n<li>Do not use when the field should always be in edit mode -- use TextInput instead.</li>\n<li>Do not use when form submission semantics are needed -- use EditableForm instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a display mode (<code>&lt;span&gt;</code> with <code>role=\"button\"</code>) and edit mode (<code>&lt;input type=\"text\"&gt;</code>) with full keyboard support (Enter to confirm, Escape to cancel), draft value management, two-way binding on both <code>value</code> and <code>editing</code>, and disabled state handling. The consumer provides all visual styling for both display and edit modes.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.editable</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>editable</code></li>\n<li>Verify aria-label={label}` -- provides an accessible name in both display and edit modes</li>\n<li>Verify role=\"button\"` -- on the display span, indicates it is an activatable element</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Make the display text visually distinct from static text (e.g., with an underline or hover effect) so users know it is editable. Provide clear visual feedback when entering and exiting edit mode.</li>\n<li><strong>Developers</strong>: Use the <code>editing</code> bind to coordinate with other components or show save/cancel buttons. The component manages a draft value internally, so cancellation safely reverts changes.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>editable-form</code> — a form wrapper for inline editing of content</li>\n<li><code>text-input</code> — a single-line text input field &lt;input type=\"text\"&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Button Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/button/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"editable\" contenteditable=\"true\">Click to edit this text</div>";
  const svelteSource: string = "// In your Svelte component:\nimport Editable from \"lily-design-system-svelte-headless/components/Editable/Editable.svelte\";\n\n<Editable>\n  <!-- content -->\n</Editable>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Editable — Lily Design System</title>
  <meta name="description" content="An editable component allows inline text editing. It displays a value as plain text that can be clicked or activated via keyboard to enter edit mode, where it becomes an input field. Pressing Enter..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

