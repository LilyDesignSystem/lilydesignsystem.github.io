<script lang="ts">
  const html: string = "<h1>Product Card</h1>\n\n<p>A product card is a specialized <code>&lt;article&gt;</code> that displays a product's image, name, and price together with optional description, badges, and action buttons, presenting one product as a self-contained landmark for catalogs, search results, and recommendation strips.</p>\n\n<p>Use it for e-commerce listings, marketplace browse pages, recommendation modules, and any context where products are presented in a uniform card layout.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders an <code>&lt;article&gt;</code> with <code>class=\"product-card\"</code> so each card is a discrete landmark</li>\n<li><code>aria-label</code> defaults to the product name; consumers may override via <code>label</code></li>\n<li>Optional <code>imageUrl</code> renders an <code>&lt;img class=\"product-card-image\" alt={imageAlt}&gt;</code></li>\n<li>The header contains <code>&lt;h3 class=\"product-card-name\"&gt;{name}&lt;/h3&gt;</code> and <code>&lt;p class=\"product-card-price\"&gt;{price}&lt;/p&gt;</code></li>\n<li>Children render after the header for description, ratings, action buttons, etc.</li>\n<li>Price text is consumer-formatted — the component does not perform currency localization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>name</code>: string (required) -- product name</li>\n<li><code>price</code>: string (required) -- pre-formatted price text</li>\n<li><code>imageUrl</code>: string (optional) -- product image src</li>\n<li><code>imageAlt</code>: string (optional) -- product image alt text</li>\n<li><code>label</code>: string (optional) -- aria-label override (defaults to <code>name</code>)</li>\n<li><code>children</code>: slot -- additional content (description, actions)</li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Basic product card:</p>\n\n<pre><code class=\"language-html\">&lt;ProductCard\n  name=\"Premium notebook\"\n  price=\"$19.99\"\n  imageUrl=\"/img/notebook.jpg\"\n  imageAlt=\"Premium notebook with embossed cover\"\n/&gt;</code></pre>\n\n<p>Product card with description and actions:</p>\n\n<pre><code class=\"language-html\">&lt;ProductCard\n  name=\"Premium notebook\"\n  price=\"$19.99\"\n  imageUrl=\"/img/notebook.jpg\"\n  imageAlt=\"Premium notebook with embossed cover\"\n&gt;\n  &lt;p&gt;180 pages, dot-grid, hardcover.&lt;/p&gt;\n  &lt;FlexStack direction=\"row\"&gt;\n    &lt;Button&gt;Add to cart&lt;/Button&gt;\n    &lt;IconButton label=\"Save for later\"&gt;♡&lt;/IconButton&gt;\n  &lt;/FlexStack&gt;\n&lt;/ProductCard&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>None on the card itself.</li>\n<li>Focusable controls inside the card (e.g., add-to-cart button) follow their own keyboard behavior.</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>&lt;article&gt;</code> provides an implicit landmark</li>\n<li><code>aria-label</code> defaults to <code>name</code> and may be overridden by <code>label</code></li>\n<li>The image's <code>alt</code> text is the consumer's responsibility (pass via <code>imageAlt</code>)</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to present a single product in catalogs, search results, recommendation strips, and shopping cart summaries.</li>\n<li>Use when products are displayed in a uniform layout and each item has an image, name, and price.</li>\n<li>Use as a child of Grid or Masonry for product listings.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for non-product content (use Card or Tile).</li>\n<li>Do not use as a navigation tile (use Tile or a labeled link).</li>\n<li>Do not omit <code>imageAlt</code> when an image is provided — alt text is required for non-decorative images.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>Provides only the semantic structure (article, headings, image, slot for extras). Layout, sizing, badge styling, hover states, and any visual treatment are the consumer's responsibility.</p>\n\n<h2>Styles</h2>\n\n<p>Consumer CSS targets <code>.product-card</code>, <code>.product-card-image</code>, <code>.product-card-header</code>, <code>.product-card-name</code>, and <code>.product-card-price</code> for visual styling.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders an <code>&lt;article&gt;</code> element with class <code>product-card</code></li>\n<li>Verify <code>aria-label</code> defaults to <code>name</code> and is overridable by <code>label</code></li>\n<li>Verify the image renders only when <code>imageUrl</code> is provided, with the supplied <code>alt</code></li>\n<li>Verify the name renders inside <code>&lt;h3 class=\"product-card-name\"&gt;</code></li>\n<li>Verify the price renders inside <code>&lt;p class=\"product-card-price\"&gt;</code></li>\n<li>Verify children content is rendered after the header</li>\n</ul>\n\n<h2>Domain Knowledge</h2>\n\n<ul>\n<li>Pre-format <code>price</code> to include currency symbol, thousands separators, and decimal places appropriate to the user's locale.</li>\n<li>For sale or strike-through pricing, pass formatted text (e.g., <code>&lt;del&gt;$24.99&lt;/del&gt; $19.99</code>) via the slot or include it in <code>price</code>.</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Maintain a consistent aspect ratio for product images across cards. Keep the price visually prominent and the action button reachable on small screens.</li>\n<li><strong>Developers</strong>: Always supply <code>imageAlt</code>. For decorative product photos that duplicate the name, an empty <code>alt=\"\"</code> is acceptable, but if the image conveys information not in the name, write descriptive alt text.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>card</code> — a grouped content container with header, body, and footer areas</li>\n<li><code>feature-card</code> — a large content card with a prominent image positioned alongside or above the text</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN article element: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/article</li>\n<li>WCAG 1.1.1 Non-text Content: https://www.w3.org/WAI/WCAG22/Understanding/non-text-content</li>\n</ul>";
</script>

<svelte:head>
  <title>Product Card — Lily Design System</title>
  <meta name="description" content="A product card is a specialized &lt;article&gt; that displays a product's image, name, and price together with optional description, badges, and action buttons, presenting one product as a self-contained..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
