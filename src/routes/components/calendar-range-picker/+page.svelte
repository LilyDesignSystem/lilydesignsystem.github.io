<script lang="ts">
  const html: string = "<h1>Calendar Range Picker</h1>\n\n<p>A calendar range picker is a component that allows users to select a contiguous range of dates by interacting with a visual calendar grid. Commonly used in booking systems, travel planners, analytics dashboards, and reporting tools, the calendar range picker lets users define a start date and an end date by clicking or tapping on calendar cells.</p>\n\n<p>This headless component renders a <code>&lt;div&gt;</code> with <code>role=\"application\"</code> and an accessible label, providing a container for rich calendar-based date range selection. The consumer provides the calendar grid content, selection logic, and visual feedback as children.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> with <code>role=\"application\"</code> to support rich keyboard interactions beyond standard navigation</li>\n<li>Consumer provides all calendar grid rendering, date selection logic, and range highlighting</li>\n<li>The <code>role=\"application\"</code> signals to screen readers that the widget handles its own keyboard interactions</li>\n<li>Accepts <code>...restProps</code> for forwarding additional attributes to the container</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name describing the picker purpose, applied via <code>aria-label</code></li>\n<li><code>children</code>: slot (required) -- the calendar grid content including date cells and navigation controls</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;CalendarRangePicker label=\"Select holiday dates\"&gt;\n  &lt;CalendarTable label=\"April 2026\"&gt;\n    &lt;CalendarTableHead&gt;\n      &lt;CalendarTableRow&gt;\n        &lt;th&gt;Mon&lt;/th&gt;&lt;th&gt;Tue&lt;/th&gt;&lt;th&gt;Wed&lt;/th&gt;&lt;th&gt;Thu&lt;/th&gt;&lt;th&gt;Fri&lt;/th&gt;&lt;th&gt;Sat&lt;/th&gt;&lt;th&gt;Sun&lt;/th&gt;\n      &lt;/CalendarTableRow&gt;\n    &lt;/CalendarTableHead&gt;\n    &lt;CalendarTableBody&gt;\n      &lt;CalendarTableRow&gt;\n        &lt;CalendarTableTD selected&gt;6&lt;/CalendarTableTD&gt;\n        &lt;CalendarTableTD selected&gt;7&lt;/CalendarTableTD&gt;\n        &lt;CalendarTableTD selected&gt;8&lt;/CalendarTableTD&gt;\n        &lt;CalendarTableTD selected&gt;9&lt;/CalendarTableTD&gt;\n        &lt;CalendarTableTD selected&gt;10&lt;/CalendarTableTD&gt;\n        &lt;CalendarTableTD&gt;11&lt;/CalendarTableTD&gt;\n        &lt;CalendarTableTD&gt;12&lt;/CalendarTableTD&gt;\n      &lt;/CalendarTableRow&gt;\n    &lt;/CalendarTableBody&gt;\n  &lt;/CalendarTable&gt;\n&lt;/CalendarRangePicker&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;CalendarRangePicker label=\"Select reporting period\"&gt;\n  &lt;!-- consumer renders two side-by-side month grids with range highlighting --&gt;\n&lt;/CalendarRangePicker&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None built-in -- the consumer should implement date cell navigation (arrow keys), selection (Enter/Space), and range extension (Shift + arrow keys or Shift + click) as appropriate.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"application\"</code> -- indicates that the widget manages its own keyboard interactions, overriding standard screen reader navigation</li>\n<li><code>aria-label={label}</code> -- provides an accessible name for the calendar range picker</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for selecting a start and end date visually on a calendar grid</li>\n<li>Use when the date range is relative or the user needs visual context of the dates, such as booking systems, travel planners, or analytics dashboards</li>\n<li>Use when users benefit from seeing the span of selected days highlighted across the calendar</li>\n<li>Use when the interaction requires richer keyboard navigation than a simple date input</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when users know exact dates -- use two DateInput fields instead for a compact entry</li>\n<li>Do not use for single date selection -- use CalendarTable or DateInput</li>\n<li>Do not use when screen space is very limited -- use DateInput with a popover calendar instead</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides a <code>&lt;div&gt;</code> with <code>role=\"application\"</code> and <code>aria-label</code> as a container for calendar-based range selection, with zero visual styling. The consumer is responsible for all rendering of the calendar grid, date cells, range highlighting, navigation controls, and all CSS including layout, colors, selected state, and hover effects.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.calendar-range-picker</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>calendar-range-picker</code></li>\n<li>Verify role=\"application\"` -- indicates that the widget manages its own keyboard interactions, overriding standard screen reader navigation</li>\n<li>Verify aria-label={label}` -- provides an accessible name for the calendar range picker</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Clearly highlight the selected range with a distinct background color spanning from the start date to the end date. Show hover previews as the user moves toward the end date.</li>\n<li><strong>Developers</strong>: Implement keyboard navigation for date cells (arrow keys), selection (Enter/Space), and range extension (Shift+click or Shift+arrow). Since <code>role=\"application\"</code> overrides screen reader navigation, ensure all keyboard interactions are fully implemented.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>date-range</code> — a display of a start and end date range</li>\n<li><code>date-field</code> — a structured field for entering date components</li>\n<li><code>calendar-table</code> — a calendar table interactive grid for managing dates, days, etc. &lt;table&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Application Role: https://www.w3.org/TR/wai-aria-1.2/#application</li>\n<li>WAI-ARIA Grid Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/grid/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"calendar-range-picker\" aria-label=\"Date range\"><input type=\"date\" aria-label=\"Start date\" value=\"2026-01-01\" /> to <input type=\"date\" aria-label=\"End date\" value=\"2026-01-31\" /></div>";
  const svelteSource: string = "// In your Svelte component:\nimport CalendarRangePicker from \"lily-design-system-svelte-headless/components/CalendarRangePicker/CalendarRangePicker.svelte\";\n\n<CalendarRangePicker label=\"Picker\" />\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Calendar Range Picker — Lily Design System</title>
  <meta name="description" content="A calendar range picker is a component that allows users to select a contiguous range of dates by interacting with a visual calendar grid. Commonly used in booking systems, travel planners, analyti..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

