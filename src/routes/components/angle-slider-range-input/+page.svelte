<script lang="ts">
  const html: string = "<h1>Angle Slider Range Input</h1>\n\n<p>An angle slider range input is a specialized range input component for selecting an angle value, typically between 0 and 360 degrees. Built on the native HTML range input, it provides semantic ARIA slider attributes including value text that conveys the angle with a degree symbol to screen readers.</p>\n\n<p>Common uses include rotation controls, compass direction selectors, gradient angle pickers, and any interface where a circular or angular value needs to be selected by the user. The step property allows snapping to common increments like 15 or 45 degrees.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Built on native <code>&lt;input type=\"range\"&gt;</code> for inherent slider behavior</li>\n<li>Supports two-way binding for two-way value binding</li>\n<li><code>aria-valuetext</code> provides human-readable angle text (e.g., \"90\") for screen readers</li>\n<li>Customizable suffix via <code>valueTextSuffix</code> prop (default \"°\", can be \" degrees\" etc.)</li>\n<li>derived value computes valueText reactively from value and suffix</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>value</code>: number (default: 0, bindable) -- current angle in degrees</li>\n<li><code>min</code>: number (default: 0) -- minimum angle</li>\n<li><code>max</code>: number (default: 360) -- maximum angle</li>\n<li><code>step</code>: number (default: 1) -- step increment</li>\n<li><code>label</code>: string (required) -- accessible label</li>\n<li><code>disabled</code>: boolean (default: false)</li>\n<li><code>name</code>: string (optional) -- form field name</li>\n<li><code>id</code>: string (optional) -- element ID</li>\n<li><code>valueTextSuffix</code>: string (default: \"°\") -- suffix for aria-valuetext</li>\n<li><code>...restProps</code>: Any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Basic angle slider range input:</p>\n\n<pre><code class=\"language-html\">&lt;AngleSliderRangeInput label=\"Rotation\" value={angle} /&gt;\n&lt;p&gt;Angle: {angle} degrees&lt;/p&gt;</code></pre>\n\n<p>With step snapping (every 15 degrees):</p>\n\n<pre><code class=\"language-html\">&lt;AngleSliderRangeInput label=\"Direction\" value={direction} step={15} /&gt;</code></pre>\n\n<p>Custom range and suffix:</p>\n\n<pre><code class=\"language-html\">&lt;AngleSliderRangeInput\n  label=\"Tilt\"\n  value={tilt}\n  min={-90}\n  max={90}\n  valueTextSuffix=\" degrees\"\n/&gt;</code></pre>\n\n<p>Disabled slider:</p>\n\n<pre><code class=\"language-html\">&lt;AngleSliderRangeInput label=\"Locked rotation\" value={45} disabled /&gt;</code></pre>\n\n<p>Within a form:</p>\n\n<pre><code class=\"language-html\">&lt;form&gt;\n  &lt;AngleSliderRangeInput label=\"Gradient angle\" value={angle} name=\"gradient-angle\" id=\"gradient\" /&gt;\n&lt;/form&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Arrow Right/Up: Increase value by step</li>\n<li>Arrow Left/Down: Decrease value by step</li>\n<li>Home: Set to minimum</li>\n<li>End: Set to maximum</li>\n<li>(All handled natively by input[type=\"range\"])</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"slider\"</code> (implicit from input[type=\"range\"])</li>\n<li><code>aria-label</code> from label prop</li>\n<li><code>aria-valuemin</code>, <code>aria-valuemax</code>, <code>aria-valuenow</code></li>\n<li><code>aria-valuetext</code> for human-readable angle (e.g., \"90°\")</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for selecting an angle in degrees, such as rotation, compass direction, or gradient angle.</li>\n<li>Use when you need a slider with degree-specific semantics and screen reader value text (e.g., \"90 degrees\").</li>\n<li>Use for tilt adjustments, CSS gradient angle pickers, or image rotation controls.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for linear value ranges -- use RangeInput or Slider instead.</li>\n<li>Do not use for rotary value selection without degree semantics -- use Dial instead.</li>\n<li>Do not use for general numeric input without angular meaning -- use NumberInput instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides a native <code>&lt;input type=\"range\"&gt;</code> with ARIA slider attributes (<code>aria-valuetext</code> with degree formatting, <code>aria-label</code>, <code>aria-valuemin</code>, <code>aria-valuemax</code>) and zero visual styling. The consumer is responsible for all CSS including track appearance, thumb styling, tick marks, and any visual degree indicator or dial overlay.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.angle-slider-range-input</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;input&gt;</code> element with class <code>angle-slider-range-input</code></li>\n<li>Verify role=\"slider\"` (implicit from input[type=\"range\"])</li>\n<li>Verify aria-label` from label prop</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Consider pairing the slider with a visual degree indicator (circular dial or angle preview) so users can see the selected angle. Ensure the thumb has a minimum 44x44px touch target on mobile.</li>\n<li><strong>Developers</strong>: Use the <code>step</code> prop to snap to common increments (15, 45, 90 degrees) when fine-grained control is not needed. Test that <code>aria-valuetext</code> announcements are clear with screen readers.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>text-input</code> — a single-line text input field &lt;input type=\"text\"&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Slider Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/slider/</li>\n</ul>";
</script>

<svelte:head>
  <title>Angle Slider Range Input — Lily Design System</title>
  <meta name="description" content="An angle slider range input is a specialized range input component for selecting an angle value, typically between 0 and 360 degrees. Built on the native HTML range input, it provides semantic ARIA..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
