<script lang="ts">
  const html: string = "<h1>Info State</h1>\n\n<p>An info state is a composition of an optional illustration, a title, an optional description, and an optional action area used to communicate empty, informational, error, or success conditions in place of normal content.</p>\n\n<p>Use it whenever a region of the UI cannot show its usual content — empty search results, an error fetching data, an onboarding placeholder, or a success acknowledgment.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;section&gt;</code> with <code>role=\"status\"</code> so updates are announced politely by assistive technology</li>\n<li>Sets <code>data-level={level}</code> so consumer CSS can apply variant treatment</li>\n<li>Uses an <code>&lt;h2&gt;</code> for the title and <code>&lt;p&gt;</code> for the description</li>\n<li>Illustration and action are optional slots placed above the title and below the description respectively</li>\n<li>Default <code>aria-label</code> is the title; consumer may override via <code>label</code></li>\n<li>Children, when provided, become the entire body and replace the title/description/action layout</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>level</code>: \"info\" | \"empty\" | \"error\" | \"success\" (default: \"info\") -- variant</li>\n<li><code>title</code>: string (required) -- section heading text</li>\n<li><code>description</code>: string (optional) -- body text</li>\n<li><code>label</code>: string (optional) -- aria-label override (defaults to <code>title</code>)</li>\n<li><code>illustration</code>: slot (optional) -- visual element above the title</li>\n<li><code>action</code>: slot (optional) -- call-to-action below the description</li>\n<li><code>children</code>: slot (optional) -- replaces the structured layout entirely</li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Empty state with action:</p>\n\n<pre><code class=\"language-html\">&lt;InfoState\n  level=\"empty\"\n  title=\"No results found\"\n  description=\"Try adjusting your filters.\"\n&gt;\n  &lt;svg slot=\"illustration\"&gt;...&lt;/svg&gt;\n  &lt;Button slot=\"action\"&gt;Clear filters&lt;/Button&gt;\n&lt;/InfoState&gt;</code></pre>\n\n<p>Error state:</p>\n\n<pre><code class=\"language-html\">&lt;InfoState\n  level=\"error\"\n  title=\"Could not load orders\"\n  description=\"Please try again or contact support.\"\n&gt;\n  &lt;Button slot=\"action\" onclick={retry}&gt;Retry&lt;/Button&gt;\n&lt;/InfoState&gt;</code></pre>\n\n<p>Custom body via children slot:</p>\n\n<pre><code class=\"language-html\">&lt;InfoState level=\"info\" title=\"Welcome\"&gt;\n  &lt;p&gt;Anything you want here.&lt;/p&gt;\n&lt;/InfoState&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>None on the container itself</li>\n<li>Focusable controls inside the <code>action</code> slot follow their own keyboard behavior</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"status\"</code> on the section announces non-disruptive updates</li>\n<li><code>aria-label</code> falls back to the title when no label is provided</li>\n<li><code>data-level</code> exposes the variant for consumer CSS without changing semantics</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for empty results, errors, success acknowledgments, and informational placeholders.</li>\n<li>Use to give users a clear next step via the <code>action</code> slot.</li>\n<li>Use in place of an empty content region so the UI remains explanatory rather than blank.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for blocking modal dialogs (use AlertDialog).</li>\n<li>Do not use for transient toasts or alerts (use Toast or Alert).</li>\n<li>Do not use as a heavyweight error boundary — keep messaging concise.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>Provides only the semantic structure (<code>&lt;section&gt;</code> with role, heading, paragraph, illustration and action slots, data-level). All visual treatment, including variant colors, spacing, and illustration sizing, is the consumer's responsibility.</p>\n\n<h2>Styles</h2>\n\n<p>Consumer CSS targets <code>.info-state</code> and <code>.info-state[data-level=\"error\"]</code>, <code>[data-level=\"empty\"]</code>, <code>[data-level=\"success\"]</code>, <code>[data-level=\"info\"]</code> for variants.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;section&gt;</code> element with class <code>info-state</code></li>\n<li>Verify <code>role=\"status\"</code> is applied</li>\n<li>Verify <code>data-level</code> matches the <code>level</code> prop</li>\n<li>Verify the title renders inside an <code>&lt;h2&gt;</code></li>\n<li>Verify the description renders inside a <code>&lt;p&gt;</code> when provided</li>\n<li>Verify the illustration and action slots render when provided</li>\n<li>Verify <code>aria-label</code> defaults to <code>title</code> and can be overridden by <code>label</code></li>\n<li>Verify children replace the structured layout</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Pair each info state with a clear next-step action — empty states without guidance are dead ends. Differentiate <code>level</code> variants visually with color and iconography.</li>\n<li><strong>Developers</strong>: Use <code>level=\"error\"</code> for recoverable failures and provide a Retry action. Reserve children-slot replacement for cases that genuinely need a custom body.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>panel</code> — a generic content panel with optional heading</li>\n<li><code>loading</code> — a loading indicator, such as text, or image, or animation</li>\n<li><code>skeleton</code> — a placeholder loading animation for content</li>\n<li><code>alert</code> — a status message for important information or feedback</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA status role: https://www.w3.org/TR/wai-aria-1.2/#status</li>\n<li>Nielsen Norman empty state guidance: https://www.nngroup.com/articles/empty-state-interface-design/</li>\n</ul>";
</script>

<svelte:head>
  <title>Info State — Lily Design System</title>
  <meta name="description" content="An info state is a composition of an optional illustration, a title, an optional description, and an optional action area used to communicate empty, informational, error, or success conditions in p..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
