<script lang="ts">
  const html: string = "<h1>Search Input</h1>\n\n<p>A search input is a UI/UX component that provides a text field specifically for search queries. It uses the native HTML <code>&lt;input type=\"search\"&gt;</code> element, which provides semantic meaning and may trigger search-specific browser behaviors such as a clear button or search icon. The component includes aria-label for accessibility and supports bindable value for two-way data binding, making it easy to integrate with search functionality in any application.</p>\n\n<p>Search inputs are commonly used for site-wide search bars, filtering lists, and any scenario where users need to enter a search query. Mobile browsers may display a search-optimized keyboard layout when this input type is focused.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a single <code>&lt;input type=\"search\"&gt;</code> element for semantic search behavior</li>\n<li>Supports two-way binding for the <code>value</code> prop enabling two-way data binding with two-way <code>value</code> binding</li>\n<li>The <code>aria-label</code> prop provides an accessible name since there is no visible <code>&lt;label&gt;</code> element</li>\n<li>Supports rest props via <code>...restProps</code> for extensibility (e.g., <code>id</code>, <code>name</code>, <code>placeholder</code>, <code>class</code>)</li>\n<li>The search type may trigger browser-native clear buttons and search-specific behaviors</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name applied via <code>aria-label</code></li>\n<li><code>value</code>: string (default: \"\") -- current search text, bindable with two-way <code>value</code> binding</li>\n<li><code>required</code>: boolean (default: false) -- whether the field must be filled before form submission</li>\n<li><code>disabled</code>: boolean (default: false) -- whether the input is disabled</li>\n<li><code>...restProps</code>: spread onto the <code>&lt;input&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Site-wide search bar:</p>\n\n<pre><code class=\"language-html\">&lt;SearchInput label=\"Search the site\" value={query} placeholder=\"Search...\" /&gt;</code></pre>\n\n<p>Filtering a product list:</p>\n\n<pre><code class=\"language-html\">&lt;SearchInput label=\"Filter products\" value={productFilter} required /&gt;</code></pre>\n\n<p>Table search within an admin panel:</p>\n\n<pre><code class=\"language-html\">&lt;SearchInput label=\"Search patients\" value={patientSearch} placeholder=\"Name or ID...\" /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Escape: clears the search field (browser-native behavior for search inputs)</li>\n<li>Enter: submits the containing form (standard input behavior)</li>\n</ul>\n\n<p>Note: keyboard interactions are provided natively by the <code>&lt;input type=\"search\"&gt;</code> element.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides the accessible name for the search field since no visible label element is associated</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use SearchInput for site-wide search bars where users enter free-text queries.</li>\n<li>Use SearchInput for section-level filtering, such as searching within a table or list.</li>\n<li>Use SearchInput to benefit from browser-native search behaviors such as the Escape-to-clear shortcut and search-optimized mobile keyboards.</li>\n<li>Use SearchInput when a simple search field without a dedicated action button is sufficient.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use SearchInput for filtering within a visible dropdown list -- use Combobox instead.</li>\n<li>Do not use SearchInput for general text entry -- use TextInput instead.</li>\n<li>Do not use SearchInput when an explicit search button is needed alongside the input -- use TextInputWithSearch instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>The SearchInput headless component provides a native <code>&lt;input type=\"search\"&gt;</code> with <code>aria-label</code> for accessible naming and two-way value binding. The browser provides semantic search behavior and may show a native clear button. The consumer provides all visual styling, search icons, and result handling.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.search-input</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;input&gt;</code> element with class <code>search-input</code></li>\n<li>Verify aria-label={label}` -- provides the accessible name for the search field since no visible label element is associated</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Include a visible search icon or placeholder text to clearly indicate the field's purpose, and ensure the clear button (native or custom) is easily discoverable.</li>\n<li><strong>Developers</strong>: Debounce the search input value to avoid excessive search requests, and wrap the input in a <code>&lt;form&gt;</code> with <code>role=\"search\"</code> for full semantic search landmark support.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>text-input</code> — a single-line text input field &lt;input type=\"text\"&gt;</li>\n<li><code>text-input-with-search</code> — a single-line text input field &lt;input type=\"text\"&gt; with search capability</li>\n<li><code>combobox</code> — a text input combined with a dropdown list for filtering options</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Search Role: https://www.w3.org/WAI/ARIA/apg/patterns/search/</li>\n<li>MDN input type=\"search\": https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/search</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<label class=\"label\" for=\"demo-input\">Label</label><input class=\"search-input\" type=\"search\" id=\"demo-input\" aria-label=\"SearchInput\" />";
  const svelteSource: string = "// In your Svelte component:\nimport SearchInput from \"lily-design-system-svelte-headless/components/SearchInput/SearchInput.svelte\";\n\n<SearchInput label=\"Label\" />\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Search Input — Lily Design System</title>
  <meta name="description" content="A search input is a UI/UX component that provides a text field specifically for search queries. It uses the native HTML &lt;input type=&quot;search&quot;&gt; element, which provides semantic meaning and may trigge..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

