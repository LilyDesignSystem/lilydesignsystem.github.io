<script lang="ts">
  const html: string = "<h1>Progress</h1>\n\n<p>A progress bar visually indicates the completion status of a task or process using the native HTML <code>&lt;progress&gt;</code> element. It is commonly used for file uploads, form submissions, loading sequences, installation wizards, and any process where users benefit from knowing how much of a task has been completed.</p>\n\n<p>The component supports both determinate progress (with a known value and maximum) and indeterminate progress (when the value is undefined, indicating an ongoing process of unknown duration). The native <code>&lt;progress&gt;</code> element provides built-in semantic meaning and accessibility support.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a single <code>&lt;progress&gt;</code> element with no wrapper</li>\n<li>When <code>value</code> is undefined, the progress bar is indeterminate (the browser displays an animated or pulsing indicator)</li>\n<li>When <code>value</code> is a number, the progress bar shows determinate progress as a fraction of <code>max</code></li>\n<li>Uses <code>aria-label</code> for accessible naming</li>\n<li>Spreads <code>restProps</code> onto the progress element for consumer extensibility</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name applied via <code>aria-label</code></li>\n<li><code>value</code>: number (default: undefined) -- current progress value; when undefined, the bar is indeterminate</li>\n<li><code>max</code>: number (default: 100) -- maximum value representing 100% completion</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the progress element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>File upload with real-time percentage:</p>\n\n<pre><code class=\"language-html\">&lt;Progress label=\"Uploading report.pdf\" value={73} max={100} /&gt;</code></pre>\n\n<p>Multi-step onboarding wizard:</p>\n\n<pre><code class=\"language-html\">&lt;Progress label=\"Onboarding progress\" value={2} max={5} /&gt;</code></pre>\n\n<p>Indeterminate loading while fetching data:</p>\n\n<pre><code class=\"language-html\">&lt;Progress label=\"Loading patient records\" /&gt;</code></pre>\n\n<p>Download progress with byte-based maximum:</p>\n\n<pre><code class=\"language-html\">&lt;Progress label=\"Downloading update\" value={450} max={1024} /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive display element that does not accept user input.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides an accessible name describing what process the progress bar represents</li>\n<li>The <code>&lt;progress&gt;</code> element implicitly has <code>role=\"Progress\"</code> with <code>aria-valuenow</code>, <code>aria-valuemin</code>, and <code>aria-valuemax</code> attributes managed by the browser</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use Progress to show a horizontal bar indicating file upload completion percentage.</li>\n<li>Use Progress for multi-step form or wizard progress, such as \"Step 3 of 5\".</li>\n<li>Use Progress to indicate download or installation progress with a known total size.</li>\n<li>Use Progress without a <code>value</code> prop for indeterminate loading states where duration is unknown.</li>\n<li>Use Progress for background processing tasks where the server reports a completion percentage.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use Progress for indeterminate loading when the duration is unknown and no percentage is available -- use ProgressSpinner instead.</li>\n<li>Do not use Progress for scalar gauges such as disk usage or battery level -- use Meter instead.</li>\n<li>Do not use Progress when a circular indicator better fits the layout, such as dashboard widgets -- use ProgressCircle instead.</li>\n<li>Do not use Progress for page-level loading overlays -- use Loading instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>The Progress headless component provides a native <code>&lt;progress&gt;</code> element with <code>aria-label</code> for accessible naming, supporting both determinate and indeterminate states. The browser provides built-in semantics including <code>aria-valuenow</code>, <code>aria-valuemin</code>, and <code>aria-valuemax</code>. The consumer provides all visual styling for the progress bar appearance.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.progress</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;progress&gt;</code> element with class <code>progress</code></li>\n<li>Verify aria-label={label}` -- provides an accessible name describing what process the progress bar represents</li>\n<li>Verify The <code>&lt;progress&gt;</code> element implicitly has <code>role=\"Progress\"</code> with <code>aria-valuenow</code>, <code>aria-valuemin</code>, and <code>aria-valuemax</code> attributes managed by the browser</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Provide a clear visual distinction between determinate (filled bar) and indeterminate (animated/pulsing) states, and ensure the progress bar has sufficient color contrast.</li>\n<li><strong>Developers</strong>: Include a text fallback inside the <code>&lt;progress&gt;</code> element for older browsers, and update the <code>value</code> prop in real time during async operations to give users meaningful feedback.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>progress-bar</code> — a horizontal progress indicator</li>\n<li><code>progress-circle</code> — a circular progress indicator</li>\n<li><code>progress-spinner</code> — an indeterminate spinning progress indicator</li>\n<li><code>meter</code> — a gauge displaying a scalar value within a known range</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN progress element: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/progress</li>\n<li>WAI-ARIA Progress role: https://www.w3.org/WAI/ARIA/apg/patterns/meter/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<progress class=\"progress\" value=\"60\" max=\"100\" aria-label=\"Progress\">60%</progress>";
  const svelteSource: string = "// In your Svelte component:\nimport Progress from \"lily-design-system-svelte-headless/components/Progress/Progress.svelte\";\n\n<Progress>\n  <!-- content -->\n</Progress>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Progress — Lily Design System</title>
  <meta name="description" content="A progress bar visually indicates the completion status of a task or process using the native HTML &lt;progress&gt; element. It is commonly used for file uploads, form submissions, loading sequences, ins..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

