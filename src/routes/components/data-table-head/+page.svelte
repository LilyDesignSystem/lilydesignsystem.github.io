<script lang="ts">
  const html: string = "<h1>Data Table Head</h1>\n\n<p>A data table head is the header section of a data table, wrapping one or more rows that label the columns of the table. Typically contains a single row with <code>&lt;th&gt;</code> elements that identify each column. It is designed to be used inside a DataTable <code>&lt;table&gt;</code> structure.</p>\n\n<p>The component renders a <code>&lt;thead&gt;</code> element and passes through its children, which are expected to be DataTableRow or <code>&lt;tr&gt;</code> elements containing <code>&lt;th&gt;</code> header cells with appropriate <code>scope</code> attributes.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as a <code>&lt;thead&gt;</code> element for the header section of a data table</li>\n<li>Children should be one or more rows with <code>&lt;th scope=\"col\"&gt;</code> cells labeling table columns</li>\n<li>Designed to be used inside a DataTable <code>&lt;table&gt;</code> structure</li>\n<li>Spreads <code>restProps</code> onto the <code>&lt;thead&gt;</code> element for consumer customization</li>\n<li>No internal state -- purely a structural wrapper</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>children</code>: slot (required) -- header rows containing column labels</li>\n<li><code>...restProps</code>: Any additional HTML attributes passed to the <code>&lt;thead&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;DataTable caption=\"User accounts\"&gt;\n  &lt;DataTableHead&gt;\n    &lt;tr&gt;&lt;th scope=\"col\"&gt;Name&lt;/th&gt;&lt;th scope=\"col\"&gt;Email&lt;/th&gt;&lt;th scope=\"col\"&gt;Role&lt;/th&gt;&lt;/tr&gt;\n  &lt;/DataTableHead&gt;\n  &lt;DataTableBody&gt;...&lt;/DataTableBody&gt;\n&lt;/DataTable&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive container. Navigation within the table follows standard browser behavior.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Implicit <code>rowgroup</code> role from the <code>&lt;thead&gt;</code> element -- groups the header rows of the table</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside DataTable to provide the <code>&lt;thead&gt;</code> header section with column labels.</li>\n<li>Use to wrap one or more rows of <code>&lt;th&gt;</code> elements that describe the data columns below.</li>\n<li>Use to enable sticky header behaviour in scrollable tables.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside DataTable -- use TableHead, CalendarTableHead, GanttTableHead, or KanbanTableHead for their respective table types.</li>\n<li>Do not use for footer summary rows -- use DataTableFoot instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless DataTableHead component provides a semantic <code>&lt;thead&gt;</code> element with implicit <code>rowgroup</code> role for assistive technology. The consumer provides all visual styling including header background color, font weight, border separation from body rows, and sticky positioning.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.data-table-head</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;thead&gt;</code> element with class <code>data-table-head</code></li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Make header cells visually distinct from data cells with bold text, a contrasting background, or a bottom border. Consider sticky headers for long tables.</li>\n<li><strong>Developers</strong>: Use <code>&lt;th scope=\"col\"&gt;</code> for column headers within the DataTableRow children. For multi-row headers, use <code>scope</code> and <code>headers</code> attributes to maintain accessibility.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>DataTableHead is part of the DataTable composition pattern. It sits inside a DataTable and contains DataTableRow components with <code>&lt;th&gt;</code> header cells. The full hierarchy is DataTable &gt; DataTableHead/DataTableBody/DataTableFoot &gt; DataTableRow &gt; DataTableTD.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>data-table</code> — a data table interactive grid for displaying and sorting tabular data &lt;table&gt;</li>\n<li><code>data-table-body</code> — a data table interactive grid tbody for displaying and sorting tabular data &lt;tbody&gt;</li>\n<li><code>data-table-foot</code> — a data table interactive grid tfoot for displaying and sorting tabular data &lt;tfoot&gt;</li>\n<li><code>data-table-row</code> — a data table interactive grid row for displaying and sorting tabular data &lt;tr&gt;</li>\n<li><code>data-table-td</code> — a data table interactive grid data cell for displaying and sorting tabular data &lt;td&gt;</li>\n<li><code>data-table-th</code> — a data table interactive grid header cell for displaying and sorting tabular data &lt;th&gt;</li>\n<li><code>table</code> — a table with rows and columns &lt;table&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Table Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/table/</li>\n<li>WAI Tutorial on Tables: https://www.w3.org/WAI/tutorials/tables/</li>\n</ul>";
</script>

<svelte:head>
  <title>Data Table Head — Lily Design System</title>
  <meta name="description" content="A data table head is the header section of a data table, wrapping one or more rows that label the columns of the table. Typically contains a single row with &lt;th&gt; elements that identify each column...." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
