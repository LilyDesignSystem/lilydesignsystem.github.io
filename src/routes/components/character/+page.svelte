<script lang="ts">
  const html: string = "<h1>Character</h1>\n\n<p>A character component displays a single character (glyph, symbol, emoji, or letter) with proper accessibility semantics. When the character conveys meaning (like a checkmark for success or a warning symbol), it uses <code>role=\"img\"</code> with an <code>aria-label</code> so screen readers announce its purpose. When the character is purely decorative, the <code>decorative</code> prop hides it from assistive technologies.</p>\n\n<p>This component is useful for initials, status indicators, rating symbols, decorative flourishes, and any place where a single glyph needs clear accessible context.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as a <code>&lt;span&gt;</code> wrapper around the character content</li>\n<li>Uses <code>role=\"img\"</code> with <code>aria-label</code> for meaningful characters so assistive technology announces the character's purpose</li>\n<li>Uses <code>role=\"presentation\"</code> with <code>aria-hidden=\"true\"</code> for decorative characters to hide from screen readers</li>\n<li>Uses <code>slot</code> for children content</li>\n<li>Spreads <code>...restProps</code> on the root <code>&lt;span&gt;</code> element for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (optional) -- accessible description of the character for screen readers</li>\n<li><code>decorative</code>: boolean (default: false) -- when true, hides the character from assistive technology</li>\n<li><code>children</code>: slot (required) -- the character to display</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Meaningful characters with accessible labels:</p>\n\n<pre><code class=\"language-html\">&lt;Character label=\"Check mark\"&gt;✓&lt;/Character&gt;\n&lt;Character label=\"Warning symbol\"&gt;⚠&lt;/Character&gt;</code></pre>\n\n<p>Decorative character hidden from assistive technology:</p>\n\n<pre><code class=\"language-html\">&lt;Character decorative&gt;★&lt;/Character&gt;</code></pre>\n\n<p>Status indicator in a task list:</p>\n\n<pre><code class=\"language-html\">&lt;ul&gt;\n  &lt;li&gt;&lt;Character label=\"Complete\"&gt;✓&lt;/Character&gt; Set up repository&lt;/li&gt;\n  &lt;li&gt;&lt;Character label=\"In progress\"&gt;◐&lt;/Character&gt; Write documentation&lt;/li&gt;\n  &lt;li&gt;&lt;Character label=\"Not started\"&gt;○&lt;/Character&gt; Add tests&lt;/li&gt;\n&lt;/ul&gt;</code></pre>\n\n<p>Decorative drop cap at the start of a paragraph:</p>\n\n<pre><code class=\"language-html\">&lt;p&gt;\n  &lt;Character decorative class=\"drop-cap\"&gt;O&lt;/Character&gt;nce upon a time, in a design system far, far away...\n&lt;/p&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive display element.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"img\"</code> -- applied when the character is meaningful (decorative is false), indicating the span represents an image-like glyph</li>\n<li><code>role=\"presentation\"</code> -- applied when <code>decorative</code> is true, removing semantic meaning</li>\n<li><code>aria-label</code> -- provides the accessible name for meaningful characters; omitted when decorative</li>\n<li><code>aria-hidden=\"true\"</code> -- applied when <code>decorative</code> is true, hiding the element from the accessibility tree</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for displaying a single glyph, symbol, or character that needs accessible context, such as a checkmark for success or a warning symbol.</li>\n<li>Use for decorative symbols like stars or flourishes where you want to hide them from assistive technology.</li>\n<li>Use for status indicators represented by a single character, such as a bullet or arrow.</li>\n<li>Use for decorative initial caps or drop caps at the start of a paragraph.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for emoji -- use Emoji with an accessible name instead.</li>\n<li>Do not use for icons -- use Icon instead.</li>\n<li>Do not use for multi-character text strings -- use a standard <code>&lt;span&gt;</code> or text element instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless Character component provides the semantic <code>&lt;span&gt;</code> with conditional <code>role=\"img\"</code> and <code>aria-label</code> for meaningful characters, or <code>role=\"presentation\"</code> with <code>aria-hidden</code> for decorative ones. The consumer provides all visual styling including font size, color, spacing, and any decorative effects.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.character</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;span&gt;</code> element with class <code>character</code></li>\n<li>Verify role=\"img\"` -- applied when the character is meaningful (decorative is false), indicating the span represents an image-like glyph</li>\n<li>Verify role=\"presentation\"<code> -- applied when </code>decorative` is true, removing semantic meaning</li>\n<li>Verify aria-label` -- provides the accessible name for meaningful characters; omitted when decorative</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Ensure meaningful characters have sufficient size and color contrast to be legible. Consider pairing symbols with supporting text for clarity.</li>\n<li><strong>Developers</strong>: Always provide a descriptive <code>label</code> for meaningful characters. Set <code>decorative</code> to true for purely visual symbols that add no information.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>character-counter</code> — a counter showing remaining or used characters in a text field</li>\n<li><code>kbd</code> — show keyboard shortcuts and key combinations in a styled inline element</li>\n<li><code>emoji</code> — a single emoji character with accessible name</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/HTML/Element/span\">MDN span element</a></li>\n</ul>";
</script>

<svelte:head>
  <title>Character — Lily Design System</title>
  <meta name="description" content="A character component displays a single character (glyph, symbol, emoji, or letter) with proper accessibility semantics. When the character conveys meaning (like a checkmark for success or a warnin..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
