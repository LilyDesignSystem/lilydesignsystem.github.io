<script lang="ts">
  const html: string = "<h1>Medical Banner Box For Danger</h1>\n\n<p>MedicalBannerBoxForDanger is a headless component for displaying critical medical record danger information such as reactions, warnings, alarms, and other danger-level clinical alerts. It renders a <code>&lt;div&gt;</code> with <code>role=\"region\"</code>, <code>aria-label</code>, and <code>data-type=\"danger\"</code> for consumer styling.</p>\n\n<p>This component is useful in clinical interfaces, electronic health records, and patient summary screens where danger-level information must be prominently displayed and accessible.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> element with class <code>medical-banner-box-for-danger</code></li>\n<li>Uses <code>role=\"region\"</code> and <code>aria-label</code> for accessibility</li>\n<li>Uses <code>data-type=\"danger\"</code> attribute for consumer CSS targeting</li>\n<li>Uses <code>children</code> slot for flexible content rendering</li>\n<li>Spreads <code>restProps</code> onto the <code>&lt;div&gt;</code> element for consumer extensibility</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name applied via <code>aria-label</code></li>\n<li><code>children</code>: slot (required) -- the danger banner box content</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;div&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;MedicalBanner label=\"Patient summary\"&gt;\n  &lt;MedicalBannerBox&gt;\n    &lt;strong&gt;Sarah Mitchell&lt;/strong&gt; | NHS: 485 777 3456\n  &lt;/MedicalBannerBox&gt;\n  &lt;MedicalBannerBoxForDanger label=\"Allergies and reactions\"&gt;\n    &lt;p&gt;Penicillin -- Anaphylaxis&lt;/p&gt;\n    &lt;p&gt;Latex -- Skin rash&lt;/p&gt;\n  &lt;/MedicalBannerBoxForDanger&gt;\n&lt;/MedicalBanner&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;MedicalBannerBoxForDanger label=\"Critical warnings\"&gt;\n  &lt;p&gt;Fall risk -- High&lt;/p&gt;\n  &lt;p&gt;MRSA -- Positive&lt;/p&gt;\n&lt;/MedicalBannerBoxForDanger&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>None (passive layout element)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"region\"</code> -- establishes the box as a landmark for assistive technology</li>\n<li><code>aria-label={label}</code> -- provides an accessible name describing the danger information</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside MedicalBanner for danger-level information such as allergies, adverse drug reactions, warnings, or alarms</li>\n<li>Use to visually distinguish critical safety information from other banner content</li>\n<li>Use when clinicians need immediate visibility of life-threatening patient data</li>\n<li>Use for infection control alerts, fall risk warnings, or Do Not Resuscitate indicators</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for general warnings outside a clinical context -- use WarningCallout instead</li>\n<li>Do not use outside MedicalBanner -- it relies on the parent for landmark and live region semantics</li>\n<li>Do not use for routine clinical information -- use MedicalBannerBoxForAdvice for contacts, plans, and context</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;div&gt;</code> with <code>role=\"region\"</code>, <code>aria-label</code>, and <code>data-type=\"danger\"</code>. The consumer is responsible for all CSS styling including background color (typically red/danger), text styling, and layout.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.medical-banner-box-for-danger</code> class for targeting. No default styles are included -- this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>medical-banner-box-for-danger</code></li>\n<li>Verify <code>role=\"region\"</code> is present</li>\n<li>Verify <code>aria-label</code> is set from the <code>label</code> prop</li>\n<li>Verify <code>data-type=\"danger\"</code> is present</li>\n<li>Verify children content is rendered</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use a strong danger color (e.g., red background) to visually distinguish this from normal information. Ensure text contrast meets WCAG AAA requirements.</li>\n<li><strong>Developers</strong>: Always provide a meaningful <code>label</code> prop that describes the danger information category (e.g., \"Allergies and reactions\", \"Critical warnings\").</li>\n</ul>\n\n<h2>Domain Knowledge</h2>\n\n<p>In clinical systems, danger-level information includes drug allergies, adverse reactions, critical alerts, and safety warnings that must be immediately visible to clinicians. This information is typically displayed with red or high-contrast styling per clinical UI conventions.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>announcement-banner</code> — a banner highlighting important messages for all users</li>\n<li><code>banner</code> — a prominent message bar across the top of a page</li>\n<li><code>banner-box</code> — a banner box that is inside a banner component, using flexbox horizontal</li>\n<li><code>government-banner</code> — a banner identifying a website as belonging to a government, with an expandable details panel</li>\n<li><code>medical-banner</code> — a prominent message bar across the top of a page, with medical information</li>\n<li><code>medical-banner-box</code> — a medical banner box that is inside a medical-banner component, using flexbox horizontal, with medical information</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>NHS UK Design System: https://service-manual.nhs.uk/design-system</li>\n</ul>";
</script>

<svelte:head>
  <title>Medical Banner Box For Danger — Lily Design System</title>
  <meta name="description" content="MedicalBannerBoxForDanger is a headless component for displaying critical medical record danger information such as reactions, warnings, alarms, and other danger-level clinical alerts. It renders a..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
