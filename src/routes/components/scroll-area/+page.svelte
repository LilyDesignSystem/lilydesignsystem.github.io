<script lang="ts">
  const html: string = "<h1>Scroll Area</h1>\n\n<p>ScrollArea is a headless scrollable container that provides keyboard accessibility for overflowing content. It renders a focusable region so keyboard users can scroll through content using standard scrolling keys without needing a mouse.</p>\n\n<p>Use ScrollArea when you have content that may overflow its container, such as long lists, code blocks, chat messages, or data tables. The component provides the semantic structure and keyboard focus needed for accessible scrolling, while the consumer controls all visual styling including overflow behavior, dimensions, and scrollbar appearance.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> element with <code>role=\"region\"</code> to create an accessible landmark</li>\n<li>Sets <code>tabindex=\"0\"</code> to make the container focusable, enabling keyboard scrolling (arrow keys, Page Up/Down, Home/End)</li>\n<li>Consumer must apply CSS <code>overflow</code> and dimension properties to enable actual scrolling behavior</li>\n<li>Spreads <code>restProps</code> onto the div for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible label for the scrollable region, applied via <code>aria-label</code></li>\n<li><code>children</code>: slot (required) -- scrollable content to render inside the container</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;div&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Chat message history with vertical scroll:</p>\n\n<pre><code class=\"language-html\">&lt;ScrollArea label=\"Chat messages\" style=\"max-height: 400px; overflow: auto;\"&gt;\n  &lt;div class=\"message\"&gt;Hello, how can I help?&lt;/div&gt;\n  &lt;div class=\"message\"&gt;I need to update my address.&lt;/div&gt;\n  &lt;!-- more messages --&gt;\n&lt;/ScrollArea&gt;</code></pre>\n\n<p>Code block viewer with horizontal and vertical scroll:</p>\n\n<pre><code class=\"language-html\">&lt;ScrollArea label=\"Source code\" style=\"max-height: 300px; overflow: auto;\"&gt;\n  &lt;pre&gt;&lt;code&gt;function greet(name) { return `Hello, ${name}!`; }&lt;/code&gt;&lt;/pre&gt;\n&lt;/ScrollArea&gt;</code></pre>\n\n<p>Sidebar navigation with constrained height:</p>\n\n<pre><code class=\"language-html\">&lt;ScrollArea label=\"Navigation links\" style=\"height: 100vh; overflow: auto;\"&gt;\n  &lt;nav&gt;\n    &lt;a href=\"/dashboard\"&gt;Dashboard&lt;/a&gt;\n    &lt;a href=\"/patients\"&gt;Patients&lt;/a&gt;\n    &lt;a href=\"/settings\"&gt;Settings&lt;/a&gt;\n  &lt;/nav&gt;\n&lt;/ScrollArea&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Moves focus to the scroll area container</li>\n<li>Arrow Up / Arrow Down: Scrolls content vertically (native browser behavior when focused)</li>\n<li>Arrow Left / Arrow Right: Scrolls content horizontally (native browser behavior when focused)</li>\n<li>Page Up / Page Down: Scrolls content by one page (native browser behavior when focused)</li>\n<li>Home / End: Scrolls to the beginning or end of content (native browser behavior when focused)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"region\"</code> -- identifies the container as a landmark region so assistive technologies can announce it</li>\n<li><code>aria-label={label}</code> -- provides an accessible name describing what content is scrollable</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use ScrollArea for content that overflows its container, such as long lists, chat messages, code blocks, or data tables.</li>\n<li>Use ScrollArea when keyboard users need to scroll through content without a mouse via standard scrolling keys.</li>\n<li>Use ScrollArea to provide a custom scrollbar appearance consistent with your design system.</li>\n<li>Use ScrollArea for sidebar navigation panels or log viewers with constrained height.</li>\n<li>Use ScrollArea for horizontally scrolling content such as image carousels or timeline strips.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use ScrollArea for content that fits within its container -- unnecessary scroll regions add complexity for assistive technology users.</li>\n<li>Do not use ScrollArea when native page scrolling is sufficient and no custom scrollbar styling is needed.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>The ScrollArea headless component provides a <code>&lt;div&gt;</code> with <code>role=\"region\"</code>, <code>aria-label</code>, and <code>tabindex=\"0\"</code> for keyboard-focusable scrolling. The consumer provides all visual styling, including <code>overflow</code>, dimensions, and custom scrollbar appearance.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.scroll-area</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>scroll-area</code></li>\n<li>Verify role=\"region\"` -- identifies the container as a landmark region so assistive technologies can announce it</li>\n<li>Verify aria-label={label}` -- provides an accessible name describing what content is scrollable</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Indicate scrollable content with visible scrollbars, fade edges, or truncation cues so users know more content is available.</li>\n<li><strong>Developers</strong>: Apply CSS <code>overflow: auto</code> and explicit height or max-height to enable actual scrolling; the component provides semantics only.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>ScrollArea can contain a ScrollBar child for custom scrollbar rendering. Use <code>&lt;ScrollArea&gt;</code> as the scrollable container and nest <code>&lt;ScrollBar&gt;</code> within for styled scrollbar controls that sync with the scroll position.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>scroll-bar</code> — a custom scrollbar element</li>\n<li><code>container</code> — a generic block-level content container</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Region Role: https://www.w3.org/TR/wai-aria-1.2/#region</li>\n<li>WCAG 2.1 Reflow: https://www.w3.org/WAI/WCAG21/Understanding/reflow.html</li>\n</ul>";
</script>

<svelte:head>
  <title>Scroll Area — Lily Design System</title>
  <meta name="description" content="ScrollArea is a headless scrollable container that provides keyboard accessibility for overflowing content. It renders a focusable region so keyboard users can scroll through content using standard..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
