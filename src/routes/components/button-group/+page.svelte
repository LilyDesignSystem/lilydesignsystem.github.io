<script lang="ts">
  const html: string = "<h1>Button Group</h1>\n\n<p>A button group is a wrapper element that groups together related buttons, typically a primary action with one or more secondary actions, so that assistive technology can announce them as a single unit.</p>\n\n<p>Use it whenever a small set of buttons share a common purpose — Save and Cancel, Confirm and Discard, Previous and Next — and should be presented to screen readers as one group.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> with <code>role=\"group\"</code> so screen readers announce the buttons as a related set</li>\n<li><code>label</code> prop is <strong>required</strong> — it sets <code>aria-label</code> so the group has an accessible name</li>\n<li>Children are the related buttons; the component does not constrain how many or which type</li>\n<li>No interactive state of its own — it is a passive grouping container</li>\n<li>Pass-through attributes are forwarded to the root <code>&lt;div&gt;</code></li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (<strong>required</strong>) -- aria-label for the group</li>\n<li><code>children</code>: slot -- the related buttons</li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Basic group with a primary and secondary action:</p>\n\n<pre><code class=\"language-html\">&lt;ButtonGroup label=\"Form actions\"&gt;\n  &lt;Button type=\"submit\"&gt;Save&lt;/Button&gt;\n  &lt;Button type=\"button\"&gt;Cancel&lt;/Button&gt;\n&lt;/ButtonGroup&gt;</code></pre>\n\n<p>Pagination-style group:</p>\n\n<pre><code class=\"language-html\">&lt;ButtonGroup label=\"Pagination controls\"&gt;\n  &lt;Button&gt;Previous&lt;/Button&gt;\n  &lt;Button&gt;Next&lt;/Button&gt;\n&lt;/ButtonGroup&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab moves focus through the buttons in the group</li>\n<li>All key handling is delegated to the contained buttons</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"group\"</code> on the wrapping <code>&lt;div&gt;</code></li>\n<li><code>aria-label</code> provides the accessible name of the group</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use when two or more related buttons should be announced together.</li>\n<li>Use to visually and semantically associate confirmation and dismissal actions.</li>\n<li>Use in toolbars, form footers, and dialog action rows.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for a single button — wrap nothing.</li>\n<li>Do not use for navigation links — use a nav landmark instead.</li>\n<li>Do not use as a replacement for a <code>&lt;fieldset&gt;</code> of form inputs.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>Renders only structure: a <code>&lt;div role=\"group\" aria-label&gt;</code> with consumer-supplied buttons. The component supplies semantics and grouping; the consumer supplies visual layout (horizontal stack, vertical stack, gap spacing).</p>\n\n<h2>Styles</h2>\n\n<p>Consumer CSS targets the <code>button-group</code> class. Typical treatments include horizontal flex layout with consistent gap, optional reversed order on small screens, and matching visual weight between primary and secondary buttons.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> with class <code>button-group</code></li>\n<li>Verify <code>role=\"group\"</code> is present</li>\n<li>Verify <code>aria-label</code> is present and equals the <code>label</code> prop</li>\n<li>Verify children render inside the group</li>\n<li>Verify additional HTML attributes pass through to the root <code>&lt;div&gt;</code></li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Pair a primary button with at most two secondary buttons. Maintain consistent spacing across all groups in the product.</li>\n<li><strong>Developers</strong>: Always supply a meaningful <code>label</code> that describes the group's purpose, not the buttons themselves (e.g. \"Form actions\" rather than \"Save and Cancel\").</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>button</code> — a generic clickable button element</li>\n<li><code>action-group</code> — a group of action buttons that can collapse to an overflow menu when space is constrained</li>\n<li><code>segment-group</code> — a group of mutually exclusive segment options</li>\n<li><code>toggle-group</code> — a group of toggle buttons for selecting options</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>US Web Design System Button Group: https://designsystem.digital.gov/components/button-group/</li>\n<li>WAI-ARIA group role: https://www.w3.org/TR/wai-aria-1.2/#group</li>\n<li>WCAG 1.3.1 Info and Relationships: https://www.w3.org/WAI/WCAG22/Understanding/info-and-relationships</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"button-group\" aria-label=\"a wrapper that groups related buttons together\">ButtonGroup</div>";
  const svelteSource: string = "// In your Svelte component:\nimport ButtonGroup from \"lily-design-system-svelte-headless/components/ButtonGroup/ButtonGroup.svelte\";\n\n<ButtonGroup>\n  <!-- list children -->\n</ButtonGroup>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Button Group — Lily Design System</title>
  <meta name="description" content="A button group is a wrapper element that groups together related buttons, typically a primary action with one or more secondary actions, so that assistive technology can announce them as a single u..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

