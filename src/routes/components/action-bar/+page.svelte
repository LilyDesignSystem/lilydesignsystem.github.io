<script lang="ts">
  const html: string = "<h1>Action Bar</h1>\n\n<p>An action bar is a contextual toolbar that appears when items are selected, showing the current selection count alongside bulk action buttons.</p>\n\n<p>Use it above a list, table, gallery, or canvas where users can multi-select items and apply bulk operations such as delete, archive, move, or share. The action bar communicates how many items are affected and offers a clear way to dismiss the selection.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses a <code>&lt;div&gt;</code> with <code>role=\"toolbar\"</code> so assistive tech announces it as a grouping of actions</li>\n<li><code>label</code> prop is <strong>required</strong> and renders as <code>aria-label</code> on the toolbar</li>\n<li><code>selectedCount</code> is exposed via <code>data-selected-count</code> for consumer styling and tests</li>\n<li><code>selectedCountLabel</code> is consumer-supplied (pre-formatted, locale-aware)</li>\n<li>The clear-selection button renders only when both <code>clearSelectionLabel</code> and <code>onClearSelection</code> are provided</li>\n<li>Bulk action buttons are children — typically <code>action-bar-button</code> components</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (<strong>required</strong>) -- aria-label for the toolbar</li>\n<li><code>selectedCount</code>: number (<strong>required</strong>) -- count of currently selected items</li>\n<li><code>selectedCountLabel</code>: string (<strong>required</strong>) -- pre-formatted count text (e.g., \"3 selected\")</li>\n<li><code>clearSelectionLabel</code>: string (optional) -- accessible label for the clear-selection button</li>\n<li><code>onClearSelection</code> / <code>onclearselection</code>: callback (optional) -- clear-selection click handler</li>\n<li><code>children</code>: slot -- bulk action buttons</li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;ActionBar\n  label=\"Bulk actions\"\n  selectedCount={3}\n  selectedCountLabel=\"3 selected\"\n  clearSelectionLabel=\"Clear selection\"\n  onClearSelection={handleClear}\n&gt;\n  &lt;ActionBarButton label=\"Delete\" onClick={handleDelete}&gt;Delete&lt;/ActionBarButton&gt;\n  &lt;ActionBarButton label=\"Archive\" onClick={handleArchive}&gt;Archive&lt;/ActionBarButton&gt;\n&lt;/ActionBar&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Move focus into and out of the toolbar</li>\n<li>Arrow keys: (Consumer-provided) roving tabindex among action buttons is recommended</li>\n<li>Enter / Space: Activate the focused action button (native button behavior)</li>\n<li>Escape: (Consumer-provided) dismiss or clear selection</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"toolbar\"</code> on the container</li>\n<li><code>aria-label</code> provides the accessible name (required)</li>\n<li><code>data-selected-count</code> exposes selection count for styling and tests</li>\n<li>Clear button has its own <code>aria-label</code> via <code>clearSelectionLabel</code></li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use when users can select multiple items and apply bulk operations</li>\n<li>Use to surface contextual actions only while a selection exists</li>\n<li>Use to communicate the count of selected items unambiguously</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use as a primary navigation toolbar — use a TabBar or ToolBar</li>\n<li>Do not use when only a single item action is needed — use the row's own controls</li>\n<li>Do not hard-code the count text — always pass a localized <code>selectedCountLabel</code></li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>The action bar pairs with <code>action-bar-button</code>:</p>\n\n<pre><code class=\"language-html\">&lt;ActionBar label=\"Bulk actions\" selectedCount={2} selectedCountLabel=\"2 selected\"&gt;\n  &lt;ActionBarButton label=\"Delete\"&gt;Delete&lt;/ActionBarButton&gt;\n  &lt;ActionBarButton label=\"Move\"&gt;Move&lt;/ActionBarButton&gt;\n&lt;/ActionBar&gt;</code></pre>\n\n<h2>Headless</h2>\n\n<p>Renders a <code>&lt;div role=\"toolbar\"&gt;</code> with <code>aria-label</code> and <code>data-selected-count</code>. The visibility (show/hide based on selection), animation, positioning, and visual treatment are entirely the consumer's responsibility.</p>\n\n<h2>Styles</h2>\n\n<p>Consumer CSS targets the <code>action-bar</code> class. Use <code>data-selected-count</code> to adapt visual treatment when no items are selected. Provide a clear focus indicator on every interactive descendant.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> with <code>role=\"toolbar\"</code> and class <code>action-bar</code></li>\n<li>Verify <code>aria-label</code> equals the <code>label</code> prop</li>\n<li>Verify <code>data-selected-count</code> equals the <code>selectedCount</code> prop</li>\n<li>Verify the selectedCountLabel text is rendered</li>\n<li>Verify the clear button is rendered only when both <code>clearSelectionLabel</code> and <code>onClearSelection</code> are supplied</li>\n<li>Verify the clear-selection handler fires when the clear button is activated</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Position the action bar predictably (top of the list or as a sticky bar). Maintain a high-contrast focus indicator.</li>\n<li><strong>Developers</strong>: Format the count via <code>Intl.PluralRules</code> or your i18n library and pass the result as <code>selectedCountLabel</code>.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>action-bar-button</code> — one action button inside an action bar</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Toolbar Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/toolbar/</li>\n<li>Adobe Spectrum Action Bar: https://spectrum.adobe.com/page/action-bar/</li>\n<li>WCAG 4.1.2 Name, Role, Value: https://www.w3.org/WAI/WCAG22/Understanding/name-role-value</li>\n</ul>";
</script>

<svelte:head>
  <title>Action Bar — Lily Design System</title>
  <meta name="description" content="An action bar is a contextual toolbar that appears when items are selected, showing the current selection count alongside bulk action buttons." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
