<script lang="ts">
  const html: string = "<h1>Navigation Menu</h1>\n\n<p>A navigation menu provides a semantic <code>&lt;nav&gt;</code> landmark with an accessible label, containing navigation links or other interactive elements for site-wide or section-level navigation. It enables screen reader users to quickly locate and jump to the navigation area using landmark navigation, and it clearly communicates the purpose of the enclosed links.</p>\n\n<p>This component is suitable for primary navigation bars, sidebar navigation, footer navigation, breadcrumb wrappers, and any grouped set of navigation links that benefit from landmark semantics.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a native <code>&lt;nav&gt;</code> element, which is automatically recognized as a navigation landmark by screen readers.</li>\n<li>Uses <code>aria-label</code> to distinguish this navigation from other <code>&lt;nav&gt;</code> elements on the same page (e.g., \"Main navigation\" vs. \"Footer navigation\").</li>\n<li>Content is passed via the <code>children</code> slot, allowing any markup structure (links, lists, nested menus).</li>\n<li>Spreads <code>restProps</code> onto the <code>&lt;nav&gt;</code> for consumer customization.</li>\n<li>No internal state or keyboard handling; relies on native link and focus behavior.</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name applied via <code>aria-label</code> to distinguish this nav landmark.</li>\n<li><code>children</code>: slot (required) -- navigation content to render inside the <code>&lt;nav&gt;</code> element.</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;nav&gt;</code> element.</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Primary site navigation with main section links:</p>\n\n<pre><code class=\"language-html\">&lt;NavigationMenu label=\"Main navigation\"&gt;\n    &lt;ul&gt;\n        &lt;li&gt;&lt;a href=\"/\"&gt;Home&lt;/a&gt;&lt;/li&gt;\n        &lt;li&gt;&lt;a href=\"/products\"&gt;Products&lt;/a&gt;&lt;/li&gt;\n        &lt;li&gt;&lt;a href=\"/about\"&gt;About&lt;/a&gt;&lt;/li&gt;\n        &lt;li&gt;&lt;a href=\"/contact\"&gt;Contact&lt;/a&gt;&lt;/li&gt;\n    &lt;/ul&gt;\n&lt;/NavigationMenu&gt;</code></pre>\n\n<p>Footer navigation with legal and support links:</p>\n\n<pre><code class=\"language-html\">&lt;NavigationMenu label=\"Footer navigation\"&gt;\n    &lt;ul&gt;\n        &lt;li&gt;&lt;a href=\"/privacy\"&gt;Privacy Policy&lt;/a&gt;&lt;/li&gt;\n        &lt;li&gt;&lt;a href=\"/terms\"&gt;Terms of Service&lt;/a&gt;&lt;/li&gt;\n        &lt;li&gt;&lt;a href=\"/contact\"&gt;Contact Us&lt;/a&gt;&lt;/li&gt;\n    &lt;/ul&gt;\n&lt;/NavigationMenu&gt;</code></pre>\n\n<p>Sidebar section navigation:</p>\n\n<pre><code class=\"language-html\">&lt;NavigationMenu label=\"Documentation sections\"&gt;\n    &lt;ul&gt;\n        &lt;li&gt;&lt;a href=\"/docs/getting-started\"&gt;Getting Started&lt;/a&gt;&lt;/li&gt;\n        &lt;li&gt;&lt;a href=\"/docs/components\"&gt;Components&lt;/a&gt;&lt;/li&gt;\n        &lt;li&gt;&lt;a href=\"/docs/api\"&gt;API Reference&lt;/a&gt;&lt;/li&gt;\n    &lt;/ul&gt;\n&lt;/NavigationMenu&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive container. Navigation links inside use standard Tab key focus and Enter/Space key activation provided by the browser.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>The <code>&lt;nav&gt;</code> element implicitly has <code>role=\"navigation\"</code>, making it a navigation landmark.</li>\n<li><code>aria-label</code> -- provides an accessible name to distinguish this navigation from other <code>&lt;nav&gt;</code> landmarks on the page.</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for site-wide navigation with links to main sections.</li>\n<li>Use as the primary navigation component wrapping a group of navigation links.</li>\n<li>Use when screen reader users need to quickly locate and jump to a navigation region via landmark navigation.</li>\n<li>Use for footer navigation, sidebar navigation, or breadcrumb wrappers.</li>\n<li>Use when a semantic <code>&lt;nav&gt;</code> landmark is needed to group related links.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for action menus -- use Menu or DropdownMenu instead.</li>\n<li>Do not use for hierarchical tree navigation -- use TreeNav instead.</li>\n<li>Do not use for command palettes -- use Command instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a native <code>&lt;nav&gt;</code> element with <code>aria-label</code> for landmark identification, automatically recognized by assistive technologies. The consumer provides all visual styling, including link layout, responsive behavior, hover states, and active indicators.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.navigation-menu</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;nav&gt;</code> element with class <code>navigation-menu</code></li>\n<li>Verify The <code>&lt;nav&gt;</code> element implicitly has <code>role=\"navigation\"</code>, making it a navigation landmark.</li>\n<li>Verify aria-label<code> -- provides an accessible name to distinguish this navigation from other </code>&lt;nav&gt;` landmarks on the page.</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Highlight the current page link in the navigation to orient users, and ensure the navigation is responsive across screen sizes.</li>\n<li><strong>Developers</strong>: Use distinct <code>aria-label</code> values when multiple <code>&lt;nav&gt;</code> elements exist on the same page (e.g., \"Main navigation\" vs. \"Footer navigation\") so screen reader users can distinguish them.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>sidebar</code> — a side panel for navigation or supplementary content</li>\n<li><code>menu-bar</code> — a horizontal bar of menu triggers</li>\n<li><code>hamburger-menu</code> — a toggle button that opens a mobile navigation menu</li>\n<li><code>tree-nav</code> — a hierarchical navigation with expandable branches</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Navigation Landmark: https://www.w3.org/TR/wai-aria-1.2/#navigation</li>\n<li>WAI-ARIA Landmarks: https://www.w3.org/WAI/ARIA/apd/practices/landmark-regions/</li>\n</ul>";
</script>

<svelte:head>
  <title>Navigation Menu — Lily Design System</title>
  <meta name="description" content="A navigation menu provides a semantic &lt;nav&gt; landmark with an accessible label, containing navigation links or other interactive elements for site-wide or section-level navigation. It enables screen..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
