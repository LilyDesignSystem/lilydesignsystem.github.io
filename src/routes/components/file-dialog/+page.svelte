<script lang="ts">
  const html: string = "<h1>File Dialog</h1>\n\n<p>A file dialog provides a modal dialog for file-related operations such as opening, saving, or managing files. It renders a native <code>&lt;dialog&gt;</code> element conditionally based on its <code>open</code> state, with accessible labelling and keyboard dismissal support.</p>\n\n<p>This component is useful in applications that need custom file operation interfaces, such as file managers, document editors, or content management systems. The consumer provides all dialog content -- file lists, action buttons, and navigation controls -- as children.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Conditionally renders a <code>&lt;dialog open&gt;</code> element only when <code>open</code> is true</li>\n<li>Supports two-way binding on the <code>open</code> prop for two-way state binding</li>\n<li>Uses the native <code>&lt;dialog&gt;</code> HTML element with the <code>open</code> attribute</li>\n<li>Sets <code>tabindex=\"-1\"</code> to allow the dialog to receive focus programmatically</li>\n<li>Escape key handler sets <code>open</code> to <code>false</code></li>\n<li>Spreads <code>restProps</code> onto the <code>&lt;dialog&gt;</code> for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name for the dialog, applied as <code>aria-label</code></li>\n<li><code>open</code>: boolean (default: false) -- whether the dialog is visible, two-way bindable via <code>open</code></li>\n<li><code>children</code>: slot (required) -- dialog content</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>File open dialog with a file list and actions:</p>\n\n<pre><code class=\"language-html\">&lt;FileDialog label=\"Open file\" open={fileDialogOpen}&gt;\n  &lt;h2&gt;Select a file&lt;/h2&gt;\n  &lt;ul&gt;\n    &lt;li&gt;&lt;button onclick={() =&gt; selectFile('report.pdf')}&gt;report.pdf&lt;/button&gt;&lt;/li&gt;\n    &lt;li&gt;&lt;button onclick={() =&gt; selectFile('data.csv')}&gt;data.csv&lt;/button&gt;&lt;/li&gt;\n    &lt;li&gt;&lt;button onclick={() =&gt; selectFile('photo.jpg')}&gt;photo.jpg&lt;/button&gt;&lt;/li&gt;\n  &lt;/ul&gt;\n  &lt;button onclick={() =&gt; (fileDialogOpen = false)}&gt;Cancel&lt;/button&gt;\n&lt;/FileDialog&gt;</code></pre>\n\n<p>Save-as dialog:</p>\n\n<pre><code class=\"language-html\">&lt;FileDialog label=\"Save as\" open={saveDialogOpen}&gt;\n  &lt;label for=\"filename\"&gt;File name&lt;/label&gt;\n  &lt;input id=\"filename\" type=\"text\" value={fileName} /&gt;\n  &lt;button onclick={saveFile}&gt;Save&lt;/button&gt;\n  &lt;button onclick={() =&gt; (saveDialogOpen = false)}&gt;Cancel&lt;/button&gt;\n&lt;/FileDialog&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Escape: closes the dialog by setting <code>open</code> to <code>false</code></li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides an accessible name for the dialog</li>\n<li>Native <code>&lt;dialog&gt;</code> element -- provides implicit <code>role=\"dialog\"</code> semantics</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for a dialog interface for browsing and selecting files.</li>\n<li>Use for custom file operation interfaces such as open, save, or browse dialogs.</li>\n<li>Use in file managers, document editors, or CMS applications.</li>\n<li>Use when you need more control over the file selection UI than the native file picker provides.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for simple file uploads -- use FileUpload instead.</li>\n<li>Do not use for file management and browsing within a page section -- use FileManager instead.</li>\n<li>Do not use when the modal content is not file-specific -- use Dialog instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a native <code>&lt;dialog&gt;</code> element with conditional rendering, accessible labeling via <code>aria-label</code>, keyboard dismissal with Escape, and two-way binding on <code>open</code>. The consumer provides all visual styling, file listing content, navigation controls, and action buttons.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.file-dialog</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;dialog&gt;</code> element with class <code>file-dialog</code></li>\n<li>Verify aria-label={label}` -- provides an accessible name for the dialog</li>\n<li>Verify Native <code>&lt;dialog&gt;</code> element -- provides implicit <code>role=\"dialog\"</code> semantics</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Include clear action buttons (Open, Cancel) and a file list or navigation area within the dialog. Provide visual feedback for the currently selected file.</li>\n<li><strong>Developers</strong>: Manage focus by moving it into the dialog when opened and returning it to the trigger on close. Use the <code>open</code> binding to coordinate visibility with parent component state.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>file-input</code> — an input for selecting files from the file system &lt;input type=\"file\"&gt;</li>\n<li><code>file-manager</code> — a file browser for navigating and managing files</li>\n<li><code>dialog</code> — a modal or non-modal dialog window</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Dialog Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/</li>\n<li>MDN dialog element: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/dialog</li>\n</ul>";
</script>

<svelte:head>
  <title>File Dialog — Lily Design System</title>
  <meta name="description" content="A file dialog provides a modal dialog for file-related operations such as opening, saving, or managing files. It renders a native &lt;dialog&gt; element conditionally based on its open state, with access..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
