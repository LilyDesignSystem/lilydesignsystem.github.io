<script lang="ts">
  const html: string = "<h1>Contents Nav</h1>\n\n<p>A contents list provides a table-of-contents navigation with ordered links to page sections. It helps users quickly scan and jump to different parts of a long page, improving navigation and content discoverability.</p>\n\n<p>This component is commonly used at the top of documentation pages, articles, or forms with multiple sections. It renders a semantic <code>&lt;nav&gt;</code> landmark with an ordered list, making the hierarchical structure clear to both visual users and assistive technology. Use ContentsListItem components or <code>&lt;li&gt;</code> elements as children.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;nav&gt;</code> element with <code>aria-label</code> for accessible landmark identification</li>\n<li>Contains an <code>&lt;ol&gt;</code> (ordered list) to convey the sequential nature of page sections</li>\n<li>Children slot is rendered inside the ordered list</li>\n<li>Label prop is required for accessibility, distinguishing this navigation from others on the page</li>\n<li>Spreads <code>restProps</code> on the outer <code>&lt;nav&gt;</code> element</li>\n<li>This is a compound component; use with ContentsListItem for individual entries</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name for the navigation landmark (e.g., \"Contents\", \"On this page\")</li>\n<li><code>children</code>: slot (required) -- list items, typically ContentsListItem components or <code>&lt;li&gt;</code> elements with links</li>\n<li><code>...restProps</code>: any additional HTML attributes spread onto the <code>&lt;nav&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;ContentsNav label=\"Pages in this guide\"&gt;\n  &lt;ContentsList&gt;\n    &lt;ContentsListItem aria-current=\"page\"&gt;\n      What is type 2 diabetes?\n    &lt;/ContentsListItem&gt;\n    &lt;ContentsListItem&gt;\n      &lt;ContentsLink href=\"/conditions/type-2-diabetes/symptoms\"&gt;Symptoms&lt;/ContentsLink&gt;\n    &lt;/ContentsListItem&gt;\n    &lt;ContentsListItem&gt;\n      &lt;ContentsLink href=\"/conditions/type-2-diabetes/getting-diagnosed\"&gt;Getting diagnosed&lt;/ContentsLink&gt;\n    &lt;/ContentsListItem&gt;\n    &lt;ContentsListItem&gt;\n      &lt;ContentsLink href=\"/conditions/type-2-diabetes/treatment\"&gt;Treatment&lt;/ContentsLink&gt;\n    &lt;/ContentsListItem&gt;\n  &lt;/ContentsList&gt;\n&lt;/ContentsNav&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive navigation container. Keyboard navigation is handled natively by the anchor links within list items.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides an accessible name for the <code>&lt;nav&gt;</code> landmark, distinguishing it from other navigation regions on the page</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use at the top of a page to help users navigate a small group of related pages, such as pages about a health condition.</li>\n<li>Use for collections of up to 8 related pages -- the mini-hub pattern pairs this with PaginationNav at the bottom.</li>\n<li>Use with <code>aria-label</code> to describe the navigation purpose, such as \"Pages in this guide\".</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for unrelated pages that are not part of a cohesive group -- it will confuse users.</li>\n<li>Do not use for hierarchical site navigation -- use BreadcrumbNav instead.</li>\n<li>Do not use for in-page section links on a single long page -- use anchor links with a table of contents pattern.</li>\n<li>Do not use within multi-page forms -- use Button with BackLink for form step navigation.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless ContentsNav component provides a semantic <code>&lt;nav&gt;</code> element with <code>aria-label</code> for landmark identification, containing an <code>&lt;ol&gt;</code> for ordered section links. The consumer provides all visual styling including positioning (sticky sidebar or inline), borders, spacing, and link appearance.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.contents-nav</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;nav&gt;</code> element with class <code>contents-nav</code></li>\n<li>Verify aria-label={label}<code> -- provides an accessible name for the </code>&lt;nav&gt;` landmark, distinguishing it from other navigation regions on the page</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Place the contents navigation in a consistent location, such as the top of the content area or a sticky sidebar. Ensure it is visually distinct from the main navigation.</li>\n<li><strong>Developers</strong>: Set the <code>label</code> prop to a descriptive name (e.g., \"On this page\") to differentiate this landmark from other <code>&lt;nav&gt;</code> elements. Use ContentsList and ContentsListItem as children.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>ContentsNav follows the Nav/List/ListItem composition pattern. Use ContentsNav as the outer <code>&lt;nav&gt;</code> landmark, ContentsList as the inner ordered list, and ContentsListItem for each section link entry. This three-level structure ensures proper semantics and landmark navigation for assistive technology users.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>contents-list</code> — an contents ordered list of contents list item components</li>\n<li><code>contents-list-item</code> — one contents list item</li>\n<li><code>contents-link</code> — one table of contents link</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Navigation Landmark: https://www.w3.org/WAI/ARIA/apd/practices/landmark-regions/</li>\n<li>MDN nav element: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/nav</li>\n</ul>";
</script>

<svelte:head>
  <title>Contents Nav — Lily Design System</title>
  <meta name="description" content="A contents list provides a table-of-contents navigation with ordered links to page sections. It helps users quickly scan and jump to different parts of a long page, improving navigation and content..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
