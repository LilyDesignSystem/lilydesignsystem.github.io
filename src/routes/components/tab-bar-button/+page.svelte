<script lang="ts">
  const html: string = "<h1>Tab Bar Button</h1>\n\n<p>A tab is a UI/UX component that allows users to navigate between different sections of content within a single interface by clicking or tapping on labeled tabs, typically displayed horizontally at the top of a content area. Each tab represents a distinct section, and only the content related to the active tab is displayed at a time, keeping the interface clean and organized. TabBarButtons help group similar information in a compact and accessible way, allowing users to switch between views without reloading or navigating away from the page. They enhance usability by providing an intuitive, quick way to explore different content or options within the same context.</p>\n\n<p>This component renders an individual tab button with proper ARIA attributes including role=\"tab\", aria-selected for the active state, and aria-controls to associate the tab with its corresponding tabpanel. It follows the roving tabindex pattern where only the selected tab participates in the tab order.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as <code>&lt;button role=\"tab\"&gt;</code> for proper tab semantics</li>\n<li><code>aria-selected</code> reflects active/inactive state</li>\n<li><code>aria-controls</code> links to the associated tabpanel id</li>\n<li>Roving tabindex: selected tab has tabindex=0, others have tabindex=-1</li>\n<li>Should be placed within a <code>role=\"tablist\"</code> container</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>selected</code>: boolean (default: false) -- whether this tab is active</li>\n<li><code>controls</code>: string (required) -- id of the associated tabpanel</li>\n<li><code>children</code>: slot -- tab label content</li>\n<li><code>...restProps</code>: Any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;TabBarButton\n  selected={activeTab === 'overview'}\n  controls=\"panel-overview\"\n  onclick={() =&gt; activeTab = 'overview'}\n&gt;\n  Overview\n&lt;/TabBarButton&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>TabBarButton: moves focus to the selected tab in the tablist</li>\n<li>ArrowLeft/ArrowRight: moves between tabs (handled by parent tablist)</li>\n<li>Enter/Space: activates the tab (native button behavior)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"tab\"</code> identifies this as a tab</li>\n<li><code>aria-selected</code> communicates selection state</li>\n<li><code>aria-controls</code> links to the corresponding tabpanel</li>\n<li>Roving <code>tabindex</code> (0 for selected, -1 for unselected)</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside TabBar to represent one selectable tab that reveals its associated content panel</li>\n<li>Use with <code>selected</code> state to indicate the currently active tab</li>\n<li>Use with <code>aria-controls</code> to associate the tab with its content panel</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside of a TabBar -- it has no standalone meaning without a <code>role=\"tablist\"</code> parent</li>\n<li>Do not use for actions that navigate to a different page -- use a link or NavigationMenu</li>\n<li>Do not use for toggle actions unrelated to content panels -- use ToggleButton or SegmentGroupItem instead</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component renders a <code>&lt;button&gt;</code> with <code>role=\"tab\"</code>, <code>aria-selected</code>, <code>aria-controls</code>, and roving <code>tabindex</code> for proper tab semantics and keyboard navigation. The consumer provides all visual styling including active/inactive states, typography, and spacing.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.tab-bar-button</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;button&gt;</code> element with class <code>tab-bar-button</code></li>\n<li>Verify role=\"tab\"` identifies this as a tab</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Visually differentiate the selected tab from unselected tabs using weight, color, or an underline indicator. Maintain consistent tab sizing so the layout does not shift on selection.</li>\n<li><strong>Developers</strong>: Set the <code>controls</code> prop to the <code>id</code> of the associated <code>role=\"tabpanel\"</code> element. Manage the <code>selected</code> prop from the parent to keep tab and panel state synchronized.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>TabBarButton is designed to be used as a child of TabBar. The TabBar provides the <code>role=\"tablist\"</code> container with arrow key navigation, and each TabBarButton provides an individual <code>role=\"tab\"</code> button with selection state.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>tab-bar</code> — a group of tabs for switching between content panels</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA TabBarButtons Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/tabs/</li>\n<li>WAI-ARIA TabBarButton Role: https://www.w3.org/TR/wai-aria-1.2/#tab</li>\n</ul>";
</script>

<svelte:head>
  <title>Tab Bar Button — Lily Design System</title>
  <meta name="description" content="A tab is a UI/UX component that allows users to navigate between different sections of content within a single interface by clicking or tapping on labeled tabs, typically displayed horizontally at ..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
