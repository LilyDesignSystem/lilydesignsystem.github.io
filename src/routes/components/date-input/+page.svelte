<script lang="ts">
  const html: string = "<h1>Date Input</h1>\n\n<p>A date input provides a headless native HTML date input element with accessible labelling via <code>aria-label</code>. It is a minimal wrapper for <code>&lt;input type=\"date\"&gt;</code> that leverages the browser's built-in date picker for date selection.</p>\n\n<p>This component is useful in forms where users need to enter a calendar date. The value is stored in ISO 8601 <code>YYYY-MM-DD</code> format. It supports min and max constraints to restrict the allowable date range, and integrates with two-way binding for reactive form state.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a single <code>&lt;input type=\"date\"&gt;</code> element with no wrapper</li>\n<li>Uses <code>aria-label</code> for accessible naming instead of a visible <code>&lt;label&gt;</code> element</li>\n<li>Supports two-way binding on the <code>value</code> prop</li>\n<li>The browser provides the native date picker UI</li>\n<li>Spreads <code>restProps</code> onto the input element for consumer extensibility</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name applied via <code>aria-label</code></li>\n<li><code>value</code>: string (default: \"\") -- bindable date string in YYYY-MM-DD format</li>\n<li><code>min</code>: string (default: undefined) -- minimum allowed date in YYYY-MM-DD format</li>\n<li><code>max</code>: string (default: undefined) -- maximum allowed date in YYYY-MM-DD format</li>\n<li><code>required</code>: boolean (default: false) -- whether the input is required for form submission</li>\n<li><code>disabled</code>: boolean (default: false) -- whether the input is disabled</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the input element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;Fieldset legend=\"What is your date of birth?\"&gt;\n  &lt;Hint&gt;For example, 15 3 1984&lt;/Hint&gt;\n  &lt;DateInput label=\"Date of birth\" autocomplete=\"bday\" /&gt;\n&lt;/Fieldset&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;Fieldset legend=\"When did your symptoms start?\"&gt;\n  &lt;Hint&gt;For example, 27 11 2024&lt;/Hint&gt;\n  &lt;DateInput label=\"Symptom start date\" /&gt;\n&lt;/Fieldset&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;DateInput label=\"Deadline\" value={value} min=\"2024-01-01\" max=\"2024-12-31\" required /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Moves focus to and from the date input (native browser behavior)</li>\n<li>Arrow keys: Navigate within the date picker fields (native browser behavior)</li>\n<li>Enter: Opens or confirms the date picker (native browser behavior)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides an accessible name for the date input since there is no visible <code>&lt;label&gt;</code> element</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use when asking users for a date they already know, such as their date of birth or a specific appointment date</li>\n<li>Use within a Fieldset with a legend that poses the date question, such as \"What is your date of birth?\"</li>\n<li>Use with Hint to show the expected format, for example \"For example, 15 3 1984\"</li>\n<li>Use with autocomplete attributes (bday-day, bday-month, bday-year) for date of birth fields</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when users are unlikely to know the exact date — consider a DateField with flexible input or a CalendarRangePicker</li>\n<li>Do not use for selecting a date from a constrained range — use CalendarTable or CalendarRangePicker instead</li>\n<li>Do not use for date-and-time entry — use DateTimeNowInput or separate DateInput and TimeInput fields</li>\n<li>Do not use a single text field for dates — three separate fields (day, month, year) reduce errors</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless DateInput component provides a native <code>&lt;input type=\"date\"&gt;</code> with <code>aria-label</code> for accessible naming, two-way binding on the <code>value</code> prop, and <code>min</code>/<code>max</code> constraints for date range validation. The consumer provides all visual styling including input appearance, calendar icon, and any surrounding label or helper text.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.date-input</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;input&gt;</code> element with class <code>date-input</code></li>\n<li>Verify aria-label={label}<code> -- provides an accessible name for the date input since there is no visible </code>&lt;label&gt;` element</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Ensure the date input is wide enough to display the full date format. Consider showing the expected format (e.g., YYYY-MM-DD) as placeholder or helper text nearby.</li>\n<li><strong>Developers</strong>: Use ISO 8601 <code>YYYY-MM-DD</code> format for <code>value</code>, <code>min</code>, and <code>max</code> props. The browser provides the native date picker UI, which varies across browsers and platforms.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>date-field</code> — a structured field for entering date components</li>\n<li><code>date-range</code> — a display of a start and end date range</li>\n<li><code>date-time-local-input</code> — an input for entering a date and time without time zone &lt;input type=\"datetime-local\"&gt;</li>\n<li><code>date-time-now-input</code> — an input for entering a date and time and \"now\" button &lt;input type=\"date\"&gt;&lt;input type=\"time\"&gt;&lt;button type=\"button\"&gt;</li>\n<li><code>calendar-range-picker</code> — a picker for selecting a date range on a calendar</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN date input: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/date</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<label class=\"label\" for=\"demo-input\">Label</label><input class=\"date-input\" type=\"date\" id=\"demo-input\" aria-label=\"DateInput\" />";
  const svelteSource: string = "// In your Svelte component:\nimport DateInput from \"lily-design-system-svelte-headless/components/DateInput/DateInput.svelte\";\n\n<DateInput label=\"Label\" />\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Date Input — Lily Design System</title>
  <meta name="description" content="A date input provides a headless native HTML date input element with accessible labelling via aria-label. It is a minimal wrapper for &lt;input type=&quot;date&quot;&gt; that leverages the browser's built-in date ..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

