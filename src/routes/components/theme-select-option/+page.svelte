<script lang="ts">
  const html: string = "<h1>Theme Select Option</h1>\n\n<p>A theme select option is a single <code>&lt;option&gt;</code> element intended for use within a ThemeSelect dropdown. It represents one available theme choice (e.g. light, dark, high-contrast) and renders as a native <code>&lt;option&gt;</code> element for full browser and assistive technology compatibility.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;option&gt;</code> element with class <code>theme-select-option</code></li>\n<li>Spreads <code>restProps</code> onto the root element for consumer customization</li>\n</ul>\n\n<h2>Help</h2>\n\n<p>Use ThemeSelectOption for individual theme choices within a ThemeSelect dropdown. Common scenarios include light/dark mode options, high-contrast themes, and system-default options. Can be used instead of plain <code>&lt;option&gt;</code> elements when you want a dedicated component.</p>\n\n<h2>Syntax</h2>\n\n<pre><code class=\"language-html\">&lt;ThemeSelectOption value=\"...\"&gt;Label&lt;/ThemeSelectOption&gt;</code></pre>\n\n<h2>Usage</h2>\n\n<p>Basic theme options inside a ThemeSelect:</p>\n\n<pre><code class=\"language-html\">&lt;ThemeSelect label=\"Theme\" value={theme}&gt;\n  &lt;ThemeSelectOption value=\"light\"&gt;Light&lt;/ThemeSelectOption&gt;\n  &lt;ThemeSelectOption value=\"dark\"&gt;Dark&lt;/ThemeSelectOption&gt;\n&lt;/ThemeSelect&gt;</code></pre>\n\n<p>With a disabled placeholder option:</p>\n\n<pre><code class=\"language-html\">&lt;ThemeSelect label=\"Theme\" value={theme}&gt;\n  &lt;ThemeSelectOption value=\"\" disabled&gt;Choose a theme...&lt;/ThemeSelectOption&gt;\n  &lt;ThemeSelectOption value=\"light\"&gt;Light&lt;/ThemeSelectOption&gt;\n  &lt;ThemeSelectOption value=\"dark\"&gt;Dark&lt;/ThemeSelectOption&gt;\n  &lt;ThemeSelectOption value=\"high-contrast\"&gt;High Contrast&lt;/ThemeSelectOption&gt;\n&lt;/ThemeSelect&gt;</code></pre>\n\n<p>With system default as the first selectable option:</p>\n\n<pre><code class=\"language-html\">&lt;ThemeSelect label=\"Appearance\" value={theme}&gt;\n  &lt;ThemeSelectOption value=\"system\"&gt;System Default&lt;/ThemeSelectOption&gt;\n  &lt;ThemeSelectOption value=\"light\"&gt;Light&lt;/ThemeSelectOption&gt;\n  &lt;ThemeSelectOption value=\"dark\"&gt;Dark&lt;/ThemeSelectOption&gt;\n&lt;/ThemeSelect&gt;</code></pre>\n\n<h2>Props</h2>\n\n<table>\n<thead>\n<tr><th>Prop</th><th>Type</th><th>Default</th><th>Description</th></tr>\n</thead>\n<tbody>\n<tr><td><code>value</code></td><td><code>string</code></td><td><code>\"\"</code></td><td>The value submitted when this option is selected</td></tr>\n<tr><td><code>disabled</code></td><td><code>boolean</code></td><td><code>false</code></td><td>Whether this option is disabled</td></tr>\n<tr><td><code>children</code></td><td><code>slot</code></td><td>(required)</td><td>Display text for the option</td></tr>\n<tr><td><code>...restProps</code></td><td>HTML attributes</td><td></td><td>Additional attributes spread onto the <code>&lt;option&gt;</code> element</td></tr>\n</tbody>\n</table>\n\n<h2>Examples</h2>\n\n<p>With disabled option:</p>\n\n<pre><code class=\"language-html\">&lt;ThemeSelect label=\"Theme\" value={value}&gt;\n  &lt;ThemeSelectOption value=\"\" disabled&gt;Choose a theme...&lt;/ThemeSelectOption&gt;\n  &lt;ThemeSelectOption value=\"light\"&gt;Light&lt;/ThemeSelectOption&gt;\n  &lt;ThemeSelectOption value=\"dark\"&gt;Dark&lt;/ThemeSelectOption&gt;\n  &lt;ThemeSelectOption value=\"high-contrast\"&gt;High Contrast&lt;/ThemeSelectOption&gt;\n&lt;/ThemeSelect&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Arrow Up/Down: Navigates between options within the parent select (native browser behavior)</li>\n<li>Enter/Space: Selects the focused option (native browser behavior)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Native <code>&lt;option&gt;</code> element provides built-in accessibility semantics</li>\n<li><code>disabled</code> attribute communicates disabled state to assistive technology</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside a ThemeSelect to represent one selectable theme option.</li>\n<li>Use when you want a dedicated component per option rather than plain <code>&lt;option&gt;</code> elements for consistency.</li>\n<li>Use when each theme option may need additional props such as <code>disabled</code> for placeholder options.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside of a ThemeSelect -- it renders a native <code>&lt;option&gt;</code> that requires a <code>&lt;select&gt;</code> context.</li>\n<li>Do not use for standard non-theme Select dropdowns -- use <a href=\"../option/\">Option</a> instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a native <code>&lt;option&gt;</code> element with <code>value</code> and <code>disabled</code> attributes. Full browser and assistive technology compatibility comes from the native element. The consumer provides the display text as children and all visual styling is handled by the parent select and browser defaults.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.theme-select-option</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;option&gt;</code> element with class <code>theme-select-option</code></li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Keep option labels short and descriptive (e.g., \"Light\", \"Dark\", \"High Contrast\") so they are scannable in the dropdown.</li>\n<li><strong>Developers</strong>: Use the <code>disabled</code> attribute on a placeholder option (e.g., \"Choose a theme...\") to prevent it from being selected as a valid value.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>ThemeSelectOption is a child of ThemeSelect, following the Select/SelectOption pattern. Each ThemeSelectOption represents one available theme choice within the parent dropdown.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>theme-select</code> — a select dropdown for choosing a theme</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN option element: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/option</li>\n<li>MDN select element: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/select</li>\n</ul>";
</script>

<svelte:head>
  <title>Theme Select Option — Lily Design System</title>
  <meta name="description" content="A theme select option is a single &lt;option&gt; element intended for use within a ThemeSelect dropdown. It represents one available theme choice (e.g. light, dark, high-contrast) and renders as a native..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
