<script lang="ts">
  const html: string = "<h1>Number Input</h1>\n\n<p>NumberInput is a headless component that wraps a native <code>&lt;input type=\"number\"&gt;</code> element with accessible labelling and optional min, max, and step constraints. It provides a bindable numeric value for two-way data flow.</p>\n\n<p>This component is useful for quantity selectors, age inputs, price fields, configuration values, or any form field that accepts numeric input. The native number input provides browser-built-in validation, increment/decrement controls, and appropriate virtual keyboard on mobile devices.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a native <code>&lt;input type=\"number\"&gt;</code> element</li>\n<li>Uses <code>aria-label</code> for an accessible name describing the input purpose</li>\n<li>The <code>value</code> prop uses two-way binding with an initial undefined state</li>\n<li>Supports optional <code>min</code>, <code>max</code>, and <code>step</code> constraints</li>\n<li>Supports <code>required</code> and <code>disabled</code> boolean states</li>\n<li>Spreads <code>restProps</code> onto the input for consumer customization (e.g., <code>id</code>, <code>name</code>, <code>placeholder</code>)</li>\n<li>pattern: <code>two-way binding</code> for the value prop</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name for the input via <code>aria-label</code></li>\n<li><code>value</code>: number | undefined (default: <code>undefined</code>) -- current numeric value; bindable with two-way <code>value</code> binding</li>\n<li><code>min</code>: number (optional) -- minimum allowed value</li>\n<li><code>max</code>: number (optional) -- maximum allowed value</li>\n<li><code>step</code>: number (optional) -- increment/decrement step size</li>\n<li><code>required</code>: boolean (default: <code>false</code>) -- whether the input is required</li>\n<li><code>disabled</code>: boolean (default: <code>false</code>) -- whether the input is disabled</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;input&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Basic quantity input with range constraints:</p>\n\n<pre><code class=\"language-html\">&lt;NumberInput label=\"Quantity\" value={quantity} min={0} max={100} step={1} /&gt;</code></pre>\n\n<p>Age input in a registration form:</p>\n\n<pre><code class=\"language-html\">&lt;Field label=\"Age\" required&gt;\n  &lt;NumberInput label=\"Age\" value={age} min={0} max={150} step={1} required /&gt;\n&lt;/Field&gt;</code></pre>\n\n<p>Number input for a configuration setting:</p>\n\n<pre><code class=\"language-html\">&lt;Field label=\"Session timeout (minutes)\"&gt;\n  &lt;NumberInput label=\"Session timeout in minutes\" value={timeout} min={1} max={120} step={5} /&gt;\n&lt;/Field&gt;</code></pre>\n\n<p>Number input in a product order form:</p>\n\n<pre><code class=\"language-html\">&lt;Form label=\"Add to basket\" onsubmit={addToBasket}&gt;\n  &lt;Field label=\"Quantity\"&gt;\n    &lt;NumberInput label=\"Quantity\" value={qty} min={1} max={10} step={1} /&gt;\n  &lt;/Field&gt;\n  &lt;Button type=\"submit\"&gt;Add to basket&lt;/Button&gt;\n&lt;/Form&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None at the component level. Keyboard behavior is handled natively by the browser's number input implementation (Up/Down arrows to increment/decrement).</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides an accessible name describing the purpose of the number input</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for form fields that accept numeric values, such as quantity selectors, age inputs, or configuration values.</li>\n<li>Use when you need browser-built-in validation, increment/decrement controls, and numeric virtual keyboards on mobile.</li>\n<li>Use for settings or preferences that require a bounded numeric range with step increments.</li>\n<li>Use for measurement inputs where the unit is displayed separately.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for phone numbers -- use TelInput instead.</li>\n<li>Do not use for codes or reference numbers -- use TextInput with <code>inputmode=\"numeric\"</code> instead.</li>\n<li>Do not use for currency values -- use CurrencyInput instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component wraps a native <code>&lt;input type=\"number\"&gt;</code> with <code>aria-label</code> for accessible naming, two-way bindable value, and optional <code>min</code>, <code>max</code>, and <code>step</code> constraints. The consumer provides all visual styling, including input dimensions, borders, spinner button appearance, and validation error display.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.number-input</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;input&gt;</code> element with class <code>number-input</code></li>\n<li>Verify aria-label={label}` -- provides an accessible name describing the purpose of the number input</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Clearly indicate the expected range and step size near the input (e.g., \"Enter a value between 1 and 100\") to reduce input errors.</li>\n<li><strong>Developers</strong>: Set <code>min</code>, <code>max</code>, and <code>step</code> props to leverage browser-native validation. Be aware that <code>&lt;input type=\"number\"&gt;</code> returns an empty string for non-numeric input.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>text-input</code> — a single-line text input field &lt;input type=\"text\"&gt;</li>\n<li><code>currency-input</code> — a locale-aware currency input with automatic formatting, symbols, separators</li>\n<li><code>range-input</code> — a slider input for selecting a value within a range &lt;input type=\"range\"&gt;</li>\n<li><code>meter</code> — a gauge displaying a scalar value within a known range</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>HTML number input: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/number</li>\n<li>WAI-ARIA spinbutton role (implicit): https://www.w3.org/TR/wai-aria-1.2/#spinbutton</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<label class=\"label\" for=\"demo-input\">Label</label><input class=\"number-input\" type=\"number\" id=\"demo-input\" aria-label=\"NumberInput\" />";
  const svelteSource: string = "// In your Svelte component:\nimport NumberInput from \"lily-design-system-svelte-headless/components/NumberInput/NumberInput.svelte\";\n\n<NumberInput label=\"Label\" />\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Number Input — Lily Design System</title>
  <meta name="description" content="NumberInput is a headless component that wraps a native &lt;input type=&quot;number&quot;&gt; element with accessible labelling and optional min, max, and step constraints. It provides a bindable numeric value for..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

