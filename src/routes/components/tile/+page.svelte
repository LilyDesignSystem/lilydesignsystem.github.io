<script lang="ts">
  const html: string = "<h1>Tile</h1>\n\n<p>A tile is a grouping container that presents related content in a structured format, often as a single clickable area. Tiles are used in dashboards, grids, and selection interfaces where users choose from a set of options or navigate to related content.</p>\n\n<p>This headless component uses a <code>&lt;div&gt;</code> element with appropriate ARIA attributes for accessible grouping and optional interactive behavior.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses <code>&lt;div&gt;</code> element for flexible content grouping</li>\n<li><code>aria-label</code> describes the tile content for screen readers</li>\n<li>May be interactive (clickable) or static (informational)</li>\n<li>When interactive, manages focus and click behavior</li>\n<li>Contains slots for icon, title, description, and metadata</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (optional) -- accessible label for the tile</li>\n<li><code>interactive</code>: boolean (default: false) -- whether the tile is clickable</li>\n<li><code>onclick</code>: callback (optional) -- click handler for interactive tiles</li>\n<li><code>children</code>: slot (required) -- tile content</li>\n<li><code>...restProps</code>: Any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Static informational tile:</p>\n\n<pre><code class=\"language-html\">&lt;Tile label=\"Analytics dashboard\"&gt;\n  &lt;Icon name=\"chart\" /&gt;\n  &lt;h3&gt;Analytics&lt;/h3&gt;\n  &lt;p&gt;View your site analytics and metrics.&lt;/p&gt;\n&lt;/Tile&gt;</code></pre>\n\n<p>Interactive tile for selecting a pricing plan:</p>\n\n<pre><code class=\"language-html\">&lt;Tile interactive onclick={handleSelect} label=\"Select Pro plan\"&gt;\n  &lt;h3&gt;Pro Plan&lt;/h3&gt;\n  &lt;p&gt;$29/month&lt;/p&gt;\n  &lt;ul&gt;\n    &lt;li&gt;Unlimited projects&lt;/li&gt;\n    &lt;li&gt;Priority support&lt;/li&gt;\n    &lt;li&gt;Custom domains&lt;/li&gt;\n  &lt;/ul&gt;\n&lt;/Tile&gt;</code></pre>\n\n<p>Dashboard grid with multiple tiles:</p>\n\n<pre><code class=\"language-html\">&lt;div class=\"dashboard-grid\"&gt;\n  &lt;Tile interactive onclick={() =&gt; navigateTo('/patients')} label=\"View patients\"&gt;\n    &lt;Icon name=\"people\" /&gt;\n    &lt;h3&gt;Patients&lt;/h3&gt;\n    &lt;p&gt;1,247 registered&lt;/p&gt;\n  &lt;/Tile&gt;\n  &lt;Tile interactive onclick={() =&gt; navigateTo('/appointments')} label=\"View appointments\"&gt;\n    &lt;Icon name=\"calendar\" /&gt;\n    &lt;h3&gt;Appointments&lt;/h3&gt;\n    &lt;p&gt;23 today&lt;/p&gt;\n  &lt;/Tile&gt;\n  &lt;Tile interactive onclick={() =&gt; navigateTo('/reports')} label=\"View reports\"&gt;\n    &lt;Icon name=\"document\" /&gt;\n    &lt;h3&gt;Reports&lt;/h3&gt;\n    &lt;p&gt;5 pending review&lt;/p&gt;\n  &lt;/Tile&gt;\n&lt;/div&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus the tile (when interactive)</li>\n<li>Enter: Activate the tile (when interactive)</li>\n<li>Space: Activate the tile (when interactive)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label</code> -- describes the tile for screen readers</li>\n<li><code>tabindex=\"0\"</code> when interactive for keyboard focus</li>\n<li><code>role=\"button\"</code> when interactive</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for dashboard widgets, option grids, or navigation cards.</li>\n<li>Use when content needs to be presented as a cohesive, selectable unit.</li>\n<li>Use as a clickable container presenting related content in a structured format.</li>\n<li>Use in selection interfaces where users choose from a set of options.</li>\n<li>Use for feature or plan selection grids where each option is a distinct unit.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for non-clickable content grouping -- use Card or Panel instead.</li>\n<li>Do not use for navigation menus -- use NavigationMenu instead.</li>\n<li>Do not use for simple text links -- use ActionLink instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides a <code>&lt;div&gt;</code> element with optional interactive behavior and ARIA attributes, with zero visual styling. The consumer is responsible for all CSS including background, border, padding, hover effects, focus indicator, grid layout, and responsive sizing.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.tile</code> class for targeting. No default styles are included -- this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>tile</code></li>\n<li>Verify <code>aria-label</code> is applied when provided</li>\n<li>Verify interactive tiles have <code>tabindex=\"0\"</code> and <code>role=\"button\"</code></li>\n<li>Verify keyboard interactions work for interactive tiles</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use consistent sizing within tile grids. Provide clear hover and focus states for interactive tiles. Include visual hierarchy with icon, title, and description.</li>\n<li><strong>Developers</strong>: Set <code>interactive</code> to true only when the entire tile is clickable. For tiles with internal links, keep the tile static and let the link handle navigation.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>card</code> — a grouped content container with header, body, and footer areas</li>\n<li><code>panel</code> — a generic content panel with optional heading</li>\n<li><code>feature-card</code> — a large content card with a prominent image positioned alongside or above the text</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/HTML/Element/div\">MDN div element</a></li>\n</ul>";
</script>

<svelte:head>
  <title>Tile — Lily Design System</title>
  <meta name="description" content="A tile is a grouping container that presents related content in a structured format, often as a single clickable area. Tiles are used in dashboards, grids, and selection interfaces where users choo..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
