<script lang="ts">
  const html: string = "<h1>Five Star Rating View</h1>\n\n<p>A read-only five-star rating display that renders filled and empty star characters to visually represent a rating value from 0 to 5. This is the display-only companion to FiveStarRatingInput, used for showing ratings in product listings, review summaries, and user profiles. Screen readers receive the full rating description via <code>aria-label</code> rather than reading individual star characters.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;span&gt;</code> with <code>role=\"img\"</code> as the outer container for the star display</li>\n<li>Contains 5 inner <code>&lt;span&gt;</code> elements, each displaying a filled star (filled: data-filled true) or empty star (unfilled: data-filled false)</li>\n<li>Inner star spans use <code>aria-hidden=\"true\"</code> so screen readers skip individual characters</li>\n<li>Exposes <code>data-value</code> on the outer span and <code>data-filled</code> on each star span for consumer CSS</li>\n<li>Displays Unicode star characters: filled star and empty star</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>value</code>: number (required) -- rating value (0-5) to display</li>\n<li><code>label</code>: string (required) -- accessible description via <code>aria-label</code> (e.g., \"4 out of 5 stars\")</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the outer <code>&lt;span&gt;</code></li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;FiveStarRatingView value={4} label=\"4 out of 5 stars\" /&gt;</code></pre>\n\n<p>In a product listing:</p>\n\n<pre><code class=\"language-html\">&lt;Card&gt;\n    &lt;h3&gt;Wireless Headphones&lt;/h3&gt;\n    &lt;FiveStarRatingView value={4} label=\"Rated 4 out of 5 stars\" /&gt;\n    &lt;p&gt;Based on 128 reviews&lt;/p&gt;\n&lt;/Card&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive, read-only display.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"img\"</code> -- indicates the span represents a visual/graphic element (the star rating)</li>\n<li><code>aria-label=\"...\"</code> -- provides the full rating description for screen readers</li>\n<li><code>aria-hidden=\"true\"</code> -- on each inner star span, prevents screen readers from reading individual star characters</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to display a read-only star rating in product listings, review summaries, or user profiles.</li>\n<li>Use when the rating value should be visible but not editable.</li>\n<li>Use alongside product titles or review headings to show aggregate ratings.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when the user needs to select or change a rating -- use FiveStarRatingPicker instead.</li>\n<li>Do not use for satisfaction survey results with face labels -- use FiveFaceRatingView instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides semantic HTML with <code>role=\"img\"</code>, <code>aria-label</code>, and <code>aria-hidden</code> inner spans for accessible read-only star display. The consumer provides all visual styling including star characters, colors, and sizing.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.five-star-rating-view</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>five-star-rating-view</code></li>\n<li>Verify role=\"img\"` -- indicates the span represents a visual/graphic element (the star rating)</li>\n<li>Verify aria-label=\"...\"` -- provides the full rating description for screen readers</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use clear visual distinction between filled and empty stars. Consider half-star displays for fractional ratings by styling at the consumer level.</li>\n<li><strong>Developers</strong>: The <code>data-filled</code> attribute on each star span enables CSS-only styling of filled vs. empty states without JavaScript.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>five-star-rating-picker</code> — a picker for selecting a 1-5 star rating using radio buttons</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA <code>img</code> role: https://www.w3.org/WAI/ARIA/apd/roles/img/</li>\n</ul>";
</script>

<svelte:head>
  <title>Five Star Rating View — Lily Design System</title>
  <meta name="description" content="A read-only five-star rating display that renders filled and empty star characters to visually represent a rating value from 0 to 5. This is the display-only companion to FiveStarRatingInput, used ..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
