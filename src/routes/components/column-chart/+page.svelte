<script lang="ts">
  const html: string = "<h1>ColumnChart</h1>\n\n<p>A categorical visualization with bars running vertically, used to compare a measured value across a set of named categories. Vertical orientation reads naturally for time-ordered categories (months, quarters, releases) and works well when category labels are short.</p>\n\n<p>The component is a headless wrapper around an inline <code>&lt;svg&gt;</code>: it renders a <code>&lt;figure role=\"img\"&gt;</code> containing the columns, names the figure via <code>aria-label</code> (from <code>label</code>), and references an extended <code>description</code> and an optional <code>dataTable</code> slot via <code>aria-describedby</code>. The data table is the canonical accessible alternative — screen readers and exports both benefit from a real <code>&lt;table&gt;</code> rendering of the same numbers, and Lily encourages always providing it.</p>\n\n<p>For the same comparison with horizontal bars (better for long labels) use <code>BarChart</code>. For trends over continuous data use <code>LineChart</code> or <code>AreaChart</code>. For tiny inline summaries use <code>Sparkline</code>.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;figure role=\"img\"&gt;</code> containing an inline <code>&lt;svg&gt;</code> rendering of vertical columns</li>\n<li>Data is supplied as <code>categories</code> (each with a label and value)</li>\n<li>Columns run vertically; values are read along the y-axis</li>\n<li>An optional accessible data table is rendered via the <code>dataTable</code> slot</li>\n<li>Spreads <code>restProps</code> onto the root <code>&lt;figure&gt;</code></li>\n</ul>\n\n<h2>Props</h2>\n\n<table>\n<thead>\n<tr><th>Prop</th><th>Type</th><th>Default</th><th>Description</th></tr>\n</thead>\n<tbody>\n<tr><td><code>label</code></td><td>string (required)</td><td>—</td><td>Accessible name</td></tr>\n<tr><td><code>description</code></td><td>string</td><td>—</td><td>Extended description</td></tr>\n<tr><td><code>categories</code></td><td>array of <code>{ label: string; value: number }</code></td><td>[]</td><td>Categories to plot</td></tr>\n<tr><td><code>dataTable</code></td><td>slot</td><td>—</td><td>Optional fallback <code>&lt;table&gt;</code></td></tr>\n<tr><td><code>...restProps</code></td><td>HTML attributes</td><td>—</td><td>Spread onto the root <code>&lt;figure&gt;</code></td></tr>\n</tbody>\n</table>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;ColumnChart label=\"Monthly revenue\" categories={monthlyRevenue} description=\"Revenue per month for the last year\" /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>No keyboard interactions on the rendered columns</li>\n<li>Data table (when rendered) follows native table keyboard behaviour</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"img\"</code> exposes the chart as a single image</li>\n<li><code>aria-label</code> and <code>aria-describedby</code> provide the accessible name and description</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Comparing categorical values where the categories have short labels (months, quarters)</li>\n<li>Time series at low resolution</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Use <code>BarChart</code> (horizontal) when category labels are long</li>\n<li>Use <code>LineChart</code> or <code>AreaChart</code> for high-resolution time series</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component renders semantic HTML with appropriate ARIA wiring. The consumer provides all visual styling — no CSS, animations, or layout assumptions are baked in.</p>\n\n<h2>Styles</h2>\n\n<p>The component renders with <code>.column-chart</code> as the root class. No default styles are included.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>bar-chart</code> — horizontal bar equivalent</li>\n<li><code>line-chart</code> / <code>area-chart</code> — continuous-data alternatives</li>\n<li><code>graphic-block</code> — chart wrapper with title/notes</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/HTML/Element/figure\">MDN figure element</a></li>\n</ul>";
</script>

<svelte:head>
  <title>ColumnChart — Lily Design System</title>
  <meta name="description" content="A categorical visualization with bars running vertically, used to compare a measured value across a set of named categories. Vertical orientation reads naturally for time-ordered categories (months..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
