<script lang="ts">
  const html: string = "<h1>Visible</h1>\n\n<p>An IntersectionObserver wrapper that exposes element visibility state, enabling lazy loading, scroll-triggered animations, and viewport-based analytics.</p>\n\n<p>This headless component uses a <code>&lt;div&gt;</code> element that wraps content and passes a visibility boolean to its children via a render prop or slot.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses <code>&lt;div&gt;</code> element as an observation target for IntersectionObserver</li>\n<li>Passes a visibility boolean to the children slot/render prop</li>\n<li><code>once</code> mode disconnects the observer after the first intersection</li>\n<li>Supports configurable <code>threshold</code> and <code>rootMargin</code> for IntersectionObserver</li>\n<li>Consumer uses the visibility state to conditionally render or style content</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>children</code>: slot (required) -- receives visibility boolean (e.g., <code>children(visible)</code>)</li>\n<li><code>once</code>: boolean (default: false) -- if true, stops observing after first visibility</li>\n<li><code>threshold</code>: number (default: 0) -- IntersectionObserver threshold (0-1)</li>\n<li><code>rootMargin</code>: string (default: \"0px\") -- IntersectionObserver root margin</li>\n<li><code>...restProps</code>: Any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;Visible once threshold={0.5}&gt;\n  {(isVisible) =&gt; isVisible\n    ? &lt;img src=\"photo.jpg\" alt=\"Lazy loaded photo\" /&gt;\n    : &lt;div class=\"placeholder\" /&gt;\n  }\n&lt;/Visible&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;Visible threshold={0.1}&gt;\n  {(isVisible) =&gt; (\n    &lt;section class={isVisible ? \"animate-in\" : \"hidden\"}&gt;\n      &lt;h2&gt;Welcome&lt;/h2&gt;\n      &lt;p&gt;This section animates when scrolled into view.&lt;/p&gt;\n    &lt;/section&gt;\n  )}\n&lt;/Visible&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;Visible once&gt;\n  {(isVisible) =&gt; isVisible\n    ? &lt;iframe src=\"https://example.com/embed\" title=\"Embedded content\" /&gt;\n    : &lt;div class=\"loading-placeholder\"&gt;Loading...&lt;/div&gt;\n  }\n&lt;/Visible&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>None -- presentational wrapper; interactive children handle their own keyboard interactions</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>None -- presentational wrapper with no semantic role</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for lazy loading content such as images, iframes, or heavy components</li>\n<li>Use to trigger animations when content scrolls into the viewport</li>\n<li>Use for viewport-based analytics tracking (e.g., measuring whether content was seen)</li>\n<li>Use with <code>once</code> mode when content only needs to load once and stay rendered</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for scrollytelling -- use Scroller</li>\n<li>Do not use when content must always render -- just render it directly</li>\n<li>Do not use for complex scroll-linked animations that need scroll position -- use a scroll event listener</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides a <code>&lt;div&gt;</code> wrapper that uses IntersectionObserver to track visibility, with zero visual styling. The consumer is responsible for all CSS including placeholder styling, transition animations, and layout of conditionally rendered content.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.visible</code> class for targeting. No default styles are included -- this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>visible</code></li>\n<li>Verify IntersectionObserver is created with correct threshold and rootMargin</li>\n<li>Verify visibility boolean is passed to children slot</li>\n<li>Verify <code>once</code> mode disconnects observer after first intersection</li>\n<li>Verify observer is cleaned up on component unmount</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Plan placeholder states for content that loads lazily. Ensure loading states communicate to users that content is forthcoming. Consider skeleton placeholders for better perceived performance.</li>\n<li><strong>Developers</strong>: Always provide meaningful alt text or labels for lazily loaded content. Use <code>once</code> mode for content that should stay rendered after first load. Choose appropriate threshold values -- 0 triggers on first pixel visible, 1 requires full visibility.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>scroller-base</code> — see components.tsv</li>\n<li><code>scroller</code> — see components.tsv</li>\n<li><code>video-player</code> — see components.tsv</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li><a href=\"https://github.com/reuters-graphics/graphics-components\">Reuters Graphics Visible component</a></li>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/API/IntersectionObserver\">IntersectionObserver API (MDN)</a></li>\n</ul>";
</script>

<svelte:head>
  <title>Visible — Lily Design System</title>
  <meta name="description" content="An IntersectionObserver wrapper that exposes element visibility state, enabling lazy loading, scroll-triggered animations, and viewport-based analytics." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
