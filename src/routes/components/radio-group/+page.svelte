<script lang="ts">
  const html: string = "<h1>Radio Group</h1>\n\n<p>RadioGroup is a headless fieldset container that groups related radio buttons under the ARIA <code>radiogroup</code> role. It provides an accessible label for the entire group, enabling screen readers and assistive technologies to communicate the relationship between individual radio buttons.</p>\n\n<p>Use RadioGroup when you need a set of mutually exclusive options, such as selecting a size, choosing a shipping method, or picking a preference. The component wraps native radio inputs in a <code>&lt;fieldset&gt;</code> element, ensuring proper semantic grouping and accessibility without imposing any visual styling.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;fieldset&gt;</code> element with <code>role=\"radiogroup\"</code> for explicit ARIA semantics</li>\n<li>The <code>aria-label</code> attribute on the fieldset provides an accessible name for the group</li>\n<li>Consumer provides radio button children (typically <code>&lt;label&gt;</code> elements wrapping <code>&lt;input type=\"radio\"&gt;</code>)</li>\n<li>Spreads <code>restProps</code> onto the fieldset for consumer customization</li>\n<li>No internal state management; radio selection is handled by native radio input behavior</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible label for the radio group, applied via <code>aria-label</code></li>\n<li><code>children</code>: slot (required) -- radio button elements to render inside the fieldset</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;fieldset&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;Fieldset legend=\"Do you know your NHS number?\"&gt;\n  &lt;RadioGroup name=\"nhs-number-known\"&gt;\n    &lt;RadioInput label=\"Yes, I know my NHS number\" value=\"yes\" /&gt;\n    &lt;RadioInput label=\"No, I do not know my NHS number\" value=\"no\" /&gt;\n    &lt;RadioInput label=\"I am not sure\" value=\"unsure\" /&gt;\n  &lt;/RadioGroup&gt;\n&lt;/Fieldset&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Moves focus into the radio group; within the group, arrow keys move between radio buttons (native browser behavior)</li>\n<li>Arrow Up / Arrow Down: Moves selection between radio buttons within the group (native radio behavior)</li>\n<li>Space: Selects the focused radio button (native radio behavior)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"radiogroup\"</code> -- identifies the fieldset as a group of radio buttons</li>\n<li><code>aria-label={label}</code> -- provides an accessible name for the group so screen readers announce the purpose</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to wrap multiple RadioInput components that share a common name and enforce single-selection behaviour</li>\n<li>Use within a Fieldset so the group has a descriptive legend for accessibility</li>\n<li>Use when the form needs to track which single option is selected from the set</li>\n<li>Use when all options should be visible at once so users can compare them</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when users may select more than one option -- use CheckboxGroup with CheckboxInput instead</li>\n<li>Do not use for visual-only grouping -- use Fieldset alone if no shared selection state is needed</li>\n<li>Do not use with only one radio -- a single option is not a meaningful choice</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>The RadioGroup headless component provides a <code>&lt;fieldset&gt;</code> with <code>role=\"radiogroup\"</code> and <code>aria-label</code> for accessible group naming. It relies on native radio input behavior for selection and keyboard navigation. The consumer provides all visual styling for the fieldset, labels, and radio buttons.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.radio-group</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>radio-group</code></li>\n<li>Verify role=\"radiogroup\"` -- identifies the fieldset as a group of radio buttons</li>\n<li>Verify aria-label={label}` -- provides an accessible name for the group so screen readers announce the purpose</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Stack radio options vertically for readability, and use clear labels that distinguish each choice without ambiguity.</li>\n<li><strong>Developers</strong>: Ensure all radio inputs within the group share the same <code>name</code> attribute for proper mutual exclusion, and pre-select a sensible default when appropriate.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>RadioGroup is the container for RadioInput children. Use <code>&lt;RadioGroup&gt;</code> to wrap multiple <code>&lt;RadioInput&gt;</code> elements, providing the group-level accessible label while each RadioInput carries its own option label.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>radio-input</code> — a single radio button input &lt;input type=\"radio\"&gt;</li>\n<li><code>checkbox-group</code> — a group component that manages a collection of checkboxes with shared state</li>\n<li><code>segment-group</code> — a group of mutually exclusive segment options</li>\n<li><code>mutually-exclusive</code> — a container ensuring only one option can be selected from a group</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Radio Group Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/radiobutton/</li>\n<li>WAI-ARIA Authoring Practices - Radio Group: https://www.w3.org/WAI/ARIA/apg/patterns/radio/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"radio-group\" role=\"radiogroup\" aria-label=\"Choice\"><label><input type=\"radio\" name=\"demo\" checked /> Yes</label><label><input type=\"radio\" name=\"demo\" /> No</label></div>";
  const svelteSource: string = "// In your Svelte component:\nimport RadioGroup from \"lily-design-system-svelte-headless/components/RadioGroup/RadioGroup.svelte\";\n\n<RadioGroup>\n  <!-- list children -->\n</RadioGroup>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Radio Group — Lily Design System</title>
  <meta name="description" content="RadioGroup is a headless fieldset container that groups related radio buttons under the ARIA radiogroup role. It provides an accessible label for the entire group, enabling screen readers and assis..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

