<script lang="ts">
  const html: string = "<h1>Toggle Group</h1>\n\n<p>ToggleGroup is a headless container for a collection of related toggle buttons. It uses the ARIA <code>group</code> role with an accessible label to semantically associate toggle buttons, making their relationship understandable to assistive technologies.</p>\n\n<p>This component is useful for text formatting toolbars (bold, italic, underline), view mode selectors, or any interface where multiple options can be independently toggled on or off. Each child button should use <code>aria-pressed</code> to indicate its toggle state. Consumers provide the individual toggle buttons as children and bring their own styling.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> with <code>role=\"group\"</code> to semantically group child toggle buttons</li>\n<li>Uses <code>aria-label</code> for an accessible name describing the group</li>\n<li>Accepts a <code>children</code> slot for rendering toggle button elements inside the group</li>\n<li>Spreads <code>restProps</code> onto the container for consumer customization</li>\n<li>pattern: standard props destructuring with typed props</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name for the group via <code>aria-label</code></li>\n<li><code>children</code>: slot (required) -- toggle button elements to render inside the group</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the container <code>&lt;div&gt;</code></li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Text formatting toolbar with independent toggles:</p>\n\n<pre><code class=\"language-html\">&lt;ToggleGroup label=\"Text formatting\"&gt;\n  &lt;ToggleButton label=\"Bold\" pressed={isBold} /&gt;\n  &lt;ToggleButton label=\"Italic\" pressed={isItalic} /&gt;\n  &lt;ToggleButton label=\"Underline\" pressed={isUnderline} /&gt;\n&lt;/ToggleGroup&gt;</code></pre>\n\n<p>View mode selector:</p>\n\n<pre><code class=\"language-html\">&lt;ToggleGroup label=\"View mode\"&gt;\n  &lt;ToggleButton label=\"Grid view\" pressed={isGrid} /&gt;\n  &lt;ToggleButton label=\"List view\" pressed={isList} /&gt;\n&lt;/ToggleGroup&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None at the container level. Keyboard behavior depends on the child toggle button elements provided by the consumer (typically Tab to navigate between buttons, Enter/Space to toggle).</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"group\"</code> -- identifies the container as a semantic grouping of related toggle buttons</li>\n<li><code>aria-label={label}</code> -- provides an accessible name describing the purpose of the toggle collection</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to group multiple toggle buttons for selecting one or more options, such as text alignment or view modes.</li>\n<li>Use for text formatting toolbars where bold, italic, and underline can be independently toggled.</li>\n<li>Use when multiple toggles share a common purpose and should be perceived as a group by assistive technology.</li>\n<li>Use for filter panels where multiple independent options can be activated simultaneously.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for mutually exclusive single selection -- use SegmentGroup instead.</li>\n<li>Do not use for form choices requiring submission -- use RadioGroup or CheckboxGroup instead.</li>\n<li>Do not use for a single toggle -- render ToggleButton directly without a group wrapper.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;div&gt;</code> with <code>role=\"group\"</code> and <code>aria-label</code> for accessible grouping of related toggle buttons. The consumer provides individual toggle button elements as children and all visual styling including layout, spacing, and button appearance.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.toggle-group</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>toggle-group</code></li>\n<li>Verify role=\"group\"` -- identifies the container as a semantic grouping of related toggle buttons</li>\n<li>Verify aria-label={label}` -- provides an accessible name describing the purpose of the toggle collection</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Visually connect grouped toggles (e.g., shared border, joined buttons) to reinforce that they belong together.</li>\n<li><strong>Developers</strong>: Each child button should use <code>aria-pressed</code> to indicate its individual toggle state. The group does not manage child state; each toggle is independent.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>ToggleGroup contains ToggleButton children (or plain buttons with <code>aria-pressed</code>) following the Group/Item pattern. ToggleGroup provides the semantic grouping container, and each child ToggleButton manages its own pressed state independently.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>toggle-button</code> — a button that toggles between pressed and unpressed states</li>\n<li><code>checkbox-group</code> — a group component that manages a collection of checkboxes with shared state</li>\n<li><code>segment-group</code> — a group of mutually exclusive segment options</li>\n<li><code>button-group</code> — a wrapper that groups related buttons together</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA group role: https://www.w3.org/TR/wai-aria-1.2/#group</li>\n<li>WAI-ARIA Button Pattern (toggle): https://www.w3.org/WAI/ARIA/apg/patterns/button/</li>\n</ul>";
</script>

<svelte:head>
  <title>Toggle Group — Lily Design System</title>
  <meta name="description" content="ToggleGroup is a headless container for a collection of related toggle buttons. It uses the ARIA group role with an accessible label to semantically associate toggle buttons, making their relations..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
