<script lang="ts">
  const html: string = "<h1>Badge</h1>\n\n<p>A badge is a small inline component for displaying dynamic status information, counts, or short labels in a compact, prominent format. Badges are commonly used to show unread message counts, notification indicators, or activity statuses attached to icons, buttons, or list items.</p>\n\n<p>The component renders as a <code>&lt;span&gt;</code> with <code>role=\"status\"</code>, creating an ARIA live region that announces content changes to screen readers. The <code>data-type</code> attribute exposes the semantic variant for consumer CSS styling, and an optional <code>aria-label</code> provides expanded context when the visible badge text alone is insufficient (e.g., \"3\" displayed visually but \"3 unread messages\" announced to screen readers).</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as <code>&lt;span role=\"status\"&gt;</code> for screen reader announcements of dynamic content</li>\n<li><code>data-type</code> attribute exposes variant for consumer CSS styling</li>\n<li><code>aria-label</code> provides expanded context when badge text alone is insufficient</li>\n<li>Five semantic variants: default, info, success, warning, error</li>\n<li>Content is provided through child elements</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>type</code>: \"default\" | \"info\" | \"success\" | \"warning\" | \"error\" (default: \"default\") -- badge variant for semantic meaning</li>\n<li><code>label</code>: string (optional) -- accessible label for screen readers</li>\n<li><code>children</code>: slot (required) -- badge content (text, number, etc.)</li>\n<li><code>...restProps</code>: Any additional HTML attributes passed to the <code>&lt;span&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Unread message count on a navigation link:</p>\n\n<pre><code class=\"language-html\">&lt;a href=\"/messages\"&gt;\n  Messages &lt;Badge label=\"12 unread messages\"&gt;12&lt;/Badge&gt;\n&lt;/a&gt;</code></pre>\n\n<p>Notification count on an icon button:</p>\n\n<pre><code class=\"language-html\">&lt;button aria-label=\"Notifications\"&gt;\n  &lt;Icon name=\"bell\" /&gt;\n  &lt;Badge label=\"3 new notifications\"&gt;3&lt;/Badge&gt;\n&lt;/button&gt;</code></pre>\n\n<p>Warning status badge:</p>\n\n<pre><code class=\"language-html\">&lt;Badge type=\"warning\" label=\"2 items require attention\"&gt;2&lt;/Badge&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>None -- badges are informational, not interactive</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"status\"</code> -- creates an ARIA live region for dynamic content announcements</li>\n<li><code>aria-label</code> -- optional expanded context for screen readers beyond the visible text</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for small counts, categories, or labels attached to another element, such as unread message counts or notification indicators.</li>\n<li>Use when the badge value is supplementary and the parent element is understandable without it.</li>\n<li>Use when dynamic content changes need to be announced to screen readers via the <code>role=\"status\"</code> live region.</li>\n<li>Use with the <code>label</code> prop to provide expanded context for screen readers when the visible text alone is insufficient.</li>\n<li>Use the <code>type</code> prop to communicate semantic meaning (info, success, warning, error) through <code>data-type</code> for consumer styling.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for status indicators that are the primary content -- use Tag for labels like \"Active\" or \"Completed\".</li>\n<li>Do not use for prominent call-to-action labels -- use CallToAction instead.</li>\n<li>Do not use for decorative emphasis -- use Flair instead.</li>\n<li>Do not use for long text content -- use Alert or Banner instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides <code>role=\"status\"</code> for ARIA live region announcements, a <code>data-type</code> attribute for semantic variants, and optional <code>aria-label</code> with zero visual styling. The consumer is responsible for all CSS including background color, text color, border radius, font size, padding, and positioning relative to the parent element.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.badge</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;span&gt;</code> element with class <code>badge</code></li>\n<li>Verify role=\"status\"` -- creates an ARIA live region for dynamic content announcements</li>\n<li>Verify aria-label` -- optional expanded context for screen readers beyond the visible text</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Keep badge content to 1-3 characters for counts or short labels. Use consistent sizing and position (e.g., top-right of an icon) across the interface.</li>\n<li><strong>Developers</strong>: Use the <code>label</code> prop to provide expanded context for screen readers, e.g., <code>label=\"3 unread messages\"</code> when the visible badge only shows \"3\".</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>status-tag</code> — a tag showing the current status of a task</li>\n<li><code>tag</code> — a keyword label for categorizing content</li>\n<li><code>flair</code> — a decorative highlight or emphasis element</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/HTML/Element/span\">MDN span element</a></li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<span class=\"badge\">New</span>";
  const svelteSource: string = "// In your Svelte component:\nimport Badge from \"lily-design-system-svelte-headless/components/Badge/Badge.svelte\";\n\n<Badge>\n  <!-- content -->\n</Badge>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Badge — Lily Design System</title>
  <meta name="description" content="A badge is a small inline component for displaying dynamic status information, counts, or short labels in a compact, prominent format. Badges are commonly used to show unread message counts, notifi..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

