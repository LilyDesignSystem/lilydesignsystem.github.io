<script lang="ts">
  const html: string = "<h1>Header</h1>\n\n<p>A header is a headless component that renders a semantic <code>&lt;header&gt;</code> element, providing a landmark region for introductory or navigational content. It is typically positioned at the top of a page or section and contains elements like headings, navigation links, branding, search controls, or call-to-action buttons.</p>\n\n<p>The semantic <code>&lt;header&gt;</code> element helps assistive technologies identify the header region as a landmark, improving navigation for screen reader users. An optional <code>aria-label</code> allows consumers to distinguish between multiple headers on a page.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a single <code>&lt;header&gt;</code> element as a semantic landmark</li>\n<li>Uses an optional <code>aria-label</code> to provide an accessible name, which is helpful when a page has multiple header regions</li>\n<li>Uses slot for the <code>children</code> prop</li>\n<li>Spreads <code>restProps</code> onto the header element for consumer extensibility</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (default: undefined) -- optional accessible name applied via <code>aria-label</code>, useful for distinguishing multiple headers</li>\n<li><code>children</code>: slot (required) -- content rendered inside the header element</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the header element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;Header label=\"Service name\"&gt;\n  &lt;nav aria-label=\"Primary navigation\"&gt;\n    &lt;ul&gt;\n      &lt;li&gt;&lt;a href=\"/dashboard\"&gt;Dashboard&lt;/a&gt;&lt;/li&gt;\n      &lt;li&gt;&lt;a href=\"/patients\" aria-current=\"page\"&gt;Patients&lt;/a&gt;&lt;/li&gt;\n      &lt;li&gt;&lt;a href=\"/appointments\"&gt;Appointments&lt;/a&gt;&lt;/li&gt;\n    &lt;/ul&gt;\n  &lt;/nav&gt;\n&lt;/Header&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;!-- Simplified transactional header without navigation --&gt;\n&lt;Header label=\"Register with a GP\"&gt;\n  &lt;span&gt;Register with a GP&lt;/span&gt;\n&lt;/Header&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;!-- Section header with aria-label to distinguish from page header --&gt;\n&lt;Header label=\"Article header\"&gt;\n  &lt;h2&gt;Article Title&lt;/h2&gt;\n  &lt;p&gt;Published on 2024-01-15&lt;/p&gt;\n&lt;/Header&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive container. Keyboard interactions are determined by the content placed inside it by the consumer.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- optionally provides an accessible name for the header landmark, useful when multiple headers exist on a page to help screen reader users distinguish between them</li>\n<li>The <code>&lt;header&gt;</code> element implicitly has <code>role=\"banner\"</code> when it is a direct child of <code>&lt;body&gt;</code>, providing a page-level landmark</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use at the top of every page to identify the service and provide consistent site-wide navigation</li>\n<li>Use to display the service name, logo, and primary navigation links</li>\n<li>Use a simplified header without navigation or search on transactional pages to keep users focused on their task</li>\n<li>Use when the page needs a <code>banner</code> landmark for assistive technology navigation</li>\n<li>Use <code>aria-label</code> to distinguish section headers from the page header when multiple headers exist on a page</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for page-specific content or actions -- Header is for site-wide identification and navigation only</li>\n<li>Do not use sticky or fixed positioning -- it can obscure content that has keyboard focus (WCAG 2.2)</li>\n<li>Do not use as a replacement for NavigationMenu -- Header contains the navigation, but complex menus should use dedicated components</li>\n<li>Do not nest a header directly inside another header</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a semantic <code>&lt;header&gt;</code> element that automatically exposes the <code>banner</code> landmark role (when a direct child of <code>&lt;body&gt;</code>) to assistive technologies. The consumer provides all visual styling including layout, background, typography, and spacing.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.header</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;header&gt;</code> element with class <code>header</code></li>\n<li>Verify aria-label={label}` -- optionally provides an accessible name for the header landmark, useful when multiple headers exist on a page to help screen reader users distinguish between them</li>\n<li>Verify The <code>&lt;header&gt;</code> element implicitly has <code>role=\"banner\"</code> when it is a direct child of <code>&lt;body&gt;</code>, providing a page-level landmark</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Keep the header visually consistent across pages. Include branding and primary navigation prominently. Use a sticky or fixed position for easy access on long pages.</li>\n<li><strong>Developers</strong>: When multiple headers exist on a page, provide the <code>label</code> prop to help screen reader users distinguish between them.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>footer</code> — a page or section footer area</li>\n<li><code>grail-layout-top-header</code> — grail layout top header full width with &lt;header&gt; tag</li>\n<li><code>navigation-menu</code> — a site-wide navigation menu with links</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN header element: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/header</li>\n<li>WAI-ARIA Banner Landmark: https://www.w3.org/WAI/ARIA/apd/practices/landmark-regions/</li>\n</ul>";
</script>

<svelte:head>
  <title>Header — Lily Design System</title>
  <meta name="description" content="A header is a headless component that renders a semantic &lt;header&gt; element, providing a landmark region for introductory or navigational content. It is typically positioned at the top of a page or s..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
