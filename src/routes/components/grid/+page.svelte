<script lang="ts">
  const html: string = "<h1>Grid</h1>\n\n<p>A grid is a CSS grid layout container with a configurable number of columns and gap, allowing consumers to lay out two-dimensional content without writing custom grid CSS for every container.</p>\n\n<p>Use it for card grids, image galleries, dashboard tile layouts, and any two-dimensional arrangement of content where you want explicit column control and consistent gutters.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> with <code>class=\"grid\"</code></li>\n<li>If <code>columns</code> is a number, applies inline <code>grid-template-columns: repeat({columns}, minmax(0, 1fr));</code></li>\n<li>If <code>columns</code> is a string, uses it as-is for <code>grid-template-columns</code></li>\n<li>Always applies <code>display: grid</code> and <code>gap: {gap}</code></li>\n<li>Sets <code>data-columns</code> for consumer CSS hooks</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>columns</code>: number | string (default: 12) -- column count or <code>grid-template-columns</code> value</li>\n<li><code>gap</code>: string (default: \"1rem\") -- gap CSS value</li>\n<li><code>children</code>: slot -- the laid-out content</li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Twelve-column default grid:</p>\n\n<pre><code class=\"language-html\">&lt;Grid&gt;\n  &lt;div&gt;Item 1&lt;/div&gt;\n  &lt;div&gt;Item 2&lt;/div&gt;\n&lt;/Grid&gt;</code></pre>\n\n<p>Three-column card grid:</p>\n\n<pre><code class=\"language-html\">&lt;Grid columns={3} gap=\"1.5rem\"&gt;\n  &lt;Card&gt;One&lt;/Card&gt;\n  &lt;Card&gt;Two&lt;/Card&gt;\n  &lt;Card&gt;Three&lt;/Card&gt;\n&lt;/Grid&gt;</code></pre>\n\n<p>Custom track sizes:</p>\n\n<pre><code class=\"language-html\">&lt;Grid columns=\"200px 1fr 200px\" gap=\"1rem\"&gt;\n  &lt;aside&gt;Left&lt;/aside&gt;\n  &lt;main&gt;Center&lt;/main&gt;\n  &lt;aside&gt;Right&lt;/aside&gt;\n&lt;/Grid&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>None -- this is a layout container; focusable children manage their own keyboard behavior.</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>None — purely structural. Do not confuse the <code>grid</code> CSS layout primitive with the WAI-ARIA <code>role=\"grid\"</code> widget pattern (used by DataTable and similar components).</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for two-dimensional layouts of cards, tiles, or images.</li>\n<li>Use when columns should remain aligned across rows.</li>\n<li>Use when you want responsive column counts via consumer media queries.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for one-dimensional row/column stacks (use FlexStack).</li>\n<li>Do not use for variable-height masonry layouts (use Masonry).</li>\n<li>Do not confuse with the WAI-ARIA grid widget — this is purely a layout helper.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>Provides only the CSS grid behavior via inline styles. Item placement, span overrides, and responsive column count changes are the consumer's responsibility, typically via media queries on <code>.grid</code> or descendant selectors.</p>\n\n<h2>Styles</h2>\n\n<p>Consumers may target <code>.grid</code> for responsive column overrides:</p>\n\n<pre><code class=\"language-css\">@media (max-width: 600px) {\n  .grid { grid-template-columns: 1fr; }\n}</code></pre>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>grid</code></li>\n<li>Verify inline style sets <code>display: grid</code> and <code>gap: {gap}</code></li>\n<li>Verify when <code>columns</code> is a number, <code>grid-template-columns</code> is <code>repeat(N, minmax(0, 1fr))</code></li>\n<li>Verify when <code>columns</code> is a string, <code>grid-template-columns</code> matches it exactly</li>\n<li>Verify <code>data-columns</code> attribute matches the <code>columns</code> prop</li>\n<li>Verify children content is rendered</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Pick a small column count (3, 4, 6, 12) for predictable alignment. Reserve custom track strings for layouts where fixed-width sidebars or asymmetric tracks are required.</li>\n<li><strong>Developers</strong>: Use the numeric <code>columns</code> API for the common case. Drop down to the string API only when you need fixed tracks or named lines.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>flex-stack</code> — a flex layout container for vertical or horizontal stacking with consistent gap</li>\n<li><code>container</code> — a generic block-level content container</li>\n<li><code>masonry</code> — a masonry layout container for variable-height items</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN grid-template-columns: https://developer.mozilla.org/en-US/docs/Web/CSS/grid-template-columns</li>\n<li>MDN CSS grid: https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_grid_layout</li>\n</ul>";
</script>

<svelte:head>
  <title>Grid — Lily Design System</title>
  <meta name="description" content="A grid is a CSS grid layout container with a configurable number of columns and gap, allowing consumers to lay out two-dimensional content without writing custom grid CSS for every container." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
