<script lang="ts">
  const html: string = "<h1>AddressInput</h1>\n\n<p>A single-line text input for capturing a postal address, with <code>autocomplete=\"street-address\"</code> set so browsers, password managers, and platform autofill can populate it from the user's saved addresses.</p>\n\n<p>The component is intentionally minimal: it provides only the input and the autocomplete hint. Postcode lookup, suggestion dropdowns, and structured multi-field forms (street / city / region / postcode) are the consumer's responsibility — <code>AddressInput</code> is the building block, not the full address-capture flow. For typeahead behaviour, wrap or compose with <code>Autosuggest</code> or <code>Combobox</code>. For per-field validation, use a structured set of inputs instead.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a single <code>&lt;input type=\"text\"&gt;</code> with <code>autocomplete=\"street-address\"</code> so browsers and password managers can populate it from saved addresses</li>\n<li>The consumer is responsible for any postcode lookup, autocomplete dropdown, or fallback to a multi-field structured form</li>\n<li>Supports two-way binding on the <code>value</code> prop</li>\n<li>Accepts <code>required</code> and <code>disabled</code> HTML attributes</li>\n<li>Spreads <code>restProps</code> onto the root <code>&lt;input&gt;</code> for consumer customization</li>\n<li>All text content (label, placeholder, error messages) comes through props for internationalization</li>\n</ul>\n\n<h2>Props</h2>\n\n<table>\n<thead>\n<tr><th>Prop</th><th>Type</th><th>Default</th><th>Description</th></tr>\n</thead>\n<tbody>\n<tr><td><code>label</code></td><td>string (required)</td><td>—</td><td>Accessible name set on <code>aria-label</code></td></tr>\n<tr><td><code>value</code></td><td>string</td><td>\"\"</td><td>Bindable address value</td></tr>\n<tr><td><code>required</code></td><td>boolean</td><td>false</td><td>Whether the field is required</td></tr>\n<tr><td><code>disabled</code></td><td>boolean</td><td>false</td><td>Whether the field is disabled</td></tr>\n<tr><td><code>autocomplete</code></td><td>string</td><td>\"street-address\"</td><td>HTML autocomplete token; useful values include <code>street-address</code>, <code>address-line1</code>, <code>address-line2</code>, <code>postal-code</code></td></tr>\n<tr><td><code>...restProps</code></td><td>HTML attributes</td><td>—</td><td>Spread onto the root <code>&lt;input&gt;</code> element</td></tr>\n</tbody>\n</table>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;AddressInput label=\"Postal address\" value=\"\" autocomplete=\"street-address\" /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Native <code>&lt;input&gt;</code> keyboard behaviour applies (typing, selection, clipboard)</li>\n<li>Tab moves focus into and out of the input</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label</code> (set from <code>label</code>) provides the accessible name when no visible <code>&lt;label&gt;</code> element wraps the input</li>\n<li><code>aria-required=\"true\"</code> is implied when <code>required</code> is set</li>\n<li><code>aria-invalid</code> and <code>aria-errormessage</code> should be set by the consumer when validation fails</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Capturing a single-line postal address as part of a contact or delivery form</li>\n<li>Building a postcode lookup widget where the consumer wires the lookup logic externally</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Use a structured set of inputs (street, city, region, postcode) when you need separate fields for validation or formatting</li>\n<li>Use <code>Combobox</code> or <code>Autosuggest</code> when you want a typeahead dropdown of results</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component renders semantic HTML with appropriate ARIA wiring. The consumer provides all visual styling — no CSS, animations, or layout assumptions are baked in.</p>\n\n<h2>Styles</h2>\n\n<p>The component renders with <code>.address-input</code> as the root class. No default styles are included.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>text-input</code> — single-line text input without address-specific autocomplete</li>\n<li><code>autosuggest</code> — text input with proposed matching options</li>\n<li><code>postal-code-input</code> — postal/ZIP code input with format validation</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/text\">MDN input type=text</a></li>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/HTML/Attributes/autocomplete\">MDN autocomplete attribute</a></li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<label class=\"label\" for=\"demo-input\">Label</label><input class=\"address-input\" id=\"demo-input\" aria-label=\"an input for searching or manually entering an address\" />";
  const svelteSource: string = "// In your Svelte component:\nimport AddressInput from \"lily-design-system-svelte-headless/components/AddressInput/AddressInput.svelte\";\n\n<AddressInput label=\"Label\" />\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>AddressInput — Lily Design System</title>
  <meta name="description" content="A single-line text input for capturing a postal address, with autocomplete=&quot;street-address&quot; set so browsers, password managers, and platform autofill can populate it from the user's saved addresses." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

