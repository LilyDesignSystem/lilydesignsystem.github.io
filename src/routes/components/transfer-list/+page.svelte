<script lang="ts">
  const html: string = "<h1>Transfer List</h1>\n\n<p>A transfer list is a dual list box for moving items between two lists. It groups a source list, optional action buttons, and a target list into a single labeled region.</p>\n\n<p>Use it for permission assignment, attendee selection, tag picking, or any workflow where users move items from a candidate list to a chosen list.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div class=\"transfer-list\" role=\"group\"&gt;</code> container</li>\n<li><code>label</code> is <strong>required</strong> and sets <code>aria-label</code> on the group</li>\n<li>Source content renders inside <code>&lt;section class=\"transfer-list-source\" aria-label={sourceLabel}&gt;</code></li>\n<li>Target content renders inside <code>&lt;section class=\"transfer-list-target\" aria-label={targetLabel}&gt;</code></li>\n<li>Optional <code>actions</code> content renders inside <code>&lt;div class=\"transfer-list-actions\"&gt;</code></li>\n<li><code>sourceLabel</code> and <code>targetLabel</code> are <strong>required</strong> and set <code>aria-label</code> on each section</li>\n<li>Consumer typically supplies a Listbox in source and target slots and move buttons in actions</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (<strong>required</strong>) -- aria-label on the outer group</li>\n<li><code>sourceLabel</code>: string (<strong>required</strong>) -- aria-label for the source section</li>\n<li><code>targetLabel</code>: string (<strong>required</strong>) -- aria-label for the target section</li>\n<li><code>source</code>: slot (<strong>required</strong>) -- source list content</li>\n<li><code>target</code>: slot (<strong>required</strong>) -- target list content</li>\n<li><code>actions</code>: slot (optional) -- action buttons between the lists</li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;TransferList\n  label=\"Assign roles\"\n  sourceLabel=\"Available roles\"\n  targetLabel=\"Selected roles\"\n&gt;\n  &lt;template #source&gt;\n    &lt;ul role=\"listbox\"&gt;...&lt;/ul&gt;\n  &lt;/template&gt;\n  &lt;template #actions&gt;\n    &lt;button type=\"button\" aria-label=\"Move to selected\"&gt;→&lt;/button&gt;\n    &lt;button type=\"button\" aria-label=\"Move to available\"&gt;←&lt;/button&gt;\n  &lt;/template&gt;\n  &lt;template #target&gt;\n    &lt;ul role=\"listbox\"&gt;...&lt;/ul&gt;\n  &lt;/template&gt;\n&lt;/TransferList&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>None at the container level — interactive behavior lives on the consumer-provided listboxes and action buttons.</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"group\"</code> on the container with <code>aria-label</code> from the <code>label</code> prop</li>\n<li>Each <code>&lt;section&gt;</code> has <code>aria-label</code> from <code>sourceLabel</code> / <code>targetLabel</code></li>\n<li>Consumer-supplied listboxes carry their own ARIA roles</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to move items between two lists when both lists need to be visible at once.</li>\n<li>Use for assignment workflows: roles, tags, attendees, permissions.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for picking a single value — use Select or Listbox.</li>\n<li>Do not use when the candidate list is very long without filtering — provide a search input or use a different pattern.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>Renders semantic group and section markup with no styling. Consumer provides the listboxes, the action buttons, the move logic, and the visual layout.</p>\n\n<h2>Styles</h2>\n\n<p>Consumer CSS targets <code>transfer-list</code>, <code>transfer-list-source</code>, <code>transfer-list-target</code>, and <code>transfer-list-actions</code>. Use a horizontal flex layout with the actions column between the two lists.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>transfer-list</code></li>\n<li>Verify <code>role=\"group\"</code> is set</li>\n<li>Verify <code>aria-label</code> equals the <code>label</code> prop</li>\n<li>Verify the source content renders inside <code>&lt;section class=\"transfer-list-source\"&gt;</code> with <code>aria-label</code> from <code>sourceLabel</code></li>\n<li>Verify the target content renders inside <code>&lt;section class=\"transfer-list-target\"&gt;</code> with <code>aria-label</code> from <code>targetLabel</code></li>\n<li>Verify the actions content renders inside <code>&lt;div class=\"transfer-list-actions\"&gt;</code> only when provided</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Show counts and a search field above each list when the option set is large. Use clear directional icons on the move buttons.</li>\n<li><strong>Developers</strong>: Use a Listbox component in each slot for proper ARIA. Manage the source and target arrays in the consumer.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>listbox</code> — a list of selectable options with keyboard navigation</li>\n<li><code>select</code> — a dropdown select element for choosing one option</li>\n<li><code>checkbox-group</code> — a group component that manages a collection of checkboxes with shared state</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Listbox Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/listbox/</li>\n<li>Ant Design Transfer: https://ant.design/components/transfer</li>\n</ul>";
</script>

<svelte:head>
  <title>Transfer List — Lily Design System</title>
  <meta name="description" content="A transfer list is a dual list box for moving items between two lists. It groups a source list, optional action buttons, and a target list into a single labeled region." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
