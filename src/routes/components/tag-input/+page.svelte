<script lang="ts">
  const html: string = "<h1>Tag Input</h1>\n\n<p>TagInput is a headless text input for adding tags to a collection. When the user types a value and presses Enter, the component calls the <code>onadd</code> callback with the trimmed text and automatically clears the input, providing a streamlined tag entry experience.</p>\n\n<p>This component is commonly used alongside TagGroup to build tag management interfaces, such as adding skills to a profile, keywords to an article, or labels to an issue tracker. The input value is bindable, allowing the consumer to read or control the current text.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a native <code>&lt;input type=\"text\"&gt;</code> element for tag entry</li>\n<li>Uses <code>aria-label</code> for accessible identification</li>\n<li>Handles Enter key via an <code>onkeydown</code> handler that calls <code>onadd</code> with the trimmed value and resets the input</li>\n<li>Supports two-way binding on the <code>value</code> prop</li>\n<li>Supports <code>disabled</code> state to prevent interaction</li>\n<li>Spreads <code>restProps</code> onto the input for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name for the input via <code>aria-label</code></li>\n<li><code>value</code>: string (default: <code>\"\"</code>) -- current input text; bindable with two-way <code>value</code> binding</li>\n<li><code>onadd</code>: <code>(value: string) =&gt; void</code> (optional) -- callback fired when Enter is pressed with a non-empty trimmed value</li>\n<li><code>disabled</code>: boolean (default: <code>false</code>) -- whether the input is disabled</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;input&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Basic tag input:</p>\n\n<pre><code class=\"language-html\">&lt;TagInput label=\"Add tag\" value={tagText} onadd={(tag) =&gt; tags.push(tag)} /&gt;</code></pre>\n\n<p>Tag input paired with a TagGroup for skills on a profile:</p>\n\n<pre><code class=\"language-html\">&lt;Field label=\"Skills\"&gt;\n  &lt;TagGroup&gt;\n    {#each skills as skill}\n      &lt;Tag&gt;{skill}&lt;/Tag&gt;\n    {/each}\n  &lt;/TagGroup&gt;\n  &lt;TagInput\n    label=\"Add a skill\"\n    value={newSkill}\n    onadd={(skill) =&gt; { skills = [...skills, skill]; }}\n    placeholder=\"Type a skill and press Enter\"\n  /&gt;\n&lt;/Field&gt;</code></pre>\n\n<p>Tag input in a blog post editor for categories:</p>\n\n<pre><code class=\"language-html\">&lt;Form label=\"Post settings\" onsubmit={savePost}&gt;\n  &lt;Field label=\"Title\" required&gt;\n    &lt;TextInput label=\"Post title\" value={title} required /&gt;\n  &lt;/Field&gt;\n  &lt;Field label=\"Tags\"&gt;\n    &lt;TagGroup&gt;\n      {#each postTags as tag}\n        &lt;Tag&gt;{tag} &lt;button onclick={() =&gt; removeTag(tag)} aria-label=\"Remove {tag}\"&gt;x&lt;/button&gt;&lt;/Tag&gt;\n      {/each}\n    &lt;/TagGroup&gt;\n    &lt;TagInput\n      label=\"Add a tag\"\n      value={newTag}\n      onadd={(tag) =&gt; { postTags = [...postTags, tag]; }}\n      placeholder=\"e.g. accessibility, design\"\n    /&gt;\n  &lt;/Field&gt;\n  &lt;Button type=\"submit\"&gt;Publish&lt;/Button&gt;\n&lt;/Form&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Enter: When the input has a non-empty trimmed value, prevents default form submission, calls <code>onadd</code> with the trimmed value, and clears the input</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides an accessible name for the text input</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use alongside a TagGroup to build tag management interfaces where users add tags by typing and pressing Enter.</li>\n<li>Use for adding keywords, labels, skills, or categories to items such as articles, profiles, or issue trackers.</li>\n<li>Use in content management systems for tagging posts, pages, or media assets.</li>\n<li>Use in issue trackers or project boards for adding labels to work items.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for read-only tag display -- use TagGroup instead.</li>\n<li>Do not use for predefined selections only -- use CheckboxGroup, Combobox, or Select instead.</li>\n<li>Do not use when autocomplete suggestions should appear as the user types -- use Combobox instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component renders a native <code>&lt;input type=\"text\"&gt;</code> with <code>aria-label</code>, Enter key handling for tag submission, and two-way value binding. It provides the text input semantics and add-on-enter behavior. The consumer provides all visual styling and manages the tag collection state.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.tag-input</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>tag-input</code></li>\n<li>Verify aria-label={label}` -- provides an accessible name for the text input</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Place the TagInput visually adjacent to or inside the TagGroup so users understand the relationship. Show a clear placeholder text such as \"Add a tag...\" to indicate purpose.</li>\n<li><strong>Developers</strong>: Use the <code>onadd</code> callback to append the new tag to your collection state. Consider trimming and deduplicating tags in the callback. Pair with a TagGroup for displaying the resulting tags.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>text-input</code> — a single-line text input field &lt;input type=\"text\"&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA textbox role (implicit via input type=\"text\"): https://www.w3.org/TR/wai-aria-1.2/#textbox</li>\n</ul>";
</script>

<svelte:head>
  <title>Tag Input — Lily Design System</title>
  <meta name="description" content="TagInput is a headless text input for adding tags to a collection. When the user types a value and presses Enter, the component calls the onadd callback with the trimmed text and automatically clea..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
