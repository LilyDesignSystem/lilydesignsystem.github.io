<script lang="ts">
  const html: string = "<h1>Icon List</h1>\n\n<p>An icon list is a <code>&lt;ul&gt;</code> of items where each item begins with a leading icon, suitable for short feature lists, benefit lists, or step-by-step summaries.</p>\n\n<p>Use it when a list of short statements would benefit from a visual marker at the start of each line.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a native <code>&lt;ul&gt;</code> element with class <code>icon-list</code></li>\n<li><code>label</code> prop is <strong>optional</strong> — when supplied it sets <code>aria-label</code> on the list</li>\n<li>Children are expected to be <code>icon-list-item</code> elements</li>\n<li>The <code>&lt;ul&gt;</code> semantics provide list count and item position to screen readers</li>\n<li>Pass-through attributes are forwarded to the root <code>&lt;ul&gt;</code></li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>This component is part of the <code>*List</code> <code>*ListItem</code> composition pattern. Pair it with <code>IconListItem</code> children to form a complete icon list.</p>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (optional) -- aria-label for the list</li>\n<li><code>children</code>: slot -- <code>icon-list-item</code> children</li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Feature list with check-mark icons:</p>\n\n<pre><code class=\"language-html\">&lt;IconList label=\"Plan benefits\"&gt;\n  &lt;IconListItem&gt;&lt;CheckIcon /&gt; Free shipping&lt;/IconListItem&gt;\n  &lt;IconListItem&gt;&lt;CheckIcon /&gt; 24/7 support&lt;/IconListItem&gt;\n  &lt;IconListItem&gt;&lt;CheckIcon /&gt; Cancel anytime&lt;/IconListItem&gt;\n&lt;/IconList&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: focus moves to interactive descendants (links, buttons) within items</li>\n<li>The list itself is not interactive</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Implicit <code>list</code> role from <code>&lt;ul&gt;</code> element</li>\n<li><code>aria-label</code> is provided when the <code>label</code> prop is set</li>\n<li>Each child contributes a list item via its <code>&lt;li&gt;</code> element</li>\n<li>Each child icon is decorative (<code>aria-hidden=\"true\"</code>)</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to highlight a short series of features, benefits, or steps.</li>\n<li>Use when a leading icon adds scannability or recognition to each item.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for navigation menus — use a <code>Nav</code> + <code>List</code> + <code>ListItem</code> composition.</li>\n<li>Do not use for tabular data — use <code>DataTable</code>.</li>\n<li>Do not use when icons would be the primary content — use a different pattern.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>Renders only the <code>&lt;ul&gt;</code> semantics. The list spacing, icon dimensions, and any visual rhythm are entirely the consumer's responsibility.</p>\n\n<h2>Styles</h2>\n\n<p>Consumer CSS targets the <code>icon-list</code> class. Typical treatments remove default list bullets (since the icon replaces the bullet) and apply consistent vertical spacing.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;ul&gt;</code> element with class <code>icon-list</code></li>\n<li>Verify <code>aria-label</code> is present when <code>label</code> is supplied</li>\n<li>Verify <code>aria-label</code> is absent when <code>label</code> is omitted</li>\n<li>Verify children render inside the list</li>\n<li>Verify additional HTML attributes pass through to the root <code>&lt;ul&gt;</code></li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use a single icon per list to maintain visual rhythm. Choose icons that match the tone (check, arrow, dot).</li>\n<li><strong>Developers</strong>: Always supply <code>IconListItem</code> children. Add <code>label</code> when the list's purpose is not clear from surrounding context.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>icon-list-item</code> — one item in an icon list with a leading icon slot</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>US Web Design System Icon List: https://designsystem.digital.gov/components/icon-list/</li>\n<li>HTML <code>&lt;ul&gt;</code>: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/ul</li>\n<li>WCAG 1.3.1 Info and Relationships: https://www.w3.org/WAI/WCAG22/Understanding/info-and-relationships</li>\n</ul>";
</script>

<svelte:head>
  <title>Icon List — Lily Design System</title>
  <meta name="description" content="An icon list is a &lt;ul&gt; of items where each item begins with a leading icon, suitable for short feature lists, benefit lists, or step-by-step summaries." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
