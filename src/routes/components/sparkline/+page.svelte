<script lang="ts">
  const html: string = "<h1>Sparkline</h1>\n\n<p>A sparkline is a compact, minimalist component used to represent trends or variations in data within a small, simple graph, typically embedded within text or tables. Unlike full-scale charts, sparklines display data as tiny, unadorned line, bar, or win/loss charts, offering a quick at-a-glance view of patterns such as fluctuations in sales, stock prices, or performance over time.</p>\n\n<p>This headless component provides a semantic container with <code>role=\"img\"</code> and an accessible label. The consumer is responsible for providing the actual SVG, canvas, or other visualization content as children.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> with <code>role=\"img\"</code> to semantically represent a graphical image</li>\n<li>Consumer provides all chart rendering (SVG, canvas, etc.) via the <code>children</code> slot</li>\n<li>Accepts <code>...restProps</code> for forwarding additional attributes to the container</li>\n<li>No internal state or effects -- purely a structural wrapper</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name describing the sparkline data, applied via <code>aria-label</code></li>\n<li><code>children</code>: slot (required) -- the visualization content to render inside the container</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;Sparkline label=\"Sales trend over past 7 days\"&gt;\n    &lt;svg&gt;&lt;!-- your sparkline SVG here --&gt;&lt;/svg&gt;\n&lt;/Sparkline&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive container for data visualization.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"img\"</code> -- identifies the container as a graphical image to assistive technologies</li>\n<li><code>aria-label={label}</code> -- provides an accessible name describing what the sparkline represents</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to display compact data trends inline within text, tables, or dashboards where a full chart would be too large.</li>\n<li>Use when a quick at-a-glance visual of data patterns (e.g., sales trends, stock movement, performance over time) is more useful than raw numbers.</li>\n<li>Avoid when users need to interact with or drill into the data; use a full chart or graph component instead.</li>\n<li>Consider providing the underlying data in a table or tooltip for accessibility.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when users must interact with or drill into the data — use a full chart (<code>LineChart</code>, <code>BarChart</code>, <code>AreaChart</code>).</li>\n<li>Do not use when precise numeric values matter — pair the trend with a <code>Statistic</code> or include the values in a table or tooltip.</li>\n<li>Do not use as a single point reading of a value within a range — use <code>Meter</code> instead.</li>\n<li>Do not use when the chart needs axes, gridlines, or legends — that contradicts the inline-trend purpose of a sparkline.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a semantic container with <code>role=\"img\"</code> and <code>aria-label</code> to identify the visualization for assistive technologies. The consumer provides the actual chart rendering (SVG, canvas, or a charting library) as children, along with all visual styling, dimensions, and colors.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.sparkline</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;span&gt;</code> element with class <code>sparkline</code></li>\n<li>Verify role=\"img\"` -- identifies the container as a graphical image to assistive technologies</li>\n<li>Verify aria-label={label}` -- provides an accessible name describing what the sparkline represents</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Keep sparklines small and unadorned -- omit axes, labels, and gridlines to maintain their compact nature. Use a consistent color and line weight across all sparklines in the same context.</li>\n<li><strong>Developers</strong>: Write a descriptive <code>label</code> that conveys the data trend (e.g., \"Sales up 12% over 7 days\") rather than just a title. Ensure the SVG or canvas has appropriate dimensions and does not cause layout shift.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>statistic</code> — a numeric value display with title, prefix, and suffix</li>\n<li><code>line-chart</code> — a line chart visualization connecting data points to display data</li>\n<li><code>meter</code> — a gauge displaying a scalar value within a known range</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA img role: https://www.w3.org/TR/wai-aria-1.2/#img</li>\n</ul>";
</script>

<svelte:head>
  <title>Sparkline — Lily Design System</title>
  <meta name="description" content="A sparkline is a compact, minimalist component used to represent trends or variations in data within a small, simple graph, typically embedded within text or tables. Unlike full-scale charts, spark..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
