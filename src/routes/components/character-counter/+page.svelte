<script lang="ts">
  const html: string = "<h1>Character Counter</h1>\n\n<p>A character counter displays the number of characters a user has typed into an input field, providing real-time feedback to help users stay within constraints. It is commonly used alongside text inputs with character limits such as tweets, SMS messages, form fields, or bios. The counter shows \"count / max\" when a maximum is provided, or just the count otherwise.</p>\n\n<p>The component uses <code>role=\"status\"</code> with <code>aria-live=\"polite\"</code> so screen readers announce updates without interrupting the user, and exposes data attributes for consumer CSS styling based on count state.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as a <code>&lt;span&gt;</code> element with <code>role=\"status\"</code> and <code>aria-live=\"polite\"</code> for non-intrusive screen reader announcements</li>\n<li>Uses derived value to compute <code>remaining</code> count and <code>overLimit</code> boolean from <code>count</code> and <code>max</code> props</li>\n<li>Displays \"count / max\" when <code>max</code> is provided, otherwise displays just the count</li>\n<li>Exposes data attributes for consumer CSS: <code>data-count</code>, <code>data-max</code>, <code>data-remaining</code>, <code>data-over-limit</code></li>\n<li>The <code>data-over-limit</code> attribute is only present when the count exceeds the max, enabling conditional styling</li>\n<li>Spreads <code>...restProps</code> on the root <code>&lt;span&gt;</code> element for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>count</code>: number (default: 0) -- current character count</li>\n<li><code>max</code>: number (optional) -- maximum allowed characters; when provided, enables remaining/over-limit tracking</li>\n<li><code>label</code>: string (optional) -- accessible label for the counter via aria-label</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Basic counter linked to a text-area-input:</p>\n\n<pre><code class=\"language-html\">&lt;CharacterCounter count={text.length} max={280} label=\"Characters remaining\" /&gt;</code></pre>\n\n<p>Counter with threshold display (consumer shows/hides based on data attributes):</p>\n\n<pre><code class=\"language-html\">&lt;CharacterCounter count={referralNotes.length} max={500} label=\"Referral note characters\" /&gt;</code></pre>\n\n<p>Counter without a maximum, showing only the current count:</p>\n\n<pre><code class=\"language-html\">&lt;CharacterCounter count={message.length} label=\"Message length\" /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive status display.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"status\"</code> -- identifies the element as a live region that presents advisory information</li>\n<li><code>aria-live=\"polite\"</code> -- screen readers announce updates at the next convenient pause, without interrupting the user</li>\n<li><code>aria-label</code> -- set from the <code>label</code> prop to provide context for what the counter represents</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use when there is a clear reason for limiting the number of characters, such as a database field constraint or regulatory requirement.</li>\n<li>Use when evidence shows users regularly enter more text than needed for the purpose of the field.</li>\n<li>Use to help users understand how much text they can still enter, with a dynamic count that updates as they type.</li>\n<li>Use with a threshold setting so the counter only appears after users have entered a significant portion of the limit, reducing visual noise.</li>\n<li>Use alongside text inputs that have a maximum length, such as referral notes, feedback fields, SMS messages, or bio descriptions.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use if the character limit could be increased or removed — relax backend constraints instead of restricting users.</li>\n<li>Do not use without first testing whether your service works without a character limit.</li>\n<li>Do not use for short fields like names or postcodes — use TextInput with an appropriate width instead.</li>\n<li>Do not use as a standalone component — pair it with TextAreaInput inside TextAreaInputWithCharacterCounter for a complete user experience.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless CharacterCounter component provides <code>role=\"status\"</code> with <code>aria-live=\"polite\"</code> for non-intrusive screen reader announcements, and exposes <code>data-count</code>, <code>data-max</code>, <code>data-remaining</code>, and <code>data-over-limit</code> attributes for styling hooks. The consumer provides all visual styling including typography, colors, positioning relative to the input, and over-limit warning styles.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.character-counter</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;span&gt;</code> element with class <code>character-counter</code></li>\n<li>Verify role=\"status\"` -- identifies the element as a live region that presents advisory information</li>\n<li>Verify aria-label<code> -- set from the </code>label` prop to provide context for what the counter represents</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Position the counter near the associated input field, typically below or to the right. Use a warning color when the user approaches or exceeds the limit.</li>\n<li><strong>Developers</strong>: Bind the <code>count</code> prop to your input's character length. Use the <code>data-over-limit</code> attribute in CSS to trigger visual warnings when the maximum is exceeded.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>text-area-input-with-character-counter</code> — a multi-line text area input with a caption below that is a character counter \"[number] of [maximum] characters\"</li>\n<li><code>character</code> — a single character display element</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/HTML/Element/span\">MDN span element</a></li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<span class=\"character-counter\">42/100</span>";
  const svelteSource: string = "// In your Svelte component:\nimport CharacterCounter from \"lily-design-system-svelte-headless/components/CharacterCounter/CharacterCounter.svelte\";\n\n<CharacterCounter>\n  <!-- content -->\n</CharacterCounter>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Character Counter — Lily Design System</title>
  <meta name="description" content="A character counter displays the number of characters a user has typed into an input field, providing real-time feedback to help users stay within constraints. It is commonly used alongside text in..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

