<script lang="ts">
  const html: string = "<h1>Net Promoter Score Picker</h1>\n\n<p>A Net Promoter Score (NPS) picker is a UI/UX component that allows users to select a score from 0 to 10, representing their likelihood to recommend a product, service, or organization. It is a widely used customer loyalty metric developed by Fred Reichheld and Bain &amp; Company. The component renders as a fieldset with a radiogroup role containing 11 radio buttons, one for each score value. Scores are typically categorized as Detractors (0-6), Passives (7-8), and Promoters (9-10). This headless component provides the semantic structure and accessibility features while allowing consumers to apply their own visual styling.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as <code>&lt;fieldset role=\"radiogroup\"&gt;</code> with 11 radio buttons (0-10)</li>\n<li>Each radio has <code>aria-label</code> with its numeric value for screen readers</li>\n<li>The <code>value</code> prop is two-way binding</li>\n<li>Default radio group name is \"nps\", customizable via <code>name</code> prop</li>\n<li>Radio values are strings (\"0\" through \"10\")</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible label for the radiogroup</li>\n<li><code>value</code>: string (default: \"\") -- currently selected score, bindable</li>\n<li><code>name</code>: string (default: \"nps\") -- name attribute for the radio group</li>\n<li><code>...restProps</code>: Any additional HTML attributes spread onto the fieldset</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>NPS survey question with endpoint labels:</p>\n\n<pre><code class=\"language-html\">&lt;p&gt;How likely are you to recommend us to a friend or colleague?&lt;/p&gt;\n&lt;NetPromoterScorePicker label=\"How likely are you to recommend us?\" value={score}&gt;\n    &lt;NetPromoterScorePickerButton value=\"0\" label=\"0 - Not at all likely\" onclick={handleClick} /&gt;\n    &lt;NetPromoterScorePickerButton value=\"1\" label=\"1\" onclick={handleClick} /&gt;\n    &lt;NetPromoterScorePickerButton value=\"2\" label=\"2\" onclick={handleClick} /&gt;\n    &lt;NetPromoterScorePickerButton value=\"3\" label=\"3\" onclick={handleClick} /&gt;\n    &lt;NetPromoterScorePickerButton value=\"4\" label=\"4\" onclick={handleClick} /&gt;\n    &lt;NetPromoterScorePickerButton value=\"5\" label=\"5\" onclick={handleClick} /&gt;\n    &lt;NetPromoterScorePickerButton value=\"6\" label=\"6\" onclick={handleClick} /&gt;\n    &lt;NetPromoterScorePickerButton value=\"7\" label=\"7\" onclick={handleClick} /&gt;\n    &lt;NetPromoterScorePickerButton value=\"8\" label=\"8\" onclick={handleClick} /&gt;\n    &lt;NetPromoterScorePickerButton value=\"9\" label=\"9\" onclick={handleClick} /&gt;\n    &lt;NetPromoterScorePickerButton value=\"10\" label=\"10 - Extremely likely\" onclick={handleClick} /&gt;\n&lt;/NetPromoterScorePicker&gt;\n&lt;p&gt;Selected score: {score}&lt;/p&gt;</code></pre>\n\n<p>With a custom radio group name:</p>\n\n<pre><code class=\"language-html\">&lt;NetPromoterScorePicker label=\"Rate our service\" value={score} name=\"service-nps\" /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Arrow keys navigate between radio buttons (native radio group behavior)</li>\n<li>Space selects the focused radio button</li>\n<li>Tab moves focus into/out of the radio group</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"radiogroup\"</code> on the fieldset</li>\n<li><code>aria-label</code> on the fieldset from the label prop</li>\n<li>Each radio has <code>aria-label</code> with its numeric value</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to collect a Net Promoter Score (0-10) from users in feedback forms, surveys, or satisfaction questionnaires.</li>\n<li>Use when you need a standardized NPS input asking \"How likely are you to recommend...\" on a 0-10 scale.</li>\n<li>Use in customer loyalty surveys where Detractor/Passive/Promoter classification is needed.</li>\n<li>Use when the NPS metric will be reported and benchmarked against industry standards.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for display-only NPS scores -- use NetPromoterScoreView instead.</li>\n<li>Do not use for general 1-5 numeric ratings -- use FiveStarRatingPicker.</li>\n<li>Do not use for satisfaction surveys with face labels -- use FiveFaceRatingPicker.</li>\n<li>Do not use for status indicators -- use RedAmberGreenPicker.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;fieldset&gt;</code> with <code>role=\"radiogroup\"</code> containing 11 radio buttons (0-10) with <code>aria-label</code> on each. The consumer provides all visual styling, including button layout, color coding for Detractor/Passive/Promoter ranges, and selected-state appearance.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.net-promoter-score-picker</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>net-promoter-score-picker</code></li>\n<li>Verify role=\"radiogroup\"` on the fieldset</li>\n<li>Verify aria-label` on the fieldset from the label prop</li>\n<li>Verify Each radio has <code>aria-label</code> with its numeric value</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Color-code the score ranges (0-6 Detractors, 7-8 Passives, 9-10 Promoters) and add endpoint labels such as \"Not at all likely\" and \"Extremely likely\".</li>\n<li><strong>Developers</strong>: The <code>value</code> prop is two-way bindable and stores the selected score as a string (\"0\" through \"10\"). Use the <code>name</code> prop to differentiate multiple NPS pickers on one page.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>NetPromoterScorePicker is a compound component that can contain NetPromoterScorePickerButton children. The picker provides the radiogroup container, while each button represents one score value.</p>\n\n<pre><code>NetPromoterScorePicker (role=\"radiogroup\") → NetPromoterScorePickerButton (0-10)</code></pre>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>net-promoter-score-picker-button</code> — a picker button for selecting a 0-10 Net Promoter Score</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>Net Promoter Score: https://en.wikipedia.org/wiki/Net_promoter_score</li>\n<li>WAI-ARIA Radiogroup Role: https://www.w3.org/TR/wai-aria-1.2/#radiogroup</li>\n</ul>";
</script>

<svelte:head>
  <title>Net Promoter Score Picker — Lily Design System</title>
  <meta name="description" content="A Net Promoter Score (NPS) picker is a UI/UX component that allows users to select a score from 0 to 10, representing their likelihood to recommend a product, service, or organization. It is a wide..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
