<script lang="ts">
  const html: string = "<h1>Input Group</h1>\n\n<p>An input group wraps a form input together with optional prefix and suffix addons (icons, units, currency symbols, action buttons) inside a single ARIA group so that they are perceived and announced as one composite control.</p>\n\n<p>Use it for currency fields with leading symbols, search fields with trailing icon buttons, percentage fields with trailing units, and any input where contextual addons clarify the value.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> with <code>role=\"group\"</code> and <code>class=\"input-group\"</code></li>\n<li>Optional prefix renders inside <code>&lt;span class=\"input-group-prefix\"&gt;</code></li>\n<li>Optional suffix renders inside <code>&lt;span class=\"input-group-suffix\"&gt;</code></li>\n<li>Children (the actual input) sit between the prefix and suffix</li>\n<li><code>aria-label</code> is rendered when the <code>label</code> prop is provided</li>\n<li>Does not modify the inner input — the consumer is responsible for label association on the input itself</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (optional) -- aria-label for the group</li>\n<li><code>prefix</code>: slot (optional) -- content rendered before the input</li>\n<li><code>suffix</code>: slot (optional) -- content rendered after the input</li>\n<li><code>children</code>: slot -- the input the consumer places inside</li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Currency input with leading symbol:</p>\n\n<pre><code class=\"language-html\">&lt;InputGroup label=\"Amount\"&gt;\n  &lt;span slot=\"prefix\"&gt;$&lt;/span&gt;\n  &lt;TextInput label=\"Amount\" value={amount} onChange={setAmount} /&gt;\n&lt;/InputGroup&gt;</code></pre>\n\n<p>Search input with trailing icon button:</p>\n\n<pre><code class=\"language-html\">&lt;InputGroup label=\"Search\"&gt;\n  &lt;SearchInput label=\"Search\" value={query} onChange={setQuery} /&gt;\n  &lt;IconButton slot=\"suffix\" label=\"Submit search\" onclick={submit}&gt;\n    🔍\n  &lt;/IconButton&gt;\n&lt;/InputGroup&gt;</code></pre>\n\n<p>Percentage input with trailing unit:</p>\n\n<pre><code class=\"language-html\">&lt;InputGroup label=\"Discount\"&gt;\n  &lt;NumberInput label=\"Discount\" value={discount} onChange={setDiscount} /&gt;\n  &lt;span slot=\"suffix\"&gt;%&lt;/span&gt;\n&lt;/InputGroup&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>The wrapped input retains its native keyboard behavior.</li>\n<li>Focusable controls inside <code>prefix</code> / <code>suffix</code> slots (e.g., IconButton) follow their own keyboard behavior.</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"group\"</code> on the wrapping <code>&lt;div&gt;</code> so assistive technology treats the addons and input as one unit</li>\n<li><code>aria-label</code> rendered when <code>label</code> is provided</li>\n<li>The inner input still requires its own accessible name (label, aria-label, or aria-labelledby)</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to attach a prefix or suffix addon to a form input.</li>\n<li>Use for currency, percentage, and unit inputs.</li>\n<li>Use for search inputs with trailing action buttons.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use as a generic form layout (use Field for label/input/error composition).</li>\n<li>Do not omit a label on the inner input — the group's aria-label does not replace the input's accessible name.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>Provides only the structural wrapper, the prefix and suffix slot containers, and the ARIA group semantics. Visual treatment such as connected borders, addon backgrounds, and focus styling is entirely consumer-supplied.</p>\n\n<h2>Styles</h2>\n\n<p>Consumer CSS targets <code>.input-group</code>, <code>.input-group-prefix</code>, and <code>.input-group-suffix</code> to apply visual treatment such as shared borders, addon backgrounds, and connected radii.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>input-group</code> and <code>role=\"group\"</code></li>\n<li>Verify <code>aria-label</code> is rendered when the <code>label</code> prop is provided</li>\n<li>Verify the prefix renders inside <code>&lt;span class=\"input-group-prefix\"&gt;</code> when provided</li>\n<li>Verify the suffix renders inside <code>&lt;span class=\"input-group-suffix\"&gt;</code> when provided</li>\n<li>Verify children render between prefix and suffix</li>\n<li>Verify focus order is prefix → input → suffix</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use prefix and suffix addons sparingly — overusing them clutters forms. Reserve them for unit symbols, currency markers, and trailing action buttons.</li>\n<li><strong>Developers</strong>: Continue to apply a label on the inner input itself (via <code>&lt;label&gt;</code>, <code>aria-label</code>, or <code>aria-labelledby</code>); the group's <code>label</code> prop names the composite control, not the input.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>input</code> — a generic HTML input element</li>\n<li><code>field</code> — a form field wrapper with label, input, and error message</li>\n<li><code>form</code> — a form element for collecting and submitting user data</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA group role: https://www.w3.org/TR/wai-aria-1.2/#group</li>\n<li>WCAG 1.3.1 Info and Relationships: https://www.w3.org/WAI/WCAG22/Understanding/info-and-relationships</li>\n</ul>";
</script>

<svelte:head>
  <title>Input Group — Lily Design System</title>
  <meta name="description" content="An input group wraps a form input together with optional prefix and suffix addons (icons, units, currency symbols, action buttons) inside a single ARIA group so that they are perceived and announce..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
