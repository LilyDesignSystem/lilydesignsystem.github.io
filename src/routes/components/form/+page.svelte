<script lang="ts">
  const html: string = "<h1>Form</h1>\n\n<p>A form component wraps a native HTML <code>&lt;form&gt;</code> element with accessible labeling, automatic submit default prevention, and reset support. It simplifies form handling by intercepting the submit event and calling <code>preventDefault()</code> before passing it to the consumer's callback, which is the most common pattern in single-page applications.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a native <code>&lt;form&gt;</code> element with <code>aria-label</code> for accessible naming</li>\n<li>Automatically calls <code>event.preventDefault()</code> on submit before invoking the <code>onsubmit</code> callback</li>\n<li>Passes the raw <code>onreset</code> callback directly to the form (no default prevention)</li>\n<li>Accepts a <code>children</code> slot for form content (inputs, buttons, etc.)</li>\n<li>Spreads <code>restProps</code> for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name for the form via <code>aria-label</code></li>\n<li><code>onsubmit</code>: function (default: <code>undefined</code>) -- callback receiving <code>SubmitEvent</code> (default already prevented)</li>\n<li><code>onreset</code>: function (default: <code>undefined</code>) -- callback receiving <code>Event</code> on form reset</li>\n<li><code>children</code>: slot (required) -- form content to render inside</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;form&gt;</code></li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Login form with email and password fields:</p>\n\n<pre><code class=\"language-html\">&lt;Form label=\"Sign in\" onsubmit={handleLogin}&gt;\n  &lt;Field label=\"Email\" required&gt;\n    &lt;EmailInput label=\"Email\" value={email} required /&gt;\n  &lt;/Field&gt;\n  &lt;Field label=\"Password\" required&gt;\n    &lt;PasswordInput label=\"Password\" value={password} required /&gt;\n  &lt;/Field&gt;\n  &lt;Button type=\"submit\"&gt;Sign in&lt;/Button&gt;\n&lt;/Form&gt;</code></pre>\n\n<p>Contact form with reset support:</p>\n\n<pre><code class=\"language-html\">&lt;Form label=\"Contact us\" onsubmit={handleSubmit} onreset={handleReset}&gt;\n  &lt;Field label=\"Name\" required&gt;\n    &lt;TextInput label=\"Name\" value={name} required /&gt;\n  &lt;/Field&gt;\n  &lt;Field label=\"Message\" required&gt;\n    &lt;TextAreaInput label=\"Message\" value={message} required /&gt;\n  &lt;/Field&gt;\n  &lt;SubmitInput value=\"Send message\" /&gt;\n  &lt;ResetInput value=\"Clear form\" /&gt;\n&lt;/Form&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None beyond native form behavior -- Enter key submits the form when a text input is focused (browser default).</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label=\"...\"</code> -- provides an accessible name for the form, helping screen readers identify its purpose</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use as the top-level form element for collecting and submitting user data.</li>\n<li>Use with Field components for structured form layout.</li>\n<li>Use when you need automatic <code>preventDefault()</code> on submit, which is standard in single-page applications.</li>\n<li>Use to wrap any collection of form inputs that are submitted together as a unit.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for read-only data display -- use SummaryList instead.</li>\n<li>Do not use for inline editing -- use EditableForm instead.</li>\n<li>Do not use to wrap unrelated input groups; use separate Form components for distinct submission actions.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a native <code>&lt;form&gt;</code> element with <code>aria-label</code>, automatic submit default prevention, and reset support. The consumer provides all visual styling including layout, spacing, and button placement.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.form</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;form&gt;</code> element with class <code>form</code></li>\n<li>Verify aria-label=\"...\"` -- provides an accessible name for the form, helping screen readers identify its purpose</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Group related fields visually using spacing or Fieldset components. Place the primary submit button in a predictable location.</li>\n<li><strong>Developers</strong>: Use the Form composition pattern: Form wraps Field components, which in turn wrap input components. Pair with ErrorSummary for validation feedback.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>Form uses the Form composition pattern: Form contains Field components, which wrap input components (TextInput, EmailInput, Select, etc.). Add ErrorSummary for validation errors and Button for submission actions.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>field</code> — a form field wrapper with label, input, and error message</li>\n<li><code>fieldset</code> — a group of related form fields with a legend</li>\n<li><code>input-group</code> — a group wrapping an input with prefix and suffix addons</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN <code>&lt;form&gt;</code>: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/form</li>\n<li>WAI Forms Tutorial: https://www.w3.org/WAI/tutorials/forms/</li>\n</ul>";
</script>

<svelte:head>
  <title>Form — Lily Design System</title>
  <meta name="description" content="A form component wraps a native HTML &lt;form&gt; element with accessible labeling, automatic submit default prevention, and reset support. It simplifies form handling by intercepting the submit event an..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
