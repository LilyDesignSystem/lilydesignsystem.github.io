<script lang="ts">
  const html: string = "<h1>Red Amber Green Picker</h1>\n\n<p>A RAG (Red/Amber/Green) picker is a UI/UX component that allows users to select a traffic-light status value from a dropdown. RAG status is one of the most widely used status indicators in project management, dashboards, and reporting tools. Red indicates a critical problem that needs immediate attention, amber signals caution or minor issues, and green means everything is on track. The component renders as a select element with appropriate accessibility attributes, providing native keyboard navigation and screen reader support. This headless component provides the semantic structure while allowing consumers to apply their own visual styling.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as <code>&lt;select aria-label={label}&gt;</code> with three options: red, amber, green</li>\n<li>The <code>value</code> prop is two-way binding</li>\n<li>Uses native select keyboard navigation</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible label for the select</li>\n<li><code>value</code>: string (default: \"\") -- currently selected status, bindable</li>\n<li><code>...restProps</code>: Any additional HTML attributes spread onto the select</li>\n</ul>\n\n<h2>Options</h2>\n\n<ul>\n<li><code>red</code>: Critical problem, needs immediate attention</li>\n<li><code>amber</code>: Caution, minor issues or at risk</li>\n<li><code>green</code>: On track, everything is fine</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Risk assessment in a project dashboard:</p>\n\n<pre><code class=\"language-html\">&lt;form&gt;\n  &lt;fieldset&gt;\n    &lt;legend&gt;Sprint 14 Status&lt;/legend&gt;\n    &lt;RedAmberGreenPicker label=\"Overall sprint health\"&gt;\n      &lt;RedAmberGreenPickerButton value=\"red\" label=\"Red - Critical\" onclick={handleSelect} /&gt;\n      &lt;RedAmberGreenPickerButton value=\"amber\" label=\"Amber - At risk\" selected onclick={handleSelect} /&gt;\n      &lt;RedAmberGreenPickerButton value=\"green\" label=\"Green - On track\" onclick={handleSelect} /&gt;\n    &lt;/RedAmberGreenPicker&gt;\n  &lt;/fieldset&gt;\n&lt;/form&gt;</code></pre>\n\n<p>Compliance tracking with pre-selected status:</p>\n\n<pre><code class=\"language-html\">&lt;RedAmberGreenPicker label=\"GDPR compliance status\"&gt;\n  &lt;RedAmberGreenPickerButton value=\"red\" label=\"Red - Non-compliant\" onclick={handleSelect} /&gt;\n  &lt;RedAmberGreenPickerButton value=\"amber\" label=\"Amber - Partial\" onclick={handleSelect} /&gt;\n  &lt;RedAmberGreenPickerButton value=\"green\" label=\"Green - Compliant\" selected onclick={handleSelect} /&gt;\n&lt;/RedAmberGreenPicker&gt;</code></pre>\n\n<p>Health dashboard with data attributes:</p>\n\n<pre><code class=\"language-html\">&lt;RedAmberGreenPicker label=\"Patient alert level\" data-patient-id=\"P-1042\"&gt;\n  &lt;RedAmberGreenPickerButton value=\"red\" label=\"Red - Immediate attention\" onclick={handleSelect} /&gt;\n  &lt;RedAmberGreenPickerButton value=\"amber\" label=\"Amber - Monitor closely\" selected onclick={handleSelect} /&gt;\n  &lt;RedAmberGreenPickerButton value=\"green\" label=\"Green - Stable\" onclick={handleSelect} /&gt;\n&lt;/RedAmberGreenPicker&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Arrow keys navigate options (native select behavior)</li>\n<li>Enter/Space opens the dropdown</li>\n<li>Escape closes the dropdown</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label</code> on the select from the label prop</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for selecting a traffic-light status (red/amber/green) in risk assessments and project trackers.</li>\n<li>Use in compliance tracking dashboards where items need a three-level severity rating.</li>\n<li>Use in health dashboards where a simple status indicator captures the current state.</li>\n<li>Use when a three-level severity or progress indicator is sufficient for the use case.</li>\n<li>Use when users need to set or change a RAG status interactively.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for display-only status -- use <a href=\"../red-amber-green-view/\">RedAmberGreenView</a> instead.</li>\n<li>Do not use when five-level granularity is needed -- use <a href=\"../red-orange-yellow-green-blue-picker/\">RedOrangeYellowGreenBluePicker</a> instead.</li>\n<li>Do not use for binary on/off states -- use <a href=\"../switch-button/\">SwitchButton</a> or <a href=\"../checkbox-input/\">CheckboxInput</a> instead.</li>\n<li>Do not use for free-form status text -- use <a href=\"../select/\">Select</a> with custom options instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>The RedAmberGreenPicker headless component provides a <code>&lt;select&gt;</code> with <code>aria-label</code> and three predefined options (red, amber, green) with two-way value binding. Native select keyboard navigation is fully supported. The consumer provides all visual styling, including color coding.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.red-amber-green-picker</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>red-amber-green-picker</code></li>\n<li>Verify aria-label` on the select from the label prop</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Color-code the options (red, amber, green) but also include text labels, as color alone is insufficient for users with color vision deficiencies.</li>\n<li><strong>Developers</strong>: Initialize the <code>value</code> prop to a meaningful default rather than leaving it empty, so the status is always in a valid state.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>RedAmberGreenPicker is the container for RedAmberGreenPickerButton children. Use <code>&lt;RedAmberGreenPicker&gt;</code> to wrap <code>&lt;RedAmberGreenPickerButton&gt;</code> elements, where each button represents one RAG status level with <code>aria-pressed</code> toggle state.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>red-amber-green-picker-button</code> — a picker button for selecting a red/amber/green status</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>RAG Status: https://en.wikipedia.org/wiki/Traffic_light_rating_system</li>\n</ul>";
</script>

<svelte:head>
  <title>Red Amber Green Picker — Lily Design System</title>
  <meta name="description" content="A RAG (Red/Amber/Green) picker is a UI/UX component that allows users to select a traffic-light status value from a dropdown. RAG status is one of the most widely used status indicators in project ..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
