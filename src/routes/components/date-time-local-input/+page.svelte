<script lang="ts">
  const html: string = "<h1>DateTime Local Input</h1>\n\n<p>A datetime-local input allows users to select both a date and time using the native <code>&lt;input type=\"datetime-local\"&gt;</code> element. It provides accessible labelling via <code>aria-label</code> and a bindable value for reactive form state.</p>\n\n<p>This component is useful for scheduling interfaces, event creation forms, appointment booking, and any scenario where both date and time must be captured in a single field. The value follows the format <code>YYYY-MM-DDThh:mm</code>, and the browser provides the native date-time picker UI.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a single <code>&lt;input type=\"datetime-local\"&gt;</code> element with no wrapper</li>\n<li>Uses <code>aria-label</code> for accessible naming instead of a visible <code>&lt;label&gt;</code> element</li>\n<li>Supports two-way binding on the <code>value</code> prop</li>\n<li>The browser provides the native datetime picker UI</li>\n<li>Spreads <code>restProps</code> onto the input element for consumer extensibility</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name applied via <code>aria-label</code></li>\n<li><code>value</code>: string (default: \"\") -- bindable datetime string (format: YYYY-MM-DDThh:mm)</li>\n<li><code>min</code>: string (default: undefined) -- minimum allowed datetime</li>\n<li><code>max</code>: string (default: undefined) -- maximum allowed datetime</li>\n<li><code>required</code>: boolean (default: false) -- whether the input is required for form submission</li>\n<li><code>disabled</code>: boolean (default: false) -- whether the input is disabled</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the input element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Scheduling an event with constrained date-time range:</p>\n\n<pre><code class=\"language-html\">&lt;DateTimeLocalInput label=\"Meeting start\" value={meetingStart} min=\"2026-01-01T08:00\" max=\"2026-12-31T18:00\" required /&gt;</code></pre>\n\n<p>Booking an appointment:</p>\n\n<pre><code class=\"language-html\">&lt;DateTimeLocalInput label=\"Appointment date and time\" value={appointmentDatetime} required /&gt;</code></pre>\n\n<p>Read-only locked departure time:</p>\n\n<pre><code class=\"language-html\">&lt;DateTimeLocalInput label=\"Departure\" value={departure} disabled={true} /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Moves focus to and from the datetime input (native browser behavior)</li>\n<li>Arrow keys: Navigate within the datetime picker fields (native browser behavior)</li>\n<li>Enter: Opens or confirms the datetime picker (native browser behavior)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides an accessible name for the datetime input since there is no visible <code>&lt;label&gt;</code> element</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for entering a combined date and time without timezone in a single field.</li>\n<li>Use when the browser's native datetime-local picker is appropriate.</li>\n<li>Use for scheduling events, booking appointments, or setting deadlines.</li>\n<li>Use when the time zone is not relevant or is handled separately.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when you need a \"Now\" shortcut button -- use DateTimeNowInput instead.</li>\n<li>Do not use when day, month, and year need separate fields -- use DateField instead.</li>\n<li>Do not use when only a date is needed -- use DateInput instead.</li>\n<li>Do not use when only a time is needed -- use TimeInput instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a native <code>&lt;input type=\"datetime-local\"&gt;</code> with accessible labeling via <code>aria-label</code>, two-way value binding, and standard form attributes. The browser supplies the native datetime picker UI. The consumer provides all visual styling, layout, and any custom validation feedback.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.date-time-local-input</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;input&gt;</code> element with class <code>date-time-local-input</code></li>\n<li>Verify aria-label={label}<code> -- provides an accessible name for the datetime input since there is no visible </code>&lt;label&gt;` element</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Place the datetime input near contextual information (e.g., time zone indicator) so users understand the expected format. Ensure sufficient width for the native picker to display properly.</li>\n<li><strong>Developers</strong>: Use the <code>min</code> and <code>max</code> props to constrain selectable datetimes. Remember that the value format is <code>YYYY-MM-DDThh:mm</code>, not a full ISO 8601 string.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>date-input</code> — an input for entering a date value &lt;input type=\"date\"&gt;</li>\n<li><code>time-input</code> — an input for entering a time value &lt;input type=\"time\"&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN datetime-local input: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/datetime-local</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<label class=\"label\" for=\"demo-input\">Label</label><input class=\"date-time-local-input\" type=\"datetime-local\" id=\"demo-input\" aria-label=\"DateTimeLocalInput\" />";
  const svelteSource: string = "// In your Svelte component:\nimport DateTimeLocalInput from \"lily-design-system-svelte-headless/components/DateTimeLocalInput/DateTimeLocalInput.svelte\";\n\n<DateTimeLocalInput label=\"Label\" />\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>DateTime Local Input — Lily Design System</title>
  <meta name="description" content="A datetime-local input allows users to select both a date and time using the native &lt;input type=&quot;datetime-local&quot;&gt; element. It provides accessible labelling via aria-label and a bindable value for r..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

