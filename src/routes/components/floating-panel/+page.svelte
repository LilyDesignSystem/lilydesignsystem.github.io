<script lang="ts">
  const html: string = "<h1>Floating Panel</h1>\n\n<p>A floating panel is a container that overlays page content, typically used for tooltips, popovers, dropdown menus, or contextual UI. It renders conditionally based on an <code>open</code> prop, making it suitable for toggle-based visibility patterns. The component is headless, so consumers are responsible for positioning the panel relative to a trigger element using their own CSS.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> with <code>role=\"region\"</code> only when the <code>open</code> prop is true</li>\n<li>Uses conditional rendering (fully removed from DOM when closed)</li>\n<li>Accepts a <code>children</code> slot for panel content</li>\n<li>Provides <code>aria-label</code> for accessible naming of the panel region</li>\n<li>Spreads <code>restProps</code> for consumer customization (e.g., positioning attributes)</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>open</code>: boolean (default: <code>false</code>) -- whether the panel is visible</li>\n<li><code>label</code>: string (required) -- accessible name for the panel region via <code>aria-label</code></li>\n<li><code>children</code>: slot (required) -- panel content to render inside</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;div&gt;</code></li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Chat widget floating above page content:</p>\n\n<pre><code class=\"language-html\">&lt;button onclick={() =&gt; (chatOpen = !chatOpen)}&gt;Chat&lt;/button&gt;\n&lt;FloatingPanel open={chatOpen} label=\"Live chat\"&gt;\n  &lt;h3&gt;Support chat&lt;/h3&gt;\n  &lt;div class=\"messages\"&gt;...&lt;/div&gt;\n  &lt;input type=\"text\" placeholder=\"Type a message...\" /&gt;\n&lt;/FloatingPanel&gt;</code></pre>\n\n<p>Help panel triggered by a button:</p>\n\n<pre><code class=\"language-html\">&lt;FloatingPanel open={showHelp} label=\"Help\"&gt;\n  &lt;h3&gt;Need assistance?&lt;/h3&gt;\n  &lt;p&gt;Contact support at help@example.com&lt;/p&gt;\n&lt;/FloatingPanel&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive container. Consumers should implement focus management and Escape-to-close behavior as needed for their use case.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"region\"</code> -- identifies the panel as a landmark region</li>\n<li><code>aria-label=\"...\"</code> -- provides an accessible name for the region</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for a panel that floats above page content, such as a chat widget or help panel.</li>\n<li>Use for tooltips, popovers, dropdown menus, or contextual UI that overlays page content.</li>\n<li>Use when content should be conditionally visible based on a toggle or trigger interaction.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for modal overlays that block interaction with the rest of the page -- use Dialog instead.</li>\n<li>Do not use for slide-in panels -- use Drawer or Sheet instead.</li>\n<li>Do not use for persistent content that should always be visible -- use Panel or Card instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;div&gt;</code> with <code>role=\"region\"</code> and <code>aria-label</code> that conditionally renders based on the <code>open</code> prop. The consumer provides all visual styling including positioning, shadows, borders, and z-index layering.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.floating-panel</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>floating-panel</code></li>\n<li>Verify role=\"region\"` -- identifies the panel as a landmark region</li>\n<li>Verify aria-label=\"...\"` -- provides an accessible name for the region</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Ensure the floating panel has sufficient contrast and shadow to distinguish it from underlying content. Consider adding an arrow or caret pointing to the trigger element.</li>\n<li><strong>Developers</strong>: Implement focus management and Escape-to-close behavior in your consumer code. Use CSS absolute/fixed positioning relative to the trigger element.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>panel</code> — a generic content panel with optional heading</li>\n<li><code>popover</code> — a floating content box anchored to a trigger element</li>\n<li><code>drawer</code> — a panel that slides in from the edge of the screen</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Practices - Disclosure Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/disclosure/</li>\n</ul>";
</script>

<svelte:head>
  <title>Floating Panel — Lily Design System</title>
  <meta name="description" content="A floating panel is a container that overlays page content, typically used for tooltips, popovers, dropdown menus, or contextual UI. It renders conditionally based on an open prop, making it suitab..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
