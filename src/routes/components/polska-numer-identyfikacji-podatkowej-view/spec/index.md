# PolskaNumerIdentyfikacjiPodatkowejView doc page spec

This page documents the polska-numer-identyfikacji-podatkowej-view component. See `components/polska-numer-identyfikacji-podatkowej-view/` for the canonical spec.
