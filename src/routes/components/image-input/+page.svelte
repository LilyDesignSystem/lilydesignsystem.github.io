<script lang="ts">
  const html: string = "<h1>Image Input</h1>\n\n<p>ImageInput is a headless graphical submit button that renders a native <code>&lt;input type=\"image\"&gt;</code> element. It displays an image defined by the <code>src</code> attribute and submits the form when clicked. The <code>alt</code> attribute displays if the image source is missing.</p>\n\n<p>Use this component when you need a graphical submit button in forms, such as custom-styled submit buttons, clickable image actions, or branded form submission controls.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a native <code>&lt;input type=\"image\"&gt;</code> element for graphical form submission</li>\n<li>The browser automatically triggers the containing form's <code>submit</code> event when clicked</li>\n<li>Submits the click coordinates (x, y) as part of the form data</li>\n<li>The <code>src</code> prop specifies the image URL to display</li>\n<li>The <code>alt</code> prop provides alternative text when the image cannot be displayed and serves as the accessible name</li>\n<li>Supports <code>width</code> and <code>height</code> props for image dimensions</li>\n<li>Supports <code>disabled</code> to prevent interaction</li>\n<li>Spreads <code>restProps</code> onto the input for consumer customization</li>\n<li>No additional ARIA attributes needed beyond the native semantics of <code>&lt;input type=\"image\"&gt;</code></li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>src</code>: string (required) -- URL of the image to display</li>\n<li><code>alt</code>: string (required) -- alternative text for the image; serves as accessible name</li>\n<li><code>width</code>: number | undefined (default: <code>undefined</code>) -- width of the image in pixels</li>\n<li><code>height</code>: number | undefined (default: <code>undefined</code>) -- height of the image in pixels</li>\n<li><code>disabled</code>: boolean (default: <code>false</code>) -- whether the button is disabled</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;input&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;ImageInput src=\"/icons/submit.png\" alt=\"Submit form\" /&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;ImageInput src=\"/icons/search.svg\" alt=\"Search\" width={24} height={24} /&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;ImageInput src=\"/branding/go-button.png\" alt=\"Go\" disabled={!formValid} /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Enter: activates the image button and submits the form</li>\n<li>Space: activates the image button and submits the form</li>\n</ul>\n\n<p>Note: keyboard interactions are provided natively by the <code>&lt;input type=\"image\"&gt;</code> element.</p>\n\n<h2>ARIA</h2>\n\n<p>No additional ARIA attributes are needed. The native <code>&lt;input type=\"image\"&gt;</code> element has an implicit <code>button</code> role and the <code>alt</code> attribute serves as its accessible name.</p>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use as a graphical submit button inside HTML forms when you want a clickable image instead of a text button.</li>\n<li>Use when branding or design requires a custom image as the form submission trigger.</li>\n<li>Avoid when you need a standard text-based submit button; use SubmitInput instead.</li>\n<li>Avoid when you need a non-submit image button; use a Button with an Image child instead.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when a plain text submit button is sufficient — use <code>SubmitInput</code> for that.</li>\n<li>Do not use for a non-submit image button (no form submission, just a click handler) — use <code>Button</code> with an <code>Image</code> child or use <code>IconButton</code>.</li>\n<li>Do not use as a generic image — use <code>Image</code> for non-interactive images.</li>\n<li>Do not use without meaningful <code>alt</code> text; the alt is the accessible name and is required for screen reader users.</li>\n<li>Do not rely on the click coordinates submitted by <code>&lt;input type=\"image\"&gt;</code> (<code>name.x</code>, <code>name.y</code>) for new code — modern UIs should not depend on this legacy behavior.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component renders a native <code>&lt;input type=\"image\"&gt;</code> element with configurable <code>src</code>, <code>alt</code>, <code>width</code>, <code>height</code>, and <code>disabled</code> props. It provides built-in form submission behavior with no additional ARIA needed. The consumer provides all visual styling including borders, shadows, hover/focus states, and sizing.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.image-input</code> class for targeting. Common styling includes removing default borders, adding hover effects, and controlling dimensions.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders an <code>&lt;input&gt;</code> element with <code>type=\"image\"</code></li>\n<li>Verify <code>src</code> is set from the <code>src</code> prop</li>\n<li>Verify <code>alt</code> is set from the <code>alt</code> prop</li>\n<li>Verify <code>width</code> and <code>height</code> attributes are set when provided</li>\n<li>Verify <code>disabled</code> attribute propagates correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Ensure the image clearly communicates its submit action. Provide sufficient contrast and hover/focus indicators. Always include meaningful alt text for accessibility.</li>\n<li><strong>Developers</strong>: Always provide a descriptive <code>alt</code> prop for screen reader users. Consider providing <code>width</code> and <code>height</code> to prevent layout shift during image loading.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>image</code> — an image element with alt text</li>\n<li><code>button-input</code> — an input element of type button for form actions &lt;input type=\"button\"&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN input type=\"image\": https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/image</li>\n<li>HTML spec input type=\"image\": https://html.spec.whatwg.org/multipage/input.html#image-button-state-(type=image)</li>\n</ul>";
</script>

<svelte:head>
  <title>Image Input — Lily Design System</title>
  <meta name="description" content="ImageInput is a headless graphical submit button that renders a native &lt;input type=&quot;image&quot;&gt; element. It displays an image defined by the src attribute and submits the form when clicked. The alt att..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
