<script lang="ts">
  const html: string = "<h1>URL Input</h1>\n\n<p>A URL input is a UI/UX component that provides a text field specifically for entering web addresses. It uses the native HTML <code>&lt;input type=\"url\"&gt;</code> element, which provides built-in URL validation and may trigger URL-specific keyboard layouts on mobile devices. The component includes aria-label for accessibility and supports bindable value for two-way data binding, along with standard form attributes like required and disabled.</p>\n\n<p>URL inputs are commonly used in profile forms, link submission pages, and any context where users need to enter a valid web address. The browser's built-in validation ensures the entered value matches URL format before form submission.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a single <code>&lt;input type=\"url\"&gt;</code> element for semantic URL input with built-in validation</li>\n<li>Supports two-way binding for the <code>value</code> prop enabling two-way data binding with two-way <code>value</code> binding</li>\n<li>The <code>aria-label</code> prop provides an accessible name since there is no visible <code>&lt;label&gt;</code> element</li>\n<li>Supports rest props via <code>...restProps</code> for extensibility (e.g., <code>id</code>, <code>name</code>, <code>placeholder</code>, <code>pattern</code>, <code>class</code>)</li>\n<li>Browser-native validation requires a valid URL format (including protocol) on form submission when <code>required</code> is set</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name applied via <code>aria-label</code></li>\n<li><code>value</code>: string (default: \"\") -- current URL value, bindable with two-way <code>value</code> binding</li>\n<li><code>required</code>: boolean (default: false) -- whether the field must be filled before form submission</li>\n<li><code>disabled</code>: boolean (default: false) -- whether the input is disabled</li>\n<li><code>...restProps</code>: spread onto the <code>&lt;input&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Basic website URL input:</p>\n\n<pre><code class=\"language-html\">&lt;UrlInput label=\"Website URL\" value={websiteUrl} placeholder=\"https://example.com\" /&gt;</code></pre>\n\n<p>Required portfolio link in a profile form:</p>\n\n<pre><code class=\"language-html\">&lt;UrlInput label=\"Portfolio link\" value={portfolioUrl} required /&gt;</code></pre>\n\n<p>Webhook URL configuration field:</p>\n\n<pre><code class=\"language-html\">&lt;Field label=\"Webhook URL\" required&gt;\n  &lt;UrlInput label=\"Webhook URL\" value={webhookUrl} required placeholder=\"https://api.example.com/webhook\" /&gt;\n  &lt;Hint&gt;We will send POST requests to this URL when events occur.&lt;/Hint&gt;\n&lt;/Field&gt;</code></pre>\n\n<p>Social media link in a user profile form:</p>\n\n<pre><code class=\"language-html\">&lt;Form label=\"Social links\" onsubmit={saveProfile}&gt;\n  &lt;Field label=\"Personal website\"&gt;\n    &lt;UrlInput label=\"Personal website\" value={website} placeholder=\"https://yoursite.com\" /&gt;\n  &lt;/Field&gt;\n  &lt;Field label=\"LinkedIn profile\"&gt;\n    &lt;UrlInput label=\"LinkedIn profile URL\" value={linkedin} placeholder=\"https://linkedin.com/in/username\" /&gt;\n  &lt;/Field&gt;\n  &lt;Button type=\"submit\"&gt;Save&lt;/Button&gt;\n&lt;/Form&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None beyond native text input behavior. The <code>&lt;input type=\"url\"&gt;</code> element supports standard text editing keys (typing, selection, clipboard operations) natively.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides the accessible name for the URL input since no visible label element is associated</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use when users need to enter a web address, such as in profile forms, link submission pages, or configuration settings.</li>\n<li>Use when built-in browser URL validation requiring a protocol like <code>https://</code> is appropriate.</li>\n<li>Use for webhook URL configuration, callback URLs, or API endpoint settings.</li>\n<li>Use for social media profile link fields in user settings.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for general text that may contain URLs mixed with other content -- use TextInput instead.</li>\n<li>Do not use for email addresses -- use EmailInput instead.</li>\n<li>Do not use when you need to accept URLs without a protocol prefix -- use TextInput with a custom pattern instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a native <code>&lt;input type=\"url\"&gt;</code> element with <code>aria-label</code>, two-way value binding, and support for <code>required</code> and <code>disabled</code> states. Built-in browser validation ensures URL format compliance. The consumer provides all visual styling and any surrounding labels or hints.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.url-input</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;input&gt;</code> element with class <code>url-input</code></li>\n<li>Verify aria-label={label}` -- provides the accessible name for the URL input since no visible label element is associated</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Include a placeholder (e.g., \"https://example.com\") to clarify the expected format. Show validation errors inline near the input.</li>\n<li><strong>Developers</strong>: The browser requires a valid URL with a protocol (e.g., <code>https://</code>) for validation. Pass <code>placeholder</code> and <code>pattern</code> via restProps to customize behavior.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>text-input</code> — a single-line text input field &lt;input type=\"text\"&gt;</li>\n<li><code>email-input</code> — an input for entering an email address &lt;input type=\"email\"&gt;</li>\n<li><code>tel-input</code> — an input for entering a telephone number &lt;input type=\"tel\"&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN input type=\"url\": https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/url</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<label class=\"label\" for=\"demo-input\">Label</label><input class=\"url-input\" type=\"url\" id=\"demo-input\" aria-label=\"UrlInput\" />";
  const svelteSource: string = "// In your Svelte component:\nimport UrlInput from \"lily-design-system-svelte-headless/components/UrlInput/UrlInput.svelte\";\n\n<UrlInput label=\"Label\" />\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>URL Input — Lily Design System</title>
  <meta name="description" content="A URL input is a UI/UX component that provides a text field specifically for entering web addresses. It uses the native HTML &lt;input type=&quot;url&quot;&gt; element, which provides built-in URL validation and m..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

