<script lang="ts">
  const html: string = "<h1>Label</h1>\n\n<p>A label is a UI component that provides a descriptive name or instruction for an associated form element, such as an input field, checkbox, or radio button. The Label component renders a semantic <code>&lt;label&gt;</code> HTML element that can be linked to a form control via the <code>for</code> attribute, enabling click-to-focus behavior and improving accessibility for screen reader users.</p>\n\n<p>Properly associated labels are essential for form usability. When a user clicks the label text, the browser focuses or activates the linked form control. Screen readers announce the label when the associated control receives focus, ensuring users understand the purpose of each form field.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a native <code>&lt;label&gt;</code> HTML element</li>\n<li>Uses the <code>for</code> prop (mapped from the reserved keyword via <code>for: forProp</code>) to associate with a form control by ID</li>\n<li>Content is provided via the <code>children</code> slot</li>\n<li>Rest props are spread onto the <code>&lt;label&gt;</code> element for consumer customization</li>\n<li>The <code>for</code> prop is optional, allowing labels to wrap their associated control directly</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>for</code>: string (optional) -- the <code>id</code> of the associated form control, rendered as the HTML <code>for</code> attribute</li>\n<li><code>children</code>: slot (required) -- the label text content</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;label&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Label linked to a form control by ID:</p>\n\n<pre><code class=\"language-html\">&lt;Label for=\"patient-name\"&gt;Patient name&lt;/Label&gt;\n&lt;input id=\"patient-name\" type=\"text\" /&gt;</code></pre>\n\n<p>Label wrapping a checkbox (no <code>for</code> attribute needed):</p>\n\n<pre><code class=\"language-html\">&lt;Label&gt;\n  &lt;input type=\"checkbox\" /&gt; I agree to the terms and conditions\n&lt;/Label&gt;</code></pre>\n\n<p>Label for a select dropdown:</p>\n\n<pre><code class=\"language-html\">&lt;Label for=\"country-select\"&gt;Country&lt;/Label&gt;\n&lt;select id=\"country-select\"&gt;\n  &lt;option&gt;United Kingdom&lt;/option&gt;\n  &lt;option&gt;Ireland&lt;/option&gt;\n&lt;/select&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive form label. Clicking the label focuses or activates its associated form control via native browser behavior.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>The native <code>&lt;label&gt;</code> element with <code>for</code> attribute provides an implicit accessible name for the linked form control</li>\n<li>No additional ARIA attributes are needed because the semantic HTML <code>&lt;label&gt;</code> element is fully recognized by assistive technology</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to associate descriptive text with a form input via the <code>for</code> attribute.</li>\n<li>Use on every visible form field to provide a visible, accessible name.</li>\n<li>Use when you need click-to-focus behavior so clicking the label text activates the associated control.</li>\n<li>Use when building custom form layouts outside of the Field component.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use as a substitute for Hint -- labels name the field, hints explain how to fill it.</li>\n<li>Do not use for non-form text -- use a heading or paragraph instead.</li>\n<li>Do not use when the Field component already provides label rendering.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component renders a native <code>&lt;label&gt;</code> element with optional <code>for</code> attribute linking, providing built-in browser accessibility and click-to-focus behavior. The consumer provides all visual styling, including typography, spacing, required-field indicators, and layout relative to the associated control.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.label</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;label&gt;</code> element with class <code>label</code></li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Position labels consistently relative to their controls (typically above or to the left) and visually indicate required fields with a marker such as an asterisk.</li>\n<li><strong>Developers</strong>: Use the <code>for</code> prop to link the label to its control by ID, or wrap the control inside the label element. Avoid using <code>aria-label</code> as a substitute for a visible label.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>field</code> — a form field wrapper with label, input, and error message</li>\n<li><code>hint</code> — hint text providing guidance for a form field</li>\n<li><code>error-message</code> — an error message associated with a form field</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI Forms Tutorial - Labels: https://www.w3.org/WAI/tutorials/forms/labels/</li>\n<li>HTML label element: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/label</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<label class=\"label\" for=\"demo\">Form label</label>";
  const svelteSource: string = "// In your Svelte component:\nimport Label from \"lily-design-system-svelte-headless/components/Label/Label.svelte\";\n\n<Label>\n  <!-- content -->\n</Label>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Label — Lily Design System</title>
  <meta name="description" content="A label is a UI component that provides a descriptive name or instruction for an associated form element, such as an input field, checkbox, or radio button. The Label component renders a semantic &lt;..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

