<script lang="ts">
  const html: string = "<h1>Theme Picker</h1>\n\n<p>ThemePicker is a headless theme selection component that renders a <code>&lt;fieldset&gt;</code> with the <code>radiogroup</code> role. It provides an accessible container for radio button options that let users choose between themes such as light, dark, or system preference.</p>\n\n<p>This component is useful for settings pages, preference panels, or any interface where users need to select from a set of theme options. Consumers provide the radio button markup as children, allowing full control over the option labels and values.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;fieldset&gt;</code> element with <code>role=\"radiogroup\"</code> for semantic grouping of radio options</li>\n<li>Uses <code>aria-label</code> for an accessible name describing the radio group</li>\n<li>Accepts a <code>children</code> slot for rendering radio button options inside the fieldset</li>\n<li>Spreads <code>restProps</code> onto the fieldset for consumer customization</li>\n<li>pattern: standard props destructuring with typed props</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name for the radio group via <code>aria-label</code></li>\n<li><code>children</code>: slot (required) -- radio button option elements to render inside the fieldset</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;fieldset&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Basic light/dark theme selection using radio buttons:</p>\n\n<pre><code class=\"language-html\">&lt;ThemePicker label=\"Theme\"&gt;\n  &lt;label&gt;&lt;input type=\"radio\" name=\"theme\" value=\"light\" checked /&gt; Light&lt;/label&gt;\n  &lt;label&gt;&lt;input type=\"radio\" name=\"theme\" value=\"dark\" /&gt; Dark&lt;/label&gt;\n&lt;/ThemePicker&gt;</code></pre>\n\n<p>Settings page with multiple themes using ThemePickerButton:</p>\n\n<pre><code class=\"language-html\">&lt;ThemePicker label=\"Choose theme\"&gt;\n  &lt;ThemePickerButton pressed={theme === 'light'} label=\"Light theme\" onclick={() =&gt; setTheme('light')}&gt;\n    Light\n  &lt;/ThemePickerButton&gt;\n  &lt;ThemePickerButton pressed={theme === 'dark'} label=\"Dark theme\" onclick={() =&gt; setTheme('dark')}&gt;\n    Dark\n  &lt;/ThemePickerButton&gt;\n  &lt;ThemePickerButton pressed={theme === 'high-contrast'} label=\"High contrast theme\" onclick={() =&gt; setTheme('high-contrast')}&gt;\n    High Contrast\n  &lt;/ThemePickerButton&gt;\n&lt;/ThemePicker&gt;</code></pre>\n\n<p>With system preference option:</p>\n\n<pre><code class=\"language-html\">&lt;ThemePicker label=\"Appearance\"&gt;\n  &lt;label&gt;&lt;input type=\"radio\" name=\"appearance\" value=\"system\" checked /&gt; System&lt;/label&gt;\n  &lt;label&gt;&lt;input type=\"radio\" name=\"appearance\" value=\"light\" /&gt; Light&lt;/label&gt;\n  &lt;label&gt;&lt;input type=\"radio\" name=\"appearance\" value=\"dark\" /&gt; Dark&lt;/label&gt;\n&lt;/ThemePicker&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None at the container level. Keyboard behavior is handled natively by the <code>&lt;input type=\"radio\"&gt;</code> elements provided by the consumer (Arrow keys to navigate between options, Space to select).</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"radiogroup\"</code> -- identifies the fieldset as a group of related radio buttons</li>\n<li><code>aria-label={label}</code> -- provides an accessible name describing the purpose of the theme selection</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for selecting a visual theme such as light, dark, or high-contrast mode.</li>\n<li>Use in settings pages or preference panels where all theme options should be visible at once.</li>\n<li>Use when a radiogroup-based selection provides better discoverability than a dropdown.</li>\n<li>Use when you want to show visual previews or swatches alongside each theme option.</li>\n<li>Use when there are two to five theme options that fit comfortably in the layout.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for display-only theme information -- use <a href=\"../theme-view/\">ThemeView</a> instead.</li>\n<li>Do not use when a compact dropdown is preferred -- use <a href=\"../theme-select/\">ThemeSelect</a> instead.</li>\n<li>Do not use for a simple binary light/dark toggle -- use <a href=\"../switch-button/\">SwitchButton</a> or <a href=\"../toggle-button/\">ToggleButton</a> instead.</li>\n<li>Do not use for non-theme selections -- use <a href=\"../radio-group/\">RadioGroup</a> or <a href=\"../segment-group/\">SegmentGroup</a> instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a semantic <code>&lt;fieldset&gt;</code> with <code>role=\"radiogroup\"</code> and an <code>aria-label</code> for accessible theme selection. The consumer provides all radio button markup, visual styling, layout, and theme application logic.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.theme-picker</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>theme-picker</code></li>\n<li>Verify role=\"radiogroup\"` -- identifies the fieldset as a group of related radio buttons</li>\n<li>Verify aria-label={label}` -- provides an accessible name describing the purpose of the theme selection</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Place theme options in a clear, evenly spaced row or column with recognizable labels or icons for each theme.</li>\n<li><strong>Developers</strong>: Persist the selected theme to localStorage or a user preferences API so it survives page reloads.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>ThemePicker is a standalone picker component that contains consumer-provided radio buttons. It is related to ThemeSelect (dropdown approach) and ThemeView (read-only display). Use ThemePicker when you want visible radio options; use ThemeSelect for a compact dropdown.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>theme-picker-button</code> — a picker button for selecting a visual theme</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA radiogroup role: https://www.w3.org/TR/wai-aria-1.2/#radiogroup</li>\n<li>WAI-ARIA Radio Group Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/radio/</li>\n</ul>";
</script>

<svelte:head>
  <title>Theme Picker — Lily Design System</title>
  <meta name="description" content="ThemePicker is a headless theme selection component that renders a &lt;fieldset&gt; with the radiogroup role. It provides an accessible container for radio button options that let users choose between th..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
