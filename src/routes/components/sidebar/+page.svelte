<script lang="ts">
  const html: string = "<h1>Sidebar</h1>\n\n<p>Sidebar is a headless complementary region that uses the semantic <code>&lt;aside&gt;</code> element. It provides a labeled landmark for content that is tangentially related to the main content, such as navigation menus, filters, related links, or supplementary information.</p>\n\n<p>Use Sidebar when you need a distinct region alongside main content that assistive technologies can identify and navigate to as a landmark. The <code>&lt;aside&gt;</code> element inherently carries the ARIA <code>complementary</code> role, so screen readers will announce it as a complementary landmark. The component applies no visual styling, leaving layout, width, positioning, and appearance entirely to the consumer.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders an <code>&lt;aside&gt;</code> element, which has an implicit <code>complementary</code> landmark role</li>\n<li>The <code>aria-label</code> attribute provides an accessible name to distinguish this sidebar from other landmarks</li>\n<li>Spreads <code>restProps</code> onto the aside for consumer customization</li>\n<li>No internal state management; this is a purely structural component</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible label for the sidebar region, applied via <code>aria-label</code></li>\n<li><code>children</code>: slot (required) -- content to render inside the sidebar</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;aside&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Sidebar with navigation links:</p>\n\n<pre><code class=\"language-html\">&lt;Sidebar label=\"Navigation\"&gt;\n  &lt;nav&gt;\n    &lt;a href=\"/dashboard\"&gt;Dashboard&lt;/a&gt;\n    &lt;a href=\"/projects\"&gt;Projects&lt;/a&gt;\n    &lt;a href=\"/team\"&gt;Team&lt;/a&gt;\n    &lt;a href=\"/settings\"&gt;Settings&lt;/a&gt;\n  &lt;/nav&gt;\n&lt;/Sidebar&gt;</code></pre>\n\n<p>Sidebar with filters for a search results page:</p>\n\n<pre><code class=\"language-html\">&lt;Sidebar label=\"Search filters\"&gt;\n  &lt;form&gt;\n    &lt;fieldset&gt;\n      &lt;legend&gt;Category&lt;/legend&gt;\n      &lt;label&gt;&lt;input type=\"checkbox\" value=\"docs\" /&gt; Documentation&lt;/label&gt;\n      &lt;label&gt;&lt;input type=\"checkbox\" value=\"api\" /&gt; API Reference&lt;/label&gt;\n      &lt;label&gt;&lt;input type=\"checkbox\" value=\"guides\" /&gt; Guides&lt;/label&gt;\n    &lt;/fieldset&gt;\n    &lt;Separator /&gt;\n    &lt;fieldset&gt;\n      &lt;legend&gt;Date range&lt;/legend&gt;\n      &lt;label for=\"from\"&gt;From&lt;/label&gt;\n      &lt;DateInput label=\"From date\" id=\"from\" value={fromDate} /&gt;\n      &lt;label for=\"to\"&gt;To&lt;/label&gt;\n      &lt;DateInput label=\"To date\" id=\"to\" value={toDate} /&gt;\n    &lt;/fieldset&gt;\n  &lt;/form&gt;\n&lt;/Sidebar&gt;</code></pre>\n\n<p>Sidebar alongside main content:</p>\n\n<pre><code class=\"language-html\">&lt;div class=\"page-layout\"&gt;\n  &lt;Sidebar label=\"Documentation navigation\"&gt;\n    &lt;TreeNav label=\"Docs sections\"&gt;\n      &lt;TreeList&gt;\n        &lt;TreeListItem&gt;&lt;TreeLink href=\"/docs/getting-started\"&gt;Getting Started&lt;/TreeLink&gt;&lt;/TreeListItem&gt;\n        &lt;TreeListItem&gt;&lt;TreeLink href=\"/docs/components\"&gt;Components&lt;/TreeLink&gt;&lt;/TreeListItem&gt;\n      &lt;/TreeList&gt;\n    &lt;/TreeNav&gt;\n  &lt;/Sidebar&gt;\n  &lt;main&gt;\n    &lt;h1&gt;Getting Started&lt;/h1&gt;\n    &lt;p&gt;...&lt;/p&gt;\n  &lt;/main&gt;\n&lt;/div&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive container. Keyboard navigation depends on the interactive content placed inside it by the consumer.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Implicit <code>complementary</code> role via the <code>&lt;aside&gt;</code> element -- identifies the container as a complementary landmark</li>\n<li><code>aria-label={label}</code> -- provides an accessible name so screen readers can distinguish this sidebar from other landmarks on the page</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for a side panel containing navigation menus, filters, or supplementary content within a page layout.</li>\n<li>Use when content is tangentially related to the main content and benefits from being in a distinct, navigable landmark region.</li>\n<li>Use when assistive technology users should be able to jump directly to this supplementary region.</li>\n<li>Use for persistent side navigation that remains visible alongside the main content.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for slide-in overlays -- use Drawer or Sheet instead.</li>\n<li>Do not use within GrailLayout -- use GrailLayoutLeftAside or GrailLayoutRightAside instead.</li>\n<li>Do not use for primary page content -- use a <code>&lt;main&gt;</code> element instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component renders a semantic <code>&lt;aside&gt;</code> element with an <code>aria-label</code> for landmark identification. It provides the complementary landmark role automatically. The consumer provides all visual styling including width, positioning, layout, and responsive behavior.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.sidebar</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;aside&gt;</code> element with class <code>sidebar</code></li>\n<li>Verify aria-label={label}` -- provides an accessible name so screen readers can distinguish this sidebar from other landmarks on the page</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Keep sidebar width proportional to the main content area, typically 20-30% of the viewport. Provide clear visual separation from the main content using borders, background color, or spacing.</li>\n<li><strong>Developers</strong>: Use a descriptive <code>label</code> prop to distinguish this sidebar from other landmarks on the page. Avoid placing more than two sidebar landmarks on a single page to prevent landmark clutter.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>drawer</code> — a panel that slides in from the edge of the screen</li>\n<li><code>navigation-menu</code> — a site-wide navigation menu with links</li>\n<li><code>section-nav</code> — a navigation container for section navigation links</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Complementary Role: https://www.w3.org/TR/wai-aria-1.2/#complementary</li>\n<li>WAI-ARIA Landmark Regions: https://www.w3.org/WAI/ARIA/apg/practices/landmark-regions/</li>\n<li>HTML <code>&lt;aside&gt;</code> element: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/aside</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<aside class=\"sidebar\" aria-label=\"Sidebar\"><nav><ul><li>Nav item 1</li><li>Nav item 2</li></ul></nav></aside>";
  const svelteSource: string = "// In your Svelte component:\nimport Sidebar from \"lily-design-system-svelte-headless/components/Sidebar/Sidebar.svelte\";\n\n<Sidebar>\n  <!-- content -->\n</Sidebar>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Sidebar — Lily Design System</title>
  <meta name="description" content="Sidebar is a headless complementary region that uses the semantic &lt;aside&gt; element. It provides a labeled landmark for content that is tangentially related to the main content, such as navigation me..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

