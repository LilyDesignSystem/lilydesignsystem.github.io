<script lang="ts">
  const html: string = "<h1>Range Input</h1>\n\n<p>A range input is a UI/UX component that provides a slider control for selecting a numeric value within a defined range. It uses the native HTML <code>&lt;input type=\"range\"&gt;</code> element, which renders as a draggable track and handle. The component supports configurable minimum, maximum, and step values, and provides a bindable value prop for two-way data binding. An aria-label ensures the control is accessible to screen readers and assistive technologies.</p>\n\n<p>Range inputs are commonly used for volume controls, brightness settings, price filters, and any scenario where users need to select a value from a continuous numeric spectrum.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a single <code>&lt;input type=\"range\"&gt;</code> element for native slider behavior</li>\n<li>Supports two-way binding for the <code>value</code> prop enabling two-way data binding with two-way <code>value</code> binding</li>\n<li>Supports rest props via <code>...restProps</code> for extensibility (e.g., <code>id</code>, <code>name</code>, <code>class</code>)</li>\n<li>The <code>aria-label</code> prop provides an accessible name since there is no visible <code>&lt;label&gt;</code> element</li>\n<li>Default value is 50, centered in the default 0-100 range</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name applied via <code>aria-label</code></li>\n<li><code>value</code>: number (default: 50) -- current numeric value, bindable with two-way <code>value</code> binding</li>\n<li><code>min</code>: number (default: 0) -- minimum allowed value</li>\n<li><code>max</code>: number (default: 100) -- maximum allowed value</li>\n<li><code>step</code>: number (default: 1) -- increment between selectable values</li>\n<li><code>disabled</code>: boolean (default: false) -- whether the input is disabled</li>\n<li><code>...restProps</code>: spread onto the <code>&lt;input&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Volume control:</p>\n\n<pre><code class=\"language-html\">&lt;RangeInput label=\"Volume\" value={volume} min={0} max={100} /&gt;</code></pre>\n\n<p>Price filter with step increments:</p>\n\n<pre><code class=\"language-html\">&lt;RangeInput label=\"Maximum price\" value={maxPrice} min={10} max={500} step={10} /&gt;</code></pre>\n\n<p>Satisfaction rating from 1 to 10:</p>\n\n<pre><code class=\"language-html\">&lt;RangeInput label=\"Satisfaction\" value={satisfaction} min={1} max={10} step={1} /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Left Arrow / Down Arrow: decrease value by one step</li>\n<li>Right Arrow / Up Arrow: increase value by one step</li>\n<li>Home: set to minimum value</li>\n<li>End: set to maximum value</li>\n<li>Page Down: decrease by a larger step (browser-defined)</li>\n<li>Page Up: increase by a larger step (browser-defined)</li>\n</ul>\n\n<p>Note: keyboard interactions are provided natively by the <code>&lt;input type=\"range\"&gt;</code> element.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides the accessible name for the slider control since no visible label element is associated</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for selecting a value within a defined range using a native HTML range slider.</li>\n<li>Use for approximate value selection like satisfaction ratings, volume, brightness, or price filters.</li>\n<li>Use when the full range should be visually represented and an approximate value is acceptable.</li>\n<li>Use for zoom levels, opacity controls, or any bounded numeric spectrum.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when exact numbers matter -- use NumberInput instead.</li>\n<li>Do not use for angle selection in degrees -- use AngleSliderRangeInput instead.</li>\n<li>Do not use for a custom slider with non-native styling needs -- use Slider instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>The RangeInput headless component provides a native <code>&lt;input type=\"range\"&gt;</code> with <code>aria-label</code>, configurable <code>min</code>, <code>max</code>, and <code>step</code> values, and two-way value binding. The browser handles all slider keyboard interactions natively. The consumer provides all visual styling for the track, thumb, and labels.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.range-input</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;input&gt;</code> element with class <code>range-input</code></li>\n<li>Verify aria-label={label}` -- provides the accessible name for the slider control since no visible label element is associated</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Display the current value near the slider thumb or in a label, and consider adding tick marks at key intervals for better spatial reference.</li>\n<li><strong>Developers</strong>: Use the <code>step</code> prop to constrain values to meaningful increments, and debounce <code>oninput</code> handlers if the slider drives expensive updates.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>slider</code> — a draggable control for selecting a value along a track</li>\n<li><code>number-input</code> — an input for entering a numeric value with validation &lt;input type=\"number\"&gt;</li>\n<li><code>angle-slider-range-input</code> — a range input for selecting an angle in degrees</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Slider Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/slider/</li>\n<li>MDN input type=\"range\": https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/range</li>\n</ul>";
</script>

<svelte:head>
  <title>Range Input — Lily Design System</title>
  <meta name="description" content="A range input is a UI/UX component that provides a slider control for selecting a numeric value within a defined range. It uses the native HTML &lt;input type=&quot;range&quot;&gt; element, which renders as a drag..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
