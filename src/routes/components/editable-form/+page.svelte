<script lang="ts">
  const html: string = "<h1>Editable Form</h1>\n\n<p>An editable form wraps inline editing functionality in a <code>&lt;form&gt;</code> element, providing submit and cancel semantics with keyboard support. It is useful for editing records in place, such as updating a user profile, renaming an item, or modifying settings without navigating to a separate page.</p>\n\n<p>The form is only rendered when the <code>editing</code> prop is true. Submitting the form automatically exits edit mode and calls the <code>onsubmit</code> callback. Pressing Escape cancels editing and calls the <code>oncancel</code> callback. This pattern gives consumers full control over the edit lifecycle.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Conditionally renders a <code>&lt;form&gt;</code> only when <code>editing</code> is true</li>\n<li>Supports two-way binding on the <code>editing</code> prop for two-way state binding</li>\n<li>Calls <code>event.preventDefault()</code> on submit to allow consumer-controlled save logic</li>\n<li>Sets <code>editing</code> to <code>false</code> on both submit and Escape key press</li>\n<li>Sets <code>tabindex=\"-1\"</code> on the form to allow programmatic focus management</li>\n<li>Escape key handler calls the <code>oncancel</code> callback</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name for the form, applied as <code>aria-label</code></li>\n<li><code>editing</code>: boolean (default: false) -- whether the form is in edit mode, two-way bindable via <code>editing</code></li>\n<li><code>onsubmit</code>: (event: SubmitEvent) =&gt; void (default: undefined) -- callback when the form is submitted</li>\n<li><code>oncancel</code>: () =&gt; void (default: undefined) -- callback when editing is cancelled</li>\n<li><code>children</code>: slot (required) -- form content (inputs, buttons, etc.)</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Inline profile editor with save and cancel:</p>\n\n<pre><code class=\"language-html\">&lt;button onclick={() =&gt; (editing = true)}&gt;Edit profile&lt;/button&gt;\n&lt;EditableForm label=\"Edit profile\" editing={editing} onsubmit={saveProfile} oncancel={revertProfile}&gt;\n  &lt;Field label=\"Full name\"&gt;\n    &lt;TextInput label=\"Full name\" value={fullName} /&gt;\n  &lt;/Field&gt;\n  &lt;button type=\"submit\"&gt;Save&lt;/button&gt;\n  &lt;button type=\"button\" onclick={() =&gt; (editing = false)}&gt;Cancel&lt;/button&gt;\n&lt;/EditableForm&gt;</code></pre>\n\n<p>Renaming an item inline:</p>\n\n<pre><code class=\"language-html\">&lt;EditableForm label=\"Rename item\" editing={renaming} onsubmit={rename} oncancel={revert}&gt;\n  &lt;input name=\"title\" value={title} /&gt;\n  &lt;button type=\"submit\"&gt;OK&lt;/button&gt;\n&lt;/EditableForm&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Enter: submits the form (native form behavior), exits edit mode, calls <code>onsubmit</code></li>\n<li>Escape: cancels editing, exits edit mode, calls <code>oncancel</code></li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides an accessible name for the form</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to wrap inline editing with form semantics for submission and validation.</li>\n<li>Use when inline editing requires submit and cancel lifecycle callbacks with proper <code>&lt;form&gt;</code> behavior.</li>\n<li>Use for updating a record, renaming an item, or modifying settings in place.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for standard page forms -- use Form instead.</li>\n<li>Do not use for single-field inline edits without submission semantics -- use Editable alone.</li>\n<li>Do not use when the editing involves many fields or complex validation -- use a full-page Form instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a conditionally rendered <code>&lt;form&gt;</code> with accessible labeling via <code>aria-label</code>, submit handling with <code>preventDefault</code>, Escape key cancellation, and two-way binding on the <code>editing</code> prop. The consumer provides all visual styling, form controls, and action buttons.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.editable-form</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;form&gt;</code> element with class <code>editable-form</code></li>\n<li>Verify aria-label={label}` -- provides an accessible name for the form</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Provide clear save and cancel affordances within the form. Visually differentiate the editing state from the read-only state so users know when they are in edit mode.</li>\n<li><strong>Developers</strong>: Use the <code>onsubmit</code> callback for save logic and <code>oncancel</code> for revert logic. The form automatically exits edit mode on both submit and Escape.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>EditableForm composes with Editable and form input components. EditableForm provides the <code>&lt;form&gt;</code> wrapper with submit/cancel lifecycle, while Editable can be used inside for individual inline-editable fields. Use Field, TextInput, and other input components as form content.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>editable</code> — an inline-editable text element that toggles between view and edit modes</li>\n<li><code>form</code> — a form element for collecting and submitting user data</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Form Role: https://www.w3.org/WAI/ARIA/apd/roles/form/</li>\n</ul>";
</script>

<svelte:head>
  <title>Editable Form — Lily Design System</title>
  <meta name="description" content="An editable form wraps inline editing functionality in a &lt;form&gt; element, providing submit and cancel semantics with keyboard support. It is useful for editing records in place, such as updating a u..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
