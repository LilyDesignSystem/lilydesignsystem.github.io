<script lang="ts">
  const html: string = "<h1>Summary List</h1>\n\n<p>A summary list is a component that presents a concise overview of key information as key-value pairs, typically in a structured, easy-to-scan format. It is often used to summarize important details such as transaction history, order details, product features, account settings, or form review data in a description list layout.</p>\n\n<p>This component renders as a semantic <code>&lt;dl&gt;</code> (description list) element with an accessible label. Each item in the summary list consists of a <code>&lt;dt&gt;</code> (term) and <code>&lt;dd&gt;</code> (description) pair, helping users quickly grasp essential information. A well-designed summary list focuses on clarity, using clear labels, concise text, and consistent formatting to enhance readability.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as <code>&lt;dl aria-label={label}&gt;</code> (description list) with a <code>children</code> slot</li>\n<li>Children typically contain <code>&lt;dt&gt;</code> (term) and <code>&lt;dd&gt;</code> (description) element pairs</li>\n<li>Semantic <code>&lt;dl&gt;</code> element provides description list semantics for key-value pairs</li>\n<li>Uses props rune for prop destructuring</li>\n<li>Spreads <code>...restProps</code> on the <code>&lt;dl&gt;</code> element for consumer extensibility</li>\n<li>No hardcoded user-facing strings; all text comes through props and children</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible label for the description list via <code>aria-label</code></li>\n<li><code>children</code>: slot (required) -- list content, typically <code>&lt;dt&gt;</code>/<code>&lt;dd&gt;</code> pairs</li>\n<li><code>...restProps</code>: any additional HTML attributes spread onto the <code>&lt;dl&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;SummaryList label=\"Personal details\"&gt;\n  &lt;SummaryListItem term=\"Name\"&gt;Karen Francis&lt;/SummaryListItem&gt;\n  &lt;SummaryListItem term=\"Date of birth\"&gt;15 March 1984&lt;/SummaryListItem&gt;\n  &lt;SummaryListItem term=\"Contact information\"&gt;\n    73 Roman Road&lt;br /&gt;Leeds&lt;br /&gt;LS2 5ZN\n  &lt;/SummaryListItem&gt;\n  &lt;SummaryListItem term=\"Email\"&gt;karen.francis@example.com&lt;/SummaryListItem&gt;\n&lt;/SummaryList&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this is a passive informational display, not an interactive element.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides the accessible name for the description list, allowing screen readers to announce the context of the key-value pairs</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to display pairs of related information as key-value entries, such as a patient's name, date of birth, and contact details</li>\n<li>Use to summarise a user's answers at the end of a form in a \"check your answers\" pattern</li>\n<li>Use with action links (like \"Change\") that include visually hidden text for screen reader context</li>\n<li>Use when information is best presented as labeled terms with corresponding descriptions in a <code>&lt;dl&gt;</code> element</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for tabular data with multiple columns -- use Table or DataTable instead</li>\n<li>Do not use for simple lists without key-value pairs -- use a standard <code>&lt;ul&gt;</code> or <code>&lt;ol&gt;</code></li>\n<li>Do not use for task tracking -- use TaskList instead</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component renders a semantic <code>&lt;dl&gt;</code> element with <code>aria-label</code> for accessible naming. It provides description list semantics for key-value pairs. The consumer provides all visual styling including layout, spacing, typography, and row separation.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.summary-list</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;ol&gt;</code> element with class <code>summary-list</code></li>\n<li>Verify aria-label={label}` -- provides the accessible name for the description list, allowing screen readers to announce the context of the key-value pairs</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Align terms and descriptions consistently, using either a side-by-side or stacked layout. Use subtle row separators or alternating backgrounds to improve scannability.</li>\n<li><strong>Developers</strong>: Use SummaryListItem child components for structured <code>&lt;dt&gt;</code>/<code>&lt;dd&gt;</code> pairs, or provide raw <code>&lt;dt&gt;</code>/<code>&lt;dd&gt;</code> elements directly. Ensure the <code>label</code> prop accurately describes the summary context.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>SummaryList uses the List/ListItem composition pattern. Place SummaryListItem components as children inside SummaryList. Each SummaryListItem renders a <code>&lt;dt&gt;</code>/<code>&lt;dd&gt;</code> pair within a <code>&lt;div&gt;</code> wrapper, while SummaryList provides the outer <code>&lt;dl&gt;</code> container with accessible labeling.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>summary-list-item</code> — one key-value pair in a summary list</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN dl element: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/dl</li>\n<li>WAI-ARIA landmark regions: https://www.w3.org/WAI/ARIA/apd/practices/landmark-regions/</li>\n</ul>";
</script>

<svelte:head>
  <title>Summary List — Lily Design System</title>
  <meta name="description" content="A summary list is a component that presents a concise overview of key information as key-value pairs, typically in a structured, easy-to-scan format. It is often used to summarize important details..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
