<script lang="ts">
  const html: string = "<h1>Medical Banner</h1>\n\n<p>MedicalBanner is a headless component for displaying prominent medical information messages at the top of a page. It renders as a <code>&lt;div role=\"region\" aria-live=\"polite\"&gt;</code> with <code>data-context=\"medical\"</code> and supports optional dismiss functionality. It follows the same pattern as Banner but is specifically intended for clinical and medical contexts.</p>\n\n<p>This component is useful for patient alerts, clinical notifications, medical record summaries, and health-related announcements in electronic health record systems and clinical interfaces.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as <code>&lt;div role=\"region\" aria-live=\"polite\"&gt;</code> for screen reader announcements</li>\n<li>Uses reactive state to track visibility; dismissing sets <code>visible</code> to <code>false</code></li>\n<li>Dismissible banners render a <code>&lt;button type=\"button\"&gt;</code> with a configurable <code>aria-label</code></li>\n<li><code>data-type</code> attribute exposes the variant (info, success, warning, error) for consumer CSS</li>\n<li><code>data-context=\"medical\"</code> attribute distinguishes this as a medical banner</li>\n<li>Uses <code>children</code> slot for flexible banner content rendering</li>\n<li>Spreads <code>...restProps</code> on the root <code>&lt;div&gt;</code> element for consumer extensibility</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name for the banner via <code>aria-label</code></li>\n<li><code>type</code>: <code>\"info\"</code> | <code>\"success\"</code> | <code>\"warning\"</code> | <code>\"error\"</code> (default: <code>\"info\"</code>) -- banner variant</li>\n<li><code>dismissible</code>: boolean (default: <code>false</code>) -- whether the banner can be dismissed</li>\n<li><code>onclose</code>: <code>() =&gt; void</code> (optional) -- callback invoked when the banner is dismissed</li>\n<li><code>closeLabel</code>: string (optional) -- accessible label for the dismiss button</li>\n<li><code>children</code>: slot (required) -- the banner content</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the root <code>&lt;div&gt;</code></li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;MedicalBanner label=\"Patient summary\"&gt;\n  &lt;MedicalBannerBox&gt;\n    &lt;strong&gt;Sarah Mitchell&lt;/strong&gt; | NHS: 485 777 3456 | DOB: 15/03/1984\n  &lt;/MedicalBannerBox&gt;\n  &lt;MedicalBannerBoxForDanger label=\"Allergies and reactions\"&gt;\n    Allergies: Penicillin, Latex\n  &lt;/MedicalBannerBoxForDanger&gt;\n  &lt;MedicalBannerBoxForAdvice label=\"Care team\"&gt;\n    GP: Dr James Lee | Next appointment: 22 April 2025\n  &lt;/MedicalBannerBoxForAdvice&gt;\n&lt;/MedicalBanner&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;MedicalBanner label=\"Clinical notice\" type=\"warning\" dismissible closeLabel=\"Dismiss\"&gt;\n  Patient has pending lab results requiring urgent review.\n&lt;/MedicalBanner&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus the dismiss button (when <code>dismissible</code> is <code>true</code>)</li>\n<li>Enter/Space: Activate the dismiss button to hide the banner</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"region\"</code> -- establishes the banner as a landmark region</li>\n<li><code>aria-live=\"polite\"</code> -- ensures screen readers announce banner content</li>\n<li><code>aria-label={label}</code> -- provides accessible name for the banner</li>\n<li><code>aria-label={closeLabel}</code> -- provides accessible name for the dismiss button</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>MedicalBanner can contain MedicalBannerBox for horizontal layout:</p>\n\n<pre><code class=\"language-html\">&lt;MedicalBanner label=\"Patient summary\"&gt;\n  &lt;MedicalBannerBox&gt;\n    &lt;span&gt;Patient: John Smith&lt;/span&gt;\n    &lt;span&gt;NHS: 123 456 7890&lt;/span&gt;\n  &lt;/MedicalBannerBox&gt;\n&lt;/MedicalBanner&gt;</code></pre>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use at the top of a clinical page to display critical patient identification and medical summary information</li>\n<li>Use in electronic health record systems where clinicians need quick access to patient context</li>\n<li>Use to present patient demographics, identifiers, and key clinical data in a persistent banner</li>\n<li>Use when medical context needs to be conveyed to assistive technologies via <code>data-context=\"medical\"</code></li>\n<li>Use as the parent container for MedicalBannerBox, MedicalBannerBoxForDanger, and MedicalBannerBoxForAdvice</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for non-medical applications -- use Banner instead</li>\n<li>Do not use for alerts or warnings about clinical conditions -- use Alert, WarningCallout, or CareCard</li>\n<li>Do not use for transient clinical notifications -- use Notification or Toast</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides <code>role=\"region\"</code> with <code>aria-live=\"polite\"</code>, <code>data-type</code> for variants, <code>data-context=\"medical\"</code>, optional dismiss button, and visibility state management. The consumer is responsible for all CSS.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.medical-banner</code> class for targeting. No default styles are included -- this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>medical-banner</code></li>\n<li>Verify <code>role=\"region\"</code> and <code>aria-live=\"polite\"</code> are present</li>\n<li>Verify <code>data-context=\"medical\"</code> is present</li>\n<li>Verify <code>aria-label</code> is set from the <code>label</code> prop</li>\n<li>Verify dismiss functionality works when <code>dismissible</code> is true</li>\n<li>Verify keyboard interactions work correctly</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use distinct styling for medical banners to differentiate them from generic site banners. Consider NHS blue or clinical color schemes.</li>\n<li><strong>Developers</strong>: Always provide a meaningful <code>label</code> prop. Use <code>data-context=\"medical\"</code> in CSS selectors for medical-specific styling.</li>\n</ul>\n\n<h2>Domain Knowledge</h2>\n\n<p>Medical banners in clinical systems display patient-specific information, clinical alerts, and health record summaries. They must be highly accessible as they convey critical information to clinicians.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>announcement-banner</code> — a banner highlighting important messages for all users</li>\n<li><code>banner</code> — a prominent message bar across the top of a page</li>\n<li><code>banner-box</code> — a banner box that is inside a banner component, using flexbox horizontal</li>\n<li><code>government-banner</code> — a banner identifying a website as belonging to a government, with an expandable details panel</li>\n<li><code>medical-banner-box</code> — a medical banner box that is inside a medical-banner component, using flexbox horizontal, with medical information</li>\n<li><code>medical-banner-box-for-advice</code> — a medical record banner box for advice information e.g. contacts, contexts, plans, etc.</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>Parent pattern: Banner</li>\n<li>NHS UK Design System: https://service-manual.nhs.uk/design-system</li>\n</ul>";
</script>

<svelte:head>
  <title>Medical Banner — Lily Design System</title>
  <meta name="description" content="MedicalBanner is a headless component for displaying prominent medical information messages at the top of a page. It renders as a &lt;div role=&quot;region&quot; aria-live=&quot;polite&quot;&gt; with data-context=&quot;medical&quot; ..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
