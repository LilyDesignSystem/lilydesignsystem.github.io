<script lang="ts">
  const html: string = "<h1>Avatar</h1>\n\n<p>An avatar circle is a component that displays a user's profile image or fallback initials within a circular frame, commonly used to represent a user identity in apps, websites, and social platforms. This visual element provides a quick, recognizable way for users to identify themselves or others, often appearing in headers, comments, chat interfaces, and account menus.</p>\n\n<p>The component handles image loading gracefully: if an image source is provided, it renders an AvatarImage; if the image fails to load or no source is provided, it falls back to displaying the user's initials via AvatarText.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> element with class <code>avatar</code></li>\n<li>Spreads <code>restProps</code> onto the root element for consumer customization</li>\n</ul>\n\n<h2>Help</h2>\n\n<p>Use Avatar as the outer container for user avatars. It renders either an AvatarImage (when a photo is available) or an AvatarText (initials fallback). Common scenarios include user profiles, comment threads, chat interfaces, account menus, and team member lists.</p>\n\n<h2>Syntax</h2>\n\n<pre><code class=\"language-html\">&lt;Avatar alt=\"...\" src=\"...\" initials=\"...\" /&gt;</code></pre>\n\n<h2>Usage</h2>\n\n<p>Avatar with image and initials fallback:</p>\n\n<pre><code class=\"language-html\">&lt;Avatar src=\"/photos/sarah-mitchell.jpg\" alt=\"Dr Sarah Mitchell\" initials=\"SM\" /&gt;</code></pre>\n\n<p>Avatar with initials only (no photo available):</p>\n\n<pre><code class=\"language-html\">&lt;Avatar alt=\"Nurse James Lee\" initials=\"JL\" /&gt;</code></pre>\n\n<p>Avatar group showing a care team:</p>\n\n<pre><code class=\"language-html\">&lt;AvatarGroup label=\"Care team\"&gt;\n  &lt;Avatar src=\"/photos/sarah-mitchell.jpg\" alt=\"Dr Sarah Mitchell\" initials=\"SM\" /&gt;\n  &lt;Avatar alt=\"Nurse James Lee\" initials=\"JL\" /&gt;\n  &lt;Avatar alt=\"Dr Priya Sharma\" initials=\"PS\" /&gt;\n&lt;/AvatarGroup&gt;</code></pre>\n\n<h2>Props</h2>\n\n<table>\n<thead>\n<tr><th>Prop</th><th>Type</th><th>Default</th><th>Description</th></tr>\n</thead>\n<tbody>\n<tr><td><code>src</code></td><td><code>string</code></td><td>(optional)</td><td>URL of the avatar image</td></tr>\n<tr><td><code>alt</code></td><td><code>string</code></td><td>(required)</td><td>Accessible description of the person, used as <code>aria-label</code> on the container and <code>alt</code> on the image</td></tr>\n<tr><td><code>initials</code></td><td><code>string</code></td><td>(optional)</td><td>Fallback text displayed when no image is available or the image fails to load</td></tr>\n<tr><td><code>...restProps</code></td><td>HTML attributes</td><td></td><td>Additional attributes spread onto the outer <code>&lt;span&gt;</code> element</td></tr>\n</tbody>\n</table>\n\n<h2>Examples</h2>\n\n<p>Team member list:</p>\n\n<pre><code class=\"language-html\">&lt;Avatar src=\"/team/alice.jpg\" alt=\"Alice Smith\" initials=\"AS\" /&gt;\n&lt;Avatar src=\"/team/bob.jpg\" alt=\"Bob Jones\" initials=\"BJ\" /&gt;\n&lt;Avatar alt=\"Charlie Brown\" initials=\"CB\" /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>None -- this is a decorative/informational component, not interactive</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"img\"</code> -- on the outer container, conveying that the entire avatar is an image representation</li>\n<li><code>aria-label</code> -- set from the <code>alt</code> prop, providing the accessible name for the avatar</li>\n<li><code>aria-hidden=\"true\"</code> -- on the inner initials <code>&lt;span&gt;</code>, preventing duplicate announcements since the outer element already has <code>aria-label</code></li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to display a user's visual identity, such as a profile photo or initials fallback, in headers, comment threads, chat messages, or contact lists.</li>\n<li>Use when you need graceful fallback from an image to initials when the photo is unavailable or fails to load.</li>\n<li>Use with AvatarImage for a photo and AvatarText as a fallback when no image is available.</li>\n<li>Use in contexts where quick visual identification of people helps users navigate, such as team rosters or conversation threads.</li>\n<li>Use with the <code>alt</code> prop to provide an accessible name for the avatar via <code>aria-label</code>.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for decorative icons or illustrations -- use Icon instead.</li>\n<li>Do not use for organisation logos or non-person images -- use Image with appropriate alt text.</li>\n<li>Do not use when only one avatar is shown alongside extended profile information -- use a Person component instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides <code>role=\"img\"</code>, <code>aria-label</code>, and image-to-initials fallback logic with zero visual styling. The consumer is responsible for all CSS including circular framing, sizing, background color for initials, font styling, border, and shadow effects.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.avatar</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>avatar</code></li>\n<li>Verify role=\"img\"` -- on the outer container, conveying that the entire avatar is an image representation</li>\n<li>Verify aria-label<code> -- set from the </code>alt` prop, providing the accessible name for the avatar</li>\n<li>Verify aria-hidden=\"true\"<code> -- on the inner initials </code>&lt;span&gt;<code>, preventing duplicate announcements since the outer element already has </code>aria-label</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Maintain consistent avatar sizes throughout the application. Use a contrasting background color behind initials text to ensure legibility.</li>\n<li><strong>Developers</strong>: Always provide the <code>alt</code> prop for accessibility. When using both <code>src</code> and <code>initials</code>, the component automatically falls back to initials if the image fails to load.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>Avatar follows a parent/child composition pattern:</p>\n\n<ul>\n<li><strong>Avatar</strong> -- outer <code>&lt;span&gt;</code> container with <code>role=\"img\"</code> and <code>aria-label</code> for the accessible name.</li>\n<li><strong>AvatarImage</strong> -- <code>&lt;img&gt;</code> element displayed when a valid image source is available.</li>\n<li><strong>AvatarText</strong> -- <code>&lt;span&gt;</code> element displayed as a fallback with initials or short text.</li>\n</ul>\n\n<pre><code class=\"language-html\">&lt;Avatar src=\"/photo.jpg\" alt=\"Jane Doe\" initials=\"JD\" /&gt;</code></pre>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>avatar-image</code> — an avatar indicator inside image such as a user photo</li>\n<li><code>avatar-text</code> — an avatar indicator inner text such as a user name</li>\n<li><code>avatar-group</code> — a group of avatar components</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Img Role: https://www.w3.org/TR/wai-aria-1.2/#img</li>\n</ul>";
</script>

<svelte:head>
  <title>Avatar — Lily Design System</title>
  <meta name="description" content="An avatar circle is a component that displays a user's profile image or fallback initials within a circular frame, commonly used to represent a user identity in apps, websites, and social platforms..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
