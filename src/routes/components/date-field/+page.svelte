<script lang="ts">
  const html: string = "<h1>Date Field</h1>\n\n<p>A date field provides a fully labeled date input with built-in support for descriptions and error messages. It wraps a native <code>&lt;input type=\"date\"&gt;</code> with a visible <code>&lt;label&gt;</code>, optional helper text, and validation error display, all properly linked via generated IDs for accessibility.</p>\n\n<p>This component is ideal for forms that require date input with validation feedback. The <code>value</code> prop is bindable, allowing two-way data flow with the parent component. Error messages are announced to screen readers via <code>role=\"alert\"</code>, and the input's invalid state is communicated through <code>aria-invalid</code> and <code>aria-errormessage</code>.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> wrapper containing a <code>&lt;label&gt;</code>, <code>&lt;input type=\"date\"&gt;</code>, optional description <code>&lt;p&gt;</code>, and optional error <code>&lt;p&gt;</code></li>\n<li>Auto-generates a unique ID for the input if none is provided, using derived value for reactivity</li>\n<li>Derives <code>descriptionId</code> and <code>errorId</code> from the input ID for ARIA linking</li>\n<li>Supports two-way binding on the <code>value</code> prop</li>\n<li>Error paragraph uses <code>role=\"alert\"</code> for live announcement to screen readers</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- visible label text for the date field</li>\n<li><code>value</code>: string (default: \"\") -- current date value in YYYY-MM-DD format, two-way bindable via <code>value</code></li>\n<li><code>description</code>: string (default: undefined) -- helper text displayed below the input</li>\n<li><code>error</code>: string (default: undefined) -- error message displayed when validation fails</li>\n<li><code>required</code>: boolean (default: false) -- whether the field is required</li>\n<li><code>disabled</code>: boolean (default: false) -- whether the field is disabled</li>\n<li><code>id</code>: string (default: undefined) -- custom ID for the input element; auto-generated if omitted</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Basic date field for a registration form:</p>\n\n<pre><code class=\"language-html\">&lt;DateField label=\"Date of birth\" value={dateOfBirth} description=\"Enter as day, month, year\" required /&gt;</code></pre>\n\n<p>Date field with validation error:</p>\n\n<pre><code class=\"language-html\">&lt;DateField label=\"Appointment date\" value={appointmentDate} error=\"Date must be in the future\" required /&gt;</code></pre>\n\n<p>Optional date field with helper text:</p>\n\n<pre><code class=\"language-html\">&lt;DateField label=\"Preferred start date\" value={startDate} description=\"Leave blank if flexible\" /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None beyond native <code>&lt;input type=\"date\"&gt;</code> keyboard behavior, which varies by browser.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>&lt;label for={inputId}&gt;</code> -- links the visible label to the input</li>\n<li><code>aria-describedby={descriptionId}</code> -- links the input to its description text when present</li>\n<li><code>aria-invalid=\"true\"</code> -- indicates the input contains an invalid value when an error is present</li>\n<li><code>aria-errormessage={errorId}</code> -- links the input to its error message when present</li>\n<li><code>role=\"alert\"</code> -- on the error paragraph, announces the error message to screen readers</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for structured date entry with separate fields for day, month, and year.</li>\n<li>Use when more control over individual date parts is needed than a plain DateInput provides.</li>\n<li>Use when you want built-in ARIA linking between the label, input, description, and error message without manual ID management.</li>\n<li>Use for forms that require date input with validation feedback, description text, and error display in a single integrated component.</li>\n<li>Use when accessibility-compliant date entry is essential, such as in healthcare or government forms.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for known exact dates where a simple DateInput is sufficient.</li>\n<li>Do not use for calendar-based date selection -- use CalendarTable or CalendarRangePicker instead.</li>\n<li>Do not use when you need a bare date input with custom label placement; use DateInput instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless DateField component provides a <code>&lt;div&gt;</code> wrapper with a <code>&lt;label&gt;</code>, <code>&lt;input type=\"date\"&gt;</code>, optional description <code>&lt;p&gt;</code>, and optional error <code>&lt;p&gt;</code> with <code>role=\"alert\"</code>, all linked by auto-generated IDs for <code>aria-describedby</code> and <code>aria-errormessage</code>. The consumer provides all visual styling including label typography, input appearance, description and error text colors, and layout.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.date-field</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>date-field</code></li>\n<li>Verify role=\"alert\"` -- on the error paragraph, announces the error message to screen readers</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Place the description text below the label and above the input to set expectations before users interact. Display error messages in a contrasting color near the input.</li>\n<li><strong>Developers</strong>: Use the bindable <code>value</code> prop for two-way date state. The auto-generated IDs handle all ARIA linking; provide a custom <code>id</code> prop only when you need a specific ID for testing or integration.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>date-input</code> — an input for entering a date value &lt;input type=\"date\"&gt;</li>\n<li><code>date-range</code> — a display of a start and end date range</li>\n<li><code>calendar-range-picker</code> — a picker for selecting a date range on a calendar</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>ARIA Error Handling: https://www.w3.org/WAI/WCAG22/Techniques/aria/ARIA21</li>\n<li>WAI Forms Tutorial: https://www.w3.org/WAI/tutorials/forms/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"date-field\" role=\"group\" aria-label=\"Date\"><input type=\"text\" aria-label=\"Day\" size=\"2\" value=\"15\" /> / <input type=\"text\" aria-label=\"Month\" size=\"2\" value=\"03\" /> / <input type=\"text\" aria-label=\"Year\" size=\"4\" value=\"2026\" /></div>";
  const svelteSource: string = "// In your Svelte component:\nimport DateField from \"lily-design-system-svelte-headless/components/DateField/DateField.svelte\";\n\n<DateField>\n  <!-- content -->\n</DateField>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Date Field — Lily Design System</title>
  <meta name="description" content="A date field provides a fully labeled date input with built-in support for descriptions and error messages. It wraps a native &lt;input type=&quot;date&quot;&gt; with a visible &lt;label&gt;, optional helper text, and v..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

