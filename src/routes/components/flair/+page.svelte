<script lang="ts">
  const html: string = "<h1>Flair</h1>\n\n<p>A flair is a small inline element used to add visual emphasis, categorization, or status labeling to elements within an interface. Commonly used as tags, badges, or labels in forums, social platforms, or dashboards, flair can indicate user roles, content types, statuses, or categories (e.g., \"Moderator\", \"Urgent\", \"New\").</p>\n\n<p>By default the flair is treated as decorative (<code>aria-hidden=\"true\"</code>), meaning screen readers skip it. When a <code>label</code> prop is provided, the flair becomes meaningful and is announced to assistive technology via <code>aria-label</code>, making it suitable for conveying important status information.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;span&gt;</code> element for inline display</li>\n<li>When no <code>label</code> is provided, sets <code>aria-hidden=\"true\"</code> to mark it as decorative</li>\n<li>When <code>label</code> is provided, sets <code>aria-label</code> for screen reader announcement and removes <code>aria-hidden</code></li>\n<li>Spreads <code>restProps</code> onto the <code>&lt;span&gt;</code> element for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (default: undefined) -- accessible label; omit for decorative flair, provide for meaningful flair</li>\n<li><code>children</code>: slot (required) -- flair content text</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Decorative \"New\" marker on a content listing:</p>\n\n<pre><code class=\"language-html\">&lt;h3&gt;Getting started guide &lt;Flair&gt;New&lt;/Flair&gt;&lt;/h3&gt;</code></pre>\n\n<p>Meaningful flair with accessible label for a user role:</p>\n\n<pre><code class=\"language-html\">&lt;span&gt;Posted by Alice &lt;Flair label=\"Role: Moderator\"&gt;Moderator&lt;/Flair&gt;&lt;/span&gt;</code></pre>\n\n<p>Featured content marker:</p>\n\n<pre><code class=\"language-html\">&lt;a href=\"/articles/best-practices\"&gt;\n  Best practices for accessible forms &lt;Flair label=\"Featured article\"&gt;Featured&lt;/Flair&gt;\n&lt;/a&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive inline display element.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-hidden=\"true\"</code> -- applied when no <code>label</code> is provided, marking the flair as decorative</li>\n<li><code>aria-label</code> -- applied when <code>label</code> is provided, giving the flair an accessible name for screen readers</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for decorative visual emphasis or highlight on text, such as \"New\", \"Featured\", or \"Popular\" markers.</li>\n<li>Use when the emphasis is supplementary and the surrounding content is understandable without the flair.</li>\n<li>Use in forums, dashboards, or content listings where user roles or content types need quick visual identification.</li>\n<li>Use with the <code>label</code> prop when the flair conveys meaningful information that screen readers should announce.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for status indicators that convey critical information -- use Tag instead.</li>\n<li>Do not use for counts or category labels that update dynamically -- use Badge instead.</li>\n<li>Do not use for AI transparency indicators -- use AiLabel instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;span&gt;</code> with conditional <code>aria-hidden</code> or <code>aria-label</code> depending on whether the flair is decorative or meaningful. The consumer provides all visual styling including background color, border, font size, and padding.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.flair</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;span&gt;</code> element with class <code>flair</code></li>\n<li>Verify aria-label<code> -- applied when </code>label` is provided, giving the flair an accessible name for screen readers</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use consistent color coding across your application for flair categories. Keep flair text short -- one or two words maximum.</li>\n<li><strong>Developers</strong>: Provide the <code>label</code> prop whenever the flair conveys meaningful information that is not available from surrounding text. Omit <code>label</code> only for purely decorative flair.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>badge</code> — a small label for counts, statuses, or categories</li>\n<li><code>tag</code> — a keyword label for categorizing content</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/HTML/Element/span\">MDN span element</a></li>\n</ul>";
</script>

<svelte:head>
  <title>Flair — Lily Design System</title>
  <meta name="description" content="A flair is a small inline element used to add visual emphasis, categorization, or status labeling to elements within an interface. Commonly used as tags, badges, or labels in forums, social platfor..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
