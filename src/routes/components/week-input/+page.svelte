<script lang="ts">
  const html: string = "<h1>Week Input</h1>\n\n<p>A week input is a UI/UX component that provides a field for selecting a specific week of the year using the native HTML <code>&lt;input type=\"week\"&gt;</code> element. It renders a week picker control that allows users to select a year and week number, with values in YYYY-Www format (e.g., \"2024-W01\"). The component includes aria-label for accessibility and supports bindable value for two-way data binding, along with standard form attributes like required and disabled.</p>\n\n<p>Week inputs are useful for scheduling applications, time tracking systems, reporting tools, and any scenario where data is organized by week rather than by specific date.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a single <code>&lt;input type=\"week\"&gt;</code> element for native week picker behavior</li>\n<li>Supports two-way binding for the <code>value</code> prop enabling two-way data binding with two-way <code>value</code> binding</li>\n<li>Values follow the ISO 8601 week format: YYYY-Www (e.g., \"2024-W01\" for the first week of 2024)</li>\n<li>The <code>aria-label</code> prop provides an accessible name since there is no visible <code>&lt;label&gt;</code> element</li>\n<li>Supports rest props via <code>...restProps</code> for extensibility (e.g., <code>id</code>, <code>name</code>, <code>min</code>, <code>max</code>, <code>class</code>)</li>\n<li>Browser support varies; some browsers render a native week picker while others fall back to a text input</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name applied via <code>aria-label</code></li>\n<li><code>value</code>: string (default: \"\") -- current week value in YYYY-Www format, bindable with two-way <code>value</code> binding</li>\n<li><code>required</code>: boolean (default: false) -- whether the field must be filled before form submission</li>\n<li><code>disabled</code>: boolean (default: false) -- whether the input is disabled</li>\n<li><code>...restProps</code>: spread onto the <code>&lt;input&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Sprint planning week selection:</p>\n\n<pre><code class=\"language-html\">&lt;WeekInput label=\"Sprint week\" value={sprintWeek} required /&gt;</code></pre>\n\n<p>Payroll period with constrained range:</p>\n\n<pre><code class=\"language-html\">&lt;WeekInput label=\"Payroll week\" value={payrollWeek} min=\"2026-W01\" max=\"2026-W52\" required /&gt;</code></pre>\n\n<p>Weekly report filter:</p>\n\n<pre><code class=\"language-html\">&lt;WeekInput label=\"Report week\" value={reportWeek} /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>When the browser renders a native week picker:</p>\n\n<ul>\n<li>Up Arrow / Down Arrow: adjust the focused segment (year or week number)</li>\n<li>Left Arrow / Right Arrow: move focus between year and week segments</li>\n<li>Enter: confirm selection / submit form</li>\n</ul>\n\n<p>Note: keyboard interactions depend on the browser's native week picker implementation.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides the accessible name for the week input since no visible label element is associated</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for selecting a specific week and year, such as weekly schedules or reporting.</li>\n<li>Use when data is organized by week rather than by specific date.</li>\n<li>Use for time tracking, sprint planning, or payroll period selection.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when a full date is needed -- use DateInput instead.</li>\n<li>Do not use when a month is needed -- use MonthInput instead.</li>\n<li>Do not use when consistent cross-browser behavior is critical without a fallback, since native <code>&lt;input type=\"week\"&gt;</code> support varies.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a native <code>&lt;input type=\"week\"&gt;</code> element with <code>aria-label</code>, two-way value binding in ISO 8601 YYYY-Www format, and support for <code>required</code> and <code>disabled</code> states. The consumer provides all visual styling and any surrounding labels or hints.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.week-input</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;input&gt;</code> element with class <code>week-input</code></li>\n<li>Verify aria-label={label}` -- provides the accessible name for the week input since no visible label element is associated</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Display a hint showing the expected format (e.g., \"2024-W01\") since the native week picker appearance varies significantly across browsers. Provide a fallback UI for unsupported browsers.</li>\n<li><strong>Developers</strong>: Values follow ISO 8601 week format (YYYY-Www). Use <code>min</code> and <code>max</code> via restProps to constrain the selectable range. Test across browsers, as some fall back to a plain text input.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>date-input</code> — an input for entering a date value &lt;input type=\"date\"&gt;</li>\n<li><code>month-input</code> — an input for selecting a month and year &lt;input type=\"month\"&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN input type=\"week\": https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/week</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<label class=\"label\" for=\"demo-input\">Label</label><input class=\"week-input\" type=\"week\" id=\"demo-input\" aria-label=\"WeekInput\" />";
  const svelteSource: string = "// In your Svelte component:\nimport WeekInput from \"lily-design-system-svelte-headless/components/WeekInput/WeekInput.svelte\";\n\n<WeekInput label=\"Label\" />\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Week Input — Lily Design System</title>
  <meta name="description" content="A week input is a UI/UX component that provides a field for selecting a specific week of the year using the native HTML &lt;input type=&quot;week&quot;&gt; element. It renders a week picker control that allows use..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

