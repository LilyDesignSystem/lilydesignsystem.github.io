<script lang="ts">
  const html: string = "<h1>Digital Object Identifier Link</h1>\n\n<p>A digital object identifier link is a permanent hyperlink for a Digital Object Identifier (DOI) to an electronic source. DOIs provide persistent, actionable identification for content objects in digital environments and are widely used in academic publishing, scientific journals, and digital libraries.</p>\n\n<p>This headless component uses the native HTML <code>&lt;a&gt;</code> element for inherent link semantics. It constructs a DOI resolver URL (e.g., <code>https://doi.org/10.1234/example</code>) from the provided DOI string.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders an <code>&lt;a&gt;</code> element for hyperlink semantics</li>\n<li>Constructs the <code>href</code> from the DOI using the <code>https://doi.org/</code> resolver prefix</li>\n<li><code>rel=\"noopener noreferrer\"</code> for security when opening external links</li>\n<li><code>target</code> can be set via restProps (e.g., <code>target=\"_blank\"</code> for new tab)</li>\n<li>Consumer provides the display text as children, or the DOI is displayed by default</li>\n<li>Spreads <code>restProps</code> onto the <code>&lt;a&gt;</code> for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>doi</code>: string (required) -- the Digital Object Identifier (e.g., \"10.1000/xyz123\")</li>\n<li><code>children</code>: slot (optional) -- link display text; defaults to the DOI string if not provided</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;a&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Basic DOI link:</p>\n\n<pre><code class=\"language-html\">&lt;DigitalObjectIdentifierLink doi=\"10.1000/xyz123\" /&gt;</code></pre>\n\n<p>DOI link with custom display text:</p>\n\n<pre><code class=\"language-html\">&lt;DigitalObjectIdentifierLink doi=\"10.1000/xyz123\"&gt;\n  View the original paper\n&lt;/DigitalObjectIdentifierLink&gt;</code></pre>\n\n<p>DOI link opening in a new tab:</p>\n\n<pre><code class=\"language-html\">&lt;DigitalObjectIdentifierLink doi=\"10.1000/xyz123\" target=\"_blank\"&gt;\n  10.1000/xyz123\n&lt;/DigitalObjectIdentifierLink&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus the link</li>\n<li>Enter: Activate the link and navigate to the DOI URL</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Implicit <code>link</code> role from <code>&lt;a&gt;</code> element with <code>href</code></li>\n<li><code>aria-label</code> can be provided via restProps for additional context (e.g., \"DOI link to Research Paper Title\")</li>\n<li>External link indication can be added by the consumer</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to create a permanent hyperlink for a Digital Object Identifier (DOI) to an electronic source.</li>\n<li>Use for linking to academic papers, journal articles, datasets, and other digital objects identified by a DOI.</li>\n<li>Use in bibliographies, reference lists, and citation sections.</li>\n<li>Use in academic, research, or clinical evidence contexts.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for non-DOI links -- use Citation for general references or ActionLink for navigation.</li>\n<li>Do not use for general-purpose hyperlinks -- use ActionLink or a standard <code>&lt;a&gt;</code> element instead.</li>\n<li>Do not use for non-DOI permanent identifiers -- use a standard link with the appropriate resolver.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides an <code>&lt;a&gt;</code> element with the DOI resolver URL as the <code>href</code>, with zero visual styling. The consumer is responsible for all CSS including link color, underline, hover states, focus indicators, and any external link icon.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.digital-object-identifier-link</code> class for targeting. No default styles are included -- this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders an <code>&lt;a&gt;</code> element with class <code>digital-object-identifier-link</code></li>\n<li>Verify the <code>href</code> is correctly constructed from the DOI (e.g., <code>https://doi.org/10.1000/xyz123</code>)</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Style DOI links consistently with other citation links. Consider adding an external link icon to indicate the link opens an external resource. Ensure the link text is descriptive enough for context.</li>\n<li><strong>Developers</strong>: Pass only the DOI identifier (e.g., \"10.1000/xyz123\"), not the full URL. The component constructs the full resolver URL automatically. Use <code>rel=\"noopener noreferrer\"</code> when opening in a new tab.</li>\n</ul>\n\n<h2>Domain Knowledge</h2>\n\n<p>A Digital Object Identifier (DOI) is a persistent identifier used to uniquely identify objects such as academic papers, datasets, and government reports. The DOI system is managed by the International DOI Foundation (IDF). DOIs are resolved via <code>https://doi.org/</code> followed by the DOI string. Example: <code>https://doi.org/10.1000/xyz123</code> resolves to the identified resource.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>citation</code> — a citation acknowledges the relevance of a work of another to the topic of discussion</li>\n<li><code>footnote</code> — a footnote reference and content element</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>DOI Handbook: https://www.doi.org/hb.html</li>\n<li>MDN HTML a element: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/a</li>\n</ul>";
</script>

<svelte:head>
  <title>Digital Object Identifier Link — Lily Design System</title>
  <meta name="description" content="A digital object identifier link is a permanent hyperlink for a Digital Object Identifier (DOI) to an electronic source. DOIs provide persistent, actionable identification for content objects in di..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
