<script lang="ts">
  const html: string = "<h1>Collapsible</h1>\n\n<p>A collapsible (disclosure) component allows users to expand or collapse a section of content. Built on the native HTML <code>&lt;details&gt;</code> and <code>&lt;summary&gt;</code> elements, it provides built-in keyboard support and screen reader announcements without any JavaScript.</p>\n\n<p>The summary text is always visible and acts as the toggle trigger. The content below is hidden when collapsed and revealed when expanded. The <code>open</code> prop is bindable, enabling parent components to programmatically control or observe the expanded/collapsed state.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses native <code>&lt;details&gt;</code> and <code>&lt;summary&gt;</code> elements for built-in accessibility support</li>\n<li>The <code>open</code> prop uses two-way binding</li>\n<li>No custom ARIA attributes needed because native elements handle all announcements</li>\n<li>Spreads <code>restProps</code> onto the <code>&lt;details&gt;</code> element for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>summary</code>: string (required) -- the clickable summary text shown as the toggle trigger</li>\n<li><code>open</code>: boolean (default: false) -- whether the content is expanded; bindable for two-way control</li>\n<li><code>children</code>: slot (required) -- the collapsible content revealed when expanded</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Basic collapsible section:</p>\n\n<pre><code class=\"language-html\">&lt;Collapsible summary=\"More info\"&gt;\n  &lt;p&gt;Hidden content revealed on expand.&lt;/p&gt;\n&lt;/Collapsible&gt;</code></pre>\n\n<p>Programmatically controlled collapsible for advanced settings:</p>\n\n<pre><code class=\"language-html\">&lt;Collapsible summary=\"Advanced settings\" open={showAdvanced}&gt;\n  &lt;label for=\"timeout\"&gt;Timeout (seconds)&lt;/label&gt;\n  &lt;NumberInput label=\"Timeout\" id=\"timeout\" value={timeout} min={1} max={300} /&gt;\n  &lt;label for=\"retries\"&gt;Max retries&lt;/label&gt;\n  &lt;NumberInput label=\"Max retries\" id=\"retries\" value={retries} min={0} max={10} /&gt;\n&lt;/Collapsible&gt;</code></pre>\n\n<p>FAQ page with multiple collapsible sections:</p>\n\n<pre><code class=\"language-html\">&lt;h2&gt;Frequently Asked Questions&lt;/h2&gt;\n&lt;Collapsible summary=\"How do I reset my password?\"&gt;\n  &lt;p&gt;Go to the login page and click \"Forgot password\". Enter your email address and follow the instructions sent to your inbox.&lt;/p&gt;\n&lt;/Collapsible&gt;\n&lt;Collapsible summary=\"Can I change my username?\"&gt;\n  &lt;p&gt;Yes. Navigate to Settings &gt; Profile and update your display name.&lt;/p&gt;\n&lt;/Collapsible&gt;\n&lt;Collapsible summary=\"What payment methods do you accept?\"&gt;\n  &lt;p&gt;We accept Visa, Mastercard, and bank transfers.&lt;/p&gt;\n&lt;/Collapsible&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Enter/Space: Toggle the disclosure open/closed (handled natively by <code>&lt;summary&gt;</code>)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>No custom ARIA attributes required -- native <code>&lt;details&gt;</code>/<code>&lt;summary&gt;</code> elements provide built-in expanded/collapsed state announcements to screen readers</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to hide secondary content that users may optionally expand, such as advanced settings, additional details, or FAQ answers.</li>\n<li>Use to reduce visual clutter on content-heavy pages while keeping information accessible.</li>\n<li>Use as a programmatically controlled container where open/close state is driven by application logic.</li>\n<li>Use for progressive disclosure in long forms or configuration panels.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for user-triggered disclosure with a visible toggle -- use Details or Expander instead.</li>\n<li>Do not use for navigation hierarchies -- use AccordionNav or TreeNav instead.</li>\n<li>Do not use for critical content that all users must see -- keep essential information visible by default.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless Collapsible component provides native <code>&lt;details&gt;</code> and <code>&lt;summary&gt;</code> elements with built-in keyboard support and screen reader announcements, plus a bindable <code>open</code> prop for programmatic control. The consumer provides all visual styling including summary appearance, expand/collapse icons, transition animations, and content padding.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.collapsible</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>collapsible</code></li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Include a clear visual indicator (such as a chevron or plus/minus icon) showing whether the section is expanded or collapsed. Ensure the summary text clearly describes the hidden content.</li>\n<li><strong>Developers</strong>: Use the bindable <code>open</code> prop to programmatically control or observe the expanded state. The native <code>&lt;details&gt;</code> element handles all accessibility announcements automatically.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>details</code> — a disclosure widget that shows and hides content</li>\n<li><code>expander</code> — a control that expands to reveal more content</li>\n<li><code>accordion-list-item</code> — an accordion list item component</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Disclosure Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/disclosure/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"collapsible\"><button aria-expanded=\"false\">Show more</button><div hidden>Collapsed content</div></div>";
  const svelteSource: string = "// In your Svelte component:\nimport Collapsible from \"lily-design-system-svelte-headless/components/Collapsible/Collapsible.svelte\";\n\n<Collapsible>\n  <!-- content -->\n</Collapsible>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Collapsible — Lily Design System</title>
  <meta name="description" content="A collapsible (disclosure) component allows users to expand or collapse a section of content. Built on the native HTML &lt;details&gt; and &lt;summary&gt; elements, it provides built-in keyboard support and sc..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

