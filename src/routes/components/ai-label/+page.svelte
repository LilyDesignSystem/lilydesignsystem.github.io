<script lang="ts">
  const html: string = "<h1>AI Label</h1>\n\n<p>AiLabel is a headless inline indicator that communicates the presence of AI-generated content and provides a pathway to AI explainability. It renders as a <code>&lt;span&gt;</code> element with appropriate ARIA attributes, serving as a visual marker that content has been generated, modified, or enhanced by artificial intelligence.</p>\n\n<p>Use AiLabel wherever AI is generating, modifying, or enhancing content to reinforce AI transparency, accountability, and explainability. The component can be placed inline with text, within form fields, alongside data visualizations, or anywhere users need to identify AI-generated content.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;span&gt;</code> element for inline placement</li>\n<li>Uses <code>aria-label</code> for accessible naming, defaulting to \"AI\" if not provided</li>\n<li>The <code>text</code> prop provides visible label text, defaulting to \"AI\"</li>\n<li>Spreads <code>restProps</code> onto the span for consumer customization</li>\n<li>Designed as the first layer of AI explainability; consumers can wrap in a button or attach a popover for deeper explainability content</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (default: \"AI\") -- accessible name for the indicator via <code>aria-label</code></li>\n<li><code>text</code>: string (default: \"AI\") -- visible text displayed inside the indicator</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;span&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Inline AI indicator within text:</p>\n\n<pre><code class=\"language-html\">&lt;p&gt;This summary was created by &lt;AiLabel /&gt; based on your uploaded documents.&lt;/p&gt;</code></pre>\n\n<p>Custom label with explainability context:</p>\n\n<pre><code class=\"language-html\">&lt;AiLabel text=\"AI generated\" label=\"AI generated content\" /&gt;</code></pre>\n\n<p>Wrapped in a button as an explainability trigger:</p>\n\n<pre><code class=\"language-html\">&lt;button onclick={showExplainability}&gt;\n  &lt;AiLabel text=\"AI assisted\" label=\"AI assisted diagnosis -- click for details\" /&gt;\n&lt;/button&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None at the component level. When used as an explainability trigger, the consumer wraps AiLabel in a <code>&lt;button&gt;</code> element for keyboard accessibility.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides an accessible name for the AI indicator so screen readers announce its purpose</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to indicate that content was generated or assisted by artificial intelligence.</li>\n<li>Use as a pathway to AI explainability, linking to or triggering information about how the AI was used.</li>\n<li>Use when transparency about AI involvement is required by policy, regulation, or user trust expectations.</li>\n<li>Use inline with text, inside form fields, or alongside data visualisations to mark AI-generated output.</li>\n<li>Use as the first layer of AI explainability -- wrap in a button and attach a Popover for deeper explanation.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for general status indicators -- use Tag or Badge instead.</li>\n<li>Do not use for content authored entirely by humans -- the label would be misleading.</li>\n<li>Do not use without an explainability pathway -- the label alone is insufficient for meaningful transparency.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;span&gt;</code> with visible text and an <code>aria-label</code> for accessible naming. The consumer provides all visual styling including colors, borders, typography, sizing, and positioning. For explainability popover behavior, the consumer wraps AiLabel in a button and attaches their own popover component.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with an <code>.ai-label</code> class for targeting. Common styling includes a small pill or badge shape, distinct color to differentiate AI content, and inline-flex display for alignment with surrounding text.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;span&gt;</code> element with the <code>ai-label</code> class</li>\n<li>Verify the default text \"AI\" is rendered</li>\n<li>Verify custom <code>text</code> prop is rendered</li>\n<li>Verify <code>aria-label</code> defaults to \"AI\" when not provided</li>\n<li>Verify custom <code>label</code> prop is applied as <code>aria-label</code></li>\n<li>Verify pass-through attributes are applied to the span</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Make the AI label visually distinct from surrounding content using a contrasting color or badge style. Keep it small and unobtrusive but clearly visible. Consider a consistent AI color across your product.</li>\n<li><strong>Developers</strong>: Always include AiLabel wherever AI generates content, even if the output is reviewed by a human. For explainability popover behavior, wrap the label in a button and use your popover component to show explanatory content on click.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>badge</code> — a small label for counts, statuses, or categories</li>\n<li><code>tag</code> — a keyword label for categorizing content</li>\n<li><code>flair</code> — a decorative highlight or emphasis element</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>Carbon Design System AI Label: https://carbondesignsystem.com/components/ai-label/usage/</li>\n<li>Carbon for AI Guidelines: https://carbondesignsystem.com/guidelines/carbon-for-ai/</li>\n</ul>";
</script>

<svelte:head>
  <title>AI Label — Lily Design System</title>
  <meta name="description" content="AiLabel is a headless inline indicator that communicates the presence of AI-generated content and provides a pathway to AI explainability. It renders as a &lt;span&gt; element with appropriate ARIA attri..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
