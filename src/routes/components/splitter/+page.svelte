<script lang="ts">
  const html: string = "<h1>Splitter</h1>\n\n<p>Splitter is a headless separator element used between resizable panels. It uses the ARIA <code>separator</code> role to indicate a draggable divider, with keyboard focus support and value tracking for reporting the current split position to assistive technologies.</p>\n\n<p>Use Splitter between two adjacent panels to provide a visual and interactive handle for resizing. The component renders the necessary ARIA attributes for screen readers to announce the separator's orientation and current position. Consumer is responsible for implementing the actual drag-to-resize behavior, visual styling, and updating the value attributes based on user interaction.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> element with <code>role=\"separator\"</code> for ARIA semantics</li>\n<li>Sets <code>tabindex=\"0\"</code> to make the separator keyboard-focusable</li>\n<li>Includes <code>aria-orientation</code> to indicate whether the split is horizontal or vertical</li>\n<li>Includes <code>aria-valuenow</code>, <code>aria-valuemin</code>, and <code>aria-valuemax</code> for communicating the current split position as a percentage (default: 50, min: 0, max: 100)</li>\n<li>Consumer should update <code>aria-valuenow</code> dynamically as the user drags the splitter</li>\n<li>Consumer is responsible for implementing keyboard handlers (arrow keys to adjust position) and pointer/drag handlers</li>\n<li>Spreads <code>restProps</code> onto the div for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible label for the splitter, applied via <code>aria-label</code></li>\n<li><code>orientation</code>: <code>\"horizontal\" | \"vertical\"</code> (default: <code>\"vertical\"</code>) -- the orientation of the split direction</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;div&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Vertical splitter between a file tree and editor:</p>\n\n<pre><code class=\"language-html\">&lt;div style=\"display: flex;\"&gt;\n  &lt;div class=\"file-tree\"&gt;&lt;!-- file list --&gt;&lt;/div&gt;\n  &lt;Splitter label=\"Resize file tree and editor\" orientation=\"vertical\" /&gt;\n  &lt;div class=\"editor\"&gt;&lt;!-- code editor --&gt;&lt;/div&gt;\n&lt;/div&gt;</code></pre>\n\n<p>Horizontal splitter between a list and detail pane:</p>\n\n<pre><code class=\"language-html\">&lt;div style=\"display: flex; flex-direction: column;\"&gt;\n  &lt;div class=\"email-list\"&gt;&lt;!-- email list --&gt;&lt;/div&gt;\n  &lt;Splitter label=\"Resize email list and preview\" orientation=\"horizontal\" /&gt;\n  &lt;div class=\"email-preview\"&gt;&lt;!-- email content --&gt;&lt;/div&gt;\n&lt;/div&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Moves focus to the splitter</li>\n<li>Arrow keys: Consumer should implement arrow key handling to adjust the split position (Left/Right for horizontal, Up/Down for vertical)</li>\n<li>Home: Consumer should implement to set the splitter to the minimum position</li>\n<li>End: Consumer should implement to set the splitter to the maximum position</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"separator\"</code> -- identifies the element as a separator between two regions</li>\n<li><code>aria-label={label}</code> -- provides an accessible name describing the splitter's purpose</li>\n<li><code>aria-orientation={orientation}</code> -- indicates whether the separator divides content horizontally or vertically</li>\n<li><code>aria-valuenow={50}</code> -- reports the current position of the splitter as a percentage</li>\n<li><code>aria-valuemin={0}</code> -- the minimum allowed position value</li>\n<li><code>aria-valuemax={100}</code> -- the maximum allowed position value</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use Splitter between two adjacent panels to provide a draggable handle for resizing, such as in code editors, file managers, or split-view layouts.</li>\n<li>Use Splitter when users need to adjust the relative sizes of adjacent content areas.</li>\n<li>Use Splitter for email or messaging interfaces with a list pane and detail pane.</li>\n<li>Use Splitter in IDE-style layouts separating a file tree from an editor area.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use Splitter for a single resizable container -- use Resizable instead.</li>\n<li>Do not use Splitter for visual dividers that do not involve resizing -- use Separator instead.</li>\n<li>Do not use Splitter when fixed-width panels are more appropriate for the layout.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component renders a <code>&lt;div&gt;</code> with <code>role=\"separator\"</code>, keyboard focus support (<code>tabindex=\"0\"</code>), <code>aria-orientation</code>, and value attributes (<code>aria-valuenow</code>, <code>aria-valuemin</code>, <code>aria-valuemax</code>) for screen reader position reporting. The consumer provides all visual styling, drag behavior, keyboard handlers for arrow keys, and dynamic value updates.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.splitter</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>splitter</code></li>\n<li>Verify role=\"separator\"` -- identifies the element as a separator between two regions</li>\n<li>Verify aria-label={label}` -- provides an accessible name describing the splitter's purpose</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Make the splitter handle at least 8px wide for easy grabbing, with a visible drag affordance such as dots or a grip icon. Change the cursor to <code>col-resize</code> or <code>row-resize</code> on hover.</li>\n<li><strong>Developers</strong>: Update <code>aria-valuenow</code> dynamically as the user drags. Implement arrow key handlers to adjust position in small increments, and Home/End to jump to min/max values.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>split-view</code> — a two-panel resizable layout container with a draggable divider between them</li>\n<li><code>resizable</code> — a container that the user can resize by dragging</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Window Splitter Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/windowsplitter/</li>\n<li>WAI-ARIA Separator Role: https://www.w3.org/TR/wai-aria-1.2/#separator</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"splitter\" role=\"separator\" aria-label=\"Resize panels\"></div>";
  const svelteSource: string = "// In your Svelte component:\nimport Splitter from \"lily-design-system-svelte-headless/components/Splitter/Splitter.svelte\";\n\n<Splitter>\n  <!-- content -->\n</Splitter>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Splitter — Lily Design System</title>
  <meta name="description" content="Splitter is a headless separator element used between resizable panels. It uses the ARIA separator role to indicate a draggable divider, with keyboard focus support and value tracking for reporting..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

