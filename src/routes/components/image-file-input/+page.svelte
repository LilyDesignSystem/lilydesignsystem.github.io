<script lang="ts">
  const html: string = "<h1>Image File Input</h1>\n\n<p>An image input provides a file upload control that is pre-configured to accept image files. It wraps a native <code>&lt;input type=\"file\"&gt;</code> element with accessible labeling and sensible defaults for image selection. This component is useful for profile photo uploads, gallery image selection, document scanning, and any form that requires image file input.</p>\n\n<p>The consumer can override the accepted file types, control required/disabled states, and add additional attributes as needed.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a single <code>&lt;input type=\"file\"&gt;</code> element with <code>accept=\"image/*\"</code> by default.</li>\n<li>Uses <code>aria-label</code> for accessible naming since there is no visible <code>&lt;label&gt;</code> element.</li>\n<li>Supports <code>required</code> and <code>disabled</code> native input attributes.</li>\n<li>The <code>accept</code> prop defaults to <code>\"image/*\"</code> but can be narrowed (e.g., <code>\"image/png, image/jpeg\"</code>).</li>\n<li>Spreads <code>restProps</code> onto the <code>&lt;input&gt;</code> for consumer customization.</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name applied via <code>aria-label</code>.</li>\n<li><code>accept</code>: string (default: <code>\"image/*\"</code>) -- MIME type filter for the file picker dialog.</li>\n<li><code>required</code>: boolean (default: <code>false</code>) -- whether the input is required for form submission.</li>\n<li><code>disabled</code>: boolean (default: <code>false</code>) -- whether the input is disabled.</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;input&gt;</code> element.</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Profile photo upload:</p>\n\n<pre><code class=\"language-html\">&lt;ImageFileInput label=\"Upload profile photo\" accept=\"image/png, image/jpeg\" required /&gt;</code></pre>\n\n<p>Medical image attachment:</p>\n\n<pre><code class=\"language-html\">&lt;ImageFileInput label=\"Upload scan image\" accept=\"image/png, image/jpeg, image/dicom\" /&gt;</code></pre>\n\n<p>ID document scan:</p>\n\n<pre><code class=\"language-html\">&lt;ImageFileInput label=\"Upload ID photo\" accept=\"image/png, image/jpeg\" required /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component uses the native <code>&lt;input type=\"file\"&gt;</code> element, which provides built-in keyboard support (Tab to focus, Enter or Space to open the file picker dialog).</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label</code> -- provides an accessible name for the file input when no visible label is present.</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for selecting image files with a preview before upload.</li>\n<li>Use for profile photos, document scans, or medical images.</li>\n<li>Use when you want to pre-filter the file picker to image files only via <code>accept=\"image/*\"</code>.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for non-image files -- use FileUpload or FileInput instead.</li>\n<li>Do not use without needing an image-specific file picker -- use FileInput instead.</li>\n<li>Do not use when drag-and-drop is needed -- use FileUpload instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a native <code>&lt;input type=\"file\"&gt;</code> with <code>accept=\"image/*\"</code> and <code>aria-label</code> for accessible file selection. The consumer provides all visual styling including the file button appearance, drag-and-drop zones, and image preview functionality.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.image-file-input</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;input&gt;</code> element with class <code>image-file-input</code></li>\n<li>Verify aria-label` -- provides an accessible name for the file input when no visible label is present.</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Consider adding an image preview area adjacent to the input so users can confirm their selection. Show accepted file type guidance near the input.</li>\n<li><strong>Developers</strong>: Narrow the <code>accept</code> prop to specific MIME types (e.g., <code>\"image/png, image/jpeg\"</code>) when only certain formats are supported. Validate file size on the client side before submission.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>file-input</code> — an input for selecting files from the file system &lt;input type=\"file\"&gt;</li>\n<li><code>file-upload</code> — a drag-and-drop area for uploading files</li>\n<li><code>image</code> — an image element with alt text</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN Input type=\"file\": https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/file</li>\n</ul>";
</script>

<svelte:head>
  <title>Image File Input — Lily Design System</title>
  <meta name="description" content="An image input provides a file upload control that is pre-configured to accept image files. It wraps a native &lt;input type=&quot;file&quot;&gt; element with accessible labeling and sensible defaults for image se..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
