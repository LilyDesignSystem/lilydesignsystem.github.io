<script lang="ts">
  const html: string = "<h1>Time Picker Input</h1>\n\n<p>A time picker is a component that allows users to select a specific time of day, often used alongside a date picker in forms, scheduling tools, or booking systems. It provides a structured way to enter time values, reducing input mistakes and improving the user experience compared to free-text entry.</p>\n\n<p>This headless component wraps a native <code>&lt;input type=\"time\"&gt;</code> element with an accessible label and support for two-way binding of the time value. It supports required and disabled states and delegates all visual presentation to the consumer.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses a native <code>&lt;input type=\"time\"&gt;</code> element for built-in browser time selection behavior</li>\n<li>Supports two-way binding on the <code>value</code> prop</li>\n<li>Native input handles 12-hour/24-hour format based on browser locale</li>\n<li>Accepts <code>...restProps</code> for forwarding additional attributes to the input element</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name applied via <code>aria-label</code></li>\n<li><code>value</code>: string (default: \"\") -- current time value in HH:MM format; bindable</li>\n<li><code>required</code>: boolean (default: false) -- whether a value is required for form submission</li>\n<li><code>disabled</code>: boolean (default: false) -- whether the input is disabled</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Appointment booking with 15-minute intervals:</p>\n\n<pre><code class=\"language-html\">&lt;TimePickerInput label=\"Appointment time\" value={appointmentTime} step=\"900\" required /&gt;</code></pre>\n\n<p>Reservation time selection:</p>\n\n<pre><code class=\"language-html\">&lt;TimePickerInput label=\"Reservation time\" value={reservationTime} /&gt;</code></pre>\n\n<p>Shift scheduling with constrained hours:</p>\n\n<pre><code class=\"language-html\">&lt;TimePickerInput label=\"Shift start\" value={shiftStart} min=\"06:00\" max=\"22:00\" step=\"1800\" required /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Move focus to/from the time input</li>\n<li>Up Arrow / Down Arrow: Increment/decrement the focused time segment (hours, minutes, AM/PM)</li>\n<li>Left Arrow / Right Arrow: Move between time segments within the input</li>\n</ul>\n\n<p>(These are provided by the native <code>&lt;input type=\"time\"&gt;</code> element.)</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides an accessible name for the time input</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use when users need to select a time from a dropdown list of slots, such as 15-minute or 30-minute intervals.</li>\n<li>Use for appointment booking, scheduling, or reservation systems where specific time slots are offered.</li>\n<li>Use when you want a dedicated time selection component that can be styled independently from a plain time input.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when any arbitrary time is valid -- use TimeInput instead.</li>\n<li>Do not use for date selection -- use DateInput or CalendarTable instead.</li>\n<li>Do not use for time duration or elapsed time tracking; use a custom duration component instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a native <code>&lt;input type=\"time\"&gt;</code> element with <code>aria-label</code>, two-way value binding, and <code>required</code>/<code>disabled</code> states. The browser handles 12-hour/24-hour format based on locale. The consumer provides all visual styling and layout.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.time-picker-input</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>time-picker-input</code></li>\n<li>Verify aria-label={label}` -- provides an accessible name for the time input</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Provide clear visual feedback for the selected time and consider adding a visible label above the picker for clarity.</li>\n<li><strong>Developers</strong>: The <code>value</code> is always in HH:MM 24-hour format internally. Use the <code>step</code> attribute via restProps to control minute granularity (e.g., <code>step=\"900\"</code> for 15-minute intervals).</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>text-input</code> — a single-line text input field &lt;input type=\"text\"&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN input type=\"time\": https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/time</li>\n</ul>";
</script>

<svelte:head>
  <title>Time Picker Input — Lily Design System</title>
  <meta name="description" content="A time picker is a component that allows users to select a specific time of day, often used alongside a date picker in forms, scheduling tools, or booking systems. It provides a structured way to e..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
