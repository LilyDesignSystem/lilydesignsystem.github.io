<script lang="ts">
  const html: string = "<h1>Chat Nav</h1>\n\n<p>A chat nav is a navigation container for chat information that provides a landmark for a list of chat conversations. It allows users to browse, select, and navigate between chat conversations in messaging interfaces, customer support tools, and collaborative platforms.</p>\n\n<p>ChatNav is a compound component. It provides the parent container structure (a <code>&lt;nav&gt;</code>), while the chat list and individual chat entries are rendered using the ChatList and ChatListItem components. Each ChatListItem typically contains a ChatMessage component showing the conversation preview.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;nav&gt;</code> element for the navigation landmark</li>\n<li><code>aria-label</code> on the <code>&lt;nav&gt;</code> provides an accessible name (e.g. \"Chat conversations\")</li>\n<li>Consumer places ChatList component and ChatListItem components as children</li>\n<li>Each ChatListItem typically contains a ChatMessage component</li>\n<li>Spreads <code>restProps</code> onto the <code>&lt;nav&gt;</code> for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name for the navigation landmark</li>\n<li><code>children</code>: slot (required) -- ChatList with ChatListItem components</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;nav&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;ChatNav label=\"Chat conversations\"&gt;\n  &lt;ChatList&gt;\n    &lt;ChatListItem&gt;\n      &lt;ChatMessage&gt;...&lt;/ChatMessage&gt;\n    &lt;/ChatListItem&gt;\n    &lt;ChatListItem&gt;\n      &lt;ChatMessage&gt;...&lt;/ChatMessage&gt;\n    &lt;/ChatListItem&gt;\n  &lt;/ChatList&gt;\n&lt;/ChatNav&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus moves between interactive elements in the chat list</li>\n<li>Enter: Activates the focused chat list item or link</li>\n<li>Arrow Down: Move focus to the next chat list item</li>\n<li>Arrow Up: Move focus to the previous chat list item</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>&lt;nav&gt;</code> with <code>aria-label</code> creates a navigation landmark for the chat list</li>\n<li><code>&lt;ol&gt;</code> conveys the ordered relationship of the chat entries</li>\n<li>Implicit <code>listitem</code> role from semantic <code>&lt;li&gt;</code> elements</li>\n<li><code>aria-current</code> on the currently selected chat item</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use as a navigation landmark for chat conversations in messaging applications, customer support interfaces, or collaborative platforms.</li>\n<li>Use when users need to browse and select between multiple chat threads.</li>\n<li>Use when conversations should be listed in a navigable, ordered structure with <code>aria-current</code> support.</li>\n<li>Use when the chat interface needs a semantic <code>&lt;nav&gt;</code> landmark for screen reader users.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for non-chat navigation -- use ContentsNav for page sections or TreeNav for hierarchical navigation.</li>\n<li>Do not use for a single conversation thread -- render ChatList directly without the <code>&lt;nav&gt;</code> wrapper.</li>\n<li>Do not use for displaying system notifications -- use Alert or Notification instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides a <code>&lt;nav&gt;</code> landmark with <code>aria-label</code> containing an <code>&lt;ol&gt;</code> for ordered chat entries, with zero visual styling. The consumer is responsible for all CSS including chat item layout, active/selected states, unread indicators, avatar positioning, and responsive design.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.chat-nav</code> class for targeting. No default styles are included -- this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;nav&gt;</code> element with class <code>chat-nav</code></li>\n<li>Verify <code>&lt;nav&gt;</code> with <code>aria-label</code> creates a navigation landmark</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Show clear visual distinction between read and unread conversations. Display the most recent message preview and timestamp for each chat entry. Ensure the currently selected conversation is visually highlighted.</li>\n<li><strong>Developers</strong>: Always provide a descriptive <code>label</code> prop (e.g., \"Chat conversations\") for the navigation landmark. Use <code>aria-current</code> on the currently active chat item.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>ChatNav follows the Nav / List / ListItem composition pattern:</p>\n\n<ul>\n<li><strong>ChatNav</strong> -- outer <code>&lt;nav&gt;</code> container providing the navigation landmark and accessible label.</li>\n<li><strong>ChatList</strong> -- <code>&lt;ol&gt;</code> list conveying the ordered relationship of chat entries.</li>\n<li><strong>ChatListItem</strong> -- individual <code>&lt;li&gt;</code> items, each typically containing a ChatMessage component.</li>\n<li><strong>ChatMessage</strong> -- <code>&lt;article&gt;</code> element displaying one chat message with author, time, and content.</li>\n</ul>\n\n<pre><code class=\"language-html\">&lt;ChatNav label=\"Chat conversations\"&gt;\n  &lt;ChatList&gt;\n    &lt;ChatListItem&gt;\n      &lt;ChatMessage&gt;...&lt;/ChatMessage&gt;\n    &lt;/ChatListItem&gt;\n    &lt;ChatListItem&gt;\n      &lt;ChatMessage&gt;...&lt;/ChatMessage&gt;\n    &lt;/ChatListItem&gt;\n  &lt;/ChatList&gt;\n&lt;/ChatNav&gt;</code></pre>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>chat-list</code> — an ordered list of chat list item components</li>\n<li><code>chat-list-item</code> — one chat list item component, typically containing one chat message component</li>\n<li><code>chat-message</code> — a chat message shows one chat conversation message entry and all its data, including the author avatar, author name, time, etc.</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Navigation Landmark: https://www.w3.org/WAI/ARIA/apg/patterns/landmark/</li>\n</ul>";
</script>

<svelte:head>
  <title>Chat Nav — Lily Design System</title>
  <meta name="description" content="A chat nav is a navigation container for chat information that provides a landmark for a list of chat conversations. It allows users to browse, select, and navigate between chat conversations in me..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
