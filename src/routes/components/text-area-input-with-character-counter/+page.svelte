<script lang="ts">
  const html: string = "<h1>Text Area With Character Counter</h1>\n\n<p>TextAreaInputWithCharacterCounter is a headless component that wraps a native <code>&lt;textarea&gt;</code> and a character counter caption inside a <code>&lt;div&gt;</code>. The counter displays \"[number] of [maximum] characters\" below the text-area-input and updates reactively as the user types.</p>\n\n<p>This component is useful for feedback forms, comment fields, bio inputs, and any interface where users need to know how many characters they have used relative to a maximum limit.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a wrapper <code>&lt;div&gt;</code> containing a <code>&lt;textarea&gt;</code> and a character counter caption</li>\n<li>The character counter displays \"[number] of [maximum] characters\" below the text-area-input</li>\n<li>The counter updates reactively as the user types</li>\n<li>Uses <code>aria-describedby</code> to link the text-area-input to the counter for screen readers</li>\n<li>The counter uses <code>aria-live=\"polite\"</code> so screen readers announce changes</li>\n<li>Supports two-way binding on the <code>value</code> prop</li>\n<li>The <code>counterTemplate</code> prop allows customization of the counter text for internationalization</li>\n<li>Spreads <code>restProps</code> onto the wrapper <code>&lt;div&gt;</code> for consumer extensibility</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name for the text-area-input via <code>aria-label</code></li>\n<li><code>value</code>: string (default: \"\") -- bindable text-area-input value</li>\n<li><code>maxLength</code>: number (required) -- maximum number of characters allowed</li>\n<li><code>counterTemplate</code>: string (default: \"{count} of {max} characters\") -- template for the counter text</li>\n<li><code>rows</code>: number (optional) -- number of visible text rows</li>\n<li><code>placeholder</code>: string (optional) -- placeholder text for the text-area-input</li>\n<li><code>required</code>: boolean (default: false) -- whether the text-area-input is required</li>\n<li><code>disabled</code>: boolean (default: false) -- whether the text-area-input is disabled</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the wrapper <code>&lt;div&gt;</code></li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Basic feedback field with a 500-character limit:</p>\n\n<pre><code class=\"language-html\">&lt;TextAreaInputWithCharacterCounter label=\"Feedback\" maxLength={500} /&gt;</code></pre>\n\n<p>Referral notes with hint text, pre-filled value, and visible rows:</p>\n\n<pre><code class=\"language-html\">&lt;TextAreaInputWithCharacterCounter\n  label=\"Additional notes for the referral\"\n  value={referralNotes}\n  maxLength={500}\n  rows={6}\n  placeholder=\"Include any relevant medical history or current medications\"\n/&gt;</code></pre>\n\n<p>User bio with two-way binding:</p>\n\n<pre><code class=\"language-html\">&lt;TextAreaInputWithCharacterCounter label=\"Bio\" value={bio} maxLength={200} rows={4} placeholder=\"Tell us about yourself\" /&gt;</code></pre>\n\n<p>Internationalized counter text (French):</p>\n\n<pre><code class=\"language-html\">&lt;TextAreaInputWithCharacterCounter label=\"Commentaire\" maxLength={300} counterTemplate=\"{count} sur {max} caractères\" /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Moves focus to and from the text-area-input (native browser behavior)</li>\n<li>Standard text-area-input keyboard interactions (native browser behavior)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides an accessible name for the text-area-input</li>\n<li><code>aria-describedby</code> -- links the text-area-input to the character counter</li>\n<li><code>aria-live=\"polite\"</code> -- on the counter so screen readers announce updates</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use when you need a multi-line text input with a visible character limit indicator.</li>\n<li>Use for free-text fields where a maximum length is required, such as referral notes, feedback forms, or medical history descriptions.</li>\n<li>Use when the character count message \"[number] of [maximum] characters\" provides useful feedback during entry.</li>\n<li>Use when you want the text-area-input and counter linked automatically via <code>aria-describedby</code> without manual wiring.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when no character limit is needed — use TextAreaInput on its own.</li>\n<li>Do not use for single-line inputs — use TextInput instead.</li>\n<li>Do not use when a word count is more appropriate than a character count — adapt CharacterCounter with word counting logic.</li>\n<li>Do not use when the counter needs to be positioned independently from the text-area-input — use CharacterCounter as a standalone component.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;div&gt;</code> wrapping a native <code>&lt;textarea&gt;</code> and a character counter <code>&lt;span&gt;</code> with <code>aria-describedby</code> linking, <code>aria-live=\"polite\"</code> for announcements, and a configurable counter template. The consumer provides all visual styling.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.text-area-input-with-character-counter</code> class for targeting. No default styles are included -- this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a wrapper <code>&lt;div&gt;</code> with class <code>text-area-input-with-character-counter</code></li>\n<li>Verify it contains a <code>&lt;textarea&gt;</code> with <code>aria-label</code> and <code>aria-describedby</code></li>\n<li>Verify it contains a counter element with <code>aria-live=\"polite\"</code></li>\n<li>Verify the counter displays the correct character count</li>\n<li>Verify the counter updates as the user types</li>\n<li>Verify <code>maxLength</code> is applied to the text-area-input</li>\n<li>Verify <code>disabled</code> and <code>required</code> attributes propagate correctly</li>\n<li>Verify the <code>counterTemplate</code> prop customizes the counter text</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Position the counter below the text-area-input. Consider visual feedback when nearing the limit (the consumer can style based on character count).</li>\n<li><strong>Developers</strong>: The <code>counterTemplate</code> prop uses <code>{count}</code> and <code>{max}</code> placeholders. Override for internationalization.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>text-area-input</code> — a multi-line text input area</li>\n<li><code>character-counter</code> — a counter showing remaining or used characters in a text field</li>\n<li><code>text-input</code> — a single-line text input field &lt;input type=\"text\"&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN text-area-input: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/text-area-input</li>\n<li>WAI-ARIA aria-describedby: https://www.w3.org/TR/wai-aria-1.2/#aria-describedby</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"text-area-input-with-character-counter\" aria-label=\"a multi-line text area input with a caption below that is a character counter \"[number] of [maximum] characters\"\">TextAreaInputWithCharacterCounter</div>";
  const svelteSource: string = "// In your Svelte component:\nimport TextAreaInputWithCharacterCounter from \"lily-design-system-svelte-headless/components/TextAreaInputWithCharacterCounter/TextAreaInputWithCharacterCounter.svelte\";\n\n<TextAreaInputWithCharacterCounter>\n  <!-- content -->\n</TextAreaInputWithCharacterCounter>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Text Area With Character Counter — Lily Design System</title>
  <meta name="description" content="TextAreaInputWithCharacterCounter is a headless component that wraps a native &lt;textarea&gt; and a character counter caption inside a &lt;div&gt;. The counter displays &quot;[number] of [maximum] characters&quot; belo..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

