<script lang="ts">
  const html: string = "<h1>Slider</h1>\n\n<p>A slider is a UI/UX component that allows users to select a numeric value by dragging a handle along a track, typically used for adjusting settings like volume, brightness, price ranges, or timelines. It provides an intuitive and interactive way to input data within a defined minimum and maximum range.</p>\n\n<p>This headless component wraps a native <code>&lt;input type=\"range\"&gt;</code> element with configurable min, max, and step values, providing two-way binding for reactive state management and an accessible label for screen readers.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses a native <code>&lt;input type=\"range\"&gt;</code> element for built-in browser slider behavior</li>\n<li>Supports two-way binding on the <code>value</code> prop</li>\n<li>Accepts <code>...restProps</code> for forwarding additional attributes to the input element</li>\n<li>No internal state management beyond the bindable value -- relies on native input behavior</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name applied via <code>aria-label</code></li>\n<li><code>value</code>: number (default: 50) -- current slider value; bindable</li>\n<li><code>min</code>: number (default: 0) -- minimum allowed value</li>\n<li><code>max</code>: number (default: 100) -- maximum allowed value</li>\n<li><code>step</code>: number (default: 1) -- increment between allowed values</li>\n<li><code>disabled</code>: boolean (default: false) -- whether the slider is disabled</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Volume control with step increments:</p>\n\n<pre><code class=\"language-html\">&lt;Slider label=\"Volume\" value={volume} min={0} max={100} step={5} /&gt;</code></pre>\n\n<p>Brightness adjustment with displayed value:</p>\n\n<pre><code class=\"language-html\">&lt;Slider label=\"Brightness\" value={brightness} min={0} max={100} /&gt;\n&lt;p&gt;Brightness: {brightness}%&lt;/p&gt;</code></pre>\n\n<p>Price range filter:</p>\n\n<pre><code class=\"language-html\">&lt;Slider label=\"Maximum price\" value={maxPrice} min={0} max={500} step={10} /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Left Arrow / Down Arrow: Decrease value by one step</li>\n<li>Right Arrow / Up Arrow: Increase value by one step</li>\n<li>Home: Set to minimum value</li>\n<li>End: Set to maximum value</li>\n<li>Page Up: Increase by a larger step (browser-dependent)</li>\n<li>Page Down: Decrease by a larger step (browser-dependent)</li>\n</ul>\n\n<p>(These are provided by the native <code>&lt;input type=\"range\"&gt;</code> element.)</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides an accessible name for the slider</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for selecting a value along a continuous track, such as volume, brightness, or a numeric range.</li>\n<li>Use when approximate value selection is acceptable and a visual track provides helpful context.</li>\n<li>Use for adjusting settings like zoom level, playback speed, or opacity.</li>\n<li>Use when the full range should be visually represented to help users gauge their selection.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when exact numeric input is needed -- use NumberInput instead.</li>\n<li>Do not use for confirming an action by sliding -- use SliderButton instead.</li>\n<li>Do not use for angle-specific selection in degrees -- use AngleSliderRangeInput instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component wraps a native <code>&lt;input type=\"range\"&gt;</code> element with <code>aria-label</code>, configurable min/max/step values, and two-way binding. It provides full native slider keyboard navigation and accessibility. The consumer provides all visual styling for the track, thumb, and any value labels.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.slider</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>slider</code></li>\n<li>Verify aria-label={label}` -- provides an accessible name for the slider</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Display the current value near the slider thumb or in a nearby label so users can see the exact selection. Use a track fill color to indicate the selected portion of the range.</li>\n<li><strong>Developers</strong>: Use the <code>step</code> prop to constrain values to meaningful increments. Consider debouncing the bound value if the slider drives expensive operations like API calls.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>range-input</code> — a slider input for selecting a value within a range &lt;input type=\"range\"&gt;</li>\n<li><code>slider-button</code> — a button with a slider that the user needs to slide to confirm</li>\n<li><code>dial</code> — a rotary dial control for selecting a value</li>\n<li><code>angle-slider-range-input</code> — a range input for selecting an angle in degrees</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Slider Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/slider/</li>\n<li>MDN input type=\"range\": https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/range</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"slider\" role=\"slider\" aria-valuenow=\"50\" aria-valuemin=\"0\" aria-valuemax=\"100\" aria-label=\"Value\">50</div>";
  const svelteSource: string = "// In your Svelte component:\nimport Slider from \"lily-design-system-svelte-headless/components/Slider/Slider.svelte\";\n\n<Slider>\n  <!-- content -->\n</Slider>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Slider — Lily Design System</title>
  <meta name="description" content="A slider is a UI/UX component that allows users to select a numeric value by dragging a handle along a track, typically used for adjusting settings like volume, brightness, price ranges, or timelin..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

