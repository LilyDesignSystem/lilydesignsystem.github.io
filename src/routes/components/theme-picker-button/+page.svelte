<script lang="ts">
  const html: string = "<h1>ThemeSelectButton</h1>\n\n<p>A theme picker button is a button for selecting a visual theme within a ThemeSelect component. Each button represents one theme option and shows a preview or label for that theme.</p>\n\n<p>This headless component uses a <code>&lt;button&gt;</code> element with <code>aria-pressed</code> to indicate the currently selected theme, following the toggle button pattern within a picker group.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses <code>&lt;button&gt;</code> element for native keyboard and accessibility support</li>\n<li><code>aria-pressed</code> indicates whether this theme is currently selected</li>\n<li><code>aria-label</code> describes the theme this button represents</li>\n<li>Part of the ThemeSelect composition pattern</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>pressed</code>: boolean (default: false) -- whether this theme is currently selected</li>\n<li><code>label</code>: string (required) -- accessible label describing the theme</li>\n<li><code>disabled</code>: boolean (default: false) -- whether the button is disabled</li>\n<li><code>onclick</code>: callback (optional) -- click handler for theme selection</li>\n<li><code>children</code>: slot -- button content (theme preview, label, swatch)</li>\n<li><code>...restProps</code>: Any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Basic theme picker with light and dark options:</p>\n\n<pre><code class=\"language-html\">&lt;ThemeSelect label=\"Choose theme\"&gt;\n  &lt;ThemeSelectButton pressed={theme === 'light'} label=\"Light theme\" onclick={() =&gt; setTheme('light')}&gt;\n    Light\n  &lt;/ThemeSelectButton&gt;\n  &lt;ThemeSelectButton pressed={theme === 'dark'} label=\"Dark theme\" onclick={() =&gt; setTheme('dark')}&gt;\n    Dark\n  &lt;/ThemeSelectButton&gt;\n&lt;/ThemeSelect&gt;</code></pre>\n\n<p>With high-contrast option and visual swatches:</p>\n\n<pre><code class=\"language-html\">&lt;ThemeSelect label=\"Appearance\"&gt;\n  &lt;ThemeSelectButton pressed={theme === 'light'} label=\"Light theme\" onclick={() =&gt; setTheme('light')}&gt;\n    &lt;span class=\"swatch swatch-light\"&gt;&lt;/span&gt; Light\n  &lt;/ThemeSelectButton&gt;\n  &lt;ThemeSelectButton pressed={theme === 'dark'} label=\"Dark theme\" onclick={() =&gt; setTheme('dark')}&gt;\n    &lt;span class=\"swatch swatch-dark\"&gt;&lt;/span&gt; Dark\n  &lt;/ThemeSelectButton&gt;\n  &lt;ThemeSelectButton pressed={theme === 'high-contrast'} label=\"High contrast theme\" onclick={() =&gt; setTheme('high-contrast')}&gt;\n    &lt;span class=\"swatch swatch-high-contrast\"&gt;&lt;/span&gt; High Contrast\n  &lt;/ThemeSelectButton&gt;\n&lt;/ThemeSelect&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus the button</li>\n<li>Enter: Select the theme</li>\n<li>Space: Select the theme</li>\n<li>(All handled natively by <code>&lt;button&gt;</code> element)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Implicit <code>button</code> role from <code>&lt;button&gt;</code> element</li>\n<li><code>aria-pressed</code> -- indicates whether this theme is currently selected</li>\n<li><code>aria-label</code> -- describes the theme this button represents</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside a ThemeSelect to represent one selectable theme option.</li>\n<li>Use when you want button-based theme selection with visual previews or swatches.</li>\n<li>Use when each theme option needs custom content such as icons or colour samples.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside of a ThemeSelect -- the button relies on the picker for grouping and context.</li>\n<li>Do not use for non-theme selections -- use <a href=\"../segment-group-item/\">SegmentGroupItem</a> instead.</li>\n<li>Do not use for standalone toggle actions -- use <a href=\"../toggle-button/\">ToggleButton</a> instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides a <code>&lt;button&gt;</code> element with <code>aria-pressed</code> for toggle state and <code>aria-label</code> for theme identification, with zero visual styling. The consumer is responsible for all CSS including theme preview swatch, selected state indicator, hover effects, and focus styling.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.theme-select-button</code> class for targeting. No default styles are included -- this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;button&gt;</code> element with class <code>theme-select-button</code></li>\n<li>Verify <code>aria-pressed</code> reflects the selected state</li>\n<li>Verify <code>aria-label</code> is applied</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Show a visual preview of each theme (color swatches, mini layout). Clearly indicate the selected theme with a visual marker.</li>\n<li><strong>Developers</strong>: Manage the pressed state from the parent ThemeSelect component. Use <code>aria-pressed</code> to communicate selection state.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>ThemeSelectButton is used within a ThemeSelect component following the Picker/PickerButton pattern.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>theme-select</code> — a picker for selecting a visual theme</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/HTML/Element/button\">MDN button element</a></li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<button class=\"theme-select-button\" role=\"radio\">Light</button>";
  const svelteSource: string = "// In your Svelte component:\nimport ThemeSelectButton from \"lily-design-system-svelte-headless/components/ThemeSelectButton/ThemeSelectButton.svelte\";\n\n<ThemeSelectButton>ThemeSelectButton</ThemeSelectButton>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>ThemeSelectButton — Lily Design System</title>
  <meta name="description" content="A theme picker button is a button for selecting a visual theme within a ThemeSelect component. Each button represents one theme option and shows a preview or label for that theme." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

