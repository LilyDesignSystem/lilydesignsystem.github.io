<script lang="ts">
  const html: string = "<h1>Slide-Out Drawer</h1>\n\n<p>A slide-out drawer is a UI/UX component that slides in from the side of the screen -- usually from the left or right -- to reveal additional content or navigation options without leaving the current page. Often triggered by a button or swipe gesture, it is commonly used for menus, filters, settings, or secondary content in mobile and web applications. This component allows for efficient use of screen space by keeping less frequently used options hidden until needed, enhancing focus and minimizing clutter in the main interface while maintaining easy access to extended functionality.</p>\n\n<p>This component renders as a <code>&lt;div&gt;</code> with <code>role=\"dialog\"</code>, <code>aria-label</code>, and <code>aria-modal=\"true\"</code> when open, and is completely removed from the DOM when closed. The <code>open</code> prop is two-way binding, and the Escape key closes the drawer.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as <code>&lt;div role=\"dialog\" aria-label={label} aria-modal=\"true\"&gt;</code> when open</li>\n<li>Conditionally rendered with conditional rendering -- not in the DOM when closed</li>\n<li>The <code>open</code> prop is two-way binding</li>\n<li>Escape key closes the drawer via a keydown handler</li>\n<li><code>tabindex=\"-1\"</code> allows the dialog div to receive focus programmatically</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible label for the dialog</li>\n<li><code>open</code>: boolean (default: false) -- whether the drawer is visible, bindable</li>\n<li><code>children</code>: slot (required) -- drawer content</li>\n<li><code>...restProps</code>: Any additional HTML attributes spread onto the <code>&lt;div&gt;</code></li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;button onclick={() =&gt; (isOpen = true)}&gt;Open menu&lt;/button&gt;\n\n&lt;SlideOutDrawer label=\"Navigation menu\" open={isOpen}&gt;\n  &lt;nav&gt;\n    &lt;ul&gt;\n      &lt;li&gt;&lt;a href=\"/\"&gt;Home&lt;/a&gt;&lt;/li&gt;\n      &lt;li&gt;&lt;a href=\"/about\"&gt;About&lt;/a&gt;&lt;/li&gt;\n      &lt;li&gt;&lt;a href=\"/contact\"&gt;Contact&lt;/a&gt;&lt;/li&gt;\n    &lt;/ul&gt;\n  &lt;/nav&gt;\n  &lt;button onclick={() =&gt; (isOpen = false)}&gt;Close&lt;/button&gt;\n&lt;/SlideOutDrawer&gt;</code></pre>\n\n<p>Settings panel:</p>\n\n<pre><code class=\"language-html\">&lt;button onclick={() =&gt; (settingsOpen = true)}&gt;Settings&lt;/button&gt;\n\n&lt;SlideOutDrawer label=\"Settings panel\" open={settingsOpen}&gt;\n  &lt;h2&gt;Settings&lt;/h2&gt;\n  &lt;form&gt;\n    &lt;label&gt;\n      &lt;input type=\"checkbox\" /&gt; Enable notifications\n    &lt;/label&gt;\n  &lt;/form&gt;\n&lt;/SlideOutDrawer&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Escape closes the drawer</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"dialog\"</code> for modal dialog semantics</li>\n<li><code>aria-label</code> from the label prop provides the accessible name</li>\n<li><code>aria-modal=\"true\"</code> indicates modal behavior (content behind the drawer is inert)</li>\n<li><code>tabindex=\"-1\"</code> for programmatic focus management</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for a drawer that specifically slides out from the side of the page.</li>\n<li>Use for menus, filters, settings, or secondary content that should slide in from the side without navigating away.</li>\n<li>Use on mobile and responsive layouts to conserve screen space by hiding infrequently used options.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when a standard Drawer with multi-directional support is sufficient -- use Drawer instead.</li>\n<li>Do not use for bottom sheets on mobile -- use Sheet instead.</li>\n<li>Do not use for primary content or critical actions that users should see without extra interaction -- use Dialog or AlertDialog instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a modal dialog container with <code>role=\"dialog\"</code>, <code>aria-modal=\"true\"</code>, <code>aria-label</code>, Escape key dismissal, and <code>tabindex=\"-1\"</code> for programmatic focus. The consumer provides all visual styling including slide-in animations, backdrop overlay, width, and positioning.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.slide-out-drawer</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>slide-out-drawer</code></li>\n<li>Verify role=\"dialog\"` for modal dialog semantics</li>\n<li>Verify aria-label` from the label prop provides the accessible name</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Include a visible close button inside the drawer and use a semi-transparent backdrop to indicate the drawer is an overlay. Animate the slide transition to reinforce direction.</li>\n<li><strong>Developers</strong>: Implement focus trapping within the drawer so keyboard users cannot interact with content behind it. Restore focus to the trigger element when the drawer closes.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>drawer</code> — a panel that slides in from the edge of the screen</li>\n<li><code>sheet</code> — a panel that slides in from a screen edge as an overlay</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Dialog Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/</li>\n<li>WAI-ARIA dialog role: https://www.w3.org/TR/wai-aria-1.2/#dialog</li>\n</ul>";
</script>

<svelte:head>
  <title>Slide-Out Drawer — Lily Design System</title>
  <meta name="description" content="A slide-out drawer is a UI/UX component that slides in from the side of the screen -- usually from the left or right -- to reveal additional content or navigation options without leaving the curren..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
