<script lang="ts">
  const html: string = "<h1>Alert</h1>\n\n<p>An alert is a UI/UX component designed to capture the user's attention and convey important information, such as warnings, errors, confirmations, or status updates. Alerts interrupt the user's workflow to ensure visibility and are commonly used for critical feedback or decision points -- such as confirming deletion or notifying of failed actions. They should be clear, concise, and appropriately styled (e.g., color-coded for severity by the consumer) to avoid confusion or unnecessary disruption.</p>\n\n<p>This component renders as a <code>&lt;div&gt;</code> with ARIA live region attributes for automatic screen reader announcements. It supports different severity types (info, success, warning, error) exposed via a <code>data-type</code> attribute that consumers can use for styling. An optional heading is rendered with <code>&lt;strong&gt;</code> emphasis without assuming a heading level.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses <code>role=\"alert\"</code> with <code>aria-live=\"assertive\"</code> by default for immediate screen reader announcements</li>\n<li>Supports <code>role=\"status\"</code> with <code>aria-live=\"polite\"</code> for less urgent messages</li>\n<li><code>aria-atomic=\"true\"</code> ensures the entire alert is re-read when content changes</li>\n<li><code>data-type</code> attribute exposes severity type for CSS styling by consumers</li>\n<li>Heading rendered as <code>&lt;p&gt;&lt;strong&gt;</code> for semantic emphasis without assuming heading level</li>\n<li>The <code>live</code> prop can override the default aria-live value derived from the role</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>type</code>: \"info\" | \"success\" | \"warning\" | \"error\" (default: \"info\") -- severity type</li>\n<li><code>heading</code>: string (optional) -- heading text for the alert</li>\n<li><code>role</code>: \"alert\" | \"status\" (default: \"alert\") -- ARIA role</li>\n<li><code>live</code>: \"assertive\" | \"polite\" | \"off\" (optional) -- override for aria-live politeness</li>\n<li><code>children</code>: slot (required) -- the alert content</li>\n<li><code>...restProps</code>: Any additional HTML attributes spread onto the <code>&lt;div&gt;</code></li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Session expiry warning:</p>\n\n<pre><code class=\"language-html\">&lt;Alert type=\"warning\" heading=\"Session expiring\"&gt;\n  Your session will expire in 5 minutes. Save your work to avoid losing changes.\n&lt;/Alert&gt;</code></pre>\n\n<p>Form submission confirmation:</p>\n\n<pre><code class=\"language-html\">&lt;Alert type=\"success\" heading=\"Appointment confirmed\"&gt;\n  Your appointment for 14 March 2026 at 10:00 has been booked successfully.\n&lt;/Alert&gt;</code></pre>\n\n<p>Error feedback after a failed action:</p>\n\n<pre><code class=\"language-html\">&lt;Alert type=\"error\" heading=\"Payment failed\"&gt;\n  We could not process your payment. Check your card details and try again.\n&lt;/Alert&gt;</code></pre>\n\n<p>Polite status update using <code>role=\"status\"</code>:</p>\n\n<pre><code class=\"language-html\">&lt;Alert role=\"status\"&gt;3 of 12 records loaded.&lt;/Alert&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>None -- alerts are passive announcements, not interactive elements</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"alert\"</code> or <code>role=\"status\"</code> for live region semantics</li>\n<li><code>aria-live=\"assertive\"</code> (default for alert role) or <code>aria-live=\"polite\"</code> (default for status role)</li>\n<li><code>aria-atomic=\"true\"</code> ensures the entire region is re-announced on any change</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for important status messages that need to be announced to screen readers via <code>role=\"alert\"</code>, such as form submission confirmations or system status changes.</li>\n<li>Use for time-sensitive information such as session expiry warnings or connectivity status updates.</li>\n<li>Use when the message does not require user interaction -- it is informational only and dismisses itself or persists passively.</li>\n<li>Use <code>role=\"status\"</code> with <code>aria-live=\"polite\"</code> for lower-priority updates that should not interrupt the user's current task.</li>\n<li>Use the <code>type</code> prop to communicate severity (info, success, warning, error) through <code>data-type</code> for consumer styling.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for messages requiring user acknowledgment before continuing -- use AlertDialog instead.</li>\n<li>Do not use for page-level service announcements or banners that span the full width -- use Banner instead.</li>\n<li>Do not use for inline form field validation errors -- use ErrorMessage and ErrorSummary instead.</li>\n<li>Do not use for persistent warnings within content pages -- use WarningCallout instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides ARIA live region semantics (<code>role=\"alert\"</code> or <code>role=\"status\"</code>), <code>aria-atomic</code>, and a <code>data-type</code> attribute for severity variants, with zero visual styling. The consumer is responsible for all CSS including background colors, border styles, icons, typography, and spacing for each severity level.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.alert</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>alert</code></li>\n<li>Verify role=\"alert\"<code> or </code>role=\"status\"` for live region semantics</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use consistent color coding across severity types (info, success, warning, error) and pair colors with icons so that meaning is not conveyed by color alone.</li>\n<li><strong>Developers</strong>: Use <code>role=\"alert\"</code> with <code>aria-live=\"assertive\"</code> for critical messages and <code>role=\"status\"</code> with <code>aria-live=\"polite\"</code> for non-urgent updates to avoid disrupting the user unnecessarily.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>alert-dialog</code> — a modal dialog for urgent messages requiring user acknowledgment</li>\n<li><code>banner</code> — a prominent message bar across the top of a page</li>\n<li><code>notification</code> — a brief message about an event or update</li>\n<li><code>warning-callout</code> — a callout box highlighting a warning message</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Alert Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/alert/</li>\n<li>WAI-ARIA alert role: https://www.w3.org/TR/wai-aria-1.2/#alert</li>\n<li>WAI-ARIA status role: https://www.w3.org/TR/wai-aria-1.2/#status</li>\n</ul>";
</script>

<svelte:head>
  <title>Alert — Lily Design System</title>
  <meta name="description" content="An alert is a UI/UX component designed to capture the user's attention and convey important information, such as warnings, errors, confirmations, or status updates. Alerts interrupt the user's work..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
