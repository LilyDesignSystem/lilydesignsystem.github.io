<script lang="ts">
  const html: string = "<h1>Context Menu Item</h1>\n\n<p>A context menu item is a UI/UX component representing a single action or option within a context menu (also known as a right-click menu). Context menus appear in response to a secondary interaction (typically right-click on desktop or long-press on mobile) and provide contextually relevant actions for the element that was interacted with.</p>\n\n<p>This component renders as a div with <code>role=\"menuitem\"</code> and <code>tabindex=\"-1\"</code>, following the WAI-ARIA menu pattern. The <code>tabindex=\"-1\"</code> removes the item from the normal tab order so that the parent menu can manage focus programmatically using Arrow keys. The consumer provides the menu item content (action label, keyboard shortcut hint, etc.) through the children slot. Context menu items should be placed within a container with <code>role=\"menu\"</code>.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as <code>&lt;div role=\"menuitem\" tabindex=\"-1\"&gt;</code> for context menu items</li>\n<li><code>tabindex=\"-1\"</code> supports roving focus managed by the parent menu</li>\n<li>Should be placed inside a <code>role=\"menu\"</code> container</li>\n<li>Focus management (ArrowUp/ArrowDown) is handled by the parent menu component</li>\n<li>All additional HTML attributes are spread onto the div via <code>restProps</code></li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>children</code>: slot (required) -- menu item content (text, icon, shortcut hint, etc.)</li>\n<li><code>...restProps</code>: any -- additional HTML attributes spread onto the div</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Context menu items inside a ContextMenu with click handlers:</p>\n\n<pre><code class=\"language-html\">&lt;ContextMenu label=\"Text actions\" open={open}&gt;\n    &lt;ContextMenuItem onclick={() =&gt; handleCut()}&gt;Cut&lt;/ContextMenuItem&gt;\n    &lt;ContextMenuItem onclick={() =&gt; handleCopy()}&gt;Copy&lt;/ContextMenuItem&gt;\n    &lt;ContextMenuItem onclick={() =&gt; handlePaste()}&gt;Paste&lt;/ContextMenuItem&gt;\n    &lt;ContextMenuItem aria-disabled=\"true\"&gt;Delete&lt;/ContextMenuItem&gt;\n&lt;/ContextMenu&gt;</code></pre>\n\n<p>Context menu item with keyboard shortcut hint:</p>\n\n<pre><code class=\"language-html\">&lt;ContextMenuItem aria-keyshortcuts=\"Control+C\" onclick={() =&gt; handleCopy()}&gt;\n    Copy\n&lt;/ContextMenuItem&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Focus is managed by the parent menu using ArrowUp/ArrowDown</li>\n<li>Enter or Space activates the menu item (handled by consumer event listeners)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"menuitem\"</code> -- identifies this element as an item within a menu</li>\n<li><code>tabindex=\"-1\"</code> -- supports roving focus management by the parent menu</li>\n<li>Supports <code>aria-disabled</code> for disabled state (added by consumer)</li>\n<li>Supports <code>aria-keyshortcuts</code> for keyboard shortcut hints (added by consumer)</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside ContextMenu for one action.</li>\n<li>Use for each action or option within a context menu, such as cut, copy, paste, or delete.</li>\n<li>Use when the action needs keyboard shortcut hints or icon labels alongside the action text.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside ContextMenu -- it requires a parent with <code>role=\"menu\"</code> for proper ARIA semantics.</li>\n<li>Do not use for regular menu items -- use MenuItem inside Menu instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless ContextMenuItem component provides a <code>&lt;div&gt;</code> with <code>role=\"menuitem\"</code> and <code>tabindex=\"-1\"</code> for roving focus managed by the parent ContextMenu. The consumer provides all visual styling including text layout, icon placement, keyboard shortcut hints, hover/focus states, and disabled appearance.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.context-menu-item</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>context-menu-item</code></li>\n<li>Verify role=\"menuitem\"` -- identifies this element as an item within a menu</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Show keyboard shortcut hints right-aligned within each menu item for discoverability. Use a disabled visual state for unavailable actions.</li>\n<li><strong>Developers</strong>: Add <code>aria-disabled=\"true\"</code> for disabled items and <code>aria-keyshortcuts</code> for keyboard shortcut hints. Focus management is handled by the parent ContextMenu.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>ContextMenuItem is the child component in the ContextMenu/ContextMenuItem composition pattern. Place ContextMenuItem components inside a ContextMenu. Each item receives focus programmatically via the parent's ArrowUp/ArrowDown keyboard navigation, so set <code>tabindex=\"-1\"</code> to support roving focus.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>context-menu</code> — a menu that appears on right-click or long-press</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Menu Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/menu/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"context-menu-item\" role=\"menuitem\">Menu action</div>";
  const svelteSource: string = "// In your Svelte component:\nimport ContextMenuItem from \"lily-design-system-svelte-headless/components/ContextMenuItem/ContextMenuItem.svelte\";\n\n<ContextMenuItem>\n  <!-- content -->\n</ContextMenuItem>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Context Menu Item — Lily Design System</title>
  <meta name="description" content="A context menu item is a UI/UX component representing a single action or option within a context menu (also known as a right-click menu). Context menus appear in response to a secondary interaction..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

