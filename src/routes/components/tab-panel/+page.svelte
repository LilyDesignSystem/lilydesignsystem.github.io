<script lang="ts">
  const html: string = "<h1>Tab Panel</h1>\n\n<p>A tab panel is a content panel associated with a tab in a tab bar. It shows the content for the currently selected tab.</p>\n\n<p>Use it as the content companion to a <code>TabBar</code> and <code>TabBarButton</code>. Each panel is labelled by its corresponding tab and is shown only when its tab is selected.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses a <code>&lt;div&gt;</code> with <code>role=\"tabpanel\"</code> and class <code>tab-panel</code></li>\n<li>When <code>labelledBy</code> is provided, the panel uses <code>aria-labelledby={labelledBy}</code> and omits <code>aria-label</code></li>\n<li>When <code>labelledBy</code> is not provided, the panel uses <code>aria-label={label}</code> instead</li>\n<li>The panel is hidden via the <code>hidden</code> attribute when <code>selected</code> is <code>false</code></li>\n<li>The panel uses <code>tabindex=\"0\"</code> so keyboard users can scroll the panel content</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (<strong>required</strong>) -- aria-label for the panel</li>\n<li><code>labelledBy</code>: string (optional) -- ID of the tab button that controls this panel; when provided, sets <code>aria-labelledby</code> and the panel does not set <code>aria-label</code></li>\n<li><code>selected</code>: boolean (default <code>false</code>) -- whether the panel is the currently selected one</li>\n<li><code>children</code>: slot -- panel content</li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;TabBar&gt;\n  &lt;TabBarButton id=\"tab-overview\" controls=\"panel-overview\" selected&gt;Overview&lt;/TabBarButton&gt;\n  &lt;TabBarButton id=\"tab-details\" controls=\"panel-details\"&gt;Details&lt;/TabBarButton&gt;\n&lt;/TabBar&gt;\n&lt;TabPanel id=\"panel-overview\" labelledBy=\"tab-overview\" selected={tab === 'overview'}&gt;\n  Overview content...\n&lt;/TabPanel&gt;\n&lt;TabPanel id=\"panel-details\" labelledBy=\"tab-details\" selected={tab === 'details'}&gt;\n  Details content...\n&lt;/TabPanel&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Move focus into and out of the panel</li>\n<li>(Provided by the parent <code>TabBar</code>) Arrow keys to navigate between tabs</li>\n<li>Within the panel, the consumer provides any additional keyboard handling</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"tabpanel\"</code> on the container</li>\n<li><code>aria-labelledby={labelledBy}</code> when controlled by a tab; otherwise <code>aria-label={label}</code></li>\n<li><code>hidden</code> reflects <code>!selected</code></li>\n<li><code>tabindex=\"0\"</code> so the panel itself is keyboard-focusable</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use as the content companion to a <code>TabBar</code></li>\n<li>Use to switch among related views without navigating to a new page</li>\n<li>Use when only one view is shown at a time</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use without a corresponding tab in a <code>TabBar</code></li>\n<li>Do not show multiple selected panels at once</li>\n<li>Do not use for primary navigation between pages — use <code>NavigationMenu</code></li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>Tab panels pair with tabs:</p>\n\n<pre><code class=\"language-html\">&lt;TabBar label=\"Sections\"&gt;\n  &lt;TabBarButton id=\"t1\" controls=\"p1\" selected&gt;One&lt;/TabBarButton&gt;\n  &lt;TabBarButton id=\"t2\" controls=\"p2\"&gt;Two&lt;/TabBarButton&gt;\n&lt;/TabBar&gt;\n&lt;TabPanel id=\"p1\" labelledBy=\"t1\" selected&gt;...&lt;/TabPanel&gt;\n&lt;TabPanel id=\"p2\" labelledBy=\"t2\"&gt;...&lt;/TabPanel&gt;</code></pre>\n\n<h2>Headless</h2>\n\n<p>Renders a <code>&lt;div role=\"tabpanel\"&gt;</code> linked to its tab via <code>aria-labelledby</code>. Selection state is controlled by the consumer via the <code>selected</code> prop; animation, lazy loading, and styling are entirely the consumer's responsibility.</p>\n\n<h2>Styles</h2>\n\n<p>Consumer CSS targets the <code>tab-panel</code> class. Use the <code>hidden</code> attribute selector or <code>[aria-selected]</code> on the parent tab to style transitions. Provide a clear focus indicator on the panel since it is keyboard-focusable.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> with <code>role=\"tabpanel\"</code> and class <code>tab-panel</code></li>\n<li>Verify <code>tabindex=\"0\"</code> is present</li>\n<li>Verify <code>aria-labelledby</code> is set and <code>aria-label</code> is absent when <code>labelledBy</code> is provided</li>\n<li>Verify <code>aria-label</code> is set and <code>aria-labelledby</code> is absent when <code>labelledBy</code> is not provided</li>\n<li>Verify <code>hidden</code> is set when <code>selected</code> is <code>false</code></li>\n<li>Verify <code>hidden</code> is absent when <code>selected</code> is <code>true</code></li>\n<li>Verify children are rendered inside the panel</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Keep tab labels short; communicate the selection clearly with both color and a non-color indicator (underline, weight).</li>\n<li><strong>Developers</strong>: Use stable ids on tabs and panels so <code>aria-labelledby</code> and <code>aria-controls</code> survive re-renders.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>tab-bar</code> — a group of tabs for switching between content panels</li>\n<li><code>tab-bar-button</code> — one tab button in a tab group</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Tabs Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/tabs/</li>\n<li>Adobe Spectrum Tabs: https://spectrum.adobe.com/page/tabs/</li>\n<li>WCAG 1.3.1 Info and Relationships: https://www.w3.org/WAI/WCAG22/Understanding/info-and-relationships</li>\n</ul>";
</script>

<svelte:head>
  <title>Tab Panel — Lily Design System</title>
  <meta name="description" content="A tab panel is a content panel associated with a tab in a tab bar. It shows the content for the currently selected tab." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
