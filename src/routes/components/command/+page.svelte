<script lang="ts">
  const html: string = "<h1>Command</h1>\n\n<p>A command palette provides a searchable list of actions or items. Users type into a search input to filter and select commands via keyboard or mouse. This pattern is commonly used for quick-access command palettes (e.g., Ctrl+K interfaces), searchable menus, and action launchers.</p>\n\n<p>The component renders a search region containing a text input and a listbox for results. The search value is bindable, allowing consumers to implement their own filtering logic and dynamically update the listbox content.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Wraps content in a <code>&lt;div&gt;</code> with <code>role=\"search\"</code> and <code>aria-label</code> for semantic search region</li>\n<li>Contains an <code>&lt;input type=\"search\"&gt;</code> with <code>autocomplete=\"off\"</code> to prevent browser autocomplete interference</li>\n<li>Contains a <code>&lt;div&gt;</code> with <code>role=\"listbox\"</code> for displaying filtered results</li>\n<li>Supports two-way binding on the <code>value</code> prop component</li>\n<li>Children slot is rendered inside the listbox for consumer-controlled list items</li>\n<li>Spreads <code>restProps</code> on the outer search container</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name for both the search region and the input</li>\n<li><code>placeholder</code>: string (default: <code>undefined</code>) -- placeholder text for the search input</li>\n<li><code>value</code>: string (default: <code>\"\"</code>, bindable) -- current search text, supports two-way binding via two-way <code>value</code> binding</li>\n<li><code>children</code>: slot (required) -- listbox content, typically option or command items</li>\n<li><code>...restProps</code>: any additional HTML attributes spread onto the outer <code>&lt;div&gt;</code></li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Application command palette triggered by Ctrl+K:</p>\n\n<pre><code class=\"language-html\">&lt;Command label=\"Command palette\" placeholder=\"Type a command...\" value={query}&gt;\n    &lt;div role=\"option\" tabindex=\"-1\" onclick={() =&gt; navigate('/dashboard')}&gt;Go to Dashboard&lt;/div&gt;\n    &lt;div role=\"option\" tabindex=\"-1\" onclick={() =&gt; navigate('/settings')}&gt;Open Settings&lt;/div&gt;\n    &lt;div role=\"option\" tabindex=\"-1\" onclick={() =&gt; toggleTheme()}&gt;Toggle Dark Mode&lt;/div&gt;\n&lt;/Command&gt;</code></pre>\n\n<p>Admin panel action launcher with dynamic filtering:</p>\n\n<pre><code class=\"language-html\">&lt;Command label=\"Actions\" placeholder=\"Search actions...\" value={actionQuery}&gt;\n    {#each filteredActions as action}\n        &lt;div role=\"option\" tabindex=\"-1\" onclick={() =&gt; action.execute()}&gt;\n            {action.name} &lt;kbd&gt;{action.shortcut}&lt;/kbd&gt;\n        &lt;/div&gt;\n    {/each}\n&lt;/Command&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li><strong>Tab</strong>: Moves focus into and out of the search input</li>\n<li>Additional keyboard navigation (arrow keys within the listbox) should be implemented by the consumer</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"search\"</code> -- identifies the outer container as a search landmark region</li>\n<li><code>aria-label={label}</code> -- provides accessible name for the search region and input</li>\n<li><code>role=\"listbox\"</code> -- identifies the results container as a listbox for selectable options</li>\n<li><code>aria-label={label}</code> -- provides accessible name for the listbox</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use Command for quick-access search interfaces such as Ctrl+K action launchers or searchable menus.</li>\n<li>Use Command when users need to search and execute actions from a large set without navigating through menus.</li>\n<li>Use Command for keyboard-driven navigation where users can jump to pages, run actions, or switch contexts.</li>\n<li>Use Command in developer tools, admin panels, or productivity applications where power users expect a command palette.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use Command for site search or content search -- use SearchInput instead.</li>\n<li>Do not use Command for form input with a dropdown list of options -- use Combobox instead.</li>\n<li>Do not use Command for navigation menus that should always be visible -- use NavigationMenu instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless Command component provides a <code>&lt;div&gt;</code> with <code>role=\"search\"</code> and <code>aria-label</code>, an <code>&lt;input type=\"search\"&gt;</code> with <code>autocomplete=\"off\"</code>, and a <code>&lt;div&gt;</code> with <code>role=\"listbox\"</code> for displaying results. The consumer provides all visual styling including modal/overlay presentation, result item layout, keyboard shortcut hints, grouping headers, and animations.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.command</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>command</code></li>\n<li>Verify role=\"search\"` -- identifies the outer container as a search landmark region</li>\n<li>Verify aria-label={label}` -- provides accessible name for the search region and input</li>\n<li>Verify role=\"listbox\"` -- identifies the results container as a listbox for selectable options</li>\n<li>Verify aria-label={label}` -- provides accessible name for the listbox</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Present the command palette in a modal overlay centered on screen. Group related commands and show keyboard shortcut hints alongside each action.</li>\n<li><strong>Developers</strong>: Implement filtering logic by reacting to the bindable <code>value</code> prop. Add arrow key navigation for the listbox items in your consumer code. Pair with a Dialog for modal presentation.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>combobox</code> — a text input combined with a dropdown list for filtering options</li>\n<li><code>menu</code> — a list of actions or options triggered by a button</li>\n<li><code>search-input</code> — an input for entering a search query &lt;input type=\"search\"&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Listbox Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/listbox/</li>\n<li>WAI-ARIA Combobox Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/combobox/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"command\" role=\"dialog\" aria-label=\"Command palette\"><input type=\"search\" placeholder=\"Type a command...\" /><ul role=\"listbox\"><li role=\"option\">Open file</li><li role=\"option\">Save</li></ul></div>";
  const svelteSource: string = "// In your Svelte component:\nimport Command from \"lily-design-system-svelte-headless/components/Command/Command.svelte\";\n\n<Command>\n  <!-- content -->\n</Command>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Command — Lily Design System</title>
  <meta name="description" content="A command palette provides a searchable list of actions or items. Users type into a search input to filter and select commands via keyboard or mouse. This pattern is commonly used for quick-access ..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

