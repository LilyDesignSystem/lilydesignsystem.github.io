<script lang="ts">
  const html: string = "<h1>Select</h1>\n\n<p>A select is a headless component that wraps the native HTML <code>&lt;select&gt;</code> element, providing a dropdown menu for choosing one option from a list. It is commonly used in forms for selecting a country, category, status, or any other value from a predefined set of choices.</p>\n\n<p>The consumer provides <code>&lt;option&gt;</code> elements as children, giving full control over the available choices. The component uses <code>aria-label</code> for accessible naming and supports two-way binding on the selected value.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a single <code>&lt;select&gt;</code> element containing consumer-provided <code>&lt;option&gt;</code> children</li>\n<li>Uses <code>aria-label</code> for accessible naming instead of a visible <code>&lt;label&gt;</code> element</li>\n<li>Supports two-way binding on the <code>value</code> prop</li>\n<li>Uses slot for the <code>children</code> prop to render <code>&lt;option&gt;</code> elements</li>\n<li>Spreads <code>restProps</code> onto the select element for consumer extensibility</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name applied via <code>aria-label</code></li>\n<li><code>value</code>: string (default: \"\") -- bindable string representing the currently selected value</li>\n<li><code>required</code>: boolean (default: false) -- whether the select is required for form submission</li>\n<li><code>disabled</code>: boolean (default: false) -- whether the select is disabled</li>\n<li><code>children</code>: slot (required) -- <code>&lt;option&gt;</code> elements to render inside the select</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the select element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;Field label=\"Sort by\"&gt;\n  &lt;Select label=\"Sort by\"&gt;\n    &lt;Option value=\"recently-published\" selected&gt;Recently published&lt;/Option&gt;\n    &lt;Option value=\"recently-updated\"&gt;Recently updated&lt;/Option&gt;\n    &lt;Option value=\"most-views\"&gt;Most views&lt;/Option&gt;\n  &lt;/Select&gt;\n&lt;/Field&gt;\n\n&lt;Field label=\"Choose your region\"&gt;\n  &lt;Hint&gt;This can be different to what you selected before&lt;/Hint&gt;\n  &lt;Select label=\"Choose your region\"&gt;\n    &lt;Option value=\"\"&gt;Choose region&lt;/Option&gt;\n    &lt;Option value=\"east\"&gt;East of England&lt;/Option&gt;\n    &lt;Option value=\"london\"&gt;London&lt;/Option&gt;\n    &lt;Option value=\"midlands\"&gt;Midlands&lt;/Option&gt;\n    &lt;Option value=\"northwest\"&gt;North West&lt;/Option&gt;\n    &lt;Option value=\"southeast\"&gt;South East&lt;/Option&gt;\n  &lt;/Select&gt;\n&lt;/Field&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Moves focus to and from the select element (native browser behavior)</li>\n<li>Space/Enter: Opens the dropdown list when focused (native browser behavior)</li>\n<li>Arrow Up/Down: Navigates through options (native browser behavior)</li>\n<li>Escape: Closes the dropdown list (native browser behavior)</li>\n<li>Home/End: Jumps to first/last option (native browser behavior)</li>\n<li>Type-ahead: Typing characters jumps to matching options (native browser behavior)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides an accessible name for the select element since there is no visible <code>&lt;label&gt;</code> element</li>\n<li>The <code>&lt;select&gt;</code> element implicitly has <code>role=\"combobox\"</code> or <code>role=\"listbox\"</code> depending on the browser, with built-in accessible semantics</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use as a last resort when RadioGroup is not practical due to a very long list of options</li>\n<li>Use for settings where a sensible default can be pre-selected, such as a sort order or display preference</li>\n<li>Use when screen space is limited and a compact dropdown is needed</li>\n<li>Use with a descriptive label and optional Hint to help users understand the available choices</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when there are fewer than 7 options -- use RadioGroup with RadioInput instead, which is easier to use</li>\n<li>Do not use the multiple selection attribute -- use CheckboxGroup with CheckboxInput for multi-select</li>\n<li>Do not pre-select an option for question-based inputs -- this can bias user responses</li>\n<li>Do not use when users need to search or filter options -- use Combobox or SelectWithExtras instead</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>The Select headless component provides a native <code>&lt;select&gt;</code> element with <code>aria-label</code> for accessible naming and two-way value binding. Consumer-provided <code>&lt;option&gt;</code> children define the available choices. The browser handles all keyboard navigation and dropdown behavior. The consumer provides all visual styling.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.select</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;select&gt;</code> element with class <code>select</code></li>\n<li>Verify aria-label={label}<code> -- provides an accessible name for the select element since there is no visible </code>&lt;label&gt;` element</li>\n<li>Verify The <code>&lt;select&gt;</code> element implicitly has <code>role=\"combobox\"</code> or <code>role=\"listbox\"</code> depending on the browser, with built-in accessible semantics</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Include a placeholder option (e.g., \"Select a country...\") as the first item to guide users, and avoid overly long option lists that are hard to scan.</li>\n<li><strong>Developers</strong>: Set the placeholder option's <code>value</code> to an empty string and combine with the <code>required</code> prop to enforce selection, ensuring form validation works correctly.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>select-with-extras</code> — a select dropdown with additional features like search or groups</li>\n<li><code>option</code> — an option element within a select dropdown</li>\n<li><code>combobox</code> — a text input combined with a dropdown list for filtering options</li>\n<li><code>theme-select</code> — a select dropdown for choosing a theme</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN select element: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/select</li>\n</ul>";
</script>

<svelte:head>
  <title>Select — Lily Design System</title>
  <meta name="description" content="A select is a headless component that wraps the native HTML &lt;select&gt; element, providing a dropdown menu for choosing one option from a list. It is commonly used in forms for selecting a country, ca..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
