<script lang="ts">
  const html: string = "<h1>Banner</h1>\n\n<p>A banner is a prominent component used to display important messages, updates, or calls to action at the top or bottom of an interface. Often spanning the full width of the screen, banners are designed to catch the user's attention without fully interrupting their experience. They are commonly used for announcements, cookie consent, warnings, promotions, or system alerts.</p>\n\n<p>Banners may be persistent or dismissible, depending on the message's importance. This component supports four semantic variants (info, success, warning, error) exposed via a <code>data-type</code> attribute, and an optional dismiss button that hides the banner and invokes a callback. The <code>aria-live=\"polite\"</code> region ensures screen readers announce banner content without interrupting the user.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as <code>&lt;div role=\"region\" aria-live=\"polite\"&gt;</code> for screen reader announcements</li>\n<li>Uses reactive state rune to track visibility; dismissing sets <code>visible</code> to <code>false</code></li>\n<li>Dismissible banners render a <code>&lt;button type=\"button\"&gt;</code> with a configurable <code>aria-label</code></li>\n<li><code>data-type</code> attribute exposes the variant (info, success, warning, error) for consumer CSS</li>\n<li>The <code>dismiss()</code> function sets visibility to false and calls the <code>onclose</code> callback</li>\n<li>Uses <code>children</code> slot for flexible banner content rendering</li>\n<li>Spreads <code>...restProps</code> on the root <code>&lt;div&gt;</code> element for consumer extensibility</li>\n<li>Entire banner is conditionally rendered with conditional rendering</li>\n<li>No hardcoded user-facing strings; all text comes through props and children</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>type</code>: <code>\"info\"</code> | <code>\"success\"</code> | <code>\"warning\"</code> | <code>\"error\"</code> (default: <code>\"info\"</code>) -- banner variant for semantic meaning, exposed as <code>data-type</code></li>\n<li><code>dismissible</code>: boolean (default: <code>false</code>) -- whether the banner can be dismissed</li>\n<li><code>onclose</code>: <code>() =&gt; void</code> (optional) -- callback invoked when the banner is dismissed</li>\n<li><code>closeLabel</code>: string (optional) -- accessible label for the dismiss button via <code>aria-label</code></li>\n<li><code>children</code>: slot (required) -- the banner content</li>\n<li><code>...restProps</code>: any additional HTML attributes spread onto the root <code>&lt;div&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;!-- Information banner for a service-wide delay --&gt;\n&lt;Banner type=\"info\"&gt;\n  &lt;p&gt;You have 7 days left to send your application.\n  &lt;a href=\"/applications\"&gt;View application&lt;/a&gt;.&lt;/p&gt;\n&lt;/Banner&gt;\n\n&lt;!-- Success banner confirming a completed action --&gt;\n&lt;Banner type=\"success\"&gt;\n  &lt;p&gt;Patient record for Sarah Mitchell has been updated.&lt;/p&gt;\n&lt;/Banner&gt;\n\n&lt;!-- Dismissible warning banner with callback --&gt;\n&lt;Banner type=\"warning\" dismissible closeLabel=\"Dismiss warning\" onclose={handleDismiss}&gt;\n  &lt;p&gt;Planned maintenance is scheduled for Saturday 12 April from 09:00 to 13:00.&lt;/p&gt;\n&lt;/Banner&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus the dismiss button (when <code>dismissible</code> is <code>true</code>)</li>\n<li>Enter/Space: Activate the dismiss button to hide the banner</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"region\"</code> -- establishes the banner as a landmark region for assistive technology navigation</li>\n<li><code>aria-live=\"polite\"</code> -- ensures screen readers announce banner content without interrupting the current reading</li>\n<li><code>aria-label={closeLabel}</code> -- provides the accessible name for the dismiss button when present</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to communicate important information that is not directly related to the current page content, such as service-wide delays or planned maintenance</li>\n<li>Use to show a success confirmation after a user completes an action, such as saving or updating a record</li>\n<li>Use sparingly -- evidence shows users often miss banners when overused</li>\n<li>Use at most one banner per page, positioned at the top of the main content area below breadcrumbs and before the h1 heading</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for information directly relevant to the current page task -- use InsetText or WarningCallout instead</li>\n<li>Do not use for form validation errors -- use ErrorMessage and ErrorSummary instead</li>\n<li>Do not use simultaneously with ErrorSummary on the same page</li>\n<li>Do not use for critical health warnings -- use WarningCallout or CareCard instead</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides <code>role=\"region\"</code> with <code>aria-live=\"polite\"</code>, a <code>data-type</code> attribute for semantic variants, optional dismiss button with <code>aria-label</code>, and visibility state management, all with zero visual styling. The consumer is responsible for all CSS including background colors, text styling, dismiss button appearance, full-width layout, and position (sticky, fixed, or static).</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.banner</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>banner</code></li>\n<li>Verify role=\"region\"` -- establishes the banner as a landmark region for assistive technology navigation</li>\n<li>Verify aria-label={closeLabel}` -- provides the accessible name for the dismiss button when present</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Ensure the dismiss button has a minimum 44x44px touch target and is visually distinct. Use color and an icon together so meaning is not conveyed by color alone.</li>\n<li><strong>Developers</strong>: Provide a meaningful <code>closeLabel</code> prop for the dismiss button so screen reader users understand its purpose. Handle the <code>onclose</code> callback to persist dismissal state if needed.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>announcement-banner</code> — a banner highlighting important messages for all users</li>\n<li><code>banner-box</code> — a banner box that is inside a banner component, using flexbox horizontal</li>\n<li><code>government-banner</code> — a banner identifying a website as belonging to a government, with an expandable details panel</li>\n<li><code>medical-banner</code> — a prominent message bar across the top of a page, with medical information</li>\n<li><code>medical-banner-box</code> — a medical banner box that is inside a medical-banner component, using flexbox horizontal, with medical information</li>\n<li><code>medical-banner-box-for-advice</code> — a medical record banner box for advice information e.g. contacts, contexts, plans, etc.</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA banner role: https://www.w3.org/TR/wai-aria-1.2/#banner</li>\n<li>WAI-ARIA alert role (for urgent banners): https://www.w3.org/TR/wai-aria-1.2/#alert</li>\n<li>WAI-ARIA live regions: https://www.w3.org/TR/wai-aria-1.2/#aria-live</li>\n</ul>";
</script>

<svelte:head>
  <title>Banner — Lily Design System</title>
  <meta name="description" content="A banner is a prominent component used to display important messages, updates, or calls to action at the top or bottom of an interface. Often spanning the full width of the screen, banners are desi..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
