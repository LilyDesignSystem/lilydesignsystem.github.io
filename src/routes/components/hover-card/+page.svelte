<script lang="ts">
  const html: string = "<h1>Hover Card</h1>\n\n<p>HoverCard is a headless component that displays supplementary content when triggered, typically by a hover or focus interaction on a trigger element. It conditionally renders a container with <code>role=\"tooltip\"</code> and an accessible label when the <code>open</code> prop is true.</p>\n\n<p>This component is useful for user profile previews, link previews, contextual help, or any popover-style content that should appear on hover or focus. The <code>open</code> prop is bindable, allowing the consumer to control visibility from hover/focus event handlers on a trigger element. Consumers provide the card content as children and manage the show/hide logic externally.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Conditionally renders a <code>&lt;div&gt;</code> with <code>role=\"tooltip\"</code> when <code>open</code> is true</li>\n<li>Uses <code>aria-label</code> for an accessible name describing the card content</li>\n<li>Supports two-way binding on the <code>open</code> prop</li>\n<li>Accepts a <code>children</code> slot for flexible card content</li>\n<li>Spreads <code>restProps</code> onto the container for consumer customization</li>\n<li>The consumer is responsible for managing hover/focus events on the trigger element to toggle <code>open</code></li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name for the hover card via <code>aria-label</code></li>\n<li><code>open</code>: boolean (default: <code>false</code>) -- whether the hover card is currently visible; bindable with two-way <code>open</code> binding</li>\n<li><code>children</code>: slot (required) -- content to display inside the hover card</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the container <code>&lt;div&gt;</code></li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Basic hover card with a trigger button:</p>\n\n<pre><code class=\"language-html\">&lt;button\n  onmouseenter={() =&gt; open = true}\n  onmouseleave={() =&gt; open = false}\n  onfocus={() =&gt; open = true}\n  onblur={() =&gt; open = false}\n&gt;\n  Hover me\n&lt;/button&gt;\n&lt;HoverCard label=\"User info\" open={open}&gt;\n  &lt;p&gt;Profile details here&lt;/p&gt;\n&lt;/HoverCard&gt;</code></pre>\n\n<p>User profile preview on a username link:</p>\n\n<pre><code class=\"language-html\">&lt;a\n  href=\"/users/jdoe\"\n  onmouseenter={() =&gt; profileOpen = true}\n  onmouseleave={() =&gt; profileOpen = false}\n  onfocus={() =&gt; profileOpen = true}\n  onblur={() =&gt; profileOpen = false}\n&gt;\n  @jdoe\n&lt;/a&gt;\n&lt;HoverCard label=\"Profile preview for jdoe\" open={profileOpen}&gt;\n  &lt;Avatar label=\"Jane Doe\"&gt;\n    &lt;AvatarImage src=\"/avatars/jdoe.jpg\" alt=\"Jane Doe\" /&gt;\n  &lt;/Avatar&gt;\n  &lt;strong&gt;Jane Doe&lt;/strong&gt;\n  &lt;p&gt;Software Engineer at Acme Corp&lt;/p&gt;\n  &lt;p&gt;12 projects &amp;middot; 48 contributions&lt;/p&gt;\n&lt;/HoverCard&gt;</code></pre>\n\n<p>Link preview hover card:</p>\n\n<pre><code class=\"language-html\">&lt;a\n  href=\"https://example.com/article\"\n  onmouseenter={() =&gt; linkPreviewOpen = true}\n  onmouseleave={() =&gt; linkPreviewOpen = false}\n  onfocus={() =&gt; linkPreviewOpen = true}\n  onblur={() =&gt; linkPreviewOpen = false}\n&gt;\n  Understanding Accessibility\n&lt;/a&gt;\n&lt;HoverCard label=\"Article preview\" open={linkPreviewOpen}&gt;\n  &lt;Image src=\"/thumbnails/a11y.jpg\" alt=\"Accessibility article thumbnail\" /&gt;\n  &lt;strong&gt;Understanding Accessibility&lt;/strong&gt;\n  &lt;p&gt;A practical guide to building inclusive web applications.&lt;/p&gt;\n&lt;/HoverCard&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None at the component level. The consumer manages focus-based visibility on the trigger element.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"tooltip\"</code> -- identifies the hover card as supplementary descriptive content</li>\n<li><code>aria-label={label}</code> -- provides an accessible name describing the hover card content</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for user profile previews triggered by hovering over a username or avatar.</li>\n<li>Use for link previews that show a summary of the destination page.</li>\n<li>Use for contextual help or supplementary information triggered by hover or focus.</li>\n<li>Use when the content is supplementary and does not require user interaction to dismiss.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for content that requires user interaction such as buttons or forms -- use Popover instead.</li>\n<li>Do not use for simple text descriptions -- use Tooltip instead.</li>\n<li>Do not use as the only way to access content -- hover is not available on touch devices.</li>\n<li>Do not use for essential information the user must see -- place it inline instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;div&gt;</code> with <code>role=\"tooltip\"</code> and <code>aria-label</code> that conditionally renders when <code>open</code> is true. The consumer provides all visual styling including positioning, shadows, borders, arrow indicators, and content layout.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.hover-card</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>hover-card</code></li>\n<li>Verify role=\"tooltip\"` -- identifies the hover card as supplementary descriptive content</li>\n<li>Verify aria-label={label}` -- provides an accessible name describing the hover card content</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Add a slight delay before showing and hiding the card to prevent flickering on accidental hover. Include an arrow pointing to the trigger element.</li>\n<li><strong>Developers</strong>: Manage hover/focus events on the trigger element to toggle <code>open</code>. Consider adding a delay to prevent the card from disappearing when the user moves the mouse between the trigger and the card.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>popover</code> — a floating content box anchored to a trigger element</li>\n<li><code>tooltip</code> — a small popup showing descriptive text on hover or focus</li>\n<li><code>popup</code> — a temporary overlay that appears above page content</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA tooltip role: https://www.w3.org/TR/wai-aria-1.2/#tooltip</li>\n<li>WAI-ARIA Tooltip Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/tooltip/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"hover-card\"><p>Card shown on hover</p></div>";
  const svelteSource: string = "// In your Svelte component:\nimport HoverCard from \"lily-design-system-svelte-headless/components/HoverCard/HoverCard.svelte\";\n\n<HoverCard>\n  <!-- content -->\n</HoverCard>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Hover Card — Lily Design System</title>
  <meta name="description" content="HoverCard is a headless component that displays supplementary content when triggered, typically by a hover or focus interaction on a trigger element. It conditionally renders a container with role=..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

