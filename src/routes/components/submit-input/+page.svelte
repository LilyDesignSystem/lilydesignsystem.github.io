<script lang="ts">
  const html: string = "<h1>Submit Input</h1>\n\n<p>A submit input is a UI/UX component that provides a form submission button using the native HTML <code>&lt;input type=\"submit\"&gt;</code> element. When clicked within a form, it triggers the form's submit event. The component supports custom button text through the value prop and can be disabled to prevent premature submission.</p>\n\n<p>Submit inputs provide a simple, semantic way to add form submission capability that works with the browser's built-in form handling. They are the standard mechanism for triggering form validation and submission in HTML forms.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a single <code>&lt;input type=\"submit\"&gt;</code> element for native form submission behavior</li>\n<li>The browser automatically triggers the containing form's <code>submit</code> event when clicked</li>\n<li>Default button text is \"Submit\", configurable via the <code>value</code> prop</li>\n<li>Supports rest props via <code>...restProps</code> for extensibility (e.g., <code>id</code>, <code>name</code>, <code>class</code>)</li>\n<li>No ARIA attributes needed beyond the native semantics of <code>&lt;input type=\"submit\"&gt;</code></li>\n<li>When used within a form, clicking this input also triggers built-in browser validation</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>value</code>: string (default: \"Submit\") -- the visible button text</li>\n<li><code>disabled</code>: boolean (default: false) -- whether the button is disabled</li>\n<li><code>...restProps</code>: spread onto the <code>&lt;input&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Form with a descriptive submit label:</p>\n\n<pre><code class=\"language-html\">&lt;Form label=\"Contact us\" onsubmit={handleSubmit}&gt;\n  &lt;Field label=\"Message\" required&gt;\n    &lt;TextAreaInput label=\"Message\" value={message} required /&gt;\n  &lt;/Field&gt;\n  &lt;SubmitInput value=\"Send message\" /&gt;\n&lt;/Form&gt;</code></pre>\n\n<p>Disabled submit until form is valid:</p>\n\n<pre><code class=\"language-html\">&lt;SubmitInput value=\"Save changes\" disabled={!formValid} /&gt;</code></pre>\n\n<p>Simple submit with default label:</p>\n\n<pre><code class=\"language-html\">&lt;SubmitInput /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Enter: activates the submit button</li>\n<li>Space: activates the submit button</li>\n</ul>\n\n<p>Note: keyboard interactions are provided natively by the <code>&lt;input type=\"submit\"&gt;</code> element.</p>\n\n<h2>ARIA</h2>\n\n<p>No additional ARIA attributes are needed. The native <code>&lt;input type=\"submit\"&gt;</code> element has an implicit <code>button</code> role and the <code>value</code> attribute serves as its accessible name.</p>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use as a form-native submit button via <code>&lt;input type=\"submit\"&gt;</code>.</li>\n<li>Use when a plain text label is sufficient for the submission action.</li>\n<li>Use as the primary submission trigger inside HTML forms to trigger built-in browser validation.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when you need rich content inside the button (icons, multiple elements) -- use Button with <code>type=\"submit\"</code> instead.</li>\n<li>Do not use for non-form actions -- use Button instead.</li>\n<li>Do not use when the button needs to show a loading spinner or other dynamic content.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component renders a native <code>&lt;input type=\"submit\"&gt;</code> element with configurable <code>value</code> text and <code>disabled</code> state. It provides built-in form submission and validation behavior with no additional ARIA needed. The consumer provides all visual styling including colors, padding, borders, and hover/focus states.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.submit-input</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;input&gt;</code> element with class <code>submit-input</code></li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use a clear, action-oriented label (e.g., \"Save changes\", \"Send message\") rather than generic text like \"Submit\". Visually distinguish the submit button from other form controls.</li>\n<li><strong>Developers</strong>: Disable the submit button while the form is processing to prevent duplicate submissions. Place the SubmitInput at the end of the form for a logical tab order.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>reset-input</code> — a button input that resets a form to default values &lt;input type=\"reset\"&gt;</li>\n<li><code>button-input</code> — an input element of type button for form actions &lt;input type=\"button\"&gt;</li>\n<li><code>button</code> — a generic clickable button element</li>\n<li><code>form</code> — a form element for collecting and submitting user data</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN input type=\"submit\": https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/submit</li>\n</ul>";
</script>

<svelte:head>
  <title>Submit Input — Lily Design System</title>
  <meta name="description" content="A submit input is a UI/UX component that provides a form submission button using the native HTML &lt;input type=&quot;submit&quot;&gt; element. When clicked within a form, it triggers the form's submit event. The ..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
