<script lang="ts">
  const html: string = "<h1>Summary Box</h1>\n\n<p>A summary box is an <code>&lt;aside&gt;</code> callout that highlights key takeaways or next steps from a longer page, with a required heading and any body content.</p>\n\n<p>Use it to surface the most important information from a long article so that scanning users can find the essentials without reading every word.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses an <code>&lt;aside&gt;</code> element so it is announced as a complementary landmark</li>\n<li><code>heading</code> prop is <strong>required</strong> — rendered as an <code>&lt;h3&gt;</code> inside the box</li>\n<li><code>label</code> prop is optional — when omitted it defaults to the heading text</li>\n<li>Children render inside <code>&lt;div class=\"summary-box-body\"&gt;</code></li>\n<li>Pass-through attributes are forwarded to the root <code>&lt;aside&gt;</code></li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>heading</code>: string (<strong>required</strong>) -- box heading text</li>\n<li><code>label</code>: string (optional) -- aria-label override (defaults to heading)</li>\n<li><code>children</code>: slot -- body content</li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Key takeaways summary:</p>\n\n<pre><code class=\"language-html\">&lt;SummaryBox heading=\"Key takeaways\"&gt;\n  &lt;ul&gt;\n    &lt;li&gt;Eligibility extends to all U.S. citizens 18 and older.&lt;/li&gt;\n    &lt;li&gt;Applications are reviewed within 10 business days.&lt;/li&gt;\n    &lt;li&gt;Approved applicants receive a confirmation by mail.&lt;/li&gt;\n  &lt;/ul&gt;\n&lt;/SummaryBox&gt;</code></pre>\n\n<p>Next steps summary with custom aria-label:</p>\n\n<pre><code class=\"language-html\">&lt;SummaryBox heading=\"Next steps\" label=\"What to do next\"&gt;\n  &lt;ol&gt;\n    &lt;li&gt;Gather your supporting documents.&lt;/li&gt;\n    &lt;li&gt;Complete the online form.&lt;/li&gt;\n    &lt;li&gt;Submit before the deadline.&lt;/li&gt;\n  &lt;/ol&gt;\n&lt;/SummaryBox&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: focus moves to interactive descendants (links, buttons) within the body</li>\n<li>The box itself is not interactive</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>&lt;aside&gt;</code> provides the implicit <code>complementary</code> landmark role</li>\n<li><code>aria-label</code> defaults to the <code>heading</code> prop, or uses <code>label</code> when supplied</li>\n<li>The heading is a real <code>&lt;h3&gt;</code> element so it appears in the document outline</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to highlight key takeaways, next steps, or a TL;DR for a long page.</li>\n<li>Use when the same content also appears in the body and the box should serve as a quick reference.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for warnings or alerts — use <code>Alert</code>, <code>WarningCallout</code>, or <code>InformationCallout</code>.</li>\n<li>Do not use as a generic content container — use <code>Card</code> or <code>Panel</code> instead.</li>\n<li>Do not use as a replacement for proper headings within the article body.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>Renders the structural skeleton — <code>&lt;aside&gt;</code> with <code>&lt;h3&gt;</code> heading and a body <code>&lt;div&gt;</code>. Visual treatment, border, padding, color, and any leading icon are entirely the consumer's responsibility.</p>\n\n<h2>Styles</h2>\n\n<p>Consumer CSS targets the <code>summary-box</code> class plus the inner <code>summary-box-heading</code> and <code>summary-box-body</code> classes for fine-grained control.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders an <code>&lt;aside&gt;</code> element with class <code>summary-box</code></li>\n<li>Verify the heading is rendered inside an <code>&lt;h3&gt;</code> with class <code>summary-box-heading</code></li>\n<li>Verify the heading text equals the <code>heading</code> prop</li>\n<li>Verify <code>aria-label</code> defaults to the heading when <code>label</code> is omitted</li>\n<li>Verify <code>aria-label</code> uses the <code>label</code> prop when supplied</li>\n<li>Verify children render inside <code>&lt;div class=\"summary-box-body\"&gt;</code></li>\n<li>Verify additional HTML attributes pass through to the root <code>&lt;aside&gt;</code></li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Place the summary box near the top of the page so scanning users see it first. Keep the heading short and the body to a few bullet points.</li>\n<li><strong>Developers</strong>: Always supply a meaningful <code>heading</code>. Use <code>label</code> only when the heading would not make sense as a standalone landmark name.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>panel</code> — a generic content panel with optional heading</li>\n<li><code>card</code> — a grouped content container with header, body, and footer areas</li>\n<li><code>info-state</code> — a composition of illustration, title, description, and action for empty, error, or info states</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>US Web Design System Summary Box: https://designsystem.digital.gov/components/summary-box/</li>\n<li>HTML <code>&lt;aside&gt;</code>: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/aside</li>\n<li>WAI-ARIA complementary role: https://www.w3.org/TR/wai-aria-1.2/#complementary</li>\n</ul>";
</script>

<svelte:head>
  <title>Summary Box — Lily Design System</title>
  <meta name="description" content="A summary box is an &lt;aside&gt; callout that highlights key takeaways or next steps from a longer page, with a required heading and any body content." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
