<script lang="ts">
  const html: string = "<h1>Measurement Instance View</h1>\n\n<p>A measurement instance view displays a specific measured value in a <code>&lt;span&gt;</code> element. A measurement instance is a concrete data point such as \"72 kg\", \"98.6 F\", or \"120/80 mmHg\". This component is part of the Input/View pattern for measurement data, providing a read-only display of measurement values.</p>\n\n<p>The component supports an optional <code>aria-label</code> for additional accessible context, which is useful when the displayed value alone may not convey enough meaning to screen reader users (e.g., labeling \"72 kg\" as \"Patient weight\").</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as a <code>&lt;span&gt;</code> element displaying the measurement value as text content</li>\n<li>A measurement instance is a specific measured value (e.g., \"72 kg\", \"98.6 F\", \"120/80 mmHg\")</li>\n<li>Optional <code>label</code> prop maps to <code>aria-label</code> for additional accessible context</li>\n<li>Text content is inherently accessible to screen readers</li>\n<li>Spreads <code>...restProps</code> on the root <code>&lt;span&gt;</code> element for consumer customization</li>\n<li>No hardcoded user-facing strings; all text comes through props</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>value</code>: string (required) -- the measurement instance value to display</li>\n<li><code>label</code>: string (optional) -- accessible name via aria-label for additional context</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Display a patient weight reading:</p>\n\n<pre><code class=\"language-html\">&lt;MeasurementInstanceView value=\"72 kg\" label=\"Patient weight\" /&gt;</code></pre>\n\n<p>Display a body temperature in a clinical summary:</p>\n\n<pre><code class=\"language-html\">&lt;MeasurementInstanceView value=\"98.6 F\" label=\"Body temperature\" /&gt;</code></pre>\n\n<p>Display a blood pressure reading:</p>\n\n<pre><code class=\"language-html\">&lt;MeasurementInstanceView value=\"120/80 mmHg\" label=\"Blood pressure\" /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this is a display-only component.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label</code> -- optionally set from the <code>label</code> prop to provide additional context beyond the displayed value</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to display a measurement value and unit in read-only format.</li>\n<li>Use in dashboards, reports, or detail views where the measurement should be visible but not editable.</li>\n<li>Use when displaying recorded values such as \"72 kg\", \"98.6 F\", or \"120/80 mmHg\".</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for entering measurements -- use MeasurementInstanceInput instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component renders a <code>&lt;span&gt;</code> with the measurement value as text content and optional <code>aria-label</code> for additional accessible context. The consumer provides all visual styling, including typography, color coding based on value ranges, and spacing.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.measurement-instance-view</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>measurement-instance-view</code></li>\n<li>Verify aria-label<code> -- optionally set from the </code>label` prop to provide additional context beyond the displayed value</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use consistent formatting and alignment for measurement values so users can quickly scan and compare readings across a page.</li>\n<li><strong>Developers</strong>: Provide the <code>label</code> prop when the displayed value alone may be ambiguous to screen reader users, such as labeling \"72 kg\" as \"Patient weight\".</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>MeasurementInstanceView is part of the measurement component family. It is the read-only counterpart to MeasurementInstanceInput and works alongside MeasurementUnitView, MeasurementSystemView, and their input counterparts.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>measurement-instance-input</code> — an input for entering a measurement value and unit</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/HTML/Element/div\">MDN div element</a></li>\n</ul>";
</script>

<svelte:head>
  <title>Measurement Instance View — Lily Design System</title>
  <meta name="description" content="A measurement instance view displays a specific measured value in a &lt;span&gt; element. A measurement instance is a concrete data point such as &quot;72 kg&quot;, &quot;98.6 F&quot;, or &quot;120/80 mmHg&quot;. This component is pa..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
