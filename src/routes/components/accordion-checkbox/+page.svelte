<script lang="ts">
  const html: string = "<h1>Accordion Checkbox</h1>\n\n<p>A compound disclosure widget that pairs a checkbox option with a revealable panel. The user sees a vertical list of checkbox options; checking a box reveals an accordion panel of additional content beneath that option. Common use cases include conditional follow-up questions, opt-in disclosures, and form sections that only apply once selected.</p>\n\n<p>Unlike <code>Details</code>, which uses a <code>&lt;summary&gt;</code> toggle, this component uses a real form control. The checkbox state is the source of truth — <code>checked</code> controls whether the panel is visible. Multiple AccordionCheckbox instances can stack vertically to make a list of independent toggleable options.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div class=\"accordion-checkbox\"&gt;</code> containing an <code>&lt;input type=\"checkbox\"&gt;</code>, its <code>&lt;label&gt;</code>, and a <code>&lt;div class=\"accordion-checkbox-panel\"&gt;</code> that is hidden when unchecked</li>\n<li>Auto-generates a unique id linking the checkbox (<code>&lt;id&gt;-checkbox</code>) and the panel (<code>&lt;id&gt;-panel</code>) so <code>aria-controls</code> and <code>&lt;label for&gt;</code> resolve correctly</li>\n<li>Sets <code>aria-controls</code> on the checkbox pointing at the panel and <code>aria-expanded</code> matching the checked state</li>\n<li>Sets <code>role=\"region\"</code> and <code>aria-labelledby</code> on the panel so screen readers identify the revealed area as a labeled region</li>\n<li>Applies the native <code>hidden</code> attribute to the panel when unchecked so it is removed from the layout and the accessibility tree</li>\n<li>Supports two-way binding on the <code>checked</code> prop for parent components to control or react to the state</li>\n<li>Spreads <code>restProps</code> onto the root <code>&lt;div&gt;</code> for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) — the checkbox label text</li>\n<li><code>checked</code>: boolean (default: false) — whether the checkbox is checked and the panel is revealed; bindable</li>\n<li><code>id</code>: string (optional) — base id for the checkbox/panel link; auto-generated if not provided</li>\n<li><code>children</code>: slot (required) — content shown when the checkbox is checked</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;AccordionCheckbox label=\"I have a follow-up question\"&gt;\n  &lt;p&gt;Tell us more in the text-area-input below.&lt;/p&gt;\n  &lt;textarea aria-label=\"Your question\"&gt;&lt;/textarea&gt;\n&lt;/AccordionCheckbox&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;AccordionCheckbox label=\"Subscribe to optional updates\" checked&gt;\n  &lt;fieldset&gt;\n    &lt;legend&gt;Topics&lt;/legend&gt;\n    &lt;label&gt;&lt;input type=\"checkbox\" name=\"topic\" value=\"news\"&gt; News&lt;/label&gt;\n    &lt;label&gt;&lt;input type=\"checkbox\" name=\"topic\" value=\"events\"&gt; Events&lt;/label&gt;\n  &lt;/fieldset&gt;\n&lt;/AccordionCheckbox&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: moves focus onto the checkbox</li>\n<li>Space: toggles the checkbox checked state, which reveals or hides the panel</li>\n<li>Native checkbox keyboard behavior applies; no custom handlers required</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-controls</code> on the <code>&lt;input&gt;</code> references the panel id so assistive technology can navigate from the trigger to its target</li>\n<li><code>aria-expanded</code> on the <code>&lt;input&gt;</code> reflects the checked state, so screen readers announce open/closed</li>\n<li><code>role=\"region\"</code> and <code>aria-labelledby</code> on the panel identify it as a labeled landmark region tied to the checkbox</li>\n<li><code>hidden</code> on the panel removes it from the accessibility tree when unchecked</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use when an option introduces follow-up content the user only needs to see if they opt in</li>\n<li>Use for conditional sections of forms (for example, \"I have additional comments\" → text-area-input)</li>\n<li>Use when stacking several independent opt-in disclosures vertically as a list</li>\n<li>Use when the trigger should be a real form control, so the value is part of form submission</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when the trigger is informational only — use <code>Details</code> (<code>&lt;details&gt;</code> / <code>&lt;summary&gt;</code>) instead</li>\n<li>Do not use to switch a setting on/off without revealing additional content — use <code>SwitchButton</code> or a plain <code>CheckboxInput</code></li>\n<li>Do not use for mutually exclusive options — use <code>RadioGroup</code></li>\n<li>Do not use when only one of many options can be expanded at a time — use <code>AccordionList</code> / <code>AccordionListItem</code></li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component renders semantic HTML (<code>&lt;div&gt;</code> wrapping a <code>&lt;label&gt;</code> + <code>&lt;input type=\"checkbox\"&gt;</code> + revealable panel) with all required ARIA wiring. The consumer provides all visual styling — including spacing, indentation, focus rings, and any animation on the reveal.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with these class hooks:</p>\n\n<ul>\n<li><code>.accordion-checkbox</code> on the root <code>&lt;div&gt;</code></li>\n<li><code>.accordion-checkbox-input</code> on the <code>&lt;input type=\"checkbox\"&gt;</code></li>\n<li><code>.accordion-checkbox-label</code> on the <code>&lt;label&gt;</code></li>\n<li><code>.accordion-checkbox-panel</code> on the revealable <code>&lt;div&gt;</code></li>\n</ul>\n\n<p>No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the root <code>&lt;div&gt;</code> has class <code>accordion-checkbox</code></li>\n<li>Verify the checkbox toggles <code>aria-expanded</code> and the panel <code>hidden</code> attribute together</li>\n<li>Verify <code>aria-controls</code> on the checkbox matches the panel <code>id</code></li>\n<li>Verify the panel content is in the accessibility tree only when checked</li>\n<li>Verify two-way binding on <code>checked</code> reflects parent state</li>\n<li>Verify keyboard activation via Space toggles the panel</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Indicate that the option reveals more content (a chevron or \"+/–\" cue helps). Make sure the revealed panel is visually grouped with its trigger so the relationship is unambiguous.</li>\n<li><strong>Developers</strong>: Keep the panel content lightweight; if it grows large, consider lazy rendering tied to the <code>checked</code> prop. Validate any inputs inside the panel only when the checkbox is checked.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>accordion-list</code> — an accordion ordered list of list item components</li>\n<li><code>accordion-list-item</code> — an accordion list item component</li>\n<li><code>details</code> — a disclosure widget that shows and hides content</li>\n<li><code>switch-button</code> — a toggle switch for turning a setting on or off</li>\n<li><code>checkbox-input</code> — a checkbox input for toggling a boolean value &lt;input type=\"checkbox\"&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Disclosure Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/disclosure/</li>\n<li>MDN input type=checkbox: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/checkbox</li>\n<li>MDN aria-controls: https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/Attributes/aria-controls</li>\n<li>MDN hidden attribute: https://developer.mozilla.org/en-US/docs/Web/HTML/Global_attributes/hidden</li>\n</ul>";
</script>

<svelte:head>
  <title>Accordion Checkbox — Lily Design System</title>
  <meta name="description" content="A compound disclosure widget that pairs a checkbox option with a revealable panel. The user sees a vertical list of checkbox options; checking a box reveals an accordion panel of additional content..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
