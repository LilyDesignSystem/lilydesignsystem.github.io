<script lang="ts">
  const html: string = "<h1>Tooltip</h1>\n\n<p>A tooltip is a headless component that displays brief, contextual information when made visible. It uses <code>role=\"tooltip\"</code> for semantic identification and is designed to be linked to a trigger element via <code>aria-describedby</code>, providing supplementary text that screen readers can announce.</p>\n\n<p>Tooltips are used to clarify the function or meaning of an element without cluttering the interface, offering helpful hints, labels, or explanations on demand. The consumer controls visibility through the bindable <code>visible</code> prop and is responsible for toggling it on hover, focus, or other interaction events.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> with <code>role=\"tooltip\"</code> when visible, removed from the DOM when hidden</li>\n<li>Uses conditional rendering, so the tooltip is fully removed from the DOM when not shown</li>\n<li>Supports two-way binding on the <code>visible</code> prop</li>\n<li>The consumer is responsible for managing visibility (e.g., on hover/focus of a trigger element)</li>\n<li>The <code>id</code> prop enables linking the tooltip to its trigger element via <code>aria-describedby</code></li>\n<li>Spreads <code>restProps</code> onto the div element for consumer extensibility</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- the tooltip text content</li>\n<li><code>visible</code>: boolean (default: false) -- bindable boolean controlling whether the tooltip is shown</li>\n<li><code>id</code>: string (default: undefined) -- optional id for linking to a trigger element via <code>aria-describedby</code></li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the tooltip div</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Tooltip on an icon button:</p>\n\n<pre><code class=\"language-html\">&lt;button aria-describedby=\"save-tip\"\n  onmouseenter={() =&gt; showSaveTip = true}\n  onmouseleave={() =&gt; showSaveTip = false}\n  onfocusin={() =&gt; showSaveTip = true}\n  onfocusout={() =&gt; showSaveTip = false}\n&gt;\n  &lt;Icon name=\"save\" /&gt;\n  &lt;ScreenReaderSpan&gt;Save document&lt;/ScreenReaderSpan&gt;\n&lt;/button&gt;\n&lt;Tooltip id=\"save-tip\" label=\"Save document\" visible={showSaveTip} /&gt;</code></pre>\n\n<p>Tooltip explaining an abbreviation:</p>\n\n<pre><code class=\"language-html\">&lt;span aria-describedby=\"a11y-tip\"\n  onmouseenter={() =&gt; showA11yTip = true}\n  onmouseleave={() =&gt; showA11yTip = false}\n  onfocusin={() =&gt; showA11yTip = true}\n  onfocusout={() =&gt; showA11yTip = false}\n  tabindex=\"0\"\n&gt;\n  WCAG\n&lt;/span&gt;\n&lt;Tooltip id=\"a11y-tip\" label=\"Web Content Accessibility Guidelines\" visible={showA11yTip} /&gt;</code></pre>\n\n<p>Tooltip for a toolbar action:</p>\n\n<pre><code class=\"language-html\">&lt;button aria-describedby=\"delete-tip\"\n  onmouseenter={() =&gt; showDeleteTip = true}\n  onmouseleave={() =&gt; showDeleteTip = false}\n  onfocusin={() =&gt; showDeleteTip = true}\n  onfocusout={() =&gt; showDeleteTip = false}\n&gt;\n  &lt;Icon name=\"trash\" /&gt;\n&lt;/button&gt;\n&lt;Tooltip id=\"delete-tip\" label=\"Delete selected items\" visible={showDeleteTip} /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Escape: The consumer should hide the tooltip when Escape is pressed (not built into the component; must be handled by the consumer)</li>\n<li>The tooltip itself does not receive focus; it is a supplementary description for another element</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"tooltip\"</code> -- identifies the element as a tooltip, a popup that displays a description for an element</li>\n<li><code>id={id}</code> -- used with <code>aria-describedby</code> on the trigger element to create an accessible association between the trigger and the tooltip</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use Tooltip to provide brief, supplementary descriptions for UI elements on hover or focus, such as explaining an icon button's purpose.</li>\n<li>Use to clarify abbreviations, technical terms, or truncated text.</li>\n<li>Use when the information is helpful but not essential for completing a task.</li>\n<li>Use for labelling icon-only buttons where a visible label would not fit.</li>\n<li>Use for showing full text when content is truncated with ellipsis.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for critical information that users must see -- use inline text or Alert instead.</li>\n<li>Do not use for interactive content such as links, buttons, or forms -- use Popover instead.</li>\n<li>Do not use for long text -- keep tooltip content to one short sentence.</li>\n<li>Do not use for rich content previews -- use HoverCard instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;div&gt;</code> with <code>role=\"tooltip\"</code> and conditional rendering (fully removed from DOM when hidden). The consumer manages visibility toggling (hover, focus events), positioning, and all visual styling including background, border, and arrow indicators.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.tooltip</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>tooltip</code></li>\n<li>Verify role=\"tooltip\"` -- identifies the element as a tooltip, a popup that displays a description for an element</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Keep tooltip text concise (one line if possible). Position tooltips so they do not obscure the trigger element or important content.</li>\n<li><strong>Developers</strong>: Link the tooltip to its trigger using <code>id</code> and <code>aria-describedby</code>. Handle Escape key to dismiss the tooltip and ensure it works on both hover and focus for keyboard accessibility.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>hover-card</code> — a card that appears on hover over a trigger element</li>\n<li><code>popover</code> — a floating content box anchored to a trigger element</li>\n<li><code>contextual-help</code> — a help button that opens a popover with explanatory content</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Tooltip Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/tooltip/</li>\n<li>MDN aria-describedby: https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/Attributes/aria-describedby</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"tooltip\" role=\"tooltip\">Helpful tip text</div>";
  const svelteSource: string = "// In your Svelte component:\nimport Tooltip from \"lily-design-system-svelte-headless/components/Tooltip/Tooltip.svelte\";\n\n<Tooltip>\n  <!-- content -->\n</Tooltip>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Tooltip — Lily Design System</title>
  <meta name="description" content="A tooltip is a headless component that displays brief, contextual information when made visible. It uses role=&quot;tooltip&quot; for semantic identification and is designed to be linked to a trigger element..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

