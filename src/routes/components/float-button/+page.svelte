<script lang="ts">
  const html: string = "<h1>Float Button</h1>\n\n<p>A float button is a floating action button anchored to a viewport corner. It uses inline <code>position: fixed</code> as the documented headless behavior and a required <code>aria-label</code> so screen readers announce its purpose.</p>\n\n<p>Use it for primary single-action shortcuts (compose, scroll-to-top, new chat, contact support) that should remain reachable regardless of scroll position.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a native <code>&lt;button&gt;</code> element with class <code>float-button</code></li>\n<li>Inline style applies <code>position: fixed</code> plus the corner offset for the chosen <code>position</code></li>\n<li><code>position</code> defaults to <code>\"bottom-right\"</code> and emits <code>data-position</code></li>\n<li>Default offset is <code>1rem</code> from each edge</li>\n<li><code>label</code> is <strong>required</strong> and sets <code>aria-label</code></li>\n<li>Defaults to <code>type=\"button\"</code> to prevent accidental form submissions</li>\n<li>Native <code>disabled</code> attribute prevents click events automatically</li>\n<li>Children render the icon content (decorative)</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (<strong>required</strong>) -- aria-label</li>\n<li><code>position</code>: \"top-left\" | \"top-right\" | \"bottom-left\" | \"bottom-right\" (default: \"bottom-right\")</li>\n<li><code>disabled</code>: boolean (default: false)</li>\n<li><code>type</code>: \"button\" | \"submit\" | \"reset\" (default: \"button\")</li>\n<li><code>onclick</code> / <code>onClick</code>: callback (optional)</li>\n<li><code>children</code>: slot -- icon content</li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;FloatButton label=\"Compose\" position=\"bottom-right\" onclick={openComposer}&gt;\n  &lt;svg&gt;...&lt;/svg&gt;\n&lt;/FloatButton&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;FloatButton label=\"Scroll to top\" position=\"bottom-left\" onclick={scrollTop}&gt;\n  ↑\n&lt;/FloatButton&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus the button</li>\n<li>Enter: Activate the button</li>\n<li>Space: Activate the button</li>\n<li>(All handled natively by <code>&lt;button&gt;</code> element)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Implicit <code>button</code> role from <code>&lt;button&gt;</code> element</li>\n<li><code>aria-label</code> is the entire accessible name (required)</li>\n<li><code>aria-disabled</code> from native <code>disabled</code> attribute</li>\n<li><code>data-position</code> exposes the corner anchor for CSS hooks</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for one or two persistent shortcuts that should always be reachable.</li>\n<li>Use for compose, contact, scroll-to-top, or other ever-available actions.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for primary navigation — use a navigation menu or tab bar.</li>\n<li>Do not stack many float buttons — clutter and overlap reduce usability.</li>\n<li>Do not omit <code>label</code> — icon-only buttons without an accessible name fail WCAG.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>Renders a <code>&lt;button&gt;</code> with inline <code>position: fixed</code> style and the right corner offset. The icon glyph, size, focus indicator, hover state, and elevation/shadow are entirely the consumer's responsibility.</p>\n\n<h2>Styles</h2>\n\n<p>Consumer CSS targets the <code>float-button</code> class. Use <code>data-position</code> to pick the corner. Provide a minimum 44×44 px touch target, a clear focus indicator, and elevation that matches the surrounding theme.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;button&gt;</code> with class <code>float-button</code></li>\n<li>Verify <code>aria-label</code> is present and equals the <code>label</code> prop</li>\n<li>Verify <code>type</code> defaults to <code>\"button\"</code></li>\n<li>Verify <code>position</code> defaults to <code>\"bottom-right\"</code> and emits <code>data-position</code></li>\n<li>Verify inline style includes <code>position: fixed</code></li>\n<li>Verify inline style includes the correct corner offset for each position</li>\n<li>Verify <code>disabled</code> attribute is applied when <code>disabled</code> is true</li>\n<li>Verify the click handler fires on Enter, Space, and click</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Place the float button where it will not obscure key content. Maintain sufficient contrast against any underlying scroll surface.</li>\n<li><strong>Developers</strong>: Always supply <code>label</code>. Avoid stacking multiple float buttons that overlap; group related actions inside a single trigger.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>button</code> — a generic clickable button element</li>\n<li><code>icon-button</code> — a button containing only an icon with a required accessible label</li>\n<li><code>affix</code> — a wrapper that pins its content to a viewport position while the page scrolls</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Button Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/button/</li>\n<li>WCAG 4.1.2 Name, Role, Value: https://www.w3.org/WAI/WCAG22/Understanding/name-role-value</li>\n<li>Ant Design FloatButton: https://ant.design/components/float-button</li>\n</ul>";
</script>

<svelte:head>
  <title>Float Button — Lily Design System</title>
  <meta name="description" content="A float button is a floating action button anchored to a viewport corner. It uses inline position: fixed as the documented headless behavior and a required aria-label so screen readers announce its..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
