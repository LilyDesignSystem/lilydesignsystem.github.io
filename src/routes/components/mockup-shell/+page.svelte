<script lang="ts">
  const html: string = "<h1>MockupShell</h1>\n\n<p>A mockup shell is a decorative container that visually resembles a terminal shell or command-line interface, used to frame terminal output, commands, or CLI examples in documentation and tutorials.</p>\n\n<p>This headless component uses a <code>&lt;div&gt;</code> element with <code>role=\"img\"</code> and <code>aria-label</code> to present the mockup as a decorative image to assistive technologies.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses <code>&lt;div&gt;</code> element with <code>role=\"img\"</code> for decorative presentation</li>\n<li><code>aria-label</code> describes the mockup content for screen readers</li>\n<li>Contains a slot for the terminal content area</li>\n<li>Consumer styles the shell chrome (title bar, prompt, background)</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible label describing the mockup content</li>\n<li><code>children</code>: slot (required) -- the content displayed inside the shell mockup</li>\n<li><code>...restProps</code>: Any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Basic shell mockup with an installation command:</p>\n\n<pre><code class=\"language-html\">&lt;MockupShell label=\"Terminal showing installation command\"&gt;\n  &lt;code&gt;npm install lily-design-system&lt;/code&gt;\n&lt;/MockupShell&gt;</code></pre>\n\n<p>Shell mockup with multi-line output:</p>\n\n<pre><code class=\"language-html\">&lt;MockupShell label=\"Terminal showing build output\"&gt;\n  &lt;pre&gt;&lt;code&gt;$ pnpm build\nBuilding project...\n✓ Compiled 42 components\n✓ Generated type definitions\n✓ Build complete in 3.2s&lt;/code&gt;&lt;/pre&gt;\n&lt;/MockupShell&gt;</code></pre>\n\n<p>Shell mockup in a getting-started guide:</p>\n\n<pre><code class=\"language-html\">&lt;h3&gt;Step 1: Install dependencies&lt;/h3&gt;\n&lt;MockupShell label=\"Terminal showing project setup commands\"&gt;\n  &lt;pre&gt;&lt;code&gt;$ mkdir my-app &amp;&amp; cd my-app\n$ pnpm init\n$ pnpm add lily-design-system&lt;/code&gt;&lt;/pre&gt;\n&lt;/MockupShell&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>None -- mockups are decorative, not interactive</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"img\"</code> -- presents the mockup as a single image to assistive technologies</li>\n<li><code>aria-label</code> -- describes the mockup content for screen readers</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to showcase terminal commands, CLI output, or shell interactions in a realistic frame.</li>\n<li>Use in documentation and tutorials to frame command-line installation or setup steps.</li>\n<li>Use in developer-facing marketing pages to highlight CLI tooling.</li>\n<li>Use in README previews or onboarding guides to show expected terminal output.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for code snippets or source code display -- use CodeBlock instead.</li>\n<li>Do not use for functional terminal emulators -- build a custom interactive terminal instead.</li>\n<li>Do not use for non-terminal content -- use MockupWindow or MockupBrowser instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides a <code>&lt;div&gt;</code> with <code>role=\"img\"</code> and <code>aria-label</code> for accessible decorative presentation, with zero visual styling. The consumer is responsible for all CSS including terminal chrome, title bar, prompt styling, monospace font, background color, and content area sizing.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.mockup-shell</code> class for targeting. No default styles are included -- this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>mockup-shell</code></li>\n<li>Verify <code>role=\"img\"</code> is present</li>\n<li>Verify <code>aria-label</code> is applied</li>\n<li>Verify child content is rendered</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use a dark background with light monospace text for a recognizable terminal appearance. Include a title bar with window controls for authenticity.</li>\n<li><strong>Developers</strong>: Always provide a descriptive <code>label</code> prop so screen readers can convey the mockup's purpose.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>mockup-browser</code> — a box area that looks like a web browser</li>\n<li><code>mockup-laptop</code> — a box area that looks like a laptop computer</li>\n<li><code>mockup-phone-landscape</code> — a box area that looks like a mobile phone in landscape orientation</li>\n<li><code>mockup-phone-portrait</code> — a box area that looks like a mobile phone</li>\n<li><code>mockup-tablet-landscape</code> — a box area that looks like a tablet computer in landscape mode</li>\n<li><code>mockup-tablet-portrait</code> — a box area that looks like a tablet computer in portrait mode</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/HTML/Element/div\">MDN div element</a></li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"mockup-shell\" role=\"img\" aria-label=\"a box area that looks like a terminal shell\"><div style=\"padding:1rem;background:#f3f4f6;border:2px solid #d1d5db;border-radius:8px;text-align:center;min-height:100px;\">MockupShell Preview</div></div>";
  const svelteSource: string = "// In your Svelte component:\nimport MockupShell from \"lily-design-system-svelte-headless/components/MockupShell/MockupShell.svelte\";\n\n<MockupShell>\n  <!-- content -->\n</MockupShell>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>MockupShell — Lily Design System</title>
  <meta name="description" content="A mockup shell is a decorative container that visually resembles a terminal shell or command-line interface, used to frame terminal output, commands, or CLI examples in documentation and tutorials." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

