<script lang="ts">
  const html: string = "<h1>Calendar Table Body</h1>\n\n<p>A calendar table body is the main content section of a calendar grid, wrapping the rows that contain the day cells. It groups the data rows of the calendar, where each row represents a week and each cell represents a day. It is designed to be used inside a CalendarTable <code>&lt;table&gt;</code> structure.</p>\n\n<p>The component renders a <code>&lt;tbody&gt;</code> element and passes through its children, which are expected to be CalendarTableRow or <code>&lt;tr&gt;</code> elements containing day cells.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as a <code>&lt;tbody&gt;</code> element for the body section of a calendar grid</li>\n<li>Children should be rows of day cells, typically one row per week</li>\n<li>Designed to be used inside a CalendarTable <code>&lt;table&gt;</code> structure</li>\n<li>Spreads <code>restProps</code> onto the <code>&lt;tbody&gt;</code> element for consumer customization</li>\n<li>No internal state -- purely a structural wrapper</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>children</code>: slot (required) -- rows of day cells representing weeks in the calendar</li>\n<li><code>...restProps</code>: Any additional HTML attributes passed to the <code>&lt;tbody&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;CalendarTable label=\"April 2026\"&gt;\n  &lt;CalendarTableHead&gt;...&lt;/CalendarTableHead&gt;\n  &lt;CalendarTableBody&gt;\n    &lt;CalendarTableRow&gt;\n      &lt;CalendarTableTD&gt;1&lt;/CalendarTableTD&gt;\n      &lt;CalendarTableTD&gt;2&lt;/CalendarTableTD&gt;\n      &lt;CalendarTableTD&gt;3&lt;/CalendarTableTD&gt;\n      &lt;CalendarTableTD&gt;4&lt;/CalendarTableTD&gt;\n      &lt;CalendarTableTD&gt;5&lt;/CalendarTableTD&gt;\n      &lt;CalendarTableTD&gt;6&lt;/CalendarTableTD&gt;\n      &lt;CalendarTableTD&gt;7&lt;/CalendarTableTD&gt;\n    &lt;/CalendarTableRow&gt;\n    &lt;CalendarTableRow&gt;\n      &lt;CalendarTableTD today&gt;8&lt;/CalendarTableTD&gt;\n      &lt;CalendarTableTD&gt;9&lt;/CalendarTableTD&gt;\n      &lt;CalendarTableTD&gt;10&lt;/CalendarTableTD&gt;\n      &lt;CalendarTableTD&gt;11&lt;/CalendarTableTD&gt;\n      &lt;CalendarTableTD&gt;12&lt;/CalendarTableTD&gt;\n      &lt;CalendarTableTD&gt;13&lt;/CalendarTableTD&gt;\n      &lt;CalendarTableTD&gt;14&lt;/CalendarTableTD&gt;\n    &lt;/CalendarTableRow&gt;\n  &lt;/CalendarTableBody&gt;\n&lt;/CalendarTable&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive container. Keyboard navigation is handled by the parent CalendarTable grid.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Implicit <code>rowgroup</code> role from the <code>&lt;tbody&gt;</code> element -- groups the body rows of the grid</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside CalendarTable to provide the <code>&lt;tbody&gt;</code> for the calendar grid</li>\n<li>Use to group the week rows containing day cells in the main calendar area</li>\n<li>Use when each row represents one week and each cell represents one day</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside CalendarTable -- use TableBody or DataTableBody for other table types</li>\n<li>Do not use for header rows -- use CalendarTableHead for day-of-week labels</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides a semantic <code>&lt;tbody&gt;</code> element with implicit <code>rowgroup</code> role and zero visual styling. The consumer is responsible for all CSS including row spacing, cell layout, and any visual distinction between the body and header/footer sections.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.calendar-table-body</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;tbody&gt;</code> element with class <code>calendar-table-body</code></li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Ensure consistent row heights and cell sizes across all weeks in the body. Visually distinguish the body from the header row of day-of-week labels.</li>\n<li><strong>Developers</strong>: Each child row should contain exactly 7 cells (one per day of the week). Use empty cells or disabled styling for days outside the current month.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>CalendarTableBody is part of the CalendarTable composition pattern:</p>\n\n<ul>\n<li><strong>CalendarTable</strong> -- outer <code>&lt;table&gt;</code> with <code>role=\"grid\"</code>.</li>\n<li><strong>CalendarTableHead</strong> -- <code>&lt;thead&gt;</code> for day-of-week column headers.</li>\n<li><strong>CalendarTableBody</strong> -- <code>&lt;tbody&gt;</code> grouping the week rows of day cells.</li>\n<li><strong>CalendarTableRow</strong> -- <code>&lt;tr&gt;</code> representing one week.</li>\n<li><strong>CalendarTableCell</strong> -- <code>&lt;td&gt;</code> representing one day.</li>\n</ul>\n\n<pre><code class=\"language-html\">&lt;CalendarTable label=\"January 2025\"&gt;\n  &lt;CalendarTableHead&gt;...&lt;/CalendarTableHead&gt;\n  &lt;CalendarTableBody&gt;\n    &lt;CalendarTableRow&gt;\n      &lt;CalendarTableCell&gt;1&lt;/CalendarTableCell&gt;\n      &lt;CalendarTableCell&gt;2&lt;/CalendarTableCell&gt;\n    &lt;/CalendarTableRow&gt;\n  &lt;/CalendarTableBody&gt;\n&lt;/CalendarTable&gt;</code></pre>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>calendar-table</code> — a calendar table interactive grid for managing dates, days, etc. &lt;table&gt;</li>\n<li><code>calendar-table-foot</code> — a calendar table interactive grid tfoot for managing dates, days, etc. &lt;tfoot&gt;</li>\n<li><code>calendar-table-head</code> — a calendar table interactive grid thead for managing dates, days, etc. &lt;thead&gt;</li>\n<li><code>calendar-table-row</code> — a calendar table interactive grid row for managing dates, days, etc. &lt;tr&gt;</li>\n<li><code>calendar-table-td</code> — a calendar table interactive grid data cell for managing dates, days, etc. &lt;td&gt;</li>\n<li><code>calendar-table-th</code> — a calendar table interactive grid header cell for managing dates, days, etc. &lt;th&gt;</li>\n<li><code>table</code> — a table with rows and columns &lt;table&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Grid Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/grid/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<tbody class=\"calendar-table-body\"><tr class=\"calendar-table-row\"><td class=\"calendar-table-td\">1</td></tr></tbody>";
  const svelteSource: string = "// In your Svelte component:\nimport CalendarTableBody from \"lily-design-system-svelte-headless/components/CalendarTableBody/CalendarTableBody.svelte\";\n\n<CalendarTableBody>\n  <!-- CalendarTableBody content -->\n</CalendarTableBody>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Calendar Table Body — Lily Design System</title>
  <meta name="description" content="A calendar table body is the main content section of a calendar grid, wrapping the rows that contain the day cells. It groups the data rows of the calendar, where each row represents a week and eac..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

