<script lang="ts">
  const html: string = "<h1>Medical Banner Box For Advice</h1>\n\n<p>MedicalBannerBoxForAdvice is a headless component for displaying routine medical record advice information such as contacts, contexts, care plans, and other advice-level clinical information. It renders a <code>&lt;div&gt;</code> with <code>role=\"region\"</code>, <code>aria-label</code>, and <code>data-type=\"advice\"</code> for consumer styling.</p>\n\n<p>This component is useful in clinical interfaces, electronic health records, and patient summary screens where advice-level information should be clearly presented alongside danger-level information.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> element with class <code>medical-banner-box-for-advice</code></li>\n<li>Uses <code>role=\"region\"</code> and <code>aria-label</code> for accessibility</li>\n<li>Uses <code>data-type=\"advice\"</code> attribute for consumer CSS targeting</li>\n<li>Uses <code>children</code> slot for flexible content rendering</li>\n<li>Spreads <code>restProps</code> onto the <code>&lt;div&gt;</code> element for consumer extensibility</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name applied via <code>aria-label</code></li>\n<li><code>children</code>: slot (required) -- the advice banner box content</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;div&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;MedicalBanner label=\"Patient summary\"&gt;\n  &lt;MedicalBannerBox&gt;\n    &lt;strong&gt;Sarah Mitchell&lt;/strong&gt; | NHS: 485 777 3456\n  &lt;/MedicalBannerBox&gt;\n  &lt;MedicalBannerBoxForAdvice label=\"Care team contacts\"&gt;\n    &lt;p&gt;GP: Dr James Lee&lt;/p&gt;\n    &lt;p&gt;District Nurse: Nurse Jones&lt;/p&gt;\n    &lt;p&gt;Next appointment: 22 April 2025&lt;/p&gt;\n  &lt;/MedicalBannerBoxForAdvice&gt;\n&lt;/MedicalBanner&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;MedicalBannerBoxForAdvice label=\"Discharge plan\"&gt;\n  &lt;p&gt;Target discharge: 15 April 2025&lt;/p&gt;\n  &lt;p&gt;Follow-up: Outpatient cardiology in 2 weeks&lt;/p&gt;\n&lt;/MedicalBannerBoxForAdvice&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>None (passive layout element)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"region\"</code> -- establishes the box as a landmark for assistive technology</li>\n<li><code>aria-label={label}</code> -- provides an accessible name describing the information</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside MedicalBanner for advice-level information such as care contacts, clinical context, and care plans</li>\n<li>Use to display the patient's GP, key worker, next appointment, or care pathway</li>\n<li>Use when clinicians need quick reference to routine supporting information alongside the patient banner</li>\n<li>Use for displaying treatment goals, discharge plans, or referral information</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for danger-level information such as allergies or adverse reactions -- use MedicalBannerBoxForDanger</li>\n<li>Do not use outside MedicalBanner -- it relies on the parent for landmark and live region semantics</li>\n<li>Do not use for system-level alerts or warnings -- use Alert or WarningCallout</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;div&gt;</code> with <code>role=\"region\"</code>, <code>aria-label</code>, and <code>data-type=\"advice\"</code>. The consumer is responsible for all CSS styling including background color, text styling, and layout.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.medical-banner-box-for-advice</code> class for targeting. No default styles are included -- this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>medical-banner-box-for-advice</code></li>\n<li>Verify <code>role=\"region\"</code> is present</li>\n<li>Verify <code>aria-label</code> is set from the <code>label</code> prop</li>\n<li>Verify <code>data-type=\"advice\"</code> is present</li>\n<li>Verify children content is rendered</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use a neutral background color to visually distinguish this from danger information. Ensure text contrast meets WCAG AAA requirements.</li>\n<li><strong>Developers</strong>: Always provide a meaningful <code>label</code> prop that describes the information category (e.g., \"Care team contacts\", \"Care plan summary\").</li>\n</ul>\n\n<h2>Domain Knowledge</h2>\n\n<p>In clinical systems, advice-level information includes care team contacts, care plans, clinical context, and routine patient data. This information is typically displayed with neutral styling to contrast with danger-level alerts.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>announcement-banner</code> — a banner highlighting important messages for all users</li>\n<li><code>banner</code> — a prominent message bar across the top of a page</li>\n<li><code>banner-box</code> — a banner box that is inside a banner component, using flexbox horizontal</li>\n<li><code>government-banner</code> — a banner identifying a website as belonging to a government, with an expandable details panel</li>\n<li><code>medical-banner</code> — a prominent message bar across the top of a page, with medical information</li>\n<li><code>medical-banner-box</code> — a medical banner box that is inside a medical-banner component, using flexbox horizontal, with medical information</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>NHS UK Design System: https://service-manual.nhs.uk/design-system</li>\n</ul>";
</script>

<svelte:head>
  <title>Medical Banner Box For Advice — Lily Design System</title>
  <meta name="description" content="MedicalBannerBoxForAdvice is a headless component for displaying routine medical record advice information such as contacts, contexts, care plans, and other advice-level clinical information. It re..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
