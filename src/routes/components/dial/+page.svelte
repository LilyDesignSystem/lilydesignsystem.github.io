<script lang="ts">
  const html: string = "<h1>Dial</h1>\n\n<p>A circular dial or knob control that allows users to adjust a numeric value within a defined range. Dials provide an intuitive interaction model inspired by physical knobs found on devices like volume controls, thermostats, and audio equipment. This component is useful in control panels, audio applications, configuration interfaces, and anywhere a value needs fine-grained adjustment.</p>\n\n<p>The component renders as a <code>div</code> with <code>role=\"slider\"</code> and full keyboard navigation support. It exposes a bindable <code>value</code> prop for two-way data flow with configurable <code>min</code>, <code>max</code>, and <code>step</code> constraints. The Shift key modifier enables coarse adjustment at 10x the normal step size.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> with <code>role=\"slider\"</code> since there is no native dial HTML element</li>\n<li>Supports two-way binding on the <code>value</code> prop with the parent</li>\n<li>Internal <code>clamp()</code> function ensures the value always stays within the <code>min</code>/<code>max</code> range</li>\n<li>Keyboard handler supports both fine (arrow keys) and coarse (Shift+arrow) adjustment</li>\n<li>Home and End keys jump directly to <code>min</code> and <code>max</code> bounds</li>\n<li>The <code>tabindex</code> is set to <code>0</code> when enabled (focusable) or <code>-1</code> when disabled (not focusable)</li>\n<li><code>aria-disabled</code> is only set when the dial is disabled; omitted otherwise</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>value</code>: number (default: 0) -- current dial value; bindable</li>\n<li><code>min</code>: number (default: 0) -- minimum allowed value</li>\n<li><code>max</code>: number (default: 100) -- maximum allowed value</li>\n<li><code>step</code>: number (default: 1) -- increment per key press</li>\n<li><code>label</code>: string (required) -- accessible name for the dial</li>\n<li><code>disabled</code>: boolean (default: false) -- whether the dial is disabled</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;Dial label=\"Volume\" value={value} min={0} max={100} /&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;Dial label=\"Brightness\" value={brightness} min={0} max={100} step={5} /&gt;\n&lt;p&gt;Brightness: {brightness}%&lt;/p&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;Dial label=\"Disabled control\" value={30} disabled /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>ArrowRight / ArrowUp: increase value by one step</li>\n<li>ArrowLeft / ArrowDown: decrease value by one step</li>\n<li>Shift + ArrowRight / ArrowUp: increase value by 10 steps</li>\n<li>Shift + ArrowLeft / ArrowDown: decrease value by 10 steps</li>\n<li>Home: set value to minimum</li>\n<li>End: set value to maximum</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"slider\"</code> -- indicates a range input control</li>\n<li><code>aria-label=\"{label}\"</code> -- provides an accessible name for the dial</li>\n<li><code>aria-valuenow=\"{value}\"</code> -- communicates the current value to assistive technology</li>\n<li><code>aria-valuemin=\"{min}\"</code> -- communicates the minimum allowed value</li>\n<li><code>aria-valuemax=\"{max}\"</code> -- communicates the maximum allowed value</li>\n<li><code>aria-disabled=\"true\"</code> -- present only when the dial is disabled</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for a rotary control for selecting a value, such as a timer, temperature, or volume knob.</li>\n<li>Use for fine-grained numeric adjustment in control panels, audio interfaces, or configuration screens.</li>\n<li>Use when the knob metaphor is intuitive, such as physical device simulators or audio mixing applications.</li>\n<li>Use when the value range is bounded and continuous.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when a linear slider is more intuitive -- use Slider instead.</li>\n<li>Do not use for angle-specific input in degrees -- use AngleSliderRangeInput instead.</li>\n<li>Do not use when users need to type an exact value -- use NumberInput instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;div&gt;</code> with <code>role=\"slider\"</code>, full ARIA value attributes (<code>aria-valuenow</code>, <code>aria-valuemin</code>, <code>aria-valuemax</code>), keyboard navigation with fine and coarse step support, and two-way binding. The consumer provides all visual styling, the rotary knob appearance, and any value display.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.dial</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>dial</code></li>\n<li>Verify role=\"slider\"` -- indicates a range input control</li>\n<li>Verify aria-label=\"{label}\"` -- provides an accessible name for the dial</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Provide a clear visual indicator of the current value position on the dial. Include a numeric readout near the dial so users can see the exact value.</li>\n<li><strong>Developers</strong>: Use the <code>step</code> prop to match the precision users need. The Shift+Arrow coarse adjustment (10x step) is built in, so choose a base step that makes both fine and coarse increments intuitive.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>dial-group</code> — a group of dial components</li>\n<li><code>slider</code> — a draggable control for selecting a value along a track</li>\n<li><code>angle-slider-range-input</code> — a range input for selecting an angle in degrees</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Slider Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/slider/</li>\n<li>WAI-ARIA slider role: https://www.w3.org/TR/wai-aria-1.2/#slider</li>\n</ul>";
</script>

<svelte:head>
  <title>Dial — Lily Design System</title>
  <meta name="description" content="A circular dial or knob control that allows users to adjust a numeric value within a defined range. Dials provide an intuitive interaction model inspired by physical knobs found on devices like vol..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
