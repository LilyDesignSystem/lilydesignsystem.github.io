<script lang="ts">
  const html: string = "<h1>Tool Bar</h1>\n\n<p>A toolbar is a container for action buttons and controls using the ARIA toolbar role. It provides left/right arrow key navigation between items with Home/End keys to jump to first/last items, implementing the roving focus pattern for efficient keyboard interaction.</p>\n\n<p>The component queries focusable children via <code>button</code>, <code>[role='button']</code>, and <code>[tabindex]</code> selectors, enabling arrow key navigation to wrap around from last to first and vice versa. This allows users to Tab into the toolbar once, then use arrow keys to move between items, and Tab out to continue through the page.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as a <code>&lt;div&gt;</code> with <code>role=\"toolbar\"</code> and built-in keyboard navigation</li>\n<li>Arrow keys (Left/Right) move focus between toolbar items using roving focus</li>\n<li>Home/End keys jump to first/last toolbar item</li>\n<li>Focus wraps around from last item to first and vice versa</li>\n<li>Queries focusable items via <code>button</code>, <code>[role='button']</code>, and <code>[tabindex]</code> selectors</li>\n<li>Uses reactive state for the toolbar element reference and two-way <code>this</code> binding for DOM access</li>\n<li>Accessible name is set via <code>aria-label</code> from the <code>label</code> prop</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name for the toolbar via <code>aria-label</code></li>\n<li><code>children</code>: slot (required) -- toolbar item elements</li>\n<li><code>...restProps</code>: Any additional HTML attributes passed to the <code>&lt;div&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;ToolBar label=\"Text formatting\"&gt;\n  &lt;ToolBarButton&gt;Bold&lt;/ToolBarButton&gt;\n  &lt;ToolBarButton&gt;Italic&lt;/ToolBarButton&gt;\n  &lt;ToolBarButton&gt;Underline&lt;/ToolBarButton&gt;\n  &lt;ToolBarButton disabled&gt;Strikethrough&lt;/ToolBarButton&gt;\n&lt;/ToolBar&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;ToolBar label=\"Image editing controls\"&gt;\n  &lt;ToolBarButton aria-label=\"Crop\"&gt;Crop&lt;/ToolBarButton&gt;\n  &lt;ToolBarButton aria-label=\"Rotate left\"&gt;Rotate&lt;/ToolBarButton&gt;\n  &lt;ToolBarButton aria-label=\"Zoom in\"&gt;Zoom In&lt;/ToolBarButton&gt;\n  &lt;ToolBarButton aria-label=\"Zoom out\"&gt;Zoom Out&lt;/ToolBarButton&gt;\n&lt;/ToolBar&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>ArrowRight: Move focus to next item (wraps to first)</li>\n<li>ArrowLeft: Move focus to previous item (wraps to last)</li>\n<li>Home: Move focus to first item</li>\n<li>End: Move focus to last item</li>\n<li>Tab: Enter/exit the toolbar</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"toolbar\"</code> -- identifies the container as a toolbar widget</li>\n<li><code>aria-label</code> -- provides the accessible name for the toolbar</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for a horizontal bar of tool actions, such as formatting buttons in a text editor or actions on a selected item.</li>\n<li>Use when you have a row of action buttons that benefit from roving focus keyboard navigation.</li>\n<li>Use when you want users to Tab into the toolbar once, arrow between items, and Tab out.</li>\n<li>Use for drawing tools, media controls, image editing, or code editor actions.</li>\n<li>Use with ToolBarButton children for consistent button semantics within the bar.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for navigation -- use TabBar or MenuBar instead.</li>\n<li>Do not use for task shortcuts -- use TaskBar instead.</li>\n<li>Do not use when there are only two or three actions that do not need roving focus -- use a simple button group.</li>\n<li>Do not use for dropdown-style menus -- use Menu or DropdownMenu instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;div&gt;</code> with <code>role=\"toolbar\"</code>, <code>aria-label</code>, and built-in roving focus keyboard navigation (ArrowLeft, ArrowRight, Home, End). It queries focusable children automatically. The consumer provides all button elements as children and all visual styling.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.tool-bar</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>tool-bar</code></li>\n<li>Verify role=\"toolbar\"` -- identifies the container as a toolbar widget</li>\n<li>Verify aria-label` -- provides the accessible name for the toolbar</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Group related actions visually with separators or spacing. Use consistent icon sizes and provide tooltips for icon-only buttons.</li>\n<li><strong>Developers</strong>: Ensure all toolbar items are focusable (<code>&lt;button&gt;</code>, <code>[role=\"button\"]</code>, or elements with <code>tabindex</code>). The component handles focus management automatically.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>ToolBar contains ToolBarButton children following the Bar/BarButton pattern. ToolBar provides the toolbar container with keyboard navigation, and each ToolBarButton provides a single action button within the toolbar.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>tool-bar-button</code> — one action button in a tool bar</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Toolbar Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/toolbar/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"tool-bar\" role=\"toolbar\" aria-label=\"Tools\"><button class=\"tool-bar-button\">Bold</button><button class=\"tool-bar-button\">Italic</button></div>";
  const svelteSource: string = "// In your Svelte component:\nimport ToolBar from \"lily-design-system-svelte-headless/components/ToolBar/ToolBar.svelte\";\n\n<ToolBar>\n  <!-- list children -->\n</ToolBar>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Tool Bar — Lily Design System</title>
  <meta name="description" content="A toolbar is a container for action buttons and controls using the ARIA toolbar role. It provides left/right arrow key navigation between items with Home/End keys to jump to first/last items, imple..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

