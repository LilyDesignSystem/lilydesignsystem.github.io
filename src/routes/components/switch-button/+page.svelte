<script lang="ts">
  const html: string = "<h1>Switch Button</h1>\n\n<p>A switch input is a UI/UX component that provides a binary toggle control, similar to a physical on/off switch. It uses <code>role=\"switch\"</code> with <code>aria-checked</code> to communicate the current state to assistive technologies. The component supports both click and keyboard (Space key) activation for accessibility compliance.</p>\n\n<p>Switch inputs are commonly used for settings, preferences, and feature toggles where users need to enable or disable a single option without submitting a form. Unlike checkboxes, switches provide immediate effect and convey an on/off semantic rather than checked/unchecked.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;button type=\"button\"&gt;</code> element with <code>role=\"switch\"</code> for switch semantics</li>\n<li>Supports two-way binding for the <code>checked</code> prop enabling two-way data binding with two-way <code>checked</code> binding</li>\n<li>Click handler toggles <code>checked</code> when not disabled</li>\n<li>Custom <code>onkeydown</code> handler processes the Space key to toggle state, with <code>preventDefault()</code> to avoid scrolling</li>\n<li>The <code>type=\"button\"</code> prevents accidental form submission</li>\n<li>Supports rest props via <code>...restProps</code> for extensibility (e.g., <code>id</code>, <code>class</code>)</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name applied via <code>aria-label</code></li>\n<li><code>checked</code>: boolean (default: false) -- whether the switch is on, bindable with two-way <code>checked</code> binding</li>\n<li><code>disabled</code>: boolean (default: false) -- whether the switch is disabled</li>\n<li><code>...restProps</code>: spread onto the <code>&lt;button&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Notification toggle in a settings panel:</p>\n\n<pre><code class=\"language-html\">&lt;SwitchButton label=\"Enable notifications\" checked={notificationsEnabled} /&gt;</code></pre>\n\n<p>Dark mode toggle with disabled state when locked by admin:</p>\n\n<pre><code class=\"language-html\">&lt;SwitchButton label=\"Dark mode\" checked={darkMode} disabled={locked} /&gt;</code></pre>\n\n<p>Feature flag toggle in a dashboard:</p>\n\n<pre><code class=\"language-html\">&lt;SwitchButton label=\"Enable beta features\" checked={betaEnabled} /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Space: toggles the switch between on and off states (custom handler with <code>preventDefault</code>)</li>\n<li>Enter: activates the button (native button behavior)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"switch\"</code> -- identifies the element as a two-state toggle switch</li>\n<li><code>aria-checked={checked}</code> -- communicates whether the switch is currently on (<code>true</code>) or off (<code>false</code>)</li>\n<li><code>aria-label={label}</code> -- provides the accessible name for the switch control</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for binary settings that take immediate effect, such as enabling or disabling notifications.</li>\n<li>Use for toggling features on and off, such as dark mode, Wi-Fi, or Bluetooth.</li>\n<li>Use when the on/off semantic is more appropriate than a checked/unchecked semantic.</li>\n<li>Use when a single control needs to toggle a preference without a separate save action.</li>\n<li>Use in settings panels, preference screens, and feature flag dashboards.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for form selections that require submission to take effect -- use CheckboxInput instead.</li>\n<li>Do not use for choosing between two distinct options -- use RadioGroup instead.</li>\n<li>Do not use when the control is part of a group of related options in a form -- use CheckboxGroup instead.</li>\n<li>Do not use for toggling a button's pressed state in a toolbar -- use ToggleButton instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component renders a <code>&lt;button&gt;</code> with <code>role=\"switch\"</code> and <code>aria-checked</code>, providing proper switch semantics and keyboard activation (Space and Enter keys). The consumer provides all visual styling including the track, thumb, on/off indicators, and transition animations.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.switch-button</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;button&gt;</code> element with class <code>switch-button</code></li>\n<li>Verify role=\"switch\"` -- identifies the element as a two-state toggle switch</li>\n<li>Verify aria-label={label}` -- provides the accessible name for the switch control</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Make the on and off states visually distinct using color, position, and optionally text labels. Ensure the switch is large enough for touch targets (at least 44x44px).</li>\n<li><strong>Developers</strong>: Use the two-way bindable <code>checked</code> prop for reactive state. Remember that switches convey immediate effect -- do not use them for settings that require a separate save action.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>toggle-button</code> — a button that toggles between pressed and unpressed states</li>\n<li><code>checkbox-input</code> — a checkbox input for toggling a boolean value &lt;input type=\"checkbox\"&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Switch Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/switch/</li>\n<li>WAI-ARIA Switch Role: https://www.w3.org/TR/wai-aria-1.2/#switch</li>\n</ul>";
</script>

<svelte:head>
  <title>Switch Button — Lily Design System</title>
  <meta name="description" content="A switch input is a UI/UX component that provides a binary toggle control, similar to a physical on/off switch. It uses role=&quot;switch&quot; with aria-checked to communicate the current state to assistive..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
