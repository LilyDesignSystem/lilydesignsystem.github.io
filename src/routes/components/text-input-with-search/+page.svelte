<script lang="ts">
  const html: string = "<h1>Text Input With Search</h1>\n\n<p>TextInputWithSearch is a headless component that wraps a native <code>&lt;input type=\"text\"&gt;</code> and a <code>&lt;button type=\"button\"&gt;</code> inside a <code>&lt;div role=\"search\"&gt;</code>. The search button triggers a callback with the current input value, and pressing Enter in the input also triggers the search.</p>\n\n<p>This component is useful for search bars, filter inputs, lookup fields, and any interface where users type a query and explicitly trigger a search action.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a wrapper <code>&lt;div role=\"search\"&gt;</code> containing a text input and a search button</li>\n<li>The text input is <code>&lt;input type=\"text\"&gt;</code> for entering a search query</li>\n<li>The search button is <code>&lt;button type=\"button\"&gt;</code> that triggers the search callback</li>\n<li>Pressing Enter in the input also triggers the search callback</li>\n<li>Uses <code>aria-label</code> for accessible naming on the region, input, and button</li>\n<li>Supports two-way binding on the <code>value</code> prop</li>\n<li>Spreads <code>restProps</code> onto the wrapper <code>&lt;div&gt;</code> for consumer extensibility</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name for the search region via <code>aria-label</code></li>\n<li><code>inputLabel</code>: string (default: \"Search\") -- accessible name for the text input via <code>aria-label</code></li>\n<li><code>searchLabel</code>: string (default: \"Search\") -- accessible label and text for the search button</li>\n<li><code>value</code>: string (default: \"\") -- bindable text input value</li>\n<li><code>placeholder</code>: string (optional) -- placeholder text for the input</li>\n<li><code>onsearch</code>: <code>(value: string) =&gt; void</code> (optional) -- callback when search is triggered</li>\n<li><code>required</code>: boolean (default: false) -- whether the input is required</li>\n<li><code>disabled</code>: boolean (default: false) -- whether the input and button are disabled</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the wrapper <code>&lt;div&gt;</code></li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Patient lookup with custom labels:</p>\n\n<pre><code class=\"language-html\">&lt;TextInputWithSearch\n  label=\"Patient lookup\"\n  inputLabel=\"Patient name\"\n  searchLabel=\"Find\"\n  value={query}\n  onsearch={handlePatientSearch}\n  placeholder=\"Enter patient name or ID\"\n/&gt;</code></pre>\n\n<p>Address lookup triggering an external API:</p>\n\n<pre><code class=\"language-html\">&lt;TextInputWithSearch\n  label=\"Address search\"\n  inputLabel=\"Postcode or street\"\n  searchLabel=\"Look up\"\n  value={address}\n  onsearch={handleAddressLookup}\n  placeholder=\"e.g. SW1A 1AA\"\n/&gt;</code></pre>\n\n<p>Site-wide search bar with button:</p>\n\n<pre><code class=\"language-html\">&lt;TextInputWithSearch label=\"Site search\" onsearch={handleSiteSearch} placeholder=\"Search...\" /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Moves focus between the text input and search button</li>\n<li>Enter in the text input: Triggers the search callback</li>\n<li>Enter/Space on the search button: Triggers the search callback</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"search\"</code> -- on the wrapper <code>&lt;div&gt;</code> to establish a search landmark</li>\n<li><code>aria-label={label}</code> -- provides an accessible name for the search region</li>\n<li><code>aria-label={inputLabel}</code> -- provides an accessible name for the text input</li>\n<li><code>aria-label={searchLabel}</code> -- provides an accessible name for the search button</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use TextInputWithSearch for a text input with an integrated search action button, such as address lookup or patient search.</li>\n<li>Use TextInputWithSearch for search bars where users explicitly trigger a query by clicking a button or pressing Enter.</li>\n<li>Use TextInputWithSearch for lookup fields where the search action calls an external service.</li>\n<li>Use TextInputWithSearch when the search action has visible side effects, such as navigating to a results page.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use TextInputWithSearch for simple search fields without a button -- use SearchInput instead.</li>\n<li>Do not use TextInputWithSearch for filtering within a fixed list of options -- use Combobox instead.</li>\n<li>Do not use TextInputWithSearch for general text entry without search functionality -- use TextInput instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;div role=\"search\"&gt;</code> wrapping a native <code>&lt;input type=\"text\"&gt;</code> and <code>&lt;button type=\"button\"&gt;</code> with accessible labeling via <code>aria-label</code>, two-way value binding, Enter key support, and a search callback. The consumer provides all visual styling.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.text-input-with-search</code> class for targeting. No default styles are included -- this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a wrapper <code>&lt;div&gt;</code> with class <code>text-input-with-search</code> and <code>role=\"search\"</code></li>\n<li>Verify it contains an <code>&lt;input type=\"text\"&gt;</code> with <code>aria-label</code></li>\n<li>Verify it contains a <code>&lt;button type=\"button\"&gt;</code> with <code>aria-label</code></li>\n<li>Verify clicking the search button calls the <code>onsearch</code> callback with the input value</li>\n<li>Verify pressing Enter in the input calls the <code>onsearch</code> callback</li>\n<li>Verify <code>disabled</code> and <code>required</code> attributes propagate correctly</li>\n<li>Verify pass-through attributes are applied to the wrapper <code>&lt;div&gt;</code></li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Position the search button adjacent to the input. Use a recognizable search icon or text label.</li>\n<li><strong>Developers</strong>: The <code>onsearch</code> callback receives the current input value as a string. Handle empty string searches as appropriate for your use case.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>text-input</code> — a single-line text input field &lt;input type=\"text\"&gt;</li>\n<li><code>search-input</code> — an input for entering a search query &lt;input type=\"search\"&gt;</li>\n<li><code>combobox</code> — a text input combined with a dropdown list for filtering options</li>\n<li><code>autosuggest</code> — a text input that proposes matching options as users type</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN text input: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/text</li>\n<li>WAI-ARIA search role: https://www.w3.org/TR/wai-aria-1.2/#search</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"text-input-with-search\" aria-label=\"a single-line text input field <input type=\"text\"> with search capability\">TextInputWithSearch</div>";
  const svelteSource: string = "// In your Svelte component:\nimport TextInputWithSearch from \"lily-design-system-svelte-headless/components/TextInputWithSearch/TextInputWithSearch.svelte\";\n\n<TextInputWithSearch>\n  <!-- content -->\n</TextInputWithSearch>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Text Input With Search — Lily Design System</title>
  <meta name="description" content="TextInputWithSearch is a headless component that wraps a native &lt;input type=&quot;text&quot;&gt; and a &lt;button type=&quot;button&quot;&gt; inside a &lt;div role=&quot;search&quot;&gt;. The search button triggers a callback with the current..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

