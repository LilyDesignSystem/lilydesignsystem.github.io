<script lang="ts">
  const html: string = "<h1>Gantt Table Row</h1>\n\n<p>A Gantt table row represents a single row within a Gantt chart grid, typically corresponding to one task or milestone. It wraps a task-name cell and time-period cells in a horizontal sequence. It is designed to be used inside a GanttTableHead, GanttTableBody, or GanttTableTfoot section.</p>\n\n<p>The component renders a <code>&lt;tr&gt;</code> element and passes through its children, which are expected to be <code>&lt;td&gt;</code>, <code>&lt;th&gt;</code>, or GanttTableTD elements.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as a <code>&lt;tr&gt;</code> element for one row in a Gantt chart grid</li>\n<li>Children should be a task-name header cell (<code>&lt;th&gt;</code>) followed by time-period data cells (<code>&lt;td&gt;</code> or GanttTableTD)</li>\n<li>Designed to be used inside GanttTableHead, GanttTableBody, or GanttTableTfoot</li>\n<li>Spreads <code>restProps</code> onto the <code>&lt;tr&gt;</code> element for consumer customization</li>\n<li>No internal state -- purely a structural wrapper</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>children</code>: slot (required) -- cells for this row, typically a task name and time-period indicators</li>\n<li><code>...restProps</code>: Any additional HTML attributes passed to the <code>&lt;tr&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;GanttTableBody&gt;\n  &lt;GanttTableTR&gt;\n    &lt;GanttTableTH scope=\"row\"&gt;Design phase&lt;/GanttTableTH&gt;\n    &lt;GanttTableTD active&gt;████&lt;/GanttTableTD&gt;\n    &lt;GanttTableTD active&gt;████&lt;/GanttTableTD&gt;\n    &lt;GanttTableTD&gt;&lt;/GanttTableTD&gt;\n  &lt;/GanttTableTR&gt;\n  &lt;GanttTableTR&gt;\n    &lt;GanttTableTH scope=\"row\"&gt;Development&lt;/GanttTableTH&gt;\n    &lt;GanttTableTD&gt;&lt;/GanttTableTD&gt;\n    &lt;GanttTableTD active&gt;████&lt;/GanttTableTD&gt;\n    &lt;GanttTableTD active&gt;████&lt;/GanttTableTD&gt;\n  &lt;/GanttTableTR&gt;\n&lt;/GanttTableBody&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive container. Keyboard navigation is handled by the parent GanttTable grid.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Implicit <code>row</code> role from the <code>&lt;tr&gt;</code> element -- identifies a row of cells within the grid</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside GanttTable to provide the <code>&lt;tr&gt;</code> row element for a task, header, or summary row.</li>\n<li>Use inside GanttTableHead, GanttTableBody, or GanttTableTfoot to represent a single row in the Gantt chart.</li>\n<li>Use to wrap a task-name cell followed by time-period data cells.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside of a GanttTable structure -- use TableRow or DataTableRow for general tables.</li>\n<li>Do not use for standalone row content outside a table context.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;tr&gt;</code> element with implicit <code>row</code> role for identifying a row within the Gantt chart grid. The consumer provides all visual styling including row height, hover states, and borders.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.gantt-table-tr</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;tr&gt;</code> element with class <code>gantt-table-tr</code></li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Ensure rows have enough height to display bar segments clearly. Add hover states to help users track across wide timelines.</li>\n<li><strong>Developers</strong>: Start each body row with a <code>&lt;th&gt;</code> for the task name, followed by GanttTableTD cells for the time period columns.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>gantt-table</code> — a Gantt chart table interactive grid for planning schedule visualization &lt;table&gt;</li>\n<li><code>gantt-table-thead</code> — see components.tsv</li>\n<li><code>gantt-table-tbody</code> — see components.tsv</li>\n<li><code>gantt-table-tfoot</code> — see components.tsv</li>\n<li><code>gantt-table-th</code> — a Gantt chart table interactive grid header cell for planning schedule visualization &lt;th&gt;</li>\n<li><code>gantt-table-td</code> — a Gantt chart table interactive grid data cell for planning schedule visualization &lt;td&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Grid Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/grid/</li>\n</ul>";
</script>

<svelte:head>
  <title>Gantt Table Row — Lily Design System</title>
  <meta name="description" content="A Gantt table row represents a single row within a Gantt chart grid, typically corresponding to one task or milestone. It wraps a task-name cell and time-period cells in a horizontal sequence. It i..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
