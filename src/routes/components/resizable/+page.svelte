<script lang="ts">
  const html: string = "<h1>Resizable</h1>\n\n<p>Resizable is a headless container that allows users to resize its contents in one or both directions. It provides a focusable region with an accessible label, enabling keyboard users to interact with the resizable area.</p>\n\n<p>Use Resizable when you need a panel, pane, or content area that users can resize, such as a code editor panel, a sidebar, or an adjustable preview area. The component exposes a <code>data-resize</code> attribute indicating the resize direction, which consumers can use to apply CSS <code>resize</code> property styling. The component itself is headless and applies no visual styling.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> element with <code>role=\"region\"</code> for landmark semantics</li>\n<li>Sets <code>tabindex=\"0\"</code> to make the container keyboard-focusable</li>\n<li>Exposes a <code>data-resize</code> attribute with the value of the <code>direction</code> prop (<code>\"both\"</code>, <code>\"horizontal\"</code>, or <code>\"vertical\"</code>)</li>\n<li>Consumer is responsible for applying CSS <code>resize</code> and <code>overflow</code> properties based on the <code>data-resize</code> attribute</li>\n<li>Spreads <code>restProps</code> onto the div for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible label for the resizable region, applied via <code>aria-label</code></li>\n<li><code>direction</code>: <code>\"both\" | \"horizontal\" | \"vertical\"</code> (default: <code>\"both\"</code>) -- which directions the container can be resized</li>\n<li><code>children</code>: slot (required) -- content to render inside the resizable container</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;div&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Resizable code editor panel:</p>\n\n<pre><code class=\"language-html\">&lt;Resizable label=\"Code editor\" direction=\"both\"&gt;\n  &lt;pre&gt;&lt;code&gt;const x = 42;&lt;/code&gt;&lt;/pre&gt;\n&lt;/Resizable&gt;</code></pre>\n\n<p>Horizontally resizable sidebar:</p>\n\n<pre><code class=\"language-html\">&lt;Resizable label=\"Sidebar navigation\" direction=\"horizontal\"&gt;\n  &lt;nav&gt;\n    &lt;a href=\"/dashboard\"&gt;Dashboard&lt;/a&gt;\n    &lt;a href=\"/settings\"&gt;Settings&lt;/a&gt;\n  &lt;/nav&gt;\n&lt;/Resizable&gt;</code></pre>\n\n<p>Vertically resizable preview area:</p>\n\n<pre><code class=\"language-html\">&lt;Resizable label=\"Document preview\" direction=\"vertical\"&gt;\n  &lt;iframe src=\"/preview\" title=\"Document preview\"&gt;&lt;/iframe&gt;\n&lt;/Resizable&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive container. The consumer is responsible for implementing any keyboard-driven resize behavior. The <code>tabindex=\"0\"</code> ensures the region is focusable for screen reader navigation.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"region\"</code> -- identifies the container as a landmark region for assistive technologies</li>\n<li><code>aria-label={label}</code> -- provides an accessible name describing the resizable area</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use Resizable for adjustable panels such as code editors, sidebars, or preview areas where users benefit from controlling the content size.</li>\n<li>Use Resizable when layout flexibility is important and content may need more or less space depending on the task.</li>\n<li>Use Resizable for dashboard widgets that users can expand or shrink to prioritize information.</li>\n<li>Use Resizable for image or document preview panes that benefit from manual sizing.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use Resizable for fixed-layout content that should not change size.</li>\n<li>Do not use Resizable for splitting two adjacent panels that share available space -- use Splitter instead.</li>\n<li>Do not use Resizable for simple visual dividers between sections -- use Separator instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>The Resizable headless component provides a <code>&lt;div&gt;</code> with <code>role=\"region\"</code>, <code>aria-label</code>, <code>tabindex=\"0\"</code> for keyboard focus, and a <code>data-resize</code> attribute indicating the allowed direction. The consumer provides all visual styling including CSS <code>resize</code> and <code>overflow</code> properties, drag handles, and dimension constraints.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.resizable</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>resizable</code></li>\n<li>Verify role=\"region\"` -- identifies the container as a landmark region for assistive technologies</li>\n<li>Verify aria-label={label}` -- provides an accessible name describing the resizable area</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Provide a visible resize handle or grip indicator so users know the area is resizable, and set sensible minimum/maximum dimensions.</li>\n<li><strong>Developers</strong>: Apply <code>overflow: auto</code> and the appropriate CSS <code>resize</code> value (<code>both</code>, <code>horizontal</code>, or <code>vertical</code>) based on the <code>data-resize</code> attribute.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>splitter</code> — a draggable divider for resizing adjacent panels</li>\n<li><code>split-view</code> — a two-panel resizable layout container with a draggable divider between them</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Region Role: https://www.w3.org/TR/wai-aria-1.2/#region</li>\n<li>WAI-ARIA Authoring Practices - Window Splitter: https://www.w3.org/WAI/ARIA/apg/patterns/windowsplitter/</li>\n</ul>";
</script>

<svelte:head>
  <title>Resizable — Lily Design System</title>
  <meta name="description" content="Resizable is a headless container that allows users to resize its contents in one or both directions. It provides a focusable region with an accessible label, enabling keyboard users to interact wi..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
