<script lang="ts">
  const html: string = "<h1>Section Heading</h1>\n\n<p>A section heading is a <code>&lt;header&gt;</code> introducing a major content section. It pairs the main heading with an optional eyebrow above and an optional subtitle below, while letting the consumer choose the heading level (<code>&lt;h2&gt;</code>–<code>&lt;h6&gt;</code>) to match document hierarchy.</p>\n\n<p>Use it to introduce sections within a page, articles within a feed, or panels within a dashboard.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses a native <code>&lt;header&gt;</code> element with class <code>section-heading</code></li>\n<li><code>heading</code> is <strong>required</strong> and renders as <code>&lt;h{level}&gt;</code> (default <code>&lt;h2&gt;</code>)</li>\n<li><code>level</code> accepts <code>2 | 3 | 4 | 5 | 6</code> so the heading fits the surrounding outline</li>\n<li><code>eyebrow</code> renders as a <code>&lt;p class=\"section-heading-eyebrow\"&gt;</code> above the heading</li>\n<li><code>subtitle</code> renders as a <code>&lt;p class=\"section-heading-subtitle\"&gt;</code> below the heading</li>\n<li>No JavaScript behavior; the component is passive</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>heading</code>: string (<strong>required</strong>) -- main heading text</li>\n<li><code>eyebrow</code>: string (optional) -- eyebrow text above the heading</li>\n<li><code>subtitle</code>: string (optional) -- subtitle text below the heading</li>\n<li><code>level</code>: 2 | 3 | 4 | 5 | 6 (default: 2) -- heading level</li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Basic section heading:</p>\n\n<pre><code class=\"language-html\">&lt;SectionHeading heading=\"Our principles\" /&gt;</code></pre>\n\n<p>With eyebrow and subtitle, h3 level:</p>\n\n<pre><code class=\"language-html\">&lt;SectionHeading\n  level={3}\n  eyebrow=\"Why Lily\"\n  heading=\"Designed for healthcare\"\n  subtitle=\"Patient-first, clinician-tested, accessible by default.\"\n/&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>None (passive element)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Implicit <code>banner</code>-like grouping from <code>&lt;header&gt;</code> (note: <code>&lt;header&gt;</code> only acts as a banner landmark when it is a direct child of the <code>&lt;body&gt;</code>; inside a <code>&lt;section&gt;</code> or <code>&lt;article&gt;</code> it is just a generic header element)</li>\n<li>The heading element provides hierarchical document structure and is exposed via the heading role</li>\n<li>No additional ARIA needed; semantics come from the chosen heading element</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to introduce a section, article, or panel with consistent typography.</li>\n<li>Use when the section benefits from a small \"eyebrow\" label and/or a supporting subtitle.</li>\n<li>Use to maintain a clean heading outline by passing the appropriate <code>level</code>.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for the page title — use a top-level <code>&lt;h1&gt;</code> directly.</li>\n<li>Do not use for body paragraphs that look like a heading; use the correct heading element.</li>\n<li>Do not skip heading levels (e.g., from <code>&lt;h2&gt;</code> to <code>&lt;h4&gt;</code>); pick the correct <code>level</code> to match the document outline.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>Renders a <code>&lt;header&gt;</code> containing optional eyebrow <code>&lt;p&gt;</code>, the chosen heading element with the heading text, and an optional subtitle <code>&lt;p&gt;</code>. Visual typography, spacing, alignment, and decorative treatments are entirely the consumer's responsibility.</p>\n\n<h2>Styles</h2>\n\n<p>Consumer CSS targets the <code>section-heading</code> class on the <code>&lt;header&gt;</code> and the inner <code>section-heading-eyebrow</code>, <code>section-heading-heading</code>, and <code>section-heading-subtitle</code> classes for fine-grained styling.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;header&gt;</code> element with class <code>section-heading</code></li>\n<li>Verify the heading renders as <code>&lt;h{level}&gt;</code> matching the <code>level</code> prop (default <code>&lt;h2&gt;</code>)</li>\n<li>Verify the eyebrow renders as <code>&lt;p class=\"section-heading-eyebrow\"&gt;</code> only when provided</li>\n<li>Verify the subtitle renders as <code>&lt;p class=\"section-heading-subtitle\"&gt;</code> only when provided</li>\n<li>Verify <code>level</code> validation accepts only 2 through 6</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use the eyebrow to label the section's category or theme. Keep the subtitle short — it is supporting text, not a paragraph.</li>\n<li><strong>Developers</strong>: Always pass the correct <code>level</code> to keep heading hierarchy intact (the page should have one <code>&lt;h1&gt;</code>; sections inside should be <code>&lt;h2&gt;</code>, subsections <code>&lt;h3&gt;</code>, and so on).</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>headline</code> — see components.tsv</li>\n<li><code>hero-headline</code> — see components.tsv</li>\n<li><code>byline</code> — see components.tsv</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN <code>&lt;header&gt;</code>: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/header</li>\n<li>MDN Heading Elements: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/Heading_Elements</li>\n<li>WCAG 2.4.6 Headings and Labels: https://www.w3.org/WAI/WCAG22/Understanding/headings-and-labels</li>\n<li>Mozilla Protocol Design System: https://protocol.mozilla.org/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"section-heading\" aria-label=\"a styled heading introducing a major content section, with optional eyebrow and subtitle\">SectionHeading</div>";
  const svelteSource: string = "// In your Svelte component:\nimport SectionHeading from \"lily-design-system-svelte-headless/components/SectionHeading/SectionHeading.svelte\";\n\n<SectionHeading>\n  <!-- content -->\n</SectionHeading>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Section Heading — Lily Design System</title>
  <meta name="description" content="A section heading is a &lt;header&gt; introducing a major content section. It pairs the main heading with an optional eyebrow above and an optional subtitle below, while letting the consumer choose the h..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

