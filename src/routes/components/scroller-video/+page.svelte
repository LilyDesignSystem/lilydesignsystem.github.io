<script lang="ts">
  const html: string = "<h1>ScrollerVideo</h1>\n\n<p>A video-driven scrollytelling component with frame-by-frame scrubbing. The video playback position maps to scroll position, allowing the user to scrub through a video by scrolling while foreground step content overlays the video.</p>\n\n<p>This headless component uses a <code>&lt;div&gt;</code> element with an <code>aria-label</code> and a muted video element whose playback is driven entirely by scroll position.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses <code>&lt;div&gt;</code> element with class <code>scroller-video</code></li>\n<li>Video playback position maps to scroll position</li>\n<li>Video scrubs frame-by-frame as user scrolls</li>\n<li>Can use WebCodecs API for smooth frame extraction where supported</li>\n<li>Falls back to standard video currentTime seeking</li>\n<li>Video is muted (scroll-driven, not time-driven)</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>src</code>: string (required) -- video source URL</li>\n<li><code>children</code>: slot (required) -- foreground step content overlaid on video</li>\n<li><code>offset</code>: number (default: 0.5) -- viewport offset where step triggers</li>\n<li><code>index</code>: number (bindable, default: 0) -- current active step index</li>\n<li><code>progress</code>: number (bindable, default: 0) -- overall scroll progress (0-1)</li>\n<li><code>label</code>: string (required) -- accessible label describing the video story</li>\n<li><code>alt</code>: string (required) -- text alternative describing the video content</li>\n<li><code>...restProps</code>: Any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;ScrollerVideo\n  src=\"/videos/climate-story.mp4\"\n  label=\"Rising sea levels\"\n  alt=\"Animation showing coastal flooding progression from 2020 to 2050\"\n  bind:progress\n&gt;\n  &lt;div class=\"step\"&gt;&lt;p&gt;Current sea levels&lt;/p&gt;&lt;/div&gt;\n  &lt;div class=\"step\"&gt;&lt;p&gt;Projected 2030 levels&lt;/p&gt;&lt;/div&gt;\n  &lt;div class=\"step\"&gt;&lt;p&gt;Projected 2050 levels&lt;/p&gt;&lt;/div&gt;\n&lt;/ScrollerVideo&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;ScrollerVideo\n  src=\"/videos/cell-division.mp4\"\n  label=\"Cell division process\"\n  alt=\"Microscope footage showing the stages of mitosis\"\n  bind:index\n  bind:progress\n&gt;\n  &lt;div class=\"step\"&gt;&lt;p&gt;Prophase: chromosomes condense.&lt;/p&gt;&lt;/div&gt;\n  &lt;div class=\"step\"&gt;&lt;p&gt;Metaphase: chromosomes align.&lt;/p&gt;&lt;/div&gt;\n  &lt;div class=\"step\"&gt;&lt;p&gt;Anaphase: chromosomes separate.&lt;/p&gt;&lt;/div&gt;\n  &lt;div class=\"step\"&gt;&lt;p&gt;Telophase: cell divides.&lt;/p&gt;&lt;/div&gt;\n&lt;/ScrollerVideo&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Standard scroll behavior drives video position</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label</code> on container describing the video story</li>\n<li><code>role=\"img\"</code> with <code>aria-roledescription=\"scrollable video\"</code> on video area</li>\n<li><code>alt</code> text provides a text alternative for non-visual access</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for scroll-driven video narratives where the video illustrates a progression</li>\n<li>Use when a video should advance frame-by-frame in sync with the reader's scroll position</li>\n<li>Use for data journalism, educational content, or storytelling with video evidence</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for standard video playback -- use a video player instead</li>\n<li>Do not use when video is supplementary rather than central to the narrative</li>\n<li>Do not use for short clips that are better served by autoplay or click-to-play</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides a <code>&lt;div&gt;</code> element with scroll-driven video playback and foreground step composition, with zero visual styling. The consumer is responsible for all CSS including video sizing, foreground overlay positioning, step spacing, and responsive layout.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.scroller-video</code> class for targeting. No default styles are included -- this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>scroller-video</code></li>\n<li>Verify <code>aria-label</code> is applied</li>\n<li>Verify <code>alt</code> text is provided for non-visual access</li>\n<li>Verify video is muted</li>\n<li>Verify video playback position maps to scroll position</li>\n<li>Verify index updates as steps scroll into view</li>\n<li>Verify progress updates as the user scrolls</li>\n<li>Verify <code>role=\"img\"</code> and <code>aria-roledescription=\"scrollable video\"</code> are on the video area</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Choose videos that communicate a clear progression. Ensure foreground text is readable over the video at every frame. Consider providing a static fallback image for users who prefer reduced motion.</li>\n<li><strong>Developers</strong>: Always provide both <code>label</code> and <code>alt</code> props for accessibility. The <code>alt</code> text should describe the overall video content, not individual frames. Test with WebCodecs API support and the currentTime fallback. Ensure the video file is optimized for scrubbing (short GOP length).</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>scroller</code> — see components.tsv</li>\n<li><code>scroller-base</code> — see components.tsv</li>\n<li><code>video-player</code> — see components.tsv</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>Reuters Graphics ScrollerVideo</li>\n<li>WebCodecs API: https://developer.mozilla.org/en-US/docs/Web/API/WebCodecs_API</li>\n<li>ScrollerBase component: ../scroller-base/index.md</li>\n</ul>";
</script>

<svelte:head>
  <title>ScrollerVideo — Lily Design System</title>
  <meta name="description" content="A video-driven scrollytelling component with frame-by-frame scrubbing. The video playback position maps to scroll position, allowing the user to scrub through a video by scrolling while foreground ..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
