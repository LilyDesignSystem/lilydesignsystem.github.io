<script lang="ts">
  const html: string = "<h1>Currency Input</h1>\n\n<p>CurrencyInput is a headless locale-aware text input for entering currency values with automatic formatting, currency symbols, thousand separators, and correct decimal places. It supports 40+ world currencies including USD, EUR, GBP, and JPY.</p>\n\n<p>Use CurrencyInput for payment forms, price fields, budget inputs, and any form field that accepts monetary values. The component uses <code>inputmode=\"decimal\"</code> for appropriate mobile keyboards and formats the display value while maintaining a clean numeric value for form submission.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a native <code>&lt;input&gt;</code> element with <code>type=\"text\"</code> and <code>inputmode=\"decimal\"</code></li>\n<li>Uses <code>aria-label</code> for an accessible name describing the input purpose</li>\n<li>The <code>value</code> prop uses two-way binding with an initial undefined state</li>\n<li>Supports <code>min</code> and <code>max</code> value constraints</li>\n<li>Supports <code>required</code> and <code>disabled</code> boolean states</li>\n<li>Spreads <code>restProps</code> onto the input for consumer customization (e.g., <code>id</code>, <code>name</code>, <code>placeholder</code>)</li>\n<li>The <code>currencyCode</code> prop accepts ISO 4217 currency codes (e.g., \"USD\", \"EUR\", \"GBP\", \"JPY\")</li>\n<li>Formatting is handled by the consumer or by using the Intl.NumberFormat API</li>\n<li>pattern: <code>two-way binding</code> for the value prop</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name for the input via <code>aria-label</code></li>\n<li><code>value</code>: number | undefined (default: <code>undefined</code>) -- current numeric value; bindable with two-way binding</li>\n<li><code>currencyCode</code>: string (default: <code>\"USD\"</code>) -- ISO 4217 currency code for formatting</li>\n<li><code>min</code>: number (optional) -- minimum allowed value</li>\n<li><code>max</code>: number (optional) -- maximum allowed value</li>\n<li><code>required</code>: boolean (default: <code>false</code>) -- whether the input is required</li>\n<li><code>disabled</code>: boolean (default: <code>false</code>) -- whether the input is disabled</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;input&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Basic USD price input:</p>\n\n<pre><code class=\"language-html\">&lt;CurrencyInput label=\"Price\" value={amount} currencyCode=\"USD\" /&gt;</code></pre>\n\n<p>EUR budget input with min/max constraints:</p>\n\n<pre><code class=\"language-html\">&lt;CurrencyInput label=\"Annual budget\" value={budget} currencyCode=\"EUR\" min={0} max={10000} /&gt;</code></pre>\n\n<p>JPY total input (no decimal places):</p>\n\n<pre><code class=\"language-html\">&lt;CurrencyInput label=\"Total\" value={total} currencyCode=\"JPY\" required /&gt;</code></pre>\n\n<p>Currency input in a payment form:</p>\n\n<pre><code class=\"language-html\">&lt;Form label=\"Payment details\" onsubmit={handlePayment}&gt;\n  &lt;Field label=\"Payment amount\" required&gt;\n    &lt;CurrencyInput label=\"Payment amount\" value={paymentAmount} currencyCode=\"GBP\" min={1} required /&gt;\n  &lt;/Field&gt;\n  &lt;Field label=\"Tip (optional)\"&gt;\n    &lt;CurrencyInput label=\"Tip amount\" value={tipAmount} currencyCode=\"GBP\" min={0} /&gt;\n  &lt;/Field&gt;\n  &lt;Button type=\"submit\"&gt;Pay now&lt;/Button&gt;\n&lt;/Form&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None at the component level. Keyboard behavior is handled natively by the browser's text input implementation. Consumers may add increment/decrement via arrow keys if desired.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides an accessible name describing the purpose of the currency input</li>\n<li><code>inputmode=\"decimal\"</code> -- triggers a numeric keyboard with decimal point on mobile devices</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for form fields that accept monetary values, such as prices, budgets, payment amounts, or salary fields.</li>\n<li>Use when you need locale-aware currency formatting with proper symbols and separators.</li>\n<li>Use in checkout flows, invoicing forms, or financial dashboards where currency context matters.</li>\n<li>Use when the ISO 4217 currency code needs to be associated with the input value.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for non-currency numbers -- use NumberInput instead.</li>\n<li>Do not use for displaying formatted amounts in read-only context -- format the text inline instead.</li>\n<li>Do not use for entering quantities or measurements -- use NumberInput or MeasurementInstanceInput instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component wraps a native <code>&lt;input type=\"text\"&gt;</code> with <code>inputmode=\"decimal\"</code>, <code>aria-label</code> for accessible naming, two-way bindable value, and optional <code>min</code>, <code>max</code> constraints. The consumer provides all visual styling, including currency symbol display, input dimensions, borders, and validation error display. Formatting logic (thousand separators, decimal places, currency symbols) can be implemented by the consumer using Intl.NumberFormat or similar APIs.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.currency-input</code> class for targeting. Common styling concerns include right-aligned text, currency symbol positioning (prefix or suffix), and <code>font-variant-numeric: tabular-nums</code> for consistent digit widths.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders an <code>&lt;input&gt;</code> element with <code>type=\"text\"</code></li>\n<li>Verify <code>aria-label</code> is set from the <code>label</code> prop</li>\n<li>Verify <code>inputmode=\"decimal\"</code> is present for mobile keyboard support</li>\n<li>Verify <code>disabled</code> and <code>required</code> attributes propagate correctly</li>\n<li>Verify pass-through attributes are applied to the input</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Display the currency symbol clearly near the input. Use right-aligned text and tabular numbers for consistent formatting. Show the expected currency format as placeholder text.</li>\n<li><strong>Developers</strong>: Use <code>Intl.NumberFormat</code> with the appropriate locale and currency code for formatting display values. Be aware that different currencies have different decimal place requirements (e.g., JPY uses 0, USD uses 2). Validate and clamp values on blur rather than on every keystroke.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>text-input</code> — a single-line text input field &lt;input type=\"text\"&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>HTML text input: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/text</li>\n<li>Intl.NumberFormat: https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Intl/NumberFormat</li>\n<li>ISO 4217 Currency Codes: https://www.iso.org/iso-4217-currency-codes.html</li>\n</ul>";
</script>

<svelte:head>
  <title>Currency Input — Lily Design System</title>
  <meta name="description" content="CurrencyInput is a headless locale-aware text input for entering currency values with automatic formatting, currency symbols, thousand separators, and correct decimal places. It supports 40+ world ..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
