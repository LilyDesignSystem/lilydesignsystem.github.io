<script lang="ts">
  const html: string = "<h1>Calendar Table</h1>\n\n<p>A calendar board is a component that presents dates in a structured grid layout, typically organized by weeks and days, allowing users to view and interact with calendar data at a glance. Commonly used in scheduling applications, event planners, and dashboard interfaces, the calendar board renders dates as cells within a table grid, making it easy to scan across days, weeks, or months.</p>\n\n<p>This headless component renders a <code>&lt;table&gt;</code> element with <code>role=\"grid\"</code> and an accessible label. The consumer provides the table rows and cells as children, along with any header rows for day-of-week labels.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;table&gt;</code> element with <code>role=\"grid\"</code> for structured calendar grid semantics</li>\n<li>Consumer provides <code>&lt;tr&gt;</code> rows with <code>&lt;td&gt;</code> or <code>&lt;th&gt;</code> cells as children</li>\n<li>The <code>label</code> prop should describe the calendar period (e.g., \"January 2025\")</li>\n<li>Accepts <code>...restProps</code> for forwarding additional attributes to the table element</li>\n<li>No internal state -- purely a structural wrapper</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name describing the calendar period (e.g., month and year), applied via <code>aria-label</code></li>\n<li><code>children</code>: slot (required) -- table rows and cells representing the calendar grid</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;CalendarTable label=\"April 2026\"&gt;\n  &lt;CalendarTableHead&gt;\n    &lt;CalendarTableRow&gt;\n      &lt;th&gt;Mon&lt;/th&gt;&lt;th&gt;Tue&lt;/th&gt;&lt;th&gt;Wed&lt;/th&gt;&lt;th&gt;Thu&lt;/th&gt;&lt;th&gt;Fri&lt;/th&gt;&lt;th&gt;Sat&lt;/th&gt;&lt;th&gt;Sun&lt;/th&gt;\n    &lt;/CalendarTableRow&gt;\n  &lt;/CalendarTableHead&gt;\n  &lt;CalendarTableBody&gt;\n    &lt;CalendarTableRow&gt;\n      &lt;CalendarTableTD&gt;1&lt;/CalendarTableTD&gt;\n      &lt;CalendarTableTD&gt;2&lt;/CalendarTableTD&gt;\n      &lt;CalendarTableTD&gt;3&lt;/CalendarTableTD&gt;\n      &lt;CalendarTableTD&gt;4&lt;/CalendarTableTD&gt;\n      &lt;CalendarTableTD&gt;5&lt;/CalendarTableTD&gt;\n      &lt;CalendarTableTD&gt;6&lt;/CalendarTableTD&gt;\n      &lt;CalendarTableTD&gt;7&lt;/CalendarTableTD&gt;\n    &lt;/CalendarTableRow&gt;\n    &lt;CalendarTableRow&gt;\n      &lt;CalendarTableTD today selected&gt;8&lt;/CalendarTableTD&gt;\n      &lt;CalendarTableTD&gt;9&lt;/CalendarTableTD&gt;\n      &lt;CalendarTableTD&gt;10&lt;/CalendarTableTD&gt;\n      &lt;CalendarTableTD&gt;11&lt;/CalendarTableTD&gt;\n      &lt;CalendarTableTD&gt;12&lt;/CalendarTableTD&gt;\n      &lt;CalendarTableTD&gt;13&lt;/CalendarTableTD&gt;\n      &lt;CalendarTableTD&gt;14&lt;/CalendarTableTD&gt;\n    &lt;/CalendarTableRow&gt;\n  &lt;/CalendarTableBody&gt;\n  &lt;CalendarTableFoot&gt;\n    &lt;CalendarTableRow&gt;\n      &lt;td colspan=\"7\"&gt;3 events this week&lt;/td&gt;\n    &lt;/CalendarTableRow&gt;\n  &lt;/CalendarTableFoot&gt;\n&lt;/CalendarTable&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None built-in -- the consumer should implement grid keyboard navigation on cells as needed (arrow keys for cell-to-cell movement, Enter/Space for selection).</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"grid\"</code> -- identifies the table as an interactive grid widget</li>\n<li><code>aria-label={label}</code> -- provides an accessible name describing the calendar period</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to display an interactive calendar grid for managing dates, scheduling, and date selection</li>\n<li>Use when users need to view and interact with dates in a monthly or weekly grid format</li>\n<li>Use in scheduling applications, event planners, and dashboard calendars where a visual date layout is needed</li>\n<li>Use as the container for CalendarTableHead, CalendarTableBody, CalendarTableFoot, CalendarTableRow, and CalendarTableTD</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for displaying general tabular data -- use Table or DataTable instead</li>\n<li>Do not use for date range selection without a grid -- use CalendarRangePicker for start-and-end-date picking</li>\n<li>Do not use for simple date entry where users already know the date -- use DateInput for a compact input field</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides a <code>&lt;table&gt;</code> element with <code>role=\"grid\"</code> and <code>aria-label</code> for accessible calendar grid semantics, with zero visual styling. The consumer is responsible for all CSS including cell sizing, grid lines, day-of-week header styling, selected/today state highlights, and month navigation controls.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.calendar-table</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;table&gt;</code> element with class <code>calendar-table</code></li>\n<li>Verify role=\"grid\"` -- identifies the table as an interactive grid widget</li>\n<li>Verify aria-label={label}` -- provides an accessible name describing the calendar period</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Clearly distinguish today's date and selected dates from other days. Use consistent cell sizing and ensure enough contrast for day numbers.</li>\n<li><strong>Developers</strong>: Implement arrow key navigation between cells, Enter/Space for selection, and ensure the <code>label</code> prop describes the calendar period (e.g., \"January 2025\") for screen readers.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>CalendarTable follows the Table composition pattern:</p>\n\n<ul>\n<li><strong>CalendarTable</strong> -- outer <code>&lt;table&gt;</code> with <code>role=\"grid\"</code> and <code>aria-label</code>.</li>\n<li><strong>CalendarTableHead</strong> -- <code>&lt;thead&gt;</code> containing day-of-week column headers.</li>\n<li><strong>CalendarTableBody</strong> -- <code>&lt;tbody&gt;</code> containing rows of day cells.</li>\n<li><strong>CalendarTableFoot</strong> -- <code>&lt;tfoot&gt;</code> for optional summary or navigation content.</li>\n<li><strong>CalendarTableRow</strong> -- <code>&lt;tr&gt;</code> representing one week.</li>\n<li><strong>CalendarTableCell</strong> -- <code>&lt;td&gt;</code> representing one day with selection and today state.</li>\n</ul>\n\n<pre><code class=\"language-html\">&lt;CalendarTable label=\"January 2025\"&gt;\n  &lt;CalendarTableHead&gt;\n    &lt;CalendarTableRow&gt;&lt;th&gt;Sun&lt;/th&gt;&lt;th&gt;Mon&lt;/th&gt;&lt;th&gt;Tue&lt;/th&gt;&lt;/CalendarTableRow&gt;\n  &lt;/CalendarTableHead&gt;\n  &lt;CalendarTableBody&gt;\n    &lt;CalendarTableRow&gt;\n      &lt;CalendarTableCell&gt;1&lt;/CalendarTableCell&gt;\n      &lt;CalendarTableCell today selected&gt;2&lt;/CalendarTableCell&gt;\n      &lt;CalendarTableCell&gt;3&lt;/CalendarTableCell&gt;\n    &lt;/CalendarTableRow&gt;\n  &lt;/CalendarTableBody&gt;\n&lt;/CalendarTable&gt;</code></pre>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>calendar-table-body</code> — a calendar table interactive grid tbody for managing dates, days, etc. &lt;tbody&gt;</li>\n<li><code>calendar-table-foot</code> — a calendar table interactive grid tfoot for managing dates, days, etc. &lt;tfoot&gt;</li>\n<li><code>calendar-table-head</code> — a calendar table interactive grid thead for managing dates, days, etc. &lt;thead&gt;</li>\n<li><code>calendar-table-row</code> — a calendar table interactive grid row for managing dates, days, etc. &lt;tr&gt;</li>\n<li><code>calendar-table-td</code> — a calendar table interactive grid data cell for managing dates, days, etc. &lt;td&gt;</li>\n<li><code>calendar-table-th</code> — a calendar table interactive grid header cell for managing dates, days, etc. &lt;th&gt;</li>\n<li><code>table</code> — a table with rows and columns &lt;table&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Grid Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/grid/</li>\n</ul>";
</script>

<svelte:head>
  <title>Calendar Table — Lily Design System</title>
  <meta name="description" content="A calendar board is a component that presents dates in a structured grid layout, typically organized by weeks and days, allowing users to view and interact with calendar data at a glance. Commonly ..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
