<script lang="ts">
  const html: string = "<h1>Mockup Phone</h1>\n\n<p>A mockup phone is a box area that looks like a mobile phone. It provides a visual frame that mimics a phone device, allowing designers and developers to showcase mobile content, app screenshots, or responsive designs within a realistic phone-shaped container.</p>\n\n<p>This headless component uses a <code>&lt;div&gt;</code> element as the phone frame container. The consumer provides the phone screen content as children.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> element as the phone frame container</li>\n<li><code>role=\"img\"</code> with <code>aria-label</code> identifies the mockup as a decorative phone frame</li>\n<li>Consumer provides the screen content as children</li>\n<li>The component provides semantic structure only; all phone visual styling (bezel, notch, rounded corners, screen area) is the consumer's responsibility</li>\n<li>Spreads <code>restProps</code> onto the <code>&lt;div&gt;</code> for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name describing the phone mockup (e.g., \"Phone showing app home screen\")</li>\n<li><code>children</code>: slot (required) -- content displayed within the phone screen area</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;div&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Basic phone mockup:</p>\n\n<pre><code class=\"language-html\">&lt;MockupPhonePortrait label=\"Phone showing app home screen\"&gt;\n  &lt;img src=\"screenshot.png\" alt=\"App home screen\" /&gt;\n&lt;/MockupPhonePortrait&gt;</code></pre>\n\n<p>Phone mockup with live content:</p>\n\n<pre><code class=\"language-html\">&lt;MockupPhonePortrait label=\"Phone showing login form\"&gt;\n  &lt;form&gt;\n    &lt;label for=\"email\"&gt;Email&lt;/label&gt;\n    &lt;input type=\"email\" id=\"email\" /&gt;\n    &lt;button type=\"submit\"&gt;Sign In&lt;/button&gt;\n  &lt;/form&gt;\n&lt;/MockupPhonePortrait&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus moves to interactive elements within the phone content</li>\n<li>(The phone frame itself is not interactive; keyboard interactions depend on the content within)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"img\"</code> with <code>aria-label</code> identifies the mockup as a phone frame representation</li>\n<li>Screen readers announce the label to describe the mockup context</li>\n<li>Interactive content within the mockup remains fully accessible</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for showcasing mobile app designs, screenshots, or responsive layouts within a phone-shaped frame.</li>\n<li>Use in design documentation, marketing pages, or portfolio sites to present mobile content.</li>\n<li>Use in component libraries to demonstrate mobile-specific components.</li>\n<li>Use in app store listing previews or promotional materials.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for functional mobile previews -- test on real devices or browser device emulation instead.</li>\n<li>Do not use for tablet content -- use MockupTabletPortrait or MockupTabletLandscape instead.</li>\n<li>Do not use for desktop or browser content -- use MockupLaptop or MockupBrowser instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides a <code>&lt;div&gt;</code> with <code>role=\"img\"</code> and <code>aria-label</code> for the phone frame, with zero visual styling. The consumer is responsible for all CSS including the phone bezel shape, screen area, notch or camera cutout, rounded corners, aspect ratio, shadow, and responsive sizing.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.mockup-phone-portrait</code> class for targeting. No default styles are included -- this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>mockup-phone-portrait</code></li>\n<li>Verify <code>role=\"img\"</code> with <code>aria-label</code> for the phone frame</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Maintain a realistic phone aspect ratio (approximately 9:19.5 for modern smartphones). Include visual details like a bezel, notch, and rounded corners. Ensure the screen content area is clearly defined and the mockup scales well across viewport sizes.</li>\n<li><strong>Developers</strong>: Always provide a descriptive <code>label</code> prop that explains what the phone mockup is showing. Ensure any interactive content inside the mockup remains keyboard accessible and does not trap focus.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>mockup-browser</code> — a box area that looks like a web browser</li>\n<li><code>mockup-laptop</code> — a box area that looks like a laptop computer</li>\n<li><code>mockup-phone-landscape</code> — a box area that looks like a mobile phone in landscape orientation</li>\n<li><code>mockup-shell</code> — a box area that looks like a terminal shell</li>\n<li><code>mockup-tablet-landscape</code> — a box area that looks like a tablet computer in landscape mode</li>\n<li><code>mockup-tablet-portrait</code> — a box area that looks like a tablet computer in portrait mode</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA img Role: https://www.w3.org/TR/wai-aria-1.2/#img</li>\n</ul>";
</script>

<svelte:head>
  <title>Mockup Phone — Lily Design System</title>
  <meta name="description" content="A mockup phone is a box area that looks like a mobile phone. It provides a visual frame that mimics a phone device, allowing designers and developers to showcase mobile content, app screenshots, or..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
