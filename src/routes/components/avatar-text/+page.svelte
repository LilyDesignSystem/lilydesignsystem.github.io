<script lang="ts">
  const html: string = "<h1>Avatar Text</h1>\n\n<p>Avatar text is a <code>&lt;span&gt;</code> element rendered inside an Avatar as a fallback when no image is available or the image fails to load. It displays the user's initials or short text within the circular avatar frame.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;span&gt;</code> element with class <code>avatar-text</code></li>\n<li>Spreads <code>restProps</code> onto the root element for consumer customization</li>\n</ul>\n\n<h2>Help</h2>\n\n<p>Use AvatarText inside an Avatar to show initials when a photo is unavailable. The parent Avatar typically manages rendering this component automatically when the <code>src</code> is missing or fails. Common scenarios include placeholder avatars, anonymous users, and image load failures.</p>\n\n<h2>Syntax</h2>\n\n<pre><code class=\"language-html\">&lt;AvatarText&gt;JD&lt;/AvatarText&gt;</code></pre>\n\n<h2>Usage</h2>\n\n<p>Initials fallback inside an Avatar:</p>\n\n<pre><code class=\"language-html\">&lt;Avatar alt=\"Dr Sarah Mitchell\"&gt;\n  &lt;AvatarText&gt;SM&lt;/AvatarText&gt;\n&lt;/Avatar&gt;</code></pre>\n\n<p>Single-character initial for compact display:</p>\n\n<pre><code class=\"language-html\">&lt;Avatar alt=\"Bob\"&gt;\n  &lt;AvatarText&gt;B&lt;/AvatarText&gt;\n&lt;/Avatar&gt;</code></pre>\n\n<h2>Props</h2>\n\n<table>\n<thead>\n<tr><th>Prop</th><th>Type</th><th>Default</th><th>Description</th></tr>\n</thead>\n<tbody>\n<tr><td><code>children</code></td><td><code>slot</code></td><td>(required)</td><td>Initials or short text to display</td></tr>\n<tr><td><code>...restProps</code></td><td>HTML attributes</td><td></td><td>Additional attributes spread onto the <code>&lt;span&gt;</code> element</td></tr>\n</tbody>\n</table>\n\n<h2>Examples</h2>\n\n<p>Various initials:</p>\n\n<pre><code class=\"language-html\">&lt;Avatar alt=\"Alice Smith\"&gt;\n  &lt;AvatarText&gt;AS&lt;/AvatarText&gt;\n&lt;/Avatar&gt;\n\n&lt;Avatar alt=\"Bob\"&gt;\n  &lt;AvatarText&gt;B&lt;/AvatarText&gt;\n&lt;/Avatar&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>None -- this is a decorative/informational element, not interactive</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Typically marked <code>aria-hidden=\"true\"</code> when inside an Avatar, since the parent's <code>aria-label</code> already provides the accessible name</li>\n<li>When used standalone, ensure the text is accessible or the parent provides an accessible label</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside Avatar as a text fallback (typically initials) when no AvatarImage is available or the image fails to load.</li>\n<li>Use when you need to display user initials or short identifying text within an avatar frame.</li>\n<li>Use to maintain visual consistency across avatars even when some users have no photo.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside Avatar -- it has no standalone semantic meaning without the parent container.</li>\n<li>Do not use when an image is available -- prefer AvatarImage for stronger visual recognition.</li>\n<li>Do not use for long text -- limit to 1-2 characters for legibility at small avatar sizes.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides a <code>&lt;span&gt;</code> element for displaying initials text with zero visual styling. The consumer is responsible for all CSS including font size, font weight, text color, text alignment, and positioning within the avatar container.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.avatar-text</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;span&gt;</code> element with class <code>avatar-text</code></li>\n<li>Verify Typically marked <code>aria-hidden=\"true\"</code> when inside an Avatar, since the parent's <code>aria-label</code> already provides the accessible name</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Limit initials to 1-2 characters to ensure legibility at small avatar sizes. Choose a contrasting text-on-background color combination.</li>\n<li><strong>Developers</strong>: When inside an Avatar, mark this element with <code>aria-hidden=\"true\"</code> to prevent duplicate announcements since the parent provides the accessible label.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>AvatarText is a child component within the Avatar composition pattern:</p>\n\n<ul>\n<li><strong>Avatar</strong> -- outer container with <code>role=\"img\"</code> and <code>aria-label</code>.</li>\n<li><strong>AvatarImage</strong> -- <code>&lt;img&gt;</code> element for the user's photo (when available).</li>\n<li><strong>AvatarText</strong> -- <code>&lt;span&gt;</code> fallback displaying initials or short text.</li>\n</ul>\n\n<pre><code class=\"language-html\">&lt;Avatar alt=\"Jane Doe\"&gt;\n  &lt;AvatarText&gt;JD&lt;/AvatarText&gt;\n&lt;/Avatar&gt;</code></pre>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>avatar</code> — an avatar indicator that shows an avatar image or avatar text</li>\n<li><code>avatar-image</code> — an avatar indicator inside image such as a user photo</li>\n<li><code>avatar-group</code> — a group of avatar components</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Img Role: https://www.w3.org/TR/wai-aria-1.2/#img</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<span class=\"avatar-text\">JD</span>";
  const svelteSource: string = "// In your Svelte component:\nimport AvatarText from \"lily-design-system-svelte-headless/components/AvatarText/AvatarText.svelte\";\n\n<AvatarText>\n  <!-- content -->\n</AvatarText>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Avatar Text — Lily Design System</title>
  <meta name="description" content="Avatar text is a &lt;span&gt; element rendered inside an Avatar as a fallback when no image is available or the image fails to load. It displays the user's initials or short text within the circular avat..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

