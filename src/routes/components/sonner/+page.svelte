<script lang="ts">
  const html: string = "<h1>Sonner</h1>\n\n<p>A sonner is a component that serves as a toast notification region, providing a designated area where brief, non-intrusive messages appear to inform users of events, confirmations, or status updates. Commonly used for success confirmations, error alerts, progress updates, and system messages, the sonner displays transient notifications that appear and automatically dismiss after a short duration without requiring user interaction.</p>\n\n<p>Unlike modal dialogs that interrupt workflow, toast notifications communicate feedback passively while allowing users to continue their current task. This headless component renders a <code>&lt;div&gt;</code> with <code>role=\"region\"</code>, <code>aria-live=\"polite\"</code>, and an accessible label, ensuring that screen readers announce new notifications without interrupting the current output.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> with <code>role=\"region\"</code> to define a named landmark for the notification area</li>\n<li>Uses <code>aria-live=\"polite\"</code> so new notifications are announced after the screen reader finishes current output</li>\n<li>Consumer provides individual toast notification elements as children</li>\n<li>Consumer manages toast lifecycle (appearance, auto-dismiss, manual dismiss)</li>\n<li>Accepts <code>...restProps</code> for forwarding additional attributes to the container</li>\n<li>No internal state -- purely a structural wrapper</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name identifying the notification area, applied via <code>aria-label</code></li>\n<li><code>children</code>: slot (required) -- the toast notification content</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;Sonner label=\"Notifications\"&gt;\n  &lt;Toast label=\"Success\"&gt;Patient record saved successfully.&lt;/Toast&gt;\n&lt;/Sonner&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;Sonner label=\"System notifications\"&gt;\n  &lt;Toast label=\"Saved\"&gt;Changes saved.&lt;/Toast&gt;\n  &lt;Toast label=\"Synced\"&gt;Data synchronised with server.&lt;/Toast&gt;\n  &lt;Toast label=\"Warning\" urgent&gt;Session expires in 2 minutes.&lt;/Toast&gt;\n&lt;/Sonner&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None built-in -- the consumer should implement keyboard interactions for dismissing notifications (e.g., Escape key) or navigating between stacked toasts as appropriate.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"region\"</code> -- defines the notification area as a named landmark section</li>\n<li><code>aria-label={label}</code> -- provides an accessible name identifying the notification area</li>\n<li><code>aria-live=\"polite\"</code> -- ensures new content is announced after the screen reader finishes its current output</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use as a toast notification manager to queue and display multiple toast messages.</li>\n<li>Use when your application needs stacked toast management with a designated notification region.</li>\n<li>Use to provide a single <code>aria-live</code> region that announces new Toast children to screen readers.</li>\n<li>Use for success confirmations, error alerts, progress updates, and system messages.</li>\n<li>Use one Sonner per page, positioned consistently (e.g., bottom-right).</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for a single standalone toast -- use Toast directly without a Sonner wrapper.</li>\n<li>Do not use for persistent notifications -- use Banner instead.</li>\n<li>Do not use for critical messages requiring acknowledgment -- use AlertDialog instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component renders a <code>&lt;div&gt;</code> with <code>role=\"region\"</code>, <code>aria-live=\"polite\"</code>, and <code>aria-label</code> to create an accessible notification landmark. It provides the live region semantics so screen readers announce new content. The consumer provides all visual styling, positioning, stacking, and animation for the notification area.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.sonner</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>sonner</code></li>\n<li>Verify role=\"region\"` -- defines the notification area as a named landmark section</li>\n<li>Verify aria-label={label}` -- provides an accessible name identifying the notification area</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Position the notification region in a consistent screen corner (typically bottom-right or top-right). Limit visible toasts to 3-5 at a time to avoid overwhelming users.</li>\n<li><strong>Developers</strong>: Manage toast lifecycle (auto-dismiss timers, manual dismiss) in the consumer. Ensure new toasts are appended as children so the <code>aria-live</code> region announces them.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>Sonner serves as the container region that manages Toast notification components. Place individual Toast elements as children inside the Sonner region. The Sonner provides the live region semantics while each Toast provides the individual notification content and dismiss behavior.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>toast</code> — a brief auto-dismissing notification message</li>\n<li><code>notification</code> — a brief message about an event or update</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Live Regions: https://www.w3.org/TR/wai-aria-1.2/#aria-live</li>\n<li>WAI-ARIA region role: https://www.w3.org/TR/wai-aria-1.2/#region</li>\n</ul>";
</script>

<svelte:head>
  <title>Sonner — Lily Design System</title>
  <meta name="description" content="A sonner is a component that serves as a toast notification region, providing a designated area where brief, non-intrusive messages appear to inform users of events, confirmations, or status update..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
