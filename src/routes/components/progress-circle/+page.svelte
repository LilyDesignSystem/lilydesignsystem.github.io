<script lang="ts">
  const html: string = "<h1>Progress Circle</h1>\n\n<p>A progress circle indicates completion status using a <code>&lt;div&gt;</code> with ARIA Progress semantics. Unlike a linear progress bar, the progress circle is intended to convey progress in a radial layout, commonly used for loading indicators, completion percentages, and dashboard widgets. The component is headless, so consumers provide their own CSS to create the circular visual representation using the ARIA value attributes.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> with <code>role=\"Progress\"</code> and full ARIA value attributes</li>\n<li>Provides <code>aria-valuenow</code>, <code>aria-valuemin</code>, and <code>aria-valuemax</code> to communicate progress to screen readers</li>\n<li>Accepts an optional <code>children</code> slot for content inside the circle (e.g., percentage text)</li>\n<li>Uses <code>aria-label</code> for accessible naming</li>\n<li>Spreads <code>restProps</code> for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name for the progress indicator via <code>aria-label</code></li>\n<li><code>value</code>: number (default: <code>0</code>) -- current progress value</li>\n<li><code>min</code>: number (default: <code>0</code>) -- minimum value of the progress range</li>\n<li><code>max</code>: number (default: <code>100</code>) -- maximum value of the progress range</li>\n<li><code>children</code>: slot (default: <code>undefined</code>) -- optional content rendered inside (e.g., percentage text)</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;div&gt;</code></li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Dashboard widget showing project completion:</p>\n\n<pre><code class=\"language-html\">&lt;ProgressCircle label=\"Project completion\" value={75} max={100}&gt;\n  &lt;span&gt;75%&lt;/span&gt;\n&lt;/ProgressCircle&gt;</code></pre>\n\n<p>Profile completion indicator:</p>\n\n<pre><code class=\"language-html\">&lt;ProgressCircle label=\"Profile completion\" value={4} min={0} max={6}&gt;\n  &lt;span&gt;4 of 6&lt;/span&gt;\n&lt;/ProgressCircle&gt;</code></pre>\n\n<p>File upload card with circular indicator:</p>\n\n<pre><code class=\"language-html\">&lt;ProgressCircle label=\"Uploading document\" value={42}&gt;\n  &lt;span&gt;42%&lt;/span&gt;\n&lt;/ProgressCircle&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive, read-only display.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"Progress\"</code> -- identifies the element as a progress indicator</li>\n<li><code>aria-label=\"...\"</code> -- provides an accessible name for the progress indicator</li>\n<li><code>aria-valuenow</code> -- the current progress value</li>\n<li><code>aria-valuemin</code> -- the minimum value of the progress range</li>\n<li><code>aria-valuemax</code> -- the maximum value of the progress range</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use ProgressCircle for compact progress displays in dashboards and widget cards where a radial indicator fits the layout.</li>\n<li>Use ProgressCircle when you want to show a percentage value inside the indicator via the children slot.</li>\n<li>Use ProgressCircle for upload or processing progress in space-constrained areas such as file cards or notification badges.</li>\n<li>Use ProgressCircle for profile completion indicators or skill ratings in user interfaces.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use ProgressCircle for indeterminate loading when no percentage is available -- use ProgressSpinner instead.</li>\n<li>Do not use ProgressCircle for standard linear progress bars such as form wizards or file downloads -- use Progress instead.</li>\n<li>Do not use ProgressCircle for static scalar measurements like disk usage or battery level -- use Meter instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>The ProgressCircle headless component provides a <code>&lt;div&gt;</code> with <code>role=\"progressbar\"</code> and full ARIA value attributes (<code>aria-valuenow</code>, <code>aria-valuemin</code>, <code>aria-valuemax</code>) plus <code>aria-label</code> for accessible naming. The consumer provides all visual styling, including the circular SVG or CSS rendering and any inner content.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.progress-circle</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>progress-circle</code></li>\n<li>Verify role=\"Progress\"` -- identifies the element as a progress indicator</li>\n<li>Verify aria-label=\"...\"` -- provides an accessible name for the progress indicator</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use a ring or arc visual with clear fill to indicate progress, and display the percentage value in the center of the circle for quick readability.</li>\n<li><strong>Developers</strong>: Use the ARIA value attributes to drive your CSS or SVG arc calculations, keeping the visual and semantic states synchronized.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>progress</code> — a horizontal progress bar showing completion</li>\n<li><code>progress-spinner</code> — an indeterminate spinning progress indicator</li>\n<li><code>loading</code> — a loading indicator, such as text, or image, or animation</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Meter Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/meter/</li>\n<li>MDN <code>role=\"Progress\"</code>: https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/Roles/Progress_role</li>\n</ul>";
</script>

<svelte:head>
  <title>Progress Circle — Lily Design System</title>
  <meta name="description" content="A progress circle indicates completion status using a &lt;div&gt; with ARIA Progress semantics. Unlike a linear progress bar, the progress circle is intended to convey progress in a radial layout, common..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
