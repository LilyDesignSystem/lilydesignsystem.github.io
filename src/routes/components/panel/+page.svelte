<script lang="ts">
  const html: string = "<h1>Panel</h1>\n\n<p>A panel is a UI component that serves as a labeled container for grouping related content, controls, or settings within an interface. The Panel component renders a semantic <code>&lt;section&gt;</code> element with an accessible label, creating a named region landmark that assistive technology can identify and navigate to.</p>\n\n<p>Panels are commonly used in dashboards, settings pages, sidebars, and multi-section layouts to organize information into logical groups. The required label ensures each panel is identifiable, which is especially important when multiple panels appear on the same page.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;section&gt;</code> HTML element, which creates a landmark region when labelled</li>\n<li>The <code>label</code> prop is required and applied via <code>aria-label</code> to name the region</li>\n<li>Content is provided via the <code>children</code> slot</li>\n<li>Rest props are spread onto the <code>&lt;section&gt;</code> element</li>\n<li>A <code>&lt;section&gt;</code> with <code>aria-label</code> becomes a navigable region in screen reader landmark lists</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name for the panel region via <code>aria-label</code></li>\n<li><code>children</code>: slot (required) -- the panel content</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;section&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;!-- Confirmation panel on a transaction completion page --&gt;\n&lt;Panel label=\"Application complete\"&gt;\n  &lt;h1&gt;Application complete&lt;/h1&gt;\n  &lt;p&gt;We have sent you a confirmation email to sarah.mitchell@example.com&lt;/p&gt;\n  &lt;p&gt;Your reference number is &lt;strong&gt;HDJ2123F&lt;/strong&gt;&lt;/p&gt;\n&lt;/Panel&gt;\n\n&lt;!-- Appointment confirmation panel --&gt;\n&lt;Panel label=\"Appointment booked\"&gt;\n  &lt;h1&gt;Appointment booked&lt;/h1&gt;\n  &lt;p&gt;Your appointment is on 15 March 2025 at 10:30am at City Road Medical Centre.&lt;/p&gt;\n&lt;/Panel&gt;\n\n&lt;!-- Multiple panels grouping settings sections --&gt;\n&lt;Panel label=\"Account information\"&gt;\n  &lt;h2&gt;Account information&lt;/h2&gt;\n  &lt;p&gt;Name, email, and profile details.&lt;/p&gt;\n&lt;/Panel&gt;\n&lt;Panel label=\"Notification preferences\"&gt;\n  &lt;h2&gt;Notification preferences&lt;/h2&gt;\n  &lt;p&gt;Choose how you want to be notified.&lt;/p&gt;\n&lt;/Panel&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive container. Screen reader users can navigate to named regions using landmark navigation shortcuts.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>&lt;section aria-label=\"...\"&gt;</code> -- creates a named region landmark, allowing assistive technology to list and navigate to this section</li>\n<li>The <code>aria-label</code> value should be concise and descriptive of the panel's content</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use on confirmation pages to tell users they have successfully completed a transaction</li>\n<li>Use to display a brief confirmation heading like \"Application complete\" with optional supporting text such as a reference number</li>\n<li>Use to group related content into a labeled section on dashboards, settings pages, or multi-section layouts</li>\n<li>Use when screen reader users need to navigate between distinct content regions via landmark navigation</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use to highlight important information within body content -- use InsetText, WarningCallout, or CareCard instead</li>\n<li>Do not use for error states or validation messages -- use ErrorSummary and ErrorMessage instead</li>\n<li>Do not use for notifications or status updates -- use Banner or Notification instead</li>\n<li>Do not use for grouping content that needs a visual container with header, body, and footer areas -- use Card instead</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>The Panel headless component provides a semantic <code>&lt;section&gt;</code> element with <code>aria-label</code> for region landmark navigation. It handles accessible naming and landmark semantics. The consumer provides all visual styling, including borders, backgrounds, padding, and spacing.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.panel</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>panel</code></li>\n<li>Verify &lt;section aria-label=\"...\"&gt;` -- creates a named region landmark, allowing assistive technology to list and navigate to this section</li>\n<li>Verify The <code>aria-label</code> value should be concise and descriptive of the panel's content</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Keep panel labels concise and unique on each page so users can quickly distinguish between sections in a landmark list.</li>\n<li><strong>Developers</strong>: Ensure each Panel on a page has a distinct <code>label</code> value; duplicate region names make landmark navigation confusing for screen reader users.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>card</code> — a grouped content container with header, body, and footer areas</li>\n<li><code>summary-box</code> — a boxed callout highlighting key takeaways or next steps from a longer page</li>\n<li><code>info-state</code> — a composition of illustration, title, description, and action for empty, error, or info states</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA region role: https://www.w3.org/TR/wai-aria-1.2/#region</li>\n<li>HTML section element: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/section</li>\n<li>WAI Landmarks: https://www.w3.org/WAI/ARIA/apd/practices/landmark-regions/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"panel\"><h3>Panel Heading</h3><p>Panel content.</p></div>";
  const svelteSource: string = "// In your Svelte component:\nimport Panel from \"lily-design-system-svelte-headless/components/Panel/Panel.svelte\";\n\n<Panel>\n  <!-- content -->\n</Panel>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Panel — Lily Design System</title>
  <meta name="description" content="A panel is a UI component that serves as a labeled container for grouping related content, controls, or settings within an interface. The Panel component renders a semantic &lt;section&gt; element with a..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

