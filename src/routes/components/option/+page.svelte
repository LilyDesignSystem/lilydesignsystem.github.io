<script lang="ts">
  const html: string = "<h1>Option</h1>\n\n<p>An option component wraps the native HTML <code>&lt;option&gt;</code> element for use inside <code>&lt;select&gt;</code> elements. It accepts a value prop for the form submission value and renders children as the visible option label text.</p>\n\n<p>The component supports <code>selected</code> for pre-selecting an option and <code>disabled</code> for preventing selection, both using native HTML attributes. All accessibility behavior is provided automatically by the browser's native <code>&lt;option&gt;</code> element within the parent <code>&lt;select&gt;</code>.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Wraps native <code>&lt;option&gt;</code> element with runes</li>\n<li>Uses <code>children</code> slot for label text content</li>\n<li>Supports <code>selected</code> and <code>disabled</code> HTML attributes</li>\n<li>Spreads <code>...restProps</code> on the root <code>&lt;option&gt;</code> element</li>\n<li>All text content comes through children and props for internationalization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>value</code>: string (required) -- the option value submitted with the form</li>\n<li><code>selected</code>: boolean (default: false) -- whether this option is pre-selected</li>\n<li><code>disabled</code>: boolean (default: false) -- whether this option is disabled</li>\n<li><code>children</code>: slot (required) -- option label text</li>\n<li><code>...restProps</code>: Any additional HTML attributes passed to the <code>&lt;option&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;select&gt;\n  &lt;Option value=\"us\"&gt;United States&lt;/Option&gt;\n  &lt;Option value=\"uk\"&gt;United Kingdom&lt;/Option&gt;\n&lt;/select&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Native <code>&lt;option&gt;</code> keyboard behavior (managed by parent <code>&lt;select&gt;</code>)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Native <code>&lt;option&gt;</code> provides built-in accessibility via the parent <code>&lt;select&gt;</code> element</li>\n<li>Screen readers announce the option text and selected state automatically</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside a <code>&lt;select&gt;</code> element to represent one selectable choice in a dropdown.</li>\n<li>Use when each option needs a distinct form submission value separate from its display text.</li>\n<li>Avoid using Option outside of a Select component; it depends on the parent <code>&lt;select&gt;</code> for semantics and keyboard behavior.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside of a <code>&lt;select&gt;</code> element — <code>Option</code> relies on the parent <code>&lt;select&gt;</code> for semantics and keyboard handling.</li>\n<li>Do not use when you need rich content in each option (icons, descriptions, multi-line text) — native <code>&lt;option&gt;</code> cannot render arbitrary HTML; use <code>Listbox</code> or <code>Combobox</code> for that.</li>\n<li>Do not use when you need autocomplete/typeahead filtering — use <code>Combobox</code> or <code>Autosuggest</code>.</li>\n<li>Do not use a single <code>Option</code> to represent something other than a discrete selectable value (e.g. a heading) — use <code>&lt;optgroup&gt;</code> for grouping.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component wraps the native <code>&lt;option&gt;</code> element with props for <code>value</code>, <code>selected</code>, and <code>disabled</code> states. Accessibility is provided automatically by the browser through the parent <code>&lt;select&gt;</code>. The consumer provides all visual styling via the parent Select component, as native <code>&lt;option&gt;</code> elements have limited styling capabilities.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.option</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;option&gt;</code> element with class <code>option</code></li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Keep option text concise and scannable. For long lists, group related options using <code>&lt;optgroup&gt;</code> via the parent Select.</li>\n<li><strong>Developers</strong>: Use the <code>value</code> prop for the form submission value and children for the display text. Native <code>&lt;option&gt;</code> styling is limited; for custom-styled dropdowns, consider Listbox or Combobox instead.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>select</code> — a dropdown select element for choosing one option</li>\n<li><code>theme-select-option</code> — one option in a theme select dropdown</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>HTML option element: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/option</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<option class=\"option\" value=\"example\">Example option</option>";
  const svelteSource: string = "// In your Svelte component:\nimport Option from \"lily-design-system-svelte-headless/components/Option/Option.svelte\";\n\n<Option>\n  <!-- content -->\n</Option>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Option — Lily Design System</title>
  <meta name="description" content="An option component wraps the native HTML &lt;option&gt; element for use inside &lt;select&gt; elements. It accepts a value prop for the form submission value and renders children as the visible option label t..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

