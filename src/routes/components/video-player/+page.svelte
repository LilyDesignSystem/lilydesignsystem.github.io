<script lang="ts">
  const html: string = "<h1>Video Player</h1>\n\n<p>A video player is a video player with play-in-view behavior, custom controls, and IntersectionObserver support. Use it for editorial video content that should play when scrolled into view and pause when scrolled out.</p>\n\n<p>This headless component uses a <code>&lt;div&gt;</code> container with <code>aria-label</code> wrapping a <code>&lt;video&gt;</code> element, providing optional play-in-view behavior via IntersectionObserver and support for custom control overlays.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses <code>&lt;div&gt;</code> container wrapping a <code>&lt;video&gt;</code> element</li>\n<li>Optional play-in-view: auto-plays when scrolled into viewport, pauses when scrolled out</li>\n<li>Uses IntersectionObserver for visibility detection</li>\n<li>Supports custom control overlays via children slot</li>\n<li>Consumer can style control position and appearance</li>\n<li>Poster image displayed before playback</li>\n<li>Native <code>&lt;video&gt;</code> controls shown by default</li>\n<li>Consumer controls all visual presentation via CSS</li>\n<li>Inspired by Reuters Graphics Video component</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>src</code>: string (required) -- video source URL</li>\n<li><code>label</code>: string (required) -- accessible label for the video</li>\n<li><code>poster</code>: string (optional) -- poster image URL</li>\n<li><code>autoplay</code>: boolean (default: false) -- auto-play when scrolled into view</li>\n<li><code>muted</code>: boolean (default: false) -- muted state</li>\n<li><code>loop</code>: boolean (default: false) -- loop playback</li>\n<li><code>controls</code>: boolean (default: true) -- show native controls</li>\n<li><code>caption</code>: slot (optional) -- video caption</li>\n<li><code>children</code>: slot (optional) -- custom controls overlay</li>\n<li><code>...restProps</code>: Any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;VideoPlayer\n  src=\"/videos/report.mp4\"\n  label=\"Interview with climate scientist\"\n  poster=\"/images/interview-poster.jpg\"\n  autoplay\n  muted\n&gt;\n  &lt;span slot=\"caption\"&gt;Dr. Smith discusses recent findings&lt;/span&gt;\n&lt;/VideoPlayer&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;VideoPlayer\n  src=\"/videos/surgery-overview.mp4\"\n  label=\"GP surgery virtual tour\"\n  poster=\"/images/surgery-poster.jpg\"\n  controls\n&gt;\n  &lt;span slot=\"caption\"&gt;Take a virtual tour of our new surgery&lt;/span&gt;\n&lt;/VideoPlayer&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;VideoPlayer\n  src=\"/videos/timelapse.mp4\"\n  label=\"Construction timelapse from January to March 2026\"\n  autoplay\n  muted\n  loop\n/&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li><code>Space</code> -- toggle play/pause when video is focused</li>\n<li>Native video keyboard controls are available when using native controls</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label</code> on container -- describes the video content for screen readers</li>\n<li>Native <code>&lt;video&gt;</code> element provides built-in accessibility controls</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for editorial video content with play-in-view behavior</li>\n<li>Use when videos should auto-play silently as the user scrolls past them</li>\n<li>Use when custom control overlays are needed alongside or instead of native controls</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for scroll-driven video that advances frame by frame with scroll position</li>\n<li>Do not use for background ambient video -- use plain <code>&lt;video&gt;</code> element</li>\n<li>Do not use for audio-only content -- use an audio player</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides a <code>&lt;div&gt;</code> container with <code>aria-label</code> wrapping a <code>&lt;video&gt;</code> element, with zero visual styling. The consumer is responsible for all CSS including video sizing, aspect ratio, control overlay positioning, poster image display, responsive behavior, and any play/pause button styling.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.video-player</code> class for targeting. No default styles are included -- this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>video-player</code></li>\n<li>Verify <code>aria-label</code> is applied to the container</li>\n<li>Verify <code>&lt;video&gt;</code> element renders with correct <code>src</code> attribute</li>\n<li>Verify <code>poster</code> attribute is applied when provided</li>\n<li>Verify native controls are shown by default</li>\n<li>Verify <code>autoplay</code>, <code>muted</code>, and <code>loop</code> attributes are applied when set</li>\n<li>Verify play-in-view behavior: video plays when scrolled into view and pauses when scrolled out (when autoplay is true)</li>\n<li>Verify caption slot renders when provided</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Consider providing a clear poster image that communicates the video content. Ensure custom controls have sufficient contrast and touch target sizes. Plan for the video's aspect ratio across breakpoints.</li>\n<li><strong>Developers</strong>: Always provide a descriptive <code>label</code> prop for screen reader users. When using autoplay, always set <code>muted</code> to comply with browser autoplay policies. Use IntersectionObserver with an appropriate threshold for play-in-view behavior.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>image</code> — an image element with alt text</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>Reuters Graphics Video component</li>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/API/IntersectionObserver\">IntersectionObserver API</a></li>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/HTML/Element/video\">HTML video element</a></li>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/Media/Autoplay_guide\">Autoplay guide for media</a></li>\n</ul>";
</script>

<svelte:head>
  <title>Video Player — Lily Design System</title>
  <meta name="description" content="A video player is a video player with play-in-view behavior, custom controls, and IntersectionObserver support. Use it for editorial video content that should play when scrolled into view and pause..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
