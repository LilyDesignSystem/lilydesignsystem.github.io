<script lang="ts">
  const html: string = "<h1>Mentions Input</h1>\n\n<p>A mentions input is a text input with at-mention autocomplete suggestions. It pairs a single-line <code>&lt;input type=\"text\"&gt;</code> with a suggestions panel that opens when the user types a configurable trigger character.</p>\n\n<p>Use it for chat composers, comment fields, or any text entry where the user can mention people, tags, or entities by typing <code>@</code>, <code>#</code>, or another trigger.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div class=\"mentions-input\" data-trigger-char={triggerChar}&gt;</code> container</li>\n<li>Inner input has <code>class=\"mentions-input-control\"</code>, <code>type=\"text\"</code>, <code>role=\"combobox\"</code></li>\n<li>Input gets <code>aria-haspopup=\"listbox\"</code>, <code>aria-expanded</code>, <code>aria-autocomplete=\"list\"</code></li>\n<li>Suggestions panel is <code>&lt;div class=\"mentions-input-suggestions\"&gt;</code> toggled by <code>hidden</code></li>\n<li><code>label</code> is <strong>required</strong> and is set as <code>aria-label</code> on the input</li>\n<li><code>triggerChar</code> defaults to <code>\"@\"</code></li>\n<li><code>disabled</code> disables the input</li>\n<li><code>value</code> is the current text; consumer manages input state via <code>oninput</code>/<code>onInput</code></li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (<strong>required</strong>) -- aria-label for the input</li>\n<li><code>value</code>: string (default: \"\") -- input value</li>\n<li><code>triggerChar</code>: string (default: \"@\") -- character that opens suggestions</li>\n<li><code>expanded</code>: boolean (default: false) -- whether suggestions panel is open</li>\n<li><code>placeholder</code>: string (optional)</li>\n<li><code>disabled</code>: boolean (default: false)</li>\n<li><code>oninput</code> / <code>onInput</code>: callback (optional) -- input change handler</li>\n<li><code>children</code>: slot -- suggestions panel content (typically a Listbox)</li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;MentionsInput\n  label=\"Mention a teammate\"\n  value={text}\n  triggerChar=\"@\"\n  expanded={showSuggestions}\n  oninput={handleInput}\n&gt;\n  &lt;ul role=\"listbox\"&gt;\n    &lt;li role=\"option\"&gt;@alice&lt;/li&gt;\n    &lt;li role=\"option\"&gt;@bob&lt;/li&gt;\n  &lt;/ul&gt;\n&lt;/MentionsInput&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus the input</li>\n<li>Type the trigger char: Consumer should set <code>expanded</code> to true</li>\n<li>Arrow Down / Up: Consumer should move focus or active descendant inside the listbox</li>\n<li>Enter: Consumer should commit the active suggestion</li>\n<li>Escape: Consumer should close the suggestions panel</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Input has <code>role=\"combobox\"</code>, <code>aria-haspopup=\"listbox\"</code>, <code>aria-expanded</code>, <code>aria-autocomplete=\"list\"</code></li>\n<li><code>aria-label</code> is the entire accessible name of the input (required)</li>\n<li>Consumer-supplied listbox uses <code>role=\"listbox\"</code> with <code>role=\"option\"</code> children</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use in chat composers, comment fields, or task descriptions where users mention people or entities.</li>\n<li>Use when the suggestions are filtered by what the user types after the trigger.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for free-form selection unrelated to a trigger character — use Combobox.</li>\n<li>Do not use for static dropdown selection — use Select.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>Renders semantic combobox markup with no styling. Consumer manages suggestion filtering, the <code>expanded</code> state, listbox markup, focus management, and any visual treatment.</p>\n\n<h2>Styles</h2>\n\n<p>Consumer CSS targets <code>mentions-input</code>, <code>mentions-input-control</code>, and <code>mentions-input-suggestions</code>. Provide a clear focus ring on the input and a visible boundary on the suggestions panel.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>mentions-input</code></li>\n<li>Verify the inner <code>&lt;input type=\"text\"&gt;</code> has class <code>mentions-input-control</code></li>\n<li>Verify <code>role=\"combobox\"</code>, <code>aria-haspopup=\"listbox\"</code>, <code>aria-autocomplete=\"list\"</code> are set</li>\n<li>Verify <code>aria-expanded</code> reflects the <code>expanded</code> prop</li>\n<li>Verify <code>aria-label</code> equals the <code>label</code> prop</li>\n<li>Verify <code>data-trigger-char</code> equals the <code>triggerChar</code> prop (default <code>@</code>)</li>\n<li>Verify the suggestions panel is <code>hidden</code> when <code>expanded</code> is false</li>\n<li>Verify <code>disabled</code> attribute is applied to the input when <code>disabled</code> is true</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Show the trigger character in the placeholder hint so users know how to invoke suggestions.</li>\n<li><strong>Developers</strong>: Track caret position to detect the trigger char accurately. Use <code>aria-activedescendant</code> on the input to announce the current suggestion.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>text-input</code> — a single-line text input field &lt;input type=\"text\"&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Combobox Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/combobox/</li>\n<li>Ant Design Mentions: https://ant.design/components/mentions</li>\n</ul>";
</script>

<svelte:head>
  <title>Mentions Input — Lily Design System</title>
  <meta name="description" content="A mentions input is a text input with at-mention autocomplete suggestions. It pairs a single-line &lt;input type=&quot;text&quot;&gt; with a suggestions panel that opens when the user types a configurable trigger ..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
