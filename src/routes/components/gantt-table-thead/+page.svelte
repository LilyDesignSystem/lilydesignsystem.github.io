<script lang="ts">
  const html: string = "<h1>Gantt Table Head</h1>\n\n<p>A Gantt table head is the header section of a Gantt chart grid, wrapping one or more rows that label the columns of the chart. Typically contains a row with a task-name column header and time-period column headers (e.g., weeks, months, sprints). It is designed to be used inside a GanttTable <code>&lt;table&gt;</code> structure.</p>\n\n<p>The component renders a <code>&lt;thead&gt;</code> element and passes through its children, which are expected to be GanttTableTR or <code>&lt;tr&gt;</code> elements containing <code>&lt;th&gt;</code> header cells.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as a <code>&lt;thead&gt;</code> element for the header section of a Gantt chart grid</li>\n<li>Children should be one or more rows with <code>&lt;th&gt;</code> cells labeling the task column and time-period columns</li>\n<li>Designed to be used inside a GanttTable <code>&lt;table&gt;</code> structure</li>\n<li>Spreads <code>restProps</code> onto the <code>&lt;thead&gt;</code> element for consumer customization</li>\n<li>No internal state -- purely a structural wrapper</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>children</code>: slot (required) -- header rows containing column labels for tasks and time periods</li>\n<li><code>...restProps</code>: Any additional HTML attributes passed to the <code>&lt;thead&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;GanttTable label=\"Q1 2025 schedule\"&gt;\n  &lt;GanttTableHead&gt;\n    &lt;GanttTableTR&gt;\n      &lt;GanttTableTH scope=\"col\"&gt;Task&lt;/GanttTableTH&gt;\n      &lt;GanttTableTH scope=\"col\"&gt;Jan&lt;/GanttTableTH&gt;\n      &lt;GanttTableTH scope=\"col\"&gt;Feb&lt;/GanttTableTH&gt;\n      &lt;GanttTableTH scope=\"col\"&gt;Mar&lt;/GanttTableTH&gt;\n    &lt;/GanttTableTR&gt;\n  &lt;/GanttTableHead&gt;\n  &lt;GanttTableBody&gt;...&lt;/GanttTableBody&gt;\n&lt;/GanttTable&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive container. Keyboard navigation is handled by the parent GanttTable grid.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Implicit <code>rowgroup</code> role from the <code>&lt;thead&gt;</code> element -- groups the header rows of the grid</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside GanttTable to provide the <code>&lt;thead&gt;</code> header section.</li>\n<li>Use to label the task-name column and time period columns (e.g., weeks, months, sprints).</li>\n<li>Use when the Gantt chart needs clearly labelled column headers for accessibility.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside of a GanttTable structure -- use TableHead or DataTableHead for general tables.</li>\n<li>Do not use for body or footer rows -- use GanttTableBody or GanttTableTfoot instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;thead&gt;</code> element with implicit <code>rowgroup</code> role for grouping header rows within a Gantt chart grid. The consumer provides all visual styling including header background, typography, and column alignment.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.gantt-table-thead</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;thead&gt;</code> element with class <code>gantt-table-thead</code></li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Make header labels concise and readable. For long timelines, consider multi-row headers (e.g., months on one row, weeks on another).</li>\n<li><strong>Developers</strong>: Use <code>&lt;th&gt;</code> elements inside GanttTableTR children for proper header cell semantics. Add <code>scope=\"col\"</code> to column headers for screen reader clarity.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>gantt-table</code> — a Gantt chart table interactive grid for planning schedule visualization &lt;table&gt;</li>\n<li><code>gantt-table-tbody</code> — see components.tsv</li>\n<li><code>gantt-table-tfoot</code> — see components.tsv</li>\n<li><code>gantt-table-tr</code> — see components.tsv</li>\n<li><code>gantt-table-th</code> — a Gantt chart table interactive grid header cell for planning schedule visualization &lt;th&gt;</li>\n<li><code>gantt-table-td</code> — a Gantt chart table interactive grid data cell for planning schedule visualization &lt;td&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Grid Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/grid/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<thead class=\"gantt-table-thead\"><tr class=\"gantt-table-tr\"><th class=\"gantt-table-th\">Task</th></tr></thead>";
  const svelteSource: string = "// In your Svelte component:\nimport GanttTableThead from \"lily-design-system-svelte-headless/components/GanttTableThead/GanttTableThead.svelte\";\n\n<GanttTableThead>\n  <!-- GanttTableThead content -->\n</GanttTableThead>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Gantt Table Head — Lily Design System</title>
  <meta name="description" content="A Gantt table head is the header section of a Gantt chart grid, wrapping one or more rows that label the columns of the chart. Typically contains a row with a task-name column header and time-perio..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

