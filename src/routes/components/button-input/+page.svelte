<script lang="ts">
  const html: string = "<h1>Button Input</h1>\n\n<p>A button input is a form component that wraps the native HTML <code>&lt;input type=\"button\"&gt;</code> element. Unlike the Button component which uses <code>&lt;button&gt;</code> and can contain rich content (icons, formatted text) via children slots, the button input displays its label as the <code>value</code> attribute and renders only plain text.</p>\n\n<p>This component is useful in form contexts where the native input semantics are preferred, or when interacting with systems that expect <code>&lt;input&gt;</code> elements. It supports disabling, form field naming, and an optional <code>aria-label</code> override for screen readers. The button input inherits all standard keyboard interactions from the native element -- Enter and Space to activate.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Wraps the native <code>&lt;input type=\"button\"&gt;</code> element for form-native button semantics</li>\n<li>Label is the <code>value</code> attribute (plain text only, no rich content)</li>\n<li>Distinguished from the Button component: Button uses <code>&lt;button&gt;</code> with children slots</li>\n<li>Uses props rune for prop destructuring</li>\n<li>Spreads <code>...restProps</code> on the <code>&lt;input&gt;</code> element for consumer extensibility</li>\n<li>Supports <code>disabled</code>, <code>name</code>, and <code>onclick</code> for standard form interactions</li>\n<li>No hardcoded user-facing strings; label text comes through <code>value</code> and <code>label</code> props</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>value</code>: string (required) -- the button label text displayed on the input</li>\n<li><code>disabled</code>: boolean (default: <code>false</code>) -- whether the input is disabled</li>\n<li><code>name</code>: string (optional) -- form field name for form submission</li>\n<li><code>label</code>: string (optional) -- accessible label override via <code>aria-label</code> for screen readers</li>\n<li><code>onclick</code>: <code>(event: MouseEvent) =&gt; void</code> (optional) -- click event handler</li>\n<li><code>...restProps</code>: any additional HTML attributes spread onto the <code>&lt;input&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Action button with a descriptive label:</p>\n\n<pre><code class=\"language-html\">&lt;ButtonInput value=\"Calculate total\" onclick={handleCalculate} /&gt;</code></pre>\n\n<p>Disabled button with conditional state:</p>\n\n<pre><code class=\"language-html\">&lt;ButtonInput value=\"Reset filters\" onclick={handleReset} disabled={!hasFilters} /&gt;</code></pre>\n\n<p>Named button with accessible label override:</p>\n\n<pre><code class=\"language-html\">&lt;ButtonInput value=\"Save\" name=\"action\" label=\"Save changes\" /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus the button input</li>\n<li>Enter: Activate the button (native behavior)</li>\n<li>Space: Activate the button (native behavior)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Implicit <code>button</code> role -- provided natively by <code>&lt;input type=\"button\"&gt;</code></li>\n<li><code>aria-label={label}</code> -- optional screen reader label override; when provided, screen readers announce this instead of the <code>value</code> text</li>\n<li><code>aria-disabled</code> -- implicitly managed by the native <code>disabled</code> attribute</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use when you need a form-native <code>&lt;input type=\"button\"&gt;</code> element for plain text button labels.</li>\n<li>Use in contexts where form semantics require an input element rather than a button element.</li>\n<li>Use when no rich content (icons, formatted text) is needed inside the button.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when you need icons, images, or formatted content inside the button -- use Button instead, which supports child elements.</li>\n<li>Do not use for navigation -- use ActionLink or BackLink.</li>\n<li>Do not use for form submission -- use SubmitInput or <code>&lt;Button type=\"submit\"&gt;</code>.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides a native <code>&lt;input type=\"button\"&gt;</code> with optional <code>aria-label</code> override and standard form attributes (<code>name</code>, <code>disabled</code>), all with zero visual styling. The consumer is responsible for all CSS including colors, padding, border, border-radius, hover states, focus indicators, and disabled appearance.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.button-input</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;input&gt;</code> element with class <code>button-input</code></li>\n<li>Verify aria-label={label}<code> -- optional screen reader label override; when provided, screen readers announce this instead of the </code>value` text</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Apply the same visual treatment as Button components to maintain consistency. Ensure disabled state has sufficient contrast.</li>\n<li><strong>Developers</strong>: Use the <code>label</code> prop for an <code>aria-label</code> override when the <code>value</code> text alone is insufficient context for screen reader users. The <code>name</code> prop is useful for identifying the button in form submissions.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>text-input</code> — a single-line text input field &lt;input type=\"text\"&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN input type=\"button\": https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/button</li>\n<li>WAI-ARIA Button Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/button/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<input class=\"button-input\" type=\"button\" value=\"Click\" />";
  const svelteSource: string = "// In your Svelte component:\nimport ButtonInput from \"lily-design-system-svelte-headless/components/ButtonInput/ButtonInput.svelte\";\n\n<ButtonInput label=\"Label\" />\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Button Input — Lily Design System</title>
  <meta name="description" content="A button input is a form component that wraps the native HTML &lt;input type=&quot;button&quot;&gt; element. Unlike the Button component which uses &lt;button&gt; and can contain rich content (icons, formatted text) via..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

