<script lang="ts">
  const html: string = "<h1>Checkbox Input</h1>\n\n<p>A checkbox input is a minimal headless wrapper around the native HTML <code>&lt;input type=\"checkbox\"&gt;</code> element. Unlike a Checkbox component which includes a wrapping <code>&lt;label&gt;</code>, CheckboxInput uses <code>aria-label</code> for accessibility, giving consumers full control over label placement and visual presentation.</p>\n\n<p>This component is ideal when you need a bare checkbox that you will wrap in your own custom layout, such as placing the label to the left, using a custom indicator, or integrating into a complex form where label positioning must be controlled by the consumer.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a single <code>&lt;input type=\"checkbox\"&gt;</code> element with no wrapper</li>\n<li>Uses <code>aria-label</code> for accessible naming instead of a wrapping <code>&lt;label&gt;</code> element</li>\n<li>Supports two-way binding on the <code>checked</code> prop</li>\n<li>Spreads <code>restProps</code> onto the input element for consumer extensibility</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>checked</code>: boolean (default: false) -- bindable boolean indicating whether the checkbox is checked</li>\n<li><code>label</code>: string (required) -- accessible name applied via <code>aria-label</code></li>\n<li><code>disabled</code>: boolean (default: false) -- whether the checkbox is disabled</li>\n<li><code>required</code>: boolean (default: false) -- whether the checkbox is required for form submission</li>\n<li><code>name</code>: string (default: undefined) -- form field name attribute</li>\n<li><code>id</code>: string (default: undefined) -- element id attribute</li>\n<li><code>value</code>: string (default: undefined) -- value attribute for form submission</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the input element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;Fieldset legend=\"What symptoms are you experiencing?\"&gt;\n  &lt;Hint&gt;Select all that apply&lt;/Hint&gt;\n  &lt;CheckboxGroup&gt;\n    &lt;CheckboxInput label=\"Headache\" name=\"symptoms\" value=\"headache\" /&gt;\n    &lt;CheckboxInput label=\"Fever\" name=\"symptoms\" value=\"fever\" /&gt;\n    &lt;CheckboxInput label=\"Cough\" name=\"symptoms\" value=\"cough\" /&gt;\n    &lt;CheckboxInput label=\"Fatigue\" name=\"symptoms\" value=\"fatigue\" /&gt;\n  &lt;/CheckboxGroup&gt;\n&lt;/Fieldset&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;CheckboxInput label=\"I agree to the terms and conditions\" checked={agreed} required name=\"terms\" value=\"yes\" /&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;CheckboxInput label=\"Subscribe to newsletter\" checked={subscribed} disabled={!hasEmail} /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Space: Toggles the checkbox checked state (native browser behavior)</li>\n<li>Tab: Moves focus to and from the checkbox (native browser behavior)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides an accessible name for the checkbox since there is no visible <code>&lt;label&gt;</code> element</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use when users need to select one or more options from a list of choices</li>\n<li>Use to toggle a single boolean option on or off, such as agreeing to terms or opting into communications</li>\n<li>Use within a Fieldset with a legend that describes the group as a whole</li>\n<li>Use with Hint text to provide guidance like \"Select all options that apply to you\"</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when users can only select one option — use RadioInput within a RadioGroup instead</li>\n<li>Do not use for toggling a visible on/off setting — use SwitchButton for immediate-effect toggles</li>\n<li>Do not pre-select checkbox options — users may miss questions or submit wrong answers</li>\n<li>Do not use outside of a CheckboxGroup when managing shared state across multiple checkboxes</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless CheckboxInput component provides a native <code>&lt;input type=\"checkbox\"&gt;</code> with <code>aria-label</code> for accessible naming, two-way binding on the <code>checked</code> prop, and support for <code>disabled</code>, <code>required</code>, and form attributes. The consumer provides all visual styling including custom checkbox indicators, colors, sizes, and label positioning.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.checkbox-input</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;input&gt;</code> element with class <code>checkbox-input</code></li>\n<li>Verify aria-label={label}<code> -- provides an accessible name for the checkbox since there is no visible </code>&lt;label&gt;` element</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Ensure custom checkbox indicators have sufficient size (at least 24x24px touch target) and clear checked/unchecked states with adequate contrast.</li>\n<li><strong>Developers</strong>: Use the <code>aria-label</code> prop to provide an accessible name. For visible labels, wrap the CheckboxInput in your own <code>&lt;label&gt;</code> element or use a separate linked label.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>checkbox-group</code> — a group component that manages a collection of checkboxes with shared state</li>\n<li><code>radio-input</code> — a single radio button input &lt;input type=\"radio\"&gt;</li>\n<li><code>switch-button</code> — a toggle switch for turning a setting on or off</li>\n<li><code>toggle-button</code> — a button that toggles between pressed and unpressed states</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN checkbox input: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/checkbox</li>\n<li>WAI-ARIA Checkbox Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/checkbox/</li>\n</ul>";
</script>

<svelte:head>
  <title>Checkbox Input — Lily Design System</title>
  <meta name="description" content="A checkbox input is a minimal headless wrapper around the native HTML &lt;input type=&quot;checkbox&quot;&gt; element. Unlike a Checkbox component which includes a wrapping &lt;label&gt;, CheckboxInput uses aria-label f..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
