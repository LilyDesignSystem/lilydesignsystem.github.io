<script lang="ts">
  const html: string = "<h1>Tel Input</h1>\n\n<p>A telephone input component that provides a telephone number input field using the native HTML <code>&lt;input type=\"tel\"&gt;</code> element. It includes accessible labeling via <code>aria-label</code> and browser autofill support via <code>autocomplete=\"tel\"</code>, making it suitable for contact forms, registration flows, and profile editors.</p>\n\n<p>The component supports two-way data binding through a bindable <code>value</code> prop, allowing parent components to read and write the telephone number. It also supports <code>required</code> and <code>disabled</code> states for form validation and conditional interactivity.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as a single <code>&lt;input type=\"tel\"&gt;</code> element</li>\n<li>Uses <code>aria-label</code> for accessible naming (no visible label element included; consumers can wrap with their own <code>&lt;label&gt;</code>)</li>\n<li><code>autocomplete=\"tel\"</code> enables browser and password manager autofill for phone numbers</li>\n<li>Bindable <code>value</code> prop uses two-way binding for two-way data binding</li>\n<li>Supports <code>required</code> and <code>disabled</code> HTML attributes natively</li>\n<li>Spreads <code>...restProps</code> onto the <code>&lt;input&gt;</code> for additional attributes like <code>placeholder</code>, <code>pattern</code>, <code>maxlength</code>, etc.</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name for the input via <code>aria-label</code></li>\n<li><code>value</code>: string (default: <code>\"\"</code>) -- bindable telephone number value</li>\n<li><code>required</code>: boolean (default: <code>false</code>) -- whether the field is required for form submission</li>\n<li><code>disabled</code>: boolean (default: <code>false</code>) -- whether the field is disabled</li>\n<li><code>...restProps</code>: any -- additional HTML attributes spread onto the <code>&lt;input&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Contact form phone number:</p>\n\n<pre><code class=\"language-html\">&lt;TelInput label=\"Phone number\" value={phone} required placeholder=\"+1-555-0100\" /&gt;</code></pre>\n\n<p>Emergency contact with pattern validation:</p>\n\n<pre><code class=\"language-html\">&lt;TelInput label=\"Emergency contact number\" value={emergencyPhone} required pattern=\"[+][0-9\\-]+\" /&gt;</code></pre>\n\n<p>Optional mobile number:</p>\n\n<pre><code class=\"language-html\">&lt;TelInput label=\"Mobile number\" value={mobile} /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Native <code>&lt;input type=\"tel\"&gt;</code> keyboard behavior applies (Tab to focus, type to enter digits)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label</code> -- provides the accessible name for the input, set from the <code>label</code> prop</li>\n<li><code>autocomplete=\"tel\"</code> -- signals browsers to offer telephone number autofill</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for entering a telephone number with the correct mobile keyboard.</li>\n<li>Use with appropriate autocomplete attributes for faster form completion.</li>\n<li>Use in contact forms, registration flows, and profile editors.</li>\n<li>Use when browser autofill for phone numbers (<code>autocomplete=\"tel\"</code>) is beneficial.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for general numeric input -- use NumberInput instead.</li>\n<li>Do not use for displaying a phone number as a clickable link -- use TelLink instead.</li>\n<li>Do not use when you need formatted phone display without an input field.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component renders a native <code>&lt;input type=\"tel\"&gt;</code> with <code>aria-label</code>, <code>autocomplete=\"tel\"</code>, and two-way value binding. It provides telephone input semantics with browser autofill support. The consumer provides all visual styling including borders, padding, placeholder text, and validation feedback.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.tel-input</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;input&gt;</code> element with class <code>tel-input</code></li>\n<li>Verify aria-label<code> -- provides the accessible name for the input, set from the </code>label` prop</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Show an example format as placeholder text (e.g., \"+1-555-0100\") to guide user input. Place the phone input near related contact fields.</li>\n<li><strong>Developers</strong>: Use the <code>pattern</code> attribute via <code>restProps</code> for client-side validation of phone number formats. Pair TelInput with TelLink to create an input/display pair for phone numbers.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>TelInput and TelLink follow the Input/Link pattern. TelInput provides the editable telephone input for forms, while TelLink provides the read-only clickable <code>tel:</code> link for display. Use them together for edit and view modes of phone number data.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>tel-link</code> — a tel hyperlink for a telephone number</li>\n<li><code>text-input</code> — a single-line text input field &lt;input type=\"text\"&gt;</li>\n<li><code>email-input</code> — an input for entering an email address &lt;input type=\"email\"&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>HTML tel input: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/tel</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<label class=\"label\" for=\"demo-input\">Label</label><input class=\"tel-input\" type=\"tel\" id=\"demo-input\" aria-label=\"TelInput\" />";
  const svelteSource: string = "// In your Svelte component:\nimport TelInput from \"lily-design-system-svelte-headless/components/TelInput/TelInput.svelte\";\n\n<TelInput label=\"Label\" />\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Tel Input — Lily Design System</title>
  <meta name="description" content="A telephone input component that provides a telephone number input field using the native HTML &lt;input type=&quot;tel&quot;&gt; element. It includes accessible labeling via aria-label and browser autofill suppor..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

