<script lang="ts">
  const html: string = "<h1>Error Message</h1>\n\n<p>An error message component informs users when something has gone wrong, such as incorrect input, failed actions, or system issues. It uses <code>role=\"alert\"</code> to ensure screen readers announce the error immediately when it appears, without requiring user focus.</p>\n\n<p>The component renders a <code>&lt;p&gt;</code> element with <code>role=\"alert\"</code>, making it an assertive live region. This means the error text is announced to assistive technology users as soon as it enters the DOM, which is critical for form validation feedback and system error notifications.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;p&gt;</code> element with <code>role=\"alert\"</code> for immediate screen reader announcement</li>\n<li>The alert role creates an implicit <code>aria-live=\"assertive\"</code> region</li>\n<li>Children provide the error text content via a slot</li>\n<li>Spreads <code>restProps</code> onto the <code>&lt;p&gt;</code> element for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>children</code>: slot (required) -- error message text content</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;Field label=\"Date of birth\" error=\"true\"&gt;\n  &lt;Hint&gt;For example, 15 3 1984&lt;/Hint&gt;\n  &lt;ErrorMessage&gt;Enter your date of birth&lt;/ErrorMessage&gt;\n  &lt;DateInput label=\"Date of birth\" aria-invalid=\"true\" /&gt;\n&lt;/Field&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;Field label=\"Email address\" error=\"true\"&gt;\n  &lt;ErrorMessage&gt;Enter an email address in the correct format, like name@example.com&lt;/ErrorMessage&gt;\n  &lt;EmailInput label=\"Email address\" aria-invalid=\"true\" value={email} /&gt;\n&lt;/Field&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive container for displaying error text.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"alert\"</code> -- creates an assertive live region so the error message is announced immediately by screen readers when it appears</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use when a form field has a validation error and the user needs to correct their input</li>\n<li>Use to explain clearly what went wrong and how to fix it, in plain language</li>\n<li>Use after the user submits the form — not while they are still typing or navigating fields</li>\n<li>Use alongside ErrorSummary at the top of the page to link users to each error</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not show error messages when the user focuses a field, while they are typing, or when they leave a field — wait for form submission</li>\n<li>Do not use for eligibility or permission errors — take the user to a separate page explaining why and what to do next</li>\n<li>Do not use in place of Hint — preventive guidance should appear before errors occur</li>\n<li>Do not clear the user's input when showing the error — preserve what they entered (WCAG 2.2)</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;p&gt;</code> element with <code>role=\"alert\"</code>, creating an assertive live region that is announced immediately by screen readers when it appears in the DOM. The consumer provides all visual styling such as color, typography, spacing, and error icons.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.error-message</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;span&gt;</code> element with class <code>error-message</code></li>\n<li>Verify role=\"alert\"` -- creates an assertive live region so the error message is announced immediately by screen readers when it appears</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use a distinct error color (typically red) and consider an error icon to make the message visually prominent. Position the error message directly below the associated input field.</li>\n<li><strong>Developers</strong>: Conditionally render the ErrorMessage so it only appears when an error exists. The <code>role=\"alert\"</code> ensures it is announced on insertion without extra ARIA live-region setup.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>error-summary</code> — a summary of all validation errors on a form</li>\n<li><code>hint</code> — hint text providing guidance for a form field</li>\n<li><code>alert</code> — a status message for important information or feedback</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Alert Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/alert/</li>\n</ul>";
</script>

<svelte:head>
  <title>Error Message — Lily Design System</title>
  <meta name="description" content="An error message component informs users when something has gone wrong, such as incorrect input, failed actions, or system issues. It uses role=&quot;alert&quot; to ensure screen readers announce the error i..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
