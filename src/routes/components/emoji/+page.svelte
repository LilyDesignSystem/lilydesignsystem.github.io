<script lang="ts">
  const html: string = "<h1>Emoji</h1>\n\n<p>An emoji component wraps an emoji character with proper accessibility semantics so that screen readers announce a meaningful description instead of attempting to read the raw Unicode character. This ensures emoji usage in interfaces is accessible to all users.</p>\n\n<p>The component renders a <code>&lt;span&gt;</code> with <code>role=\"img\"</code> and a required <code>aria-label</code>, treating the emoji as an image with an accessible name. This approach is recommended for any emoji that conveys meaning, as opposed to purely decorative emoji which should be hidden from assistive technology.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;span&gt;</code> with <code>role=\"img\"</code> to treat the emoji as an image element</li>\n<li>Requires an <code>aria-label</code> describing the emoji meaning for screen readers</li>\n<li>Displays the emoji character as text content inside the span</li>\n<li>Spreads <code>restProps</code> onto the <code>&lt;span&gt;</code> element for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>emoji</code>: string (required) -- the emoji character(s) to display</li>\n<li><code>label</code>: string (required) -- accessible name describing the emoji for screen readers</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Status indicator with meaningful emoji:</p>\n\n<pre><code class=\"language-html\">&lt;Emoji emoji=\"✅\" label=\"Approved\" /&gt;</code></pre>\n\n<p>Warning emoji alongside alert text:</p>\n\n<pre><code class=\"language-html\">&lt;Emoji emoji=\"⚠️\" label=\"Warning\" /&gt; This action cannot be undone.</code></pre>\n\n<p>Reaction emoji in a message:</p>\n\n<pre><code class=\"language-html\">&lt;Emoji emoji=\"👍\" label=\"Thumbs up\" /&gt;\n&lt;Emoji emoji=\"❤️\" label=\"Love\" /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive inline display element.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"img\"</code> -- identifies the emoji as an image for assistive technologies</li>\n<li><code>aria-label</code> -- provides a textual description of the emoji character for screen readers</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to display a single emoji character with an accessible name for screen readers.</li>\n<li>Use when the emoji conveys meaning and needs to be announced with a descriptive label.</li>\n<li>Use in interfaces where emoji supplement text content, such as status indicators, reactions, or labels.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for selecting emojis -- use EmojiCharacterPicker instead.</li>\n<li>Do not use for decorative icons -- use Icon instead.</li>\n<li>Do not use for purely decorative emoji that add no informational value; hide those with <code>aria-hidden=\"true\"</code> instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;span&gt;</code> with <code>role=\"img\"</code> and a required <code>aria-label</code> to ensure emoji are announced meaningfully by screen readers. The consumer provides all visual styling such as font size, spacing, and color adjustments.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.emoji</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;span&gt;</code> element with class <code>emoji</code></li>\n<li>Verify role=\"img\"` -- identifies the emoji as an image for assistive technologies</li>\n<li>Verify aria-label` -- provides a textual description of the emoji character for screen readers</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Ensure emoji are large enough to be recognizable and are not the sole means of conveying information. Pair emoji with text labels when clarity is important.</li>\n<li><strong>Developers</strong>: Always provide a descriptive <code>label</code> that conveys the emoji's meaning in context, not just its Unicode name (e.g., \"Approved\" rather than \"Check mark\").</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>emoji-character-picker</code> — a picker for browsing and selecting emoji characters</li>\n<li><code>character</code> — a single character display element</li>\n<li><code>icon</code> — a container for displaying an icon</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/HTML/Element/span\">MDN span element</a></li>\n</ul>";
</script>

<svelte:head>
  <title>Emoji — Lily Design System</title>
  <meta name="description" content="An emoji component wraps an emoji character with proper accessibility semantics so that screen readers announce a meaningful description instead of attempting to read the raw Unicode character. Thi..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
