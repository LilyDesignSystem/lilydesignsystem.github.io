<script lang="ts">
  const html: string = "<h1>Card</h1>\n\n<p>A card groups related content and actions into a self-contained semantic container, making it easy to browse and compare items. Cards are commonly used for product listings, user profiles, article previews, media galleries, and dashboard widgets. They present a digestible summary of information that can include headings, text, and interactive elements.</p>\n\n<p>The component renders as an <code>&lt;article&gt;</code> element for standalone content grouping. It supports a configurable heading level (h2-h6) for proper document outline, and the heading can optionally be wrapped in a link when an <code>href</code> is provided. An optional <code>aria-label</code> enables the article to be identified as a named landmark.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses <code>&lt;article&gt;</code> for semantic standalone content grouping</li>\n<li>Heading level is configurable (2-6) for proper document outline structure</li>\n<li>When <code>href</code> is provided, the heading text wraps in an <code>&lt;a&gt;</code> element for navigation</li>\n<li>Optional <code>aria-label</code> for named landmark identification</li>\n<li>Content is provided through child elements</li>\n<li>Passes through all additional HTML attributes via <code>...restProps</code></li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>heading</code>: string (optional) -- heading text for the card</li>\n<li><code>headingLevel</code>: 2 | 3 | 4 | 5 | 6 (default: 3) -- HTML heading level</li>\n<li><code>href</code>: string (optional) -- makes the heading a link</li>\n<li><code>label</code>: string (optional) -- <code>aria-label</code> for the article</li>\n<li><code>children</code>: slot (required) -- card body content</li>\n<li><code>...restProps</code>: Any additional HTML attributes passed to the <code>&lt;article&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Card with linked heading for a condition page:</p>\n\n<pre><code class=\"language-html\">&lt;Card heading=\"Diabetes\" headingLevel={2} href=\"/conditions/diabetes\"&gt;\n  &lt;p&gt;Find out about type 1 and type 2 diabetes, including symptoms,\n  diagnosis, and how to manage the condition.&lt;/p&gt;\n&lt;/Card&gt;</code></pre>\n\n<p>Card with action link for a profile:</p>\n\n<pre><code class=\"language-html\">&lt;Card heading=\"Dr Sarah Mitchell\" label=\"GP profile\"&gt;\n  &lt;p&gt;General Practitioner&lt;/p&gt;\n  &lt;p&gt;Special interests: cardiology, respiratory medicine&lt;/p&gt;\n  &lt;ActionLink href=\"/book/dr-mitchell\"&gt;Book an appointment&lt;/ActionLink&gt;\n&lt;/Card&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus the heading link when <code>href</code> is provided</li>\n<li>Enter: Activate the heading link (browser default)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Implicit <code>article</code> role from the <code>&lt;article&gt;</code> element</li>\n<li>Optional <code>aria-label</code> -- identifies the card as a named landmark for screen readers</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to group related content such as a service listing, team member profile, or article preview into a scannable unit.</li>\n<li>Use when users need to browse and compare multiple items in a grid or list layout.</li>\n<li>Use when the content is self-contained and makes sense as a standalone summary.</li>\n<li>Use the <code>href</code> prop to make the card heading a link when the card leads to a detail page.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use to highlight content within long-form pages -- use InsetText or WarningCallout instead.</li>\n<li>Do not use for medical care instructions with urgency levels -- use CareCard instead.</li>\n<li>Do not use for single full-width content sections -- use Panel or a plain section element.</li>\n<li>Do not use when content needs to be hidden/revealed -- use Details or Expander instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless Card component provides the semantic <code>&lt;article&gt;</code> element, configurable heading level for document outline, optional heading link via <code>href</code>, and <code>aria-label</code> for landmark identification. The consumer provides all visual styling including card borders, shadows, spacing, background colors, and layout of header, body, and footer areas.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.card</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>card</code></li>\n<li>Verify Optional <code>aria-label</code> -- identifies the card as a named landmark for screen readers</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Keep card content concise and scannable. Use consistent card heights within a grid to maintain visual rhythm.</li>\n<li><strong>Developers</strong>: Choose the heading level carefully to preserve the document outline hierarchy. Use the <code>label</code> prop when multiple cards appear on one page to help screen reader users distinguish between them.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>Card supports header, body, and footer content areas through its children slot. Structure the card content with a heading for the header area, paragraph or descriptive content for the body, and action links or buttons for the footer. The <code>heading</code> prop provides a built-in heading element, or consumers can supply their own heading as part of the children content.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>feature-card</code> — a large content card with a prominent image positioned alongside or above the text</li>\n<li><code>product-card</code> — a specialized card for displaying a product with image, title, price, and actions</li>\n<li><code>panel</code> — a generic content panel with optional heading</li>\n<li><code>tile</code> — a grouping container that presents related content in a structured format, often as a single clickable area</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/HTML/Element/div\">MDN div element</a></li>\n</ul>";
</script>

<svelte:head>
  <title>Card — Lily Design System</title>
  <meta name="description" content="A card groups related content and actions into a self-contained semantic container, making it easy to browse and compare items. Cards are commonly used for product listings, user profiles, article ..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
