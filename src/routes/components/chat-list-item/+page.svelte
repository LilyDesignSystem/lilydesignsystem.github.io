<script lang="ts">
  const html: string = "<h1>Chat List Item</h1>\n\n<p>A chat list item is one chat list item component, typically containing one chat message component. It represents a single entry in a ChatList, providing the list item semantics for an individual chat conversation preview.</p>\n\n<p>This headless component uses the native HTML <code>&lt;li&gt;</code> element for inherent list item semantics. It is designed to be placed inside a ChatList component and to contain a ChatMessage child.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders an <code>&lt;li&gt;</code> element for list item semantics</li>\n<li>Consumer places a ChatMessage component as a child</li>\n<li>Supports <code>current</code> prop for marking the currently selected conversation</li>\n<li><code>aria-current</code> rendered when the <code>current</code> prop is provided</li>\n<li>Spreads <code>restProps</code> onto the <code>&lt;li&gt;</code> for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>current</code>: boolean (default: false) -- marks this item as the currently selected conversation</li>\n<li><code>children</code>: slot (required) -- typically a ChatMessage component</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;li&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;ChatNav label=\"Chat conversations\"&gt;\n  &lt;ChatList&gt;\n    &lt;ChatListItem current&gt;\n      &lt;ChatMessage&gt;...&lt;/ChatMessage&gt;\n    &lt;/ChatListItem&gt;\n    &lt;ChatListItem&gt;\n      &lt;ChatMessage&gt;...&lt;/ChatMessage&gt;\n    &lt;/ChatListItem&gt;\n  &lt;/ChatList&gt;\n&lt;/ChatNav&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus the interactive element within the list item</li>\n<li>Enter: Activate the focused list item or link</li>\n<li>(Keyboard interactions are primarily handled by the interactive content within)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Implicit <code>listitem</code> role from <code>&lt;li&gt;</code> element</li>\n<li><code>aria-current=\"true\"</code> when the <code>current</code> prop is provided, indicating the active conversation</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside a ChatList component to represent one chat conversation entry.</li>\n<li>Use when each conversation needs to be selectable and navigable.</li>\n<li>Use with the <code>current</code> prop to mark the currently active conversation.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside of a ChatList context -- ChatListItem expects to be part of the ChatNav composition pattern.</li>\n<li>Do not use for non-chat list items -- use ContentsListItem for page section links or TimelineListItem for events.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides an <code>&lt;li&gt;</code> element with optional <code>aria-current</code> for the active conversation, with zero visual styling. The consumer is responsible for all CSS including layout, selected state styling, hover effects, and spacing.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.chat-list-item</code> class for targeting. No default styles are included -- this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders an <code>&lt;li&gt;</code> element with class <code>chat-list-item</code></li>\n<li>Verify <code>aria-current</code> is rendered when <code>current</code> prop is true</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Provide clear visual distinction between the selected conversation and others. Show hover and focus states. Consider showing unread message indicators, timestamps, and message previews.</li>\n<li><strong>Developers</strong>: Use the <code>current</code> prop to mark the active conversation. Place a ChatMessage component as the child content.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>ChatListItem is part of the ChatNav composition pattern:</p>\n\n<ul>\n<li><strong>ChatNav</strong> -- outer <code>&lt;nav&gt;</code> container providing the navigation landmark and accessible label.</li>\n<li><strong>ChatList</strong> -- <code>&lt;ol&gt;</code> list conveying the ordered relationship of chat entries.</li>\n<li><strong>ChatListItem</strong> -- individual <code>&lt;li&gt;</code> items, each typically containing a ChatMessage component.</li>\n<li><strong>ChatMessage</strong> -- <code>&lt;article&gt;</code> element displaying one chat message with author, time, and content.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>chat-nav</code> — a navigation container for chat information</li>\n<li><code>chat-list</code> — an ordered list of chat list item components</li>\n<li><code>chat-message</code> — a chat message shows one chat conversation message entry and all its data, including the author avatar, author name, time, etc.</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN HTML li element: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/li</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<ol class=\"chat-list\"><li class=\"chat-list-item\"><article class=\"chat-message\" aria-label=\"Message\"><p>Example message</p></article></li></ol>";
  const svelteSource: string = "// In your Svelte component:\nimport ChatListItem from \"lily-design-system-svelte-headless/components/ChatListItem/ChatListItem.svelte\";\n\n<ChatListItem>\n  <!-- ChatListItem content -->\n</ChatListItem>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Chat List Item — Lily Design System</title>
  <meta name="description" content="A chat list item is one chat list item component, typically containing one chat message component. It represents a single entry in a ChatList, providing the list item semantics for an individual ch..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

