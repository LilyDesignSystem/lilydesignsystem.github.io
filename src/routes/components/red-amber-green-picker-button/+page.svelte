<script lang="ts">
  const html: string = "<h1>Red Amber Green Picker Button</h1>\n\n<p>A RAG (Red/Amber/Green) picker button is an individual button within a RedAmberGreenPicker that represents one traffic-light status level. When clicked, it selects that RAG status value. The component uses <code>aria-pressed</code> to indicate the currently selected state and <code>data-value</code> to carry the status value, making it accessible to screen readers and enabling CSS-based styling by status color.</p>\n\n<p>Red indicates a critical problem needing immediate attention, amber signals caution or minor issues, and green means everything is on track.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as a <code>&lt;button&gt;</code> element with <code>aria-pressed</code> for toggle state</li>\n<li>Designed to be used inside a RedAmberGreenPicker component</li>\n<li>Each button represents one of three RAG status levels (red, amber, green)</li>\n<li>The <code>selected</code> prop controls <code>aria-pressed</code> to indicate the active selection</li>\n<li>The <code>data-value</code> attribute carries the status value for CSS targeting</li>\n<li>The <code>label</code> prop provides the accessible name via <code>aria-label</code></li>\n<li>Spreads <code>...restProps</code> onto the <code>&lt;button&gt;</code> element for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>value</code>: string (required) -- the RAG status value this button represents (e.g., \"red\", \"amber\", \"green\")</li>\n<li><code>selected</code>: boolean (default: false) -- whether this button is currently selected</li>\n<li><code>label</code>: string (required) -- accessible label for the button (e.g., \"Red - Critical\", \"Amber - Caution\", \"Green - On track\")</li>\n<li><code>onclick</code>: callback -- handler called when the button is clicked</li>\n<li><code>...restProps</code>: any -- additional HTML attributes spread onto the <code>&lt;button&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Basic RAG picker with one button selected:</p>\n\n<pre><code class=\"language-html\">&lt;RedAmberGreenPicker label=\"Project status\"&gt;\n  &lt;RedAmberGreenPickerButton value=\"red\" label=\"Red - Critical\" onclick={handleSelect} /&gt;\n  &lt;RedAmberGreenPickerButton value=\"amber\" label=\"Amber - Caution\" selected onclick={handleSelect} /&gt;\n  &lt;RedAmberGreenPickerButton value=\"green\" label=\"Green - On track\" onclick={handleSelect} /&gt;\n&lt;/RedAmberGreenPicker&gt;</code></pre>\n\n<p>With custom data attributes for CSS targeting:</p>\n\n<pre><code class=\"language-html\">&lt;RedAmberGreenPickerButton\n  value=\"red\"\n  label=\"Red - Immediate action required\"\n  selected\n  data-severity=\"high\"\n  onclick={handleSelect}\n/&gt;</code></pre>\n\n<p>Inside a form field for risk assessment:</p>\n\n<pre><code class=\"language-html\">&lt;Field label=\"Delivery risk\" required&gt;\n  &lt;RedAmberGreenPicker label=\"Delivery risk level\"&gt;\n    &lt;RedAmberGreenPickerButton value=\"red\" label=\"Red - Blocked\" onclick={handleSelect} /&gt;\n    &lt;RedAmberGreenPickerButton value=\"amber\" label=\"Amber - At risk\" onclick={handleSelect} /&gt;\n    &lt;RedAmberGreenPickerButton value=\"green\" label=\"Green - On track\" selected onclick={handleSelect} /&gt;\n  &lt;/RedAmberGreenPicker&gt;\n&lt;/Field&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus the button</li>\n<li>Enter / Space: Activate the button to select this status</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-pressed</code> -- indicates whether this button is the currently selected status (<code>true</code> when selected, <code>false</code> otherwise)</li>\n<li><code>aria-label</code> -- provides the accessible name for the button (e.g., \"Amber - Caution\")</li>\n<li><code>data-value</code> -- carries the status value for CSS-based styling (e.g., <code>data-value=\"red\"</code>)</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside a RedAmberGreenPicker to represent one selectable traffic-light status option.</li>\n<li>Use when you need a button-based RAG picker instead of a select dropdown.</li>\n<li>Use to provide a visually distinct, individually styled button for each RAG level.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside of a RedAmberGreenPicker container -- the button relies on the picker for grouping and context.</li>\n<li>Do not use for standalone toggle actions -- use <a href=\"../toggle-button/\">ToggleButton</a> instead.</li>\n<li>Do not use for standalone option selection -- use <a href=\"../radio-input/\">RadioInput</a> instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>The RedAmberGreenPickerButton headless component provides a <code>&lt;button&gt;</code> with <code>aria-pressed</code> for toggle state, <code>aria-label</code> for accessible naming, and <code>data-value</code> for CSS targeting. The consumer provides all visual styling, including color indicators and selected-state appearance.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.red-amber-green-picker-button</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;button&gt;</code> element with class <code>red-amber-green-picker-button</code></li>\n<li>Verify aria-label` -- provides the accessible name for the button (e.g., \"Amber - Caution\")</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Make the selected state visually prominent with a border, background fill, or scale change, and ensure each button has a text label alongside any color indicator.</li>\n<li><strong>Developers</strong>: Use the <code>data-value</code> attribute for CSS selectors to apply status-specific colors (e.g., <code>[data-value=\"red\"] { background: red; }</code>).</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>red-amber-green-picker</code> — a picker for selecting a red/amber/green status</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Button Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/button/</li>\n<li>RAG Status: https://en.wikipedia.org/wiki/Traffic_light_rating_system</li>\n</ul>";
</script>

<svelte:head>
  <title>Red Amber Green Picker Button — Lily Design System</title>
  <meta name="description" content="A RAG (Red/Amber/Green) picker button is an individual button within a RedAmberGreenPicker that represents one traffic-light status level. When clicked, it selects that RAG status value. The compon..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
