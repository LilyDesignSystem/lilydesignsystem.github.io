<script lang="ts">
  const html: string = "<h1>Action Bar Button</h1>\n\n<p>An action bar button is a single bulk-action trigger inside an <code>action-bar</code>. It uses a native <code>&lt;button&gt;</code> element and requires an accessible label so the action is unambiguously announced to assistive technology.</p>\n\n<p>Use it for each bulk operation surfaced in the action bar — for example, delete, archive, move, share, or export — alongside other action bar buttons.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses a native <code>&lt;button&gt;</code> element for inherent keyboard and accessibility support</li>\n<li><code>label</code> prop is <strong>required</strong> and renders as <code>aria-label</code></li>\n<li>Defaults to <code>type=\"button\"</code> to prevent accidental form submissions</li>\n<li>Native <code>disabled</code> attribute prevents click events automatically</li>\n<li>Children render the visual content (icon and/or text) and are decorative for AT (the <code>aria-label</code> is the source of truth)</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (<strong>required</strong>) -- aria-label</li>\n<li><code>type</code>: \"button\" | \"submit\" | \"reset\" (default: \"button\")</li>\n<li><code>disabled</code>: boolean (default: false)</li>\n<li><code>onclick</code> / <code>onClick</code>: callback (optional) -- click handler</li>\n<li><code>children</code>: slot -- the button content (icon and/or text)</li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;ActionBarButton label=\"Delete\" onClick={handleDelete}&gt;\n  Delete\n&lt;/ActionBarButton&gt;</code></pre>\n\n<p>Disabled action:</p>\n\n<pre><code class=\"language-html\">&lt;ActionBarButton label=\"Move to folder\" disabled&gt;\n  Move\n&lt;/ActionBarButton&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus the button</li>\n<li>Enter: Activate the button</li>\n<li>Space: Activate the button</li>\n<li>(All handled natively by <code>&lt;button&gt;</code> element)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Implicit <code>button</code> role from <code>&lt;button&gt;</code> element</li>\n<li><code>aria-label</code> is the accessible name (required)</li>\n<li><code>aria-disabled</code> from native <code>disabled</code> attribute</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside an <code>action-bar</code> to surface a bulk operation</li>\n<li>Use when the operation acts on the currently selected items</li>\n<li>Use a clear, action-verb label (e.g., \"Delete\", \"Archive\")</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside of an <code>action-bar</code> — use <code>Button</code> or <code>IconButton</code> instead</li>\n<li>Do not omit <code>label</code> — buttons without an accessible name fail WCAG</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>The action bar button is a child of <code>action-bar</code>:</p>\n\n<pre><code class=\"language-html\">&lt;ActionBar label=\"Bulk actions\" selectedCount={2} selectedCountLabel=\"2 selected\"&gt;\n  &lt;ActionBarButton label=\"Delete\"&gt;Delete&lt;/ActionBarButton&gt;\n  &lt;ActionBarButton label=\"Archive\"&gt;Archive&lt;/ActionBarButton&gt;\n&lt;/ActionBar&gt;</code></pre>\n\n<h2>Headless</h2>\n\n<p>Renders a native <code>&lt;button&gt;</code> with the required <code>aria-label</code>. The icon, text, focus indicator, hover state, and any visual treatment are entirely the consumer's responsibility.</p>\n\n<h2>Styles</h2>\n\n<p>Consumer CSS targets the <code>action-bar-button</code> class. Provide a clear focus indicator and hover/active visual feedback. Maintain a 44×44 px touch target.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;button&gt;</code> element with class <code>action-bar-button</code></li>\n<li>Verify <code>aria-label</code> equals the <code>label</code> prop</li>\n<li>Verify <code>type</code> defaults to <code>\"button\"</code></li>\n<li>Verify <code>disabled</code> attribute is applied when <code>disabled</code> is true</li>\n<li>Verify the click handler fires on Enter, Space, and click</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use a consistent visual treatment across all action bar buttons in a single bar.</li>\n<li><strong>Developers</strong>: Always supply a <code>label</code> prop. Include an icon plus visible text whenever space allows.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>action-bar</code> — a contextual action bar that appears when items are selected, showing the selection count and bulk action buttons</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Button Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/button/</li>\n<li>Adobe Spectrum Action Bar: https://spectrum.adobe.com/page/action-bar/</li>\n<li>WCAG 4.1.2 Name, Role, Value: https://www.w3.org/WAI/WCAG22/Understanding/name-role-value</li>\n</ul>";
</script>

<svelte:head>
  <title>Action Bar Button — Lily Design System</title>
  <meta name="description" content="An action bar button is a single bulk-action trigger inside an action-bar. It uses a native &lt;button&gt; element and requires an accessible label so the action is unambiguously announced to assistive t..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
