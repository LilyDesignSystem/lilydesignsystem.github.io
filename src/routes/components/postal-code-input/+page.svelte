<script lang="ts">
  const html: string = "<h1>Postal Code Input</h1>\n\n<p>A postal code input provides a text field specifically designed for entering postal or ZIP codes. It includes <code>autocomplete=\"postal-code\"</code> to enable browsers to auto-fill the value from stored address data, streamlining address form completion.</p>\n\n<p>This component is useful in address forms, checkout flows, shipping calculators, and any interface that requires postal code entry. It pairs with PostalCodeView for displaying the entered value in read-only contexts. The bindable value prop supports two-way data binding with the parent component.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a native <code>&lt;input type=\"text\"&gt;</code> element (not <code>type=\"number\"</code>, since postal codes may contain letters or dashes)</li>\n<li>Uses <code>autocomplete=\"postal-code\"</code> to hint browser autofill for postal/ZIP code fields</li>\n<li>Uses <code>aria-label</code> for accessible naming (no visible label element included; consumers can add their own)</li>\n<li>Supports two-way binding for the <code>value</code> prop, enabling two-way <code>value</code> binding in the parent</li>\n<li>Supports two-way binding on the <code>value</code> prop</li>\n<li>Supports standard HTML form attributes: <code>required</code>, <code>disabled</code></li>\n<li>Spreads <code>restProps</code> for consumer customization (e.g., <code>placeholder</code>, <code>pattern</code>, <code>maxlength</code>)</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name via <code>aria-label</code></li>\n<li><code>value</code>: string (default: <code>\"\"</code>, bindable) -- current postal code value, supports two-way binding</li>\n<li><code>required</code>: boolean (default: <code>false</code>) -- whether the field is required</li>\n<li><code>disabled</code>: boolean (default: <code>false</code>) -- whether the field is disabled</li>\n<li><code>...restProps</code>: any additional HTML attributes spread onto the input</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Basic postal code input:</p>\n\n<pre><code class=\"language-html\">&lt;PostalCodeInput label=\"Postal code\" value={postalCode} /&gt;</code></pre>\n\n<p>Required ZIP code input for a US address form:</p>\n\n<pre><code class=\"language-html\">&lt;PostalCodeInput label=\"ZIP code\" value={zipCode} required placeholder=\"90210\" /&gt;</code></pre>\n\n<p>UK postcode input with placeholder format hint:</p>\n\n<pre><code class=\"language-html\">&lt;PostalCodeInput label=\"Postcode\" value={postcode} placeholder=\"SW1A 1AA\" /&gt;</code></pre>\n\n<p>Postal code input within an address form:</p>\n\n<pre><code class=\"language-html\">&lt;Form label=\"Shipping address\" onsubmit={handleSubmit}&gt;\n  &lt;Field label=\"Street address\" required&gt;\n    &lt;TextInput label=\"Street address\" value={street} required /&gt;\n  &lt;/Field&gt;\n  &lt;Field label=\"City\" required&gt;\n    &lt;TextInput label=\"City\" value={city} required /&gt;\n  &lt;/Field&gt;\n  &lt;Field label=\"Postcode\" required&gt;\n    &lt;PostalCodeInput label=\"Postcode\" value={postcode} required placeholder=\"SW1A 1AA\" /&gt;\n  &lt;/Field&gt;\n  &lt;Button type=\"submit\"&gt;Save address&lt;/Button&gt;\n&lt;/Form&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None beyond native input behavior -- standard text editing keys (typing, backspace, selection, clipboard shortcuts) are handled by the browser.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides an accessible name for the input since no visible <code>&lt;label&gt;</code> element is included</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use in address forms, checkout flows, and shipping calculators where users need to enter a postal or ZIP code.</li>\n<li>Use when you want browser autofill support via <code>autocomplete=\"postal-code\"</code>.</li>\n<li>Use in registration forms or location-based search interfaces that require postal code entry.</li>\n<li>Use for delivery address entry where postal code validation is important.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for displaying a postal code in read-only format -- use PostalCodeView instead.</li>\n<li>Do not use for general text or numeric input -- use TextInput or NumberInput instead.</li>\n<li>Do not use for full address entry -- combine with other address fields in a form.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>The PostalCodeInput headless component provides a native <code>&lt;input type=\"text\"&gt;</code> with <code>autocomplete=\"postal-code\"</code>, <code>aria-label</code> for accessible naming, and two-way value binding. The consumer provides all visual styling, validation patterns, placeholder text, and layout.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.postal-code-input</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;input&gt;</code> element with class <code>postal-code-input</code></li>\n<li>Verify aria-label={label}<code> -- provides an accessible name for the input since no visible </code>&lt;label&gt;` element is included</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Size the input field to match the expected postal code format for your target locale, and include a placeholder showing the expected format (e.g., \"SW1A 1AA\" or \"90210\").</li>\n<li><strong>Developers</strong>: Use the <code>pattern</code> attribute via <code>restProps</code> to validate locale-specific postal code formats, and pair with PostalCodeView for read-only display contexts.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>PostalCodeInput is the editable counterpart of PostalCodeView. Use PostalCodeInput for data entry and PostalCodeView for read-only display of the same postal code value.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>postal-code-view</code> — a read-only display of a postal or ZIP code</li>\n<li><code>text-input</code> — a single-line text input field &lt;input type=\"text\"&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>HTML autocomplete attribute: https://developer.mozilla.org/en-US/docs/Web/HTML/Attributes/autocomplete</li>\n<li>MDN input type=\"text\": https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/text</li>\n</ul>";
</script>

<svelte:head>
  <title>Postal Code Input — Lily Design System</title>
  <meta name="description" content="A postal code input provides a text field specifically designed for entering postal or ZIP codes. It includes autocomplete=&quot;postal-code&quot; to enable browsers to auto-fill the value from stored addres..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
