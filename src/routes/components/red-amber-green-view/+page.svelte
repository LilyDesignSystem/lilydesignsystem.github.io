<script lang="ts">
  const html: string = "<h1>Red Amber Green View</h1>\n\n<p>A RAG (Red/Amber/Green) view is a component that displays a traffic-light status value as read-only text. It is used to present previously captured RAG status data in dashboards, reports, or detail views. Red indicates a critical problem, amber signals caution or minor issues, and green means everything is on track.</p>\n\n<p>The component renders as a <code>&lt;span&gt;</code> element with an accessible label, making the status readable by screen readers and other assistive technologies. This headless component provides the semantic structure and accessibility features while allowing consumers to apply their own visual styling, such as color-coded backgrounds or text colors.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as <code>&lt;span aria-label={label}&gt;</code> displaying the RAG status value as text content</li>\n<li>Read-only display component, not interactive</li>\n<li>Value defaults to an empty string when not provided</li>\n<li>Uses props rune for prop destructuring</li>\n<li>Spreads <code>...restProps</code> on the <code>&lt;span&gt;</code> element for consumer extensibility</li>\n<li>No hardcoded user-facing strings; label and value are fully configurable</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible label for screen readers via <code>aria-label</code></li>\n<li><code>value</code>: string (default: <code>\"\"</code>) -- the RAG status to display (e.g., \"red\", \"amber\", \"green\")</li>\n<li><code>...restProps</code>: any additional HTML attributes spread onto the <code>&lt;span&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Display RAG status in a project dashboard:</p>\n\n<pre><code class=\"language-html\">&lt;RedAmberGreenView label=\"Project status\" value=\"green\" /&gt;</code></pre>\n\n<p>Display dynamic status in a summary card:</p>\n\n<pre><code class=\"language-html\">&lt;SummaryList&gt;\n  &lt;SummaryListItem&gt;\n    &lt;dt&gt;Health check&lt;/dt&gt;\n    &lt;dd&gt;&lt;RedAmberGreenView label=\"Health check status\" value={status} /&gt;&lt;/dd&gt;\n  &lt;/SummaryListItem&gt;\n&lt;/SummaryList&gt;</code></pre>\n\n<p>With data attribute for CSS color coding:</p>\n\n<pre><code class=\"language-html\">&lt;RedAmberGreenView label=\"Compliance status\" value=\"amber\" data-value=\"amber\" /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this is a passive informational display, not an interactive element.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides the accessible name for the status display, allowing screen readers to announce the context of the value</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to display a red/amber/green status in read-only format in dashboards and reports.</li>\n<li>Use in summary cards and detail views where the RAG status has already been set.</li>\n<li>Use when presenting historical or locked status data that should not be edited.</li>\n<li>Use in tables and lists to show RAG status alongside other data fields.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when users need to select or change the status -- use <a href=\"../red-amber-green-picker/\">RedAmberGreenPicker</a> instead.</li>\n<li>Do not use for five-level status display -- use <a href=\"../red-orange-yellow-green-blue-view/\">RedOrangeYellowGreenBlueView</a> instead.</li>\n<li>Do not use for general-purpose text display -- use a <code>&lt;span&gt;</code> or <a href=\"../badge/\">Badge</a> instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>The RedAmberGreenView headless component provides a <code>&lt;span&gt;</code> with <code>aria-label</code> for accessible naming and displays the RAG status value as text content. The consumer provides all visual styling, including color-coded backgrounds, text colors, or badge indicators.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.red-amber-green-view</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>red-amber-green-view</code></li>\n<li>Verify aria-label={label}` -- provides the accessible name for the status display, allowing screen readers to announce the context of the value</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Pair the color indicator with a text label (e.g., \"Green - On track\") so the status is understandable without relying on color alone.</li>\n<li><strong>Developers</strong>: Apply CSS styles using the text content or a <code>data-*</code> attribute to dynamically color-code the display based on the status value.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>red-amber-green-picker</code> — a picker for selecting a red/amber/green status</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA status role: https://www.w3.org/TR/wai-aria-1.2/#status</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<span class=\"red-amber-green-view\" role=\"img\" aria-label=\"Status: Green\">Green</span>";
  const svelteSource: string = "// In your Svelte component:\nimport RedAmberGreenView from \"lily-design-system-svelte-headless/components/RedAmberGreenView/RedAmberGreenView.svelte\";\n\n<RedAmberGreenView label=\"Label\" value=\"Sample value\" />\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Red Amber Green View — Lily Design System</title>
  <meta name="description" content="A RAG (Red/Amber/Green) view is a component that displays a traffic-light status value as read-only text. It is used to present previously captured RAG status data in dashboards, reports, or detail..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

