<script lang="ts">
  const html: string = "<h1>Pagination Nav</h1>\n\n<p>A pagination nav is a navigation container for page navigation links, allowing users to move between pages of content such as search results, product listings, or data tables. It renders as a <code>&lt;nav&gt;</code> element with an <code>aria-label</code>, providing a navigation landmark that can be identified by assistive technologies. The component contains a PaginationList with PaginationListItem children for individual page links.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as a <code>&lt;nav&gt;</code> element to create a navigation landmark for pagination controls</li>\n<li>The <code>label</code> prop sets <code>aria-label</code> to distinguish this navigation region from other <code>&lt;nav&gt;</code> elements on the page</li>\n<li>Consumer provides a PaginationList with PaginationListItem children inside the nav</li>\n<li>Spreads <code>...restProps</code> onto the <code>&lt;nav&gt;</code> element for consumer customization</li>\n<li>No internal state -- purely a structural wrapper providing the navigation landmark</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name for the navigation landmark via <code>aria-label</code></li>\n<li><code>children</code>: slot (required) -- PaginationList with page navigation items</li>\n<li><code>...restProps</code>: any -- additional HTML attributes spread onto the <code>&lt;nav&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Previous/next style pagination for a series of related content pages, such as a multi-page health condition guide:</p>\n\n<pre><code class=\"language-html\">&lt;PaginationNav label=\"Pagination\"&gt;\n  &lt;PaginationList&gt;\n    &lt;PaginationListItem&gt;\n      &lt;PaginationLink href=\"/conditions/diabetes/symptoms\" rel=\"prev\"&gt;\n        Previous: Symptoms\n      &lt;/PaginationLink&gt;\n    &lt;/PaginationListItem&gt;\n    &lt;PaginationListItem&gt;\n      &lt;PaginationLink href=\"/conditions/diabetes/treatment\" rel=\"next\"&gt;\n        Next: Treatment\n      &lt;/PaginationLink&gt;\n    &lt;/PaginationListItem&gt;\n  &lt;/PaginationList&gt;\n&lt;/PaginationNav&gt;</code></pre>\n\n<p>Numbered pagination for search results, with <code>aria-current=\"page\"</code> on the active page:</p>\n\n<pre><code class=\"language-html\">&lt;PaginationNav label=\"Search results pages\"&gt;\n  &lt;PaginationList&gt;\n    &lt;PaginationListItem&gt;\n      &lt;PaginationLink href=\"/results?page=1\" label=\"Page 1\"&gt;1&lt;/PaginationLink&gt;\n    &lt;/PaginationListItem&gt;\n    &lt;PaginationListItem&gt;\n      &lt;PaginationLink href=\"/results?page=2\" label=\"Page 2\" aria-current=\"page\"&gt;2&lt;/PaginationLink&gt;\n    &lt;/PaginationListItem&gt;\n    &lt;PaginationListItem&gt;\n      &lt;PaginationLink href=\"/results?page=3\" label=\"Page 3\"&gt;3&lt;/PaginationLink&gt;\n    &lt;/PaginationListItem&gt;\n  &lt;/PaginationList&gt;\n&lt;/PaginationNav&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Moves focus between pagination links within the navigation</li>\n<li>Enter: Activates the focused link to navigate to that page</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>&lt;nav aria-label=\"...\"&gt;</code> -- creates a navigation landmark with a descriptive label for the pagination region</li>\n<li>Consumer applies <code>aria-current=\"page\"</code> on the link representing the current page</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use at the bottom of a page when content is split across multiple related pages to help users navigate forwards and backwards</li>\n<li>Use when showing all content on a single page would make it too long to load or read</li>\n<li>Use with ContentsNav at the top of the page as a mini-hub navigation pattern</li>\n<li>Use instead of infinite scroll, which creates problems for keyboard users</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for unrelated pages that are not part of a grouped series -- use standard navigation links instead</li>\n<li>Do not use for navigating through multi-page forms -- use Button (\"Continue\") with BackLink instead</li>\n<li>Do not use when there is only one page of content -- omit pagination entirely</li>\n<li>Do not use for in-page section navigation -- use ContentsNav instead</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;nav&gt;</code> element with <code>aria-label</code> for navigation landmark identification. The consumer provides all visual styling, including layout, positioning, and responsive behavior of the pagination region.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.pagination-nav</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;nav&gt;</code> element with class <code>pagination-nav</code></li>\n<li>Verify &lt;nav aria-label=\"...\"&gt;` -- creates a navigation landmark with a descriptive label for the pagination region</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Position pagination consistently (typically below the content it paginates) and ensure it is visible without scrolling.</li>\n<li><strong>Developers</strong>: Use a unique <code>aria-label</code> (e.g., \"Search results pages\") to distinguish this navigation from other <code>&lt;nav&gt;</code> landmarks on the page. Nest PaginationList inside.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>PaginationNav is the outermost component in the pagination compound pattern. It contains PaginationList, which contains PaginationListItem children.</p>\n\n<pre><code>PaginationNav (nav) → PaginationList (nav + ul) → PaginationListItem (li)</code></pre>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>pagination-list</code> — an ordered list of pagination list items</li>\n<li><code>pagination-list-item</code> — one pagination list item in the trail</li>\n<li><code>pagination-link</code> — one pagination link in the trail</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Navigation Role: https://www.w3.org/TR/wai-aria-1.2/#navigation</li>\n<li>WAI-ARIA Practices - Navigation: https://www.w3.org/WAI/ARIA/apg/patterns/navigation/</li>\n</ul>";
</script>

<svelte:head>
  <title>Pagination Nav — Lily Design System</title>
  <meta name="description" content="A pagination nav is a navigation container for page navigation links, allowing users to move between pages of content such as search results, product listings, or data tables. It renders as a &lt;nav&gt;..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
