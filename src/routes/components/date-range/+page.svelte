<script lang="ts">
  const html: string = "<h1>Date Range</h1>\n\n<p>A date range is a component that provides paired start and end date inputs, allowing users to specify a span of time. Commonly used in booking forms, search filters, reporting interfaces, and scheduling tools, the date range component groups two date inputs together with a shared label to communicate that they represent a related pair.</p>\n\n<p>This headless component renders a <code>&lt;fieldset&gt;</code> containing two native <code>&lt;input type=\"date\"&gt;</code> elements, each with its own accessible label. Both the start and end values support two-way binding for reactive state management.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;fieldset&gt;</code> to semantically group the paired date inputs</li>\n<li>Uses two native <code>&lt;input type=\"date\"&gt;</code> elements for built-in browser date selection</li>\n<li>Both <code>start</code> and <code>end</code> use two-way binding</li>\n<li>Each input has its own <code>aria-label</code> for individual identification</li>\n<li>Accepts <code>...restProps</code> for forwarding additional attributes to the fieldset</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible group label applied to the fieldset via <code>aria-label</code></li>\n<li><code>startLabel</code>: string (required) -- accessible label for the start date input</li>\n<li><code>endLabel</code>: string (required) -- accessible label for the end date input</li>\n<li><code>start</code>: string (default: \"\") -- start date value in YYYY-MM-DD format; bindable</li>\n<li><code>end</code>: string (default: \"\") -- end date value in YYYY-MM-DD format; bindable</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Hotel booking date range:</p>\n\n<pre><code class=\"language-html\">&lt;DateRange\n    label=\"Stay dates\"\n    startLabel=\"Check-in\"\n    endLabel=\"Check-out\"\n    start={checkIn}\n    end={checkOut}\n/&gt;</code></pre>\n\n<p>Report filter with date range:</p>\n\n<pre><code class=\"language-html\">&lt;DateRange\n    label=\"Report period\"\n    startLabel=\"From date\"\n    endLabel=\"To date\"\n    start={reportStart}\n    end={reportEnd}\n/&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Move focus between the start and end date inputs</li>\n<li>Up Arrow / Down Arrow: Increment/decrement the focused date segment</li>\n<li>Left Arrow / Right Arrow: Move between date segments within an input</li>\n</ul>\n\n<p>(These are provided by the native <code>&lt;input type=\"date\"&gt;</code> elements.)</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> on <code>&lt;fieldset&gt;</code> -- provides an accessible group name for the date range pair</li>\n<li><code>aria-label={startLabel}</code> on the start input -- identifies the start date field</li>\n<li><code>aria-label={endLabel}</code> on the end input -- identifies the end date field</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to display a formatted start and end date range.</li>\n<li>Use for showing booking periods, appointment windows, or report date ranges.</li>\n<li>Use when the two dates are semantically paired and should be grouped under a single label.</li>\n<li>Use in search filters, scheduling tools, and leave request forms where a time span is required.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for selecting a date range visually on a calendar -- use CalendarRangePicker instead.</li>\n<li>Do not use for a single date -- use DateInput or ReviewDate instead.</li>\n<li>Do not use when start and end dates are unrelated and belong to separate form sections.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a semantic <code>&lt;fieldset&gt;</code> grouping two native <code>&lt;input type=\"date\"&gt;</code> elements with accessible labels and two-way binding. It handles ARIA labeling for both the group and individual inputs. The consumer provides all visual styling, layout, spacing, and any validation indicators.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.date-range</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;span&gt;</code> element with class <code>date-range</code></li>\n<li>Verify aria-label={label}<code> on </code>&lt;fieldset&gt;` -- provides an accessible group name for the date range pair</li>\n<li>Verify aria-label={startLabel}` on the start input -- identifies the start date field</li>\n<li>Verify aria-label={endLabel}` on the end input -- identifies the end date field</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Clearly indicate which field is the start and which is the end, using visual labels or positioning. Ensure the date inputs are visually grouped so users understand they form a pair.</li>\n<li><strong>Developers</strong>: Validate that the end date is not before the start date. Use the <code>min</code> and <code>max</code> attributes on the underlying inputs via <code>restProps</code> to constrain selectable ranges.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>date-input</code> — an input for entering a date value &lt;input type=\"date\"&gt;</li>\n<li><code>calendar-range-picker</code> — a picker for selecting a date range on a calendar</li>\n<li><code>date-field</code> — a structured field for entering date components</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN input type=\"date\": https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/date</li>\n<li>MDN fieldset element: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/fieldset</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"date-range\"><time datetime=\"2026-01-01\">1 Jan 2026</time> \u2014 <time datetime=\"2026-12-31\">31 Dec 2026</time></div>";
  const svelteSource: string = "// In your Svelte component:\nimport DateRange from \"lily-design-system-svelte-headless/components/DateRange/DateRange.svelte\";\n\n<DateRange>\n  <!-- content -->\n</DateRange>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Date Range — Lily Design System</title>
  <meta name="description" content="A date range is a component that provides paired start and end date inputs, allowing users to specify a span of time. Commonly used in booking forms, search filters, reporting interfaces, and sched..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

