<script lang="ts">
  const html: string = "<h1>Icon</h1>\n\n<p>An icon is a compact visual element that represents actions, objects, or concepts using simplified graphical symbols. The Icon component provides a headless wrapper around icon content with proper accessibility semantics, supporting both meaningful icons (with accessible labels) and decorative icons (hidden from assistive technology).</p>\n\n<p>This component is useful in buttons, menus, toolbars, and navigation where visual cues help users quickly identify functions. The consumer provides the actual icon content (text characters, SVG, or any markup), while the component handles the semantic role and ARIA attributes.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;span&gt;</code> element as the icon wrapper</li>\n<li>Uses <code>role=\"img\"</code> for meaningful icons that convey information</li>\n<li>Uses <code>aria-hidden=\"true\"</code> for decorative icons that should be ignored by screen readers</li>\n<li>When <code>decorative</code> is true, the <code>role</code> attribute is omitted entirely</li>\n<li>Supports rest props spread for consumer customization</li>\n<li>Consumer provides icon content via the <code>children</code> slot</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (optional) -- accessible name announced by screen readers for meaningful icons</li>\n<li><code>decorative</code>: boolean (default: false) -- when true, hides the icon from assistive technology and removes the img role</li>\n<li><code>children</code>: slot (required) -- the icon content (text, SVG, or other markup)</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;span&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;!-- Meaningful icon with accessible label --&gt;\n&lt;Icon label=\"Close\"&gt;x&lt;/Icon&gt;\n\n&lt;!-- Decorative icon hidden from screen readers --&gt;\n&lt;Icon decorative&gt;*&lt;/Icon&gt;\n\n&lt;!-- Icon with custom attributes --&gt;\n&lt;Icon label=\"Search\" data-testid=\"search-icon\"&gt;Q&lt;/Icon&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive container. Keyboard behavior depends on the parent interactive element (such as a button wrapping the icon).</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"img\"</code> -- applied to meaningful icons so assistive technology treats the span as an image</li>\n<li><code>aria-label=\"...\"</code> -- provides the accessible name for meaningful icons</li>\n<li><code>aria-hidden=\"true\"</code> -- applied when <code>decorative</code> is true, hiding the icon from the accessibility tree</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to display a visual symbol representing an action, object, or concept in buttons, menus, toolbars, or navigation.</li>\n<li>Use with <code>decorative</code> for icons that are purely visual and have adjacent text labels.</li>\n<li>Avoid using a meaningful icon without a <code>label</code> prop; screen reader users would miss the icon's purpose.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use as the only target of a click — wrap interactive icons in <code>IconButton</code> or another button so they pick up button semantics and focus styles.</li>\n<li>Do not use to render a single emoji character that already has a textual meaning — use <code>Emoji</code> for that.</li>\n<li>Do not use as a substitute for a labeled image with caption — use <code>Figure</code> for content imagery.</li>\n<li>Do not use without <code>label</code> for an icon that conveys meaning standalone, and do not pass a <code>label</code> to a decorative icon.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;span&gt;</code> with <code>role=\"img\"</code> and <code>aria-label</code> for meaningful icons, or <code>aria-hidden=\"true\"</code> for decorative icons. The consumer provides all visual content (SVG, text characters, icon fonts) and styling.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.icon</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;span&gt;</code> element with class <code>icon</code></li>\n<li>Verify role=\"img\"` -- applied to meaningful icons so assistive technology treats the span as an image</li>\n<li>Verify aria-label=\"...\"` -- provides the accessible name for meaningful icons</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Ensure icons are visually clear at small sizes. Pair icons with text labels when the meaning may be ambiguous. Maintain consistent icon sizing across the interface.</li>\n<li><strong>Developers</strong>: Set <code>decorative={true}</code> when the icon is adjacent to a text label that conveys the same meaning. Always provide <code>label</code> for standalone icons.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>icon-button</code> — a button containing only an icon with a required accessible label</li>\n<li><code>emoji</code> — a single emoji character with accessible name</li>\n<li><code>qr-code</code> — a QR code image generated from text or URL data</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA img role: https://www.w3.org/TR/wai-aria-1.2/#img</li>\n<li>WAI-ARIA Practices - Images: https://www.w3.org/WAI/tutorials/images/decorative/</li>\n</ul>";
</script>

<svelte:head>
  <title>Icon — Lily Design System</title>
  <meta name="description" content="An icon is a compact visual element that represents actions, objects, or concepts using simplified graphical symbols. The Icon component provides a headless wrapper around icon content with proper ..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
