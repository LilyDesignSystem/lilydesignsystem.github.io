<script lang="ts">
  const html: string = "<h1>Status Light</h1>\n\n<p>A status light is a small colored dot status indicator paired with a status label.</p>\n\n<p>Use it to communicate the state of a record, system, user, or process — \"Active\", \"Offline\", \"Error\", \"Pending\" — at a glance, without relying on color alone because the label is always present.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses a <code>&lt;span&gt;</code> with class <code>status-light</code> and <code>role=\"status\"</code></li>\n<li><code>data-variant</code> exposes the chosen variant for consumer CSS</li>\n<li>The colored dot is <code>&lt;span class=\"status-light-dot\" aria-hidden=\"true\"&gt;</code> because the text label conveys meaning to assistive tech</li>\n<li>The text label is <code>&lt;span class=\"status-light-label\"&gt;{label}&lt;/span&gt;</code></li>\n<li>Variants are: <code>neutral</code>, <code>informative</code>, <code>positive</code>, <code>notice</code>, <code>negative</code>, <code>active</code>, <code>inactive</code></li>\n<li><code>variant</code> defaults to <code>\"neutral\"</code></li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>variant</code>: <code>\"neutral\" | \"informative\" | \"positive\" | \"notice\" | \"negative\" | \"active\" | \"inactive\"</code> (default <code>\"neutral\"</code>) -- status variant; rendered as <code>data-variant</code></li>\n<li><code>label</code>: string (<strong>required</strong>) -- the status text (e.g., \"Active\", \"Offline\")</li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;StatusLight variant=\"positive\" label=\"Active\" /&gt;\n&lt;StatusLight variant=\"negative\" label=\"Offline\" /&gt;\n&lt;StatusLight variant=\"notice\" label=\"Pending review\" /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None — <code>status-light</code> is a passive indicator, not interactive.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"status\"</code> on the container, so updates can be announced as appropriate</li>\n<li>The dot has <code>aria-hidden=\"true\"</code> because color alone is not the accessible indicator</li>\n<li>The text label is the accessible content</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to indicate the current state of an item or system</li>\n<li>Use whenever color is not sufficient on its own — pair the dot with a label</li>\n<li>Use in tables, cards, lists, and headers to summarize status</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use as an interactive control — use <code>Button</code>, <code>ToggleButton</code>, or <code>SwitchButton</code></li>\n<li>Do not use for transient feedback — use <code>Toast</code> or <code>Notification</code></li>\n<li>Do not omit the label — color alone fails accessibility (WCAG 1.4.1 Use of Color)</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>Renders a <code>&lt;span role=\"status\"&gt;</code> containing a hidden dot and a visible label. Visual color and shape are entirely the consumer's responsibility, typically driven by the <code>data-variant</code> attribute.</p>\n\n<h2>Styles</h2>\n\n<p>Consumer CSS targets the <code>status-light</code> class and uses <code>data-variant</code> to choose dot colors. Sub-elements expose <code>status-light-dot</code> and <code>status-light-label</code> for fine-grained styling. Always provide visible text in addition to color.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;span&gt;</code> with class <code>status-light</code></li>\n<li>Verify <code>role=\"status\"</code> is present</li>\n<li>Verify <code>data-variant</code> equals the <code>variant</code> prop (defaulting to <code>\"neutral\"</code>)</li>\n<li>Verify the dot is rendered inside <code>&lt;span class=\"status-light-dot\"&gt;</code> with <code>aria-hidden=\"true\"</code></li>\n<li>Verify the label is rendered inside <code>&lt;span class=\"status-light-label\"&gt;</code></li>\n<li>Verify the label text equals the <code>label</code> prop</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Pair every color with a textual label and an icon shape if possible; never communicate state by color alone.</li>\n<li><strong>Developers</strong>: Localize the <code>label</code> prop. If multiple status lights update at once, consider de-duplicating announcements at the page level.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>status-tag</code> — a tag showing the current status of a task</li>\n<li><code>badge</code> — a small label for counts, statuses, or categories</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA <code>status</code> role: https://www.w3.org/TR/wai-aria-1.2/#status</li>\n<li>WCAG 1.4.1 Use of Color: https://www.w3.org/WAI/WCAG22/Understanding/use-of-color</li>\n<li>Adobe Spectrum Status Light: https://spectrum.adobe.com/page/status-light/</li>\n</ul>";
</script>

<svelte:head>
  <title>Status Light — Lily Design System</title>
  <meta name="description" content="A status light is a small colored dot status indicator paired with a status label." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
