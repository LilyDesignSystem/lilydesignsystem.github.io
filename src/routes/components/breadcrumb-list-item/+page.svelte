<script lang="ts">
  const html: string = "<h1>Breadcrumb List Item</h1>\n\n<p>A breadcrumb list item represents a single step in a breadcrumb navigation trail. It renders as a semantic <code>&lt;li&gt;</code> element designed to be placed inside a BreadcrumbList component (which provides the parent <code>&lt;nav&gt;</code> and <code>&lt;ol&gt;</code> structure). The component supports <code>aria-current=\"page\"</code> to indicate when the item represents the user's current location in the site hierarchy.</p>\n\n<p>Typically, each breadcrumb list item contains a link for navigable ancestor pages, while the final item (current page) contains plain text and is marked with the <code>current</code> prop.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as a semantic <code>&lt;li&gt;</code> element for use inside a breadcrumb <code>&lt;nav&gt;</code> / <code>&lt;ol&gt;</code> structure</li>\n<li>Supports <code>aria-current=\"page\"</code> via the <code>current</code> prop to indicate the active page in the breadcrumb trail</li>\n<li>Content is provided through the children slot, typically an <code>&lt;a&gt;</code> link or plain text</li>\n<li>Passes through all additional HTML attributes via <code>...restProps</code></li>\n<li>Uses <code>slot</code> for children content</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>current</code>: boolean (default: false) -- whether this item represents the current page in the breadcrumb trail</li>\n<li><code>children</code>: slot (required) -- breadcrumb content, typically a link or text</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;BreadcrumbNav label=\"Breadcrumb\"&gt;\n  &lt;BreadcrumbList&gt;\n    &lt;BreadcrumbListItem&gt;\n      &lt;BreadcrumbLink href=\"/\"&gt;Home&lt;/BreadcrumbLink&gt;\n    &lt;/BreadcrumbListItem&gt;\n    &lt;BreadcrumbListItem&gt;\n      &lt;BreadcrumbLink href=\"/health-a-z\"&gt;Health A to Z&lt;/BreadcrumbLink&gt;\n    &lt;/BreadcrumbListItem&gt;\n    &lt;BreadcrumbListItem aria-current=\"page\"&gt;\n      Diabetes\n    &lt;/BreadcrumbListItem&gt;\n  &lt;/BreadcrumbList&gt;\n&lt;/BreadcrumbNav&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None directly -- keyboard interaction is handled by the contained links.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Implicit <code>listitem</code> role from the semantic <code>&lt;li&gt;</code> element</li>\n<li><code>aria-current=\"page\"</code> -- set when <code>current</code> is true, indicating the user's current location in the breadcrumb trail</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside BreadcrumbList to represent one level in the breadcrumb trail</li>\n<li>Use with <code>aria-current=\"page\"</code> on the last item to indicate the current page</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside of BreadcrumbList -- it has no standalone meaning</li>\n<li>Do not include the current page as a link -- the H1 heading already communicates the current page</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides a semantic <code>&lt;li&gt;</code> element with conditional <code>aria-current=\"page\"</code> and zero visual styling. The consumer is responsible for all CSS including link styling, separator decorations, current-page visual treatment, and spacing.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.breadcrumb-list-item</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;li&gt;</code> element with class <code>breadcrumb-list-item</code></li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Style the current page item differently from linked items (e.g., bold text, no underline) to help users identify where they are in the hierarchy.</li>\n<li><strong>Developers</strong>: Set the <code>current</code> prop to <code>true</code> only on the last item in the trail. Use plain text (not a link) for the current page content.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>BreadcrumbListItem follows the Nav / List / ListItem composition pattern:</p>\n\n<ul>\n<li><strong>BreadcrumbNav</strong> -- outer <code>&lt;nav&gt;</code> container providing the navigation landmark.</li>\n<li><strong>BreadcrumbList</strong> -- <code>&lt;ol&gt;</code> list conveying the ordered hierarchy.</li>\n<li><strong>BreadcrumbListItem</strong> -- individual <code>&lt;li&gt;</code> items with links or current page text.</li>\n</ul>\n\n<pre><code class=\"language-html\">&lt;BreadcrumbNav label=\"Breadcrumb\"&gt;\n  &lt;BreadcrumbList&gt;\n    &lt;BreadcrumbListItem&gt;\n      &lt;BreadcrumbLink href=\"/\"&gt;Home&lt;/BreadcrumbLink&gt;\n    &lt;/BreadcrumbListItem&gt;\n    &lt;BreadcrumbListItem&gt;\n      &lt;BreadcrumbLink href=\"/health-a-z\"&gt;Health A to Z&lt;/BreadcrumbLink&gt;\n    &lt;/BreadcrumbListItem&gt;\n    &lt;BreadcrumbListItem aria-current=\"page\"&gt;\n      Diabetes\n    &lt;/BreadcrumbListItem&gt;\n  &lt;/BreadcrumbList&gt;\n&lt;/BreadcrumbNav&gt;</code></pre>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>breadcrumb-nav</code> — a navigation container for breadcrumb trail links</li>\n<li><code>breadcrumb-list</code> — an ordered list of breadcrumb list items</li>\n<li><code>breadcrumb-link</code> — one breadcrumb link in the trail</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Breadcrumb Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/breadcrumb/</li>\n</ul>";
</script>

<svelte:head>
  <title>Breadcrumb List Item — Lily Design System</title>
  <meta name="description" content="A breadcrumb list item represents a single step in a breadcrumb navigation trail. It renders as a semantic &lt;li&gt; element designed to be placed inside a BreadcrumbList component (which provides the p..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
