<script lang="ts">
  const html: string = "<h1>Task List</h1>\n\n<p>A task list is a UI/UX component that organizes and displays a series of tasks, actions, or to-dos for users to complete. It is typically presented as a checklist, where each task can be marked as completed or remain pending. Task lists are commonly used in project management tools, to-do apps, or onboarding processes to guide users through a sequence of actions. They help improve productivity by breaking down complex workflows into manageable steps, offering clear progress tracking. Effective task list design includes features like task prioritization, due dates, and visual indicators (such as checkboxes or progress bars) to enhance user clarity and motivation.</p>\n\n<p>This component renders as an unordered list (<code>&lt;ul&gt;</code>) with <code>role=\"list\"</code> and an accessible label. The explicit <code>role=\"list\"</code> ensures screen readers announce the list semantics even when CSS removes default list styling. The consumer provides task items as <code>&lt;li&gt;</code> children through the children slot.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as <code>&lt;ul role=\"list\" aria-label={label}&gt;</code> for task list semantics</li>\n<li>Explicit <code>role=\"list\"</code> ensures list semantics when CSS removes default styling</li>\n<li>All task content provided through children slot (should be <code>&lt;li&gt;</code> elements)</li>\n<li>Consumers can use checkboxes within <code>&lt;li&gt;</code> for completable tasks</li>\n<li>No hardcoded strings; all text content comes through props and children</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible label via aria-label</li>\n<li><code>children</code>: slot -- task items (should be <code>&lt;li&gt;</code> elements)</li>\n<li><code>...restProps</code>: Any additional HTML attributes spread onto the <code>&lt;ul&gt;</code></li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;TaskList label=\"Your health assessment\"&gt;\n  &lt;TaskListItem href=\"/your-health/exercise\" status=\"completed\"&gt;\n    Exercise\n  &lt;/TaskListItem&gt;\n  &lt;TaskListItem href=\"/your-health/diet\" status=\"incomplete\"&gt;\n    Diet and nutrition\n  &lt;/TaskListItem&gt;\n  &lt;TaskListItem href=\"/your-health/family-history\" status=\"incomplete\"\n    hint=\"Details of your parents, brothers and sisters\"&gt;\n    Family health history\n  &lt;/TaskListItem&gt;\n  &lt;TaskListItem status=\"cannot-start-yet\"&gt;\n    Blood test\n  &lt;/TaskListItem&gt;\n&lt;/TaskList&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>None directly -- standard list navigation by screen readers</li>\n<li>Tab moves focus between interactive elements (checkboxes, links) within task items</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"list\"</code> explicitly marks as a list</li>\n<li><code>aria-label</code> provides accessible name for the list</li>\n<li>Screen readers announce the list with its label and item count</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to show all the tasks a user needs to complete for a lengthy or complex service</li>\n<li>Use when users need to complete tasks across multiple sessions and track their progress</li>\n<li>Use when users should be free to choose the order in which they complete tasks</li>\n<li>Use with status indicators (Completed, Incomplete, Cannot start yet) so users can plan their time</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when the service can be simplified to fewer steps -- simplify before reaching for a task list</li>\n<li>Do not use to display a user's submitted answers -- use SummaryList for the \"check your answers\" pattern</li>\n<li>Do not use for sequential wizard-style forms -- use multi-page forms with Button and BackLink</li>\n<li>Do not use for simple checklists without linked tasks -- use CheckList instead</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component renders a <code>&lt;ul&gt;</code> with explicit <code>role=\"list\"</code> and <code>aria-label</code> to ensure list semantics are preserved even when CSS removes default styling. The consumer provides all visual styling including layout, spacing, completion indicators, and any interactive elements within list items.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.task-list</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;ol&gt;</code> element with class <code>task-list</code></li>\n<li>Verify role=\"list\"` explicitly marks as a list</li>\n<li>Verify aria-label` provides accessible name for the list</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Include clear completion indicators such as checkboxes or strikethrough text. Use visual hierarchy to distinguish completed tasks from pending ones.</li>\n<li><strong>Developers</strong>: Use TaskListItem children for structured task items with completion state. The explicit <code>role=\"list\"</code> ensures screen readers announce item counts even with <code>list-style: none</code> CSS.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>TaskList uses the List/ListItem composition pattern. Place TaskListItem components as children inside TaskList. The TaskList provides the outer <code>&lt;ul&gt;</code> container with accessible labeling, while each TaskListItem provides an individual <code>&lt;li&gt;</code> with completion state tracking via <code>data-completed</code>.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>task-list-item</code> — one task item with a checkbox and label</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA list role: https://www.w3.org/TR/wai-aria-1.2/#list</li>\n</ul>";
</script>

<svelte:head>
  <title>Task List — Lily Design System</title>
  <meta name="description" content="A task list is a UI/UX component that organizes and displays a series of tasks, actions, or to-dos for users to complete. It is typically presented as a checklist, where each task can be marked as ..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
