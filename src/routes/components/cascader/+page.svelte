<script lang="ts">
  const html: string = "<h1>Cascader</h1>\n\n<p>A cascader is a multi-level dropdown for selecting a value from a hierarchy. It exposes a trigger button and a panel that holds nested option lists.</p>\n\n<p>Use it for hierarchical pickers such as country → region → city, category → subcategory → leaf, or any tree-shaped selection where the selected value is a path through the hierarchy.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> with <code>role=\"combobox\"</code>, <code>aria-haspopup=\"tree\"</code>, <code>aria-expanded</code>, <code>aria-label</code></li>\n<li>The trigger is a native <code>&lt;button class=\"cascader-trigger\" type=\"button\"&gt;</code> showing <code>value</code> or <code>placeholder</code></li>\n<li>The panel is a <code>&lt;div class=\"cascader-panel\"&gt;</code> with <code>hidden</code> toggled by the <code>expanded</code> prop</li>\n<li><code>label</code> is <strong>required</strong> and sets <code>aria-label</code> on the combobox container</li>\n<li><code>disabled</code> disables the trigger button</li>\n<li>Children populate the panel (typically nested option lists or trees)</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (<strong>required</strong>) -- aria-label on the combobox</li>\n<li><code>expanded</code>: boolean (default: false) -- whether the panel is open</li>\n<li><code>disabled</code>: boolean (default: false) -- disable the trigger button</li>\n<li><code>placeholder</code>: string (optional) -- placeholder text for the trigger</li>\n<li><code>value</code>: string (optional) -- formatted display value (consumer formats path)</li>\n<li><code>onclick</code> / <code>onClick</code>: callback (optional) -- trigger click handler</li>\n<li><code>children</code>: slot -- panel content (typically nested option lists)</li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;Cascader\n  label=\"Select location\"\n  expanded={isOpen}\n  value={selectedPath}\n  placeholder=\"Choose a region\"\n  onclick={togglePanel}\n&gt;\n  &lt;ul role=\"tree\"&gt;...&lt;/ul&gt;\n&lt;/Cascader&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus the trigger</li>\n<li>Enter / Space: Toggle the panel (consumer handler)</li>\n<li>Arrow keys: Consumer-provided tree navigation inside the panel</li>\n<li>Escape: Consumer should close the panel and return focus to the trigger</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"combobox\"</code> on the container</li>\n<li><code>aria-haspopup=\"tree\"</code> indicates the panel contains tree-shaped options</li>\n<li><code>aria-expanded</code> reflects the <code>expanded</code> prop</li>\n<li><code>aria-label</code> is the entire accessible name (required)</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use when selecting a value from a hierarchy where each level depends on the previous one.</li>\n<li>Use when users need to see ancestors of a deeply nested option (e.g., country/region/city).</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for flat lists — use Select or Combobox.</li>\n<li>Do not use for very small hierarchies — use nested radio groups or a TreeSelect.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>Renders semantic combobox markup with no styling. The consumer is responsible for opening/closing the panel, focus management, keyboard navigation, and the visual treatment of the trigger and panel.</p>\n\n<h2>Styles</h2>\n\n<p>Consumer CSS targets the <code>cascader</code>, <code>cascader-trigger</code>, and <code>cascader-panel</code> classes. Provide visible focus indicators, disabled state styling, and a clear visual distinction between collapsed and expanded states.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>cascader</code></li>\n<li>Verify <code>role=\"combobox\"</code> and <code>aria-haspopup=\"tree\"</code> are set</li>\n<li>Verify <code>aria-expanded</code> reflects the <code>expanded</code> prop</li>\n<li>Verify <code>aria-label</code> equals the <code>label</code> prop</li>\n<li>Verify the trigger button is disabled when <code>disabled</code> is true</li>\n<li>Verify the panel is <code>hidden</code> when <code>expanded</code> is false</li>\n<li>Verify children render inside the panel</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Show the selected path on the trigger so users can verify their choice without reopening the cascader. Keep depth shallow (3-4 levels).</li>\n<li><strong>Developers</strong>: Manage <code>expanded</code> state externally. Pair with a tree or nested listboxes inside the panel for keyboard support.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>combobox</code> — a text input combined with a dropdown list for filtering options</li>\n<li><code>tree-select</code> — a select dropdown showing a tree of hierarchical options</li>\n<li><code>select-with-extras</code> — a select dropdown with additional features like search or groups</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Combobox Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/combobox/</li>\n<li>Ant Design Cascader: https://ant.design/components/cascader</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"cascader\" aria-label=\"a multi-level dropdown for selecting a value from a hierarchy\">Cascader</div>";
  const svelteSource: string = "// In your Svelte component:\nimport Cascader from \"lily-design-system-svelte-headless/components/Cascader/Cascader.svelte\";\n\n<Cascader>\n  <!-- content -->\n</Cascader>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Cascader — Lily Design System</title>
  <meta name="description" content="A cascader is a multi-level dropdown for selecting a value from a hierarchy. It exposes a trigger button and a panel that holds nested option lists." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

