<script lang="ts">
  const html: string = "<h1>Do List</h1>\n\n<p>A do list presents recommended actions or best practices as a list of items. It is used in design system documentation, guidelines, and instructional content to clearly communicate what users or developers should do. The list is paired with the DontList component to create do/don't guidance patterns.</p>\n\n<p>This component renders a semantically labeled unordered list with <code>role=\"list\"</code> to ensure assistive technology announces it as a list even when CSS may remove default list semantics. The default label is \"Do\", making it immediately usable without configuration.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;ul&gt;</code> element with explicit <code>role=\"list\"</code> to preserve list semantics</li>\n<li>Uses <code>aria-label</code> to provide an accessible name (defaults to \"Do\")</li>\n<li>Children slot is rendered inside the list; each child should be an <code>&lt;li&gt;</code> element</li>\n<li>Label prop has a sensible default but can be overridden for internationalization</li>\n<li>Spreads <code>restProps</code> on the <code>&lt;ul&gt;</code> element</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (default: <code>\"Do\"</code>) -- accessible name for the list</li>\n<li><code>children</code>: slot (required) -- list items, each should be an <code>&lt;li&gt;</code> element</li>\n<li><code>...restProps</code>: any additional HTML attributes spread onto the <code>&lt;ul&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;DoList heading=\"Do\"&gt;\n  &lt;DoListItem&gt;cover blisters that are likely to burst with a soft plaster or dressing&lt;/DoListItem&gt;\n  &lt;DoListItem&gt;wash your hands before touching a burst blister&lt;/DoListItem&gt;\n  &lt;DoListItem&gt;allow the fluid in a burst blister to drain before covering it with a plaster or dressing&lt;/DoListItem&gt;\n&lt;/DoList&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;DoList heading=\"Do\"&gt;\n  &lt;DoListItem&gt;take paracetamol or ibuprofen to ease pain or discomfort&lt;/DoListItem&gt;\n  &lt;DoListItem&gt;drink plenty of fluids to avoid dehydration&lt;/DoListItem&gt;\n  &lt;DoListItem&gt;rest and let your body recover&lt;/DoListItem&gt;\n&lt;/DoList&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;DoList label=\"Recommended\"&gt;\n  &lt;DoListItem&gt;Provide alt text for images&lt;/DoListItem&gt;\n  &lt;DoListItem&gt;Use sufficient color contrast&lt;/DoListItem&gt;\n  &lt;DoListItem&gt;Test with keyboard navigation&lt;/DoListItem&gt;\n&lt;/DoList&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive container for list items.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"list\"</code> -- explicitly declares list semantics on the <code>&lt;ul&gt;</code>, ensuring assistive technology announces it as a list</li>\n<li><code>aria-label={label}</code> -- provides an accessible name for the list (defaults to \"Do\")</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to give users a clear list of encouraged actions, such as self-care advice or best practices.</li>\n<li>Use alongside DontList to present contrasting do and don't guidance on the same topic.</li>\n<li>Use when there are 2 or more recommended actions to list -- the list format reinforces the pattern.</li>\n<li>Use with lowercase text that describes each action directly, without prefixing items with \"do\".</li>\n<li>Use before any corresponding DontList -- dos come before don'ts.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use with only 1 item -- use InsetText for a single piece of guidance or WarningCallout for important advice.</li>\n<li>Do not use for step-by-step instructions -- use a numbered list or TaskList instead.</li>\n<li>Do not use inside other interactive patterns like CareCard -- keep do lists in their own section.</li>\n<li>Do not use for general-purpose lists -- use a standard <code>&lt;ul&gt;</code> or CheckList instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;ul&gt;</code> with explicit <code>role=\"list\"</code> to preserve list semantics even when CSS removes default markers, and <code>aria-label</code> for accessible naming. The consumer provides all visual styling, icons, and layout for the list and its items.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.do-list</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;ol&gt;</code> element with class <code>do-list</code></li>\n<li>Verify role=\"list\"<code> -- explicitly declares list semantics on the </code>&lt;ul&gt;`, ensuring assistive technology announces it as a list</li>\n<li>Verify aria-label={label}` -- provides an accessible name for the list (defaults to \"Do\")</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use a distinct visual treatment (such as a green check mark or positive color) to differentiate do-list items from dont-list items. Keep items concise and actionable.</li>\n<li><strong>Developers</strong>: Place DoListItem components as children. Override the <code>label</code> prop for internationalization when the default \"Do\" label is not appropriate.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>DoList composes with DoListItem using the List/ListItem pattern. DoList renders the <code>&lt;ul&gt;</code> container, and each DoListItem renders an <code>&lt;li&gt;</code> within it. Pair with DontList and DontListItem for complete do/don't guidance.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>do-list-item</code> — an encouraged-practice item in a do-list guideline list</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN ul element: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/ul</li>\n<li>WAI list role: https://www.w3.org/WAI/ARIA/apd/roles/list/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<ul class=\"do-list\"><li class=\"do-list-item\">Do use semantic HTML</li><li class=\"do-list-item\">Do add ARIA labels</li></ul>";
  const svelteSource: string = "// In your Svelte component:\nimport DoList from \"lily-design-system-svelte-headless/components/DoList/DoList.svelte\";\n\n<DoList>\n  <!-- list children -->\n</DoList>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Do List — Lily Design System</title>
  <meta name="description" content="A do list presents recommended actions or best practices as a list of items. It is used in design system documentation, guidelines, and instructional content to clearly communicate what users or de..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

