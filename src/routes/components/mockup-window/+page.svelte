<script lang="ts">
  const html: string = "<h1>MockupWindow</h1>\n\n<p>A mockup window is a decorative container that visually resembles a desktop window, used to frame content in documentation, demos, or marketing pages.</p>\n\n<p>This headless component uses a <code>&lt;div&gt;</code> element with <code>role=\"img\"</code> and <code>aria-label</code> to present the mockup as a decorative image to assistive technologies.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses <code>&lt;div&gt;</code> element with <code>role=\"img\"</code> for decorative presentation</li>\n<li><code>aria-label</code> describes the mockup content for screen readers</li>\n<li>Contains a slot for the window content area</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible label describing the mockup content</li>\n<li><code>children</code>: slot (required) -- the content displayed inside the window mockup</li>\n<li><code>...restProps</code>: Any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Basic window mockup with a screenshot:</p>\n\n<pre><code class=\"language-html\">&lt;MockupWindow label=\"Application settings panel\"&gt;\n  &lt;Image src=\"settings.png\" alt=\"Settings panel screenshot\" /&gt;\n&lt;/MockupWindow&gt;</code></pre>\n\n<p>Window mockup for a text editor preview:</p>\n\n<pre><code class=\"language-html\">&lt;MockupWindow label=\"Code editor showing HTML file\"&gt;\n  &lt;CodeBlock language=\"html\"&gt;\n    &amp;lt;h1&amp;gt;Hello, world!&amp;lt;/h1&amp;gt;\n  &lt;/CodeBlock&gt;\n&lt;/MockupWindow&gt;</code></pre>\n\n<p>Window mockup in a feature comparison:</p>\n\n<pre><code class=\"language-html\">&lt;div class=\"feature-grid\"&gt;\n  &lt;MockupWindow label=\"File manager showing grid view\"&gt;\n    &lt;Image src=\"file-manager-grid.png\" alt=\"File manager in grid view\" /&gt;\n  &lt;/MockupWindow&gt;\n  &lt;MockupWindow label=\"File manager showing list view\"&gt;\n    &lt;Image src=\"file-manager-list.png\" alt=\"File manager in list view\" /&gt;\n  &lt;/MockupWindow&gt;\n&lt;/div&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>None -- mockups are decorative, not interactive</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"img\"</code> -- presents the mockup as a single image to assistive technologies</li>\n<li><code>aria-label</code> -- describes the mockup content for screen readers</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to showcase desktop application designs, dialogs, or native software screenshots.</li>\n<li>Use in documentation to frame desktop-specific UI examples such as settings panels or editors.</li>\n<li>Use in marketing pages for native desktop applications.</li>\n<li>Use when the content represents a generic window without browser-specific chrome.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for web-specific content that needs an address bar -- use MockupBrowser instead.</li>\n<li>Do not use for terminal or CLI output -- use MockupShell instead.</li>\n<li>Do not use for mobile content -- use MockupPhonePortrait instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides a <code>&lt;div&gt;</code> with <code>role=\"img\"</code> and <code>aria-label</code> for accessible decorative presentation, with zero visual styling. The consumer is responsible for all CSS including window chrome, title bar, window control buttons, frame border, drop shadow, and content area sizing.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.mockup-window</code> class for targeting. No default styles are included -- this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>mockup-window</code></li>\n<li>Verify <code>role=\"img\"</code> is present</li>\n<li>Verify <code>aria-label</code> is applied</li>\n<li>Verify child content is rendered</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Include recognizable window elements (title bar, close/minimize/maximize buttons) for clear identification.</li>\n<li><strong>Developers</strong>: Always provide a descriptive <code>label</code> prop so screen readers can convey the mockup's purpose.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>mockup-browser</code> — a box area that looks like a web browser</li>\n<li><code>mockup-laptop</code> — a box area that looks like a laptop computer</li>\n<li><code>mockup-phone-landscape</code> — a box area that looks like a mobile phone in landscape orientation</li>\n<li><code>mockup-phone-portrait</code> — a box area that looks like a mobile phone</li>\n<li><code>mockup-shell</code> — a box area that looks like a terminal shell</li>\n<li><code>mockup-tablet-landscape</code> — a box area that looks like a tablet computer in landscape mode</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/HTML/Element/div\">MDN div element</a></li>\n</ul>";
</script>

<svelte:head>
  <title>MockupWindow — Lily Design System</title>
  <meta name="description" content="A mockup window is a decorative container that visually resembles a desktop window, used to frame content in documentation, demos, or marketing pages." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
