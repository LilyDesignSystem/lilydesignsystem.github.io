<script lang="ts">
  const html: string = "<h1>File Upload</h1>\n\n<p>A file upload component provides a button-triggered file picker for uploading files. It combines a hidden file input with a visible button and a live status region that announces how many files have been selected. This pattern is useful for document uploads, image uploads, or any form that accepts file attachments.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> container with a <code>&lt;button&gt;</code>, hidden <code>&lt;input type=\"file\"&gt;</code>, and a status <code>&lt;span&gt;</code></li>\n<li>The button triggers the hidden file input via <code>click()</code> on the input ref</li>\n<li>Uses reactive state for the input element reference (<code>inputRef</code>) and the selected file count (<code>fileCount</code>)</li>\n<li>The status <code>&lt;span&gt;</code> uses <code>aria-live=\"polite\"</code> to announce file selection changes to screen readers</li>\n<li>Exposes a <code>data-file-count</code> attribute on the status span for consumer CSS or testing</li>\n<li>The <code>onchange</code> callback receives the native <code>FileList</code> object</li>\n<li>Prevents default submit behavior by using <code>type=\"button\"</code> on the trigger</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name for the upload button, also used as visible button text</li>\n<li><code>accept</code>: string (default: <code>undefined</code>) -- accepted file types (e.g., <code>\".pdf\"</code>, <code>\"image/*\"</code>)</li>\n<li><code>multiple</code>: boolean (default: <code>false</code>) -- whether to allow selecting multiple files</li>\n<li><code>disabled</code>: boolean (default: <code>false</code>) -- whether the button is disabled</li>\n<li><code>onchange</code>: function (default: <code>undefined</code>) -- callback receiving <code>FileList | null</code> when files are selected</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the outer <code>&lt;div&gt;</code></li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;Field label=\"Upload a scan of your referral letter\"&gt;\n  &lt;Hint&gt;The file must be a JPG, PNG, or PDF and be smaller than 5MB&lt;/Hint&gt;\n  &lt;FileUpload label=\"Upload files\" accept=\".jpg,.png,.pdf\" onchange={handleFiles} /&gt;\n&lt;/Field&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;Field label=\"Upload your test results\" error=\"true\"&gt;\n  &lt;ErrorMessage&gt;The selected file must be smaller than 5MB&lt;/ErrorMessage&gt;\n  &lt;FileUpload label=\"Upload files\" accept=\".jpg,.png,.pdf\" onchange={handleFiles} /&gt;\n&lt;/Field&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None beyond native button behavior -- Tab to focus the button, Enter or Space to activate the file picker dialog.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label=\"...\"</code> -- provides an accessible name for the upload button</li>\n<li><code>aria-live=\"polite\"</code> -- on the status span, announces file selection count without interrupting the user</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use when file upload is critical to delivering your service, such as uploading medical documents, photos, or identification.</li>\n<li>Use with clear guidance about accepted file types and maximum file size.</li>\n<li>Use with descriptive error messages that tell users exactly what went wrong and how to fix it.</li>\n<li>Use drag-and-drop alongside the file input button for an enhanced experience where supported.</li>\n<li>Use when a more polished upload experience is needed compared to a raw FileInput.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when the information could be collected through form fields instead -- text input is simpler and more accessible.</li>\n<li>Do not use for image selection with preview -- use ImageFileInput which provides a preview before upload.</li>\n<li>Do not use for browsing and managing existing files -- use FileManager or FileDialog for that purpose.</li>\n<li>Do not use without specifying accepted file types -- users need to know what formats are allowed.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;button&gt;</code> trigger, a hidden <code>&lt;input type=\"file\"&gt;</code>, and a status <code>&lt;span&gt;</code> with <code>aria-live=\"polite\"</code> that announces file selection changes. It manages the input ref and file count internally. The consumer provides all visual styling for the button, status text, and overall layout.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.file-upload</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>file-upload</code></li>\n<li>Verify aria-label=\"...\"` -- provides an accessible name for the upload button</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Style the upload button prominently and display the file count or file names near the button after selection. Include accepted file type hints in nearby text.</li>\n<li><strong>Developers</strong>: Use the <code>onchange</code> callback to receive the <code>FileList</code> for processing. The <code>data-file-count</code> attribute on the status span can be used for conditional CSS styling or testing.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>file-input</code> — an input for selecting files from the file system &lt;input type=\"file\"&gt;</li>\n<li><code>file-manager</code> — a file browser for navigating and managing files</li>\n<li><code>download-button</code> — a download link styled as a button, with optional file size and format metadata</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN <code>&lt;input type=\"file\"&gt;</code>: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/file</li>\n</ul>";
</script>

<svelte:head>
  <title>File Upload — Lily Design System</title>
  <meta name="description" content="A file upload component provides a button-triggered file picker for uploading files. It combines a hidden file input with a visible button and a live status region that announces how many files hav..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
