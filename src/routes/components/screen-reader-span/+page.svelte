<script lang="ts">
  const html: string = "<h1>ScreenReaderSpan</h1>\n\n<p>A screen reader span is a visually hidden span of text intended for screen readers to provide more descriptive labels. It is used to supplement visual elements with additional context that is only announced to assistive technologies.</p>\n\n<p>This headless component uses a <code>&lt;span&gt;</code> element that is visually hidden via CSS but remains accessible to screen readers.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses <code>&lt;span&gt;</code> element with visually-hidden CSS pattern</li>\n<li>Content is visible only to screen readers, not sighted users</li>\n<li>Does not use <code>display: none</code> or <code>visibility: hidden</code> (which would hide from screen readers too)</li>\n<li>Uses clip/overflow pattern to remain in the accessibility tree</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>children</code>: slot (required) -- the text content for screen readers</li>\n<li><code>...restProps</code>: Any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Icon-only button with screen reader label:</p>\n\n<pre><code class=\"language-html\">&lt;button&gt;\n  &lt;Icon name=\"close\" /&gt;\n  &lt;ScreenReaderSpan&gt;Close dialog&lt;/ScreenReaderSpan&gt;\n&lt;/button&gt;</code></pre>\n\n<p>Icon-only link with screen reader label:</p>\n\n<pre><code class=\"language-html\">&lt;a href=\"/home\"&gt;\n  &lt;Icon name=\"home\" /&gt;\n  &lt;ScreenReaderSpan&gt;Home page&lt;/ScreenReaderSpan&gt;\n&lt;/a&gt;</code></pre>\n\n<p>Expanding abbreviated text for screen readers:</p>\n\n<pre><code class=\"language-html\">&lt;p&gt;Published &lt;time datetime=\"2025-01-15\"&gt;3 min&lt;ScreenReaderSpan&gt;utes&lt;/ScreenReaderSpan&gt; ago&lt;/time&gt;&lt;/p&gt;</code></pre>\n\n<p>Adding context to a \"Read more\" link:</p>\n\n<pre><code class=\"language-html\">&lt;article&gt;\n  &lt;h3&gt;Understanding ARIA landmarks&lt;/h3&gt;\n  &lt;p&gt;ARIA landmarks help assistive technology users navigate...&lt;/p&gt;\n  &lt;a href=\"/articles/aria-landmarks\"&gt;\n    Read more&lt;ScreenReaderSpan&gt; about understanding ARIA landmarks&lt;/ScreenReaderSpan&gt;\n  &lt;/a&gt;\n&lt;/article&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>None -- screen reader spans are informational, not interactive</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>No ARIA attributes needed -- the text content itself provides the accessible information</li>\n<li>The visually-hidden pattern ensures the text stays in the accessibility tree</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to provide descriptive text for icon-only buttons or links.</li>\n<li>Use to add context that is visually implied but not explicit for screen readers.</li>\n<li>Use for table cells or list items where visual context makes the meaning obvious but screen readers need additional text.</li>\n<li>Use to expand abbreviated text such as \"3 min\" to \"3 minutes\" for assistive technologies.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when a visible label is possible -- prefer visible text over hidden text.</li>\n<li>Do not use when <code>aria-label</code> or <code>aria-labelledby</code> on the interactive element would be more appropriate.</li>\n<li>Do not use to hide content from sighted users that they need to see.</li>\n<li>Do not use for decorative content that should be hidden from all users -- use <code>aria-hidden=\"true\"</code> instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides a <code>&lt;span&gt;</code> element with zero visual presence but full screen reader accessibility. The consumer must apply the visually-hidden CSS pattern (clip, overflow, position absolute, dimensions) to hide the element visually.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides the visually-hidden CSS. The component renders with a <code>.screen-reader-span</code> class for targeting. The recommended CSS pattern is: <code>position: absolute; width: 1px; height: 1px; padding: 0; margin: -1px; overflow: hidden; clip: rect(0, 0, 0, 0); white-space: nowrap; border: 0;</code></p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;span&gt;</code> element with class <code>screen-reader-span</code></li>\n<li>Verify child text content is rendered</li>\n<li>Verify the element is in the accessibility tree</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Identify icon-only controls that need supplementary text for screen readers. Document the intended screen reader text alongside visual designs.</li>\n<li><strong>Developers</strong>: Prefer <code>aria-label</code> on interactive elements where possible. Use ScreenReaderSpan when you need inline text that participates in the DOM flow for screen readers.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>label</code> — a label associated with a form input</li>\n<li><code>hint</code> — hint text providing guidance for a form field</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WebAIM: CSS in Action - Invisible Content Just for Screen Readers: https://webaim.org/techniques/css/invisiblecontent/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<span class=\"screen-reader-span\">Additional context for screen readers</span>";
  const svelteSource: string = "// In your Svelte component:\nimport ScreenReaderSpan from \"lily-design-system-svelte-headless/components/ScreenReaderSpan/ScreenReaderSpan.svelte\";\n\n<ScreenReaderSpan>Content</ScreenReaderSpan>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>ScreenReaderSpan — Lily Design System</title>
  <meta name="description" content="A screen reader span is a visually hidden span of text intended for screen readers to provide more descriptive labels. It is used to supplement visual elements with additional context that is only ..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

