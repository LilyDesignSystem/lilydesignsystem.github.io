<script lang="ts">
  const html: string = "<h1>Accordion Nav</h1>\n\n<p>An accordion nav is a navigation container that organizes and displays content in a space-efficient, collapsible format. It consists of vertically stacked sections, each with a clickable header that expands or collapses its associated content. This design helps reduce visual clutter, allowing users to focus on one section at a time while keeping other content hidden until needed. Commonly used in FAQs, filters, or mobile menus, accordions enhance usability by streamlining content access and improving navigation on both desktop and mobile interfaces.</p>\n\n<p>The AccordionNav component serves as the outer wrapper. Place an AccordionList inside it, containing AccordionListItem children for the individual collapsible sections.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;nav&gt;</code> element with class <code>accordion-nav</code></li>\n<li>Spreads <code>restProps</code> onto the root element for consumer customization</li>\n</ul>\n\n<h2>Help</h2>\n\n<p>Use AccordionNav as the outermost container for an accordion component. Common scenarios include FAQ sections, settings panels, filter groups, and mobile navigation menus. Pair with AccordionList, AccordionListItem, AccordionSummary, and AccordionDetails.</p>\n\n<h2>Syntax</h2>\n\n<pre><code class=\"language-html\">&lt;AccordionNav label=\"...\"&gt;\n  &lt;!-- AccordionList with AccordionListItem children --&gt;\n&lt;/AccordionNav&gt;</code></pre>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;AccordionNav label=\"Service categories\"&gt;\n  &lt;AccordionList&gt;\n    &lt;AccordionListItem heading=\"Mental health\" expanded&gt;\n      &lt;AccordionLink href=\"/services/anxiety\"&gt;Anxiety&lt;/AccordionLink&gt;\n      &lt;AccordionLink href=\"/services/depression\"&gt;Depression&lt;/AccordionLink&gt;\n      &lt;AccordionLink href=\"/services/stress\"&gt;Stress management&lt;/AccordionLink&gt;\n    &lt;/AccordionListItem&gt;\n    &lt;AccordionListItem heading=\"Physical health\"&gt;\n      &lt;AccordionLink href=\"/services/back-pain\"&gt;Back pain&lt;/AccordionLink&gt;\n      &lt;AccordionLink href=\"/services/headaches\"&gt;Headaches&lt;/AccordionLink&gt;\n    &lt;/AccordionListItem&gt;\n  &lt;/AccordionList&gt;\n&lt;/AccordionNav&gt;</code></pre>\n\n<h2>Props</h2>\n\n<table>\n<thead>\n<tr><th>Prop</th><th>Type</th><th>Default</th><th>Description</th></tr>\n</thead>\n<tbody>\n<tr><td><code>label</code></td><td><code>string</code></td><td>(required)</td><td>Accessible name for the accordion region via <code>aria-label</code></td></tr>\n<tr><td><code>children</code></td><td><code>slot</code></td><td>(required)</td><td>Accordion content to render inside the container</td></tr>\n<tr><td><code>...restProps</code></td><td>HTML attributes</td><td></td><td>Additional attributes spread onto the outer <code>&lt;div&gt;</code></td></tr>\n</tbody>\n</table>\n\n<h2>Examples</h2>\n\n<p>FAQ section:</p>\n\n<pre><code class=\"language-html\">&lt;AccordionNav label=\"Frequently asked questions\"&gt;\n  &lt;AccordionList&gt;\n    &lt;AccordionListItem summary=\"What is your return policy?\"&gt;\n      You can return items within 30 days.\n    &lt;/AccordionListItem&gt;\n    &lt;AccordionListItem summary=\"How do I contact support?\"&gt;\n      Email us at support@example.com.\n    &lt;/AccordionListItem&gt;\n  &lt;/AccordionList&gt;\n&lt;/AccordionNav&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive container. Keyboard interactions are handled by AccordionListItem children.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"region\"</code> -- identifies the accordion as a landmark region</li>\n<li><code>aria-label</code> -- provides an accessible name for the region, allowing screen readers to announce it</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use as a navigation landmark containing collapsible accordion sections, such as service category menus or grouped resource directories.</li>\n<li>Use when users need to browse and expand categories of navigation links without leaving the current page.</li>\n<li>Use with a descriptive <code>aria-label</code> to communicate the navigation purpose to screen reader users.</li>\n<li>Use when the number of navigation links is large enough that showing them all at once would overwhelm the page.</li>\n<li>Use in mobile navigation patterns where vertical space is limited and sections should be collapsed by default.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for page content that expands and collapses -- use Details or Expander instead.</li>\n<li>Do not use for flat, single-level navigation -- use ContentsNav or BreadcrumbNav instead.</li>\n<li>Do not use for single-level menus triggered by a button -- use NavigationMenu instead.</li>\n<li>Do not use when users need to compare content across sections simultaneously -- use TabBar instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides a <code>&lt;nav&gt;</code> landmark with <code>role=\"region\"</code> and <code>aria-label</code> for accessibility, with zero visual styling. The consumer is responsible for all CSS including container borders, background colors, spacing, and overall layout. Use the <code>data-*</code> attributes or element selectors as styling hooks.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.accordion-nav</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;nav&gt;</code> element with class <code>accordion-nav</code></li>\n<li>Verify role=\"region\"` -- identifies the accordion as a landmark region</li>\n<li>Verify aria-label` -- provides an accessible name for the region, allowing screen readers to announce it</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Ensure the accordion has clear visual boundaries so users can distinguish it from surrounding content. Provide a visible label or heading above the accordion group.</li>\n<li><strong>Developers</strong>: Always provide a meaningful <code>label</code> prop that describes the accordion's purpose for screen reader users navigating by landmarks.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>AccordionNav follows the Nav / List / ListItem composition pattern:</p>\n\n<ul>\n<li><strong>AccordionNav</strong> -- outer <code>&lt;nav&gt;</code> container providing the landmark region and accessible label.</li>\n<li><strong>AccordionList</strong> -- <code>&lt;ol&gt;</code> list grouping the collapsible items in order.</li>\n<li><strong>AccordionListItem</strong> -- individual <code>&lt;details&gt;</code> / <code>&lt;summary&gt;</code> sections for each expandable item.</li>\n</ul>\n\n<pre><code class=\"language-html\">&lt;AccordionNav label=\"FAQ\"&gt;\n  &lt;AccordionList&gt;\n    &lt;AccordionListItem summary=\"Question 1\"&gt;Answer 1&lt;/AccordionListItem&gt;\n    &lt;AccordionListItem summary=\"Question 2\"&gt;Answer 2&lt;/AccordionListItem&gt;\n  &lt;/AccordionList&gt;\n&lt;/AccordionNav&gt;</code></pre>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>accordion-list</code> — an accordion ordered list of list item components</li>\n<li><code>accordion-list-item</code> — an accordion list item component</li>\n<li><code>accordion-link</code> — an accordion link</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Accordion Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/accordion/</li>\n</ul>";
</script>

<svelte:head>
  <title>Accordion Nav — Lily Design System</title>
  <meta name="description" content="An accordion nav is a navigation container that organizes and displays content in a space-efficient, collapsible format. It consists of vertically stacked sections, each with a clickable header tha..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
