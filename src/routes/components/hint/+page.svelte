<script lang="ts">
  const html: string = "<h1>Hint</h1>\n\n<p>A hint provides subtle, contextual guidance text associated with a form control. It is typically displayed near an input field to clarify expected formats, explain functionality, or offer examples. The component renders a <code>&lt;p&gt;</code> element with an optional <code>id</code> for linking to a form control via <code>aria-describedby</code>.</p>\n\n<p>This component is designed to work alongside form inputs where the consumer links the hint to the input using <code>aria-describedby</code> on the input element pointing to the hint's <code>id</code>. This ensures screen readers announce the hint text when the user focuses the associated input.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;p&gt;</code> element containing the children slot</li>\n<li>Accepts an optional <code>id</code> prop for <code>aria-describedby</code> linking with form controls</li>\n<li>Spreads <code>restProps</code> onto the <code>&lt;p&gt;</code> for consumer customization</li>\n<li>The consumer is responsible for adding <code>aria-describedby</code> to the associated input</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>id</code>: string (default: undefined) -- ID for linking via <code>aria-describedby</code> on the associated form control</li>\n<li><code>children</code>: slot (required) -- hint text content</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;Field label=\"What is your NHS number?\"&gt;\n  &lt;Hint id=\"nhs-number-hint\"&gt;This is a 10-digit number, like 485 777 3456. You can find it on any letter the NHS has sent you.&lt;/Hint&gt;\n  &lt;TextInput label=\"NHS number\" inputmode=\"numeric\" aria-describedby=\"nhs-number-hint\" /&gt;\n&lt;/Field&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;Field label=\"Email address\"&gt;\n  &lt;Hint id=\"email-hint\"&gt;We will only use this to send you a confirmation&lt;/Hint&gt;\n  &lt;EmailInput label=\"Email address\" aria-describedby=\"email-hint\" /&gt;\n&lt;/Field&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;Field label=\"Date of birth\"&gt;\n  &lt;Hint id=\"dob-hint\"&gt;For example, 15 3 1984&lt;/Hint&gt;\n  &lt;DateInput label=\"Date of birth\" aria-describedby=\"dob-hint\" /&gt;\n&lt;/Field&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive text element. It is announced by screen readers when the associated form control receives focus.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>id={id}</code> -- provides a target for <code>aria-describedby</code> on the associated form control</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to help the majority of users understand how to answer a form question</li>\n<li>Use to show the expected format of an answer, such as \"For example, 15 3 1984\" or \"Like name@example.com\"</li>\n<li>Use to explain where users can find the information being asked for</li>\n<li>Use as a single short phrase or sentence -- keep it concise because screen readers read the entire hint aloud</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for long, detailed explanations -- screen readers read the entire hint aloud, which frustrates users; use InformationCallout instead</li>\n<li>Do not include links inside hint text -- screen readers do not announce that hint text contains a link</li>\n<li>Do not include interactive elements like Details inside hint text -- users in screen reader form mode cannot interact with them</li>\n<li>Do not use as a substitute for a clear Label -- the label should make the question understandable on its own</li>\n<li>Do not use for error messages -- use ErrorMessage instead</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;p&gt;</code> element with an optional <code>id</code> for linking to a form control via <code>aria-describedby</code>. The consumer provides all visual styling including font size, color, spacing, and position relative to the input.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.hint</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;span&gt;</code> element with class <code>hint</code></li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Place hint text directly below the label and above the input. Use a lighter color or smaller font to distinguish it from labels and error messages.</li>\n<li><strong>Developers</strong>: Always set the <code>id</code> prop and add <code>aria-describedby</code> to the associated input so screen readers announce the hint when the input is focused.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>error-message</code> — an error message associated with a form field</li>\n<li><code>field</code> — a form field wrapper with label, input, and error message</li>\n<li><code>label</code> — a label associated with a form input</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI Forms Tutorial - Instructions: https://www.w3.org/WAI/tutorials/forms/instructions/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<span class=\"hint\">Enter your full name as shown on your ID</span>";
  const svelteSource: string = "// In your Svelte component:\nimport Hint from \"lily-design-system-svelte-headless/components/Hint/Hint.svelte\";\n\n<Hint>\n  <!-- content -->\n</Hint>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Hint — Lily Design System</title>
  <meta name="description" content="A hint provides subtle, contextual guidance text associated with a form control. It is typically displayed near an input field to clarify expected formats, explain functionality, or offer examples...." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

