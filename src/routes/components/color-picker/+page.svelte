<script lang="ts">
  const html: string = "<h1>Color Picker</h1>\n\n<p>A 2D area selector for choosing colors by saturation (X axis) and brightness (Y axis). Users navigate the board with keyboard arrow keys, with Shift held for larger steps. The component exposes <code>data-x</code> and <code>data-y</code> attributes for CSS-based cursor positioning, enabling consumers to render a visual indicator at the selected coordinates.</p>\n\n<p>This component is useful for advanced color pickers where the user needs fine-grained 2D selection of color properties.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> with <code>role=\"slider\"</code> for ARIA slider semantics</li>\n<li>Supports two-way binding for both <code>x</code> and <code>y</code> coordinates (range 0-100)</li>\n<li>Implements <code>onkeydown</code> handler for arrow key navigation with step size of 1 (or 10 with Shift)</li>\n<li>Home key resets <code>x</code> to 0; End key sets <code>x</code> to 100</li>\n<li>Exposes <code>data-x</code> and <code>data-y</code> attributes for CSS positioning of a visual cursor</li>\n<li><code>tabindex</code> is set to 0 when enabled, -1 when disabled</li>\n<li><code>aria-disabled</code> is set only when disabled (omitted otherwise via <code>|| undefined</code>)</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>x</code>: number (default: <code>0</code>) -- horizontal position (0-100, saturation), two-way bindable via <code>x</code></li>\n<li><code>y</code>: number (default: <code>0</code>) -- vertical position (0-100, brightness), two-way bindable via <code>y</code></li>\n<li><code>label</code>: string (required) -- accessible name via <code>aria-label</code></li>\n<li><code>disabled</code>: boolean (default: <code>false</code>) -- whether the control is disabled</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;div&gt;</code></li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;ColorPicker label=\"Color saturation and brightness\" x={x} y={y} /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>ArrowRight: increase X by 1 (Shift: by 10)</li>\n<li>ArrowLeft: decrease X by 1 (Shift: by 10)</li>\n<li>ArrowUp: decrease Y by 1 (Shift: by 10)</li>\n<li>ArrowDown: increase Y by 1 (Shift: by 10)</li>\n<li>Home: set X to 0</li>\n<li>End: set X to 100</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"slider\"</code> -- identifies the element as a slider control</li>\n<li><code>aria-label=\"...\"</code> -- provides an accessible name for the slider</li>\n<li><code>aria-valuenow</code> -- current X value (0-100)</li>\n<li><code>aria-valuemin=\"0\"</code> -- minimum slider value</li>\n<li><code>aria-valuemax=\"100\"</code> -- maximum slider value</li>\n<li><code>aria-disabled=\"true\"</code> -- set when the control is disabled</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for a two-dimensional board for picking colours by hue and saturation.</li>\n<li>Use in design tools, theme editors, or branding configuration where fine-grained colour selection is needed.</li>\n<li>Use alongside hue and opacity sliders for a complete colour selection interface.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when the native browser picker is sufficient -- use ColorInput for simple hex colour selection.</li>\n<li>Do not use for a small set of preset colours -- use a group of ColorPickerButton components instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless ColorPicker component provides a <code>&lt;div&gt;</code> with <code>role=\"slider\"</code>, <code>aria-valuenow</code>/<code>aria-valuemin</code>/<code>aria-valuemax</code> for ARIA compliance, keyboard navigation with arrow keys (and Shift for larger steps), and <code>data-x</code>/<code>data-y</code> attributes for cursor positioning. The consumer provides all visual styling including the color gradient background, cursor indicator, border, and dimensions.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.color-picker</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>color-picker</code></li>\n<li>Verify role=\"slider\"` -- identifies the element as a slider control</li>\n<li>Verify aria-label=\"...\"` -- provides an accessible name for the slider</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Display a visual cursor indicator at the selected position on the 2D area. Use a gradient background showing saturation and brightness to help users understand the mapping.</li>\n<li><strong>Developers</strong>: Use the <code>data-x</code> and <code>data-y</code> attributes in CSS to position a cursor element with <code>calc()</code> or CSS custom properties. Combine with a hue slider for a complete color picker.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>ColorPicker is the parent component in the ColorPicker/ColorPickerButton composition pattern. Use ColorPicker for free-form 2D color selection, and ColorPickerButton for individual predefined color swatches. They can be combined in a single interface where ColorPickerButton offers quick presets and ColorPicker provides fine-grained adjustment.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>color-picker-button</code> — a button showing a color swatch in a color picker</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Slider Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/slider/</li>\n</ul>";
</script>

<svelte:head>
  <title>Color Picker — Lily Design System</title>
  <meta name="description" content="A 2D area selector for choosing colors by saturation (X axis) and brightness (Y axis). Users navigate the board with keyboard arrow keys, with Shift held for larger steps. The component exposes dat..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
