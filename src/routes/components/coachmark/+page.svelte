<script lang="ts">
  const html: string = "<h1>Coachmark</h1>\n\n<p>A coachmark is an anchored popover that spotlights and explains a single feature, typically used in onboarding, feature discovery, or guided tours.</p>\n\n<p>Use it sparingly and at most one at a time, paired with a visual anchor on the feature being highlighted. A coachmark teaches; it should not block the user from continuing their task.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses a <code>&lt;div&gt;</code> with <code>role=\"dialog\"</code> and <code>aria-modal=\"false\"</code> (non-blocking)</li>\n<li><code>title</code> is rendered in an <code>&lt;h2 class=\"coachmark-title\"&gt;</code> and linked via <code>aria-labelledby</code></li>\n<li><code>description</code>, when provided, is rendered in a <code>&lt;p class=\"coachmark-description\"&gt;</code> and linked via <code>aria-describedby</code></li>\n<li>The dismiss button always renders and uses <code>dismissLabel</code> as <code>aria-label</code></li>\n<li>The whole coachmark is hidden via the <code>hidden</code> attribute when <code>open</code> is <code>false</code></li>\n<li>IDs for the title and description are generated stably via <code>useId</code>, <code>crypto.randomUUID</code>, or a Math.random helper</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>open</code>: boolean (default <code>false</code>) -- whether the coachmark is visible</li>\n<li><code>title</code>: string (<strong>required</strong>) -- heading text</li>\n<li><code>description</code>: string (optional) -- body text</li>\n<li><code>dismissLabel</code>: string (<strong>required</strong>) -- accessible label for the dismiss button</li>\n<li><code>onDismiss</code> / <code>ondismiss</code>: callback (optional) -- dismiss click handler</li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;Coachmark\n  open={open}\n  title=\"Try the new filter\"\n  description=\"Filter by status to find items faster.\"\n  dismissLabel=\"Dismiss\"\n  onDismiss={handleDismiss}\n/&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab / Shift+Tab: Move focus into and out of the coachmark</li>\n<li>Enter / Space: Activate the dismiss button when focused</li>\n<li>Escape: (Consumer-provided) call <code>onDismiss</code> to close the coachmark</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"dialog\"</code> on the container</li>\n<li><code>aria-modal=\"false\"</code> (non-blocking; does not trap focus or hide background)</li>\n<li><code>aria-labelledby</code> references the title id</li>\n<li><code>aria-describedby</code> references the description id (only when description is provided)</li>\n<li><code>hidden</code> attribute reflects <code>!open</code></li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for onboarding a single new feature</li>\n<li>Use for guided tours that highlight one element at a time</li>\n<li>Use when the dismissal is non-blocking and the user can continue working</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for blocking confirmations — use <code>Dialog</code> or <code>AlertDialog</code></li>\n<li>Do not stack multiple coachmarks at once — sequence them</li>\n<li>Do not use for help that should be persistently available — use <code>ContextualHelp</code> or <code>Tooltip</code></li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>Renders a <code>&lt;div role=\"dialog\" aria-modal=\"false\"&gt;</code> with title/description ids and an always-rendered dismiss button. Anchor positioning, fade/scale animations, and visual treatment are entirely the consumer's responsibility.</p>\n\n<h2>Styles</h2>\n\n<p>Consumer CSS targets the <code>coachmark</code> class. Sub-elements expose <code>coachmark-title</code>, <code>coachmark-description</code>, and <code>coachmark-dismiss</code> classes for selective styling. Provide a clear focus indicator on the dismiss button.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> with <code>role=\"dialog\"</code> and class <code>coachmark</code></li>\n<li>Verify <code>aria-modal=\"false\"</code></li>\n<li>Verify the title is rendered inside <code>&lt;h2 class=\"coachmark-title\"&gt;</code> and linked via <code>aria-labelledby</code></li>\n<li>Verify the description, when provided, is rendered and linked via <code>aria-describedby</code></li>\n<li>Verify the description and <code>aria-describedby</code> are absent when no description is provided</li>\n<li>Verify <code>hidden</code> is set when <code>open</code> is <code>false</code></li>\n<li>Verify the dismiss button has <code>aria-label={dismissLabel}</code> and fires <code>onDismiss</code> on click</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Anchor the coachmark visually to the feature; do not obscure other actions.</li>\n<li><strong>Developers</strong>: Persist the dismissed state in user preferences so the coachmark does not reappear.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>popover</code> — a floating content box anchored to a trigger element</li>\n<li><code>tooltip</code> — a small popup showing descriptive text on hover or focus</li>\n<li><code>tour</code> — a tour guide, such as for sightseeing, or pathways, or demonstrations, etc.</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Dialog: https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/</li>\n<li>Adobe Spectrum Coachmark: https://spectrum.adobe.com/page/coach-mark/</li>\n<li>WCAG 1.4.13 Content on Hover or Focus: https://www.w3.org/WAI/WCAG22/Understanding/content-on-hover-or-focus</li>\n</ul>";
</script>

<svelte:head>
  <title>Coachmark — Lily Design System</title>
  <meta name="description" content="A coachmark is an anchored popover that spotlights and explains a single feature, typically used in onboarding, feature discovery, or guided tours." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
