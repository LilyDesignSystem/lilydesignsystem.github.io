<script lang="ts">
  const html: string = "<h1>TileMap</h1>\n\n<p>A tile cartogram map with configurable layers for geographic data visualization, where each geographic unit is represented as an equal-sized tile.</p>\n\n<p>This headless component uses a <code>&lt;div&gt;</code> element with <code>role=\"img\"</code> and <code>aria-label</code> to present a grid of tiles representing geographic data.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses <code>&lt;div&gt;</code> element with <code>role=\"img\"</code> for the map container</li>\n<li><code>aria-roledescription=\"tile map\"</code> provides descriptive context for assistive technology</li>\n<li>Displays geographic data as a grid of equal-sized tiles (cartogram)</li>\n<li>Each tile represents a geographic unit (state, country, region)</li>\n<li>Tiles can be colored, labeled, and made interactive</li>\n<li>Consumer provides tile positioning and styling</li>\n<li>Supports multiple data layers</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible label describing the map</li>\n<li><code>children</code>: slot (required) -- map layer content (tile-map-layer components or custom)</li>\n<li><code>data</code>: array (optional) -- geographic tile data</li>\n<li><code>...restProps</code>: Any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;TileMap label=\"US election results by state\" data={stateData}&gt;\n  &lt;div class=\"tile\" style=\"grid-column: 11; grid-row: 1\"&gt;ME&lt;/div&gt;\n  &lt;div class=\"tile\" style=\"grid-column: 1; grid-row: 2\"&gt;AK&lt;/div&gt;\n  &lt;!-- ... more tiles --&gt;\n&lt;/TileMap&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;TileMap label=\"European countries by population density\"&gt;\n  &lt;div class=\"tile\" style=\"grid-column: 3; grid-row: 1\" aria-label=\"United Kingdom: 281 per km2\"&gt;UK&lt;/div&gt;\n  &lt;div class=\"tile\" style=\"grid-column: 4; grid-row: 2\" aria-label=\"France: 119 per km2\"&gt;FR&lt;/div&gt;\n  &lt;div class=\"tile\" style=\"grid-column: 5; grid-row: 2\" aria-label=\"Germany: 240 per km2\"&gt;DE&lt;/div&gt;\n&lt;/TileMap&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;TileMap label=\"US COVID-19 vaccination rates by state\"&gt;\n  &lt;div class=\"tile tile--high\" style=\"grid-column: 10; grid-row: 1\" aria-label=\"Vermont: 82%\"&gt;VT&lt;/div&gt;\n  &lt;div class=\"tile tile--low\" style=\"grid-column: 7; grid-row: 7\" aria-label=\"Alabama: 51%\"&gt;AL&lt;/div&gt;\n&lt;/TileMap&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Arrow keys navigate between tiles</li>\n<li>Enter/Space activates focused tile</li>\n<li>Escape deselects the currently selected tile</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"img\"</code> with <code>aria-label</code> for the overall map</li>\n<li><code>aria-roledescription=\"tile map\"</code> for the map container</li>\n<li>Individual tiles can have <code>role=\"listitem\"</code> with <code>aria-label</code> for data values</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for geographic data where equal representation matters more than geographic accuracy</li>\n<li>Use to compare values across geographic regions at a glance</li>\n<li>Use when geographic shape or area would distort data perception (e.g., election results, population statistics)</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when geographic shape or area matters -- use a standard map</li>\n<li>Do not use for non-geographic grid data -- use DataTable</li>\n<li>Do not use when precise geographic boundaries are important</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides a <code>&lt;div&gt;</code> element with <code>role=\"img\"</code>, <code>aria-label</code>, and <code>aria-roledescription</code> for accessible geographic visualization, with zero visual styling. The consumer is responsible for all CSS including tile size, grid positioning, colors, labels, hover effects, and responsive layout.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.tile-map</code> class for targeting. No default styles are included -- this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>tile-map</code></li>\n<li>Verify <code>role=\"img\"</code> is applied</li>\n<li>Verify <code>aria-label</code> is applied</li>\n<li>Verify <code>aria-roledescription=\"tile map\"</code> is applied</li>\n<li>Verify arrow key navigation between tiles</li>\n<li>Verify Enter/Space activates focused tile</li>\n<li>Verify Escape deselects</li>\n<li>Verify child content is rendered correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Choose tile colors with sufficient contrast for WCAG 2.2 AAA compliance. Include a legend explaining color mappings. Consider how the cartogram layout communicates equal representation versus geographic accuracy.</li>\n<li><strong>Developers</strong>: Always provide a descriptive <code>label</code> prop for screen reader users. Add <code>aria-label</code> to individual tiles with data values. Ensure keyboard navigation follows a logical reading order through the grid.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>tile</code> — a grouping container that presents related content in a structured format, often as a single clickable area</li>\n<li><code>image</code> — an image element with alt text</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li><a href=\"https://github.com/reuters-graphics/graphics-components\">Reuters Graphics TileMap component</a></li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"tile-map\" aria-label=\"a tile cartogram map with configurable layers for geographic data visualization\">TileMap</div>";
  const svelteSource: string = "// In your Svelte component:\nimport TileMap from \"lily-design-system-svelte-headless/components/TileMap/TileMap.svelte\";\n\n<TileMap>\n  <!-- content -->\n</TileMap>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>TileMap — Lily Design System</title>
  <meta name="description" content="A tile cartogram map with configurable layers for geographic data visualization, where each geographic unit is represented as an equal-sized tile." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

