<script lang="ts">
  const html: string = "<h1>Email Input</h1>\n\n<p>An email input wraps a native <code>&lt;input type=\"email\"&gt;</code> with accessible labeling. A simpler variant of EmailAddressInput for basic email collection, it omits placeholder and autocomplete support in favor of a minimal API. Use EmailInput when you need a straightforward email field with required and disabled state support.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a native <code>&lt;input type=\"email\"&gt;</code> element for browser-native email validation</li>\n<li>Supports two-way binding of the email string</li>\n<li>Provides <code>aria-label</code> for screen reader accessibility</li>\n<li>Supports <code>required</code> and <code>disabled</code> for standard form behavior</li>\n<li>Spreads <code>restProps</code> for consumer customization</li>\n<li>Unlike EmailAddressInput, does not set <code>autocomplete</code> or support <code>placeholder</code></li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name via <code>aria-label</code></li>\n<li><code>value</code>: string (default: <code>\"\"</code>) -- current email value, two-way bindable via <code>value</code></li>\n<li><code>required</code>: boolean (default: <code>false</code>) -- whether the field is required</li>\n<li><code>disabled</code>: boolean (default: <code>false</code>) -- whether the field is disabled</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;input&gt;</code></li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Contact form email field:</p>\n\n<pre><code class=\"language-html\">&lt;EmailInput label=\"Email address\" value={email} required /&gt;</code></pre>\n\n<p>Registration form with disabled state during submission:</p>\n\n<pre><code class=\"language-html\">&lt;EmailInput label=\"Work email\" value={workEmail} required disabled={isSubmitting} /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- keyboard behavior is provided by the browser-native email input.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label=\"...\"</code> -- provides an accessible name for the email input since it has no visible <code>&lt;label&gt;</code> element</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for entering an email address with the correct mobile keyboard and browser validation.</li>\n<li>Use with <code>autocomplete=\"email\"</code> for faster form completion.</li>\n<li>Use when browser-native email validation is sufficient for your needs.</li>\n<li>Use in contact forms, registration flows, and profile editors.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for general text input -- use TextInput instead.</li>\n<li>Do not use for displaying an email as a clickable link -- use EmailLink instead.</li>\n<li>Do not use when you need placeholder text or autocomplete support; consider a more feature-rich email input variant.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a native <code>&lt;input type=\"email\"&gt;</code> with accessible labeling via <code>aria-label</code>, two-way value binding, and support for <code>required</code> and <code>disabled</code> states. Browser-native email validation is included automatically. The consumer provides all visual styling, layout, and any custom validation messages.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.email-input</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;input&gt;</code> element with class <code>email-input</code></li>\n<li>Verify aria-label=\"...\"<code> -- provides an accessible name for the email input since it has no visible </code>&lt;label&gt;` element</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Place the email input near related contact fields. Provide clear error styling when validation fails so users know the expected format.</li>\n<li><strong>Developers</strong>: Rely on the browser's built-in email validation for basic format checking. Add server-side validation for stricter requirements. Pair with a Field component for label and error message display.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>text-input</code> — a single-line text input field &lt;input type=\"text\"&gt;</li>\n<li><code>email-link</code> — a mailto hyperlink for an email address</li>\n<li><code>tel-input</code> — an input for entering a telephone number &lt;input type=\"tel\"&gt;</li>\n<li><code>url-input</code> — an input for entering a URL &lt;input type=\"url\"&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN <code>&lt;input type=\"email\"&gt;</code>: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/email</li>\n</ul>";
</script>

<svelte:head>
  <title>Email Input — Lily Design System</title>
  <meta name="description" content="An email input wraps a native &lt;input type=&quot;email&quot;&gt; with accessible labeling. A simpler variant of EmailAddressInput for basic email collection, it omits placeholder and autocomplete support in favo..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
