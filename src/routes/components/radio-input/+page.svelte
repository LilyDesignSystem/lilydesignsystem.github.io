<script lang="ts">
  const html: string = "<h1>Radio Input</h1>\n\n<p>A radio input provides a bare <code>&lt;input type=\"radio\"&gt;</code> element without a wrapping label. It is designed for use in custom radio group layouts where labels are provided separately or by a parent component. The component uses <code>aria-label</code> to ensure each radio button has an accessible name.</p>\n\n<p>This component is useful in custom-designed radio groups, card selection interfaces, segmented controls, and any layout where the radio button and its label need to be independently positioned or styled. For simple radio groups with adjacent labels, consider wrapping this in a <code>&lt;label&gt;</code> element.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a native <code>&lt;input type=\"radio\"&gt;</code> element</li>\n<li>Uses <code>aria-label</code> for accessible naming (no visible label element included)</li>\n<li>Supports <code>name</code> attribute for grouping radio buttons into a mutually exclusive set</li>\n<li>Supports <code>value</code> attribute for identifying which option is selected</li>\n<li>Supports <code>checked</code> and <code>disabled</code> states</li>\n<li>Does not use two-way binding for checked state; consumers can use event handlers or bind directly</li>\n<li>Spreads <code>restProps</code> for consumer customization (e.g., <code>onchange</code>, <code>id</code>)</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name via <code>aria-label</code></li>\n<li><code>name</code>: string (default: <code>undefined</code>) -- radio group name for mutual exclusion</li>\n<li><code>value</code>: string (default: <code>undefined</code>) -- the value representing this radio option</li>\n<li><code>checked</code>: boolean (default: <code>false</code>) -- whether this radio button is currently selected</li>\n<li><code>disabled</code>: boolean (default: <code>false</code>) -- whether the radio button is disabled</li>\n<li><code>...restProps</code>: any additional HTML attributes spread onto the input</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;Fieldset legend=\"Are you 18 or over?\"&gt;\n  &lt;RadioGroup name=\"age-check\"&gt;\n    &lt;RadioInput label=\"Yes\" value=\"yes\" /&gt;\n    &lt;RadioInput label=\"No\" value=\"no\" /&gt;\n  &lt;/RadioGroup&gt;\n&lt;/Fieldset&gt;\n\n&lt;Fieldset legend=\"How would you like to receive your results?\"&gt;\n  &lt;RadioGroup name=\"results-preference\"&gt;\n    &lt;RadioInput label=\"Email\" value=\"email\" hint=\"We will send results within 24 hours\" /&gt;\n    &lt;RadioInput label=\"Post\" value=\"post\" hint=\"Results will arrive within 7 working days\" /&gt;\n    &lt;RadioInput label=\"I do not want my results\" value=\"none\" /&gt;\n  &lt;/RadioGroup&gt;\n&lt;/Fieldset&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>Native radio input keyboard behavior is handled by the browser:</p>\n\n<ul>\n<li><strong>Tab</strong>: Moves focus to the selected radio in the group (or the first if none selected)</li>\n<li><strong>Arrow Up / Arrow Left</strong>: Moves selection to the previous radio in the group</li>\n<li><strong>Arrow Down / Arrow Right</strong>: Moves selection to the next radio in the group</li>\n<li><strong>Space</strong>: Selects the focused radio button (if not already selected)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides an accessible name for the radio button since no visible <code>&lt;label&gt;</code> element is included</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use when users can only select one option from a list of mutually exclusive choices</li>\n<li>Use within a RadioGroup inside a Fieldset with a legend that poses the question</li>\n<li>Use with a \"None of these\" or \"I do not know\" option when it is valid for users to decline all options</li>\n<li>Use with Hint on individual radio items when extra context helps users choose</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when users need to select more than one option -- use CheckboxInput within a CheckboxGroup instead</li>\n<li>Do not pre-select a radio option -- users may miss the question or submit the wrong answer</li>\n<li>Do not use for toggling a single setting on or off -- use SwitchButton for immediate-effect toggles</li>\n<li>Do not use for long lists of options -- use Select or Combobox when the list exceeds 6-8 items</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>The RadioInput headless component provides a native <code>&lt;input type=\"radio\"&gt;</code> with <code>aria-label</code> for accessible naming, and supports <code>name</code>, <code>value</code>, <code>checked</code>, and <code>disabled</code> attributes. The browser handles all radio group keyboard navigation natively. The consumer provides all visual styling.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.radio-input</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;input&gt;</code> element with class <code>radio-input</code></li>\n<li>Verify aria-label={label}<code> -- provides an accessible name for the radio button since no visible </code>&lt;label&gt;` element is included</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Ensure the clickable area around each radio option is large enough for comfortable touch interaction, even when using custom visual designs.</li>\n<li><strong>Developers</strong>: Always set a consistent <code>name</code> attribute across grouped RadioInput components to ensure mutual exclusion, and use <code>onchange</code> handlers to update state.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>radio-group</code> — a group of radio buttons for selecting one option</li>\n<li><code>checkbox-input</code> — a checkbox input for toggling a boolean value &lt;input type=\"checkbox\"&gt;</li>\n<li><code>segment-group-item</code> — one selectable segment in a segment group</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Radio Group Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/radiobutton/</li>\n<li>MDN input type=\"radio\": https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/radio</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<label class=\"label\" for=\"demo-input\"><input class=\"radio-input\" type=\"radio\" id=\"demo-input\" aria-label=\"Radio input\" /> Radio</label>";
  const svelteSource: string = "// In your Svelte component:\nimport RadioInput from \"lily-design-system-svelte-headless/components/RadioInput/RadioInput.svelte\";\n\n<RadioInput label=\"Label\" />\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Radio Input — Lily Design System</title>
  <meta name="description" content="A radio input provides a bare &lt;input type=&quot;radio&quot;&gt; element without a wrapping label. It is designed for use in custom radio group layouts where labels are provided separately or by a parent compone..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

