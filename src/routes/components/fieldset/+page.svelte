<script lang="ts">
  const html: string = "<h1>Fieldset</h1>\n\n<p>A fieldset groups related form elements together using the native <code>&lt;fieldset&gt;</code> and <code>&lt;legend&gt;</code> HTML elements. This provides both a visual grouping and a semantic association that screen readers announce when users navigate into the group, helping them understand the context of related inputs.</p>\n\n<p>The component supports a <code>disabled</code> prop that disables all child form controls at once, making it useful for sections of a form that should be conditionally editable. The legend text describes the purpose of the grouped controls.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses native <code>&lt;fieldset&gt;</code> element with a <code>&lt;legend&gt;</code> child for semantic form grouping</li>\n<li>The <code>disabled</code> attribute on <code>&lt;fieldset&gt;</code> natively disables all descendant form controls</li>\n<li>Spreads <code>restProps</code> onto the <code>&lt;fieldset&gt;</code> element for consumer customization</li>\n<li>No custom ARIA needed because native elements handle group semantics</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>legend</code>: string (required) -- text for the <code>&lt;legend&gt;</code> element describing the group</li>\n<li><code>disabled</code>: boolean (default: false) -- whether all controls in the fieldset are disabled</li>\n<li><code>children</code>: slot (required) -- grouped form controls</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;Fieldset legend=\"What is your address?\"&gt;\n  &lt;Field label=\"Address line 1\"&gt;\n    &lt;TextInput label=\"Address line 1\" autocomplete=\"address-line1\" /&gt;\n  &lt;/Field&gt;\n  &lt;Field label=\"Address line 2 (optional)\"&gt;\n    &lt;TextInput label=\"Address line 2\" autocomplete=\"address-line2\" /&gt;\n  &lt;/Field&gt;\n  &lt;Field label=\"Town or city\"&gt;\n    &lt;TextInput label=\"Town or city\" autocomplete=\"address-level2\" /&gt;\n  &lt;/Field&gt;\n  &lt;Field label=\"Postcode\"&gt;\n    &lt;TextInput label=\"Postcode\" autocomplete=\"postal-code\" /&gt;\n  &lt;/Field&gt;\n&lt;/Fieldset&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;Fieldset legend=\"What is your date of birth?\"&gt;\n  &lt;Hint&gt;For example, 15 3 1984&lt;/Hint&gt;\n  &lt;DateInput label=\"Date of birth\" /&gt;\n&lt;/Fieldset&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive container. Keyboard interactions are determined by the consumer-provided child form controls.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>No custom ARIA attributes required -- native <code>&lt;fieldset&gt;</code> and <code>&lt;legend&gt;</code> elements provide built-in group semantics and labeling for screen readers</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to group related form inputs that answer the same question, such as date fields (day, month, year) or address lines.</li>\n<li>Use with a <code>&lt;legend&gt;</code> as the first child element to describe the group -- this is essential for screen reader users.</li>\n<li>Use when a question on the page applies to multiple inputs that need to be understood as a set.</li>\n<li>Use with the legend as the page heading (<code>&lt;h1&gt;</code> inside <code>&lt;legend&gt;</code>) when asking one question per page.</li>\n<li>Use when the <code>disabled</code> attribute should cascade to all child form controls at once.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use to group unrelated inputs -- only inputs that share a common question or purpose belong together.</li>\n<li>Do not use for a single input that already has its own label -- use Field with Label instead.</li>\n<li>Do not use for visual-only grouping without semantic meaning -- use a <code>&lt;div&gt;</code> or layout component instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides native <code>&lt;fieldset&gt;</code> and <code>&lt;legend&gt;</code> elements with built-in group semantics for screen readers. The <code>disabled</code> attribute natively disables all descendant form controls. The consumer provides all visual styling for the fieldset border, legend appearance, and internal layout.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.fieldset</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;fieldset&gt;</code> element with class <code>fieldset</code></li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Style the fieldset border and legend consistently. Use the legend text to clearly describe the purpose of the grouped fields (e.g., \"Shipping address\").</li>\n<li><strong>Developers</strong>: Use the <code>disabled</code> prop to conditionally disable an entire section of the form (e.g., when \"same as billing\" is checked). No custom ARIA is needed because native elements handle semantics.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>Fieldset groups multiple Field components together. Each Field wraps a label and input, and Fieldset provides the semantic group boundary with a legend. Use Fieldset inside a Form for well-structured form layouts.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>form</code> — a form element for collecting and submitting user data</li>\n<li><code>field</code> — a form field wrapper with label, input, and error message</li>\n<li><code>checkbox-group</code> — a group component that manages a collection of checkboxes with shared state</li>\n<li><code>radio-group</code> — a group of radio buttons for selecting one option</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>HTML Specification fieldset: https://html.spec.whatwg.org/multipage/form-elements.html#the-fieldset-element</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<fieldset class=\"fieldset\"><legend>Group label</legend><div class=\"field\"><label class=\"label\"><input type=\"checkbox\" /> Option A</label></div></fieldset>";
  const svelteSource: string = "// In your Svelte component:\nimport Fieldset from \"lily-design-system-svelte-headless/components/Fieldset/Fieldset.svelte\";\n\n<Fieldset>\n  <!-- content -->\n</Fieldset>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Fieldset — Lily Design System</title>
  <meta name="description" content="A fieldset groups related form elements together using the native &lt;fieldset&gt; and &lt;legend&gt; HTML elements. This provides both a visual grouping and a semantic association that screen readers announce..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

