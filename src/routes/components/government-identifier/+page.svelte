<script lang="ts">
  const html: string = "<h1>Government Identifier</h1>\n\n<p>A government identifier is a <code>&lt;section&gt;</code> placed at the bottom of a page that identifies the parent agency, links to the agency's site, optionally shows the agency logo and description, and exposes a <code>&lt;nav&gt;</code> of required government links.</p>\n\n<p>Use it on government and public-sector sites that must comply with parent agency identification standards (e.g., 21st Century IDEA Act).</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses a <code>&lt;section&gt;</code> element with <code>aria-label</code> to form a labeled region</li>\n<li><code>label</code> prop is <strong>required</strong> — sets <code>aria-label</code> on the section</li>\n<li><code>agencyName</code> prop is <strong>required</strong> — rendered inside the masthead</li>\n<li>When <code>agencyHref</code> is provided, the agency name is wrapped in an <code>&lt;a&gt;</code></li>\n<li>Optional <code>logoUrl</code> + <code>logoAlt</code> render an <code>&lt;img&gt;</code> in the masthead</li>\n<li>Optional <code>description</code> renders a paragraph in the masthead</li>\n<li>Children render inside an inner <code>&lt;nav class=\"government-identifier-links\"&gt;</code> with <code>aria-label</code></li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (<strong>required</strong>) -- aria-label for the section</li>\n<li><code>agencyName</code>: string (<strong>required</strong>) -- parent agency name</li>\n<li><code>agencyHref</code>: string (optional) -- link to the agency's site</li>\n<li><code>logoUrl</code>: string (optional) -- agency logo src</li>\n<li><code>logoAlt</code>: string (optional) -- agency logo alt text</li>\n<li><code>description</code>: string (optional) -- agency description text</li>\n<li><code>children</code>: slot -- required-links list content (typically a list of links)</li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Identifier with logo, agency link, and required links:</p>\n\n<pre><code class=\"language-html\">&lt;GovernmentIdentifier\n  label=\"Agency identifier\"\n  agencyName=\"General Services Administration\"\n  agencyHref=\"https://www.gsa.gov\"\n  logoUrl=\"/img/gsa-logo.svg\"\n  logoAlt=\"GSA logo\"\n  description=\"A U.S. General Services Administration website\"\n&gt;\n  &lt;ul&gt;\n    &lt;li&gt;&lt;a href=\"/about\"&gt;About this site&lt;/a&gt;&lt;/li&gt;\n    &lt;li&gt;&lt;a href=\"/accessibility\"&gt;Accessibility statement&lt;/a&gt;&lt;/li&gt;\n    &lt;li&gt;&lt;a href=\"/foia\"&gt;FOIA requests&lt;/a&gt;&lt;/li&gt;\n    &lt;li&gt;&lt;a href=\"/privacy\"&gt;Privacy policy&lt;/a&gt;&lt;/li&gt;\n  &lt;/ul&gt;\n&lt;/GovernmentIdentifier&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: moves focus through the agency link and child links</li>\n<li>All key handling is delegated to native interactive elements</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>&lt;section&gt;</code> with <code>aria-label</code> provides a labeled region landmark</li>\n<li>Inner <code>&lt;nav&gt;</code> with <code>aria-label</code> provides a labeled navigation landmark</li>\n<li>Image <code>alt</code> provides the logo's accessible text</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use at the bottom of every page on a government website to identify the parent agency.</li>\n<li>Use to expose required government links (FOIA, accessibility, privacy, no-FEAR, etc.).</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use on non-government sites — use a regular <code>Footer</code> instead.</li>\n<li>Do not use as the page's primary navigation.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>Renders the structural skeleton — <code>&lt;section&gt;</code> with masthead <code>&lt;div&gt;</code> and required-links <code>&lt;nav&gt;</code> — wired with the correct ARIA. Visual layout, columns, logo size, and link styling are entirely the consumer's responsibility.</p>\n\n<h2>Styles</h2>\n\n<p>Consumer CSS targets the <code>government-identifier</code> class plus the inner <code>government-identifier-masthead</code>, <code>government-identifier-agency</code>, <code>government-identifier-description</code>, and <code>government-identifier-links</code> classes for fine-grained control.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;section&gt;</code> element with class <code>government-identifier</code></li>\n<li>Verify <code>aria-label</code> is present and equals the <code>label</code> prop</li>\n<li>Verify <code>agencyName</code> is rendered in the masthead</li>\n<li>Verify the agency name is wrapped in an <code>&lt;a&gt;</code> when <code>agencyHref</code> is provided</li>\n<li>Verify the logo image renders only when <code>logoUrl</code> is provided</li>\n<li>Verify the description paragraph renders only when <code>description</code> is provided</li>\n<li>Verify children render inside the <code>&lt;nav class=\"government-identifier-links\"&gt;</code></li>\n<li>Verify the inner <code>&lt;nav&gt;</code> has <code>aria-label</code> matching <code>label</code></li>\n<li>Verify additional HTML attributes pass through to the root <code>&lt;section&gt;</code></li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Keep the masthead layout consistent across all government sites in your portfolio. Treat the description as a single short sentence.</li>\n<li><strong>Developers</strong>: Always pass an <code>&lt;ul&gt;</code> of required links as children. Always supply <code>logoAlt</code> when <code>logoUrl</code> is set.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>government-banner</code> — a banner identifying a website as belonging to a government, with an expandable details panel</li>\n<li><code>organization</code> — an organization component that contains other organization-related information</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>US Web Design System Identifier: https://designsystem.digital.gov/components/identifier/</li>\n<li>21st Century IDEA Act: https://www.congress.gov/bill/115th-congress/house-bill/5759</li>\n<li>WAI-ARIA region role: https://www.w3.org/TR/wai-aria-1.2/#region</li>\n<li>WAI-ARIA navigation role: https://www.w3.org/TR/wai-aria-1.2/#navigation</li>\n</ul>";
</script>

<svelte:head>
  <title>Government Identifier — Lily Design System</title>
  <meta name="description" content="A government identifier is a &lt;section&gt; placed at the bottom of a page that identifies the parent agency, links to the agency's site, optionally shows the agency logo and description, and exposes a ..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
