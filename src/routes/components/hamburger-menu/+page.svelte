<script lang="ts">
  const html: string = "<h1>Hamburger Menu</h1>\n\n<p>A hamburger menu is a headless toggle button that reveals or hides a navigation panel. It is commonly used on mobile layouts and compact interfaces to conserve screen space by hiding the primary navigation behind a toggle control.</p>\n\n<p>When activated, the button toggles the visibility of a navigation region containing links, menus, or other navigational content provided by the consumer. The component manages ARIA attributes for accessibility, linking the toggle button to the navigation panel via <code>aria-controls</code> and communicating the open/closed state via <code>aria-expanded</code>.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> wrapper containing a <code>&lt;button&gt;</code> toggle and a conditional navigation region</li>\n<li>The button uses <code>aria-expanded</code> and <code>aria-controls</code> to communicate state to assistive technologies</li>\n<li>The navigation region uses <code>role=\"navigation\"</code> with <code>aria-label</code> for landmark identification</li>\n<li>A unique <code>menuId</code> is generated using <code>Math.random()</code> for the <code>aria-controls</code>/<code>id</code> link</li>\n<li>Supports two-way binding on the <code>open</code> prop</li>\n<li>Navigation content is conditionally rendered with conditional rendering, removing it from the DOM when closed</li>\n<li>Uses slot for the <code>children</code> prop</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (default: \"Menu\") -- accessible name for the toggle button and the navigation region</li>\n<li><code>open</code>: boolean (default: false) -- bindable boolean controlling whether the navigation panel is visible</li>\n<li><code>children</code>: slot (required) -- navigation content rendered when the menu is open</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the outer <code>&lt;div&gt;</code> wrapper</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Mobile navigation with site links:</p>\n\n<pre><code class=\"language-html\">&lt;HamburgerMenu label=\"Main menu\" open={menuOpen}&gt;\n    &lt;ul&gt;\n        &lt;li&gt;&lt;a href=\"/\"&gt;Home&lt;/a&gt;&lt;/li&gt;\n        &lt;li&gt;&lt;a href=\"/about\"&gt;About&lt;/a&gt;&lt;/li&gt;\n        &lt;li&gt;&lt;a href=\"/contact\"&gt;Contact&lt;/a&gt;&lt;/li&gt;\n    &lt;/ul&gt;\n&lt;/HamburgerMenu&gt;</code></pre>\n\n<p>Dashboard navigation hidden behind hamburger toggle:</p>\n\n<pre><code class=\"language-html\">&lt;HamburgerMenu label=\"Dashboard menu\" open={navOpen}&gt;\n    &lt;ul&gt;\n        &lt;li&gt;&lt;a href=\"/dashboard\"&gt;Dashboard&lt;/a&gt;&lt;/li&gt;\n        &lt;li&gt;&lt;a href=\"/reports\"&gt;Reports&lt;/a&gt;&lt;/li&gt;\n        &lt;li&gt;&lt;a href=\"/settings\"&gt;Settings&lt;/a&gt;&lt;/li&gt;\n        &lt;li&gt;&lt;a href=\"/logout\"&gt;Log out&lt;/a&gt;&lt;/li&gt;\n    &lt;/ul&gt;\n&lt;/HamburgerMenu&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Enter: Toggles the menu open/closed when the button has focus (native button behavior)</li>\n<li>Space: Toggles the menu open/closed when the button has focus (native button behavior)</li>\n<li>Tab: Moves focus to/from the toggle button and into the navigation content when open</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- on the button, provides an accessible name for the toggle control</li>\n<li><code>aria-expanded={open}</code> -- on the button, indicates whether the navigation panel is currently visible</li>\n<li><code>aria-controls={menuId}</code> -- on the button, references the id of the navigation panel</li>\n<li><code>role=\"navigation\"</code> -- on the content container, identifies it as a navigation landmark</li>\n<li><code>aria-label={label}</code> -- on the navigation container, provides an accessible name for the landmark</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use as a toggle button that opens mobile navigation.</li>\n<li>Use when screen space is limited and the navigation panel should be hidden by default.</li>\n<li>Use on mobile layouts and compact interfaces to conserve screen space.</li>\n<li>Use for responsive designs where navigation collapses at smaller breakpoints.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use on desktop where there is space for full navigation -- use NavigationMenu or Header with navigation instead.</li>\n<li>Do not use for action menus -- use DropdownMenu or Menu instead.</li>\n<li>Do not use for content disclosure -- use Collapsible or Details instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;button&gt;</code> with <code>aria-expanded</code>, <code>aria-controls</code>, and a conditional navigation region with <code>role=\"navigation\"</code>. The consumer provides all visual styling including the hamburger icon, animation, panel positioning, and overlay effects.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.hamburger-menu</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;button&gt;</code> element with class <code>hamburger-menu</code></li>\n<li>Verify aria-label={label}` -- on the button, provides an accessible name for the toggle control</li>\n<li>Verify role=\"navigation\"` -- on the content container, identifies it as a navigation landmark</li>\n<li>Verify aria-label={label}` -- on the navigation container, provides an accessible name for the landmark</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use a universally recognized three-line (hamburger) icon. Animate the icon to an X when open to indicate the toggle state.</li>\n<li><strong>Developers</strong>: Use two-way binding on <code>open</code> to synchronize menu state. Ensure the navigation content receives focus when opened for keyboard accessibility.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>menu-bar</code> — a horizontal bar of menu triggers</li>\n<li><code>navigation-menu</code> — a site-wide navigation menu with links</li>\n<li><code>drawer</code> — a panel that slides in from the edge of the screen</li>\n<li><code>menu</code> — a list of actions or options triggered by a button</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Disclosure Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/disclosure/</li>\n<li>WAI-ARIA Navigation Landmark: https://www.w3.org/WAI/ARIA/apd/practices/landmark-regions/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<button class=\"hamburger-menu\" aria-expanded=\"false\" aria-label=\"Menu\">\u2630</button>";
  const svelteSource: string = "// In your Svelte component:\nimport HamburgerMenu from \"lily-design-system-svelte-headless/components/HamburgerMenu/HamburgerMenu.svelte\";\n\n<HamburgerMenu>\n  <!-- list children -->\n</HamburgerMenu>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Hamburger Menu — Lily Design System</title>
  <meta name="description" content="A hamburger menu is a headless toggle button that reveals or hides a navigation panel. It is commonly used on mobile layouts and compact interfaces to conserve screen space by hiding the primary na..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

