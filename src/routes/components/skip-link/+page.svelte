<script lang="ts">
  const html: string = "<h1>Skip Link</h1>\n\n<p>SkipLink is a headless navigation link that allows keyboard users to bypass repetitive content (such as navigation menus and headers) and jump directly to the main content area. It is one of the most important accessibility features for keyboard-only users and screen reader users.</p>\n\n<p>Use SkipLink as the very first focusable element on a page. When a keyboard user tabs into the page, the skip link receives focus first and provides a quick shortcut to the main content. Consumers typically style the link to be visually hidden until focused, then position it prominently on screen when activated. The component renders a simple <code>&lt;a&gt;</code> element with configurable href target and label text.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders an <code>&lt;a&gt;</code> element with the <code>href</code> pointing to the target content anchor</li>\n<li>Default <code>href</code> is <code>\"#content\"</code> and default <code>label</code> is <code>\"Skip to content\"</code>, both configurable</li>\n<li>The label text is rendered as the link's text content (not via <code>aria-label</code>)</li>\n<li>Consumer is responsible for styling the link (typically visually hidden until <code>:focus</code>)</li>\n<li>Consumer must ensure the target element exists with a matching <code>id</code> attribute</li>\n<li>Spreads <code>restProps</code> onto the anchor for consumer customization</li>\n<li>No slot import needed since this component does not use children</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>href</code>: string (default: <code>\"#content\"</code>) -- the anchor target that the link navigates to</li>\n<li><code>label</code>: string (default: <code>\"Skip to content\"</code>) -- the visible link text</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;a&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Default skip link as the first element in the page, targeting a <code>&lt;main id=\"content\"&gt;</code> element:</p>\n\n<pre><code class=\"language-html\">&lt;SkipLink /&gt;\n\n&lt;!-- Later in the page --&gt;\n&lt;main id=\"content\"&gt;\n  &lt;h1&gt;Page title&lt;/h1&gt;\n  ...\n&lt;/main&gt;</code></pre>\n\n<p>Custom skip link targeting a specific main content area with a custom label:</p>\n\n<pre><code class=\"language-html\">&lt;SkipLink href=\"#maincontent\" label=\"Skip to main content\" /&gt;\n\n&lt;!-- Later in the page --&gt;\n&lt;main id=\"maincontent\" tabindex=\"-1\"&gt;\n  &lt;h1&gt;Search results&lt;/h1&gt;\n  ...\n&lt;/main&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Moves focus to the skip link (should be the first focusable element on the page)</li>\n<li>Enter: Activates the link, moving focus to the target anchor element</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>No explicit ARIA roles or attributes are needed; the <code>&lt;a&gt;</code> element with an <code>href</code> is natively accessible as a link</li>\n<li>The link text content (<code>label</code> prop) serves as the accessible name</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use on every page of your service as the very first focusable element in the DOM</li>\n<li>Use to allow keyboard and screen reader users to skip repetitive navigation and jump directly to the main content</li>\n<li>Use with a target anchor like <code>#maincontent</code> on the main content area</li>\n<li>Use on pages with significant repeated content before the main area (navigation bars, headers, sidebars)</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>There are no situations where SkipLink should be omitted -- it is required for accessibility on every page (WCAG 2.1 Level A)</li>\n<li>Do not make the skip link permanently visible -- it should only appear when it receives keyboard focus</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component renders a simple <code>&lt;a&gt;</code> element with a configurable <code>href</code> target and visible link text. It provides the semantic skip navigation link with no visual styling. The consumer is responsible for styling the link to be visually hidden until focused, then positioning it prominently on screen.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.skip-link</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;a&gt;</code> element with class <code>skip-link</code></li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Style the skip link to appear at the top of the viewport with high contrast when focused. Ensure it is large enough and clearly visible so keyboard users can easily see it.</li>\n<li><strong>Developers</strong>: Ensure the target element (e.g., <code>&lt;main id=\"content\"&gt;</code>) exists and has <code>tabindex=\"-1\"</code> so focus moves correctly. Place the SkipLink component before all other focusable page content in the DOM order.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>back-link</code> — a navigation link to return to a previous page</li>\n<li><code>go-to-top</code> — a link that returns users to the top of a long page</li>\n<li><code>navigation-menu</code> — a site-wide navigation menu with links</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WCAG 2.1 Bypass Blocks: https://www.w3.org/WAI/WCAG21/Understanding/bypass-blocks.html</li>\n<li>WAI-ARIA Authoring Practices - Skip Navigation: https://www.w3.org/WAI/ARIA/apg/practices/skip-nav/</li>\n<li>WebAIM Skip Navigation Links: https://webaim.org/techniques/skipnav/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<a class=\"skip-link\" href=\"#main-content\">Skip to main content</a>";
  const svelteSource: string = "// In your Svelte component:\nimport SkipLink from \"lily-design-system-svelte-headless/components/SkipLink/SkipLink.svelte\";\n\n<SkipLink>SkipLink</SkipLink>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Skip Link — Lily Design System</title>
  <meta name="description" content="SkipLink is a headless navigation link that allows keyboard users to bypass repetitive content (such as navigation menus and headers) and jump directly to the main content area. It is one of the mo..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

