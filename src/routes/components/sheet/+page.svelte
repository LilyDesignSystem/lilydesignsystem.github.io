<script lang="ts">
  const html: string = "<h1>Sheet</h1>\n\n<p>Sheet is a headless sliding panel overlay that appears from an edge of the viewport. It functions as a modal dialog with focus trapping semantics and Escape key support, commonly used for navigation menus, settings panels, detail views, or filter interfaces.</p>\n\n<p>Use Sheet when you need a temporary panel that slides in from the top, right, bottom, or left edge of the screen. The component is conditionally rendered based on its <code>open</code> state and exposes a <code>data-side</code> attribute indicating which edge it appears from, allowing consumers to apply appropriate slide-in animations and positioning via CSS.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Conditionally renders a <code>&lt;div&gt;</code> element with <code>role=\"dialog\"</code> only when <code>open</code> is <code>true</code></li>\n<li>Sets <code>aria-modal=\"true\"</code> to indicate the dialog is modal and should trap focus</li>\n<li>Sets <code>tabindex=\"-1\"</code> to allow programmatic focus on the dialog container</li>\n<li>Exposes a <code>data-side</code> attribute with the value of the <code>side</code> prop for CSS targeting</li>\n<li>Handles Escape key via an <code>onkeydown</code> handler that sets <code>open</code> to <code>false</code></li>\n<li>Supports two-way binding for the <code>open</code> prop, enabling two-way <code>open</code> binding from the parent</li>\n<li>Consumer is responsible for focus management (moving focus into the sheet when opened)</li>\n<li>Spreads <code>restProps</code> onto the dialog div for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible label for the sheet dialog, applied via <code>aria-label</code></li>\n<li><code>open</code>: boolean (default: <code>false</code>) -- whether the sheet is visible; two-way bindable via <code>open</code></li>\n<li><code>side</code>: <code>\"left\" | \"right\" | \"top\" | \"bottom\"</code> (default: <code>\"right\"</code>) -- which edge the sheet appears from</li>\n<li><code>children</code>: slot (required) -- content to render inside the sheet</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;div&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Settings panel sliding in from the right:</p>\n\n<pre><code class=\"language-html\">&lt;Sheet label=\"Settings\" open={settingsOpen} side=\"right\"&gt;\n  &lt;h2&gt;Preferences&lt;/h2&gt;\n  &lt;form&gt;\n    &lt;label&gt;&lt;input type=\"checkbox\" /&gt; Enable notifications&lt;/label&gt;\n    &lt;label&gt;&lt;input type=\"checkbox\" /&gt; Dark mode&lt;/label&gt;\n  &lt;/form&gt;\n  &lt;button onclick={() =&gt; (settingsOpen = false)}&gt;Close&lt;/button&gt;\n&lt;/Sheet&gt;</code></pre>\n\n<p>Mobile action sheet sliding up from the bottom:</p>\n\n<pre><code class=\"language-html\">&lt;Sheet label=\"Share options\" open={shareOpen} side=\"bottom\"&gt;\n  &lt;ul&gt;\n    &lt;li&gt;&lt;button&gt;Copy link&lt;/button&gt;&lt;/li&gt;\n    &lt;li&gt;&lt;button&gt;Email&lt;/button&gt;&lt;/li&gt;\n    &lt;li&gt;&lt;button&gt;Print&lt;/button&gt;&lt;/li&gt;\n  &lt;/ul&gt;\n&lt;/Sheet&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Escape: Closes the sheet by setting <code>open</code> to <code>false</code></li>\n<li>Tab: Moves focus between focusable elements within the sheet (consumer should implement focus trapping)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"dialog\"</code> -- identifies the sheet as a dialog window</li>\n<li><code>aria-label={label}</code> -- provides an accessible name for the dialog</li>\n<li><code>aria-modal=\"true\"</code> -- indicates the dialog is modal, meaning content behind it is inert</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for a panel that slides in from a screen edge as an overlay, commonly from the bottom on mobile.</li>\n<li>Use when you need a temporary overlay for navigation, settings, filters, or detail views.</li>\n<li>Use when the content should overlay the page rather than navigate away from it.</li>\n<li>Use for action sheets on mobile that present a list of options from the bottom.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for side navigation -- use Drawer or Sidebar instead.</li>\n<li>Do not use for modal dialogs that should be centered -- use Dialog instead.</li>\n<li>Do not use for critical confirmations or urgent messages -- use AlertDialog instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a modal dialog container with <code>role=\"dialog\"</code>, <code>aria-modal=\"true\"</code>, <code>aria-label</code>, Escape key dismissal, and a <code>data-side</code> attribute for CSS targeting. The consumer provides all visual styling including slide-in animations, backdrop, positioning, and focus management.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.sheet</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>sheet</code></li>\n<li>Verify role=\"dialog\"` -- identifies the sheet as a dialog window</li>\n<li>Verify aria-label={label}` -- provides an accessible name for the dialog</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Include a visible close button and a semi-transparent backdrop so users understand the panel is an overlay. Animate the slide-in transition to reinforce which edge the sheet originates from.</li>\n<li><strong>Developers</strong>: Implement focus trapping so keyboard users cannot tab behind the sheet. Move focus into the sheet on open and restore focus to the trigger element on close.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>drawer</code> — a panel that slides in from the edge of the screen</li>\n<li><code>dialog</code> — a modal or non-modal dialog window</li>\n<li><code>slide-out-drawer</code> — a drawer that slides out from the side of the page</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Dialog (Modal) Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/</li>\n<li>WAI-ARIA Authoring Practices - Dialog: https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/examples/dialog/</li>\n</ul>";
</script>

<svelte:head>
  <title>Sheet — Lily Design System</title>
  <meta name="description" content="Sheet is a headless sliding panel overlay that appears from an edge of the viewport. It functions as a modal dialog with focus trapping semantics and Escape key support, commonly used for navigatio..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
