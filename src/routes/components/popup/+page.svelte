<script lang="ts">
  const html: string = "<h1>Popup</h1>\n\n<p>A popup is a UI component that temporarily appears over the main content to convey information, prompt user actions, or display additional options without navigating away from the current screen. The Popup component provides a headless conditional dialog overlay that renders its content only when the <code>open</code> state is true, using proper dialog semantics for assistive technology.</p>\n\n<p>The component is similar to Popover but is designed as a general-purpose overlay dialog. It uses two-way binding on the <code>open</code> prop, so parent components can control visibility. The consumer provides trigger elements, open/close logic, and all content.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Conditionally renders a <code>&lt;div&gt;</code> with <code>role=\"dialog\"</code> only when <code>open</code> is true</li>\n<li>Supports two-way binding on the <code>open</code> prop</li>\n<li>The <code>label</code> prop is required and provides the dialog's accessible name via <code>aria-label</code></li>\n<li>Rest props are spread onto the <code>&lt;div&gt;</code> element</li>\n<li>The consumer must provide their own trigger element and manage open/close behavior</li>\n<li>Content is provided via the <code>children</code> slot</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name for the popup dialog via <code>aria-label</code></li>\n<li><code>open</code>: boolean (default: false, bindable) -- controls whether the popup is visible; supports two-way binding</li>\n<li><code>children</code>: slot (required) -- the popup content rendered inside the dialog</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;div&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Quick action overlay:</p>\n\n<pre><code class=\"language-html\">&lt;button onclick={() =&gt; showPopup = !showPopup}&gt;Sort results&lt;/button&gt;\n&lt;Popup label=\"Sort options\" open={showPopup}&gt;\n  &lt;button onclick={() =&gt; { sortBy('date'); showPopup = false; }}&gt;Date&lt;/button&gt;\n  &lt;button onclick={() =&gt; { sortBy('name'); showPopup = false; }}&gt;Name&lt;/button&gt;\n  &lt;button onclick={() =&gt; { sortBy('relevance'); showPopup = false; }}&gt;Relevance&lt;/button&gt;\n&lt;/Popup&gt;</code></pre>\n\n<p>Inline editing overlay:</p>\n\n<pre><code class=\"language-html\">&lt;button onclick={() =&gt; showEdit = true}&gt;Edit note&lt;/button&gt;\n&lt;Popup label=\"Edit note\" open={showEdit}&gt;\n  &lt;textarea&gt;{noteText}&lt;/textarea&gt;\n  &lt;button onclick={() =&gt; { saveNote(); showEdit = false; }}&gt;Save&lt;/button&gt;\n  &lt;button onclick={() =&gt; showEdit = false}&gt;Cancel&lt;/button&gt;\n&lt;/Popup&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Escape: Consumer should implement closing the popup by setting <code>open</code> to false</li>\n<li>Tab: Should trap focus within the popup when open (consumer responsibility)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"dialog\"</code> -- identifies the popup as a dialog overlay for assistive technology</li>\n<li><code>aria-label=\"...\"</code> -- provides the accessible name for the dialog</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for temporary overlay content that appears above the page, such as dropdown panels, contextual options, or quick action forms.</li>\n<li>Use when the overlay does not require focus trapping or a modal backdrop -- it should allow background interaction.</li>\n<li>Use when a lightweight overlay is more appropriate than a full Dialog, such as for filter panels or inline editing.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for modal interactions requiring focus trapping -- use Dialog or AlertDialog instead.</li>\n<li>Do not use for hover-triggered content -- use HoverCard or Tooltip instead.</li>\n<li>Do not use for persistent side panels -- use Drawer or Sheet instead.</li>\n<li>Do not use for permanent content sections -- use Panel or Card instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>The Popup headless component provides a conditionally rendered <code>&lt;div&gt;</code> with <code>role=\"dialog\"</code> and <code>aria-label</code> for accessible naming. It manages open/close visibility via two-way binding. The consumer provides all visual styling, positioning, trigger elements, and focus trap logic.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.popup</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>popup</code></li>\n<li>Verify role=\"dialog\"` -- identifies the popup as a dialog overlay for assistive technology</li>\n<li>Verify aria-label=\"...\"` -- provides the accessible name for the dialog</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Give popups a clear visual elevation (shadow or border) and ensure they do not cover critical page actions the user may need to reach.</li>\n<li><strong>Developers</strong>: Implement Escape key handling to close the popup, and return focus to the trigger element when the popup closes.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>popover</code> — a floating content box anchored to a trigger element</li>\n<li><code>tooltip</code> — a small popup showing descriptive text on hover or focus</li>\n<li><code>drawer</code> — a panel that slides in from the edge of the screen</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Dialog Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/dialog/</li>\n<li>WAI-ARIA dialog role: https://www.w3.org/TR/wai-aria-1.2/#dialog</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"popup\" role=\"dialog\"><p>Popup content</p></div>";
  const svelteSource: string = "// In your Svelte component:\nimport Popup from \"lily-design-system-svelte-headless/components/Popup/Popup.svelte\";\n\n<Popup>\n  <!-- content -->\n</Popup>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Popup — Lily Design System</title>
  <meta name="description" content="A popup is a UI component that temporarily appears over the main content to convey information, prompt user actions, or display additional options without navigating away from the current screen. T..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

