<script lang="ts">
  const html: string = "<h1>Download Button</h1>\n\n<p>A download button is a hyperlink styled as a button that points to a downloadable file. It uses an <code>&lt;a&gt;</code> element (not <code>&lt;button&gt;</code>) so that users can right-click and \"Save link as…\", and so that the native <code>download</code> attribute can suggest a filename.</p>\n\n<p>Use it for prominent download calls-to-action such as installer downloads, PDF reports, datasets, exported files, and signed asset bundles.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses an <code>&lt;a&gt;</code> element so right-click \"Save link as\" works and the <code>download</code> attribute can apply</li>\n<li><code>href</code> is <strong>required</strong> — the URL of the file</li>\n<li><code>label</code> is <strong>required</strong> — sets <code>aria-label</code> so the accessible name reflects the download intent</li>\n<li><code>fileSize</code> and <code>fileFormat</code> are surfaced as <code>data-file-size</code> and <code>data-file-format</code> for consumer styling</li>\n<li><code>download</code> boolean <code>true</code> renders bare <code>download</code> (browser-suggested filename); a string sets the suggested filename</li>\n<li>Children render the visible button content; defaults to <code>label</code> when omitted</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>href</code>: string (<strong>required</strong>) -- file URL</li>\n<li><code>label</code>: string (<strong>required</strong>) -- aria-label and default visible text</li>\n<li><code>fileSize</code>: string (optional) -- pre-formatted size text (e.g., \"2.4 MB\")</li>\n<li><code>fileFormat</code>: string (optional) -- file format text (e.g., \"PDF\")</li>\n<li><code>download</code>: string | boolean (optional) -- native download attribute</li>\n<li><code>children</code>: slot (optional) -- visible button content; defaults to <code>label</code></li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Basic download link:</p>\n\n<pre><code class=\"language-html\">&lt;DownloadButton href=\"/files/report.pdf\" label=\"Download annual report (PDF, 2.4 MB)\"&gt;\n  Download report\n&lt;/DownloadButton&gt;</code></pre>\n\n<p>With download attribute and metadata:</p>\n\n<pre><code class=\"language-html\">&lt;DownloadButton\n  href=\"/files/report.pdf\"\n  label=\"Download annual report\"\n  fileSize=\"2.4 MB\"\n  fileFormat=\"PDF\"\n  download=\"annual-report-2025.pdf\"\n&gt;\n  Download report\n&lt;/DownloadButton&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus the link</li>\n<li>Enter: Activate the link (download or navigate)</li>\n<li>(All handled natively by <code>&lt;a&gt;</code> element)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Implicit <code>link</code> role from the <code>&lt;a&gt;</code> element</li>\n<li><code>aria-label</code> provides the descriptive accessible name including download intent (required)</li>\n<li><code>data-file-size</code> and <code>data-file-format</code> are non-ARIA hooks for consumer styling</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for prominent file downloads (installers, PDFs, datasets) where the action is the primary call to action.</li>\n<li>Use when the user benefits from a suggested filename via the native <code>download</code> attribute.</li>\n<li>Use when right-click \"Save link as\" is desired behavior.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for in-app actions that do not download a file — use Button instead.</li>\n<li>Do not use for cross-origin URLs where the <code>download</code> attribute will be ignored without proper headers.</li>\n<li>Do not omit <code>label</code> — the accessible name should describe what is being downloaded.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>Renders a native <code>&lt;a&gt;</code> element with <code>href</code>, <code>aria-label</code>, optional <code>download</code>, and optional <code>data-*</code> attributes. Visual button styling, file-format icons, and metadata layout are entirely the consumer's responsibility.</p>\n\n<h2>Styles</h2>\n\n<p>Consumer CSS targets the <code>download-button</code> class. Style it as a button with a clear focus indicator and hover/active feedback. Use the <code>data-file-size</code> and <code>data-file-format</code> attributes to render badges or pseudo-element labels.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders an <code>&lt;a&gt;</code> element with class <code>download-button</code></li>\n<li>Verify <code>href</code> is present and equals the <code>href</code> prop</li>\n<li>Verify <code>aria-label</code> is present and equals the <code>label</code> prop</li>\n<li>Verify <code>data-file-size</code> is rendered when <code>fileSize</code> is provided</li>\n<li>Verify <code>data-file-format</code> is rendered when <code>fileFormat</code> is provided</li>\n<li>Verify <code>download</code> is rendered as bare attribute when <code>download={true}</code> and as filename when a string</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Make file format and size visible (PDF, 2.4 MB) so users know what they are downloading. Use the same focus and hover treatment as other primary buttons.</li>\n<li><strong>Developers</strong>: Pre-format <code>fileSize</code> in the consumer's locale. Use the <code>download</code> attribute only for same-origin URLs or cross-origin URLs that send appropriate <code>Content-Disposition</code> headers.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>button</code> — a generic clickable button element</li>\n<li><code>document-list-item</code> — one document entry in a document list</li>\n<li><code>file-input</code> — an input for selecting files from the file system &lt;input type=\"file\"&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN <code>&lt;a&gt;</code> download attribute: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/a#download</li>\n<li>WAI-ARIA Link Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/link/</li>\n<li>Mozilla Protocol Design System: https://protocol.mozilla.org/</li>\n</ul>";
</script>

<svelte:head>
  <title>Download Button — Lily Design System</title>
  <meta name="description" content="A download button is a hyperlink styled as a button that points to a downloadable file. It uses an &lt;a&gt; element (not &lt;button&gt;) so that users can right-click and &quot;Save link as…&quot;, and so that the nati..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
