<script lang="ts">
  const html: string = "<h1>Action Link</h1>\n\n<p>An action link is a headless anchor component that visually emphasizes a navigation action, typically appearing as a prominent link that leads the user to a new page or triggers a significant navigation event. Inspired by the NHS England action link pattern, it renders a semantic <code>&lt;a&gt;</code> element with optional ARIA label override for screen readers.</p>\n\n<p>Action links are useful when you want to draw attention to a key navigational step, such as \"Continue to next step\" or \"Find a service near you\", distinguishing these from standard inline text links.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as a simple semantic <code>&lt;a&gt;</code> element</li>\n<li>Supports <code>aria-label</code> override via the <code>label</code> prop for when the visible link text is insufficient for screen readers</li>\n<li>Uses <code>slot</code> for children content (text, mixed content, etc.)</li>\n<li>Passes through all additional HTML anchor attributes via <code>...restProps</code></li>\n<li>No hardcoded user-facing strings; all text comes through children and props</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>href</code>: string (required) -- the URL the link points to</li>\n<li><code>label</code>: string (optional) -- accessible label override for screen readers via aria-label</li>\n<li><code>children</code>: slot (required) -- the link content (text or mixed content)</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;ActionLink href=\"/find-a-gp\"&gt;Find a GP near you&lt;/ActionLink&gt;\n\n&lt;ActionLink href=\"/book-appointment\" label=\"Book a vaccination appointment at your local pharmacy\"&gt;\n  Book a vaccination appointment\n&lt;/ActionLink&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus the link (native browser behavior)</li>\n<li>Enter: Activate the link (native browser behavior)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Implicit <code>link</code> role from the <code>&lt;a&gt;</code> element</li>\n<li><code>aria-label</code> -- optional override for screen reader text when the visible link text does not fully convey the action</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to signpost the start of a digital service or key navigational step, such as \"Find a GP near you\" or \"Book an appointment\".</li>\n<li>Use on content-heavy pages where a key action needs visual emphasis beyond a standard inline link.</li>\n<li>Use when the link represents a significant next step in the user's journey, not routine navigation.</li>\n<li>Use within text blocks or care cards to draw attention to an actionable pathway.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use within forms -- use Button for form actions and submissions.</li>\n<li>Do not use for general page-to-page navigation -- use a standard anchor link or BreadcrumbNav instead.</li>\n<li>Do not use when emphasis is not needed -- use InsetText for highlighted content without an action.</li>\n<li>Do not use for returning to a previous page -- use BackLink instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides a semantic <code>&lt;a&gt;</code> element with optional <code>aria-label</code> override and zero visual styling. The consumer is responsible for all CSS including colors, font weight, underline styles, hover effects, focus indicators, and any arrow or icon decoration.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.action-link</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;a&gt;</code> element with class <code>action-link</code></li>\n<li>Verify aria-label` -- optional override for screen reader text when the visible link text does not fully convey the action</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Make action links visually distinct from standard inline links using size, weight, or an arrow icon to signal their importance as key navigational steps.</li>\n<li><strong>Developers</strong>: Use the <code>label</code> prop to provide expanded screen reader text when the visible link text is ambiguous, such as providing \"Find a GP near your postcode\" for a link that just reads \"Find a GP\".</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>call-to-action</code> — a prominent prompt encouraging user action</li>\n<li><code>back-link</code> — a navigation link to return to a previous page</li>\n<li><code>go-to-top</code> — a link that returns users to the top of a long page</li>\n<li><code>skip-link</code> — a hidden link for keyboard users to skip to main content</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>Inspired by NHS England action link pattern</li>\n<li>WAI-ARIA link role: https://www.w3.org/TR/wai-aria-1.2/#link</li>\n</ul>";
</script>

<svelte:head>
  <title>Action Link — Lily Design System</title>
  <meta name="description" content="An action link is a headless anchor component that visually emphasizes a navigation action, typically appearing as a prominent link that leads the user to a new page or triggers a significant navig..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
