<script lang="ts">
  const html: string = "<h1>Flex Stack</h1>\n\n<p>A flex stack is a layout container that arranges its children in a single row or column with a consistent gap, exposing flex direction, alignment, and justification through props so that the consumer never needs to write boilerplate flexbox CSS.</p>\n\n<p>Use it any time you need to arrange a small set of children with predictable spacing — toolbars, button groups, vertical form sections, navigation links.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> with <code>class=\"flex-stack\"</code></li>\n<li>Applies inline styles: <code>display: flex; flex-direction: {direction}; gap: {gap};</code> plus optional <code>align-items</code> and <code>justify-content</code></li>\n<li>Sets <code>data-direction={direction}</code> for consumer CSS hooks</li>\n<li>Does not constrain width or apply any visual treatment</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>direction</code>: \"row\" | \"column\" (default: \"column\") -- flex direction</li>\n<li><code>gap</code>: string (default: \"1rem\") -- gap CSS value</li>\n<li><code>align</code>: string (optional) -- align-items value</li>\n<li><code>justify</code>: string (optional) -- justify-content value</li>\n<li><code>children</code>: slot -- the laid-out content</li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Vertical stack with default 1rem gap:</p>\n\n<pre><code class=\"language-html\">&lt;FlexStack&gt;\n  &lt;Card&gt;One&lt;/Card&gt;\n  &lt;Card&gt;Two&lt;/Card&gt;\n&lt;/FlexStack&gt;</code></pre>\n\n<p>Horizontal toolbar:</p>\n\n<pre><code class=\"language-html\">&lt;FlexStack direction=\"row\" gap=\"0.5rem\" align=\"center\"&gt;\n  &lt;Button&gt;Save&lt;/Button&gt;\n  &lt;Button&gt;Cancel&lt;/Button&gt;\n&lt;/FlexStack&gt;</code></pre>\n\n<p>Horizontal row with space-between justification:</p>\n\n<pre><code class=\"language-html\">&lt;FlexStack direction=\"row\" justify=\"space-between\" align=\"center\"&gt;\n  &lt;h2&gt;Title&lt;/h2&gt;\n  &lt;Button&gt;Action&lt;/Button&gt;\n&lt;/FlexStack&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>None -- this is a layout container; focusable children manage their own keyboard behavior.</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>None — purely structural. Use a more specific landmark or role on a wrapping element if needed (e.g., wrap in <code>&lt;nav&gt;</code> for navigation lists).</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for one-dimensional layouts (a single row or single column).</li>\n<li>Use when you need consistent gap spacing without manual margins.</li>\n<li>Use as the layout primitive inside cards, headers, and form sections.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for two-dimensional grids (use Grid).</li>\n<li>Do not use for masonry/variable-height column layouts (use Masonry).</li>\n<li>Do not use as a navigation landmark — wrap in semantic landmark elements as appropriate.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>Provides only the flexbox layout via inline styles. Sizing, padding, borders, backgrounds, and item-level alignment overrides are the consumer's responsibility.</p>\n\n<h2>Styles</h2>\n\n<p>Consumers may target <code>.flex-stack</code> for global overrides or <code>.flex-stack[data-direction=\"row\"]</code> / <code>.flex-stack[data-direction=\"column\"]</code> for direction-specific styles.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>flex-stack</code></li>\n<li>Verify inline style sets <code>display: flex</code> and <code>flex-direction</code> to the <code>direction</code> prop</li>\n<li>Verify inline style sets <code>gap</code> to the <code>gap</code> prop</li>\n<li>Verify <code>align-items</code> and <code>justify-content</code> are applied when <code>align</code> and <code>justify</code> props are provided</li>\n<li>Verify <code>data-direction</code> attribute matches the <code>direction</code> prop</li>\n<li>Verify children content is rendered</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Specify gap in a consistent spacing scale. Use <code>align=\"center\"</code> for icon-plus-text rows; <code>justify=\"space-between\"</code> for headers with trailing actions.</li>\n<li><strong>Developers</strong>: Reach for FlexStack first for small one-dimensional groups. Use Grid for two-dimensional layouts.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>grid</code> — a CSS grid layout container with configurable columns and gap</li>\n<li><code>container</code> — a generic block-level content container</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN flexbox: https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_flexible_box_layout</li>\n<li>MDN gap: https://developer.mozilla.org/en-US/docs/Web/CSS/gap</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"flex-stack\" aria-label=\"a flex layout container for vertical or horizontal stacking with consistent gap\">FlexStack</div>";
  const svelteSource: string = "// In your Svelte component:\nimport FlexStack from \"lily-design-system-svelte-headless/components/FlexStack/FlexStack.svelte\";\n\n<FlexStack>\n  <!-- content -->\n</FlexStack>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Flex Stack — Lily Design System</title>
  <meta name="description" content="A flex stack is a layout container that arranges its children in a single row or column with a consistent gap, exposing flex direction, alignment, and justification through props so that the consum..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

