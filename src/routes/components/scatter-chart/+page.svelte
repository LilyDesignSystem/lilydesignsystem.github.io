<script lang="ts">
  const html: string = "<h1>ScatterChart</h1>\n\n<p>A two-variable visualization where each observation is plotted as a single dot at its <code>(x, y)</code> coordinate, used to look at the relationship between two measurements — correlation, clustering, outliers — across a set of cases.</p>\n\n<p>Unlike a line or area chart, the x-axis isn't ordered as a sequence and points aren't connected; each dot stands on its own. Multiple series can be plotted as distinct sets of dots so groups can be compared visually.</p>\n\n<p>The component is a headless wrapper around an inline <code>&lt;svg&gt;</code>: it renders a <code>&lt;figure role=\"img\"&gt;</code> containing the dots, names the figure via <code>aria-label</code> (from <code>label</code>), and references an extended <code>description</code> and an optional <code>dataTable</code> slot via <code>aria-describedby</code>. The data table is the canonical accessible alternative — screen readers and exports both benefit from a real <code>&lt;table&gt;</code> rendering of the same numbers, and Lily encourages always providing it.</p>\n\n<p>For ordered continuous data use <code>LineChart</code> or <code>AreaChart</code>. For categorical comparison use <code>BarChart</code> or <code>ColumnChart</code>.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;figure role=\"img\"&gt;</code> containing an inline <code>&lt;svg&gt;</code> rendering of dots positioned at <code>(x, y)</code> coordinates</li>\n<li>Each series is plotted as a distinct set of dots</li>\n<li>An optional accessible data table is rendered via the <code>dataTable</code> slot</li>\n<li>Spreads <code>restProps</code> onto the root <code>&lt;figure&gt;</code></li>\n</ul>\n\n<h2>Props</h2>\n\n<table>\n<thead>\n<tr><th>Prop</th><th>Type</th><th>Default</th><th>Description</th></tr>\n</thead>\n<tbody>\n<tr><td><code>label</code></td><td>string (required)</td><td>—</td><td>Accessible name</td></tr>\n<tr><td><code>description</code></td><td>string</td><td>—</td><td>Extended description</td></tr>\n<tr><td><code>series</code></td><td>array of series</td><td>[]</td><td>Each series is <code>{ name: string; points: { x: number; y: number }[] }</code></td></tr>\n<tr><td><code>dataTable</code></td><td>slot</td><td>—</td><td>Optional fallback <code>&lt;table&gt;</code></td></tr>\n<tr><td><code>...restProps</code></td><td>HTML attributes</td><td>—</td><td>Spread onto the root <code>&lt;figure&gt;</code></td></tr>\n</tbody>\n</table>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;ScatterChart label=\"Height vs weight by age\" series={healthSeries} description=\"Height vs weight for participants, grouped by age cohort\" /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>No keyboard interactions on the rendered dots</li>\n<li>Data table (when rendered) follows native table keyboard behaviour</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"img\"</code> exposes the chart as a single image</li>\n<li><code>aria-label</code> and <code>aria-describedby</code> provide the accessible name and description</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Visualising the relationship between two continuous variables</li>\n<li>Identifying clusters or outliers</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Use <code>LineChart</code> when the x-axis represents time or another ordered dimension and a connected trend is meaningful</li>\n<li>Use <code>BarChart</code> or <code>ColumnChart</code> for categorical comparisons</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component renders semantic HTML with appropriate ARIA wiring. The consumer provides all visual styling — no CSS, animations, or layout assumptions are baked in.</p>\n\n<h2>Styles</h2>\n\n<p>The component renders with <code>.scatter-chart</code> as the root class. No default styles are included.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>line-chart</code> / <code>area-chart</code> — continuous-data alternatives</li>\n<li><code>bar-chart</code> / <code>column-chart</code> — categorical alternatives</li>\n<li><code>graphic-block</code> — chart wrapper with title/notes</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/HTML/Element/figure\">MDN figure element</a></li>\n</ul>";
</script>

<svelte:head>
  <title>ScatterChart — Lily Design System</title>
  <meta name="description" content="A two-variable visualization where each observation is plotted as a single dot at its (x, y) coordinate, used to look at the relationship between two measurements — correlation, clustering, outlier..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
