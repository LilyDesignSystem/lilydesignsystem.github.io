<script lang="ts">
  const html: string = "<h1>Net Promoter Score Picker Button</h1>\n\n<p>A Net Promoter Score picker button is an individual button within a NetPromoterScorePicker that represents one score value on a 0-10 scale. When clicked, it selects that NPS score. The component uses <code>aria-pressed</code> to indicate the currently selected state, making it accessible to screen readers and keyboard users. NPS scores are categorized as Detractors (0-6), Passives (7-8), and Promoters (9-10).</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as a <code>&lt;button&gt;</code> element with <code>aria-pressed</code> for toggle state</li>\n<li>Designed to be used inside a NetPromoterScorePicker component</li>\n<li>Each button represents one score value from 0 to 10</li>\n<li>The <code>selected</code> prop controls <code>aria-pressed</code> to indicate the active selection</li>\n<li>The <code>label</code> prop provides the accessible name via <code>aria-label</code></li>\n<li>Spreads <code>...restProps</code> onto the <code>&lt;button&gt;</code> element for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>value</code>: string (required) -- the NPS score value this button represents (e.g., \"0\" through \"10\")</li>\n<li><code>selected</code>: boolean (default: false) -- whether this button is currently selected</li>\n<li><code>label</code>: string (required) -- accessible label for the button (e.g., \"0 - Not at all likely\", \"10 - Extremely likely\")</li>\n<li><code>onclick</code>: callback -- handler called when the button is clicked</li>\n<li><code>...restProps</code>: any -- additional HTML attributes spread onto the <code>&lt;button&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;NetPromoterScorePicker label=\"How likely are you to recommend us?\"&gt;\n    &lt;NetPromoterScorePickerButton value=\"0\" label=\"0 - Not at all likely\" onclick={handleClick} /&gt;\n    &lt;NetPromoterScorePickerButton value=\"1\" label=\"1\" onclick={handleClick} /&gt;\n    &lt;NetPromoterScorePickerButton value=\"2\" label=\"2\" onclick={handleClick} /&gt;\n    &lt;NetPromoterScorePickerButton value=\"3\" label=\"3\" onclick={handleClick} /&gt;\n    &lt;NetPromoterScorePickerButton value=\"4\" label=\"4\" onclick={handleClick} /&gt;\n    &lt;NetPromoterScorePickerButton value=\"5\" label=\"5\" onclick={handleClick} /&gt;\n    &lt;NetPromoterScorePickerButton value=\"6\" label=\"6\" onclick={handleClick} /&gt;\n    &lt;NetPromoterScorePickerButton value=\"7\" label=\"7\" onclick={handleClick} /&gt;\n    &lt;NetPromoterScorePickerButton value=\"8\" label=\"8\" selected onclick={handleClick} /&gt;\n    &lt;NetPromoterScorePickerButton value=\"9\" label=\"9\" onclick={handleClick} /&gt;\n    &lt;NetPromoterScorePickerButton value=\"10\" label=\"10 - Extremely likely\" onclick={handleClick} /&gt;\n&lt;/NetPromoterScorePicker&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus the button</li>\n<li>Enter / Space: Activate the button to select this score</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-pressed</code> -- indicates whether this button is the currently selected score (<code>true</code> when selected, <code>false</code> otherwise)</li>\n<li><code>aria-label</code> -- provides the accessible name for the button (e.g., \"8\")</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside NetPromoterScorePicker to represent one value on the 0-10 NPS scale.</li>\n<li>Use when each score option needs <code>aria-pressed</code> toggle semantics and an accessible label.</li>\n<li>Use to provide custom styling for Detractor (0-6), Passive (7-8), and Promoter (9-10) ranges.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside of a NetPromoterScorePicker -- it requires the parent picker for group semantics.</li>\n<li>Do not use as a standalone toggle button -- use ToggleButton instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component renders a <code>&lt;button&gt;</code> with <code>aria-pressed</code> for toggle state and <code>aria-label</code> for accessible naming. The consumer provides all visual styling, including button size, color coding for Detractor/Passive/Promoter ranges, selected-state appearance, and hover effects.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.net-promoter-score-picker-button</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;button&gt;</code> element with class <code>net-promoter-score-picker-button</code></li>\n<li>Verify aria-label` -- provides the accessible name for the button (e.g., \"8\")</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Size buttons consistently so all 11 values (0-10) fit in a row, and use distinct visual treatment for the selected state.</li>\n<li><strong>Developers</strong>: Set descriptive <code>label</code> values for endpoint buttons (e.g., \"0 - Not at all likely\", \"10 - Extremely likely\") to give screen reader users context about the scale.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>NetPromoterScorePickerButton is a child component of NetPromoterScorePicker. The picker provides the container with radiogroup semantics, while each button represents one score value with toggle state.</p>\n\n<pre><code>NetPromoterScorePicker → NetPromoterScorePickerButton (value=\"0\" through \"10\")</code></pre>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>net-promoter-score-picker</code> — a picker for selecting a 0-10 Net Promoter Score</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Button Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/button/</li>\n</ul>";
</script>

<svelte:head>
  <title>Net Promoter Score Picker Button — Lily Design System</title>
  <meta name="description" content="A Net Promoter Score picker button is an individual button within a NetPromoterScorePicker that represents one score value on a 0-10 scale. When clicked, it selects that NPS score. The component us..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
