<script lang="ts">
  const html: string = "<h1>Sticky Promo Banner</h1>\n\n<p>A sticky promo banner is a fixed-position <code>&lt;aside&gt;</code> that renders at the top or bottom edge of the viewport with a dismiss button. It uses the <code>complementary</code> landmark role so screen readers can navigate to and from it, and it uses inline <code>position: fixed</code> styling so consumers can adopt it without writing CSS.</p>\n\n<p>Use it for time-bound announcements, cookie/consent prompts that have been explicitly designed as promo banners, marketing campaigns, and feature launches.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses an <code>&lt;aside&gt;</code> element with <code>role=\"complementary\"</code> and <code>aria-label</code></li>\n<li><code>label</code> is <strong>required</strong> — the aria-label that names the landmark</li>\n<li><code>dismissLabel</code> is <strong>required</strong> — the aria-label for the dismiss button</li>\n<li><code>open</code> defaults to <code>true</code>; when <code>false</code>, the root element renders with the <code>hidden</code> attribute</li>\n<li><code>position</code> is exposed as <code>data-position</code> (\"top\" | \"bottom\", default \"bottom\") and applied via inline <code>style</code></li>\n<li>Inline <code>style=\"position: fixed; left: 0; right: 0; top: 0;\"</code> (top variant) or <code>style=\"position: fixed; left: 0; right: 0; bottom: 0;\"</code> (bottom variant) — this is the only documented inline style in the design system besides what each framework needs</li>\n<li>The dismiss button renders only when <code>onDismiss</code>/<code>ondismiss</code> is provided</li>\n<li>All visible text comes from <code>children</code> for i18n flexibility</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (<strong>required</strong>) -- aria-label for the aside region</li>\n<li><code>open</code>: boolean (default: true) -- whether the banner is visible</li>\n<li><code>position</code>: \"top\" | \"bottom\" (default: \"bottom\") -- sticky edge</li>\n<li><code>dismissLabel</code>: string (<strong>required</strong>) -- aria-label for the dismiss button</li>\n<li><code>onDismiss</code> / <code>ondismiss</code>: callback (optional) -- dismiss click handler</li>\n<li><code>children</code>: slot -- banner content</li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Bottom banner with dismiss handler:</p>\n\n<pre><code class=\"language-html\">&lt;StickyPromoBanner\n  label=\"Beta program promotion\"\n  dismissLabel=\"Dismiss banner\"\n  ondismiss={handleDismiss}\n&gt;\n  &lt;p&gt;Try our new beta features. &lt;a href=\"/beta\"&gt;Learn more&lt;/a&gt;.&lt;/p&gt;\n&lt;/StickyPromoBanner&gt;</code></pre>\n\n<p>Top banner, hidden until <code>open</code> becomes <code>true</code>:</p>\n\n<pre><code class=\"language-html\">&lt;StickyPromoBanner\n  label=\"Maintenance window\"\n  position=\"top\"\n  open={isOpen}\n  dismissLabel=\"Dismiss\"\n  ondismiss={() =&gt; setIsOpen(false)}\n&gt;\n  &lt;p&gt;Scheduled maintenance Saturday 22:00–23:00 UTC.&lt;/p&gt;\n&lt;/StickyPromoBanner&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Move focus into the banner content and the dismiss button</li>\n<li>Enter / Space on the dismiss button: Dismiss the banner (calls handler)</li>\n<li>Escape: Not handled by default — consumers may add an Escape handler if appropriate</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>&lt;aside role=\"complementary\" aria-label={label}&gt;</code> for the landmark name</li>\n<li><code>data-position</code> reflects \"top\" | \"bottom\"</li>\n<li>Dismiss button: <code>&lt;button type=\"button\" aria-label={dismissLabel}&gt;</code></li>\n<li>The root element uses the native <code>hidden</code> attribute when <code>open</code> is <code>false</code></li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for promotional announcements, beta invites, feature launches, and other time-bound messages.</li>\n<li>Use when the message is supplementary and dismissable (not safety-critical).</li>\n<li>Use at the top of the viewport for high-visibility announcements; at the bottom for less intrusive promos.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for safety-critical or blocking messages — use AlertDialog or Banner instead.</li>\n<li>Do not use for cookie consent unless your design explicitly treats consent as a promo (most regulators expect a more prominent surface).</li>\n<li>Do not use for navigation; use Header or NavigationMenu.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>Renders an <code>&lt;aside&gt;</code> with the complementary landmark, an inline-styled fixed position, optional dismiss button, and any provided children. The fixed-position inline styles are the only styling the component applies; visual treatment, padding, animation, and z-index are the consumer's responsibility.</p>\n\n<h2>Styles</h2>\n\n<p>Consumer CSS targets the <code>sticky-promo-banner</code> class. Use the <code>data-position=\"top\"</code> and <code>data-position=\"bottom\"</code> attribute selectors to adjust appearance per edge. Style the inner <code>sticky-promo-banner-content</code> and <code>sticky-promo-banner-dismiss</code> classes for layout and the dismiss icon.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders an <code>&lt;aside&gt;</code> element with class <code>sticky-promo-banner</code> and <code>role=\"complementary\"</code></li>\n<li>Verify <code>aria-label</code> is present and equals the <code>label</code> prop</li>\n<li>Verify <code>data-position</code> reflects the <code>position</code> prop (default \"bottom\")</li>\n<li>Verify inline <code>style</code> includes <code>position: fixed; left: 0; right: 0;</code> plus <code>top: 0</code> (top) or <code>bottom: 0</code> (bottom)</li>\n<li>Verify the <code>hidden</code> attribute is applied when <code>open</code> is <code>false</code></li>\n<li>Verify the dismiss <code>&lt;button&gt;</code> renders only when <code>onDismiss</code>/<code>ondismiss</code> is provided</li>\n<li>Verify the dismiss button has <code>type=\"button\"</code> and <code>aria-label</code> equal to the <code>dismissLabel</code> prop</li>\n<li>Verify the dismiss handler fires on click and on Enter/Space</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Keep the message short — one sentence with one link. Reserve top placement for higher-impact announcements; default to bottom for promos.</li>\n<li><strong>Developers</strong>: Always supply <code>label</code> and <code>dismissLabel</code>. Persist the user's dismissal in storage so the banner does not reappear on every page load.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>announcement-banner</code> — a banner highlighting important messages for all users</li>\n<li><code>banner</code> — a prominent message bar across the top of a page</li>\n<li><code>banner-box</code> — a banner box that is inside a banner component, using flexbox horizontal</li>\n<li><code>government-banner</code> — a banner identifying a website as belonging to a government, with an expandable details panel</li>\n<li><code>medical-banner</code> — a prominent message bar across the top of a page, with medical information</li>\n<li><code>medical-banner-box</code> — a medical banner box that is inside a medical-banner component, using flexbox horizontal, with medical information</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA <code>complementary</code> role: https://www.w3.org/TR/wai-aria-1.2/#complementary</li>\n<li>MDN <code>&lt;aside&gt;</code>: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/aside</li>\n<li>MDN <code>position: fixed</code>: https://developer.mozilla.org/en-US/docs/Web/CSS/position#fixed_positioning</li>\n<li>Mozilla Protocol Design System: https://protocol.mozilla.org/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"sticky-promo-banner\" aria-label=\"a fixed-position promotional banner with a dismiss button\">StickyPromoBanner</div>";
  const svelteSource: string = "// In your Svelte component:\nimport StickyPromoBanner from \"lily-design-system-svelte-headless/components/StickyPromoBanner/StickyPromoBanner.svelte\";\n\n<StickyPromoBanner>\n  <!-- content -->\n</StickyPromoBanner>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Sticky Promo Banner — Lily Design System</title>
  <meta name="description" content="A sticky promo banner is a fixed-position &lt;aside&gt; that renders at the top or bottom edge of the viewport with a dismiss button. It uses the complementary landmark role so screen readers can navigat..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

