<script lang="ts">
  const html: string = "<h1>Segment Group Item</h1>\n\n<p>SegmentGroupItem is a single segment option within a SegmentGroup. It renders as a <code>&lt;button&gt;</code> with <code>role=\"radio\"</code> and <code>aria-checked</code>, following the WAI-ARIA radio group pattern. Each item represents one mutually exclusive option in the segmented control.</p>\n\n<p>Use SegmentGroupItem inside a SegmentGroup to build custom segmented controls such as view mode toggles (grid/list), filter selections, or display density controls.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;button&gt;</code> element with <code>role=\"radio\"</code> for ARIA semantics</li>\n<li><code>aria-checked</code> reflects the <code>checked</code> prop to indicate selection state</li>\n<li>Uses roving tabindex: <code>tabindex=\"0\"</code> when checked, <code>tabindex=\"-1\"</code> otherwise</li>\n<li><code>data-value</code> attribute exposes the segment value for CSS styling or JS queries</li>\n<li>Supports <code>disabled</code> prop to prevent interaction</li>\n<li>Spreads <code>restProps</code> onto the button for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>checked</code>: boolean (default <code>false</code>) -- whether this segment is currently selected</li>\n<li><code>value</code>: string (default <code>\"\"</code>) -- value associated with this segment, exposed as <code>data-value</code></li>\n<li><code>disabled</code>: boolean (default <code>false</code>) -- whether the segment is disabled</li>\n<li><code>children</code>: slot (required) -- label content to render inside the button</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;button&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>View mode toggle showing Grid and List options:</p>\n\n<pre><code class=\"language-html\">&lt;SegmentGroup label=\"View mode\"&gt;\n  &lt;SegmentGroupItem checked={mode === 'grid'} value=\"grid\" onclick={() =&gt; mode = 'grid'}&gt;\n    Grid\n  &lt;/SegmentGroupItem&gt;\n  &lt;SegmentGroupItem checked={mode === 'list'} value=\"list\" onclick={() =&gt; mode = 'list'}&gt;\n    List\n  &lt;/SegmentGroupItem&gt;\n&lt;/SegmentGroup&gt;</code></pre>\n\n<p>Display density selector with a disabled option:</p>\n\n<pre><code class=\"language-html\">&lt;SegmentGroup label=\"Display density\"&gt;\n  &lt;SegmentGroupItem checked={density === 'compact'} value=\"compact\" onclick={() =&gt; density = 'compact'}&gt;\n    Compact\n  &lt;/SegmentGroupItem&gt;\n  &lt;SegmentGroupItem checked={density === 'normal'} value=\"normal\" onclick={() =&gt; density = 'normal'}&gt;\n    Normal\n  &lt;/SegmentGroupItem&gt;\n  &lt;SegmentGroupItem checked={false} value=\"spacious\" disabled&gt;\n    Spacious\n  &lt;/SegmentGroupItem&gt;\n&lt;/SegmentGroup&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus moves to the checked segment (or first segment if none checked)</li>\n<li>Arrow Left / Arrow Right: Consumer should implement arrow key navigation on the parent SegmentGroup</li>\n<li>Space / Enter: Activates the focused segment (native button behavior)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"radio\"</code> -- identifies this button as a radio-like option within a radiogroup</li>\n<li><code>aria-checked</code> -- indicates whether this segment is selected (<code>true</code>) or not (<code>false</code>)</li>\n<li>Roving tabindex -- only the checked item has <code>tabindex=\"0\"</code>, others have <code>tabindex=\"-1\"</code></li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use SegmentGroupItem inside a SegmentGroup to represent one selectable segment option.</li>\n<li>Use SegmentGroupItem for custom toggle button UIs such as view mode switches, filter options, or density controls.</li>\n<li>Use SegmentGroupItem when each option needs a value attribute for programmatic identification.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use SegmentGroupItem outside of a SegmentGroup -- it requires the parent radiogroup context for proper ARIA semantics.</li>\n<li>Do not use SegmentGroupItem for standalone toggle buttons -- use ToggleButton instead.</li>\n<li>Do not use SegmentGroupItem for standard radio inputs in a form -- use RadioInput instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>The SegmentGroupItem headless component provides a <code>&lt;button&gt;</code> with <code>role=\"radio\"</code>, <code>aria-checked</code>, roving <code>tabindex</code>, and <code>data-value</code> for CSS targeting. The consumer provides all visual styling for the segment button, including selected and disabled states.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.segment-group-item</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>segment-group-item</code></li>\n<li>Verify role=\"radio\"` -- identifies this button as a radio-like option within a radiogroup</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Make the selected segment visually distinct with a filled background or bold text, and ensure disabled segments are visually muted.</li>\n<li><strong>Developers</strong>: Use the <code>checked</code> prop to control <code>aria-checked</code> and tabindex; ensure only one item in the group has <code>checked={true}</code> at a time.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>segment-group</code> — a group of mutually exclusive segment options</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Radio Group Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/radio/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<button class=\"segment-group-item\" role=\"radio\" aria-checked=\"true\">Grid</button>";
  const svelteSource: string = "// In your Svelte component:\nimport SegmentGroupItem from \"lily-design-system-svelte-headless/components/SegmentGroupItem/SegmentGroupItem.svelte\";\n\n<SegmentGroupItem>\n  <!-- content -->\n</SegmentGroupItem>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Segment Group Item — Lily Design System</title>
  <meta name="description" content="SegmentGroupItem is a single segment option within a SegmentGroup. It renders as a &lt;button&gt; with role=&quot;radio&quot; and aria-checked, following the WAI-ARIA radio group pattern. Each item represents one ..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

