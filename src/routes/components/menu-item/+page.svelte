<script lang="ts">
  const html: string = "<h1>Menu Item</h1>\n\n<p>A menu item is a UI/UX component representing a single action or option within a menu, dropdown menu, or navigation menu. Menus are used throughout applications to organize and present actions, settings, and navigation options in a structured, discoverable format.</p>\n\n<p>This component renders as a <code>&lt;div&gt;</code> with <code>role=\"menuitem\"</code> and <code>tabindex=\"-1\"</code>, following the WAI-ARIA menu pattern. The <code>tabindex=\"-1\"</code> removes the item from the normal tab order so that the parent menu can manage focus programmatically using Arrow keys. The consumer provides the menu item content (action label, keyboard shortcut hint, etc.) through the children slot. Menu items should be placed within a container with <code>role=\"menu\"</code> or <code>role=\"menubar\"</code>. Items that open submenus can use <code>aria-haspopup</code> and <code>aria-expanded</code> attributes.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as <code>&lt;div role=\"menuitem\" tabindex=\"-1\"&gt;</code> for menu items</li>\n<li><code>tabindex=\"-1\"</code> supports roving focus managed by the parent menu</li>\n<li>Should be placed inside a <code>role=\"menu\"</code> or <code>role=\"menubar\"</code> container</li>\n<li>Focus management (ArrowUp/ArrowDown) is handled by the parent menu component</li>\n<li>All content provided through children slot; no hardcoded strings</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>children</code>: slot -- menu item content</li>\n<li><code>...restProps</code>: Any additional HTML attributes spread onto the <code>&lt;div&gt;</code></li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Menu items inside a Menu component with click handlers:</p>\n\n<pre><code class=\"language-html\">&lt;Menu label=\"File\"&gt;\n    &lt;MenuItem onclick={() =&gt; handleNew()}&gt;New File&lt;/MenuItem&gt;\n    &lt;MenuItem onclick={() =&gt; handleOpen()}&gt;Open File&lt;/MenuItem&gt;\n    &lt;MenuItem onclick={() =&gt; handleSave()}&gt;Save&lt;/MenuItem&gt;\n&lt;/Menu&gt;</code></pre>\n\n<p>Disabled menu item for an unavailable action:</p>\n\n<pre><code class=\"language-html\">&lt;MenuItem aria-disabled=\"true\"&gt;Paste&lt;/MenuItem&gt;</code></pre>\n\n<p>Submenu trigger with expansion state:</p>\n\n<pre><code class=\"language-html\">&lt;MenuItem aria-haspopup=\"true\" aria-expanded={submenuOpen}&gt;Recent Files&lt;/MenuItem&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Focus is managed by the parent menu using ArrowUp/ArrowDown</li>\n<li>Enter or Space activates the menu item (handled by consumer event listeners)</li>\n<li>Home/End move focus to the first/last item (handled by the parent menu)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"menuitem\"</code> identifies this as a menu item</li>\n<li><code>tabindex=\"-1\"</code> for roving focus management</li>\n<li>Supports <code>aria-disabled</code> for disabled state</li>\n<li>Supports <code>aria-haspopup</code> for submenu triggers</li>\n<li>Supports <code>aria-expanded</code> for items with open/closed submenus</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside Menu to represent one actionable item.</li>\n<li>Use when each item needs <code>role=\"menuitem\"</code> semantics with roving tabindex focus management from the parent menu.</li>\n<li>Use for actions like Cut, Copy, Paste, Delete, or custom operations.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside Menu -- it relies on the parent for keyboard navigation.</li>\n<li>Do not use for navigation links -- use links within NavigationMenu instead.</li>\n<li>Do not use for context menu actions -- use ContextMenuItem instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;div&gt;</code> with <code>role=\"menuitem\"</code> and <code>tabindex=\"-1\"</code> for roving focus managed by the parent menu. The consumer provides all visual styling, including item appearance, hover states, disabled styling, keyboard shortcut hints, and icons.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.menu-item</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>menu-item</code></li>\n<li>Verify role=\"menuitem\"` identifies this as a menu item</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Show keyboard shortcut hints (e.g., \"Ctrl+S\") right-aligned within menu items when applicable, and visually distinguish disabled items.</li>\n<li><strong>Developers</strong>: Use <code>aria-disabled=\"true\"</code> instead of the <code>disabled</code> attribute for disabled menu items, and add <code>aria-haspopup</code> with <code>aria-expanded</code> for items that open submenus.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>MenuItem is a child component of Menu. The parent Menu provides <code>role=\"menu\"</code> with vertical keyboard navigation, while MenuItem provides <code>role=\"menuitem\"</code> for each action.</p>\n\n<pre><code>Menu (role=\"menu\") → MenuItem (role=\"menuitem\")</code></pre>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>menu</code> — a list of actions or options triggered by a button</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Menu Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/menu/</li>\n<li>WAI-ARIA menuitem role: https://www.w3.org/TR/wai-aria-1.2/#menuitem</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"menu-item\" role=\"menuitem\">Action</div>";
  const svelteSource: string = "// In your Svelte component:\nimport MenuItem from \"lily-design-system-svelte-headless/components/MenuItem/MenuItem.svelte\";\n\n<MenuItem>\n  <!-- content -->\n</MenuItem>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Menu Item — Lily Design System</title>
  <meta name="description" content="A menu item is a UI/UX component representing a single action or option within a menu, dropdown menu, or navigation menu. Menus are used throughout applications to organize and present actions, set..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

