<script lang="ts">
  const html: string = "<h1>Color Picker Button</h1>\n\n<p>A color picker swatch is a button representing a single predefined color choice within a color picker interface. It allows users to select a color from a set of predefined options by clicking on a visually representative swatch. The component communicates selected state through <code>aria-pressed</code> and exposes <code>data-color</code> for consumer CSS targeting.</p>\n\n<p>This component is useful in color palette UIs, theme builders, product customization forms, and any interface where users choose from a fixed set of color options. Each swatch is a fully accessible button that screen readers can announce by its label and selection state.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;button&gt;</code> element with <code>type=\"button\"</code> to prevent form submission</li>\n<li>Uses inline <code>style:background-color</code> to display the color visually</li>\n<li>Exposes <code>data-color</code> attribute for consumer CSS selectors (e.g., styling based on selected color)</li>\n<li>Uses <code>aria-pressed</code> to communicate toggle/selected state to assistive technology</li>\n<li>Supports <code>disabled</code> state for unavailable color options</li>\n<li>Spreads <code>restProps</code> for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>color</code>: string (required) -- CSS color value applied as background-color and exposed via <code>data-color</code></li>\n<li><code>label</code>: string (required) -- accessible name announced by screen readers (e.g., \"Red\", \"Ocean Blue\")</li>\n<li><code>selected</code>: boolean (default: <code>false</code>) -- whether the swatch is currently selected, reflected in <code>aria-pressed</code></li>\n<li><code>disabled</code>: boolean (default: <code>false</code>) -- whether the swatch is disabled</li>\n<li><code>onclick</code>: <code>(event: MouseEvent) =&gt; void</code> (default: <code>undefined</code>) -- click handler for selection</li>\n<li><code>...restProps</code>: any additional HTML attributes spread onto the button</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;ColorPicker color=\"#ff0000\" label=\"Red\" onclick={handleSelect} /&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;ColorPicker color=\"#0066cc\" label=\"Blue\" selected={true} onclick={handleSelect} /&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;ColorPicker color=\"#cccccc\" label=\"Gray\" disabled={true} /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li><strong>Tab</strong>: Moves focus to the swatch button</li>\n<li><strong>Enter / Space</strong>: Activates the button (native button behavior)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides an accessible name for the swatch since it has no visible text content</li>\n<li><code>aria-pressed={selected}</code> -- indicates whether the swatch is currently selected in a toggle context</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside ColorPicker to represent one selectable colour swatch.</li>\n<li>Use in product customisation forms, theme builders, or palette UIs where users choose from a fixed set of colours.</li>\n<li>Use when each colour option needs an accessible label and pressed/selected state.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside a colour picker context -- use SegmentGroupItem or RadioInput for non-colour selections.</li>\n<li>Do not use for free-form colour selection -- use ColorPicker for 2D saturation/brightness picking or ColorInput for hex entry.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless ColorPickerButton component provides a <code>&lt;button&gt;</code> with inline <code>style:background-color</code>, <code>aria-label</code> for the color name, <code>aria-pressed</code> for selected state, and <code>data-color</code> for CSS targeting. The consumer provides all visual styling including swatch size, border, selected-state ring or outline, hover effects, and layout within the palette.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.color-picker-button</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;button&gt;</code> element with class <code>color-picker-button</code></li>\n<li>Verify aria-label={label}` -- provides an accessible name for the swatch since it has no visible text content</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use a visible border or ring to indicate the selected swatch. Ensure swatches are large enough to distinguish similar colors and meet touch target guidelines.</li>\n<li><strong>Developers</strong>: Use the <code>data-color</code> attribute for CSS selectors and the <code>aria-pressed</code> state for selected styling. Group multiple ColorPickerButton components in a container for palette layout.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>ColorPickerButton is the child component in the ColorPicker/ColorPickerButton composition pattern. Multiple ColorPickerButton instances form a color palette. They can be combined with a ColorPicker for interfaces that offer both preset swatches and fine-grained 2D color selection.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>color-picker</code> — a two-dimensional board for picking colors by hue and saturation</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Button Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/button/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<button class=\"color-picker-button\" style=\"background:#ff0000\" aria-label=\"Red\"></button>";
  const svelteSource: string = "// In your Svelte component:\nimport ColorPickerButton from \"lily-design-system-svelte-headless/components/ColorPickerButton/ColorPickerButton.svelte\";\n\n<ColorPickerButton>ColorPickerButton</ColorPickerButton>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Color Picker Button — Lily Design System</title>
  <meta name="description" content="A color picker swatch is a button representing a single predefined color choice within a color picker interface. It allows users to select a color from a set of predefined options by clicking on a ..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

