<script lang="ts">
  const html: string = "<h1>Toast</h1>\n\n<p>A toast is a UI/UX component that provides a brief, transient notification message to the user. It uses <code>role=\"status\"</code> with <code>aria-live=\"polite\"</code> by default, or <code>role=\"alert\"</code> with <code>aria-live=\"assertive\"</code> for urgent messages. Toasts are typically displayed temporarily to confirm actions, report errors, or provide status updates without requiring user interaction.</p>\n\n<p>Toasts are similar to notifications but are generally more ephemeral and less intrusive. They are commonly used for success confirmations (\"Item saved\"), error messages (\"Connection lost\"), and informational updates (\"New message received\").</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> element with a live region role for screen reader announcements</li>\n<li>Uses conditional role assignment: <code>role=\"status\"</code> for normal messages, <code>role=\"alert\"</code> for urgent ones</li>\n<li>Uses conditional <code>aria-live</code>: <code>\"polite\"</code> waits for a pause in speech, <code>\"assertive\"</code> interrupts immediately</li>\n<li>The <code>urgent</code> prop controls both the role and aria-live behavior simultaneously</li>\n<li>Accepts a <code>children</code> slot for flexible toast content</li>\n<li>Supports rest props via <code>...restProps</code> for extensibility (e.g., <code>id</code>, <code>class</code>)</li>\n<li>slot pattern used for the <code>children</code> prop (imported from \"svelte\")</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (default: undefined) -- optional accessible label applied via <code>aria-label</code></li>\n<li><code>urgent</code>: boolean (default: false) -- when true, uses <code>role=\"alert\"</code> and <code>aria-live=\"assertive\"</code> instead of <code>role=\"status\"</code> and <code>aria-live=\"polite\"</code></li>\n<li><code>children</code>: slot (required) -- the toast message content</li>\n<li><code>...restProps</code>: spread onto the <code>&lt;div&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;Toast label=\"Success\"&gt;Patient record saved successfully.&lt;/Toast&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;Toast label=\"Error\" urgent&gt;Failed to save changes. Please try again.&lt;/Toast&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;Toast label=\"Copied\"&gt;\n  &lt;p&gt;NHS number copied to clipboard.&lt;/p&gt;\n&lt;/Toast&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;Sonner label=\"Notifications\"&gt;\n  &lt;Toast label=\"Update\"&gt;Record updated by Dr Chen at 14:32.&lt;/Toast&gt;\n&lt;/Sonner&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive notification container. Toasts are announced by screen readers through live regions and do not receive keyboard focus.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"status\"</code> -- (default) identifies a live region with advisory information, announced at the next graceful opportunity</li>\n<li><code>role=\"alert\"</code> -- (when <code>urgent</code> is true) identifies a live region with important, time-sensitive information, announced immediately</li>\n<li><code>aria-live=\"polite\"</code> -- (default) screen reader waits for a pause before announcing the content</li>\n<li><code>aria-live=\"assertive\"</code> -- (when <code>urgent</code> is true) screen reader interrupts current speech to announce the content</li>\n<li><code>aria-label={label}</code> -- optional descriptive label for the toast region</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for brief auto-dismissing notification messages such as \"Saved\", \"Copied\", or \"Record updated\".</li>\n<li>Use when the message does not require user interaction and can auto-dismiss after a short time.</li>\n<li>Use for success confirmations, informational updates, and non-critical error feedback.</li>\n<li>Use inside a Sonner container when managing multiple stacked toast notifications.</li>\n<li>Use the <code>urgent</code> prop sparingly, only for time-sensitive error messages.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for persistent messages -- use Banner instead.</li>\n<li>Do not use for form validation errors -- use ErrorSummary instead.</li>\n<li>Do not use for critical alerts requiring user acknowledgment -- use AlertDialog instead.</li>\n<li>Do not use for content that users need to reference later -- use Notification or InformationCallout instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;div&gt;</code> with a live region role (<code>role=\"status\"</code> or <code>role=\"alert\"</code>) and appropriate <code>aria-live</code> behavior for screen reader announcements. The consumer provides all content, visual styling, positioning, animation, and auto-dismiss logic.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.toast</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>toast</code></li>\n<li>Verify role=\"status\"` -- (default) identifies a live region with advisory information, announced at the next graceful opportunity</li>\n<li>Verify role=\"alert\"<code> -- (when </code>urgent` is true) identifies a live region with important, time-sensitive information, announced immediately</li>\n<li>Verify aria-label={label}` -- optional descriptive label for the toast region</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Position toasts consistently (e.g., bottom-right or top-center) and use color/icon cues to distinguish success, error, and informational messages.</li>\n<li><strong>Developers</strong>: Set <code>urgent={true}</code> only for error or time-sensitive messages. For normal confirmations, use the default polite behavior to avoid interrupting screen reader users.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>sonner</code> — a toast notification manager</li>\n<li><code>notification</code> — a brief message about an event or update</li>\n<li><code>alert</code> — a status message for important information or feedback</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Alert Role: https://www.w3.org/TR/wai-aria-1.2/#alert</li>\n<li>WAI-ARIA Status Role: https://www.w3.org/TR/wai-aria-1.2/#status</li>\n<li>WAI-ARIA Live Regions: https://www.w3.org/TR/wai-aria-1.2/#aria-live</li>\n</ul>";
</script>

<svelte:head>
  <title>Toast — Lily Design System</title>
  <meta name="description" content="A toast is a UI/UX component that provides a brief, transient notification message to the user. It uses role=&quot;status&quot; with aria-live=&quot;polite&quot; by default, or role=&quot;alert&quot; with aria-live=&quot;assertive&quot; ..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
