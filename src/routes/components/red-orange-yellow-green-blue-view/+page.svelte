<script lang="ts">
  const html: string = "<h1>Red Orange Yellow Green Blue View</h1>\n\n<p>A ROYGB (Red/Orange/Yellow/Green/Blue) view is a component that displays a five-level color-coded status value as read-only text. It is used to present previously captured ROYGB status data in dashboards, reports, or detail views. Red indicates a critical problem, orange signals a hazard, yellow means caution, green indicates normal status, and blue represents a paused or informational state.</p>\n\n<p>The component renders as a <code>&lt;span&gt;</code> element with an accessible label, making the status readable by screen readers and other assistive technologies. This headless component provides the semantic structure and accessibility features while allowing consumers to apply their own visual styling. The five-level scale offers more granularity than the traditional three-level RAG (Red/Amber/Green) status pattern.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as <code>&lt;span aria-label={label}&gt;</code> displaying a ROYGB status value as text content</li>\n<li>Five-level status scale: red, orange, yellow, green, blue</li>\n<li>Read-only display component, not interactive</li>\n<li>Value defaults to an empty string when not provided</li>\n<li>Uses props rune for prop destructuring</li>\n<li>Spreads <code>...restProps</code> on the <code>&lt;span&gt;</code> element for consumer extensibility</li>\n<li>No hardcoded user-facing strings; label and value are fully configurable</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible label for screen readers via <code>aria-label</code></li>\n<li><code>value</code>: string (default: <code>\"\"</code>) -- the ROYGB status to display (e.g., \"red\", \"orange\", \"yellow\", \"green\", \"blue\")</li>\n<li><code>...restProps</code>: any additional HTML attributes spread onto the <code>&lt;span&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Display a five-level status in a report:</p>\n\n<pre><code class=\"language-html\">&lt;RedOrangeYellowGreenBlueView label=\"Risk level\" value=\"yellow\" /&gt;</code></pre>\n\n<p>Display dynamic status in a summary list:</p>\n\n<pre><code class=\"language-html\">&lt;SummaryList&gt;\n  &lt;SummaryListItem&gt;\n    &lt;dt&gt;Alert status&lt;/dt&gt;\n    &lt;dd&gt;&lt;RedOrangeYellowGreenBlueView label=\"Alert status\" value={level} /&gt;&lt;/dd&gt;\n  &lt;/SummaryListItem&gt;\n&lt;/SummaryList&gt;</code></pre>\n\n<p>With data attribute for CSS colour coding:</p>\n\n<pre><code class=\"language-html\">&lt;RedOrangeYellowGreenBlueView label=\"Patient triage\" value=\"orange\" data-value=\"orange\" /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this is a passive informational display, not an interactive element.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides the accessible name for the status display, allowing screen readers to announce the context of the value</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to display a five-level colour status in read-only format in dashboards and reports.</li>\n<li>Use in summary cards and detail views where the ROYGB status has already been captured.</li>\n<li>Use when presenting historical or locked status data that should not be edited.</li>\n<li>Use in tables and lists to show five-level status alongside other data fields.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when users need to select or change the status -- use <a href=\"../red-orange-yellow-green-blue-picker/\">RedOrangeYellowGreenBluePicker</a> instead.</li>\n<li>Do not use for three-level status display -- use <a href=\"../red-amber-green-view/\">RedAmberGreenView</a> instead.</li>\n<li>Do not use for general-purpose text display -- use a <code>&lt;span&gt;</code> or <a href=\"../badge/\">Badge</a> instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>The RedOrangeYellowGreenBlueView headless component provides a <code>&lt;span&gt;</code> with <code>aria-label</code> for accessible naming and displays the ROYGB status value as text content. The consumer provides all visual styling, including color-coded backgrounds, text colors, or icons.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.red-orange-yellow-green-blue-view</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>red-orange-yellow-green-blue-view</code></li>\n<li>Verify aria-label={label}` -- provides the accessible name for the status display, allowing screen readers to announce the context of the value</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use both color and text to communicate the status level, ensuring the display is understandable for users with color vision deficiencies.</li>\n<li><strong>Developers</strong>: Apply dynamic CSS classes or inline styles based on the <code>value</code> prop to color-code the display element appropriately.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>red-orange-yellow-green-blue-picker</code> — a picker for selecting a five-level color status</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA status role: https://www.w3.org/TR/wai-aria-1.2/#status</li>\n</ul>";
</script>

<svelte:head>
  <title>Red Orange Yellow Green Blue View — Lily Design System</title>
  <meta name="description" content="A ROYGB (Red/Orange/Yellow/Green/Blue) view is a component that displays a five-level color-coded status value as read-only text. It is used to present previously captured ROYGB status data in dash..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
