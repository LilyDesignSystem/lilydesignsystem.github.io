<script lang="ts">
  const html: string = "<h1>Net Promoter Score View</h1>\n\n<p>A Net Promoter Score (NPS) view is a display component that presents an NPS score value as read-only text. It is used to show previously captured NPS data in dashboards, reports, or detail views. NPS scores range from 0 to 10, with respondents classified as Detractors (0-6), Passives (7-8), or Promoters (9-10).</p>\n\n<p>This headless component renders as a <code>&lt;span&gt;</code> element with an accessible label, providing the semantic structure and accessibility features while allowing consumers to apply their own visual styling, such as color-coding by score range.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as a <code>&lt;span&gt;</code> element with <code>aria-label</code> for accessible naming</li>\n<li>Displays the NPS score value as inline text content</li>\n<li>Read-only display component, not interactive</li>\n<li>Uses props for prop destructuring with rest props spread</li>\n<li>Value defaults to empty string when not provided</li>\n<li>No score validation or classification logic; consumers handle categorization and styling</li>\n<li>No hardcoded strings; all text comes through props for internationalization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible label for screen readers via <code>aria-label</code></li>\n<li><code>value</code>: string (default: <code>\"\"</code>) -- the NPS score to display</li>\n<li><code>...restProps</code>: any -- additional HTML attributes spread onto the <code>&lt;span&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;NetPromoterScoreView label=\"NPS Score\" value=\"8\" /&gt;</code></pre>\n\n<p>In an analytics dashboard:</p>\n\n<pre><code class=\"language-html\">&lt;Card&gt;\n    &lt;h3&gt;Customer Loyalty&lt;/h3&gt;\n    &lt;NetPromoterScoreView label=\"Latest NPS score\" value=\"9\" /&gt;\n    &lt;Badge&gt;Promoter&lt;/Badge&gt;\n&lt;/Card&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>None -- this is a passive informational display, not interactive</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label</code> -- provides the accessible name from the <code>label</code> prop so screen readers can announce the context of the displayed score</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to display a previously captured NPS score in dashboards, reports, or detail views.</li>\n<li>Use when the score should be visible but not editable by the user.</li>\n<li>Use in analytics dashboards to show individual respondent scores with color-coded Detractor/Passive/Promoter styling.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when the user needs to select or change the score -- use NetPromoterScorePicker instead.</li>\n<li>Do not use for star-based display -- use FiveStarRatingView instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component renders a <code>&lt;span&gt;</code> with the NPS score as text content and <code>aria-label</code> for accessible context. The consumer provides all visual styling, including typography, color coding by score range (Detractor, Passive, Promoter), and layout.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.net-promoter-score-view</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>net-promoter-score-view</code></li>\n<li>Verify aria-label<code> -- provides the accessible name from the </code>label` prop so screen readers can announce the context of the displayed score</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Color-code displayed scores by range (e.g., red for Detractors 0-6, yellow for Passives 7-8, green for Promoters 9-10) and display the category label alongside the number.</li>\n<li><strong>Developers</strong>: Provide a descriptive <code>label</code> prop (e.g., \"Customer NPS score\") so screen reader users understand the context of the displayed number.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>net-promoter-score-picker</code> — a picker for selecting a 0-10 Net Promoter Score</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>Net Promoter Score: https://en.wikipedia.org/wiki/Net_promoter_score</li>\n</ul>";
</script>

<svelte:head>
  <title>Net Promoter Score View — Lily Design System</title>
  <meta name="description" content="A Net Promoter Score (NPS) view is a display component that presents an NPS score value as read-only text. It is used to show previously captured NPS data in dashboards, reports, or detail views. N..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
