<script lang="ts">
  const html: string = "<h1>Five Star Rating Picker</h1>\n\n<p>A five-star rating input allows users to select a rating from 1 to 5 stars using radio buttons. This is one of the most common rating patterns on the web, used for product reviews, content ratings, and feedback forms. Each radio button is labeled with \"N star\" or \"N stars\" for clear screen reader communication. The component uses radio buttons grouped in a fieldset for proper accessibility and keyboard navigation.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;fieldset&gt;</code> with <code>role=\"radiogroup\"</code> containing 5 <code>&lt;label&gt;</code>/<code>&lt;input type=\"radio\"&gt;</code> pairs</li>\n<li>Supports two-way binding for the selected rating value (1-5, with 0 meaning no selection)</li>\n<li>Each radio label reads \"1 star\", \"2 stars\", \"3 stars\", etc. for screen reader clarity</li>\n<li>The <code>name</code> prop groups the radio buttons for form submission</li>\n<li>Supports <code>disabled</code> state on the fieldset level</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name for the rating group via <code>aria-label</code></li>\n<li><code>value</code>: number (default: <code>0</code>) -- current rating (1-5, 0 = none), two-way bindable via <code>value</code></li>\n<li><code>name</code>: string (default: <code>\"rating\"</code>) -- radio group name for form submission</li>\n<li><code>disabled</code>: boolean (default: <code>false</code>) -- whether the entire group is disabled</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;fieldset&gt;</code></li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;FiveStarRatingPicker label=\"Rate this product\" value={rating}&gt;\n    &lt;FiveStarRatingPickerButton value=\"1\" label=\"1 star\" onclick={handleClick} /&gt;\n    &lt;FiveStarRatingPickerButton value=\"2\" label=\"2 stars\" onclick={handleClick} /&gt;\n    &lt;FiveStarRatingPickerButton value=\"3\" label=\"3 stars\" selected onclick={handleClick} /&gt;\n    &lt;FiveStarRatingPickerButton value=\"4\" label=\"4 stars\" onclick={handleClick} /&gt;\n    &lt;FiveStarRatingPickerButton value=\"5\" label=\"5 stars\" onclick={handleClick} /&gt;\n&lt;/FiveStarRatingPicker&gt;</code></pre>\n\n<p>In a review form:</p>\n\n<pre><code class=\"language-html\">&lt;Form label=\"Write a review\" onSubmit={handleSubmit}&gt;\n    &lt;Field label=\"Your rating\" required&gt;\n        &lt;FiveStarRatingPicker label=\"Your rating\" value={rating} name=\"rating\" /&gt;\n    &lt;/Field&gt;\n    &lt;Button type=\"submit\"&gt;Submit Review&lt;/Button&gt;\n&lt;/Form&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>Standard radio group keyboard behavior provided by the browser:</p>\n\n<ul>\n<li>Tab: focus the selected radio (or first if none selected)</li>\n<li>ArrowDown / ArrowRight: move to next radio button</li>\n<li>ArrowUp / ArrowLeft: move to previous radio button</li>\n<li>Space: select the focused radio button</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"radiogroup\"</code> -- on the <code>&lt;fieldset&gt;</code>, identifies the group as a set of radio buttons</li>\n<li><code>aria-label=\"...\"</code> -- provides an accessible name for the entire rating group</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for product reviews, content ratings, or feedback forms where users select a 1-5 star rating.</li>\n<li>Use when you need a familiar, universally understood rating input pattern.</li>\n<li>Use in e-commerce product pages, app store listings, or restaurant review forms.</li>\n<li>Use when the rating will be aggregated and displayed as an average star score.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for display-only star ratings -- use FiveStarRatingView instead.</li>\n<li>Do not use for satisfaction surveys where face-based labels are more expressive -- use FiveFaceRatingPicker.</li>\n<li>Do not use for 0-10 scale surveys -- use NetPromoterScorePicker.</li>\n<li>Do not use for status indicators -- use RedAmberGreenPicker or RedOrangeYellowGreenBluePicker.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;fieldset&gt;</code> with <code>role=\"radiogroup\"</code>, individual radio buttons with proper labels, and keyboard navigation. The consumer provides all visual styling including star shapes, colors, hover effects, and sizing.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.five-star-rating-picker</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>five-star-rating-picker</code></li>\n<li>Verify role=\"radiogroup\"<code> -- on the </code>&lt;fieldset&gt;`, identifies the group as a set of radio buttons</li>\n<li>Verify aria-label=\"...\"` -- provides an accessible name for the entire rating group</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Provide clear visual distinction between filled and unfilled stars, and include a hover/focus state so users know which rating they are about to select.</li>\n<li><strong>Developers</strong>: Use two-way binding on <code>value</code> to track the selected rating. The <code>name</code> prop is important for form submission.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>FiveStarRatingPicker uses the Picker/PickerButton pattern. It contains FiveStarRatingPickerButton children, where each button represents one star level. The picker manages the group semantics while each button handles individual selection state via <code>aria-pressed</code>.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>five-star-rating-picker-button</code> — a picker button for selecting a 1-5 star rating using radio buttons</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Radio Group Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/radiobutton/</li>\n</ul>";
</script>

<svelte:head>
  <title>Five Star Rating Picker — Lily Design System</title>
  <meta name="description" content="A five-star rating input allows users to select a rating from 1 to 5 stars using radio buttons. This is one of the most common rating patterns on the web, used for product reviews, content ratings,..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
