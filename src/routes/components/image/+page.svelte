<script lang="ts">
  const html: string = "<h1>Image</h1>\n\n<p>An image component displays visual content such as photographs, illustrations, or diagrams within an interface. The Image component provides a headless wrapper that ensures proper accessibility through required alt text and optional caption support via the HTML <code>&lt;figure&gt;</code> and <code>&lt;figcaption&gt;</code> elements.</p>\n\n<p>When a caption is provided, the component renders a <code>&lt;figure&gt;</code> containing the <code>&lt;img&gt;</code> and a <code>&lt;figcaption&gt;</code>, giving the image semantic association with its description. Without a caption, it renders a plain <code>&lt;img&gt;</code> element. The component also supports native lazy loading for performance optimization.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a plain <code>&lt;img&gt;</code> element when no caption is provided</li>\n<li>Renders a <code>&lt;figure&gt;</code> with <code>&lt;img&gt;</code> and <code>&lt;figcaption&gt;</code> when a caption is provided</li>\n<li>The <code>alt</code> prop is required to enforce accessibility</li>\n<li>Rest props are spread onto the <code>&lt;img&gt;</code> element (no caption) or the <code>&lt;figure&gt;</code> element (with caption)</li>\n<li>Supports the native <code>loading</code> attribute for lazy/eager loading control</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>src</code>: string (required) -- the image source URL</li>\n<li><code>alt</code>: string (required) -- alternative text describing the image for screen readers and when the image fails to load</li>\n<li><code>caption</code>: string (optional) -- when provided, wraps the image in a <code>&lt;figure&gt;</code> with a <code>&lt;figcaption&gt;</code></li>\n<li><code>loading</code>: \"lazy\" | \"eager\" (optional) -- controls the native browser loading behavior</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the root element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;Image\n  src=\"/images/stretch-marks.jpg\"\n  alt=\"Close-up of a person's skin showing pink and silver stretch marks across the abdomen\"\n/&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;Figure&gt;\n  &lt;Image\n    src=\"/images/chickenpox-rash.jpg\"\n    alt=\"Small red spots on a child's chest and arms, typical of chickenpox\"\n  /&gt;\n  &lt;Caption&gt;Chickenpox starts with small, itchy spots. They can appear anywhere on the body.&lt;/Caption&gt;\n&lt;/Figure&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;!-- Lazy-loaded image below the fold --&gt;\n&lt;Image\n  src=\"/images/physiotherapy-exercise.jpg\"\n  alt=\"A person performing a seated hamstring stretch with one leg extended\"\n  loading=\"lazy\"\n/&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive content element. Images are not interactive by default.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>alt=\"...\"</code> -- provides the accessible name for the image, required for all non-decorative images</li>\n<li><code>&lt;figure&gt;</code> and <code>&lt;figcaption&gt;</code> -- when caption is present, these semantic HTML elements associate the caption with the image for assistive technology</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use when an image meets a genuine user need, such as helping users identify a health condition or understand a procedure</li>\n<li>Use with descriptive alt text that conveys the same information as the image</li>\n<li>Use with a caption (via Figure and Caption) to explain what users should conclude from the image</li>\n<li>Use when users with access needs like dyslexia benefit from visual content alongside text</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use decorative images that add no informational value -- they frustrate users, especially on transactional pages; use a CSS background image instead</li>\n<li>Do not use images containing embedded text -- screen readers cannot read text within images</li>\n<li>Do not arrange images side-by-side in a gallery layout -- stack vertically so users do not miss content</li>\n<li>Do not use when the information can be conveyed equally well through text alone</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides an <code>&lt;img&gt;</code> element with required <code>alt</code> text, optional <code>&lt;figure&gt;</code>/<code>&lt;figcaption&gt;</code> wrapping, and native <code>loading</code> attribute support. The consumer provides all visual styling including sizing, borders, rounded corners, and responsive behavior.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.image</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;img&gt;</code> element with class <code>image</code></li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Define consistent aspect ratios and sizing for images across the application. Provide placeholder or skeleton states for slow-loading images.</li>\n<li><strong>Developers</strong>: Always write meaningful <code>alt</code> text that describes the image content. Use <code>loading=\"lazy\"</code> for below-the-fold images to improve page performance.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>image-input</code> — a graphical submit button that displays an image &lt;input type=\"image\"&gt;</li>\n<li><code>image-file-input</code> — an input for selecting image files with preview</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI Images Tutorial: https://www.w3.org/WAI/tutorials/images/</li>\n<li>HTML figure element: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/figure</li>\n</ul>";
</script>

<svelte:head>
  <title>Image — Lily Design System</title>
  <meta name="description" content="An image component displays visual content such as photographs, illustrations, or diagrams within an interface. The Image component provides a headless wrapper that ensures proper accessibility thr..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
