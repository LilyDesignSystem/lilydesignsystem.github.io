<script lang="ts">
  const html: string = "<h1>Drawer</h1>\n\n<p>A drawer is a panel that slides in from an edge of the viewport, typically used for navigation menus, filters, or supplementary content. It overlays the main content and can be dismissed by the user. The component renders conditionally based on its <code>open</code> state and uses <code>role=\"dialog\"</code> with <code>aria-modal=\"true\"</code> for proper screen reader behavior.</p>\n\n<p>The <code>side</code> prop indicates which edge the drawer enters from via a <code>data-side</code> attribute, allowing consumers to apply appropriate positioning and animation styles. The drawer supports keyboard dismissal with the Escape key and provides a <code>tabindex=\"-1\"</code> to allow programmatic focus management.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Conditionally renders a <code>&lt;div&gt;</code> with <code>role=\"dialog\"</code> only when <code>open</code> is true</li>\n<li>Supports two-way binding on the <code>open</code> prop for two-way state binding</li>\n<li>Sets <code>aria-modal=\"true\"</code> to indicate the drawer is a modal dialog</li>\n<li>Exposes <code>data-side</code> attribute for consumer CSS positioning (left, right, top, bottom)</li>\n<li>Sets <code>tabindex=\"-1\"</code> to allow the drawer to receive focus programmatically</li>\n<li>Escape key handler sets <code>open</code> to <code>false</code></li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>open</code>: boolean (default: false) -- whether the drawer is visible, two-way bindable via <code>open</code></li>\n<li><code>label</code>: string (required) -- accessible name for the drawer</li>\n<li><code>side</code>: \"left\" | \"right\" | \"top\" | \"bottom\" (default: \"left\") -- which edge the drawer enters from</li>\n<li><code>children</code>: slot (required) -- drawer content</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Mobile navigation drawer sliding in from the left:</p>\n\n<pre><code class=\"language-html\">&lt;button onclick={() =&gt; (navOpen = true)}&gt;Menu&lt;/button&gt;\n&lt;Drawer label=\"Main navigation\" open={navOpen} side=\"left\"&gt;\n  &lt;nav&gt;\n    &lt;ul&gt;\n      &lt;li&gt;&lt;a href=\"/\"&gt;Home&lt;/a&gt;&lt;/li&gt;\n      &lt;li&gt;&lt;a href=\"/services\"&gt;Services&lt;/a&gt;&lt;/li&gt;\n      &lt;li&gt;&lt;a href=\"/contact\"&gt;Contact&lt;/a&gt;&lt;/li&gt;\n    &lt;/ul&gt;\n  &lt;/nav&gt;\n  &lt;button onclick={() =&gt; (navOpen = false)}&gt;Close&lt;/button&gt;\n&lt;/Drawer&gt;</code></pre>\n\n<p>Filter panel sliding in from the right:</p>\n\n<pre><code class=\"language-html\">&lt;Drawer label=\"Search filters\" open={showFilters} side=\"right\"&gt;\n  &lt;form&gt;\n    &lt;fieldset&gt;\n      &lt;legend&gt;Date range&lt;/legend&gt;\n      &lt;input type=\"date\" name=\"from\" /&gt;\n      &lt;input type=\"date\" name=\"to\" /&gt;\n    &lt;/fieldset&gt;\n    &lt;button type=\"submit\"&gt;Apply filters&lt;/button&gt;\n  &lt;/form&gt;\n&lt;/Drawer&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Escape: closes the drawer by setting <code>open</code> to <code>false</code></li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"dialog\"</code> -- identifies the drawer as a dialog for assistive technology</li>\n<li><code>aria-label={label}</code> -- provides an accessible name for the dialog</li>\n<li><code>aria-modal=\"true\"</code> -- indicates the drawer is modal, restricting interaction to its contents</li>\n<li><code>data-side={side}</code> -- data attribute for consumer CSS styling based on drawer position</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for a panel that slides in from the edge of the screen to show secondary content or navigation without leaving the page.</li>\n<li>Use for navigation menus, filters, or supplementary content that should overlay the main view temporarily.</li>\n<li>Use when the content is contextual and temporary, not a primary destination.</li>\n<li>Use for responsive layouts where side navigation collapses into a drawer on smaller screens.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for modal confirmations or urgent messages -- use Dialog or AlertDialog instead.</li>\n<li>Do not use for tooltip-style content -- use Popover instead.</li>\n<li>Do not use when the overlay should behave like a bottom sheet on mobile devices -- use Sheet instead.</li>\n<li>Do not use for persistent side navigation that should always be visible -- use Sidebar instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;div&gt;</code> with <code>role=\"dialog\"</code>, <code>aria-modal=\"true\"</code>, accessible labeling via <code>aria-label</code>, a <code>data-side</code> attribute for positioning, keyboard dismissal via Escape, and two-way binding on <code>open</code>. The consumer provides all visual styling, animations, backdrop, and positioning CSS.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.drawer</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>drawer</code></li>\n<li>Verify role=\"dialog\"` -- identifies the drawer as a dialog for assistive technology</li>\n<li>Verify aria-label={label}` -- provides an accessible name for the dialog</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Include a visible close button and consider adding a semi-transparent backdrop to indicate the main content is temporarily inaccessible. Animate the slide-in transition for a smooth experience.</li>\n<li><strong>Developers</strong>: Trap focus within the drawer when it is open and return focus to the trigger element on close. Use the <code>data-side</code> attribute to apply directional CSS transitions.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>sheet</code> — a panel that slides in from a screen edge as an overlay</li>\n<li><code>slide-out-drawer</code> — a drawer that slides out from the side of the page</li>\n<li><code>sidebar</code> — a side panel for navigation or supplementary content</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Dialog Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"drawer\" role=\"dialog\" aria-label=\"Drawer\"><p>Drawer content</p></div>";
  const svelteSource: string = "// In your Svelte component:\nimport Drawer from \"lily-design-system-svelte-headless/components/Drawer/Drawer.svelte\";\n\n<Drawer>\n  <!-- content -->\n</Drawer>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Drawer — Lily Design System</title>
  <meta name="description" content="A drawer is a panel that slides in from an edge of the viewport, typically used for navigation menus, filters, or supplementary content. It overlays the main content and can be dismissed by the use..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

