<script lang="ts">
  const html: string = "<h1>ScrollerBase</h1>\n\n<p>A low-level scroll position tracking primitive for scrollytelling. ScrollerBase uses IntersectionObserver to track which step is currently active and exposes bindable index and progress values that consumers use to drive visual changes.</p>\n\n<p>This headless component uses a <code>&lt;div&gt;</code> element with an <code>aria-label</code> to create a labeled scrollable region for scrollytelling content.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses <code>&lt;div&gt;</code> element with class <code>scroller-base</code></li>\n<li>Tracks scroll position using IntersectionObserver</li>\n<li>Exposes current step index and overall progress as bindable values</li>\n<li>Steps are child elements; each child becomes a scrollytelling step</li>\n<li>Consumer uses index/progress to drive visual changes</li>\n<li>Offset controls where in the viewport a step triggers (0=top, 0.5=center, 1=bottom)</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>children</code>: slot (required) -- scrollable content</li>\n<li><code>offset</code>: number (default: 0.5) -- viewport offset (0-1) where step triggers (0=top, 0.5=center, 1=bottom)</li>\n<li><code>index</code>: number (bindable, default: 0) -- current active step index</li>\n<li><code>progress</code>: number (bindable, default: 0) -- overall scroll progress (0-1)</li>\n<li><code>...restProps</code>: Any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;ScrollerBase offset={0.5} bind:index bind:progress&gt;\n  &lt;div class=\"step\"&gt;Step 1 content&lt;/div&gt;\n  &lt;div class=\"step\"&gt;Step 2 content&lt;/div&gt;\n  &lt;div class=\"step\"&gt;Step 3 content&lt;/div&gt;\n&lt;/ScrollerBase&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;ScrollerBase offset={0.3} bind:index&gt;\n  &lt;div class=\"step\"&gt;\n    &lt;h2&gt;Introduction&lt;/h2&gt;\n    &lt;p&gt;The story begins here.&lt;/p&gt;\n  &lt;/div&gt;\n  &lt;div class=\"step\"&gt;\n    &lt;h2&gt;Development&lt;/h2&gt;\n    &lt;p&gt;The plot thickens.&lt;/p&gt;\n  &lt;/div&gt;\n&lt;/ScrollerBase&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Standard scroll behavior</li>\n<li>Tab navigates between focusable step content</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label</code> for the scrollable region</li>\n<li>Steps should have appropriate roles as needed by the consumer</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use as the foundation for custom scrollytelling implementations</li>\n<li>Use when you need fine-grained control over scroll-driven interactions</li>\n<li>Use when building a custom scrollytelling layout that does not fit the Scroller foreground/background pattern</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use directly when you need the full scrollytelling experience with background/foreground composition -- use Scroller instead</li>\n<li>Do not use for simple scroll-triggered animations -- use IntersectionObserver directly or a simpler approach</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides a <code>&lt;div&gt;</code> element with IntersectionObserver-based scroll tracking, with zero visual styling. The consumer is responsible for all CSS including step layout, spacing, scroll snap, and any visual indicators of the current step.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.scroller-base</code> class for targeting. No default styles are included -- this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>scroller-base</code></li>\n<li>Verify <code>aria-label</code> is applied</li>\n<li>Verify child steps are rendered correctly</li>\n<li>Verify index updates as steps enter the viewport offset</li>\n<li>Verify progress updates as the user scrolls</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Plan step content with clear visual transitions in mind. Each step should represent a meaningful narrative beat. Consider how the viewport offset affects the reading experience.</li>\n<li><strong>Developers</strong>: Always provide a descriptive <code>aria-label</code> for the scrollable region. Use the bindable <code>index</code> and <code>progress</code> values to drive visual changes in sibling or parent components. Choose an appropriate <code>offset</code> value based on content type -- 0.5 (center) works well for most cases.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>scroller</code> — see components.tsv</li>\n<li><code>scroller-video</code> — see components.tsv</li>\n<li><code>visible</code> — see components.tsv</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>Reuters Graphics ScrollerBase</li>\n<li>IntersectionObserver API: https://developer.mozilla.org/en-US/docs/Web/API/IntersectionObserver</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"scroller-base\" aria-label=\"a low-level scroll position tracking primitive for scrollytelling\">ScrollerBase</div>";
  const svelteSource: string = "// In your Svelte component:\nimport ScrollerBase from \"lily-design-system-svelte-headless/components/ScrollerBase/ScrollerBase.svelte\";\n\n<ScrollerBase>\n  <!-- content -->\n</ScrollerBase>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>ScrollerBase — Lily Design System</title>
  <meta name="description" content="A low-level scroll position tracking primitive for scrollytelling. ScrollerBase uses IntersectionObserver to track which step is currently active and exposes bindable index and progress values that..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

