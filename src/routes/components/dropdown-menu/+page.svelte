<script lang="ts">
  const html: string = "<h1>Dropdown Menu</h1>\n\n<p>A dropdown menu triggered by a button that reveals a list of actions or options. This component combines a toggle button with a menu panel, providing a compact way to present multiple actions or choices without taking up permanent screen space. Common use cases include action menus, settings menus, context-sensitive options, and navigation.</p>\n\n<p>The component manages its own focus lifecycle: when the menu opens, focus moves automatically to the first menu item. Arrow key navigation cycles through items with wrapping, and Escape closes the menu. The trigger button communicates its state to assistive technology via <code>aria-haspopup</code> and <code>aria-expanded</code>.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a wrapper <code>&lt;div&gt;</code> containing a <code>&lt;button&gt;</code> trigger and a conditionally rendered menu <code>&lt;div&gt;</code></li>\n<li>Supports two-way binding on the <code>open</code> prop with the parent</li>\n<li>Uses reactive state to track the <code>menuRef</code> DOM reference for querying menu items</li>\n<li>Uses <code>$effect()</code> to auto-focus the first <code>[role=\"menuitem\"]</code> element when the menu opens</li>\n<li>Menu items are discovered dynamically via <code>querySelectorAll(\"[role='menuitem']\")</code></li>\n<li>ArrowDown/ArrowUp navigation wraps around from last to first and first to last</li>\n<li>Home and End keys jump to first and last menu items respectively</li>\n<li>Escape key closes the menu</li>\n<li>The trigger button displays the <code>label</code> text and uses it as <code>aria-label</code></li>\n<li>Spreads <code>restProps</code> onto the outer wrapper <code>&lt;div&gt;</code></li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name for the menu button, also displayed as button text</li>\n<li><code>open</code>: boolean (default: false) -- whether the menu is visible; bindable</li>\n<li><code>children</code>: slot (required) -- menu items, each should have <code>role=\"menuitem\"</code> and <code>tabindex=\"-1\"</code></li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>\"More actions\" dropdown on a card component:</p>\n\n<pre><code class=\"language-html\">&lt;DropdownMenu label=\"More actions\" open={menuOpen}&gt;\n    &lt;MenuItem onclick={() =&gt; handleEdit()}&gt;Edit&lt;/MenuItem&gt;\n    &lt;MenuItem onclick={() =&gt; handleDuplicate()}&gt;Duplicate&lt;/MenuItem&gt;\n    &lt;MenuItem onclick={() =&gt; handleDelete()}&gt;Delete&lt;/MenuItem&gt;\n&lt;/DropdownMenu&gt;</code></pre>\n\n<p>Settings dropdown with dynamic items:</p>\n\n<pre><code class=\"language-html\">&lt;DropdownMenu label=\"Settings\" open={settingsOpen}&gt;\n    {#each settingsOptions as option}\n        &lt;MenuItem onclick={() =&gt; handleOption(option)}&gt;\n            {option.label}\n        &lt;/MenuItem&gt;\n    {/each}\n&lt;/DropdownMenu&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Enter or Space (on button): toggles the menu open/closed</li>\n<li>ArrowDown: moves focus to the next menu item, wraps from last to first</li>\n<li>ArrowUp: moves focus to the previous menu item, wraps from first to last</li>\n<li>Home: moves focus to the first menu item</li>\n<li>End: moves focus to the last menu item</li>\n<li>Escape: closes the menu</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-haspopup=\"true\"</code> -- on the trigger button, indicates it opens a menu</li>\n<li><code>aria-expanded=\"{open}\"</code> -- on the trigger button, communicates whether the menu is currently shown</li>\n<li><code>aria-label=\"{label}\"</code> -- on both the trigger button and the menu, provides an accessible name</li>\n<li><code>role=\"menu\"</code> -- on the menu container, identifies it as a menu widget</li>\n<li><code>role=\"menuitem\"</code> -- expected on each child item provided by the consumer</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for a menu that opens below a trigger button, such as a \"More actions\" button.</li>\n<li>Use to present a list of actions or options when screen space is limited.</li>\n<li>Use for settings menus, action menus, or context-sensitive options on cards and rows.</li>\n<li>Use when the menu should appear on click rather than right-click.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for form selections -- use Select or Combobox instead.</li>\n<li>Do not use for navigation -- use NavigationMenu instead.</li>\n<li>Do not use when the menu should appear on right-click -- use ContextMenu instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a trigger <code>&lt;button&gt;</code> with <code>aria-haspopup</code> and <code>aria-expanded</code>, a <code>&lt;div&gt;</code> menu container with <code>role=\"menu\"</code>, automatic focus management (first item on open), full arrow key navigation with wrapping, Home/End support, and Escape dismissal. The consumer provides all visual styling, positioning, animation, and menu item content.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.dropdown-menu</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>dropdown-menu</code></li>\n<li>Verify aria-label=\"{label}\"` -- on both the trigger button and the menu, provides an accessible name</li>\n<li>Verify role=\"menu\"` -- on the menu container, identifies it as a menu widget</li>\n<li>Verify role=\"menuitem\"` -- expected on each child item provided by the consumer</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Position the menu directly below the trigger button. Keep the menu width consistent and avoid overly long item labels. Use separators for grouping related actions.</li>\n<li><strong>Developers</strong>: Ensure each child element has <code>role=\"menuitem\"</code> and <code>tabindex=\"-1\"</code>. Return focus to the trigger button after the menu closes.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>menu-item</code> — one item in a menu</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Menu Button Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/menubutton/</li>\n<li>WAI-ARIA menu role: https://www.w3.org/TR/wai-aria-1.2/#menu</li>\n<li>WAI-ARIA menuitem role: https://www.w3.org/TR/wai-aria-1.2/#menuitem</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"dropdown-menu\" role=\"menu\"><div role=\"menuitem\">Option 1</div><div role=\"menuitem\">Option 2</div></div>";
  const svelteSource: string = "// In your Svelte component:\nimport DropdownMenu from \"lily-design-system-svelte-headless/components/DropdownMenu/DropdownMenu.svelte\";\n\n<DropdownMenu>\n  <!-- list children -->\n</DropdownMenu>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Dropdown Menu — Lily Design System</title>
  <meta name="description" content="A dropdown menu triggered by a button that reveals a list of actions or options. This component combines a toggle button with a menu panel, providing a compact way to present multiple actions or ch..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

