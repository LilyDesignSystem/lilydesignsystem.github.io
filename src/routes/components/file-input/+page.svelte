<script lang="ts">
  const html: string = "<h1>File Input</h1>\n\n<p>A file input wraps a native <code>&lt;input type=\"file\"&gt;</code> element with accessible labelling. It allows users to select one or more files from their device for upload. The component supports filtering by accepted file types, multiple file selection, and standard form states.</p>\n\n<p>This component is useful in upload forms, profile editors, document management interfaces, and anywhere users need to attach files. It provides a headless wrapper around the native file picker, leaving all visual presentation to the consumer.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a native <code>&lt;input type=\"file\"&gt;</code> element</li>\n<li>Uses <code>aria-label</code> for accessible naming (no visible label element included; consumers can add their own)</li>\n<li>Supports <code>accept</code> attribute for restricting file types (e.g., <code>\"image/*\"</code>, <code>\".pdf,.doc\"</code>)</li>\n<li>Supports <code>multiple</code> attribute for allowing selection of more than one file</li>\n<li>Supports standard HTML form attributes: <code>required</code>, <code>disabled</code></li>\n<li>Spreads <code>restProps</code> for consumer customization (e.g., <code>capture</code>, event handlers)</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name via <code>aria-label</code></li>\n<li><code>accept</code>: string (default: <code>undefined</code>) -- comma-separated list of accepted file types (MIME types or extensions)</li>\n<li><code>multiple</code>: boolean (default: <code>false</code>) -- whether multiple files can be selected</li>\n<li><code>required</code>: boolean (default: <code>false</code>) -- whether a file selection is required</li>\n<li><code>disabled</code>: boolean (default: <code>false</code>) -- whether the file input is disabled</li>\n<li><code>...restProps</code>: any additional HTML attributes spread onto the input</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Document attachment allowing multiple files:</p>\n\n<pre><code class=\"language-html\">&lt;FileInput label=\"Attach supporting documents\" accept=\".pdf,.doc,.docx\" multiple={true} required /&gt;</code></pre>\n\n<p>Single file upload for a form submission:</p>\n\n<pre><code class=\"language-html\">&lt;FileInput label=\"Upload signed consent form\" accept=\".pdf\" required /&gt;</code></pre>\n\n<p>Optional photo upload:</p>\n\n<pre><code class=\"language-html\">&lt;FileInput label=\"Upload photo\" accept=\"image/png,image/jpeg\" /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None beyond native input behavior -- <strong>Enter</strong> or <strong>Space</strong> opens the file picker dialog when the input is focused (browser-native behavior).</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides an accessible name for the file input since no visible <code>&lt;label&gt;</code> element is included</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for a native HTML file input element when the simplest file selection is needed.</li>\n<li>Use when users need to select one or more files from their device.</li>\n<li>Use when the native file picker is sufficient and a drag-and-drop area is not needed.</li>\n<li>Use in upload forms, profile editors, or document attachment fields.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when drag-and-drop is needed -- use FileUpload instead.</li>\n<li>Do not use for image selection with preview -- use ImageFileInput instead.</li>\n<li>Do not use when you need a styled upload button with status feedback -- use FileUpload instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a native <code>&lt;input type=\"file\"&gt;</code> with accessible labeling via <code>aria-label</code>, support for <code>accept</code>, <code>multiple</code>, <code>required</code>, and <code>disabled</code> attributes, and <code>restProps</code> for consumer extensibility. The consumer provides all visual styling, custom button appearance, and any file list display.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.file-input</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;input&gt;</code> element with class <code>file-input</code></li>\n<li>Verify aria-label={label}<code> -- provides an accessible name for the file input since no visible </code>&lt;label&gt;` element is included</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Consider pairing the file input with a visible label or instructions about accepted file types and size limits. The default browser styling varies, so plan for custom styling.</li>\n<li><strong>Developers</strong>: Use the <code>accept</code> prop to restrict file types at the browser level, but always validate on the server as well. Access selected files via the input's <code>files</code> property or event handlers.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>file-upload</code> — a drag-and-drop area for uploading files</li>\n<li><code>image-file-input</code> — an input for selecting image files with preview</li>\n<li><code>file-dialog</code> — a dialog for browsing and selecting files</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN input type=\"file\": https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/file</li>\n</ul>";
</script>

<svelte:head>
  <title>File Input — Lily Design System</title>
  <meta name="description" content="A file input wraps a native &lt;input type=&quot;file&quot;&gt; element with accessible labelling. It allows users to select one or more files from their device for upload. The component supports filtering by acce..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
