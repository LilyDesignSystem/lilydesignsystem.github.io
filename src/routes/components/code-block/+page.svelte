<script lang="ts">
  const html: string = "<h1>CodeBlock</h1>\n\n<p>A code block displays a block of formatted code with optional line numbers and line highlighting. Code blocks are used for presenting multi-line code examples, configuration files, or terminal output in documentation and tutorials.</p>\n\n<p>This headless component uses a <code>&lt;pre&gt;</code> element to preserve whitespace and formatting, providing a container for structured code display.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses <code>&lt;pre&gt;</code> element to preserve whitespace and line breaks</li>\n<li>Contains a nested <code>&lt;code&gt;</code> element for semantic code marking</li>\n<li>Optional line numbers via <code>data-line-numbers</code> attribute</li>\n<li>Optional line highlighting via <code>data-highlight-lines</code> attribute</li>\n<li><code>aria-label</code> provides a description of the code block content</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (optional) -- accessible label describing the code content</li>\n<li><code>lineNumbers</code>: boolean (default: false) -- whether to display line numbers</li>\n<li><code>highlightLines</code>: string (optional) -- comma-separated line numbers to highlight (e.g., \"1,3,5-7\")</li>\n<li><code>children</code>: slot (required) -- the code content</li>\n<li><code>...restProps</code>: Any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Inline code within a paragraph, paired with a code block for a full example:</p>\n\n<pre><code class=\"language-html\">&lt;p&gt;Add the &lt;Code&gt;.button&lt;/Code&gt; class to your element.&lt;/p&gt;\n\n&lt;CodeBlock label=\"Button markup example\" lineNumbers&gt;\n  &lt;code&gt;&amp;lt;button class=\"button\" type=\"submit\"&amp;gt;\n  Save and continue\n&amp;lt;/button&amp;gt;&lt;/code&gt;\n&lt;/CodeBlock&gt;</code></pre>\n\n<p>Highlighting a specific line in a code block:</p>\n\n<pre><code class=\"language-html\">&lt;CodeBlock label=\"JavaScript greeting function\" lineNumbers highlightLines=\"2\"&gt;\n  &lt;code&gt;function greet(name) {\n  return `Hello, ${name}!`;\n}&lt;/code&gt;\n&lt;/CodeBlock&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>None -- code blocks are informational, not interactive</li>\n<li>Tab: Moves focus past the code block (not into it)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label</code> -- optional description of the code block content</li>\n<li><code>role=\"region\"</code> when <code>aria-label</code> is provided, to create a navigable landmark</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for multi-line formatted code with optional line numbers and syntax highlighting.</li>\n<li>Use in documentation, tutorials, or developer-facing content for code examples.</li>\n<li>Use when preserving whitespace and formatting is important.</li>\n<li>Use for configuration files, API responses, or structured data display.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for single inline code references -- use Code instead.</li>\n<li>Do not use for terminal commands presented in a shell context -- use MockupShell for a terminal-style presentation.</li>\n<li>Do not use for non-code preformatted text that has no semantic code meaning.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides a <code>&lt;pre&gt;</code> element with optional line numbers, line highlighting data attributes, and accessible labeling, with zero visual styling. The consumer is responsible for all CSS including font family, background color, padding, border, overflow handling, line number display, and highlight styling.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.code-block</code> class for targeting. No default styles are included -- this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;pre&gt;</code> element with class <code>code-block</code></li>\n<li>Verify <code>aria-label</code> is applied when provided</li>\n<li>Verify <code>data-line-numbers</code> attribute reflects the lineNumbers prop</li>\n<li>Verify <code>data-highlight-lines</code> attribute reflects the highlightLines prop</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use a monospace font with sufficient line height. Provide horizontal scrolling for long lines. Use a distinct background to separate code from surrounding content.</li>\n<li><strong>Developers</strong>: Use the <code>label</code> prop to describe the code block purpose for screen readers. Nest a <code>&lt;code&gt;</code> element inside the <code>&lt;pre&gt;</code> for proper semantics.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>code</code> — an inline code span for displaying short code snippets within surrounding text</li>\n<li><code>kbd</code> — show keyboard shortcuts and key combinations in a styled inline element</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN <code>&lt;pre&gt;</code> element: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/pre</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<pre class=\"code-block\"><code>function hello() { return \"world\"; }</code></pre>";
  const svelteSource: string = "// In your Svelte component:\nimport CodeBlock from \"lily-design-system-svelte-headless/components/CodeBlock/CodeBlock.svelte\";\n\n<CodeBlock>\n  <!-- content -->\n</CodeBlock>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>CodeBlock — Lily Design System</title>
  <meta name="description" content="A code block displays a block of formatted code with optional line numbers and line highlighting. Code blocks are used for presenting multi-line code examples, configuration files, or terminal outp..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

