<script lang="ts">
  const html: string = "<h1>Contents List Item</h1>\n\n<p>A contents list item is a single entry within a table of contents or navigation list. It renders as a semantic list item (<code>&lt;li&gt;</code>) that typically contains a link to a section of a page or document.</p>\n\n<p>Contents list items are used as children of ordered or unordered lists within a table of contents component. They support nesting for hierarchical document structures where sections contain subsections. The consumer provides the content (usually an anchor link with descriptive text) through the children slot, and can add <code>aria-current</code> attributes to indicate the currently active section.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as a semantic <code>&lt;li&gt;</code> element for use in contents/navigation lists</li>\n<li>All content is provided through the children slot (typically links)</li>\n<li>Supports nesting by placing a <code>&lt;ul&gt;</code> inside a ContentsListItem</li>\n<li>Should be placed inside a <code>&lt;ul&gt;</code> or <code>&lt;ol&gt;</code> parent element for valid HTML</li>\n<li>Spreads <code>...restProps</code> onto the <code>&lt;li&gt;</code> element for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>children</code>: slot (required) -- list item content, typically a link or nested list</li>\n<li><code>...restProps</code>: Any additional HTML attributes spread onto the <code>&lt;li&gt;</code></li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;ContentsList&gt;\n  &lt;ContentsListItem aria-current=\"page\"&gt;\n    What is type 2 diabetes?\n  &lt;/ContentsListItem&gt;\n  &lt;ContentsListItem&gt;\n    &lt;ContentsLink href=\"/conditions/type-2-diabetes/symptoms\"&gt;Symptoms&lt;/ContentsLink&gt;\n  &lt;/ContentsListItem&gt;\n  &lt;ContentsListItem&gt;\n    &lt;ContentsLink href=\"/conditions/type-2-diabetes/getting-diagnosed\"&gt;Getting diagnosed&lt;/ContentsLink&gt;\n  &lt;/ContentsListItem&gt;\n&lt;/ContentsList&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>None directly -- keyboard interaction is handled by contained links or the parent list</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Semantic <code>&lt;li&gt;</code> provides implicit <code>listitem</code> role</li>\n<li>Supports <code>aria-current=\"page\"</code> or <code>aria-current=\"true\"</code> on the <code>&lt;li&gt;</code> for indicating the current page in a navigation context</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside ContentsList to represent one page in the contents navigation.</li>\n<li>Use with <code>aria-current=\"page\"</code> on the item representing the current page.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside of ContentsList -- it has no standalone meaning.</li>\n<li>Do not render the current page as a link -- display it as plain text with <code>aria-current=\"page\"</code>.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless ContentsListItem component provides a semantic <code>&lt;li&gt;</code> element with support for <code>aria-current</code> and nesting. The consumer provides all visual styling including link appearance, active-section highlighting, indentation for nested items, and hover/focus states.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.contents-list-item</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;li&gt;</code> element with class <code>contents-list-item</code></li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Highlight the currently active section link to help users orient themselves on long pages. Use consistent indentation for nested subsections.</li>\n<li><strong>Developers</strong>: Set <code>aria-current=\"true\"</code> on the active item to communicate the current section to screen readers. Nest a <code>&lt;ul&gt;</code> inside a ContentsListItem to create subsection levels.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>ContentsListItem is the child component in the ContentsNav/ContentsList/ContentsListItem composition pattern. Place ContentsListItem components inside a ContentsList. Each item typically contains an anchor link. For hierarchical structures, nest a <code>&lt;ul&gt;</code> with additional ContentsListItem components inside a parent item.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>contents-nav</code> — a contents navigation area</li>\n<li><code>contents-list</code> — an contents ordered list of contents list item components</li>\n<li><code>contents-link</code> — one table of contents link</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/HTML/Element/li\">MDN li element</a></li>\n</ul>";
</script>

<svelte:head>
  <title>Contents List Item — Lily Design System</title>
  <meta name="description" content="A contents list item is a single entry within a table of contents or navigation list. It renders as a semantic list item (&lt;li&gt;) that typically contains a link to a section of a page or document." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
