<script lang="ts">
  const html: string = "<h1>Ireland Individual Health Identifier Input</h1>\n\n<p>IrelandIndividualHealthIdentifierInput is a headless input for entering an Ireland Individual Health Identifier (IHI), the unique national healthcare identifier used by the HSE. It renders as <code>&lt;input type=\"text\"&gt;</code> with a 10-digit pattern, numeric keyboard hint, and autocomplete disabled. It is the editable companion to IrelandIndividualHealthIdentifierView.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as <code>&lt;input type=\"text\"&gt;</code> with pattern <code>[0-9]{10}</code></li>\n<li><code>inputmode=\"numeric\"</code> for mobile numeric keyboard</li>\n<li><code>autocomplete=\"off\"</code> to protect sensitive health identifiers</li>\n<li>Supports two-way binding on the <code>value</code> prop</li>\n<li>Companion to IrelandIndividualHealthIdentifierView</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible label via <code>aria-label</code></li>\n<li><code>value</code>: string (default: \"\") -- bindable input value</li>\n<li><code>required</code>: boolean (default: false) -- form validation</li>\n<li><code>disabled</code>: boolean (default: false) -- disabled state</li>\n<li><code>...restProps</code>: any -- additional HTML attributes spread onto the <code>&lt;input&gt;</code></li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;Field label=\"Individual Health Identifier\" required&gt;\n  &lt;IrelandIndividualHealthIdentifierInput label=\"IHI\" value={ihi} required /&gt;\n  &lt;Hint&gt;Enter the 10-digit IHI from your HSE records&lt;/Hint&gt;\n  &lt;ErrorMessage&gt;Please enter a valid 10-digit IHI&lt;/ErrorMessage&gt;\n&lt;/Field&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Standard text input keyboard behavior</li>\n<li>Users type 10 digits</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides accessible name</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use in clinical or administrative forms to collect an Ireland Individual Health Identifier (IHI).</li>\n<li>Use with validation for the 10-digit IHI format.</li>\n<li>Use in patient registration workflows where the IHI is required by HSE systems.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for displaying read-only identifiers -- use IrelandIndividualHealthIdentifierView instead.</li>\n<li>Do not use for general text input -- use TextInput instead.</li>\n<li>Do not use for other national identifiers -- use the corresponding country-specific input component.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a bare <code>&lt;input type=\"text\"&gt;</code> with <code>aria-label</code>, pattern, <code>inputmode=\"numeric\"</code>, and <code>autocomplete=\"off\"</code>. The consumer provides all styling.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.ireland-individual-health-identifier-input</code> class for targeting.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify renders an <code>&lt;input&gt;</code> with the correct class and type=\"text\"</li>\n<li>Verify <code>aria-label</code> is set from the label prop</li>\n<li>Verify pattern <code>[0-9]{10}</code>, <code>inputmode=\"numeric\"</code>, <code>autocomplete=\"off\"</code></li>\n<li>Verify value binding works</li>\n</ul>\n\n<h2>Domain Knowledge</h2>\n\n<p>The Ireland Individual Health Identifier (IHI) is a 10-digit clinical identifier used by healthcare providers to safely match patients with their medical records across different hospitals and GPs. Unlike the Ireland Personal Public Service Number (PPSN), the IHI contains no personal information and is not used for social welfare or taxes. It is primarily used behind the scenes by the Health Service Executive (HSE).</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>ireland-individual-health-identifier-view</code> — a read-only display of Ireland Individual Health Identifier (IHI) unique national healthcare identifier</li>\n<li><code>text-input</code> — a single-line text input field &lt;input type=\"text\"&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>HSE: https://www.hse.ie/</li>\n</ul>";
</script>

<svelte:head>
  <title>Ireland Individual Health Identifier Input — Lily Design System</title>
  <meta name="description" content="IrelandIndividualHealthIdentifierInput is a headless input for entering an Ireland Individual Health Identifier (IHI), the unique national healthcare identifier used by the HSE. It renders as &lt;inpu..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
