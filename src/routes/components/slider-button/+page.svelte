<script lang="ts">
  const html: string = "<h1>SliderButton</h1>\n\n<p>A slider button is a button with a slider mechanism that the user needs to slide to confirm an action. This pattern prevents accidental activation and is used for destructive or irreversible actions that require deliberate confirmation.</p>\n\n<p>This headless component uses a <code>&lt;button&gt;</code> element with an internal slider track, requiring the user to drag a thumb across the full track to activate the button.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses <code>&lt;button&gt;</code> element for semantic button behavior</li>\n<li>Internal slider track requires drag-to-confirm interaction</li>\n<li><code>aria-label</code> describes the action to confirm</li>\n<li><code>aria-valuenow</code> tracks the slider position (0 to 100)</li>\n<li>Activates the button action only when slider reaches 100%</li>\n<li>Resets slider position if released before completion</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible label describing the confirmation action</li>\n<li><code>disabled</code>: boolean (default: false) -- whether the slider button is disabled</li>\n<li><code>onconfirm</code>: callback (optional) -- handler called when slider completes</li>\n<li><code>children</code>: slot -- button label content</li>\n<li><code>...restProps</code>: Any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Slide to confirm account deletion:</p>\n\n<pre><code class=\"language-html\">&lt;SliderButton label=\"Slide to delete account\" onconfirm={handleDeleteAccount}&gt;\n  Slide to delete account\n&lt;/SliderButton&gt;</code></pre>\n\n<p>Slide to confirm payment:</p>\n\n<pre><code class=\"language-html\">&lt;SliderButton label=\"Slide to send payment\" onconfirm={handleSendPayment}&gt;\n  Slide to send $500.00\n&lt;/SliderButton&gt;</code></pre>\n\n<p>Disabled slider button while processing:</p>\n\n<pre><code class=\"language-html\">&lt;SliderButton label=\"Slide to confirm\" onconfirm={handleConfirm} disabled={isProcessing}&gt;\n  Slide to confirm\n&lt;/SliderButton&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus the slider button</li>\n<li>Arrow Right: Move slider toward completion</li>\n<li>Arrow Left: Move slider toward start</li>\n<li>Enter: Activate immediately (accessibility fallback)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Implicit <code>button</code> role from <code>&lt;button&gt;</code> element</li>\n<li><code>aria-label</code> -- describes the confirmation action</li>\n<li><code>aria-valuenow</code> -- current slider position percentage</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for a slide-to-confirm interaction where users must drag to confirm an irreversible action.</li>\n<li>Use for destructive actions such as deleting an account, sending a payment, or initiating a transfer.</li>\n<li>Use when accidental clicks need to be prevented and a standard confirmation dialog is not sufficient.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for value selection -- use Slider or RangeInput instead.</li>\n<li>Do not use for simple confirmations -- use AlertDialog instead.</li>\n<li>Do not use for routine, easily reversible actions -- use a standard Button instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides a <code>&lt;button&gt;</code> element with slider mechanics and ARIA value tracking, with zero visual styling. The consumer is responsible for all CSS including track appearance, thumb styling, progress fill, and completion animation.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.slider-button</code> class for targeting. No default styles are included -- this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;button&gt;</code> element with class <code>slider-button</code></li>\n<li>Verify <code>aria-label</code> is applied</li>\n<li>Verify slider interaction triggers onconfirm at completion</li>\n<li>Verify slider resets when released before completion</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Make the slider track and thumb clearly visible. Provide visual feedback as the slider progresses. Show a completion animation on success.</li>\n<li><strong>Developers</strong>: Always provide a keyboard fallback (Enter key) for accessibility. Reset the slider state after confirmation or cancellation.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>button</code> — a generic clickable button element</li>\n<li><code>slider</code> — a draggable control for selecting a value along a track</li>\n<li><code>submit-input</code> — a button input that submits a form &lt;input type=\"submit\"&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/HTML/Element/button\">MDN button element</a></li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<button class=\"slider-button\">Slide to confirm</button>";
  const svelteSource: string = "// In your Svelte component:\nimport SliderButton from \"lily-design-system-svelte-headless/components/SliderButton/SliderButton.svelte\";\n\n<SliderButton>SliderButton</SliderButton>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>SliderButton — Lily Design System</title>
  <meta name="description" content="A slider button is a button with a slider mechanism that the user needs to slide to confirm an action. This pattern prevents accidental activation and is used for destructive or irreversible action..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

