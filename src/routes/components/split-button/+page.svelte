<script lang="ts">
  const html: string = "<h1>Split Button</h1>\n\n<p>A split button is a paired control composed of a primary action button and a secondary dropdown trigger, both contained inside a single ARIA group. The primary button performs the most common action; the dropdown reveals a menu of related secondary actions.</p>\n\n<p>Use it where one default action is dominant but related variants must remain discoverable — for example \"Save / Save as…\", \"Send / Schedule send\", or \"Export / Export as PDF / Export as CSV\".</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> with <code>role=\"group\"</code> and <code>class=\"split-button\"</code></li>\n<li>The group's <code>aria-label</code> is set from the <code>label</code> prop</li>\n<li>The primary button: <code>&lt;button class=\"split-button-primary\" type=\"button\"&gt;{primaryLabel}&lt;/button&gt;</code></li>\n<li>The menu trigger: <code>&lt;button class=\"split-button-menu-trigger\" type=\"button\" aria-haspopup=\"menu\" aria-expanded={menuOpen} aria-label={menuLabel}&gt;&lt;/button&gt;</code></li>\n<li>The menu container: <code>&lt;div class=\"split-button-menu\" hidden={!menuOpen}&gt;{children}&lt;/div&gt;</code></li>\n<li>The headless component renders no visual chevron — consumers may slot/style their own</li>\n<li>Keeps the primary action and dropdown as separate buttons so each has its own focus and click target</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- aria-label for the group</li>\n<li><code>primaryLabel</code>: string (required) -- text for the primary action button</li>\n<li><code>menuLabel</code>: string (required) -- aria-label for the dropdown trigger</li>\n<li><code>menuOpen</code>: boolean (default: false) -- whether the dropdown menu is open</li>\n<li><code>disabled</code>: boolean (default: false)</li>\n<li><code>onPrimaryClick</code> / <code>onprimaryclick</code>: callback -- primary action handler</li>\n<li><code>onMenuToggle</code> / <code>onmenutoggle</code>: callback -- menu toggle handler</li>\n<li><code>children</code>: slot -- the menu content (typically a <code>Menu</code> component)</li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Save with related actions:</p>\n\n<pre><code class=\"language-html\">&lt;SplitButton\n  label=\"Save options\"\n  primaryLabel=\"Save\"\n  menuLabel=\"More save options\"\n  menuOpen={isMenuOpen}\n  onPrimaryClick={handleSave}\n  onMenuToggle={() =&gt; setIsMenuOpen(!isMenuOpen)}\n&gt;\n  &lt;Menu label=\"More save options\"&gt;\n    &lt;MenuItem onclick={handleSaveAs}&gt;Save as…&lt;/MenuItem&gt;\n    &lt;MenuItem onclick={handleSaveAndClose}&gt;Save and close&lt;/MenuItem&gt;\n  &lt;/Menu&gt;\n&lt;/SplitButton&gt;</code></pre>\n\n<p>Disabled split button:</p>\n\n<pre><code class=\"language-html\">&lt;SplitButton\n  label=\"Send options\"\n  primaryLabel=\"Send\"\n  menuLabel=\"More send options\"\n  disabled\n&gt;\n  &lt;Menu label=\"More send options\"&gt;\n    &lt;MenuItem&gt;Schedule send&lt;/MenuItem&gt;\n  &lt;/Menu&gt;\n&lt;/SplitButton&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Move focus through the primary button, then the menu trigger</li>\n<li>Enter / Space on the primary button: Fire <code>onPrimaryClick</code></li>\n<li>Enter / Space on the menu trigger: Toggle <code>onMenuToggle</code></li>\n<li>Escape: The consumer's menu component handles closing</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"group\"</code> on the wrapping <code>&lt;div&gt;</code> so assistive technology treats both buttons as one composite control</li>\n<li><code>aria-label</code> on the group from the <code>label</code> prop</li>\n<li><code>aria-haspopup=\"menu\"</code> on the menu trigger</li>\n<li><code>aria-expanded={menuOpen}</code> on the menu trigger</li>\n<li><code>aria-label={menuLabel}</code> on the menu trigger because it has no text content</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use when one primary action is dominant but secondary related actions must remain one click away.</li>\n<li>Use to keep the primary action a single click — split buttons preserve \"default\" speed while exposing alternates.</li>\n<li>Use for save, send, export, deploy, and similar action families.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when there is no clear primary action — use a Menu or DropdownMenu instead.</li>\n<li>Do not use as the only navigation pattern (use NavigationMenu, MenuBar).</li>\n<li>Do not use when the actions are not related — secondary items should be variants of the primary.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>Provides only the structural composition (group, two buttons, hidden menu container) and the ARIA wiring. The dropdown chevron, menu positioning, focus management inside the menu, and any visual treatment are the consumer's responsibility.</p>\n\n<h2>Styles</h2>\n\n<p>Consumer CSS targets <code>.split-button</code>, <code>.split-button-primary</code>, <code>.split-button-menu-trigger</code>, and <code>.split-button-menu</code> for visual styling and positioning.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> with class <code>split-button</code> and <code>role=\"group\"</code></li>\n<li>Verify the group's <code>aria-label</code> equals the <code>label</code> prop</li>\n<li>Verify the primary button renders with class <code>split-button-primary</code> and text from <code>primaryLabel</code></li>\n<li>Verify the menu trigger renders with class <code>split-button-menu-trigger</code> and <code>aria-label</code> from <code>menuLabel</code></li>\n<li>Verify <code>aria-haspopup=\"menu\"</code> and <code>aria-expanded={menuOpen}</code> on the menu trigger</li>\n<li>Verify the menu container has the <code>hidden</code> attribute when <code>menuOpen</code> is false</li>\n<li>Verify <code>onPrimaryClick</code> fires when the primary button is clicked</li>\n<li>Verify <code>onMenuToggle</code> fires when the menu trigger is clicked</li>\n<li>Verify <code>disabled</code> propagates to both buttons</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Make the primary action visually dominant and the dropdown trigger a smaller adjacent affordance. Keep menu items to a short list of related variants.</li>\n<li><strong>Developers</strong>: The component does not manage menu state — the consumer owns <code>menuOpen</code> and decides when to close (Escape, outside click, item selection). Pair with the Menu component for built-in keyboard navigation inside the menu.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>button</code> — a generic clickable button element</li>\n<li><code>dropdown-menu</code> — a menu that opens below a trigger button</li>\n<li><code>menu-bar-button</code> — one item in a menu bar</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Disclosure pattern: https://www.w3.org/WAI/ARIA/apg/patterns/disclosure/</li>\n<li>WAI-ARIA Menu Button pattern: https://www.w3.org/WAI/ARIA/apg/patterns/menu-button/</li>\n<li>WAI-ARIA group role: https://www.w3.org/TR/wai-aria-1.2/#group</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"split-button\" aria-label=\"a button with a primary action and a dropdown for related actions\">SplitButton</div>";
  const svelteSource: string = "// In your Svelte component:\nimport SplitButton from \"lily-design-system-svelte-headless/components/SplitButton/SplitButton.svelte\";\n\n<SplitButton>SplitButton</SplitButton>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Split Button — Lily Design System</title>
  <meta name="description" content="A split button is a paired control composed of a primary action button and a secondary dropdown trigger, both contained inside a single ARIA group. The primary button performs the most common actio..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

