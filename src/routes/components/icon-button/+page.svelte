<script lang="ts">
  const html: string = "<h1>Icon Button</h1>\n\n<p>An icon button is a native <code>&lt;button&gt;</code> containing only an icon glyph, image, or SVG. Because icon-only buttons have no visible text, this component <strong>requires</strong> an accessible label so that screen readers can announce the purpose of the action.</p>\n\n<p>Use it for compact, recognizable actions such as close, delete, edit, share, favorite, and toolbar controls where a label would be visually redundant once a user understands the icon.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses a native <code>&lt;button&gt;</code> element for inherent keyboard and accessibility support</li>\n<li><code>label</code> prop is <strong>required</strong> — it sets <code>aria-label</code> so the button has an accessible name</li>\n<li>Defaults to <code>type=\"button\"</code> to prevent accidental form submissions</li>\n<li><code>aria-pressed</code> is rendered only when <code>pressed</code> is provided (toggle button pattern)</li>\n<li>Native <code>disabled</code> attribute prevents click events automatically</li>\n<li>Children render the icon content (svg, emoji, glyph) and are decorative</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (<strong>required</strong>) -- aria-label</li>\n<li><code>type</code>: \"button\" | \"submit\" | \"reset\" (default: \"button\")</li>\n<li><code>disabled</code>: boolean (default: false)</li>\n<li><code>pressed</code>: boolean | undefined (default: undefined) -- toggle button state</li>\n<li><code>onclick</code> / <code>onClick</code>: callback (optional) -- click handler</li>\n<li><code>children</code>: slot -- the icon content</li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Close button with required label:</p>\n\n<pre><code class=\"language-html\">&lt;IconButton label=\"Close\" onclick={handleDismiss}&gt;\n  &lt;svg&gt;...&lt;/svg&gt;\n&lt;/IconButton&gt;</code></pre>\n\n<p>Toggle favorite:</p>\n\n<pre><code class=\"language-html\">&lt;IconButton label=\"Favorite\" pressed={isFavorited} onclick={toggleFavorite}&gt;\n  ★\n&lt;/IconButton&gt;</code></pre>\n\n<p>Disabled icon action:</p>\n\n<pre><code class=\"language-html\">&lt;IconButton label=\"Delete\" disabled&gt;\n  &lt;svg&gt;...&lt;/svg&gt;\n&lt;/IconButton&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus the button</li>\n<li>Enter: Activate the button</li>\n<li>Space: Activate the button</li>\n<li>(All handled natively by <code>&lt;button&gt;</code> element)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Implicit <code>button</code> role from <code>&lt;button&gt;</code> element</li>\n<li><code>aria-label</code> is the entire accessible name (required)</li>\n<li><code>aria-pressed</code> for toggle button state (when provided)</li>\n<li><code>aria-disabled</code> from native <code>disabled</code> attribute</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for compact actions where the icon meaning is universally recognized (close, search, edit, delete).</li>\n<li>Use in toolbars and tight UI regions where a labeled button would be too wide.</li>\n<li>Use the toggle pattern (with <code>pressed</code>) for binary state controls (mute/unmute, follow/unfollow).</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when the action is unfamiliar or ambiguous — use a labeled Button.</li>\n<li>Do not use as a primary call to action — use Button so users see the verb.</li>\n<li>Do not omit <code>label</code> — icon-only buttons without an accessible name fail WCAG.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>Renders a native <code>&lt;button&gt;</code> with the required <code>aria-label</code>. The icon glyph, its size, focus indicator, hover state, and any visual treatment are entirely the consumer's responsibility.</p>\n\n<h2>Styles</h2>\n\n<p>Consumer CSS targets the <code>icon-button</code> class. Provide a minimum 44×44 px touch target, a clear focus indicator, and hover/active visual feedback.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;button&gt;</code> element with class <code>icon-button</code></li>\n<li>Verify <code>aria-label</code> is present and equals the <code>label</code> prop</li>\n<li>Verify <code>type</code> defaults to <code>\"button\"</code> when not provided</li>\n<li>Verify <code>aria-pressed</code> is rendered only when <code>pressed</code> is provided</li>\n<li>Verify <code>disabled</code> attribute is applied when <code>disabled</code> is true</li>\n<li>Verify the click handler fires on Enter, Space, and click</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use icons that are universally recognizable. Maintain a 44×44 px touch target and a high-contrast focus ring.</li>\n<li><strong>Developers</strong>: Always supply a <code>label</code> prop. The <code>pressed</code> prop is for toggle semantics only — leave it <code>undefined</code> for ordinary action buttons.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>button</code> — a generic clickable button element</li>\n<li><code>float-button</code> — a floating action button anchored to a viewport corner</li>\n<li><code>tool-bar-button</code> — one action button in a tool bar</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Button Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/button/</li>\n<li>WCAG 4.1.2 Name, Role, Value: https://www.w3.org/WAI/WCAG22/Understanding/name-role-value</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"icon-button\" aria-label=\"a button containing only an icon with a required accessible label\">IconButton</div>";
  const svelteSource: string = "// In your Svelte component:\nimport IconButton from \"lily-design-system-svelte-headless/components/IconButton/IconButton.svelte\";\n\n<IconButton>IconButton</IconButton>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Icon Button — Lily Design System</title>
  <meta name="description" content="An icon button is a native &lt;button&gt; containing only an icon glyph, image, or SVG. Because icon-only buttons have no visible text, this component **requires** an accessible label so that screen read..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

