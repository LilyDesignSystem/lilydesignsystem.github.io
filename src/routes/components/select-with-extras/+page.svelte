<script lang="ts">
  const html: string = "<h1>Select With Extras</h1>\n\n<p>SelectWithExtras is a headless enhanced select component that wraps a native <code>&lt;select&gt;</code> element with optional content areas before and after it. This allows consumers to place supplementary elements such as icons, flags, status indicators, or helper text alongside the select without breaking its accessibility.</p>\n\n<p>Use SelectWithExtras when you need a native select dropdown with additional visual context. For example, displaying a country flag before a country selector, showing a unit label after a measurement selector, or adding an icon before a category picker. The component preserves full native select behavior including keyboard navigation, form submission, and screen reader support.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a wrapper <code>&lt;div&gt;</code> containing optional <code>before</code> slot, a native <code>&lt;select&gt;</code>, and optional <code>after</code> slot</li>\n<li>The <code>&lt;select&gt;</code> element receives <code>aria-label</code> for accessibility, with two-way binding</li>\n<li>Supports <code>required</code> and <code>disabled</code> attributes on the native select</li>\n<li>Supports two-way binding for the <code>value</code> prop, enabling two-way <code>value</code> binding from the parent</li>\n<li>The <code>before</code> and <code>after</code> slots are conditionally rendered only when provided</li>\n<li>Spreads <code>restProps</code> onto the wrapper div for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible label for the select element, applied via <code>aria-label</code></li>\n<li><code>value</code>: string (default: <code>\"\"</code>) -- the currently selected value; two-way bindable via <code>value</code></li>\n<li><code>required</code>: boolean (default: <code>false</code>) -- whether the select is required for form validation</li>\n<li><code>disabled</code>: boolean (default: <code>false</code>) -- whether the select is disabled</li>\n<li><code>children</code>: slot (required) -- <code>&lt;option&gt;</code> elements for the select</li>\n<li><code>before</code>: slot (optional) -- content rendered before the select element</li>\n<li><code>after</code>: slot (optional) -- content rendered after the select element</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the wrapper <code>&lt;div&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Country selector with a flag icon before the select:</p>\n\n<pre><code class=\"language-html\">&lt;SelectWithExtras label=\"Country\" value={country}&gt;\n  {#snippet before()}&lt;span class=\"flag-icon\"&gt;🏳&lt;/span&gt;{/snippet}\n  &lt;option value=\"us\"&gt;United States&lt;/option&gt;\n  &lt;option value=\"uk\"&gt;United Kingdom&lt;/option&gt;\n  &lt;option value=\"fr\"&gt;France&lt;/option&gt;\n&lt;/SelectWithExtras&gt;</code></pre>\n\n<p>Currency selector with symbol after the select:</p>\n\n<pre><code class=\"language-html\">&lt;SelectWithExtras label=\"Currency\" value={currency}&gt;\n  &lt;option value=\"GBP\"&gt;British Pound&lt;/option&gt;\n  &lt;option value=\"USD\"&gt;US Dollar&lt;/option&gt;\n  &lt;option value=\"EUR\"&gt;Euro&lt;/option&gt;\n  {#snippet after()}&lt;span class=\"currency-symbol\"&gt;£&lt;/span&gt;{/snippet}\n&lt;/SelectWithExtras&gt;</code></pre>\n\n<p>Unit selector with a label before:</p>\n\n<pre><code class=\"language-html\">&lt;SelectWithExtras label=\"Measurement unit\" value={unit}&gt;\n  {#snippet before()}&lt;span&gt;Unit:&lt;/span&gt;{/snippet}\n  &lt;option value=\"kg\"&gt;Kilograms&lt;/option&gt;\n  &lt;option value=\"lb\"&gt;Pounds&lt;/option&gt;\n&lt;/SelectWithExtras&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Moves focus to the native select element</li>\n<li>Arrow Up / Arrow Down: Navigates between options (native select behavior)</li>\n<li>Space / Enter: Opens the select dropdown on some platforms (native select behavior)</li>\n<li>Escape: Closes the dropdown if open (native select behavior)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides an accessible name for the select element so screen readers announce its purpose</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use SelectWithExtras when a native select dropdown needs supplementary visual context such as icons, flags, or unit labels alongside the options.</li>\n<li>Use SelectWithExtras to preserve full native select behavior (keyboard navigation, form submission, screen reader support) while adding decorative elements.</li>\n<li>Use SelectWithExtras for country selectors with flag icons, currency selectors with symbols, or unit selectors with labels.</li>\n<li>Use SelectWithExtras when the before/after content is purely decorative or informational.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use SelectWithExtras when a native Select without extra visual context is sufficient.</li>\n<li>Do not use SelectWithExtras for free-text search or filterable dropdowns -- use Combobox instead.</li>\n<li>Do not use SelectWithExtras when the before/after content is interactive -- interactive elements in these slots can confuse keyboard navigation.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides the semantic structure of a native <code>&lt;select&gt;</code> element wrapped in a container with optional before/after slots, along with proper <code>aria-label</code> for accessibility. The consumer provides all visual styling for the wrapper, the select element, and any supplementary content placed in the before/after slots.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.select-with-extras</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>select-with-extras</code></li>\n<li>Verify aria-label={label}` -- provides an accessible name for the select element so screen readers announce its purpose</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Keep before/after content compact so it does not visually overwhelm the select control. Use consistent icon sizes and spacing across all SelectWithExtras instances.</li>\n<li><strong>Developers</strong>: Ensure the <code>before</code> and <code>after</code> slot content is decorative or supplementary only; interactive elements in these slots can confuse keyboard navigation. Use the <code>aria-label</code> prop rather than relying on before/after content for accessible naming.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>select</code> — a dropdown select element for choosing one option</li>\n<li><code>combobox</code> — a text input combined with a dropdown list for filtering options</li>\n<li><code>cascader</code> — a multi-level dropdown for selecting a value from a hierarchy</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Listbox Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/listbox/</li>\n<li>HTML <code>&lt;select&gt;</code> element: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/select</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"select-with-extras\"><input type=\"text\" placeholder=\"Search...\" /><select><option>Option 1</option><option>Option 2</option></select></div>";
  const svelteSource: string = "// In your Svelte component:\nimport SelectWithExtras from \"lily-design-system-svelte-headless/components/SelectWithExtras/SelectWithExtras.svelte\";\n\n<SelectWithExtras>\n  <!-- content -->\n</SelectWithExtras>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Select With Extras — Lily Design System</title>
  <meta name="description" content="SelectWithExtras is a headless enhanced select component that wraps a native &lt;select&gt; element with optional content areas before and after it. This allows consumers to place supplementary elements ..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

