<script lang="ts">
  const html: string = "<h1>Back Link</h1>\n\n<p>A back link is a navigational component that allows users to return to a previous page or step within an application. It renders as a semantic anchor (<code>&lt;a&gt;</code>) element with optional <code>aria-label</code> for enhanced screen reader context.</p>\n\n<p>Back links are especially important in mobile and multi-step interfaces, helping users maintain their sense of orientation and providing a clear, intuitive way to reverse navigation without relying solely on the browser's back button. Consumers add visual indicators (left arrow, chevron) via CSS or children content.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as a semantic <code>&lt;a&gt;</code> element for backward navigation</li>\n<li>Same HTML pattern as a standard link but semantically represents \"going back\"</li>\n<li>Supports an optional <code>aria-label</code> override for more descriptive screen reader text</li>\n<li>All link text is provided through the children slot</li>\n<li>Spreads <code>...restProps</code> onto the <code>&lt;a&gt;</code> element</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>href</code>: string (required) -- the URL to navigate back to</li>\n<li><code>label</code>: string (optional, default: undefined) -- accessible label override via <code>aria-label</code></li>\n<li><code>children</code>: slot (required) -- the link text/content</li>\n<li><code>...restProps</code>: Any additional HTML attributes spread onto the <code>&lt;a&gt;</code></li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;BackLink href=\"/registration/step-2\"&gt;Back&lt;/BackLink&gt;\n\n&lt;BackLink href=\"/application/personal-details\" label=\"Back to personal details\"&gt;\n  Back\n&lt;/BackLink&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus the link</li>\n<li>Enter: Activate the link (browser default)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Implicit <code>link</code> role from the <code>&lt;a&gt;</code> element</li>\n<li><code>aria-label</code> -- optional override for descriptive screen reader text, set from the <code>label</code> prop</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use on question pages and transactional service pages to help users navigate back to the previous step.</li>\n<li>Use in multi-page forms where users may need to review or change earlier answers.</li>\n<li>Use when the user's journey is linear and going back makes contextual sense.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use on content pages such as health information articles -- there is no transaction to go back in.</li>\n<li>Do not use alongside BreadcrumbNav -- choose one navigation pattern, not both.</li>\n<li>Do not use when the previous page is not meaningful in context -- consider BreadcrumbNav for hierarchical navigation instead.</li>\n<li>Do not use for general \"return to homepage\" actions -- use a standard link or BreadcrumbNav.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides a semantic <code>&lt;a&gt;</code> element with optional <code>aria-label</code> override and zero visual styling. The consumer is responsible for all CSS including left-arrow or chevron icons, link color, underline style, hover effects, and focus indicators.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.back-link</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;a&gt;</code> element with class <code>back-link</code></li>\n<li>Verify aria-label<code> -- optional override for descriptive screen reader text, set from the </code>label` prop</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Place the back link consistently at the top-left of the content area. Include a left-pointing arrow icon to visually reinforce the \"back\" direction.</li>\n<li><strong>Developers</strong>: Use the <code>label</code> prop to provide descriptive screen reader text when the visible text is short (e.g., \"Back\" visible but \"Return to dashboard\" announced).</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>go-to-top</code> — a link that returns users to the top of a long page</li>\n<li><code>skip-link</code> — a hidden link for keyboard users to skip to main content</li>\n<li><code>breadcrumb-nav</code> — a navigation container for breadcrumb trail links</li>\n<li><code>action-link</code> — a hyperlink styled as an action trigger</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/HTML/Element/a\">MDN a element</a></li>\n</ul>";
</script>

<svelte:head>
  <title>Back Link — Lily Design System</title>
  <meta name="description" content="A back link is a navigational component that allows users to return to a previous page or step within an application. It renders as a semantic anchor (&lt;a&gt;) element with optional aria-label for enha..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
