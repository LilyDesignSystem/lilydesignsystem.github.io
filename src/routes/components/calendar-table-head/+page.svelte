<script lang="ts">
  const html: string = "<h1>Calendar Table Head</h1>\n\n<p>A calendar table head is the header section of a calendar grid, wrapping one or more rows that label the columns of the calendar. Typically contains a single row with day-of-week abbreviations (e.g., Sun, Mon, Tue). It is designed to be used inside a CalendarTable <code>&lt;table&gt;</code> structure.</p>\n\n<p>The component renders a <code>&lt;thead&gt;</code> element and passes through its children, which are expected to be CalendarTableRow or <code>&lt;tr&gt;</code> elements containing <code>&lt;th&gt;</code> header cells.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as a <code>&lt;thead&gt;</code> element for the header section of a calendar grid</li>\n<li>Children should be one or more rows with <code>&lt;th&gt;</code> cells labeling calendar columns (e.g., days of the week)</li>\n<li>Designed to be used inside a CalendarTable <code>&lt;table&gt;</code> structure</li>\n<li>Spreads <code>restProps</code> onto the <code>&lt;thead&gt;</code> element for consumer customization</li>\n<li>No internal state -- purely a structural wrapper</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>children</code>: slot (required) -- header rows, typically containing day-of-week labels</li>\n<li><code>...restProps</code>: Any additional HTML attributes passed to the <code>&lt;thead&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;CalendarTable label=\"April 2026\"&gt;\n  &lt;CalendarTableHead&gt;\n    &lt;CalendarTableRow&gt;\n      &lt;th&gt;&lt;abbr title=\"Monday\"&gt;Mon&lt;/abbr&gt;&lt;/th&gt;\n      &lt;th&gt;&lt;abbr title=\"Tuesday\"&gt;Tue&lt;/abbr&gt;&lt;/th&gt;\n      &lt;th&gt;&lt;abbr title=\"Wednesday\"&gt;Wed&lt;/abbr&gt;&lt;/th&gt;\n      &lt;th&gt;&lt;abbr title=\"Thursday\"&gt;Thu&lt;/abbr&gt;&lt;/th&gt;\n      &lt;th&gt;&lt;abbr title=\"Friday\"&gt;Fri&lt;/abbr&gt;&lt;/th&gt;\n      &lt;th&gt;&lt;abbr title=\"Saturday\"&gt;Sat&lt;/abbr&gt;&lt;/th&gt;\n      &lt;th&gt;&lt;abbr title=\"Sunday\"&gt;Sun&lt;/abbr&gt;&lt;/th&gt;\n    &lt;/CalendarTableRow&gt;\n  &lt;/CalendarTableHead&gt;\n  &lt;CalendarTableBody&gt;...&lt;/CalendarTableBody&gt;\n&lt;/CalendarTable&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive container. Keyboard navigation is handled by the parent CalendarTable grid.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Implicit <code>rowgroup</code> role from the <code>&lt;thead&gt;</code> element -- groups the header rows of the grid</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside CalendarTable to provide the <code>&lt;thead&gt;</code> for the calendar grid</li>\n<li>Use to display day-of-week column labels such as Mon, Tue, Wed</li>\n<li>Use when you need semantic column headers so screen readers announce day names as users navigate cells</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside CalendarTable -- use TableHead or DataTableHead for other table types</li>\n<li>Do not use for calendar body rows -- use CalendarTableBody for the main date grid</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides a semantic <code>&lt;thead&gt;</code> element with implicit <code>rowgroup</code> role and zero visual styling. The consumer is responsible for all CSS including header cell typography, alignment, background color, bottom borders, and any abbreviation formatting for day names.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.calendar-table-head</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;thead&gt;</code> element with class <code>calendar-table-head</code></li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Keep day-of-week labels short (2-3 characters) to fit within cell widths. Use a visually distinct style (bold, different background) to separate the header from body rows.</li>\n<li><strong>Developers</strong>: Use <code>&lt;th&gt;</code> elements inside the header row for proper column header semantics. Consider using <code>&lt;abbr&gt;</code> within <code>&lt;th&gt;</code> for abbreviated day names with full title attributes.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>CalendarTableHead is part of the CalendarTable composition pattern:</p>\n\n<ul>\n<li><strong>CalendarTable</strong> -- outer <code>&lt;table&gt;</code> with <code>role=\"grid\"</code>.</li>\n<li><strong>CalendarTableHead</strong> -- <code>&lt;thead&gt;</code> for day-of-week column headers.</li>\n<li><strong>CalendarTableBody</strong> -- <code>&lt;tbody&gt;</code> for day rows.</li>\n<li><strong>CalendarTableRow</strong> -- <code>&lt;tr&gt;</code> within any section.</li>\n</ul>\n\n<pre><code class=\"language-html\">&lt;CalendarTable label=\"January 2025\"&gt;\n  &lt;CalendarTableHead&gt;\n    &lt;tr&gt;&lt;th&gt;Sun&lt;/th&gt;&lt;th&gt;Mon&lt;/th&gt;&lt;th&gt;Tue&lt;/th&gt;&lt;th&gt;Wed&lt;/th&gt;&lt;th&gt;Thu&lt;/th&gt;&lt;th&gt;Fri&lt;/th&gt;&lt;th&gt;Sat&lt;/th&gt;&lt;/tr&gt;\n  &lt;/CalendarTableHead&gt;\n  &lt;CalendarTableBody&gt;...&lt;/CalendarTableBody&gt;\n&lt;/CalendarTable&gt;</code></pre>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>calendar-table</code> — a calendar table interactive grid for managing dates, days, etc. &lt;table&gt;</li>\n<li><code>calendar-table-body</code> — a calendar table interactive grid tbody for managing dates, days, etc. &lt;tbody&gt;</li>\n<li><code>calendar-table-foot</code> — a calendar table interactive grid tfoot for managing dates, days, etc. &lt;tfoot&gt;</li>\n<li><code>calendar-table-row</code> — a calendar table interactive grid row for managing dates, days, etc. &lt;tr&gt;</li>\n<li><code>calendar-table-td</code> — a calendar table interactive grid data cell for managing dates, days, etc. &lt;td&gt;</li>\n<li><code>calendar-table-th</code> — a calendar table interactive grid header cell for managing dates, days, etc. &lt;th&gt;</li>\n<li><code>table</code> — a table with rows and columns &lt;table&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Grid Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/grid/</li>\n</ul>";
</script>

<svelte:head>
  <title>Calendar Table Head — Lily Design System</title>
  <meta name="description" content="A calendar table head is the header section of a calendar grid, wrapping one or more rows that label the columns of the calendar. Typically contains a single row with day-of-week abbreviations (e.g..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
