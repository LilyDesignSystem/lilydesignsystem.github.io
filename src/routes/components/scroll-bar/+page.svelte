<script lang="ts">
  const html: string = "<h1>Scroll Bar</h1>\n\n<p>A scroll bar is a UI component that allows users to navigate vertically or horizontally through content that extends beyond the visible area of a container. The ScrollBar component provides a headless scrollbar track element with proper ARIA scrollbar semantics, including orientation, value range, and accessible labeling.</p>\n\n<p>This component renders the structural markup and ARIA attributes for a custom scrollbar. The consumer provides the visual thumb element as children and is responsible for implementing the scroll interaction logic (drag handling, position updates, and syncing with scrollable content). The component sets initial value attributes that the consumer should update dynamically.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> with <code>role=\"scrollbar\"</code> and full ARIA scrollbar attributes</li>\n<li>Supports <code>\"vertical\"</code> and <code>\"horizontal\"</code> orientations via <code>aria-orientation</code></li>\n<li>Includes <code>aria-valuenow</code>, <code>aria-valuemin</code>, and <code>aria-valuemax</code> for position tracking</li>\n<li>Initial values are set to 0/0/100; consumer should update these dynamically</li>\n<li>The <code>label</code> prop provides the accessible name via <code>aria-label</code></li>\n<li>Consumer provides the scrollbar thumb or track content via the <code>children</code> slot</li>\n<li>Rest props are spread onto the <code>&lt;div&gt;</code> element</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>orientation</code>: \"vertical\" | \"horizontal\" (default: \"vertical\") -- the scrollbar direction, reflected in <code>aria-orientation</code></li>\n<li><code>label</code>: string (required) -- accessible name for the scrollbar via <code>aria-label</code></li>\n<li><code>children</code>: slot (required) -- scrollbar content, typically the draggable thumb element</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;div&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Vertical scrollbar inside a chat message area:</p>\n\n<pre><code class=\"language-html\">&lt;ScrollArea label=\"Chat messages\" style=\"max-height: 400px; overflow: auto;\"&gt;\n  &lt;div class=\"messages\"&gt;&lt;!-- chat messages --&gt;&lt;/div&gt;\n  &lt;ScrollBar orientation=\"vertical\" label=\"Chat scroll\"&gt;\n    &lt;div class=\"scroll-thumb\"&gt;&lt;/div&gt;\n  &lt;/ScrollBar&gt;\n&lt;/ScrollArea&gt;</code></pre>\n\n<p>Horizontal scrollbar for a timeline strip:</p>\n\n<pre><code class=\"language-html\">&lt;ScrollArea label=\"Project timeline\" style=\"overflow-x: auto;\"&gt;\n  &lt;div class=\"timeline\"&gt;&lt;!-- timeline content --&gt;&lt;/div&gt;\n  &lt;ScrollBar orientation=\"horizontal\" label=\"Timeline scroll\"&gt;\n    &lt;div class=\"scroll-thumb\"&gt;&lt;/div&gt;\n  &lt;/ScrollBar&gt;\n&lt;/ScrollArea&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Arrow Up / Arrow Left: Scroll backward (decrease value) by a small increment</li>\n<li>Arrow Down / Arrow Right: Scroll forward (increase value) by a small increment</li>\n<li>Page Up: Scroll backward by a large increment</li>\n<li>Page Down: Scroll forward by a large increment</li>\n<li>Home: Scroll to the beginning (minimum value)</li>\n<li>End: Scroll to the end (maximum value)</li>\n</ul>\n\n<p>Note: The consumer is responsible for implementing keyboard event handlers that update the scrollbar value.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"scrollbar\"</code> -- identifies the element as a scrollbar control</li>\n<li><code>aria-label=\"...\"</code> -- provides the accessible name for the scrollbar</li>\n<li><code>aria-orientation=\"vertical|horizontal\"</code> -- indicates the scrollbar direction</li>\n<li><code>aria-valuenow=\"0\"</code> -- the current scroll position (consumer should update dynamically)</li>\n<li><code>aria-valuemin=\"0\"</code> -- the minimum scroll value</li>\n<li><code>aria-valuemax=\"100\"</code> -- the maximum scroll value</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use ScrollBar as a custom-styled scrollbar element within a ScrollArea container.</li>\n<li>Use ScrollBar when the native scrollbar appearance does not match your design system and you need visual consistency.</li>\n<li>Use ScrollBar to provide a scrollbar with full ARIA semantics for assistive technology users.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use ScrollBar outside of a ScrollArea -- it requires the scroll container context for proper functionality.</li>\n<li>Do not use ScrollBar when native scrollbars are sufficient -- custom scrollbars add implementation complexity for drag handling and position synchronization.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>The ScrollBar headless component provides a <code>&lt;div&gt;</code> with <code>role=\"scrollbar\"</code>, <code>aria-orientation</code>, <code>aria-valuenow</code>, <code>aria-valuemin</code>, <code>aria-valuemax</code>, and <code>aria-label</code> for full ARIA scrollbar semantics. The consumer provides the visual thumb element, drag interaction logic, and position synchronization with scrollable content.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.scroll-bar</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>scroll-bar</code></li>\n<li>Verify role=\"scrollbar\"` -- identifies the element as a scrollbar control</li>\n<li>Verify aria-label=\"...\"` -- provides the accessible name for the scrollbar</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Make the scrollbar thumb large enough to grab easily, and ensure sufficient contrast between the thumb and track for visibility.</li>\n<li><strong>Developers</strong>: Update <code>aria-valuenow</code> dynamically as the user scrolls, and implement keyboard event handlers for arrow keys, Page Up/Down, Home, and End.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>scroll-area</code> — a scrollable container with custom scrollbar support</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA scrollbar role: https://www.w3.org/TR/wai-aria-1.2/#scrollbar</li>\n<li>WAI-ARIA Practices - ScrollBar: https://www.w3.org/WAI/ARIA/apg/patterns/scrollbar/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"scroll-bar\" role=\"scrollbar\" aria-valuenow=\"50\"></div>";
  const svelteSource: string = "// In your Svelte component:\nimport ScrollBar from \"lily-design-system-svelte-headless/components/ScrollBar/ScrollBar.svelte\";\n\n<ScrollBar>\n  <!-- list children -->\n</ScrollBar>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Scroll Bar — Lily Design System</title>
  <meta name="description" content="A scroll bar is a UI component that allows users to navigate vertically or horizontally through content that extends beyond the visible area of a container. The ScrollBar component provides a headl..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

