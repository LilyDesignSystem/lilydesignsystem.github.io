<script lang="ts">
  const html: string = "<h1>Password Input Or Text Input Div</h1>\n\n<p>PasswordInputOrTextInputDiv is a headless component that wraps a native <code>&lt;input type=\"password\"&gt;</code> and <code>&lt;input type=\"text\"&gt;</code> element with accessible labelling and an optional toggle button to show or hide the password text. The input automatically includes <code>autocomplete=\"current-password\"</code> for proper browser password manager integration.</p>\n\n<p>This component is useful for login forms, registration forms, account settings, or any interface requiring secure password entry. The show/hide toggle improves usability by letting users verify their input, while the <code>aria-pressed</code> attribute on the toggle button communicates the visibility state to assistive technologies.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a wrapper <code>&lt;div&gt;</code> containing the password input and an optional toggle button</li>\n<li>The input type dynamically switches between <code>\"password\"</code> and <code>\"text\"</code> based on the <code>visible</code> state</li>\n<li>Uses reactive state to track password visibility internally</li>\n<li>Supports two-way binding on the <code>value</code> prop</li>\n<li>The toggle button uses <code>aria-pressed</code> to indicate the current visibility state</li>\n<li>Includes <code>autocomplete=\"current-password\"</code> on the input for browser password manager support</li>\n<li>Supports <code>required</code> and <code>disabled</code> boolean states on the input</li>\n<li>Spreads <code>restProps</code> onto the wrapper <code>&lt;div&gt;</code> for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name for the password input via <code>aria-label</code></li>\n<li><code>value</code>: string (default: <code>\"\"</code>) -- current password value; bindable with two-way <code>value</code> binding</li>\n<li><code>showToggle</code>: boolean (default: <code>true</code>) -- whether to render the show/hide toggle button</li>\n<li><code>toggleLabel</code>: string (default: <code>\"Show password\"</code>) -- accessible label for the toggle button</li>\n<li><code>required</code>: boolean (default: <code>false</code>) -- whether the input is required</li>\n<li><code>disabled</code>: boolean (default: <code>false</code>) -- whether the input is disabled</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the wrapper <code>&lt;div&gt;</code></li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;Field label=\"Password\"&gt;\n  &lt;PasswordInputOrTextInputDiv label=\"Password\" value={value} /&gt;\n&lt;/Field&gt;\n\n&lt;Field label=\"Create a password\"&gt;\n  &lt;Hint&gt;Must be at least 12 characters&lt;/Hint&gt;\n  &lt;PasswordInputOrTextInputDiv\n    label=\"Create a password\"\n    toggleLabel=\"Show password\"\n    value={value}\n  /&gt;\n&lt;/Field&gt;\n\n&lt;Field label=\"Confirm password\"&gt;\n  &lt;PasswordInputOrTextInputDiv\n    label=\"Confirm password\"\n    toggleLabel=\"Show password\"\n    value={confirmValue}\n  /&gt;\n&lt;/Field&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Enter/Space on the toggle button: Toggles password visibility between masked and plain text</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides an accessible name for the password input</li>\n<li><code>aria-label={toggleLabel}</code> -- provides an accessible name for the toggle button</li>\n<li><code>aria-pressed={visible}</code> -- indicates whether the password is currently visible (on the toggle button)</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use when you need a password field with an explicit show/hide toggle button that switches the input type between password and text</li>\n<li>Use when the toggle behaviour needs to be visible as a button alongside the input</li>\n<li>Use for login forms, registration flows, and account settings where users benefit from verifying their password entry</li>\n<li>Use when you want the consumer to have full control over the toggle button styling and positioning</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when a standard PasswordInput with built-in show/hide is sufficient</li>\n<li>Do not use for non-password fields -- use TextInput for plain text or PinInputDiv for codes</li>\n<li>Do not use for MFA codes, one-time passwords, or PINs -- use PinInputDiv instead</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>The PasswordInputOrTextInputDiv headless component provides a <code>&lt;div&gt;</code> wrapping a native <code>&lt;input&gt;</code> with dynamic type switching between <code>password</code> and <code>text</code>, <code>aria-label</code> for accessible naming, <code>aria-pressed</code> on the toggle button, and <code>autocomplete=\"current-password\"</code>. The consumer provides all visual styling for the input, toggle button, and wrapper layout.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.password-input-or-text-input-div</code> class for targeting. Common styling includes positioning the toggle button within or beside the input field.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a wrapper <code>&lt;div&gt;</code> containing an <code>&lt;input&gt;</code> element</li>\n<li>Verify the input <code>type</code> is <code>\"password\"</code> by default</li>\n<li>Verify <code>aria-label</code> is set from the <code>label</code> prop</li>\n<li>Verify the toggle button switches the input type between <code>\"password\"</code> and <code>\"text\"</code></li>\n<li>Verify <code>aria-pressed</code> on the toggle button reflects the visibility state</li>\n<li>Verify <code>disabled</code> and <code>required</code> attributes propagate correctly</li>\n<li>Verify pass-through attributes are applied to the wrapper <code>&lt;div&gt;</code></li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Position the show/hide toggle button clearly within or beside the input field, using a recognizable eye icon with sufficient contrast.</li>\n<li><strong>Developers</strong>: Set <code>autocomplete</code> to <code>\"new-password\"</code> for registration forms by overriding via <code>restProps</code> to help password managers distinguish between login and signup flows.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>password-input</code> — an input for entering a password with obscured text &lt;input type=\"password\"&gt;</li>\n<li><code>text-input</code> — a single-line text input field &lt;input type=\"text\"&gt;</li>\n<li><code>pin-input-div</code> — a series of single-digit inputs for entering a PIN or OTP code</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>HTML password input: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/password</li>\n<li>WAI-ARIA Button Pattern (toggle): https://www.w3.org/WAI/ARIA/apg/patterns/button/</li>\n</ul>";
</script>

<svelte:head>
  <title>Password Input Or Text Input Div — Lily Design System</title>
  <meta name="description" content="PasswordInputOrTextInputDiv is a headless component that wraps a native &lt;input type=&quot;password&quot;&gt; and &lt;input type=&quot;text&quot;&gt; element with accessible labelling and an optional toggle button to show or hi..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
