<script lang="ts">
  const html: string = "<h1>Contextual Help</h1>\n\n<p>A contextual help is a small help button — often a question-mark glyph — that opens a popover with explanatory content for a nearby field, control, or concept.</p>\n\n<p>Use it next to labels and headings where users may need extra explanation on demand without cluttering the primary UI. The help content is hidden by default and revealed only when the user requests it.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses a <code>&lt;div&gt;</code> wrapper with class <code>contextual-help</code></li>\n<li>The trigger is a <code>&lt;button class=\"contextual-help-trigger\" type=\"button\"&gt;</code> with <code>aria-haspopup=\"dialog\"</code></li>\n<li><code>expanded</code> reflects open state on the trigger via <code>aria-expanded</code></li>\n<li>The trigger references the panel via <code>aria-controls={panelId}</code></li>\n<li>The panel is a <code>&lt;div class=\"contextual-help-panel\" role=\"dialog\"&gt;</code> and is hidden via the <code>hidden</code> attribute when closed</li>\n<li>The panel id is generated stably via <code>useId</code>, <code>crypto.randomUUID</code>, or a Math.random helper</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (<strong>required</strong>) -- aria-label for the help trigger button</li>\n<li><code>expanded</code>: boolean (default <code>false</code>) -- whether the popover is open</li>\n<li><code>onClick</code> / <code>onclick</code>: callback (optional) -- trigger click handler</li>\n<li><code>children</code>: slot -- popover content</li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;ContextualHelp\n  label=\"Help for username\"\n  expanded={open}\n  onClick={handleToggle}\n&gt;\n  &lt;h3&gt;Username&lt;/h3&gt;\n  &lt;p&gt;Use 3-20 characters. Letters, numbers, and underscores only.&lt;/p&gt;\n&lt;/ContextualHelp&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab / Shift+Tab: Move focus into and out of the trigger and panel</li>\n<li>Enter / Space: Activate the trigger to toggle the popover</li>\n<li>Escape: (Consumer-provided) close the popover when open</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Trigger: <code>aria-label</code>, <code>aria-haspopup=\"dialog\"</code>, <code>aria-expanded</code>, <code>aria-controls={panelId}</code></li>\n<li>Panel: <code>role=\"dialog\"</code>, <code>id={panelId}</code>, <code>hidden</code> when closed</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to add optional explanatory content next to a field, label, or concept</li>\n<li>Use when help should not occupy persistent space in the layout</li>\n<li>Use when the help is short and self-contained</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for required instructions — show those inline as <code>Hint</code> text</li>\n<li>Do not use for blocking confirmations — use <code>Dialog</code> or <code>AlertDialog</code></li>\n<li>Do not use for guided tours — use <code>Coachmark</code> or <code>Tour</code></li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>Renders a <code>&lt;button&gt;</code> trigger and a <code>&lt;div role=\"dialog\"&gt;</code> panel linked by <code>aria-controls</code>. Open/close state, positioning relative to the trigger, animation, and visual treatment are entirely the consumer's responsibility.</p>\n\n<h2>Styles</h2>\n\n<p>Consumer CSS targets the <code>contextual-help</code> wrapper, <code>contextual-help-trigger</code> button, and <code>contextual-help-panel</code> panel classes. Provide a clear focus indicator on the trigger and ensure the panel is reachable by keyboard.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the wrapper renders a <code>&lt;div&gt;</code> with class <code>contextual-help</code></li>\n<li>Verify the trigger renders a <code>&lt;button&gt;</code> with <code>aria-label={label}</code></li>\n<li>Verify the trigger has <code>aria-haspopup=\"dialog\"</code></li>\n<li>Verify <code>aria-expanded</code> on the trigger reflects <code>expanded</code></li>\n<li>Verify <code>aria-controls</code> on the trigger matches the panel id</li>\n<li>Verify the panel renders with <code>role=\"dialog\"</code> and the matching id</li>\n<li>Verify the panel uses <code>hidden</code> when <code>expanded</code> is <code>false</code></li>\n<li>Verify the panel id is stable</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use a recognizable help affordance (e.g., a question mark in a circle); do not rely on color alone.</li>\n<li><strong>Developers</strong>: Generate panel ids server-side or with <code>useId</code> to remain hydration-stable.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>tooltip</code> — a small popup showing descriptive text on hover or focus</li>\n<li><code>popover</code> — a floating content box anchored to a trigger element</li>\n<li><code>details</code> — a disclosure widget that shows and hides content</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Dialog: https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/</li>\n<li>Adobe Spectrum Contextual Help: https://spectrum.adobe.com/page/contextual-help/</li>\n<li>WCAG 1.3.1 Info and Relationships: https://www.w3.org/WAI/WCAG22/Understanding/info-and-relationships</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"contextual-help\" aria-label=\"a help button that opens a popover with explanatory content\">ContextualHelp</div>";
  const svelteSource: string = "// In your Svelte component:\nimport ContextualHelp from \"lily-design-system-svelte-headless/components/ContextualHelp/ContextualHelp.svelte\";\n\n<ContextualHelp>\n  <!-- content -->\n</ContextualHelp>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Contextual Help — Lily Design System</title>
  <meta name="description" content="A contextual help is a small help button — often a question-mark glyph — that opens a popover with explanatory content for a nearby field, control, or concept." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

