<script lang="ts">
  const html: string = "<h1>DateTime View</h1>\n\n<p>A datetime view is a read-only display of a formatted date and/or time using the semantic <code>&lt;time&gt;</code> element so that browsers and assistive technology can understand the underlying ISO 8601 value while users see a localized human-friendly rendering supplied by the consumer.</p>\n\n<p>Use it whenever you display a timestamp (publish date, last update, event start) and want both machine-readable semantics and a presentation string that the consumer has formatted in the user's locale.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;time&gt;</code> element so the value is machine-readable</li>\n<li>Sets <code>datetime={value}</code> from the ISO 8601 string</li>\n<li>Display text falls back: <code>children</code> → <code>format</code> → <code>value</code></li>\n<li>Does not perform any localization — the consumer formats the displayed text</li>\n<li>Sets <code>class=\"date-time-view\"</code></li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>value</code>: string (required) -- ISO 8601 datetime</li>\n<li><code>format</code>: string (optional) -- pre-formatted display text</li>\n<li><code>label</code>: string (optional) -- aria-label override</li>\n<li><code>children</code>: slot (optional) -- content overrides <code>format</code></li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>ISO value with consumer-formatted display:</p>\n\n<pre><code class=\"language-html\">&lt;DateTimeView value=\"2026-04-27T14:30:00Z\" format=\"April 27, 2026 at 2:30 PM\" /&gt;</code></pre>\n\n<p>Children slot for richer rendering:</p>\n\n<pre><code class=\"language-html\">&lt;DateTimeView value=\"2026-04-27\"&gt;\n  &lt;strong&gt;27 Apr 2026&lt;/strong&gt;\n&lt;/DateTimeView&gt;</code></pre>\n\n<p>Falling back to the raw ISO value:</p>\n\n<pre><code class=\"language-html\">&lt;DateTimeView value=\"2026-04-27T14:30:00Z\" /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>None -- this is a passive read-only display.</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Implicit semantics from <code>&lt;time&gt;</code> with the <code>datetime</code> attribute</li>\n<li><code>aria-label</code> rendered only when the <code>label</code> prop is provided</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to display timestamps in articles, comments, activity feeds, and notifications.</li>\n<li>Use any time you want the rendered date to be machine-readable for browsers, search engines, and assistive technology.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for editable date inputs (use DateInput, DateTimeLocalInput, etc.).</li>\n<li>Do not use for date-range displays (use DateRange).</li>\n<li>Do not use to perform localization — format upstream and pass via <code>format</code> or <code>children</code>.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>Provides a single <code>&lt;time&gt;</code> element with the correct <code>datetime</code> attribute and display text fallback chain. Locale formatting, relative-time computation, and any visual treatment are the consumer's responsibility.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer styles the <code>date-time-view</code> class. A common pattern wraps the component to apply font-variant-numeric or letter-spacing for tabular dates.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;time&gt;</code> element with class <code>date-time-view</code></li>\n<li>Verify the <code>datetime</code> attribute equals the <code>value</code> prop</li>\n<li>Verify display text uses children if provided, then <code>format</code>, then <code>value</code></li>\n<li>Verify <code>aria-label</code> is set when the <code>label</code> prop is provided</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Decide whether absolute (\"April 27, 2026\") or relative (\"3 hours ago\") formatting fits the context. Pass either via <code>format</code> or <code>children</code>.</li>\n<li><strong>Developers</strong>: Always pass an ISO 8601 value to <code>value</code> so the underlying <code>datetime</code> attribute remains machine-readable, even if your display text is relative or partial.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>date-time-local-input</code> — an input for entering a date and time without time zone &lt;input type=\"datetime-local\"&gt;</li>\n<li><code>date-range</code> — a display of a start and end date range</li>\n<li><code>review-date</code> — a display of a content review date</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN time element: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/time</li>\n<li>ISO 8601: https://www.iso.org/iso-8601-date-and-time-format.html</li>\n</ul>";
</script>

<svelte:head>
  <title>DateTime View — Lily Design System</title>
  <meta name="description" content="A datetime view is a read-only display of a formatted date and/or time using the semantic &lt;time&gt; element so that browsers and assistive technology can understand the underlying ISO 8601 value while..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
