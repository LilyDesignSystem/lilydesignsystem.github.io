<script lang="ts">
  const html: string = "<h1>Don't List</h1>\n\n<p>A don't list presents discouraged actions or practices to avoid as a list of items. It is used in design system documentation, guidelines, and instructional content to clearly communicate what users or developers should not do. The list is paired with the DoList component to create do/don't guidance patterns.</p>\n\n<p>This component renders a semantically labeled unordered list with <code>role=\"list\"</code> to ensure assistive technology announces it as a list even when CSS may remove default list semantics. The default label is \"Don't\", making it immediately usable without configuration.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;ul&gt;</code> element with explicit <code>role=\"list\"</code> to preserve list semantics</li>\n<li>Uses <code>aria-label</code> to provide an accessible name (defaults to \"Don't\")</li>\n<li>Children slot is rendered inside the list; each child should be an <code>&lt;li&gt;</code> element</li>\n<li>Label prop has a sensible default but can be overridden for internationalization</li>\n<li>Spreads <code>restProps</code> on the <code>&lt;ul&gt;</code> element</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (default: <code>\"Don't\"</code>) -- accessible name for the list</li>\n<li><code>children</code>: slot (required) -- list items, each should be an <code>&lt;li&gt;</code> element</li>\n<li><code>...restProps</code>: any additional HTML attributes spread onto the <code>&lt;ul&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;DontList heading=\"Don't\"&gt;\n  &lt;DontListItem&gt;do not burst a blister yourself&lt;/DontListItem&gt;\n  &lt;DontListItem&gt;do not peel the skin off a burst blister&lt;/DontListItem&gt;\n  &lt;DontListItem&gt;do not pick at the edges of the remaining skin&lt;/DontListItem&gt;\n&lt;/DontList&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;DontList heading=\"Don't\"&gt;\n  &lt;DontListItem&gt;do not take aspirin for children under 16&lt;/DontListItem&gt;\n  &lt;DontListItem&gt;do not give more than the recommended dose&lt;/DontListItem&gt;\n&lt;/DontList&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;DontList label=\"Avoid\"&gt;\n  &lt;DontListItem&gt;Hard-code user-facing strings&lt;/DontListItem&gt;\n  &lt;DontListItem&gt;Rely on placeholder text as labels&lt;/DontListItem&gt;\n  &lt;DontListItem&gt;Remove focus indicators&lt;/DontListItem&gt;\n&lt;/DontList&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive container for list items.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"list\"</code> -- explicitly declares list semantics on the <code>&lt;ul&gt;</code>, ensuring assistive technology announces it as a list</li>\n<li><code>aria-label={label}</code> -- provides an accessible name for the list (defaults to \"Don't\")</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to give users a clear list of discouraged actions or things to avoid.</li>\n<li>Use alongside DoList to present contrasting do and don't guidance on the same topic.</li>\n<li>Use when there are 2 or more actions to discourage -- the list format reinforces the warning.</li>\n<li>Use with text starting with \"do not\" to make each prohibition clear.</li>\n<li>Use after any corresponding DoList -- dos come before don'ts.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use with only 1 item -- use WarningCallout for a single important warning or InsetText for general guidance.</li>\n<li>Do not use for critical safety warnings that could impact health -- use WarningCallout or CareCard instead.</li>\n<li>Do not use inside other interactive patterns like CareCard -- keep don't lists in their own section.</li>\n<li>Do not use for general-purpose lists -- use a standard <code>&lt;ul&gt;</code> or CheckList instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;ul&gt;</code> with explicit <code>role=\"list\"</code> to preserve list semantics even when CSS removes default markers, and <code>aria-label</code> for accessible naming. The consumer provides all visual styling, icons, and layout for the list and its items.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.dont-list</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;ol&gt;</code> element with class <code>dont-list</code></li>\n<li>Verify role=\"list\"<code> -- explicitly declares list semantics on the </code>&lt;ul&gt;`, ensuring assistive technology announces it as a list</li>\n<li>Verify aria-label={label}` -- provides an accessible name for the list (defaults to \"Don't\")</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use a distinct visual treatment (such as a red cross or warning color) to differentiate dont-list items from do-list items. Keep items concise and specific.</li>\n<li><strong>Developers</strong>: Place DontListItem components as children. Override the <code>label</code> prop for internationalization when the default \"Don't\" label is not appropriate.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>DontList composes with DontListItem using the List/ListItem pattern. DontList renders the <code>&lt;ul&gt;</code> container, and each DontListItem renders an <code>&lt;li&gt;</code> within it. Pair with DoList and DoListItem for complete do/don't guidance.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>dont-list-item</code> — a discouraged-practice item in a dont-list guideline list</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN ul element: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/ul</li>\n<li>WAI list role: https://www.w3.org/WAI/ARIA/apd/roles/list/</li>\n</ul>";
</script>

<svelte:head>
  <title>Don't List — Lily Design System</title>
  <meta name="description" content="A don't list presents discouraged actions or practices to avoid as a list of items. It is used in design system documentation, guidelines, and instructional content to clearly communicate what user..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
