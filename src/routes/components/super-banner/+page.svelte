<script lang="ts">
  const html: string = "<h1>SuperBanner</h1>\n\n<p>A super-banner communicates a high-priority state that affects an entire app, experience, process, or system. It is displayed prominently at the very top of the page, above all other content, to ensure maximum visibility for critical messages.</p>\n\n<p>This headless component uses a <code>&lt;div&gt;</code> element with <code>role=\"alert\"</code> for immediate screen reader announcement of high-priority information.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses <code>&lt;div&gt;</code> element with <code>role=\"alert\"</code> for immediate screen reader announcement</li>\n<li><code>aria-live=\"assertive\"</code> interrupts current speech to announce the message</li>\n<li>Positioned at the top of the page above all other content</li>\n<li>May include a dismiss button for user-dismissable banners</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (optional) -- accessible label for the super banner</li>\n<li><code>dismissable</code>: boolean (default: false) -- whether the banner can be dismissed</li>\n<li><code>ondismiss</code>: callback (optional) -- handler called when banner is dismissed</li>\n<li><code>children</code>: slot (required) -- banner content</li>\n<li><code>...restProps</code>: Any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;SuperBanner label=\"System outage\"&gt;\n  Our payment processing service is currently unavailable. We are working to restore it.\n&lt;/SuperBanner&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;SuperBanner label=\"Scheduled maintenance\" dismissable ondismiss={handleDismiss}&gt;\n  Planned maintenance on Saturday 12 April from 02:00 to 06:00 UTC. Save your work before then.\n&lt;/SuperBanner&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;SuperBanner label=\"Security alert\"&gt;\n  Your password was changed from a new device. If this was not you,\n  &lt;a href=\"/account/security\"&gt;review your account security&lt;/a&gt; immediately.\n&lt;/SuperBanner&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus the dismiss button (if dismissable)</li>\n<li>Enter/Space: Dismiss the banner (if dismissable)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"alert\"</code> -- announces the banner content immediately to screen readers</li>\n<li><code>aria-live=\"assertive\"</code> -- interrupts current speech for high-priority messages</li>\n<li><code>aria-label</code> -- optional description of the banner purpose</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to communicate a high-priority state that affects the entire application, such as a system outage or emergency alert</li>\n<li>Use for global maintenance notices that must be visible on every page</li>\n<li>Use when the message requires immediate attention and cannot be missed, such as a security incident or data breach notification</li>\n<li>Use for regulatory or legal notices that must interrupt the user experience until acknowledged</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for page-specific messages -- use Banner instead</li>\n<li>Do not use for form validation errors -- use ErrorSummary to summarize field errors</li>\n<li>Do not use for success confirmations -- use Banner with a success variant or Alert</li>\n<li>Do not use for routine notifications -- use Notification or Toast for transient messages</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides a <code>&lt;div&gt;</code> with <code>role=\"alert\"</code> and <code>aria-live=\"assertive\"</code> for immediate screen reader announcements, with zero visual styling. The consumer is responsible for all CSS including background color, text color, positioning, dismiss button, and responsive behavior.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.super-banner</code> class for targeting. No default styles are included -- this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>super-banner</code></li>\n<li>Verify <code>role=\"alert\"</code> is present</li>\n<li>Verify <code>aria-live=\"assertive\"</code> is present</li>\n<li>Verify dismiss functionality works when dismissable</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use bold, high-contrast colors to distinguish super banners from regular content. Keep the message concise and actionable. Position at the very top of the viewport.</li>\n<li><strong>Developers</strong>: Use <code>role=\"alert\"</code> for critical messages that need immediate attention. Provide a dismiss mechanism for non-persistent messages.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>announcement-banner</code> — a banner highlighting important messages for all users</li>\n<li><code>banner</code> — a prominent message bar across the top of a page</li>\n<li><code>banner-box</code> — a banner box that is inside a banner component, using flexbox horizontal</li>\n<li><code>government-banner</code> — a banner identifying a website as belonging to a government, with an expandable details panel</li>\n<li><code>medical-banner</code> — a prominent message bar across the top of a page, with medical information</li>\n<li><code>medical-banner-box</code> — a medical banner box that is inside a medical-banner component, using flexbox horizontal, with medical information</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/HTML/Element/div\">MDN div element</a></li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"super-banner\" role=\"alert\">Critical system message</div>";
  const svelteSource: string = "// In your Svelte component:\nimport SuperBanner from \"lily-design-system-svelte-headless/components/SuperBanner/SuperBanner.svelte\";\n\n<SuperBanner>\n  <!-- content -->\n</SuperBanner>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>SuperBanner — Lily Design System</title>
  <meta name="description" content="A super-banner communicates a high-priority state that affects an entire app, experience, process, or system. It is displayed prominently at the very top of the page, above all other content, to en..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

