<script lang="ts">
  const html: string = "<h1>PIN Input</h1>\n\n<p>A PIN input is a specialized form component that presents a series of single-character input fields for entering Personal Identification Numbers (PINs), verification codes, one-time passwords (OTPs), or similar short numeric codes. Each digit occupies its own input field, creating a segmented entry experience that clearly communicates the expected code length to the user.</p>\n\n<p>The component automatically advances focus to the next field when a digit is entered, supports Backspace to move back to the previous field, and allows Arrow key navigation between fields. The combined value of all digit fields is available as a single bindable string. This pattern is commonly used in two-factor authentication flows, SMS verification, and secure PIN entry interfaces. Each individual input field has an accessible label indicating its position (e.g., \"Digit 1 of 4\") to support screen reader users.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as <code>&lt;div role=\"group\"&gt;</code> containing individual single-digit <code>&lt;input&gt;</code> elements</li>\n<li>Uses reactive state for internal digits array and two-way binding for combined value</li>\n<li>Auto-focuses next input on digit entry</li>\n<li>Backspace in empty input moves focus to previous input</li>\n<li>ArrowLeft/ArrowRight navigate between inputs</li>\n<li>Only accepts numeric characters (0-9)</li>\n<li><code>inputmode=\"numeric\"</code> ensures numeric keyboard on mobile devices</li>\n<li><code>maxlength=\"1\"</code> restricts each input to a single character</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible label for the group via aria-label</li>\n<li><code>length</code>: number (default: 4) -- number of digit inputs</li>\n<li><code>value</code>: string (default: \"\") -- bindable combined string of all digits</li>\n<li><code>disabled</code>: boolean (default: false) -- disables all inputs</li>\n<li><code>...restProps</code>: Any additional HTML attributes on the group div</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Basic 4-digit PIN input:</p>\n\n<pre><code class=\"language-html\">&lt;PinInput label=\"Enter PIN\" value={pin} /&gt;\n&lt;p&gt;Entered PIN: {pin}&lt;/p&gt;</code></pre>\n\n<p>6-digit verification code for two-factor authentication:</p>\n\n<pre><code class=\"language-html\">&lt;PinInput label=\"Verification Code\" length={6} value={code} /&gt;</code></pre>\n\n<p>PIN input in a login verification form:</p>\n\n<pre><code class=\"language-html\">&lt;Form label=\"Verify your identity\" onsubmit={handleVerify}&gt;\n  &lt;p&gt;Enter the 6-digit code sent to your phone.&lt;/p&gt;\n  &lt;Field label=\"Verification code\"&gt;\n    &lt;PinInput label=\"Verification code\" length={6} value={verificationCode} /&gt;\n  &lt;/Field&gt;\n  &lt;Button type=\"submit\"&gt;Verify&lt;/Button&gt;\n  &lt;ActionLink href=\"/resend\"&gt;Resend code&lt;/ActionLink&gt;\n&lt;/Form&gt;</code></pre>\n\n<p>Disabled state while verifying:</p>\n\n<pre><code class=\"language-html\">&lt;PinInput label=\"Verifying code\" length={6} value={code} disabled /&gt;\n&lt;Loading&gt;Verifying...&lt;/Loading&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Digit entry: enters value and auto-focuses next input</li>\n<li>Backspace: clears current or moves to previous input</li>\n<li>ArrowLeft: focuses previous input</li>\n<li>ArrowRight: focuses next input</li>\n<li>Tab: standard tab navigation in/out of the group</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"group\"</code> with <code>aria-label</code> on the container</li>\n<li>Each input has <code>aria-label=\"Digit X of Y\"</code> for positional context</li>\n<li><code>inputmode=\"numeric\"</code> for numeric keyboard on mobile</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for two-factor authentication, SMS verification codes, and OTP entry flows.</li>\n<li>Use when you need a segmented input that clearly communicates the expected code length.</li>\n<li>Use for MFA flows where the user enters a time-based code from an authenticator app.</li>\n<li>Use for account verification during sign-up or password reset.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for passwords -- use PasswordInput instead.</li>\n<li>Do not use for general numeric entry -- use NumberInput instead.</li>\n<li>Do not use for reference numbers or codes longer than 8 digits -- use TextInput with <code>inputmode=\"numeric\"</code> instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>The PinInput headless component provides a <code>&lt;div role=\"group\"&gt;</code> containing individual <code>&lt;input&gt;</code> fields with <code>inputmode=\"numeric\"</code>, <code>maxlength=\"1\"</code>, per-digit <code>aria-label</code> attributes, and auto-focus navigation. The consumer provides all visual styling for the digit fields, spacing, and container layout.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.pin-input-div</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>pin-input-div</code></li>\n<li>Verify role=\"group\"<code> with </code>aria-label` on the container</li>\n<li>Verify Each input has <code>aria-label=\"Digit X of Y\"</code> for positional context</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Space digit fields evenly and make each field large enough for easy touch input; consider visual separators between groups (e.g., after the third digit in a 6-digit code).</li>\n<li><strong>Developers</strong>: Handle paste events to distribute a pasted code across all digit fields, and auto-submit the form when all digits are entered for a smoother user experience.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>password-input</code> — an input for entering a password with obscured text &lt;input type=\"password\"&gt;</li>\n<li><code>input-with-mask</code> — an input displaying a format mask placeholder for the user to fill in</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Group Role: https://www.w3.org/TR/wai-aria-1.2/#group</li>\n</ul>";
</script>

<svelte:head>
  <title>PIN Input — Lily Design System</title>
  <meta name="description" content="A PIN input is a specialized form component that presents a series of single-character input fields for entering Personal Identification Numbers (PINs), verification codes, one-time passwords (OTPs..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
