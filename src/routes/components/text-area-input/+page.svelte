<script lang="ts">
  const html: string = "<h1>TextAreaInput</h1>\n\n<p>A text-area-input is a headless component that wraps the native HTML <code>&lt;textarea&gt;</code> element, providing a multi-line text input area. It is commonly used in forms for comments, messages, descriptions, feedback, code input, and any scenario where users need to enter extended text content.</p>\n\n<p>Unlike a single-line text input, a text-area-input provides a larger, scrollable text area that supports multiple lines. The component uses <code>aria-label</code> for accessible naming and supports two-way binding on the value.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a single <code>&lt;textarea&gt;</code> element with no wrapper</li>\n<li>Uses <code>aria-label</code> for accessible naming instead of a visible <code>&lt;label&gt;</code> element</li>\n<li>Supports two-way binding on the <code>value</code> prop</li>\n<li>The <code>rows</code> prop controls the visible height of the text area</li>\n<li>Spreads <code>restProps</code> onto the text-area-input element for consumer extensibility</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name applied via <code>aria-label</code></li>\n<li><code>value</code>: string (default: \"\") -- bindable text content of the text-area-input</li>\n<li><code>rows</code>: number (default: undefined) -- number of visible text rows (browser default if unset)</li>\n<li><code>required</code>: boolean (default: false) -- whether the text-area-input is required for form submission</li>\n<li><code>disabled</code>: boolean (default: false) -- whether the text-area-input is disabled</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the text-area-input element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;Field label=\"Can you describe how you move about?\"&gt;\n  &lt;Hint&gt;For example, if you use crutches, sticks, or a walking frame&lt;/Hint&gt;\n  &lt;TextAreaInput label=\"Mobility description\" rows={5} /&gt;\n&lt;/Field&gt;\n\n&lt;Field label=\"Additional notes for the referral\"&gt;\n  &lt;Hint&gt;Include any relevant medical history or current medications&lt;/Hint&gt;\n  &lt;TextAreaInput label=\"Referral notes\" rows={8} /&gt;\n&lt;/Field&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Moves focus to and from the text-area-input (native browser behavior)</li>\n<li>All standard text editing keys function normally within the text-area-input (native browser behavior)</li>\n<li>Enter: Inserts a new line (does not submit forms, unlike single-line inputs)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides an accessible name for the text-area-input since there is no visible <code>&lt;label&gt;</code> element</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use when users need to enter multiple lines of text, such as a description, feedback, or clinical notes</li>\n<li>Use with a visible Label positioned above the text-area-input -- never use placeholder text as a substitute</li>\n<li>Use the <code>rows</code> attribute to set the text-area-input height proportional to the expected amount of text</li>\n<li>Use with Hint to guide what information to include, such as \"Describe your symptoms and when they started\"</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for single-line answers like names, phone numbers, or postcodes -- use TextInput instead</li>\n<li>Do not use when a character limit is needed -- use TextAreaInputWithCharacterCounter which pairs TextAreaInput with CharacterCounter</li>\n<li>Do not disable copy and paste -- users must be able to paste text (WCAG 2.2)</li>\n<li>Do not use without consulting your clinical team if the service involves health information -- users may disclose sensitive health concerns</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component renders a native <code>&lt;textarea&gt;</code> element with <code>aria-label</code>, two-way value binding, and configurable <code>rows</code>. It provides multi-line text input semantics with required/disabled states. The consumer provides all visual styling including borders, padding, resize behavior, and responsive sizing.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.text-area-input</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;textarea&gt;</code> element with class <code>text-area-input</code></li>\n<li>Verify aria-label={label}<code> -- provides an accessible name for the text-area-input since there is no visible </code>&lt;label&gt;` element</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Set a visible height (via <code>rows</code>) that reflects the expected content length. Allow vertical resizing so users can expand the area if needed.</li>\n<li><strong>Developers</strong>: Use the <code>rows</code> prop to set an appropriate initial height. Wrap the text-area-input with a Field and Label component for a complete accessible form field. Consider debouncing the bound value for expensive reactive operations.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>text-input</code> — a single-line text input field &lt;input type=\"text\"&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN text-area-input element: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/text-area-input</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<textarea class=\"text-area-input\" aria-label=\"Message\" rows=\"4\">Type your message here...</textarea>";
  const svelteSource: string = "// In your Svelte component:\nimport TextAreaInput from \"lily-design-system-svelte-headless/components/TextAreaInput/TextAreaInput.svelte\";\n\n<TextAreaInput label=\"Label\" />\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>TextAreaInput — Lily Design System</title>
  <meta name="description" content="A text-area-input is a headless component that wraps the native HTML &lt;textarea&gt; element, providing a multi-line text input area. It is commonly used in forms for comments, messages, descriptions, f..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

