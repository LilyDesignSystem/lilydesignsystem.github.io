<script lang="ts">
  const html: string = "<h1>Input</h1>\n\n<p>An input provides a headless text input field with accessible labeling and two-way value binding. It serves as the foundational form input component for text entry, supporting various HTML input types such as text, email, password, number, search, and more.</p>\n\n<p>The component supports bindable value state, required and disabled attributes, and spreads additional props for full consumer control over the native <code>&lt;input&gt;</code> element.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a single <code>&lt;input&gt;</code> element with the specified <code>type</code> attribute.</li>\n<li>Uses <code>aria-label</code> for accessible naming since there is no built-in visible <code>&lt;label&gt;</code> element.</li>\n<li>The <code>value</code> prop uses two-way binding of the input text.</li>\n<li>The <code>type</code> prop defaults to <code>\"text\"</code> but accepts any valid HTML input type string.</li>\n<li>Supports native <code>required</code> and <code>disabled</code> attributes.</li>\n<li>Spreads <code>restProps</code> onto the <code>&lt;input&gt;</code> for consumer customization (e.g., <code>placeholder</code>, <code>maxlength</code>, <code>pattern</code>).</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name applied via <code>aria-label</code>.</li>\n<li><code>value</code>: string (default: <code>\"\"</code>) -- bindable current input value.</li>\n<li><code>type</code>: string (default: <code>\"text\"</code>) -- HTML input type attribute (e.g., <code>\"text\"</code>, <code>\"email\"</code>, <code>\"password\"</code>, <code>\"number\"</code>).</li>\n<li><code>required</code>: boolean (default: <code>false</code>) -- whether the input is required for form submission.</li>\n<li><code>disabled</code>: boolean (default: <code>false</code>) -- whether the input is disabled.</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;input&gt;</code> element.</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Generic text input for a custom field type:</p>\n\n<pre><code class=\"language-html\">&lt;Input label=\"Reference code\" value={refCode} type=\"text\" required placeholder=\"e.g. ABC-1234\" /&gt;</code></pre>\n\n<p>Numeric input for a quantity field:</p>\n\n<pre><code class=\"language-html\">&lt;Input label=\"Quantity\" value={qty} type=\"number\" min=\"1\" max=\"100\" required /&gt;</code></pre>\n\n<p>Disabled input during form submission:</p>\n\n<pre><code class=\"language-html\">&lt;Input label=\"Account ID\" value={accountId} type=\"text\" disabled={isSubmitting} /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component uses the native <code>&lt;input&gt;</code> element, which provides built-in keyboard support (Tab to focus, standard text editing keys).</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label</code> -- provides an accessible name for the input when no visible label is present.</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use as a generic HTML input when a more specific input component (TextInput, EmailInput, etc.) is not available.</li>\n<li>Use as the foundational text entry component for forms, supporting any HTML input type.</li>\n<li>Use when you need a simple, single-line input with accessible labeling and two-way value binding.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when a specialised input exists -- prefer TextInput, EmailInput, TelInput, NumberInput, DateInput, or SearchInput instead.</li>\n<li>Do not use for multi-line text entry -- use TextAreaInput instead.</li>\n<li>Do not use for file selection -- use FileInput instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a native <code>&lt;input&gt;</code> element with <code>aria-label</code>, two-way value binding, and support for <code>required</code> and <code>disabled</code> attributes. The consumer provides all visual styling including borders, padding, focus states, and placeholder text appearance.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.input</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;input&gt;</code> element with class <code>input</code></li>\n<li>Verify aria-label` -- provides an accessible name for the input when no visible label is present.</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Ensure input fields have visible focus indicators and sufficient padding. Use consistent sizing across related form fields.</li>\n<li><strong>Developers</strong>: Prefer specialized input components (EmailInput, TelInput, etc.) when available, as they provide type-specific defaults. Use the <code>type</code> prop to leverage browser-native validation and mobile keyboard optimization.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>text-input</code> — a single-line text input field &lt;input type=\"text\"&gt;</li>\n<li><code>field</code> — a form field wrapper with label, input, and error message</li>\n<li><code>form</code> — a form element for collecting and submitting user data</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN Input element: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input</li>\n<li>WAI-ARIA Forms: https://www.w3.org/WAI/tutorials/forms/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<input class=\"input\" aria-label=\"Input\" />";
  const svelteSource: string = "// In your Svelte component:\nimport Input from \"lily-design-system-svelte-headless/components/Input/Input.svelte\";\n\n<Input label=\"Label\" />\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Input — Lily Design System</title>
  <meta name="description" content="An input provides a headless text input field with accessible labeling and two-way value binding. It serves as the foundational form input component for text entry, supporting various HTML input ty..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

