<script lang="ts">
  const html: string = "<h1>Warning Callout</h1>\n\n<p>A warning callout is a component used to draw attention to important alerts or potential issues that require user attention or caution. It highlights warnings, errors, or critical information that could affect the user's experience or actions, and may include additional text or links to help users understand the issue or take corrective actions.</p>\n\n<p>This headless component renders an <code>&lt;aside&gt;</code> element with <code>role=\"alert\"</code>, which causes screen readers to announce the content immediately when it appears. The consumer provides the warning content as children and is responsible for all visual styling.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders an <code>&lt;aside&gt;</code> element with <code>role=\"alert\"</code> for immediate screen reader announcements</li>\n<li>The <code>role=\"alert\"</code> implicitly sets <code>aria-live=\"assertive\"</code>, meaning content is announced immediately</li>\n<li>The <code>label</code> prop is optional since the alert content itself provides the message</li>\n<li>Accepts <code>...restProps</code> for forwarding additional attributes to the aside element</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (optional) -- accessible name applied via <code>aria-label</code>; useful when multiple alerts exist on the same page</li>\n<li><code>children</code>: slot (required) -- the warning content to display</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;WarningCallout heading=\"Important\"&gt;\n  &lt;p&gt;If you have a peanut allergy, do not take this medicine.\n  Talk to your doctor about alternative treatments.&lt;/p&gt;\n&lt;/WarningCallout&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;WarningCallout heading=\"School, nursery or playgroup\"&gt;\n  &lt;p&gt;Tell your child's school or nursery if they have been\n  diagnosed with chickenpox. Stay away from school until all\n  the blisters have crusted over, usually 5 days after the\n  spots first appeared.&lt;/p&gt;\n&lt;/WarningCallout&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive container for alert content.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"alert\"</code> -- identifies the content as an alert; screen readers announce it immediately when it appears or changes</li>\n<li><code>aria-label={label}</code> -- optional accessible name to distinguish this alert from others on the page</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for information that is time-critical and users must not miss</li>\n<li>Use for content that could significantly impact someone's health if overlooked</li>\n<li>Use to address common misconceptions or mistakes that could lead to harm</li>\n<li>Use on content pages where the warning stands out from surrounding body text</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use on transactional pages — the pattern is untested in that context</li>\n<li>Do not use when directing users to contact their GP or seek medical help — use CareCard instead</li>\n<li>Do not use for information that is not sufficiently important — use InsetText for general callouts</li>\n<li>Do not use for system-level alerts or error states — use Alert or Banner instead</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides an <code>&lt;aside&gt;</code> element with <code>role=\"alert\"</code> (which implicitly sets <code>aria-live=\"assertive\"</code>) for immediate screen reader announcements. The consumer provides all warning content as children and all visual styling including borders, background colors, and icons.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.warning-callout</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>warning-callout</code></li>\n<li>Verify role=\"alert\"` -- identifies the content as an alert; screen readers announce it immediately when it appears or changes</li>\n<li>Verify aria-label={label}` -- optional accessible name to distinguish this alert from others on the page</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use a distinct visual treatment (e.g., amber/yellow background, warning icon) to differentiate warnings from errors and informational messages. Ensure sufficient color contrast.</li>\n<li><strong>Developers</strong>: Content within <code>role=\"alert\"</code> is announced immediately by screen readers when it appears or changes. Avoid dynamically adding and removing WarningCallout rapidly, as each change triggers an announcement.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>alert</code> — a status message for important information or feedback</li>\n<li><code>information-callout</code> — a callout box highlighting informational content</li>\n<li><code>banner</code> — a prominent message bar across the top of a page</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Alert Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/alert/</li>\n<li>WAI-ARIA alert role: https://www.w3.org/TR/wai-aria-1.2/#alert</li>\n</ul>";
</script>

<svelte:head>
  <title>Warning Callout — Lily Design System</title>
  <meta name="description" content="A warning callout is a component used to draw attention to important alerts or potential issues that require user attention or caution. It highlights warnings, errors, or critical information that ..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
