<script lang="ts">
  const html: string = "<h1>Tab Bar</h1>\n\n<p>TabBar is a headless tab list container that provides the ARIA <code>tablist</code> role and built-in keyboard navigation for tab interfaces. It manages arrow key navigation between tab elements, allowing users to cycle through tabs with Left/Right arrow keys, and jump to the first or last tab with Home/End keys.</p>\n\n<p>Use TabBar when building a tabbed interface where users switch between different views or content panels. The component wraps tab buttons in a <code>&lt;div&gt;</code> with <code>role=\"tablist\"</code> and handles keyboard interactions automatically. Consumer provides tab buttons as children (each with <code>role=\"tab\"</code> and <code>aria-selected</code>) and is responsible for managing selection state and associating tab panels.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> element with <code>role=\"tablist\"</code> for ARIA semantics</li>\n<li>Uses two-way <code>this</code> binding to capture a reference to the tablist element for DOM queries</li>\n<li>Queries all child elements with <code>[role='tab']</code> to build the list of navigable tabs</li>\n<li>Implements keyboard navigation via an <code>onkeydown</code> handler:\n<ul>\n<li>ArrowRight moves focus to the next tab (wraps to first)</li>\n<li>ArrowLeft moves focus to the previous tab (wraps to last)</li>\n<li>Home moves focus to the first tab</li>\n<li>End moves focus to the last tab</li></li></ul>\n<li>Uses reactive state for the internal DOM reference (<code>tablistRef</code>)</li>\n<li>Consumer is responsible for managing <code>aria-selected</code> state on tab buttons and displaying corresponding tab panels</li>\n<li>Spreads <code>restProps</code> onto the div for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible label for the tab list, applied via <code>aria-label</code></li>\n<li><code>children</code>: slot (required) -- tab button elements to render inside the tablist</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;div&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;TabBar label=\"Medication information\"&gt;\n  &lt;TabBarButton selected controls=\"panel-about\"&gt;About this medicine&lt;/TabBarButton&gt;\n  &lt;TabBarButton controls=\"panel-dosage\"&gt;Dosage&lt;/TabBarButton&gt;\n  &lt;TabBarButton controls=\"panel-side-effects\"&gt;Side effects&lt;/TabBarButton&gt;\n  &lt;TabBarButton controls=\"panel-interactions\"&gt;Interactions&lt;/TabBarButton&gt;\n&lt;/TabBar&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Arrow Right: Moves focus to the next tab; wraps from the last tab to the first</li>\n<li>Arrow Left: Moves focus to the previous tab; wraps from the first tab to the last</li>\n<li>Home: Moves focus to the first tab</li>\n<li>End: Moves focus to the last tab</li>\n<li>Tab: Moves focus into and out of the tab list (only one tab in the list is in the tab order at a time)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"tablist\"</code> -- identifies the container as a list of tabs</li>\n<li><code>aria-label={label}</code> -- provides an accessible name for the tab list so screen readers announce its purpose</li>\n<li>Child tabs should have <code>role=\"tab\"</code> and <code>aria-selected</code> attributes (managed by consumer)</li>\n<li>Associated tab panels should have <code>role=\"tabpanel\"</code> with <code>aria-labelledby</code> pointing to the corresponding tab (managed by consumer)</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to let users navigate between related sections of content without leaving the page</li>\n<li>Use when content naturally divides into distinct categories that users will access one at a time</li>\n<li>Use to reduce page length by showing only the relevant section</li>\n<li>Use when there are 2-6 sections -- enough to justify tabs, few enough to remain scannable</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when users need to compare information across sections -- show all content on one page or use a Table</li>\n<li>Do not use with only one section -- show the content directly without tabs</li>\n<li>Do not use for sequential steps or progress -- use TaskList or a multi-page form with BackLink</li>\n<li>Do not use for navigation between different pages -- use NavigationMenu or BreadcrumbNav instead</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component renders a <code>&lt;div&gt;</code> with <code>role=\"tablist\"</code>, <code>aria-label</code>, and built-in arrow key navigation (Left/Right to cycle tabs, Home/End to jump to first/last). The consumer provides tab button children, manages selection state, associates tab panels, and applies all visual styling.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.tab-bar</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>tab-bar</code></li>\n<li>Verify role=\"tablist\"` -- identifies the container as a list of tabs</li>\n<li>Verify aria-label={label}` -- provides an accessible name for the tab list so screen readers announce its purpose</li>\n<li>Verify Child tabs should have <code>role=\"tab\"</code> and <code>aria-selected</code> attributes (managed by consumer)</li>\n<li>Verify Associated tab panels should have <code>role=\"tabpanel\"</code> with <code>aria-labelledby</code> pointing to the corresponding tab (managed by consumer)</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Clearly indicate the active tab with a visual treatment such as an underline, background color, or border. Keep tab labels short and descriptive.</li>\n<li><strong>Developers</strong>: Pair each TabBarButton with a corresponding <code>role=\"tabpanel\"</code> element linked via <code>aria-controls</code> and <code>aria-labelledby</code>. Manage the <code>aria-selected</code> state on child tab buttons from the consumer.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>TabBar uses the Bar/BarButton composition pattern. Place TabBarButton components as children inside TabBar. The TabBar provides the <code>role=\"tablist\"</code> container with keyboard navigation, while each TabBarButton provides <code>role=\"tab\"</code> with selection state and panel association.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>tab-bar-button</code> — one tab button in a tab group</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Tabs Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/tabs/</li>\n<li>WAI-ARIA Authoring Practices - Tabs Example: https://www.w3.org/WAI/ARIA/apg/patterns/tabs/examples/tabs-automatic/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"tab-bar\" role=\"tablist\"><button class=\"tab-bar-button\" role=\"tab\" aria-selected=\"true\">Tab 1</button><button class=\"tab-bar-button\" role=\"tab\">Tab 2</button></div>";
  const svelteSource: string = "// In your Svelte component:\nimport TabBar from \"lily-design-system-svelte-headless/components/TabBar/TabBar.svelte\";\n\n<TabBar>\n  <!-- list children -->\n</TabBar>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Tab Bar — Lily Design System</title>
  <meta name="description" content="TabBar is a headless tab list container that provides the ARIA tablist role and built-in keyboard navigation for tab interfaces. It manages arrow key navigation between tab elements, allowing users..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

