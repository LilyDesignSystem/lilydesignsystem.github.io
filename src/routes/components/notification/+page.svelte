<script lang="ts">
  const html: string = "<h1>Notification</h1>\n\n<p>A notification is a UI component used to deliver timely messages to users about events, updates, or actions within an application. The Notification component provides a headless wrapper with proper ARIA live region semantics, ensuring that screen readers announce notification content when it appears. It supports both polite (non-urgent) and assertive (urgent) announcements.</p>\n\n<p>The component uses <code>role=\"status\"</code> with <code>aria-live=\"polite\"</code> for standard notifications that wait for the user to finish their current task before being announced, and <code>role=\"alert\"</code> with <code>aria-live=\"assertive\"</code> for urgent notifications that interrupt the user immediately. This distinction is critical for appropriate screen reader behavior.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> element with a live region role</li>\n<li>Uses <code>role=\"status\"</code> and <code>aria-live=\"polite\"</code> by default for non-urgent notifications</li>\n<li>When <code>urgent</code> is true, switches to <code>role=\"alert\"</code> and <code>aria-live=\"assertive\"</code></li>\n<li>The <code>label</code> prop provides an accessible name via <code>aria-label</code></li>\n<li>Content is provided via the <code>children</code> slot</li>\n<li>Rest props are spread onto the <code>&lt;div&gt;</code> element</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (optional) -- accessible name for the notification region via <code>aria-label</code></li>\n<li><code>urgent</code>: boolean (default: false) -- when true, uses <code>role=\"alert\"</code> and <code>aria-live=\"assertive\"</code> instead of <code>role=\"status\"</code> and <code>aria-live=\"polite\"</code></li>\n<li><code>children</code>: slot (required) -- the notification message content</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;div&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;!-- Non-urgent notification for new messages --&gt;\n&lt;Notification label=\"New messages\"&gt;\n  You have 3 unread messages\n&lt;/Notification&gt;\n\n&lt;!-- Non-urgent success notification --&gt;\n&lt;Notification label=\"Success\"&gt;\n  Your changes have been saved.\n&lt;/Notification&gt;\n\n&lt;!-- Urgent session expiry warning --&gt;\n&lt;Notification label=\"Session warning\" urgent&gt;\n  Your session will expire in 5 minutes.\n  &lt;a href=\"/session/extend\"&gt;Extend session&lt;/a&gt;.\n&lt;/Notification&gt;\n\n&lt;!-- Urgent error notification --&gt;\n&lt;Notification label=\"Error\" urgent&gt;\n  Something went wrong. Please try again.\n&lt;/Notification&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive announcement container. Notifications are announced automatically by screen readers via ARIA live regions without requiring user interaction.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"status\"</code> -- applied by default for non-urgent notifications; politely announces content changes</li>\n<li><code>role=\"alert\"</code> -- applied when <code>urgent</code> is true; assertively interrupts the user</li>\n<li><code>aria-label=\"...\"</code> -- provides an accessible name for the notification region</li>\n<li><code>aria-live=\"polite\"</code> -- default live region politeness; waits for user idle before announcing</li>\n<li><code>aria-live=\"assertive\"</code> -- applied when <code>urgent</code> is true; announces immediately</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to inform users about events or updates that occurred outside their current context, such as a new message or status change</li>\n<li>Use for brief, time-sensitive messages that appear temporarily and auto-dismiss</li>\n<li>Use when the notification does not require immediate user action -- it is informational only</li>\n<li>Use with <code>urgent</code> for critical messages that must immediately interrupt the user, such as errors or security alerts</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for page-level status messages -- use Banner for persistent service-wide information</li>\n<li>Do not use for form validation errors -- use ErrorMessage and ErrorSummary instead</li>\n<li>Do not use for critical warnings that must not be missed -- use Alert or AlertDialog for urgent messages requiring acknowledgment</li>\n<li>Do not use for success confirmations on form completion -- use Banner with success variant or Panel instead</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;div&gt;</code> with ARIA live region semantics -- <code>role=\"status\"</code> with <code>aria-live=\"polite\"</code> by default, or <code>role=\"alert\"</code> with <code>aria-live=\"assertive\"</code> when urgent. The consumer provides all visual styling, including colors, icons, animations, positioning, and auto-dismiss behavior.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.notification</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>notification</code></li>\n<li>Verify role=\"status\"` -- applied by default for non-urgent notifications; politely announces content changes</li>\n<li>Verify role=\"alert\"<code> -- applied when </code>urgent` is true; assertively interrupts the user</li>\n<li>Verify aria-label=\"...\"` -- provides an accessible name for the notification region</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use distinct visual styles for different notification types (success, error, warning, info) and ensure notifications do not obscure important page content.</li>\n<li><strong>Developers</strong>: Use the <code>urgent</code> prop only for messages requiring immediate attention. Polite notifications wait for the user to finish their current task before being announced.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>toast</code> — a brief auto-dismissing notification message</li>\n<li><code>alert</code> — a status message for important information or feedback</li>\n<li><code>banner</code> — a prominent message bar across the top of a page</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA alert role: https://www.w3.org/TR/wai-aria-1.2/#alert</li>\n<li>WAI-ARIA status role: https://www.w3.org/TR/wai-aria-1.2/#status</li>\n<li>WAI-ARIA Live Regions: https://www.w3.org/WAI/ARIA/apd/practices/live-regions/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"notification\" role=\"status\">You have new messages</div>";
  const svelteSource: string = "// In your Svelte component:\nimport Notification from \"lily-design-system-svelte-headless/components/Notification/Notification.svelte\";\n\n<Notification>\n  <!-- content -->\n</Notification>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Notification — Lily Design System</title>
  <meta name="description" content="A notification is a UI component used to deliver timely messages to users about events, updates, or actions within an application. The Notification component provides a headless wrapper with proper..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

