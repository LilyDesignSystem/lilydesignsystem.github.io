<script lang="ts">
  const html: string = "<h1>Task Bar Button</h1>\n\n<p>A task bar item is a single button within a task bar, rendered as a native <code>&lt;button&gt;</code> element for proper keyboard and screen reader support. It is designed to be placed inside a TaskBar container that manages layout and grouping.</p>\n\n<p>The component uses <code>&lt;button type=\"button\"&gt;</code> to avoid unintended form submissions and supports the <code>disabled</code> attribute for disabling interaction. Content is provided through the children slot, allowing full flexibility in button content.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as a <code>&lt;button type=\"button\"&gt;</code> element for native button semantics</li>\n<li>Supports a <code>disabled</code> prop to disable the button</li>\n<li>Content is provided through child elements</li>\n<li>Passes through all additional HTML attributes via <code>...restProps</code></li>\n<li>Designed to be placed inside a TaskBar container</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>disabled</code>: boolean (default: false) -- whether the button is disabled</li>\n<li><code>children</code>: slot (required) -- button content</li>\n<li><code>...restProps</code>: Any additional HTML attributes passed to the <code>&lt;button&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;TaskBar label=\"Document actions\"&gt;\n  &lt;TaskBarButton&gt;New&lt;/TaskBarButton&gt;\n  &lt;TaskBarButton&gt;Save&lt;/TaskBarButton&gt;\n  &lt;TaskBarButton disabled&gt;Delete&lt;/TaskBarButton&gt;\n&lt;/TaskBar&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;TaskBar label=\"Patient actions\"&gt;\n  &lt;TaskBarButton onclick={handleReferral}&gt;New Referral&lt;/TaskBarButton&gt;\n  &lt;TaskBarButton onclick={handleAppointment}&gt;Book Appointment&lt;/TaskBarButton&gt;\n&lt;/TaskBar&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus the button</li>\n<li>Enter / Space: Activate the button (browser default)</li>\n<li>Button is skipped in tab order when <code>disabled</code> is true</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Implicit <code>button</code> role from the <code>&lt;button&gt;</code> element</li>\n<li><code>disabled</code> attribute communicates disabled state to assistive technology</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside TaskBar to represent one task shortcut or quick action.</li>\n<li>Use for individual action buttons such as \"New\", \"Open\", \"Save\", or domain-specific operations.</li>\n<li>Use when a button should be grouped within a toolbar context for proper assistive technology announcements.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside TaskBar -- use Button for standalone actions.</li>\n<li>Do not use for navigation links -- use ActionLink instead.</li>\n<li>Do not use for toggle actions -- use ToggleButton instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component renders a <code>&lt;button type=\"button\"&gt;</code> element with <code>disabled</code> support and a children slot for content. It provides native button semantics without accidental form submission. The consumer provides all visual styling including colors, sizing, icons, and hover/focus states.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.task-bar-button</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;button&gt;</code> element with class <code>task-bar-button</code></li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use consistent button sizing within the task bar. Consider icon-only buttons with tooltips when horizontal space is limited.</li>\n<li><strong>Developers</strong>: Use <code>type=\"button\"</code> (already set) to prevent accidental form submissions. Handle click events via <code>onclick</code> in <code>restProps</code> for action behavior.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>TaskBarButton is designed to be used as a child of TaskBar. The TaskBar provides the <code>role=\"toolbar\"</code> container, and each TaskBarButton provides an individual action button within it.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>task-bar</code> — a horizontal bar of task shortcuts or actions</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/HTML/Element/button\">MDN button element</a></li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<button class=\"task-bar-button\">Task</button>";
  const svelteSource: string = "// In your Svelte component:\nimport TaskBarButton from \"lily-design-system-svelte-headless/components/TaskBarButton/TaskBarButton.svelte\";\n\n<TaskBarButton>TaskBarButton</TaskBarButton>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Task Bar Button — Lily Design System</title>
  <meta name="description" content="A task bar item is a single button within a task bar, rendered as a native &lt;button&gt; element for proper keyboard and screen reader support. It is designed to be placed inside a TaskBar container tha..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

