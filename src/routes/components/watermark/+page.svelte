<script lang="ts">
  const html: string = "<h1>Watermark</h1>\n\n<p>A watermark is a decorative repeating overlay text or image marking a page. The headless component exposes the props as data attributes and the right structural ARIA; the consumer implements the actual repeating pattern in CSS.</p>\n\n<p>Use it for branding, draft markers, confidential overlays, or visual identifiers that span a page or region.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div class=\"watermark\"&gt;</code> container with <code>data-rotate</code> and <code>data-gap</code> attributes</li>\n<li>Inside the container, an overlay <code>&lt;div class=\"watermark-overlay\" aria-hidden=\"true\"&gt;</code> carries <code>data-text</code> and <code>data-image-url</code></li>\n<li>The overlay is decorative and hidden from assistive tech</li>\n<li>Children render after the overlay</li>\n<li>Consumer is responsible for the actual repeating pattern (e.g., SVG-encoded background-image)</li>\n<li><code>gap</code> defaults to <code>\"100px\"</code> and <code>rotate</code> defaults to <code>-22</code></li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>text</code>: string (optional) -- watermark text</li>\n<li><code>imageUrl</code>: string (optional) -- watermark image URL (alternative to text)</li>\n<li><code>gap</code>: string (default: \"100px\") -- spacing between repeats (CSS length)</li>\n<li><code>rotate</code>: number (default: -22) -- rotation angle in degrees</li>\n<li><code>children</code>: slot -- content beneath the watermark</li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;Watermark text=\"DRAFT\" rotate={-22} gap=\"120px\"&gt;\n  &lt;article&gt;...&lt;/article&gt;\n&lt;/Watermark&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;Watermark imageUrl=\"/brand.svg\" rotate={0} gap=\"200px\"&gt;\n  &lt;main&gt;...&lt;/main&gt;\n&lt;/Watermark&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>None — purely decorative.</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>The watermark overlay has <code>aria-hidden=\"true\"</code> so screen readers ignore it</li>\n<li>The container itself has no role</li>\n<li>Underlying content remains fully accessible</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for visible-but-decorative branding or status markers (DRAFT, CONFIDENTIAL).</li>\n<li>Use when the watermark must repeat across a region of the page.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for content that screen readers must announce — that content belongs in the document flow.</li>\n<li>Do not use as a privacy or security control — watermarks are visual only.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>Renders only the structural markup and exposes props as data attributes. The consumer implements the repeating background pattern (e.g., a background-image that uses an SVG generated from <code>data-text</code>) and any animation, opacity, or color treatment.</p>\n\n<h2>Styles</h2>\n\n<p>Consumer CSS targets <code>watermark</code> and <code>watermark-overlay</code>. Use a CSS gradient or repeating background-image to implement the pattern; consume <code>data-rotate</code> and <code>data-gap</code> via attribute selectors or custom properties.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>watermark</code></li>\n<li>Verify <code>data-rotate</code> reflects the <code>rotate</code> prop (default -22)</li>\n<li>Verify <code>data-gap</code> reflects the <code>gap</code> prop (default \"100px\")</li>\n<li>Verify the inner <code>&lt;div class=\"watermark-overlay\"&gt;</code> has <code>aria-hidden=\"true\"</code></li>\n<li>Verify <code>data-text</code> is set on the overlay when <code>text</code> is provided</li>\n<li>Verify <code>data-image-url</code> is set on the overlay when <code>imageUrl</code> is provided</li>\n<li>Verify children render after the overlay element</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Choose a low-contrast watermark so it does not interfere with reading. Test at small and large viewports.</li>\n<li><strong>Developers</strong>: Encode the watermark as an inline SVG background-image for crisp rendering. Use the data attributes to drive the CSS.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>banner</code> — a prominent message bar across the top of a page</li>\n<li><code>image</code> — an image element with alt text</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA aria-hidden: https://www.w3.org/TR/wai-aria-1.2/#aria-hidden</li>\n<li>Ant Design Watermark: https://ant.design/components/watermark</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"watermark\" aria-label=\"a decorative repeating overlay text or image marking a page\">Watermark</div>";
  const svelteSource: string = "// In your Svelte component:\nimport Watermark from \"lily-design-system-svelte-headless/components/Watermark/Watermark.svelte\";\n\n<Watermark>\n  <!-- content -->\n</Watermark>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Watermark — Lily Design System</title>
  <meta name="description" content="A watermark is a decorative repeating overlay text or image marking a page. The headless component exposes the props as data attributes and the right structural ARIA; the consumer implements the ac..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

