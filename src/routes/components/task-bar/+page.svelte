<script lang="ts">
  const html: string = "<h1>Task Bar</h1>\n\n<p>A task bar is a toolbar-style container for task-related action buttons, using the ARIA <code>toolbar</code> role with accessible labeling. It renders as a <code>&lt;div&gt;</code> with <code>role=\"toolbar\"</code> to group related task actions together, making the container semantically meaningful to assistive technologies.</p>\n\n<p>Task bars are commonly used to provide quick access to frequently performed actions such as \"New\", \"Open\", \"Save\", or other task-specific operations. The component works with TaskBarItem child components or plain buttons.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as a <code>&lt;div&gt;</code> element with <code>role=\"toolbar\"</code> to identify it as a toolbar widget</li>\n<li>Accessible name is set via <code>aria-label</code> from the required <code>label</code> prop</li>\n<li>Content is provided through the children slot, typically TaskBarItem components or buttons</li>\n<li>Does not implement arrow key navigation itself; consumers can add keyboard navigation as needed</li>\n<li>Passes through all additional HTML attributes via <code>...restProps</code></li>\n<li>Uses <code>slot</code> for children content</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name for the toolbar via aria-label</li>\n<li><code>children</code>: slot (required) -- task bar item elements</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;TaskBar label=\"Document actions\"&gt;\n  &lt;TaskBarButton&gt;New&lt;/TaskBarButton&gt;\n  &lt;TaskBarButton&gt;Open&lt;/TaskBarButton&gt;\n  &lt;TaskBarButton&gt;Save&lt;/TaskBarButton&gt;\n  &lt;TaskBarButton&gt;Print&lt;/TaskBarButton&gt;\n&lt;/TaskBar&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;TaskBar label=\"Patient record shortcuts\"&gt;\n  &lt;TaskBarButton&gt;New Referral&lt;/TaskBarButton&gt;\n  &lt;TaskBarButton&gt;Book Appointment&lt;/TaskBarButton&gt;\n  &lt;TaskBarButton&gt;Request Test&lt;/TaskBarButton&gt;\n  &lt;TaskBarButton disabled&gt;Discharge&lt;/TaskBarButton&gt;\n&lt;/TaskBar&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus moves to the first focusable item within the toolbar (native browser behavior)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"toolbar\"</code> -- identifies the container as a toolbar widget, grouping related controls</li>\n<li><code>aria-label</code> -- provides the accessible name for the toolbar, set from the <code>label</code> prop</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for a horizontal bar of task shortcuts or quick actions such as \"New\", \"Open\", \"Save\".</li>\n<li>Use for application-level quick access to frequently performed operations.</li>\n<li>Use when related action buttons should be semantically grouped as a toolbar for assistive technologies.</li>\n<li>Use when task actions are domain-specific operations (e.g., patient workflows, document management).</li>\n<li>Use with TaskBarButton children to provide consistent button styling within the bar.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for primary navigation -- use NavigationMenu instead.</li>\n<li>Do not use for tool actions such as text formatting -- use ToolBar instead.</li>\n<li>Do not use for tab-based content switching -- use TabBar instead.</li>\n<li>Do not use for a single action -- use Button instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component renders a <code>&lt;div&gt;</code> with <code>role=\"toolbar\"</code> and <code>aria-label</code> to identify it as a toolbar widget for assistive technologies. The consumer provides all child buttons, keyboard navigation (if arrow key support is desired), and visual styling including layout, spacing, and button appearance.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.task-bar</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>task-bar</code></li>\n<li>Verify role=\"toolbar\"` -- identifies the container as a toolbar widget, grouping related controls</li>\n<li>Verify aria-label<code> -- provides the accessible name for the toolbar, set from the </code>label` prop</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Arrange buttons in a logical order of frequency or workflow. Use icon+label or icon-only buttons with tooltips for space efficiency.</li>\n<li><strong>Developers</strong>: Place TaskBarButton components or plain buttons as children. Consider implementing arrow key navigation within the toolbar for full WAI-ARIA toolbar compliance.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>TaskBar uses the Bar/BarButton composition pattern. Place TaskBarButton components as children inside TaskBar. The TaskBar provides the <code>role=\"toolbar\"</code> container with an accessible label, while each TaskBarButton provides an individual action button within the toolbar.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>task-bar-button</code> — one item in a task bar</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Toolbar Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/toolbar/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"task-bar\" role=\"toolbar\" aria-label=\"Tasks\"><button class=\"task-bar-button\">New</button><button class=\"task-bar-button\">Open</button></div>";
  const svelteSource: string = "// In your Svelte component:\nimport TaskBar from \"lily-design-system-svelte-headless/components/TaskBar/TaskBar.svelte\";\n\n<TaskBar>\n  <!-- list children -->\n</TaskBar>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Task Bar — Lily Design System</title>
  <meta name="description" content="A task bar is a toolbar-style container for task-related action buttons, using the ARIA toolbar role with accessible labeling. It renders as a &lt;div&gt; with role=&quot;toolbar&quot; to group related task action..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

