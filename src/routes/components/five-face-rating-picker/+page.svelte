<script lang="ts">
  const html: string = "<h1>Five Face Rating Picker</h1>\n\n<p>A five-face rating input allows users to select a rating from 1 to 5 using labeled radio buttons representing faces from \"Very bad\" to \"Very good\". This pattern is commonly used in satisfaction surveys, feedback forms, and experience ratings where face-based labels are more intuitive than numeric scales. The component uses radio buttons grouped in a fieldset for proper accessibility and keyboard navigation.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;fieldset&gt;</code> with <code>role=\"radiogroup\"</code> containing 5 <code>&lt;label&gt;</code>/<code>&lt;input type=\"radio\"&gt;</code> pairs</li>\n<li>Supports two-way binding for the selected rating value (1-5, with 0 meaning no selection)</li>\n<li>Default face labels are \"Very bad\", \"Bad\", \"Okay\", \"Good\", \"Very good\" -- customizable via the <code>labels</code> prop</li>\n<li>Each radio button displays the corresponding label text for screen readers and visual users</li>\n<li>The <code>name</code> prop groups the radio buttons for form submission</li>\n<li>Supports <code>disabled</code> state on the fieldset level</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name for the rating group via <code>aria-label</code></li>\n<li><code>value</code>: number (default: <code>0</code>) -- current rating (1-5, 0 = none), two-way bindable via <code>value</code></li>\n<li><code>name</code>: string (default: <code>\"face-rating\"</code>) -- radio group name for form submission</li>\n<li><code>labels</code>: string[] (default: <code>[\"Very bad\", \"Bad\", \"Okay\", \"Good\", \"Very good\"]</code>) -- accessible labels for each face</li>\n<li><code>disabled</code>: boolean (default: <code>false</code>) -- whether the entire group is disabled</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;fieldset&gt;</code></li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;FiveFaceRatingPicker label=\"How was your experience?\" value={rating}&gt;\n    &lt;FiveFaceRatingPickerButton value=\"1\" label=\"Very bad\" onclick={handleClick} /&gt;\n    &lt;FiveFaceRatingPickerButton value=\"2\" label=\"Bad\" onclick={handleClick} /&gt;\n    &lt;FiveFaceRatingPickerButton value=\"3\" label=\"Okay\" onclick={handleClick} /&gt;\n    &lt;FiveFaceRatingPickerButton value=\"4\" label=\"Good\" onclick={handleClick} /&gt;\n    &lt;FiveFaceRatingPickerButton value=\"5\" label=\"Very good\" onclick={handleClick} /&gt;\n&lt;/FiveFaceRatingPicker&gt;</code></pre>\n\n<p>With custom labels for internationalization:</p>\n\n<pre><code class=\"language-html\">&lt;FiveFaceRatingPicker label=\"Wie war Ihre Erfahrung?\" value={rating} labels={[\"Sehr schlecht\", \"Schlecht\", \"Okay\", \"Gut\", \"Sehr gut\"]} /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>Standard radio group keyboard behavior provided by the browser:</p>\n\n<ul>\n<li>Tab: focus the selected radio (or first if none selected)</li>\n<li>ArrowDown / ArrowRight: move to next radio button</li>\n<li>ArrowUp / ArrowLeft: move to previous radio button</li>\n<li>Space: select the focused radio button</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"radiogroup\"</code> -- on the <code>&lt;fieldset&gt;</code>, identifies the group as a set of radio buttons</li>\n<li><code>aria-label=\"...\"</code> -- provides an accessible name for the entire rating group</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use in satisfaction surveys to collect feedback using face-based labels that are more intuitive than numeric scales.</li>\n<li>Use in patient experience questionnaires where expressive face icons communicate sentiment clearly.</li>\n<li>Use when you need a 1-5 rating with descriptive labels like \"Very bad\" through \"Very good\".</li>\n<li>Use in post-interaction feedback forms such as support ticket follow-ups or appointment reviews.</li>\n<li>Use when the audience includes users who may find faces more accessible than stars or numbers.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for display-only ratings -- use FiveFaceRatingView instead.</li>\n<li>Do not use for generic product or service reviews where stars are the expected convention -- use FiveStarRatingPicker.</li>\n<li>Do not use for 0-10 scale survey questions -- use NetPromoterScorePicker.</li>\n<li>Do not use for traffic-light status indicators -- use RedAmberGreenPicker.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;fieldset&gt;</code> with <code>role=\"radiogroup\"</code>, five <code>&lt;label&gt;</code>/<code>&lt;input type=\"radio\"&gt;</code> pairs with customizable face labels, two-way value binding, and <code>disabled</code> support. The consumer provides all visual styling, face icons or illustrations, and layout.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.five-face-rating-picker</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>five-face-rating-picker</code></li>\n<li>Verify role=\"radiogroup\"<code> -- on the </code>&lt;fieldset&gt;`, identifies the group as a set of radio buttons</li>\n<li>Verify aria-label=\"...\"` -- provides an accessible name for the entire rating group</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use distinct face illustrations or emoji for each level to make the scale visually intuitive. Ensure the faces are large enough to be easily tapped on touch devices.</li>\n<li><strong>Developers</strong>: Customize the <code>labels</code> prop for internationalization. The <code>name</code> prop groups the radio buttons for form submission. Use the <code>value</code> binding to track the selected rating.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>FiveFaceRatingPicker composes with FiveFaceRatingPickerButton using the Picker/PickerButton pattern. The picker provides the group container with <code>role=\"radiogroup\"</code>, and each picker button represents one selectable face rating level.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>five-face-rating-picker-button</code> — a picker button for selecting a 1-5 satisfaction rating using face labels</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Radio Group Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/radiobutton/</li>\n</ul>";
</script>

<svelte:head>
  <title>Five Face Rating Picker — Lily Design System</title>
  <meta name="description" content="A five-face rating input allows users to select a rating from 1 to 5 using labeled radio buttons representing faces from &quot;Very bad&quot; to &quot;Very good&quot;. This pattern is commonly used in satisfaction sur..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
