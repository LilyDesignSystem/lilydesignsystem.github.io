<script lang="ts">
  const html: string = "<h1>Progress Spinner</h1>\n\n<p>A progress spinner indicates an ongoing loading or processing state where the completion time is unknown. Unlike ProgressCircle which shows a specific percentage, a spinner indicates indeterminate loading. It uses <code>role=\"status\"</code> and <code>aria-live=\"polite\"</code> to announce the loading state to screen readers without interrupting the user. The component is headless, allowing consumers to apply their own spinning animation via CSS.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> with <code>role=\"status\"</code> for implicit live region semantics</li>\n<li>Uses <code>aria-live=\"polite\"</code> to ensure screen readers announce the loading state without interruption</li>\n<li>Provides <code>aria-label</code> for accessible naming of the loading state</li>\n<li>Accepts an optional <code>children</code> slot for content inside (e.g., loading text)</li>\n<li>Spreads <code>restProps</code> for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name describing the loading state via <code>aria-label</code></li>\n<li><code>children</code>: slot (default: <code>undefined</code>) -- optional content rendered inside (e.g., \"Loading...\")</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;div&gt;</code></li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Inline spinner while fetching search results:</p>\n\n<pre><code class=\"language-html\">&lt;ProgressSpinner label=\"Searching patients\" /&gt;</code></pre>\n\n<p>Spinner with visible loading text:</p>\n\n<pre><code class=\"language-html\">&lt;ProgressSpinner label=\"Saving changes\"&gt;\n  &lt;span&gt;Saving...&lt;/span&gt;\n&lt;/ProgressSpinner&gt;</code></pre>\n\n<p>Spinner inside a button area during form submission:</p>\n\n<pre><code class=\"language-html\">&lt;ProgressSpinner label=\"Submitting form\" /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive, read-only status indicator.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"status\"</code> -- identifies the element as a status region that receives non-urgent updates</li>\n<li><code>aria-label=\"...\"</code> -- provides an accessible name describing what is loading</li>\n<li><code>aria-live=\"polite\"</code> -- ensures content changes are announced by screen readers at the next opportunity without interrupting</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use ProgressSpinner for indeterminate loading states where the completion time is unknown, such as data fetching or background processing.</li>\n<li>Use ProgressSpinner as a compact, inline loading indicator next to a button or field while an async action completes.</li>\n<li>Use ProgressSpinner when submitting a form and waiting for a server response.</li>\n<li>Use ProgressSpinner inside a table cell or card to indicate that a single item is refreshing.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use ProgressSpinner when the completion percentage is known -- use Progress or ProgressCircle instead.</li>\n<li>Do not use ProgressSpinner for page-level or section-level loading overlays -- use Loading instead.</li>\n<li>Do not use ProgressSpinner for content placeholders that should match the layout shape -- use Skeleton instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>The ProgressSpinner headless component provides a <code>&lt;div&gt;</code> with <code>role=\"status\"</code>, <code>aria-live=\"polite\"</code>, and <code>aria-label</code> for accessible naming. It announces loading state changes to screen readers without interrupting. The consumer provides all visual styling, including the spinning animation via CSS.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.progress-spinner</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>progress-spinner</code></li>\n<li>Verify role=\"status\"` -- identifies the element as a status region that receives non-urgent updates</li>\n<li>Verify aria-label=\"...\"` -- provides an accessible name describing what is loading</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Keep the spinner visually simple and use smooth animation; pair with a visible \"Loading...\" label for clarity in larger content areas.</li>\n<li><strong>Developers</strong>: Place the spinner near the content it represents, and remove or hide it promptly when loading completes to avoid stale status announcements.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>loading</code> — a loading indicator, such as text, or image, or animation</li>\n<li><code>progress</code> — a horizontal progress bar showing completion</li>\n<li><code>skeleton</code> — a placeholder loading animation for content</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA <code>status</code> role: https://www.w3.org/WAI/ARIA/apd/roles/status/</li>\n<li>MDN <code>aria-live</code>: https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/Attributes/aria-live</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"progress-spinner\" role=\"status\" aria-label=\"Loading\">\u27f3</div>";
  const svelteSource: string = "// In your Svelte component:\nimport ProgressSpinner from \"lily-design-system-svelte-headless/components/ProgressSpinner/ProgressSpinner.svelte\";\n\n<ProgressSpinner>\n  <!-- content -->\n</ProgressSpinner>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Progress Spinner — Lily Design System</title>
  <meta name="description" content="A progress spinner indicates an ongoing loading or processing state where the completion time is unknown. Unlike ProgressCircle which shows a specific percentage, a spinner indicates indeterminate ..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

