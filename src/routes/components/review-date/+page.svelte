<script lang="ts">
  const html: string = "<h1>Review Date</h1>\n\n<p>A review date component displays a date associated with a review, audit, or scheduled check-in. It renders as a semantic HTML <code>&lt;time&gt;</code> element with a machine-readable <code>datetime</code> attribute in ISO 8601 format and human-readable display text provided through a children slot. This separation allows browsers and assistive technologies to parse the date for accurate interpretation while displaying a user-friendly formatted date.</p>\n\n<p>The component is commonly used in dashboards, compliance tracking, audit logs, and scheduling interfaces where dates need to be both human-readable and machine-parseable.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as <code>&lt;time aria-label={label} datetime={datetime}&gt;</code> with children slot</li>\n<li>The <code>datetime</code> attribute uses ISO 8601 format for machine readability</li>\n<li>Children slot provides localized human-readable date text</li>\n<li>Semantic <code>&lt;time&gt;</code> element enables browser and assistive technology date parsing</li>\n<li>All additional HTML attributes are spread onto the time element via <code>restProps</code></li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible label for screen readers via <code>aria-label</code></li>\n<li><code>datetime</code>: string (required) -- ISO 8601 date/time string for machine readability</li>\n<li><code>children</code>: slot (required) -- human-readable display text for the date</li>\n<li><code>...restProps</code>: any -- additional HTML attributes spread onto the time element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;!-- Display last reviewed and next review dates at the bottom of a content page --&gt;\n&lt;p&gt;\n  Page last reviewed:\n  &lt;ReviewDate label=\"Last reviewed\" datetime=\"2025-03-15\"&gt;15 March 2025&lt;/ReviewDate&gt;\n&lt;/p&gt;\n&lt;p&gt;\n  Next review due:\n  &lt;ReviewDate label=\"Next review due\" datetime=\"2028-03-15\"&gt;15 March 2028&lt;/ReviewDate&gt;\n&lt;/p&gt;\n\n&lt;!-- Single review date in a compliance dashboard --&gt;\n&lt;ReviewDate label=\"Last audit\" datetime=\"2025-01-08\"&gt;8 January 2025&lt;/ReviewDate&gt;\n\n&lt;!-- Dynamic date with locale-formatted display text --&gt;\n&lt;ReviewDate label=\"Next review\" datetime={isoDate}&gt;{formattedDate}&lt;/ReviewDate&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this is an informational display, not interactive.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label</code> -- provides screen reader context from the <code>label</code> prop</li>\n<li><code>datetime</code> -- machine-readable date attribute on the <code>&lt;time&gt;</code> element</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use on content pages to reassure users that the information has been recently reviewed and is up to date</li>\n<li>Use when user research shows that knowing the review date increases trust in the content</li>\n<li>Use at the bottom of the page as low-priority supplementary information</li>\n<li>Use to display both the last reviewed date and the next review due date</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use on transactional pages -- review dates are not meaningful in form-based journeys</li>\n<li>Do not use when only part of a page has changed -- the review date should reflect a full page review</li>\n<li>Do not use for real-time or frequently changing content -- the static date pattern does not suit dynamic data</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>The ReviewDate headless component provides a <code>&lt;time&gt;</code> element with <code>aria-label</code> for accessible naming and a <code>datetime</code> attribute in ISO 8601 format for machine readability. The children slot renders human-readable text. The consumer provides all visual styling and date formatting.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.review-date</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;span&gt;</code> element with class <code>review-date</code></li>\n<li>Verify aria-label<code> -- provides screen reader context from the </code>label` prop</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Display dates in a format familiar to your target audience (e.g., \"June 15, 2025\" for US, \"15 June 2025\" for UK) and visually distinguish upcoming vs. past review dates.</li>\n<li><strong>Developers</strong>: Always pass a valid ISO 8601 string to <code>datetime</code> and format the children text using locale-appropriate date formatting for human readability.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>date-time-view</code> — a read-only display of a formatted date and time</li>\n<li><code>date-time-view</code> — see components.tsv</li>\n<li><code>byline</code> — see components.tsv</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/HTML/Element/span\">MDN span element</a></li>\n</ul>";
</script>

<svelte:head>
  <title>Review Date — Lily Design System</title>
  <meta name="description" content="A review date component displays a date associated with a review, audit, or scheduled check-in. It renders as a semantic HTML &lt;time&gt; element with a machine-readable datetime attribute in ISO 8601 f..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
