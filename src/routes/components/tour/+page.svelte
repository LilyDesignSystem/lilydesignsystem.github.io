<script lang="ts">
  const html: string = "<h1>Tour</h1>\n\n<p>Tour is a headless component that serves as a container for a guided tour experience. It wraps TourList and TourListItem components to provide a step-by-step walkthrough of features, sightseeing pathways, or product demonstrations.</p>\n\n<p>Use this component as the outer wrapper for guided tours, onboarding flows, feature walkthroughs, or any sequential step-by-step experience.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> element as the tour container</li>\n<li>Uses <code>aria-label</code> for an accessible name describing the tour</li>\n<li>Accepts <code>className</code> for CSS class targeting</li>\n<li>Spreads <code>restProps</code> onto the <code>&lt;div&gt;</code> element for consumer customization</li>\n<li>Children slot contains TourList with TourListItem steps</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name for the tour via <code>aria-label</code></li>\n<li><code>className</code>: string (default: <code>\"\"</code>) -- CSS class name for the container</li>\n<li><code>children</code>: slot (required) -- tour content, typically a TourList</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;div&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;Tour label=\"Patient record system walkthrough\"&gt;\n  &lt;TourList label=\"Getting started\" active={showTour}&gt;\n    &lt;TourListItem label=\"Welcome\" current={step === 0} stepNumber={1} totalSteps={3}&gt;\n      &lt;p&gt;Welcome to the patient record system. This tour will show you the key features.&lt;/p&gt;\n      &lt;Button onclick={() =&gt; step++}&gt;Next&lt;/Button&gt;\n      &lt;Button onclick={() =&gt; showTour = false}&gt;Skip tour&lt;/Button&gt;\n    &lt;/TourListItem&gt;\n    &lt;TourListItem label=\"Search patients\" current={step === 1} stepNumber={2} totalSteps={3}&gt;\n      &lt;p&gt;Use the search bar to find patients by name, NHS number, or date of birth.&lt;/p&gt;\n      &lt;Button onclick={() =&gt; step--}&gt;Back&lt;/Button&gt;\n      &lt;Button onclick={() =&gt; step++}&gt;Next&lt;/Button&gt;\n    &lt;/TourListItem&gt;\n    &lt;TourListItem label=\"View record\" current={step === 2} stepNumber={3} totalSteps={3}&gt;\n      &lt;p&gt;Click a patient name to open their full record, including demographics and appointments.&lt;/p&gt;\n      &lt;Button onclick={() =&gt; step--}&gt;Back&lt;/Button&gt;\n      &lt;Button onclick={() =&gt; showTour = false}&gt;Finish&lt;/Button&gt;\n    &lt;/TourListItem&gt;\n  &lt;/TourList&gt;\n&lt;/Tour&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>Standard keyboard interactions provided by child components. The Tour container itself is not interactive.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides an accessible name for the tour container</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for guided walkthroughs for onboarding, feature discovery, or demonstration paths.</li>\n<li>Use when new users need step-by-step introduction to an interface.</li>\n<li>Use with TourList and TourListItem children to define the tour steps.</li>\n<li>Use for first-time user experiences, new feature announcements, or contextual help.</li>\n<li>Use when guidance needs a structured, sequential format with progress indicators.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for sequential task completion -- use TaskList instead.</li>\n<li>Do not use for page navigation or table of contents -- use ContentsNav instead.</li>\n<li>Do not use for simple ordered lists -- use CheckList instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component renders a <code>&lt;div&gt;</code> with <code>aria-label</code>. The consumer provides all visual styling including layout, progress indicators, step highlights, and navigation controls.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.tour</code> class for targeting.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element</li>\n<li>Verify <code>aria-label</code> is set from the <code>label</code> prop</li>\n<li>Verify children content is rendered</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Include progress indicators (e.g., step 2 of 5) and clear navigation controls (next, previous, skip). Highlight the relevant UI element for each step.</li>\n<li><strong>Developers</strong>: Manage tour state (current step, completed steps) in the consumer. Use TourList and TourListItem for semantic structure.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>Tour is the parent container in the Tour pattern: Tour &gt; TourList &gt; TourListItem.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>tour-list</code> — an ordered list of tour guide steps</li>\n<li><code>tour-list-item</code> — one step in a tour guide list</li>\n<li><code>coachmark</code> — an anchored popover that spotlights and explains a single feature</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Practices: https://www.w3.org/WAI/ARIA/apg/</li>\n</ul>";
</script>

<svelte:head>
  <title>Tour — Lily Design System</title>
  <meta name="description" content="Tour is a headless component that serves as a container for a guided tour experience. It wraps TourList and TourListItem components to provide a step-by-step walkthrough of features, sightseeing pa..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
