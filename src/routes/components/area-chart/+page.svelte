<script lang="ts">
  const html: string = "<h1>AreaChart</h1>\n\n<p>A continuous-data visualization where the area below a line is filled, emphasising the magnitude of values over time rather than just the shape of the trend. Use it when the cumulative size of the value matters — page views, revenue, energy consumption — particularly when comparing two or three stacked or overlaid series.</p>\n\n<p>The component is a headless wrapper around an inline <code>&lt;svg&gt;</code>: it renders a <code>&lt;figure role=\"img\"&gt;</code> with the chart inside, names the figure via <code>aria-label</code> (from <code>label</code>), and references an extended <code>description</code> and an optional <code>dataTable</code> slot via <code>aria-describedby</code>. The data table is the canonical accessible alternative — screen readers, users on text-only browsers, and exports all benefit from a real <code>&lt;table&gt;</code> rendering of the same numbers, and Lily encourages always providing it.</p>\n\n<p>For trends without magnitude emphasis use <code>LineChart</code>; for categorical comparisons use <code>BarChart</code> or <code>ColumnChart</code>; for tiny inline trend marks use <code>Sparkline</code>.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;figure role=\"img\"&gt;</code> containing an inline <code>&lt;svg&gt;</code> rendering of the area chart</li>\n<li>The figure has an accessible name from the <code>label</code> prop and an extended description from the <code>description</code> prop</li>\n<li>Data is supplied as <code>series</code> (one or more series of <code>{ x, y }</code> points)</li>\n<li>A textual data table can be rendered inside the same figure via the <code>dataTable</code> slot for screen readers and as a fallback when SVG is not available</li>\n<li>Spreads <code>restProps</code> onto the root <code>&lt;figure&gt;</code></li>\n</ul>\n\n<h2>Props</h2>\n\n<table>\n<thead>\n<tr><th>Prop</th><th>Type</th><th>Default</th><th>Description</th></tr>\n</thead>\n<tbody>\n<tr><td><code>label</code></td><td>string (required)</td><td>—</td><td>Accessible name for the chart</td></tr>\n<tr><td><code>description</code></td><td>string</td><td>—</td><td>Extended description (becomes a <code>&lt;figcaption&gt;</code> and is referenced via <code>aria-describedby</code>)</td></tr>\n<tr><td><code>series</code></td><td>array of series</td><td>[]</td><td>Each series is <code>{ name: string; points: { x: number; y: number }[] }</code></td></tr>\n<tr><td><code>dataTable</code></td><td>slot</td><td>—</td><td>Optional fallback <code>&lt;table&gt;</code> summarising the data</td></tr>\n<tr><td><code>...restProps</code></td><td>HTML attributes</td><td>—</td><td>Spread onto the root <code>&lt;figure&gt;</code></td></tr>\n</tbody>\n</table>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;AreaChart label=\"Daily visits, last 30 days\" series={visitSeries} description=\"Daily visit count for the past 30 days\" /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>No keyboard interactions on the rendered area itself</li>\n<li>If a data table is rendered, it follows native table keyboard behaviour</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"img\"</code> exposes the chart as a single image</li>\n<li><code>aria-label</code> (from <code>label</code>) names the chart</li>\n<li><code>aria-describedby</code> references the chart caption / data table</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Showing trends over time where the magnitude of the area emphasises the size of the value</li>\n<li>Comparing two or three series with stacked or overlaid areas</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Use <code>LineChart</code> when only the trend (not magnitude) matters</li>\n<li>Use <code>BarChart</code> or <code>ColumnChart</code> for discrete categorical data</li>\n<li>Use <code>Sparkline</code> for tiny inline trend indicators</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component renders semantic HTML with appropriate ARIA wiring. The consumer provides all visual styling — no CSS, animations, or layout assumptions are baked in.</p>\n\n<h2>Styles</h2>\n\n<p>The component renders with <code>.area-chart</code> as the root class. No default styles are included.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>line-chart</code> — line chart for trend without area emphasis</li>\n<li><code>bar-chart</code> / <code>column-chart</code> — categorical comparisons</li>\n<li><code>scatter-chart</code> — relationship between two variables</li>\n<li><code>graphic-block</code> — chart wrapper with title/notes</li>\n<li><code>sparkline</code> — tiny inline trend</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/HTML/Element/figure\">MDN figure element</a></li>\n</ul>";
</script>

<svelte:head>
  <title>AreaChart — Lily Design System</title>
  <meta name="description" content="A continuous-data visualization where the area below a line is filled, emphasising the magnitude of values over time rather than just the shape of the trend. Use it when the cumulative size of the ..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
