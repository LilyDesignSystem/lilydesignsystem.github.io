<script lang="ts">
  const html: string = "<h1>Listbox</h1>\n\n<p>A listbox presents a list of selectable options using the ARIA listbox role, with full keyboard navigation support. It is used when users need to select one or more items from a visible list, as opposed to a dropdown select. Common use cases include settings panels, filter selections, multi-select interfaces, and any scenario where all options should be visible simultaneously.</p>\n\n<p>The component manages focus movement between options via arrow keys, with Home and End keys to jump to the first and last items. The consumer provides option elements as children and handles selection state externally.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> with <code>role=\"listbox\"</code> and an accessible label.</li>\n<li>Uses reactive state to track the <code>listRef</code> element reference for querying child options.</li>\n<li>Keyboard navigation queries all child elements with <code>[role='option']</code> to build the navigable item list.</li>\n<li>Arrow keys wrap around: pressing ArrowDown on the last item moves focus to the first, and vice versa.</li>\n<li>Home and End keys jump to the first and last option respectively.</li>\n<li>Spreads <code>restProps</code> onto the container for consumer customization.</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name applied via <code>aria-label</code>.</li>\n<li><code>children</code>: slot (required) -- option elements to render inside the listbox (should have <code>role=\"option\"</code> and <code>tabindex=\"-1\"</code>).</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the container <code>&lt;div&gt;</code>.</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Priority level selector:</p>\n\n<pre><code class=\"language-html\">&lt;Listbox label=\"Select priority\"&gt;\n    &lt;div role=\"option\" tabindex=\"-1\" aria-selected=\"true\"&gt;High&lt;/div&gt;\n    &lt;div role=\"option\" tabindex=\"-1\" aria-selected=\"false\"&gt;Medium&lt;/div&gt;\n    &lt;div role=\"option\" tabindex=\"-1\" aria-selected=\"false\"&gt;Low&lt;/div&gt;\n&lt;/Listbox&gt;</code></pre>\n\n<p>Department filter in a settings panel:</p>\n\n<pre><code class=\"language-html\">&lt;Listbox label=\"Filter by department\"&gt;\n    {#each departments as dept}\n        &lt;div\n            role=\"option\"\n            tabindex=\"-1\"\n            aria-selected={selectedDept === dept}\n            onclick={() =&gt; selectedDept = dept}\n        &gt;\n            {dept}\n        &lt;/div&gt;\n    {/each}\n&lt;/Listbox&gt;</code></pre>\n\n<p>Theme selection with visible options:</p>\n\n<pre><code class=\"language-html\">&lt;Listbox label=\"Choose a theme\"&gt;\n    &lt;div role=\"option\" tabindex=\"-1\" aria-selected=\"false\"&gt;Light&lt;/div&gt;\n    &lt;div role=\"option\" tabindex=\"-1\" aria-selected=\"false\"&gt;Dark&lt;/div&gt;\n    &lt;div role=\"option\" tabindex=\"-1\" aria-selected=\"true\"&gt;System&lt;/div&gt;\n&lt;/Listbox&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>ArrowDown: Moves focus to the next option, wrapping to the first option from the last.</li>\n<li>ArrowUp: Moves focus to the previous option, wrapping to the last option from the first.</li>\n<li>Home: Moves focus to the first option.</li>\n<li>End: Moves focus to the last option.</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"listbox\"</code> -- identifies the container as a listbox widget for selecting from a list of options.</li>\n<li><code>aria-label</code> -- provides an accessible name describing the purpose of the listbox.</li>\n<li>Child elements should use <code>role=\"option\"</code> and optionally <code>aria-selected</code> to indicate selection state.</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use Listbox when users need to select one or more items from a visible list of options, such as filter panels or settings.</li>\n<li>Use Listbox instead of a Select when all options should be visible simultaneously without opening a dropdown.</li>\n<li>Use Listbox for preference or settings panels where the user chooses from a short to medium list.</li>\n<li>Use Listbox for multi-select interfaces where users need to see all available options at once.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use Listbox for dropdown selection where space is limited -- use Select or Combobox instead.</li>\n<li>Do not use Listbox for checkable items with independent on/off states -- use CheckboxGroup instead.</li>\n<li>Do not use Listbox for very long option lists that benefit from type-ahead filtering -- use Combobox instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides the ARIA listbox role, keyboard navigation (arrow keys, Home, End with wrapping), and focus management for child option elements. The consumer provides all visual styling, including option appearance, selected-state highlighting, scrolling behavior, and focus indicators.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.listbox</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>listbox</code></li>\n<li>Verify role=\"listbox\"` -- identifies the container as a listbox widget for selecting from a list of options.</li>\n<li>Verify aria-label` -- provides an accessible name describing the purpose of the listbox.</li>\n<li>Verify Child elements should use <code>role=\"option\"</code> and optionally <code>aria-selected</code> to indicate selection state.</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Provide clear visual distinction for the selected and focused option states, using both color and a non-color indicator to support color-blind users.</li>\n<li><strong>Developers</strong>: Ensure each child option uses <code>role=\"option\"</code> with <code>tabindex=\"-1\"</code> and manage <code>aria-selected</code> on options to reflect the current selection state.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>select</code> — a dropdown select element for choosing one option</li>\n<li><code>combobox</code> — a text input combined with a dropdown list for filtering options</li>\n<li><code>option</code> — an option element within a select dropdown</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Listbox Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/listbox/</li>\n</ul>";
</script>

<svelte:head>
  <title>Listbox — Lily Design System</title>
  <meta name="description" content="A listbox presents a list of selectable options using the ARIA listbox role, with full keyboard navigation support. It is used when users need to select one or more items from a visible list, as op..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
