<script lang="ts">
  const html: string = "<h1>Tag</h1>\n\n<p>A tag is a small component used to label, categorize, or indicate the status of an item. Tags are commonly used in content management systems, product listings, article metadata, and filtering interfaces to visually group or classify content. They typically display short text such as category names, keywords, status labels, or attribute values.</p>\n\n<p>Tags help users quickly scan and identify relevant information, and are presented as inline elements alongside other content. This headless component renders as a <code>&lt;span&gt;</code> with <code>role=\"status\"</code> and an accessible label, providing semantic structure for screen reader announcements while consumers apply their own visual styling.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as a <code>&lt;span&gt;</code> element with <code>role=\"status\"</code> and <code>aria-label</code></li>\n<li><code>role=\"status\"</code> creates an implicit live region, so screen readers announce content changes</li>\n<li>All visible content is provided through the <code>children</code> slot</li>\n<li>Uses props for prop destructuring with rest props spread</li>\n<li>No hardcoded strings; all text comes through props for internationalization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible label for screen readers via <code>aria-label</code></li>\n<li><code>children</code>: slot (required) -- the tag's visible content (text, etc.)</li>\n<li><code>...restProps</code>: any -- additional HTML attributes spread onto the <code>&lt;span&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;Tag label=\"Status: Completed\"&gt;Completed&lt;/Tag&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;Tag label=\"Status: Overdue\"&gt;Overdue&lt;/Tag&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;Tag label=\"Status: Active\"&gt;Active&lt;/Tag&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;Tag label=\"Status: Inactive\"&gt;Inactive&lt;/Tag&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>None -- tags are informational, not interactive</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"status\"</code> -- identifies this as a live region for screen reader announcements of content changes</li>\n<li><code>aria-label</code> -- provides an accessible name from the <code>label</code> prop, giving expanded context beyond the visible tag text</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use when it is useful for users to know the status of an item, such as \"Completed\", \"Active\", or \"Urgent\"</li>\n<li>Use when items can have more than one status and users need to distinguish between them at a glance</li>\n<li>Use adjective-based labels (e.g. \"Active\", \"Overdue\") to avoid implying the tag is interactive</li>\n<li>Use within task lists, tables, or summary cards to communicate progress or state</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use as interactive elements -- tags are read-only status indicators, not buttons or links</li>\n<li>Do not use when a single status is always the same for every item -- there is nothing to distinguish</li>\n<li>Do not use for categorisation or filtering labels -- use Badge instead for labelling and categorising</li>\n<li>Do not use too many statuses -- keep the number minimal so users can remember their meaning</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component renders a <code>&lt;span&gt;</code> with <code>role=\"status\"</code> and <code>aria-label</code>, creating an implicit live region for screen reader announcements. The consumer provides all visual styling including background color, border radius, padding, and typography.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.tag</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;span&gt;</code> element with class <code>tag</code></li>\n<li>Verify role=\"status\"` -- identifies this as a live region for screen reader announcements of content changes</li>\n<li>Verify aria-label<code> -- provides an accessible name from the </code>label` prop, giving expanded context beyond the visible tag text</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use color-coding consistently to distinguish tag categories, but do not rely on color alone -- include text labels. Keep tags compact with consistent padding and font size.</li>\n<li><strong>Developers</strong>: Use the <code>label</code> prop to provide expanded context for screen readers beyond the visible tag text (e.g., <code>label=\"Priority: High\"</code> with visible text \"High\"). Be aware that <code>role=\"status\"</code> creates a live region, so dynamic content changes will be announced.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>Tag is designed to be used as a child of TagGroup. The TagGroup provides a <code>role=\"group\"</code> container with an accessible label, while each Tag provides an individual labeled status element within the group.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>tag-group</code> — a group of tag elements</li>\n<li><code>tag-input</code> — an input for adding and removing tags</li>\n<li><code>badge</code> — a small label for counts, statuses, or categories</li>\n<li><code>status-tag</code> — a tag showing the current status of a task</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Status Role: https://www.w3.org/TR/wai-aria-1.2/#status</li>\n</ul>";
</script>

<svelte:head>
  <title>Tag — Lily Design System</title>
  <meta name="description" content="A tag is a small component used to label, categorize, or indicate the status of an item. Tags are commonly used in content management systems, product listings, article metadata, and filtering inte..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
