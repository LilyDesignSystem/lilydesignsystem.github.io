<script lang="ts">
  const html: string = "<h1>Dialog</h1>\n\n<p>A modal or non-modal dialog that overlays the main interface to communicate information or prompt users for input. Dialogs are used for confirmations, alerts, forms, settings, and any interaction that requires focused user attention. Modal dialogs block interaction with the underlying page until dismissed, while non-modal dialogs allow background interaction.</p>\n\n<p>This component uses the native <code>&lt;dialog&gt;</code> HTML element with conditional rendering. When <code>open</code> is false, the dialog is removed from the DOM entirely. The dialog supports keyboard dismissal via the Escape key and exposes a bindable <code>open</code> prop for programmatic control of visibility.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a native <code>&lt;dialog&gt;</code> element conditionally via conditional rendering</li>\n<li>Supports two-way binding on the <code>open</code> prop with the parent</li>\n<li>The <code>open</code> HTML attribute is set directly on the dialog element (not using <code>.showModal()</code>)</li>\n<li><code>tabindex=\"-1\"</code> is set on the dialog to ensure it can receive focus and keyboard events</li>\n<li>Escape key handler sets <code>open</code> to false and prevents default browser behavior</li>\n<li>The <code>modal</code> prop controls whether <code>aria-modal</code> is set (defaults to true)</li>\n<li>When <code>modal</code> is false, <code>aria-modal</code> is omitted entirely (set to <code>undefined</code>)</li>\n<li>Accepts a <code>children</code> slot for dialog content including buttons, forms, or text</li>\n<li>Spreads <code>restProps</code> onto the <code>&lt;dialog&gt;</code> element for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>open</code>: boolean (default: false) -- whether the dialog is visible; bindable</li>\n<li><code>label</code>: string (required) -- accessible name for the dialog</li>\n<li><code>modal</code>: boolean (default: true) -- whether the dialog behaves as a modal</li>\n<li><code>children</code>: slot (required) -- dialog body content</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Modal dialog with a form:</p>\n\n<pre><code class=\"language-html\">&lt;button onclick={() =&gt; showDialog = true}&gt;Edit profile&lt;/button&gt;\n\n&lt;Dialog label=\"Edit profile\" open={showDialog}&gt;\n  &lt;form&gt;\n    &lt;label for=\"display-name\"&gt;Display name&lt;/label&gt;\n    &lt;input id=\"display-name\" type=\"text\" value={displayName} /&gt;\n\n    &lt;label for=\"bio\"&gt;Bio&lt;/label&gt;\n    &lt;textarea id=\"bio\"&gt;{bio}&lt;/textarea&gt;\n\n    &lt;button type=\"button\" onclick={() =&gt; showDialog = false}&gt;Cancel&lt;/button&gt;\n    &lt;button type=\"submit\"&gt;Save&lt;/button&gt;\n  &lt;/form&gt;\n&lt;/Dialog&gt;</code></pre>\n\n<p>Non-modal dialog for supplementary information:</p>\n\n<pre><code class=\"language-html\">&lt;Dialog label=\"Release notes\" open={showNotes} modal={false}&gt;\n  &lt;p&gt;Version 2.4 includes performance improvements and accessibility fixes.&lt;/p&gt;\n  &lt;button onclick={() =&gt; showNotes = false}&gt;Close&lt;/button&gt;\n&lt;/Dialog&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Escape: closes the dialog by setting <code>open</code> to false</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"dialog\"</code> -- provided natively by the <code>&lt;dialog&gt;</code> element</li>\n<li><code>aria-label=\"{label}\"</code> -- provides an accessible name for the dialog</li>\n<li><code>aria-modal=\"true\"</code> -- present when the <code>modal</code> prop is true, indicating the dialog traps focus and blocks background interaction</li>\n<li><code>tabindex=\"-1\"</code> -- allows the dialog element itself to receive focus for keyboard event handling</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for modal or non-modal dialogs that present forms, detailed content, or multi-step workflows.</li>\n<li>Use when users need to complete a focused task without leaving the current page context, such as editing a profile or configuring settings.</li>\n<li>Use with <code>aria-label</code> to provide an accessible name and with <code>aria-modal</code> for modal focus trapping.</li>\n<li>Use the <code>modal</code> prop set to <code>false</code> for supplementary information that does not require blocking background interaction.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for simple yes/no confirmations -- use AlertDialog for acknowledgment-style interactions.</li>\n<li>Do not use for brief tooltip-style information -- use Tooltip or Popover instead.</li>\n<li>Do not use when the content could be shown inline without an overlay -- avoid unnecessary modals.</li>\n<li>Do not use for persistent side panels -- use Drawer or Sheet instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a native <code>&lt;dialog&gt;</code> element with conditional rendering, accessible labeling via <code>aria-label</code>, modal/non-modal support via <code>aria-modal</code>, keyboard dismissal with Escape, and two-way binding on the <code>open</code> prop. The consumer provides all visual styling, backdrop appearance, positioning, and dialog content.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.dialog</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;dialog&gt;</code> element with class <code>dialog</code></li>\n<li>Verify role=\"dialog\"<code> -- provided natively by the </code>&lt;dialog&gt;` element</li>\n<li>Verify aria-label=\"{label}\"` -- provides an accessible name for the dialog</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Include a clear close or cancel affordance in every dialog. For modal dialogs, dim the background to communicate that the main content is not interactive.</li>\n<li><strong>Developers</strong>: Manage focus carefully -- move focus into the dialog when it opens and return it to the trigger element when it closes. Use the <code>modal</code> prop to control <code>aria-modal</code> behavior.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>alert-dialog</code> — a modal dialog for urgent messages requiring user acknowledgment</li>\n<li><code>popconfirm-dialog</code> — a popover dialog with confirm and cancel buttons</li>\n<li><code>file-dialog</code> — a dialog for browsing and selecting files</li>\n<li><code>drawer</code> — a panel that slides in from the edge of the screen</li>\n<li><code>sheet</code> — a panel that slides in from a screen edge as an overlay</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Dialog Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/</li>\n<li>MDN dialog element: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/dialog</li>\n<li>WAI-ARIA dialog role: https://www.w3.org/TR/wai-aria-1.2/#dialog</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<dialog class=\"dialog\" open aria-label=\"Example dialog\"><p>Dialog content</p><button class=\"button\">Close</button></dialog>";
  const svelteSource: string = "// In your Svelte component:\nimport Dialog from \"lily-design-system-svelte-headless/components/Dialog/Dialog.svelte\";\n\n<Dialog label=\"Dialog\" open>\n  <!-- dialog content -->\n</Dialog>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Dialog — Lily Design System</title>
  <meta name="description" content="A modal or non-modal dialog that overlays the main interface to communicate information or prompt users for input. Dialogs are used for confirmations, alerts, forms, settings, and any interaction t..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

