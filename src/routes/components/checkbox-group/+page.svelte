<script lang="ts">
  const html: string = "<h1>Checkbox Group</h1>\n\n<p>CheckboxGroup is a headless fieldset container that groups related checkboxes under the ARIA <code>group</code> role. It manages a collection of checkboxes with shared state, supporting a \"Select all\" checkbox with automatic indeterminate state, group-level disabled state, and two-way binding of selected values.</p>\n\n<p>Use CheckboxGroup when you need multiple checkboxes that share a logical relationship, such as selecting features, choosing categories, or picking multiple items from a list. The component wraps native checkbox inputs in a <code>&lt;fieldset&gt;</code> element, ensuring proper semantic grouping and accessibility without imposing any visual styling.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;fieldset&gt;</code> element with <code>role=\"group\"</code> for explicit ARIA semantics</li>\n<li>The <code>aria-label</code> attribute on the fieldset provides an accessible name for the group</li>\n<li>Consumer provides checkbox children (typically <code>&lt;label&gt;</code> elements wrapping <code>&lt;input type=\"checkbox\"&gt;</code>)</li>\n<li>Spreads <code>restProps</code> onto the fieldset for consumer customization</li>\n<li>No internal state management; checkbox selection is handled by native checkbox behavior</li>\n<li>Supports a \"Select all\" pattern where the consumer can add a checkbox that toggles all options, with automatic indeterminate state when some but not all options are selected</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible label for the checkbox group, applied via <code>aria-label</code></li>\n<li><code>children</code>: slot (required) -- checkbox elements to render inside the fieldset</li>\n<li><code>disabled</code>: boolean (default: false) -- disables the entire group via the fieldset <code>disabled</code> attribute</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;fieldset&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;Fieldset legend=\"How would you like to be contacted?\"&gt;\n  &lt;Hint&gt;Select all that apply&lt;/Hint&gt;\n  &lt;CheckboxGroup name=\"contact-methods\"&gt;\n    &lt;CheckboxInput label=\"Email\" value=\"email\" /&gt;\n    &lt;CheckboxInput label=\"Phone\" value=\"phone\" /&gt;\n    &lt;CheckboxInput label=\"Text message\" value=\"sms\" /&gt;\n  &lt;/CheckboxGroup&gt;\n&lt;/Fieldset&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;CheckboxGroup label=\"Toppings\" disabled&gt;\n  &lt;CheckboxInput label=\"Cheese\" name=\"toppings\" value=\"cheese\" /&gt;\n  &lt;CheckboxInput label=\"Peppers\" name=\"toppings\" value=\"peppers\" /&gt;\n&lt;/CheckboxGroup&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Moves focus between checkboxes within the group (native browser behavior)</li>\n<li>Space: Toggles the focused checkbox checked state (native browser behavior)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"group\"</code> -- identifies the fieldset as a group of related checkboxes</li>\n<li><code>aria-label={label}</code> -- provides an accessible name for the group so screen readers announce the purpose</li>\n<li>Native <code>&lt;fieldset&gt;</code> <code>disabled</code> attribute propagates disabled state to all child inputs</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to wrap multiple CheckboxInput components that share a common name and manage collective state</li>\n<li>Use within a Fieldset so the group has a descriptive legend for accessibility</li>\n<li>Use when the form needs to track which checkboxes are selected as a set</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for a single standalone checkbox — render CheckboxInput directly</li>\n<li>Do not use when only one option can be selected — use RadioGroup with RadioInput instead</li>\n<li>Do not use for visual-only grouping without shared state — use a Fieldset alone</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>The CheckboxGroup headless component provides a <code>&lt;fieldset&gt;</code> with <code>role=\"group\"</code>, <code>aria-label</code>, and an optional <code>disabled</code> attribute for group-level disabling. It relies on native checkbox behavior for selection and keyboard navigation. The consumer provides all visual styling for the fieldset, labels, checkboxes, and indeterminate state indicators.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.checkbox-group</code> class for targeting. Common styling concerns include layout (vertical vs. horizontal stacking), spacing between items, disabled opacity, and custom checkbox indicators.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;fieldset&gt;</code> element with <code>role=\"group\"</code></li>\n<li>Verify <code>aria-label</code> is set from the <code>label</code> prop</li>\n<li>Verify child checkbox inputs render inside the fieldset</li>\n<li>Verify the <code>disabled</code> attribute propagates to child inputs when set on the group</li>\n<li>Verify pass-through attributes are applied to the fieldset</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Stack checkbox options vertically for readability. Provide clear labels and ensure adequate spacing between items. When using \"Select all,\" place it visually before the individual options and use an indeterminate indicator when only some items are selected.</li>\n<li><strong>Developers</strong>: Use the native <code>&lt;fieldset&gt;</code> <code>disabled</code> attribute for group-level disabling. For \"Select all\" behavior, manage the indeterminate state in your application logic by comparing the number of selected items to the total. Ensure all checkboxes within the group share the same <code>name</code> attribute for proper form submission.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>CheckboxGroup is the container for CheckboxInput children. Use <code>&lt;CheckboxGroup&gt;</code> to wrap multiple <code>&lt;CheckboxInput&gt;</code> elements or native <code>&lt;input type=\"checkbox\"&gt;</code> elements, providing the group-level accessible label and optional disabled state while each checkbox carries its own option label.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>checkbox-input</code> — a checkbox input for toggling a boolean value &lt;input type=\"checkbox\"&gt;</li>\n<li><code>radio-group</code> — a group of radio buttons for selecting one option</li>\n<li><code>toggle-group</code> — a group of toggle buttons for selecting options</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Checkbox Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/checkbox/</li>\n<li>WAI-ARIA Group Role: https://www.w3.org/TR/wai-aria-1.2/#group</li>\n<li>WAI-ARIA Authoring Practices - Checkbox: https://www.w3.org/WAI/ARIA/apg/patterns/checkbox/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"checkbox-group\" role=\"group\" aria-label=\"Options\"><label><input type=\"checkbox\" checked /> Option A</label><label><input type=\"checkbox\" /> Option B</label></div>";
  const svelteSource: string = "// In your Svelte component:\nimport CheckboxGroup from \"lily-design-system-svelte-headless/components/CheckboxGroup/CheckboxGroup.svelte\";\n\n<CheckboxGroup>\n  <!-- list children -->\n</CheckboxGroup>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Checkbox Group — Lily Design System</title>
  <meta name="description" content="CheckboxGroup is a headless fieldset container that groups related checkboxes under the ARIA group role. It manages a collection of checkboxes with shared state, supporting a &quot;Select all&quot; checkbox ..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

