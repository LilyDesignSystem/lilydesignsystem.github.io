<script lang="ts">
  const html: string = "<h1>Time Input</h1>\n\n<p>A time input provides a field for entering time values using the native HTML <code>&lt;input type=\"time\"&gt;</code> element. It renders a time picker control appropriate to the user's browser and operating system, allowing users to enter hours and minutes in a localized format.</p>\n\n<p>This component is useful for scheduling interfaces, appointment forms, event planners, and any form that requires time-of-day entry. The bindable value uses HH:MM format (24-hour) and supports required and disabled states. It uses <code>aria-label</code> for accessibility.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a native <code>&lt;input type=\"time\"&gt;</code> element</li>\n<li>Uses <code>aria-label</code> for accessible naming (no visible label element included; consumers can add their own)</li>\n<li>Supports two-way binding for the <code>value</code> prop, enabling two-way <code>value</code> binding in the parent</li>\n<li>Supports two-way binding on the <code>value</code> prop</li>\n<li>Value format is HH:MM (24-hour time string, e.g., \"14:30\")</li>\n<li>Supports standard HTML form attributes: <code>required</code>, <code>disabled</code></li>\n<li>Spreads <code>restProps</code> for consumer customization (e.g., <code>min</code>, <code>max</code>, <code>step</code>)</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name via <code>aria-label</code></li>\n<li><code>value</code>: string (default: <code>\"\"</code>, bindable) -- current time value in HH:MM format, supports two-way binding</li>\n<li><code>required</code>: boolean (default: <code>false</code>) -- whether the field is required</li>\n<li><code>disabled</code>: boolean (default: <code>false</code>) -- whether the field is disabled</li>\n<li><code>...restProps</code>: any additional HTML attributes spread onto the input</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Appointment scheduling with business hours constraint:</p>\n\n<pre><code class=\"language-html\">&lt;TimeInput label=\"Appointment time\" value={appointmentTime} min=\"09:00\" max=\"17:00\" required /&gt;</code></pre>\n\n<p>Shift start and end times:</p>\n\n<pre><code class=\"language-html\">&lt;TimeInput label=\"Shift start\" value={shiftStart} required /&gt;\n&lt;TimeInput label=\"Shift end\" value={shiftEnd} required /&gt;</code></pre>\n\n<p>Medication schedule entry:</p>\n\n<pre><code class=\"language-html\">&lt;TimeInput label=\"Take medication at\" value={medTime} /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None beyond native input behavior -- the browser's built-in time picker handles keyboard interactions (arrow keys to adjust hours/minutes, Tab between segments).</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides an accessible name for the input since no visible <code>&lt;label&gt;</code> element is included</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for entering a time value using the native browser time picker.</li>\n<li>Use for appointment times, medication schedules, or shift start and end times.</li>\n<li>Use for straightforward time-of-day entry where the native browser time picker is sufficient.</li>\n<li>Use when any arbitrary time value should be accepted.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when a dropdown of predefined time slots is better -- use TimePickerInput instead.</li>\n<li>Do not use for combined date-and-time entry -- use DateTimeNowInput or DateTimeLocalInput instead.</li>\n<li>Do not use for time duration or elapsed time; consider a custom duration input instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a native <code>&lt;input type=\"time\"&gt;</code> element with <code>aria-label</code>, two-way value binding, and support for <code>required</code> and <code>disabled</code> states. The browser provides the time picker UI. The consumer provides all visual styling and any surrounding label or hint text.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.time-input</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;input&gt;</code> element with class <code>time-input</code></li>\n<li>Verify aria-label={label}<code> -- provides an accessible name for the input since no visible </code>&lt;label&gt;` element is included</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Pair the time input with a visible label and hint text showing the expected format, since native time picker appearance varies by browser.</li>\n<li><strong>Developers</strong>: Use <code>min</code> and <code>max</code> attributes via restProps to constrain valid time ranges (e.g., business hours). The value is always in 24-hour HH:MM format regardless of display locale.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>time-picker-input</code> — an input with a dropdown for picking a time</li>\n<li><code>date-input</code> — an input for entering a date value &lt;input type=\"date\"&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN input type=\"time\": https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/time</li>\n</ul>";
</script>

<svelte:head>
  <title>Time Input — Lily Design System</title>
  <meta name="description" content="A time input provides a field for entering time values using the native HTML &lt;input type=&quot;time&quot;&gt; element. It renders a time picker control appropriate to the user's browser and operating system, al..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
