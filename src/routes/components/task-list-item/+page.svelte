<script lang="ts">
  const html: string = "<h1>Task List Item</h1>\n\n<p>A single item within a TaskList. Renders a semantic <code>&lt;li&gt;</code> element representing one task, action, or to-do. The <code>completed</code> prop controls a <code>data-completed</code> attribute for consumer styling, communicating task state to visual presentation.</p>\n\n<p>This component is designed to be placed inside a TaskList <code>&lt;ul&gt;</code>. The consumer provides the task content through the children slot, and optionally marks items as completed for progress tracking.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as a semantic <code>&lt;li&gt;</code> element for use within a TaskList <code>&lt;ul&gt;</code></li>\n<li>Sets <code>data-completed</code> attribute (<code>\"true\"</code> or <code>\"false\"</code>) for consumer CSS hooks</li>\n<li>Defaults <code>completed</code> to <code>false</code> when not specified</li>\n<li>All task content is provided through the children slot</li>\n<li>Spreads <code>...restProps</code> onto the <code>&lt;li&gt;</code> element for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>completed</code>: boolean (default: <code>false</code>) -- whether the task is completed</li>\n<li><code>children</code>: slot (required) -- task content</li>\n<li><code>...restProps</code>: Any additional HTML attributes spread onto the <code>&lt;li&gt;</code></li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;TaskList label=\"Today's tasks\"&gt;\n  &lt;TaskListItem completed={true}&gt;Review pull requests&lt;/TaskListItem&gt;\n  &lt;TaskListItem&gt;Update documentation&lt;/TaskListItem&gt;\n  &lt;TaskListItem&gt;Deploy to staging&lt;/TaskListItem&gt;\n&lt;/TaskList&gt;</code></pre>\n\n<p>With checkboxes for interactive tasks:</p>\n\n<pre><code class=\"language-html\">&lt;TaskList label=\"Onboarding checklist\"&gt;\n  &lt;TaskListItem completed={done1}&gt;\n    &lt;label&gt;&lt;input type=\"checkbox\" checked={done1} /&gt; Create account&lt;/label&gt;\n  &lt;/TaskListItem&gt;\n  &lt;TaskListItem completed={done2}&gt;\n    &lt;label&gt;&lt;input type=\"checkbox\" checked={done2} /&gt; Set up profile&lt;/label&gt;\n  &lt;/TaskListItem&gt;\n&lt;/TaskList&gt;</code></pre>\n\n<p>Consumer CSS example using <code>data-completed</code>:</p>\n\n<pre><code class=\"language-css\">[data-completed=\"true\"] {\n  text-decoration: line-through;\n  opacity: 0.6;\n}\n[data-completed=\"false\"] {\n  font-weight: normal;\n}</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>None directly -- standard list item with no interactive behavior</li>\n<li>If the item contains interactive elements (checkboxes, links), they handle their own keyboard behavior</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Semantic <code>&lt;li&gt;</code> provides implicit <code>listitem</code> role</li>\n<li><code>data-completed</code> communicates task state for consumer styling hooks</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside a TaskList to represent one task with its status and optional hint</li>\n<li>Use with an <code>href</code> to link to the task page when the task can be started</li>\n<li>Use when individual tasks need a <code>data-completed</code> attribute for CSS-driven visual state changes</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside of a TaskList -- it has no standalone meaning as the <code>&lt;li&gt;</code> element requires a list parent</li>\n<li>Do not use for simple checkbox items without linked tasks -- use CheckListItem instead</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component renders a semantic <code>&lt;li&gt;</code> element with a <code>data-completed</code> attribute for CSS hooks. It provides list item semantics and completion state signaling. The consumer provides all visual styling including completion indicators (strikethrough, opacity), checkboxes, and task content.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.task-list-item</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;li&gt;</code> element with class <code>task-list-item</code></li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use visual differentiation (e.g., strikethrough, reduced opacity, or a checkmark) for completed tasks so users can quickly distinguish them from pending items.</li>\n<li><strong>Developers</strong>: Bind the <code>completed</code> prop to your task state and use the <code>data-completed</code> attribute in CSS selectors for styling (e.g., <code>[data-completed=\"true\"] { text-decoration: line-through; }</code>).</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>TaskListItem is designed to be used as a child of TaskList. The TaskList provides the outer <code>&lt;ul&gt;</code> container with <code>role=\"list\"</code>, and each TaskListItem provides an individual <code>&lt;li&gt;</code> with completion tracking.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>task-list</code> — an ordered list of task list items</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN li element: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/li</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<ol class=\"task-list\"><li class=\"task-list-item\"><input type=\"checkbox\" /> Task</li></ol>";
  const svelteSource: string = "// In your Svelte component:\nimport TaskListItem from \"lily-design-system-svelte-headless/components/TaskListItem/TaskListItem.svelte\";\n\n<TaskListItem>\n  <!-- TaskListItem content -->\n</TaskListItem>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Task List Item — Lily Design System</title>
  <meta name="description" content="A single item within a TaskList. Renders a semantic &lt;li&gt; element representing one task, action, or to-do. The completed prop controls a data-completed attribute for consumer styling, communicating ..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

