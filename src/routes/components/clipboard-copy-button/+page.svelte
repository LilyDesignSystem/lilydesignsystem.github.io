<script lang="ts">
  const html: string = "<h1>Clipboard Copy Button</h1>\n\n<p>A clipboard component provides a button that copies specified text to the system clipboard when activated. It uses the ClipboardCopyButton API (<code>navigator.clipboard.writeText</code>) and exposes success and error callbacks for consumer-defined feedback.</p>\n\n<p>The component tracks a <code>copied</code> state that automatically resets after 2 seconds. This state is exposed via a <code>data-copied</code> attribute on the button, allowing consumers to show visual feedback (e.g., a \"Copied!\" indicator) through CSS selectors like <code>[data-copied=\"true\"]</code>.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;button type=\"button\"&gt;</code> with <code>aria-label</code> for the copy action</li>\n<li>Uses <code>navigator.clipboard.writeText</code> for async clipboard access</li>\n<li>reactive state tracks <code>copied</code> status, which auto-resets to <code>false</code> after 2 seconds via <code>setTimeout</code></li>\n<li>The <code>data-copied</code> attribute reflects the copied state for CSS-based feedback</li>\n<li>Optional <code>children</code> slot allows custom button content</li>\n<li>Callbacks <code>onsuccess</code> and <code>onerror</code> let consumers respond to copy results</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>text</code>: string (required) -- the text to copy to the clipboard</li>\n<li><code>label</code>: string (required) -- accessible label for the copy button via <code>aria-label</code></li>\n<li><code>onsuccess</code>: () =&gt; void (default: undefined) -- callback invoked after a successful copy</li>\n<li><code>onerror</code>: (error: unknown) =&gt; void (default: undefined) -- callback invoked if the copy fails</li>\n<li><code>children</code>: slot (default: undefined) -- optional custom button content</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Copy a URL to the clipboard:</p>\n\n<pre><code class=\"language-html\">&lt;ClipboardCopyButton text=\"https://example.com\" label=\"Copy link\" /&gt;</code></pre>\n\n<p>Copy button with custom content and success callback:</p>\n\n<pre><code class=\"language-html\">&lt;ClipboardCopyButton text={code} label=\"Copy code\" onsuccess={handleCopied}&gt;\n  Copy to clipboard\n&lt;/ClipboardCopyButton&gt;</code></pre>\n\n<p>Copy button next to a reference number:</p>\n\n<pre><code class=\"language-html\">&lt;div class=\"reference-row\"&gt;\n  &lt;span&gt;Booking ref: &lt;strong&gt;ABC-12345&lt;/strong&gt;&lt;/span&gt;\n  &lt;ClipboardCopyButton text=\"ABC-12345\" label=\"Copy booking reference\" /&gt;\n&lt;/div&gt;</code></pre>\n\n<p>Copy button alongside an API key:</p>\n\n<pre><code class=\"language-html\">&lt;Field label=\"API Key\"&gt;\n  &lt;div class=\"input-with-copy\"&gt;\n    &lt;TextInput label=\"API Key\" value={apiKey} disabled /&gt;\n    &lt;ClipboardCopyButton\n      text={apiKey}\n      label=\"Copy API key\"\n      onsuccess={() =&gt; toast('API key copied')}\n      onerror={(err) =&gt; toast('Copy failed: ' + err)}\n    /&gt;\n  &lt;/div&gt;\n&lt;/Field&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Enter/Space: Activate the copy button (native button behavior)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label</code> -- describes the copy action for screen readers</li>\n<li><code>data-copied</code> -- reflects <code>\"true\"</code> or <code>\"false\"</code> for CSS-based visual feedback (not an ARIA attribute, but useful for consumer styling)</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use next to text content that users frequently need to copy, such as URLs, code snippets, or API keys.</li>\n<li>Use when a one-click copy action improves user efficiency over manual text selection.</li>\n<li>Use alongside reference numbers, tracking codes, or identifiers that users need to paste elsewhere.</li>\n<li>Use in code documentation to let users quickly copy example commands or snippets.</li>\n<li>Use next to share links or invite URLs.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for general-purpose buttons -- use Button instead.</li>\n<li>Do not use when the text is already easily selectable and rarely needs copying.</li>\n<li>Do not use for large blocks of text -- provide a download link instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless ClipboardCopyButton component provides a <code>&lt;button&gt;</code> with <code>aria-label</code>, Clipboard API integration (<code>navigator.clipboard.writeText</code>), automatic <code>copied</code> state tracking with 2-second reset, and <code>data-copied</code> attribute for styling hooks. The consumer provides all visual styling including button appearance, copy/success icons, tooltip feedback, and animations.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.clipboard-copy-button</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;button&gt;</code> element with class <code>clipboard-copy-button</code></li>\n<li>Verify aria-label` -- describes the copy action for screen readers</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Provide clear visual feedback when copying succeeds, such as a checkmark icon or \"Copied!\" text. Keep the button close to the content it copies.</li>\n<li><strong>Developers</strong>: Use the <code>data-copied</code> attribute (e.g., <code>[data-copied=\"true\"]</code>) in CSS to show success state. Handle the <code>onerror</code> callback for browsers that restrict clipboard access.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>button</code> — a generic clickable button element</li>\n<li><code>icon-button</code> — a button containing only an icon with a required accessible label</li>\n<li><code>share-page</code> — a component for sharing the page on social media</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>ClipboardCopyButton API: https://developer.mozilla.org/en-US/docs/Web/API/ClipboardCopyButton/writeText</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<button class=\"clipboard-copy-button\">Copy to clipboard</button>";
  const svelteSource: string = "// In your Svelte component:\nimport ClipboardCopyButton from \"lily-design-system-svelte-headless/components/ClipboardCopyButton/ClipboardCopyButton.svelte\";\n\n<ClipboardCopyButton>ClipboardCopyButton</ClipboardCopyButton>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Clipboard Copy Button — Lily Design System</title>
  <meta name="description" content="A clipboard component provides a button that copies specified text to the system clipboard when activated. It uses the ClipboardCopyButton API (navigator.clipboard.writeText) and exposes success an..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

