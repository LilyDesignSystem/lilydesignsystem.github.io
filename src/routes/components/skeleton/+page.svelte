<script lang="ts">
  const html: string = "<h1>Skeleton</h1>\n\n<p>A skeleton is a loading placeholder that indicates content is in the process of loading. It shows the approximate layout of content before it loads, reducing perceived loading time and preventing layout shift. The component uses <code>aria-hidden=\"true\"</code> to hide the placeholder from screen readers and <code>aria-busy=\"true\"</code> to signal that the region is being updated. The component is headless, allowing consumers to style the placeholder shapes with their own CSS.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> with <code>aria-hidden=\"true\"</code> so screen readers completely skip the placeholder</li>\n<li>Uses <code>aria-busy=\"true\"</code> to signal to assistive technology that the region is loading</li>\n<li>Accepts an optional <code>children</code> slot for placeholder shapes (e.g., lines, circles, rectangles)</li>\n<li>Spreads <code>restProps</code> for consumer customization (e.g., sizing, data attributes)</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>children</code>: slot (default: <code>undefined</code>) -- optional placeholder content (e.g., skeleton line shapes)</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;div&gt;</code></li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Card placeholder with text lines and avatar circle:</p>\n\n<pre><code class=\"language-html\">&lt;Skeleton&gt;\n  &lt;div class=\"skeleton-circle\" style=\"width: 48px; height: 48px;\"&gt;&lt;/div&gt;\n  &lt;div class=\"skeleton-line\" style=\"width: 60%; height: 16px;\"&gt;&lt;/div&gt;\n  &lt;div class=\"skeleton-line\" style=\"width: 80%; height: 16px;\"&gt;&lt;/div&gt;\n&lt;/Skeleton&gt;</code></pre>\n\n<p>Single-line text placeholder:</p>\n\n<pre><code class=\"language-html\">&lt;Skeleton&gt;\n  &lt;div class=\"skeleton-line\" style=\"width: 100%; height: 14px;\"&gt;&lt;/div&gt;\n&lt;/Skeleton&gt;</code></pre>\n\n<p>Image placeholder for a content card:</p>\n\n<pre><code class=\"language-html\">&lt;Skeleton&gt;\n  &lt;div class=\"skeleton-rect\" style=\"width: 100%; height: 200px;\"&gt;&lt;/div&gt;\n  &lt;div class=\"skeleton-line\" style=\"width: 70%; height: 16px;\"&gt;&lt;/div&gt;\n&lt;/Skeleton&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive placeholder hidden from interaction.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-hidden=\"true\"</code> -- hides the placeholder entirely from screen readers</li>\n<li><code>aria-busy=\"true\"</code> -- signals that this region is in a loading state and content will be replaced</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use Skeleton as a placeholder while content is loading to show the approximate layout and reduce perceived wait time.</li>\n<li>Use Skeleton to prevent layout shift by reserving space for content that has not yet loaded.</li>\n<li>Use Skeleton for card-based layouts where each card can show placeholder shapes matching the expected text, image, and button positions.</li>\n<li>Use Skeleton when loading lists, tables, or profile views where the structure is known in advance.</li>\n<li>Use Skeleton for content-heavy pages where maintaining visual layout during loading improves perceived performance.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use Skeleton for error states -- show an error message instead using Alert or ErrorMessage.</li>\n<li>Do not use Skeleton for known-progress loading where a percentage is available -- use Progress instead.</li>\n<li>Do not use Skeleton for very fast operations (under 200ms) where a brief flash would be distracting.</li>\n<li>Do not use Skeleton when the content layout is unknown -- use ProgressSpinner or Loading instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;div&gt;</code> with <code>aria-hidden=\"true\"</code> and <code>aria-busy=\"true\"</code> to hide the placeholder from screen readers while signaling a loading state. The consumer provides all visual styling including placeholder shapes, shimmer animations, dimensions, and colors.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.skeleton</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>skeleton</code></li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Match skeleton shapes to the actual content layout (e.g., rectangles for text lines, circles for avatars). Use a subtle animation such as a shimmer or pulse to indicate loading.</li>\n<li><strong>Developers</strong>: Replace the skeleton with actual content as soon as data loads, ensuring <code>aria-busy</code> is removed from the parent region. Avoid rendering skeletons for content that loads in under 200ms.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>loading</code> — a loading indicator, such as text, or image, or animation</li>\n<li><code>progress-spinner</code> — an indeterminate spinning progress indicator</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA <code>aria-busy</code>: https://www.w3.org/WAI/ARIA/apd/states/aria-busy/</li>\n<li>MDN <code>aria-hidden</code>: https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/Attributes/aria-hidden</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"skeleton\" aria-hidden=\"true\" style=\"height:1em;background:#e5e7eb;border-radius:4px;\"></div>";
  const svelteSource: string = "// In your Svelte component:\nimport Skeleton from \"lily-design-system-svelte-headless/components/Skeleton/Skeleton.svelte\";\n\n<Skeleton>\n  <!-- content -->\n</Skeleton>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Skeleton — Lily Design System</title>
  <meta name="description" content="A skeleton is a loading placeholder that indicates content is in the process of loading. It shows the approximate layout of content before it loads, reducing perceived loading time and preventing l..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

