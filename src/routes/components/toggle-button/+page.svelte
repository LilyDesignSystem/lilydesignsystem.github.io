<script lang="ts">
  const html: string = "<h1>Toggle Button</h1>\n\n<p>A ToggleButton is a UI/UX component that provides a binary on/off control using a button with <code>role=\"switch\"</code>. It communicates its state through <code>aria-checked</code> and supports both click and keyboard (Space key) interaction. The pressed state is bindable for two-way data flow.</p>\n\n<p>ToggleButtons are commonly used for settings like dark mode, notifications, or feature flags where a user needs to switch between two states. While similar to SwitchInput, the ToggleButton component uses a <code>pressed</code> prop name rather than <code>checked</code>, providing a semantic distinction for use cases where \"pressed/unpressed\" better describes the interaction than \"checked/unchecked\".</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;button&gt;</code> element with class <code>toggle-button</code></li>\n<li>Spreads <code>restProps</code> onto the root element for consumer customization</li>\n</ul>\n\n<h2>Help</h2>\n\n<p>Use ToggleButton when you need a binary on/off control with button semantics. Common scenarios include dark mode toggles, mute controls, notification switches, and feature flags. For checkbox-style toggles, consider SwitchInput instead.</p>\n\n<h2>Syntax</h2>\n\n<pre><code class=\"language-html\">&lt;ToggleButton label=\"...\" pressed={pressed} /&gt;</code></pre>\n\n<h2>Usage</h2>\n\n<p>Bold formatting toggle in a text editor toolbar:</p>\n\n<pre><code class=\"language-html\">&lt;ToggleButton label=\"Bold\" pressed={isBold} /&gt;</code></pre>\n\n<p>Mute control in a media player:</p>\n\n<pre><code class=\"language-html\">&lt;ToggleButton label=\"Mute audio\" pressed={muted} disabled={locked} /&gt;</code></pre>\n\n<p>Favourite toggle on a content card:</p>\n\n<pre><code class=\"language-html\">&lt;ToggleButton label=\"Favourite\" pressed={isFavourited} /&gt;</code></pre>\n\n<h2>Props</h2>\n\n<table>\n<thead>\n<tr><th>Prop</th><th>Type</th><th>Default</th><th>Description</th></tr>\n</thead>\n<tbody>\n<tr><td><code>label</code></td><td><code>string</code></td><td>(required)</td><td>Accessible name applied via <code>aria-label</code></td></tr>\n<tr><td><code>pressed</code></td><td><code>boolean</code></td><td><code>false</code></td><td>Whether the toggle is on; bindable with two-way <code>pressed</code> binding</td></tr>\n<tr><td><code>disabled</code></td><td><code>boolean</code></td><td><code>false</code></td><td>Whether the toggle is disabled</td></tr>\n<tr><td><code>...restProps</code></td><td>HTML attributes</td><td></td><td>Additional attributes spread onto the <code>&lt;button&gt;</code> element</td></tr>\n</tbody>\n</table>\n\n<h2>Examples</h2>\n\n<p>Feature flag control:</p>\n\n<pre><code class=\"language-html\">&lt;ToggleButton label=\"Enable beta features\" pressed={betaFeatures} /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Space: Toggles between on and off states (custom handler with <code>preventDefault</code>)</li>\n<li>Enter: Activates the button (native button behavior)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"switch\"</code> -- identifies the element as a two-state toggle switch</li>\n<li><code>aria-checked={pressed}</code> -- communicates whether the toggle is currently on (<code>true</code>) or off (<code>false</code>)</li>\n<li><code>aria-label={label}</code> -- provides the accessible name for the toggle control</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for a button that toggles between pressed and unpressed states, such as bold or italic formatting.</li>\n<li>Use for mute/unmute or play/pause controls in media applications.</li>\n<li>Use when \"pressed/unpressed\" better describes the interaction than \"checked/unchecked\".</li>\n<li>Use inside a ToggleGroup for related multi-toggle options like text alignment or view modes.</li>\n<li>Use for toolbar actions where the button state needs to persist visually.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for on/off settings with immediate effect -- use SwitchButton instead.</li>\n<li>Do not use for navigation actions -- use ActionLink instead.</li>\n<li>Do not use for form fields where a checkbox is more appropriate -- use CheckboxInput instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;button&gt;</code> with <code>role=\"switch\"</code>, <code>aria-checked</code>, and <code>aria-label</code> for accessible two-state toggle behavior. It handles Space key toggling with <code>preventDefault</code>. The consumer provides all visual styling, including pressed/unpressed states and any icons or labels.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.toggle-button</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;button&gt;</code> element with class <code>toggle-button</code></li>\n<li>Verify role=\"switch\"` -- identifies the element as a two-state toggle switch</li>\n<li>Verify aria-label={label}` -- provides the accessible name for the toggle control</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Provide distinct visual states for pressed and unpressed (e.g., filled vs. outlined, color change) so the current state is immediately obvious.</li>\n<li><strong>Developers</strong>: Use two-way binding on the <code>pressed</code> prop to keep parent state in sync. ToggleButton can be used standalone or inside a ToggleGroup.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>ToggleButton works as a standalone component or as a child of ToggleGroup. When used inside a ToggleGroup, multiple ToggleButtons are semantically grouped with <code>role=\"group\"</code> for related toggle options (e.g., text formatting: bold, italic, underline).</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>switch-button</code> — a toggle switch for turning a setting on or off</li>\n<li><code>button</code> — a generic clickable button element</li>\n<li><code>checkbox-input</code> — a checkbox input for toggling a boolean value &lt;input type=\"checkbox\"&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Switch Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/switch/</li>\n<li>WAI-ARIA Switch Role: https://www.w3.org/TR/wai-aria-1.2/#switch</li>\n</ul>";
</script>

<svelte:head>
  <title>Toggle Button — Lily Design System</title>
  <meta name="description" content="A ToggleButton is a UI/UX component that provides a binary on/off control using a button with role=&quot;switch&quot;. It communicates its state through aria-checked and supports both click and keyboard (Spa..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
