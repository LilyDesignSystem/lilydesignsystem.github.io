<script lang="ts">
  const html: string = "<h1>Beach Ball</h1>\n\n<p>A beach ball is a loading/waiting indicator component, inspired by the colorful spinning cursor on macOS that appears when the system is busy. It provides a visual cue that a process is in progress and communicates to users that the system is working on a task.</p>\n\n<p>This headless component renders a <code>&lt;div&gt;</code> with <code>role=\"status\"</code> and <code>aria-live=\"polite\"</code> for non-blocking screen reader announcements of loading state changes. The <code>active</code> prop controls whether the indicator is currently showing, and an inner <code>&lt;span&gt;</code> element serves as the animation target for consumer-applied CSS.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as a <code>&lt;div&gt;</code> with <code>role=\"status\"</code> and <code>aria-live=\"polite\"</code> for non-blocking loading announcements</li>\n<li><code>aria-busy</code> reflects the <code>active</code> prop, signaling to assistive technologies whether loading is in progress</li>\n<li><code>data-active</code> attribute mirrors the active state for CSS state selectors (e.g., <code>[data-active=\"true\"]</code>)</li>\n<li>Inner <code>&lt;span aria-hidden=\"true\"&gt;</code> is only rendered when active; consumers target it for spinning/animation CSS</li>\n<li>Uses props for prop destructuring with rest props spread</li>\n<li>No hardcoded strings; all text comes through props for internationalization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible description of what is loading, via <code>aria-label</code></li>\n<li><code>active</code>: boolean (default: <code>true</code>) -- whether the loading indicator is currently active</li>\n<li><code>...restProps</code>: any -- additional HTML attributes spread onto the <code>&lt;div&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;BeachBall label=\"Loading your dashboard\" /&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;BeachBall label=\"Processing payment\" active={isProcessing} /&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;!-- Conditional loading with a fallback message --&gt;\n&lt;BeachBall label=\"Fetching search results\" active={isSearching} /&gt;\n{#if !isSearching}\n  &lt;p&gt;Showing {results.length} results&lt;/p&gt;\n{/if}</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>None -- this is a passive informational indicator, not interactive</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"status\"</code> -- identifies this as a status region that announces updates to screen readers</li>\n<li><code>aria-live=\"polite\"</code> -- ensures loading announcements do not interrupt the user's current task</li>\n<li><code>aria-busy</code> -- set to <code>true</code> when active, signaling to assistive technologies that content is loading</li>\n<li><code>aria-label</code> -- provides the accessible name describing what is loading, set from the <code>label</code> prop</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use as a decorative animated loading indicator for playful or celebratory UI moments</li>\n<li>Use when you want a whimsical, macOS-inspired waiting indicator as an alternative to a standard spinner</li>\n<li>Use during page loads, data fetching, or background processing where the duration is indeterminate</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use on clinical or transactional pages -- decorative animations are distracting in serious contexts</li>\n<li>Do not use for progress that has a known completion percentage -- use Progress or ProgressCircle instead</li>\n<li>Do not use when placeholder content is more appropriate -- use Skeleton for layout-matching loading states</li>\n<li>Do not use without a <code>prefers-reduced-motion</code> media query -- respect user motion preferences by pausing or hiding the animation</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides <code>role=\"status\"</code>, <code>aria-live=\"polite\"</code>, <code>aria-busy</code>, and a <code>data-active</code> attribute with an inner <code>&lt;span&gt;</code> animation target, all with zero visual styling. The consumer is responsible for all CSS including the spinning animation, colors, size, and any backdrop or overlay effects.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.beach-ball</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>beach-ball</code></li>\n<li>Verify role=\"status\"` -- identifies this as a status region that announces updates to screen readers</li>\n<li>Verify aria-label<code> -- provides the accessible name describing what is loading, set from the </code>label` prop</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Keep the indicator visually lightweight so it does not overwhelm the content area. Ensure the animation is smooth and not distracting at small sizes.</li>\n<li><strong>Developers</strong>: Use the <code>active</code> prop to control visibility rather than mounting/unmounting the component, so the live region remains in the DOM for consistent screen reader announcements.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>loading</code> — a loading indicator, such as text, or image, or animation</li>\n<li><code>progress-spinner</code> — an indeterminate spinning progress indicator</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Status Role: https://www.w3.org/TR/wai-aria-1.2/#status</li>\n<li>ARIA aria-busy: https://www.w3.org/TR/wai-aria-1.2/#aria-busy</li>\n</ul>";
</script>

<svelte:head>
  <title>Beach Ball — Lily Design System</title>
  <meta name="description" content="A beach ball is a loading/waiting indicator component, inspired by the colorful spinning cursor on macOS that appears when the system is busy. It provides a visual cue that a process is in progress..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
