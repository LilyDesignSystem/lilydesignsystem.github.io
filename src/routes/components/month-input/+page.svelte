<script lang="ts">
  const html: string = "<h1>Month Input</h1>\n\n<p>MonthInput is a headless component that wraps a native <code>&lt;input type=\"month\"&gt;</code> element with accessible labelling for selecting a month and year. It provides a bindable value and supports common form input states like required and disabled.</p>\n\n<p>This component is useful for date-related forms where the user needs to select a specific month, such as credit card expiration dates, report periods, billing cycles, or start/end dates at month granularity. The native month input provides a browser-provided picker interface where supported.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a native <code>&lt;input type=\"month\"&gt;</code> element</li>\n<li>Uses <code>aria-label</code> for an accessible name describing the input purpose</li>\n<li>The <code>value</code> prop uses two-way binding, storing the month in <code>YYYY-MM</code> format</li>\n<li>Supports <code>required</code> and <code>disabled</code> boolean states</li>\n<li>Spreads <code>restProps</code> onto the input for consumer customization (e.g., <code>min</code>, <code>max</code>, <code>id</code>, <code>name</code>)</li>\n<li>pattern: <code>two-way binding</code> for the value prop</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name for the input via <code>aria-label</code></li>\n<li><code>value</code>: string (default: <code>\"\"</code>) -- current month value in <code>YYYY-MM</code> format; bindable with two-way <code>value</code> binding</li>\n<li><code>required</code>: boolean (default: <code>false</code>) -- whether the input is required</li>\n<li><code>disabled</code>: boolean (default: <code>false</code>) -- whether the input is disabled</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;input&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Credit card expiry month:</p>\n\n<pre><code class=\"language-html\">&lt;MonthInput label=\"Card expiry\" value={expiryMonth} required /&gt;</code></pre>\n\n<p>Reporting period selection with constraints:</p>\n\n<pre><code class=\"language-html\">&lt;MonthInput label=\"Report month\" value={reportMonth} min=\"2025-01\" max=\"2026-12\" /&gt;</code></pre>\n\n<p>Billing cycle start:</p>\n\n<pre><code class=\"language-html\">&lt;MonthInput label=\"Billing start month\" value={billingMonth} /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None at the component level. Keyboard behavior is handled natively by the browser's month input implementation.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides an accessible name describing the purpose of the month input</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for selecting a month and year, such as credit card expiry or reporting period.</li>\n<li>Use when day-level precision is not needed and month granularity is sufficient.</li>\n<li>Use for billing cycles, subscription renewal dates, or financial reporting periods.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when a full date is needed -- use DateInput instead.</li>\n<li>Do not use when a week is needed -- use WeekInput instead.</li>\n<li>Do not use when day-level precision is required -- use DateInput or DateField instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component wraps a native <code>&lt;input type=\"month\"&gt;</code> with <code>aria-label</code> for accessible naming and two-way bindable value in <code>YYYY-MM</code> format. The consumer provides all visual styling, including input dimensions, borders, calendar icon, and any custom picker UI for browsers without native month picker support.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.month-input</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;input&gt;</code> element with class <code>month-input</code></li>\n<li>Verify aria-label={label}` -- provides an accessible name describing the purpose of the month input</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Be aware that native month picker appearance varies widely across browsers; plan fallback styling or a custom picker for consistent cross-browser experience.</li>\n<li><strong>Developers</strong>: The value format is always <code>YYYY-MM</code>. Use <code>min</code> and <code>max</code> via restProps to constrain the selectable range when needed.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>date-input</code> — an input for entering a date value &lt;input type=\"date\"&gt;</li>\n<li><code>week-input</code> — an input for selecting a week and year &lt;input type=\"week\"&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>HTML month input: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/month</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<label class=\"label\" for=\"demo-input\">Label</label><input class=\"month-input\" type=\"month\" id=\"demo-input\" aria-label=\"MonthInput\" />";
  const svelteSource: string = "// In your Svelte component:\nimport MonthInput from \"lily-design-system-svelte-headless/components/MonthInput/MonthInput.svelte\";\n\n<MonthInput label=\"Label\" />\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Month Input — Lily Design System</title>
  <meta name="description" content="MonthInput is a headless component that wraps a native &lt;input type=&quot;month&quot;&gt; element with accessible labelling for selecting a month and year. It provides a bindable value and supports common form i..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

