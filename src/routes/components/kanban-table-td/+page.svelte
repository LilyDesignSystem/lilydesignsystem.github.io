<script lang="ts">
  const html: string = "<h1>Kanban Table TD</h1>\n\n<p>A Kanban board item is a component that represents a single work item or task card within a Kanban board column. Each item typically displays summary information about a task, such as its title, assignee, priority, or due date, and serves as the unit that moves between workflow stages.</p>\n\n<p>This component is designed to be used as a child element within a Kanban column that has <code>role=\"list\"</code>. It renders as a <code>&lt;div&gt;</code> with <code>role=\"listitem\"</code> to convey its semantic position within the list, supports an optional accessible label for screen reader identification, and allows consumers to render any content inside it through the children slot.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as a <code>&lt;div&gt;</code> with <code>role=\"listitem\"</code> for proper list semantics within a parent <code>role=\"list\"</code> container</li>\n<li>Designed to be used inside a Kanban column; the parent column provides the <code>role=\"list\"</code> context</li>\n<li>Optional <code>label</code> prop sets <code>aria-label</code> for screen reader identification of the card</li>\n<li>All card content is provided through the <code>children</code> slot</li>\n<li>Uses props for prop destructuring with rest props spread</li>\n<li>No drag-and-drop behavior built in; consumers implement interaction as needed</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (optional) -- accessible label for the card via <code>aria-label</code></li>\n<li><code>children</code>: slot (required) -- card content (title, assignee, metadata, etc.)</li>\n<li><code>...restProps</code>: any -- additional HTML attributes spread onto the <code>&lt;div&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Task card with title and assignee:</p>\n\n<pre><code class=\"language-html\">&lt;KanbanTableTD label=\"Fix login bug\"&gt;\n    &lt;h3&gt;Fix login bug&lt;/h3&gt;\n    &lt;p&gt;Assigned to Alice&lt;/p&gt;\n    &lt;span&gt;High priority&lt;/span&gt;\n&lt;/KanbanTableTD&gt;</code></pre>\n\n<p>Dynamic task card with data binding:</p>\n\n<pre><code class=\"language-html\">&lt;KanbanTableTD label={task.title}&gt;\n    &lt;span&gt;{task.title}&lt;/span&gt;\n    &lt;span&gt;{task.priority}&lt;/span&gt;\n    &lt;span&gt;{task.assignee}&lt;/span&gt;\n&lt;/KanbanTableTD&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>None directly -- keyboard interaction depends on interactive elements within the card content provided by the consumer</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"listitem\"</code> -- identifies this as an item within a list container (the parent Kanban column with <code>role=\"list\"</code>)</li>\n<li><code>aria-label</code> -- optional accessible name for the card, set from the <code>label</code> prop, allowing screen readers to identify individual cards</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside KanbanTable to represent a single work item or task card within a board column.</li>\n<li>Use when each card needs an accessible label and listitem semantics within a list container.</li>\n<li>Use for displaying task summaries including title, assignee, priority, or due date.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside of a KanbanTable context -- use TableTD or DataTableTD for general tables.</li>\n<li>Do not use for column headings -- use KanbanTableTD instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides semantic listitem structure with <code>role=\"listitem\"</code> and optional <code>aria-label</code> for accessible card identification. The consumer provides all visual styling, including card layout, colors, shadows, drag handles, and any interactive affordances.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.kanban-table-td</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;td&gt;</code> element with class <code>kanban-table-td</code></li>\n<li>Verify role=\"listitem\"<code> -- identifies this as an item within a list container (the parent Kanban column with </code>role=\"list\"`)</li>\n<li>Verify aria-label<code> -- optional accessible name for the card, set from the </code>label` prop, allowing screen readers to identify individual cards</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Design cards with a clear visual hierarchy so that the title, assignee, and priority are scannable at a glance across columns.</li>\n<li><strong>Developers</strong>: Always provide a descriptive <code>label</code> prop so screen reader users can distinguish individual cards without reading all card content.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>KanbanTableTD is the leaf component in the KanbanTable compound pattern. It is placed inside column containers within KanbanTableBody, where each column uses <code>role=\"list\"</code>.</p>\n\n<pre><code>KanbanTable → KanbanTableBody → column (role=\"list\") → KanbanTableTD (role=\"listitem\")</code></pre>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>kanban-table</code> — a kanban board table interactive grid for organizing items by status &lt;table&gt;</li>\n<li><code>kanban-table-body</code> — a kanban board table interactive grid tbody for organizing items by status &lt;tbody&gt;</li>\n<li><code>kanban-table-foot</code> — a kanban board table interactive grid tfoot for organizing items by status &lt;tfoot&gt;</li>\n<li><code>kanban-table-head</code> — a kanban board table interactive grid thead for organizing items by status &lt;thead&gt;</li>\n<li><code>kanban-table-row</code> — a kanban board table interactive grid row for organizing items by status &lt;tr&gt;</li>\n<li><code>kanban-table-th</code> — a kanban board table interactive grid header cell for organizing items by status &lt;th&gt;</li>\n<li><code>table</code> — a table with rows and columns &lt;table&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Listitem Role: https://www.w3.org/TR/wai-aria-1.2/#listitem</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<td class=\"kanban-table-td\">Task item</td>";
  const svelteSource: string = "// In your Svelte component:\nimport KanbanTableTD from \"lily-design-system-svelte-headless/components/KanbanTableTD/KanbanTableTD.svelte\";\n\n<KanbanTableTD>\n  <!-- KanbanTableTD content -->\n</KanbanTableTD>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Kanban Table TD — Lily Design System</title>
  <meta name="description" content="A Kanban board item is a component that represents a single work item or task card within a Kanban board column. Each item typically displays summary information about a task, such as its title, as..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

