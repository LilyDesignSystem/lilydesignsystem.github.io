<script lang="ts">
  const html: string = "<h1>QR Code</h1>\n\n<p>A QR code component provides a headless container for displaying a two-dimensional barcode encoding data such as URLs, text, contact information, or other machine-readable content. Commonly used for sharing links, mobile payments, event check-ins, product information, and authentication workflows, the QR code provides a quick way to transfer information from a screen to a mobile device camera.</p>\n\n<p>The component serves as a semantic container that consumers populate with their preferred rendering approach, whether SVG paths, canvas drawings, or a third-party QR generation library. It uses <code>role=\"img\"</code> with a required <code>aria-label</code> so screen reader users understand what the QR code represents and what action scanning it will perform.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> with <code>role=\"img\"</code> to indicate the container represents an image</li>\n<li>Requires an <code>aria-label</code> describing the encoded content for accessibility</li>\n<li>Consumers provide the actual QR code rendering (SVG, canvas, etc.) as children</li>\n<li>Spreads <code>restProps</code> onto the container for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible description of the QR code content via <code>aria-label</code></li>\n<li><code>children</code>: slot (required) -- QR code rendering (SVG, canvas, or other visual output)</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;QrCode label=\"Scan to visit example.com\"&gt;\n  &lt;svg&gt;&lt;!-- QR code SVG paths --&gt;&lt;/svg&gt;\n&lt;/QrCode&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- this component is a passive display container.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"img\"</code> -- identifies the container as an image for assistive technologies</li>\n<li><code>aria-label</code> -- provides an accessible description of the QR code content, ensuring screen reader users know what the code encodes</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use QrCode to encode URLs, text, or contact information for quick mobile scanning in marketing materials, event check-ins, or payment flows.</li>\n<li>Use QrCode when users need to transfer information from screen to mobile device without manual typing.</li>\n<li>Avoid using QrCode as the sole means of sharing a link; always provide a text-based alternative for accessibility.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use as the only way to access information — always pair with a text-based equivalent (a written URL, a copy-link button) so it is accessible to keyboard, screen reader, and desktop users.</li>\n<li>Do not use to encode large or sensitive payloads — large QR codes degrade scanning reliability and printed/screenshotted codes are not secret.</li>\n<li>Do not use as a generic image — use <code>Image</code> for arbitrary pictures, or <code>Icon</code> for symbolic glyphs.</li>\n<li>Do not use without a meaningful <code>label</code> describing what scanning will do, not the raw encoded string.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>The QrCode headless component provides a <code>&lt;div&gt;</code> with <code>role=\"img\"</code> and <code>aria-label</code> so screen readers can describe what the QR code encodes. The consumer provides the actual QR code rendering (SVG, canvas, or a third-party library) as children, along with all visual styling.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.qr-code</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>qr-code</code></li>\n<li>Verify role=\"img\"` -- identifies the container as an image for assistive technologies</li>\n<li>Verify aria-label` -- provides an accessible description of the QR code content, ensuring screen reader users know what the code encodes</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Ensure the QR code has sufficient size and contrast for reliable scanning, and include a short text label or caption explaining what scanning will do.</li>\n<li><strong>Developers</strong>: Use a proven QR generation library for the children content, and set the <code>aria-label</code> to describe the action (e.g., \"Scan to visit example.com\") rather than the raw encoded data.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>image</code> — an image element with alt text</li>\n<li><code>icon</code> — a container for displaying an icon</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA img Role: https://www.w3.org/TR/wai-aria-1.2/#img</li>\n</ul>";
</script>

<svelte:head>
  <title>QR Code — Lily Design System</title>
  <meta name="description" content="A QR code component provides a headless container for displaying a two-dimensional barcode encoding data such as URLs, text, contact information, or other machine-readable content. Commonly used fo..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
