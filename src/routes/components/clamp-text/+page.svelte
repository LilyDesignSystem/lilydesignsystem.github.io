<script lang="ts">
  const html: string = "<h1>Clamp Text</h1>\n\n<p>A clamp text container truncates its inner content to a maximum number of visible lines, exposing the line count as a data attribute and CSS custom property so that consumer stylesheets can apply line-clamp without changing the underlying DOM.</p>\n\n<p>Use it when you need to constrain potentially long text such as product descriptions, comments, or card summaries to a predictable height while keeping the full content available to assistive technology.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> so block-level children can flow inside without breaking semantics</li>\n<li>Sets <code>data-lines={lines}</code> for CSS hooks and inline style <code>--clamp-text-lines: {lines}</code> for line-clamp</li>\n<li>Does not apply <code>overflow: hidden</code> or <code>-webkit-line-clamp</code> itself — the consumer chooses the clamping technique</li>\n<li>Full content remains in the DOM so screen readers can read the entire text even when visually truncated</li>\n<li><code>aria-label</code> is optional and only applied when supplied</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>lines</code>: number (default: 2) -- maximum visible lines</li>\n<li><code>label</code>: string (optional) -- aria-label for screen readers</li>\n<li><code>children</code>: slot -- the text content to clamp</li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Default two-line clamp:</p>\n\n<pre><code class=\"language-html\">&lt;ClampText&gt;\n  This is a long description that will be visually truncated to two lines\n  while remaining fully present in the document for assistive technology.\n&lt;/ClampText&gt;</code></pre>\n\n<p>Three-line clamp with accessible label:</p>\n\n<pre><code class=\"language-html\">&lt;ClampText lines={3} label=\"Product description\"&gt;\n  A longer marketing block of text that should clamp to three lines.\n&lt;/ClampText&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>None -- this is a passive presentation container.</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label</code> is rendered only when the <code>label</code> prop is provided</li>\n<li>The full content remains accessible — text is not visually hidden, just clamped</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use when text length is variable and you need a predictable visual height (cards, lists, tiles).</li>\n<li>Use when you want truncation to be a CSS responsibility instead of a DOM responsibility.</li>\n<li>Use when the full text must remain readable by screen readers.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use when the consumer needs an explicit \"Show more\" toggle -- use Collapsible or Expander instead.</li>\n<li>Do not use when truncation must be applied at the character level -- this component clamps by lines.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component renders only the structural <code>&lt;div&gt;</code> with the <code>clamp-text</code> class plus a <code>data-lines</code> attribute and a <code>--clamp-text-lines</code> CSS custom property. The consumer applies the actual line-clamp behavior (typically through <code>-webkit-line-clamp</code> and <code>display: -webkit-box</code>, or modern <code>line-clamp</code>) using the custom property.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer is responsible for the line-clamp CSS. A typical consumer style hooks the <code>--clamp-text-lines</code> custom property:</p>\n\n<pre><code class=\"language-css\">.clamp-text {\n  display: -webkit-box;\n  -webkit-box-orient: vertical;\n  -webkit-line-clamp: var(--clamp-text-lines);\n  line-clamp: var(--clamp-text-lines);\n  overflow: hidden;\n}</code></pre>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>clamp-text</code></li>\n<li>Verify <code>data-lines</code> matches the <code>lines</code> prop value</li>\n<li>Verify the inline style includes <code>--clamp-text-lines: {lines}</code></li>\n<li>Verify <code>aria-label</code> is set when the <code>label</code> prop is provided</li>\n<li>Verify children content is rendered</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Choose a line count that fits the worst-case content while preserving readability. Two or three lines is typical for cards.</li>\n<li><strong>Developers</strong>: Do not add <code>overflow: hidden</code> to the headless component itself — apply it in your consumer CSS so the truncation strategy stays explicit.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>blockquote</code> — a block-level quotation with optional source citation</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN line-clamp: https://developer.mozilla.org/en-US/docs/Web/CSS/line-clamp</li>\n<li>WCAG 1.4.4 Resize Text: https://www.w3.org/WAI/WCAG22/Understanding/resize-text</li>\n</ul>";
</script>

<svelte:head>
  <title>Clamp Text — Lily Design System</title>
  <meta name="description" content="A clamp text container truncates its inner content to a maximum number of visible lines, exposing the line count as a data attribute and CSS custom property so that consumer stylesheets can apply l..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
