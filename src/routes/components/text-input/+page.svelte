<script lang="ts">
  const html: string = "<h1>Text Input</h1>\n\n<p>A text input provides a single-line text field for entering short text values such as names, titles, or other brief textual data. It wraps the native HTML <code>&lt;input type=\"text\"&gt;</code> element with accessible labelling and two-way data binding.</p>\n\n<p>This component is suitable for any form field requiring free-text entry. It supports required and disabled states and uses <code>aria-label</code> to ensure screen readers announce the field purpose. The bindable <code>value</code> prop enables reactive two-way data flow with the parent component.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a native <code>&lt;input type=\"text\"&gt;</code> element</li>\n<li>Uses <code>aria-label</code> for accessible naming (no visible label element included; consumers can add their own)</li>\n<li>Supports two-way binding for the <code>value</code> prop, enabling two-way <code>value</code> binding in the parent</li>\n<li>Supports two-way binding on the <code>value</code> prop</li>\n<li>Supports standard HTML form attributes: <code>required</code>, <code>disabled</code></li>\n<li>Spreads <code>restProps</code> for consumer customization (e.g., <code>placeholder</code>, <code>maxlength</code>, <code>pattern</code>)</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name via <code>aria-label</code></li>\n<li><code>value</code>: string (default: <code>\"\"</code>, bindable) -- current text value, supports two-way binding</li>\n<li><code>required</code>: boolean (default: <code>false</code>) -- whether the field is required</li>\n<li><code>disabled</code>: boolean (default: <code>false</code>) -- whether the field is disabled</li>\n<li><code>...restProps</code>: any additional HTML attributes spread onto the input</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;Field label=\"What is your full name?\"&gt;\n  &lt;TextInput label=\"Full name\" autocomplete=\"name\" /&gt;\n&lt;/Field&gt;\n\n&lt;Field label=\"Enter a full postcode in England\"&gt;\n  &lt;Hint&gt;For example, LS1 1AB&lt;/Hint&gt;\n  &lt;TextInput label=\"Postcode\" autocomplete=\"postal-code\" /&gt;\n&lt;/Field&gt;\n\n&lt;Field label=\"What is your NHS number?\"&gt;\n  &lt;Hint&gt;This is a 10-digit number, like 485 777 3456&lt;/Hint&gt;\n  &lt;TextInput label=\"NHS number\" inputmode=\"numeric\" /&gt;\n&lt;/Field&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None beyond native input behavior -- standard text editing keys (typing, backspace, selection, clipboard shortcuts) are handled by the browser.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>aria-label={label}</code> -- provides an accessible name for the input since no visible <code>&lt;label&gt;</code> element is included</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use when users need to enter a short, single-line text response such as a name, postcode, or reference number</li>\n<li>Use with a visible Label positioned above the input -- never rely on placeholder text as a label</li>\n<li>Use with an appropriate input width that reflects the expected length of the answer</li>\n<li>Use with <code>inputmode=\"numeric\"</code> for fields that expect numbers but are not quantity inputs</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for multi-line responses -- use TextAreaInput instead</li>\n<li>Do not use for searching with results -- use SearchInput or TextInputWithSearch</li>\n<li>Do not use for structured date entry -- use DateInput with separate day/month/year fields</li>\n<li>Do not use for passwords -- use PasswordInput which includes show/hide functionality</li>\n<li>Do not use for email, URL, or telephone -- use EmailInput, UrlInput, or TelInput for correct keyboard and validation</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component renders a native <code>&lt;input type=\"text\"&gt;</code> with <code>aria-label</code> and two-way value binding. It provides standard text input semantics, required/disabled states, and rest prop forwarding for attributes like <code>placeholder</code>, <code>maxlength</code>, and <code>pattern</code>. The consumer provides all visual styling.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.text-input</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;input&gt;</code> element with class <code>text-input</code></li>\n<li>Verify aria-label={label}<code> -- provides an accessible name for the input since no visible </code>&lt;label&gt;` element is included</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Size the input width to match the expected content length (e.g., wider for full names, narrower for short codes). Use placeholder text sparingly and never as a substitute for labels.</li>\n<li><strong>Developers</strong>: Use <code>restProps</code> to pass <code>placeholder</code>, <code>maxlength</code>, <code>pattern</code>, and other native attributes. Wrap the input with a Field and Label component for a complete accessible form field.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>email-input</code> — an input for entering an email address &lt;input type=\"email\"&gt;</li>\n<li><code>password-input</code> — an input for entering a password with obscured text &lt;input type=\"password\"&gt;</li>\n<li><code>search-input</code> — an input for entering a search query &lt;input type=\"search\"&gt;</li>\n<li><code>tel-input</code> — an input for entering a telephone number &lt;input type=\"tel\"&gt;</li>\n<li><code>url-input</code> — an input for entering a URL &lt;input type=\"url\"&gt;</li>\n<li><code>number-input</code> — an input for entering a numeric value with validation &lt;input type=\"number\"&gt;</li>\n<li><code>text-area-input</code> — a multi-line text input area</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN input type=\"text\": https://developer.mozilla.org/en-US/docs/Web/HTML/Element/input/text</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<label class=\"label\" for=\"demo-input\">Label</label><input class=\"text-input\" type=\"text\" id=\"demo-input\" aria-label=\"TextInput\" />";
  const svelteSource: string = "// In your Svelte component:\nimport TextInput from \"lily-design-system-svelte-headless/components/TextInput/TextInput.svelte\";\n\n<TextInput label=\"Label\" />\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Text Input — Lily Design System</title>
  <meta name="description" content="A text input provides a single-line text field for entering short text values such as names, titles, or other brief textual data. It wraps the native HTML &lt;input type=&quot;text&quot;&gt; element with accessibl..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

