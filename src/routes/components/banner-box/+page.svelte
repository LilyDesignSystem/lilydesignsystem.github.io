<script lang="ts">
  const html: string = "<h1>Banner Box</h1>\n\n<p>BannerBox is a headless layout component designed to be placed inside a Banner component. It renders a <code>&lt;div&gt;</code> that the consumer styles with flexbox horizontal layout (<code>display: flex; flex-direction: row</code>) to arrange banner content items side by side, such as a message and action buttons.</p>\n\n<p>This component is useful for structuring banner content into a horizontal row, aligning text, icons, and action buttons within a banner message bar.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> element with class <code>banner-box</code></li>\n<li>Intended to be placed inside a Banner component</li>\n<li>Consumer applies flexbox horizontal styles via the <code>.banner-box</code> CSS class</li>\n<li>Uses <code>children</code> slot for flexible content rendering</li>\n<li>Spreads <code>restProps</code> onto the <code>&lt;div&gt;</code> element for consumer extensibility</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (optional) -- accessible name applied via <code>aria-label</code> if needed</li>\n<li><code>children</code>: slot (required) -- the banner box content</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;div&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;Banner label=\"Site notice\" type=\"info\"&gt;\n  &lt;BannerBox&gt;\n    &lt;span&gt;We are upgrading our servers on Saturday 12 April from 02:00 to 06:00.&lt;/span&gt;\n    &lt;a href=\"/status\"&gt;Check status&lt;/a&gt;\n  &lt;/BannerBox&gt;\n&lt;/Banner&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;Banner label=\"Session warning\" type=\"warning\" dismissible closeLabel=\"Dismiss\"&gt;\n  &lt;BannerBox&gt;\n    &lt;span&gt;Your session will expire in 5 minutes.&lt;/span&gt;\n    &lt;button type=\"button\"&gt;Extend session&lt;/button&gt;\n  &lt;/BannerBox&gt;\n&lt;/Banner&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;Banner label=\"Cookie consent\" type=\"info\"&gt;\n  &lt;BannerBox&gt;\n    &lt;p&gt;We use cookies to improve your experience.&lt;/p&gt;\n    &lt;button type=\"button\"&gt;Accept&lt;/button&gt;\n    &lt;button type=\"button\"&gt;Reject&lt;/button&gt;\n  &lt;/BannerBox&gt;\n&lt;/Banner&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>None (passive layout element)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>No additional ARIA attributes required; the parent Banner provides the landmark role and aria-live region</li>\n<li>Optional <code>aria-label</code> via <code>label</code> prop if the box needs an accessible name</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>BannerBox is a child of Banner:</p>\n\n<pre><code>Banner\n  └── BannerBox (flexbox horizontal row)\n        ├── message content\n        └── action buttons</code></pre>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside a Banner to create a horizontal flexbox container for banner content</li>\n<li>Use to arrange banner text and action buttons side by side in a single row</li>\n<li>Use to group an icon, a message, and a dismiss or action button within a Banner</li>\n<li>Use when the Banner needs a consistent horizontal layout across different banner types</li>\n<li>Use to separate content zones within a Banner, such as left-aligned text and right-aligned controls</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside a Banner -- use a standard <code>&lt;div&gt;</code> or layout component for non-banner horizontal layouts</li>\n<li>Do not use for medical banners -- use MedicalBannerBox inside MedicalBanner instead</li>\n<li>Do not use for vertical stacking of banner content -- apply column-direction styles directly to the Banner or use multiple BannerBox elements</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;div&gt;</code> with a <code>.banner-box</code> class for consumer CSS targeting. The consumer is responsible for all flexbox styling including <code>display: flex</code>, <code>flex-direction: row</code>, alignment, gap, and any responsive behavior.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.banner-box</code> class for targeting. Typical consumer styles include <code>display: flex; flex-direction: row; align-items: center; gap: 1rem;</code>. No default styles are included -- this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>banner-box</code></li>\n<li>Verify it renders children content</li>\n<li>Verify pass-through attributes are applied to the <code>&lt;div&gt;</code></li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use BannerBox to create a horizontal layout within banners. Consider responsive behavior -- the consumer may want to switch to vertical stacking on narrow screens.</li>\n<li><strong>Developers</strong>: Apply <code>display: flex; flex-direction: row;</code> to <code>.banner-box</code> in your CSS. Use <code>align-items: center</code> for vertical centering and <code>gap</code> for spacing between items.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>announcement-banner</code> — a banner highlighting important messages for all users</li>\n<li><code>banner</code> — a prominent message bar across the top of a page</li>\n<li><code>government-banner</code> — a banner identifying a website as belonging to a government, with an expandable details panel</li>\n<li><code>medical-banner</code> — a prominent message bar across the top of a page, with medical information</li>\n<li><code>medical-banner-box</code> — a medical banner box that is inside a medical-banner component, using flexbox horizontal, with medical information</li>\n<li><code>medical-banner-box-for-advice</code> — a medical record banner box for advice information e.g. contacts, contexts, plans, etc.</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>Parent component: Banner</li>\n<li>CSS Flexbox: https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_flexible_box_layout</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"banner-box\" aria-label=\"a banner box that is inside a banner component, using flexbox horizontal\">BannerBox</div>";
  const svelteSource: string = "// In your Svelte component:\nimport BannerBox from \"lily-design-system-svelte-headless/components/BannerBox/BannerBox.svelte\";\n\n<BannerBox>\n  <!-- content -->\n</BannerBox>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Banner Box — Lily Design System</title>
  <meta name="description" content="BannerBox is a headless layout component designed to be placed inside a Banner component. It renders a &lt;div&gt; that the consumer styles with flexbox horizontal layout (display: flex; flex-direction: ..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

