<script lang="ts">
  const html: string = "<h1>Tree Select</h1>\n\n<p>A tree select is a select dropdown showing a tree of hierarchical options. It exposes a trigger button and a panel that holds tree-shaped options.</p>\n\n<p>Use it for hierarchical selection where the options form a tree (e.g., organizational unit, category, file system path) and the user selects one or many leaves or branches.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> with <code>role=\"combobox\"</code>, <code>aria-haspopup=\"tree\"</code>, <code>aria-expanded</code>, <code>aria-label</code></li>\n<li>Sets <code>aria-multiselectable</code> on the container when <code>multiple</code> is true</li>\n<li>The trigger is a native <code>&lt;button class=\"tree-select-trigger\" type=\"button\"&gt;</code></li>\n<li>The panel is a <code>&lt;div class=\"tree-select-panel\"&gt;</code> with <code>hidden</code> toggled by <code>expanded</code></li>\n<li><code>label</code> is <strong>required</strong> and sets <code>aria-label</code> on the combobox</li>\n<li>Children typically include a TreeNav/TreeList component</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (<strong>required</strong>) -- aria-label on the combobox</li>\n<li><code>expanded</code>: boolean (default: false) -- whether the tree panel is open</li>\n<li><code>disabled</code>: boolean (default: false) -- disable the trigger button</li>\n<li><code>multiple</code>: boolean (default: false) -- allow multiple selection (sets aria-multiselectable)</li>\n<li><code>placeholder</code>: string (optional) -- placeholder text on the trigger</li>\n<li><code>value</code>: string (optional) -- formatted display value</li>\n<li><code>onclick</code> / <code>onClick</code>: callback (optional) -- trigger click handler</li>\n<li><code>children</code>: slot -- tree content (typically TreeNav/TreeList)</li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;TreeSelect\n  label=\"Select category\"\n  expanded={isOpen}\n  value={selectedLabel}\n  placeholder=\"Choose…\"\n  onclick={togglePanel}\n&gt;\n  &lt;ul role=\"tree\"&gt;...&lt;/ul&gt;\n&lt;/TreeSelect&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;TreeSelect\n  label=\"Select multiple departments\"\n  multiple\n  expanded={isOpen}\n  value={selectedNames}\n  onclick={togglePanel}\n&gt;\n  &lt;ul role=\"tree\" aria-multiselectable=\"true\"&gt;...&lt;/ul&gt;\n&lt;/TreeSelect&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus the trigger</li>\n<li>Enter / Space: Toggle the panel (consumer handler)</li>\n<li>Arrow keys: Consumer-supplied tree navigation inside the panel</li>\n<li>Escape: Consumer should close the panel and return focus to the trigger</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"combobox\"</code> on the container</li>\n<li><code>aria-haspopup=\"tree\"</code> indicates the panel contains tree-shaped options</li>\n<li><code>aria-expanded</code> reflects the <code>expanded</code> prop</li>\n<li><code>aria-label</code> is the entire accessible name (required)</li>\n<li><code>aria-multiselectable=\"true\"</code> is set on the container only when <code>multiple</code> is true</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use when the option set is naturally hierarchical and benefits from a tree presentation.</li>\n<li>Use when the user must select one or more nodes anywhere in the hierarchy.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for flat lists — use Select or Combobox.</li>\n<li>Do not use when only path-style selection is needed — use Cascader.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>Renders semantic combobox markup with no styling. The consumer is responsible for opening/closing the panel, focus management, keyboard navigation, and the visual treatment.</p>\n\n<h2>Styles</h2>\n\n<p>Consumer CSS targets <code>tree-select</code>, <code>tree-select-trigger</code>, and <code>tree-select-panel</code>. Provide visible focus rings, disabled state styling, and a clear distinction between the collapsed and expanded states.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>tree-select</code></li>\n<li>Verify <code>role=\"combobox\"</code> and <code>aria-haspopup=\"tree\"</code> are set</li>\n<li>Verify <code>aria-expanded</code> reflects the <code>expanded</code> prop</li>\n<li>Verify <code>aria-label</code> equals the <code>label</code> prop</li>\n<li>Verify <code>aria-multiselectable=\"true\"</code> is set only when <code>multiple</code> is true</li>\n<li>Verify the trigger button is disabled when <code>disabled</code> is true</li>\n<li>Verify the panel is <code>hidden</code> when <code>expanded</code> is false</li>\n<li>Verify children render inside the panel</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Show selected values clearly on the trigger. For multiple selection, consider a chip/tag list.</li>\n<li><strong>Developers</strong>: Manage <code>expanded</code> state externally. Use a TreeNav/TreeList in the panel to provide proper ARIA tree semantics.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>combobox</code> — a text input combined with a dropdown list for filtering options</li>\n<li><code>tree-nav</code> — a hierarchical navigation with expandable branches</li>\n<li><code>select</code> — a dropdown select element for choosing one option</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Combobox Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/combobox/</li>\n<li>WAI-ARIA Tree Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/treeview/</li>\n<li>Ant Design TreeSelect: https://ant.design/components/tree-select</li>\n</ul>";
</script>

<svelte:head>
  <title>Tree Select — Lily Design System</title>
  <meta name="description" content="A tree select is a select dropdown showing a tree of hierarchical options. It exposes a trigger button and a panel that holds tree-shaped options." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
