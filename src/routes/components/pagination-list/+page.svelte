<script lang="ts">
  const html: string = "<h1>Pagination List</h1>\n\n<p>A pagination list is a navigation component that divides content into separate pages, allowing users to navigate through large sets of data or information one section at a time. The PaginationList component provides a headless wrapper using the semantic <code>&lt;nav&gt;</code> and <code>&lt;ul&gt;</code> elements, with an accessible label to identify the navigation purpose.</p>\n\n<p>This component is commonly used in search results, product listings, and data tables where displaying all content at once would be impractical. The consumer provides pagination items (links or buttons) as <code>&lt;li&gt;</code> children, and can use <code>aria-current=\"page\"</code> to indicate the currently active page.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;nav&gt;</code> element wrapping a <code>&lt;ul&gt;</code> list</li>\n<li>The <code>&lt;nav&gt;</code> element uses <code>aria-label</code> for identification by assistive technology</li>\n<li>The <code>label</code> prop is required to distinguish this navigation from other <code>&lt;nav&gt;</code> elements on the page</li>\n<li>Consumer provides <code>&lt;li&gt;</code> children containing page links or buttons</li>\n<li>Rest props are spread onto the <code>&lt;nav&gt;</code> element</li>\n<li>This is a compound component; see PaginationListItem for individual page items</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name for the pagination navigation region via <code>aria-label</code></li>\n<li><code>children</code>: slot (required) -- pagination list items, typically <code>&lt;li&gt;</code> elements containing links or buttons</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;nav&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Numbered pagination list with previous/next controls for search results:</p>\n\n<pre><code class=\"language-html\">&lt;PaginationNav label=\"Search results pages\"&gt;\n  &lt;PaginationList&gt;\n    &lt;PaginationListItem&gt;\n      &lt;PaginationLink href=\"/results?page=1\" rel=\"prev\"&gt;Previous&lt;/PaginationLink&gt;\n    &lt;/PaginationListItem&gt;\n    &lt;PaginationListItem&gt;\n      &lt;PaginationLink href=\"/results?page=1\" label=\"Page 1\"&gt;1&lt;/PaginationLink&gt;\n    &lt;/PaginationListItem&gt;\n    &lt;PaginationListItem&gt;\n      &lt;PaginationLink href=\"/results?page=2\" label=\"Page 2\" aria-current=\"page\"&gt;2&lt;/PaginationLink&gt;\n    &lt;/PaginationListItem&gt;\n    &lt;PaginationListItem&gt;\n      &lt;PaginationLink href=\"/results?page=3\" label=\"Page 3\"&gt;3&lt;/PaginationLink&gt;\n    &lt;/PaginationListItem&gt;\n    &lt;PaginationListItem&gt;\n      &lt;PaginationLink href=\"/results?page=3\" rel=\"next\"&gt;Next&lt;/PaginationLink&gt;\n    &lt;/PaginationListItem&gt;\n  &lt;/PaginationList&gt;\n&lt;/PaginationNav&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Moves focus between pagination links/buttons in the list</li>\n<li>Enter: Activates the focused link or button to navigate to that page</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>&lt;nav aria-label=\"...\"&gt;</code> -- identifies the pagination as a navigation landmark with a descriptive label</li>\n<li><code>aria-current=\"page\"</code> -- consumer applies this to the link representing the current page</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside PaginationNav to contain the ordered list of pagination links</li>\n<li>Use as a semantic list to convey the sequence of pages to assistive technologies</li>\n<li>Use when paginating large data sets such as search results, product listings, or data tables</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside of a PaginationNav container -- it requires the nav landmark for accessibility</li>\n<li>Do not use for general lists of links -- use ContentsList or a standard list instead</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;nav&gt;</code> wrapping a <code>&lt;ul&gt;</code> with <code>aria-label</code> for landmark identification and proper list semantics for pagination items. The consumer provides all visual styling, including page link appearance, active-page highlighting, previous/next button styling, and responsive layout.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.pagination-list</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;ol&gt;</code> element with class <code>pagination-list</code></li>\n<li>Verify &lt;nav aria-label=\"...\"&gt;` -- identifies the pagination as a navigation landmark with a descriptive label</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Highlight the current page clearly and provide previous/next controls. Consider showing ellipsis for large page ranges to keep the pagination compact.</li>\n<li><strong>Developers</strong>: Apply <code>aria-current=\"page\"</code> to the link for the currently active page. Use PaginationListItem for each page entry to maintain consistent markup.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>PaginationList is part of the PaginationNav compound component. It renders inside PaginationNav and contains PaginationListItem children.</p>\n\n<pre><code>PaginationNav → PaginationList (nav + ul) → PaginationListItem (li)</code></pre>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>pagination-nav</code> — an ordered list of page navigation links</li>\n<li><code>pagination-list-item</code> — one pagination list item in the trail</li>\n<li><code>pagination-link</code> — one pagination link in the trail</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Practices - Pagination: https://www.w3.org/WAI/ARIA/apg/patterns/navigation/</li>\n<li>WAI-ARIA navigation role: https://www.w3.org/TR/wai-aria-1.2/#navigation</li>\n</ul>";
</script>

<svelte:head>
  <title>Pagination List — Lily Design System</title>
  <meta name="description" content="A pagination list is a navigation component that divides content into separate pages, allowing users to navigate through large sets of data or information one section at a time. The PaginationList ..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
