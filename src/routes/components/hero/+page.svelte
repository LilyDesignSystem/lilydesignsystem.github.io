<script lang="ts">
  const html: string = "<h1>Hero</h1>\n\n<p>A hero is a large, prominent section typically placed at the top of a page, featuring a title, description, and optionally a background image or call-to-action. Heroes are used to create a strong visual introduction and draw attention to key content.</p>\n\n<p>This headless component uses a <code>&lt;section&gt;</code> element with an <code>aria-label</code> to create a labeled landmark region for the hero content.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses <code>&lt;section&gt;</code> element for landmark semantics</li>\n<li><code>aria-label</code> describes the hero section for screen readers</li>\n<li>Contains slots for title, description, image, and call-to-action</li>\n<li>Consumer controls the visual presentation via CSS</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible label for the hero section</li>\n<li><code>children</code>: slot (required) -- hero content including title, description, and CTA</li>\n<li><code>...restProps</code>: Any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;Hero label=\"Welcome to our platform\"&gt;\n  &lt;h1&gt;Build Something Amazing&lt;/h1&gt;\n  &lt;p&gt;The fastest way to create beautiful, accessible web applications.&lt;/p&gt;\n  &lt;Button&gt;Get Started&lt;/Button&gt;\n&lt;/Hero&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;Hero label=\"Register for a GP\"&gt;\n  &lt;h1&gt;Register with a GP surgery&lt;/h1&gt;\n  &lt;p&gt;Everyone in England can register with a GP surgery. It is free to register.&lt;/p&gt;\n  &lt;ActionLink href=\"/register\"&gt;Register now&lt;/ActionLink&gt;\n&lt;/Hero&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;Hero label=\"Lily Design System\"&gt;\n  &lt;h1&gt;Lily Design System&lt;/h1&gt;\n  &lt;p&gt;Headless, accessible components for healthcare and government services.&lt;/p&gt;\n  &lt;Button&gt;View components&lt;/Button&gt;\n  &lt;Button&gt;Read the docs&lt;/Button&gt;\n&lt;/Hero&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>None at the section level -- interactive children handle their own keyboard interactions</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Implicit <code>region</code> role from <code>&lt;section&gt;</code> element with <code>aria-label</code></li>\n<li><code>aria-label</code> -- describes the hero section for screen readers</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use at the top of a landing page to introduce the service with a prominent heading, description, and optional background image</li>\n<li>Use when you need a large visual introduction that sets the context for the page</li>\n<li>Use on marketing or public-facing pages where a strong first impression matters</li>\n<li>Use to combine a headline, supporting text, and a primary call-to-action in a single prominent section</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use on transactional pages -- keep forms focused without large visual elements</li>\n<li>Do not use multiple heroes on one page -- use Card or Panel for secondary content blocks</li>\n<li>Do not use for repeated content sections within a page -- use Card, Tile, or Panel instead</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides a <code>&lt;section&gt;</code> element with <code>aria-label</code> for landmark navigation, with zero visual styling. The consumer is responsible for all CSS including background image, overlay, text alignment, padding, responsive sizing, and CTA button placement.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.hero</code> class for targeting. No default styles are included -- this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;section&gt;</code> element with class <code>hero</code></li>\n<li>Verify <code>aria-label</code> is applied</li>\n<li>Verify child content slots are rendered correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Keep hero content concise and focused. Use high-contrast text over images. Ensure text is readable at all viewport sizes. Include a clear call-to-action.</li>\n<li><strong>Developers</strong>: Always provide a descriptive <code>label</code> prop for screen reader landmark navigation. Use responsive images and consider lazy loading for hero backgrounds.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>feature-card</code> — a large content card with a prominent image positioned alongside or above the text</li>\n<li><code>call-to-action</code> — a prominent prompt encouraging user action</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/HTML/Element/section\">MDN section element</a></li>\n</ul>";
</script>

<svelte:head>
  <title>Hero — Lily Design System</title>
  <meta name="description" content="A hero is a large, prominent section typically placed at the top of a page, featuring a title, description, and optionally a background image or call-to-action. Heroes are used to create a strong v..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
