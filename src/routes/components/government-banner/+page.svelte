<script lang="ts">
  const html: string = "<h1>Government Banner</h1>\n\n<p>A government banner is an <code>&lt;aside&gt;</code> placed at the very top of a page that identifies the website as belonging to a government, with an expandable details panel typically explaining the <code>.gov</code> domain and HTTPS lock icon.</p>\n\n<p>Use it on government and public-sector sites that need to clearly signal official status to visitors and provide trust context on demand.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses an <code>&lt;aside&gt;</code> element so it is announced as a complementary landmark</li>\n<li><code>label</code> prop is <strong>required</strong> — sets <code>aria-label</code> on the <code>&lt;aside&gt;</code></li>\n<li><code>headerText</code> prop is <strong>required</strong> — the always-visible top headline</li>\n<li><code>expandLabel</code> prop is <strong>required</strong> — text on the toggle button</li>\n<li><code>expanded</code> prop controls whether the details panel is visible</li>\n<li>The toggle <code>&lt;button&gt;</code> has <code>aria-expanded</code> and <code>aria-controls</code> linking to the panel</li>\n<li>The panel has a stable id and <code>hidden</code> attribute when collapsed</li>\n<li><code>onToggle</code> / <code>ontoggle</code> callback fires when the user activates the toggle</li>\n<li>Children render inside the details panel (typically two columns explaining .gov and HTTPS)</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (<strong>required</strong>) -- aria-label for the banner</li>\n<li><code>headerText</code>: string (<strong>required</strong>) -- the always-visible top headline</li>\n<li><code>expanded</code>: boolean (default: false) -- whether the details panel is open</li>\n<li><code>expandLabel</code>: string (<strong>required</strong>) -- toggle button text</li>\n<li><code>onToggle</code> / <code>ontoggle</code>: callback (optional) -- toggle handler</li>\n<li><code>children</code>: slot -- details panel content</li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Default collapsed banner:</p>\n\n<pre><code class=\"language-html\">&lt;GovernmentBanner\n  label=\"Official government website\"\n  headerText=\"An official website of the United States government\"\n  expandLabel=\"Here's how you know\"\n&gt;\n  &lt;div&gt;\n    &lt;h3&gt;Official websites use .gov&lt;/h3&gt;\n    &lt;p&gt;A .gov website belongs to an official government organization.&lt;/p&gt;\n  &lt;/div&gt;\n  &lt;div&gt;\n    &lt;h3&gt;Secure .gov websites use HTTPS&lt;/h3&gt;\n    &lt;p&gt;A lock icon means you have securely connected to the .gov website.&lt;/p&gt;\n  &lt;/div&gt;\n&lt;/GovernmentBanner&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: focus the toggle button</li>\n<li>Enter: activate the toggle button</li>\n<li>Space: activate the toggle button</li>\n<li>(All handled natively by the <code>&lt;button&gt;</code> element)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>&lt;aside&gt;</code> provides the implicit <code>complementary</code> landmark role</li>\n<li><code>aria-label</code> on the <code>&lt;aside&gt;</code> names the landmark</li>\n<li>Toggle button has <code>aria-expanded</code> reflecting the panel state</li>\n<li>Toggle button has <code>aria-controls</code> referencing the panel id</li>\n<li>Panel uses the <code>hidden</code> attribute when collapsed</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use at the top of every page on a government website to identify it as official.</li>\n<li>Use to provide on-demand explanations of government domains and HTTPS.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use on non-government sites.</li>\n<li>Do not use as a generic alert or notification — use <code>Banner</code> or <code>Alert</code> instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>Renders the structural skeleton — <code>&lt;aside&gt;</code> with <code>&lt;header&gt;</code>, toggle <code>&lt;button&gt;</code>, and panel <code>&lt;div&gt;</code> — wired with the correct ARIA. Visual treatment, icons, columns, and animation are entirely the consumer's responsibility.</p>\n\n<h2>Styles</h2>\n\n<p>Consumer CSS targets the <code>government-banner</code> class plus the inner <code>government-banner-header</code>, <code>government-banner-toggle</code>, and <code>government-banner-details</code> classes for fine-grained control.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders an <code>&lt;aside&gt;</code> element with class <code>government-banner</code></li>\n<li>Verify <code>aria-label</code> is present and equals the <code>label</code> prop</li>\n<li>Verify the headline text equals <code>headerText</code></li>\n<li>Verify the toggle button exists with the <code>expandLabel</code> text</li>\n<li>Verify <code>aria-expanded</code> reflects the <code>expanded</code> prop</li>\n<li>Verify <code>aria-controls</code> references the panel id</li>\n<li>Verify the panel is hidden when <code>expanded</code> is false</li>\n<li>Verify the panel is visible when <code>expanded</code> is true</li>\n<li>Verify the toggle handler fires on click</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Keep the headline short and treat the toggle as a passive disclosure. Use a flag or shield glyph next to the headline for fast recognition.</li>\n<li><strong>Developers</strong>: Generate a stable id for the panel (use <code>useId</code> in React, <code>crypto.randomUUID()</code> server-side, or a deterministic helper). Always wire the toggle handler to flip <code>expanded</code>.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>announcement-banner</code> — a banner highlighting important messages for all users</li>\n<li><code>banner</code> — a prominent message bar across the top of a page</li>\n<li><code>banner-box</code> — a banner box that is inside a banner component, using flexbox horizontal</li>\n<li><code>medical-banner</code> — a prominent message bar across the top of a page, with medical information</li>\n<li><code>medical-banner-box</code> — a medical banner box that is inside a medical-banner component, using flexbox horizontal, with medical information</li>\n<li><code>medical-banner-box-for-advice</code> — a medical record banner box for advice information e.g. contacts, contexts, plans, etc.</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>US Web Design System Banner: https://designsystem.digital.gov/components/banner/</li>\n<li>WAI-ARIA Disclosure Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/disclosure/</li>\n<li>WCAG 4.1.2 Name, Role, Value: https://www.w3.org/WAI/WCAG22/Understanding/name-role-value</li>\n</ul>";
</script>

<svelte:head>
  <title>Government Banner — Lily Design System</title>
  <meta name="description" content="A government banner is an &lt;aside&gt; placed at the very top of a page that identifies the website as belonging to a government, with an expandable details panel typically explaining the .gov domain an..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
