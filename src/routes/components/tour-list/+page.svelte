<script lang="ts">
  const html: string = "<h1>Tour List</h1>\n\n<p>TourList is a headless guided walkthrough overlay that presents step-by-step instructions to users. It renders as a modal dialog when active and can be dismissed with the Escape key, providing a structured onboarding or feature discovery experience.</p>\n\n<p>This component is useful for onboarding flows, feature tutorials, or contextual help systems. The <code>active</code> prop is bindable, allowing the consumer to programmatically start or stop the tour. Step content, navigation buttons, and progress indicators are provided by the consumer as children.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;ol&gt;</code> element with class <code>tour-list</code></li>\n<li>Spreads <code>restProps</code> onto the root element for consumer customization</li>\n</ul>\n\n<h2>Help</h2>\n\n<p>Use TourList when you need to walk users through a sequence of steps in a modal overlay. Common scenarios include onboarding flows, feature discovery tours, and contextual help walkthroughs. Pair with TourListItem for individual steps.</p>\n\n<h2>Syntax</h2>\n\n<pre><code class=\"language-html\">&lt;TourList label=\"...\" active={active}&gt;\n  &lt;!-- TourListItem children --&gt;\n&lt;/TourList&gt;</code></pre>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;Tour label=\"Onboarding\"&gt;\n  &lt;TourList label=\"Getting started guide\" active={showTour}&gt;\n    &lt;TourListItem label=\"Dashboard overview\" current={step === 0} stepNumber={1} totalSteps={3}&gt;\n      &lt;p&gt;This is your dashboard. It shows recent activity and key metrics.&lt;/p&gt;\n      &lt;button onclick={() =&gt; step++}&gt;Next&lt;/button&gt;\n      &lt;button onclick={() =&gt; showTour = false}&gt;Skip&lt;/button&gt;\n    &lt;/TourListItem&gt;\n    &lt;TourListItem label=\"Navigation\" current={step === 1} stepNumber={2} totalSteps={3}&gt;\n      &lt;p&gt;Use the sidebar to navigate between patients, appointments, and reports.&lt;/p&gt;\n      &lt;button onclick={() =&gt; step--}&gt;Back&lt;/button&gt;\n      &lt;button onclick={() =&gt; step++}&gt;Next&lt;/button&gt;\n    &lt;/TourListItem&gt;\n    &lt;TourListItem label=\"Quick actions\" current={step === 2} stepNumber={3} totalSteps={3}&gt;\n      &lt;p&gt;Use the task bar at the top for common actions like booking appointments.&lt;/p&gt;\n      &lt;button onclick={() =&gt; step--}&gt;Back&lt;/button&gt;\n      &lt;button onclick={() =&gt; showTour = false}&gt;Finish&lt;/button&gt;\n    &lt;/TourListItem&gt;\n  &lt;/TourList&gt;\n&lt;/Tour&gt;</code></pre>\n\n<h2>Props</h2>\n\n<table>\n<thead>\n<tr><th>Prop</th><th>Type</th><th>Default</th><th>Description</th></tr>\n</thead>\n<tbody>\n<tr><td><code>label</code></td><td><code>string</code></td><td>(required)</td><td>Accessible name for the tour dialog via <code>aria-label</code></td></tr>\n<tr><td><code>active</code></td><td><code>boolean</code></td><td><code>false</code></td><td>Whether the tour is currently visible; bindable with two-way <code>active</code> binding</td></tr>\n<tr><td><code>children</code></td><td><code>slot</code></td><td>(required)</td><td>Tour step content, navigation controls, and any additional UI</td></tr>\n<tr><td><code>...restProps</code></td><td>HTML attributes</td><td></td><td>Additional attributes spread onto the dialog <code>&lt;div&gt;</code></td></tr>\n</tbody>\n</table>\n\n<h2>Examples</h2>\n\n<p>With step management:</p>\n\n<pre><code class=\"language-html\">&lt;button onclick={() =&gt; { active = true; step = 0; }}&gt;Start tour&lt;/button&gt;\n\n&lt;TourList label=\"App walkthrough\" active={active}&gt;\n  &lt;TourListItem label=\"Step one\" current={step === 0} stepNumber={1} totalSteps={2}&gt;\n    &lt;p&gt;First step content&lt;/p&gt;\n    &lt;button onclick={() =&gt; step++}&gt;Next&lt;/button&gt;\n  &lt;/TourListItem&gt;\n  &lt;TourListItem label=\"Step two\" current={step === 1} stepNumber={2} totalSteps={2}&gt;\n    &lt;p&gt;Second step content&lt;/p&gt;\n    &lt;button onclick={() =&gt; active = false}&gt;Finish&lt;/button&gt;\n  &lt;/TourListItem&gt;\n&lt;/TourList&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Escape: Closes the tour by setting <code>active</code> to false</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"dialog\"</code> -- identifies the tour overlay as a dialog window</li>\n<li><code>aria-modal=\"true\"</code> -- indicates the dialog is modal and content behind it is inert</li>\n<li><code>aria-label={label}</code> -- provides an accessible name describing the tour</li>\n<li><code>tabindex=\"-1\"</code> -- allows the dialog element to receive focus for keyboard event handling</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside Tour to provide the ordered list of tour steps.</li>\n<li>Use when guidance needs to appear as a modal overlay that focuses user attention on one step at a time.</li>\n<li>Use with TourListItem children to define each individual step with progress information.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside Tour -- use ContentsList or TimelineList for other ordered lists.</li>\n<li>Do not use for simple tooltips or hints -- use Tooltip or InformationCallout instead.</li>\n<li>Do not use when users need to interact with the page while reading instructions -- consider a non-modal approach.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a modal <code>&lt;div&gt;</code> with <code>role=\"dialog\"</code>, <code>aria-modal=\"true\"</code>, <code>aria-label</code>, and Escape key dismissal. The consumer provides all step content, navigation buttons, progress indicators, and all visual styling including backdrop, positioning, and animations.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.tour-list</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;ol&gt;</code> element with class <code>tour-list</code></li>\n<li>Verify role=\"dialog\"` -- identifies the tour overlay as a dialog window</li>\n<li>Verify aria-label={label}` -- provides an accessible name describing the tour</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Include clear step progress indicators (e.g., \"Step 2 of 5\") and provide both \"Next\" and \"Skip\" options so users are not forced through the entire tour.</li>\n<li><strong>Developers</strong>: Manage step state externally and pass it to TourListItem children via the <code>current</code> prop. Trap focus within the dialog while active.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>TourList contains TourListItem children following the Guide/GuideList/GuideListItem pattern. TourList provides the modal dialog overlay, and each TourListItem provides one step with conditional visibility based on the <code>current</code> prop.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>tour-list-item</code> — one step in a tour guide list</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Dialog Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<ol class=\"tour-list\"><li class=\"tour-list-item\">Step 1: Welcome</li><li class=\"tour-list-item\">Step 2: Features</li></ol>";
  const svelteSource: string = "// In your Svelte component:\nimport TourList from \"lily-design-system-svelte-headless/components/TourList/TourList.svelte\";\n\n<TourList>\n  <!-- list children -->\n</TourList>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Tour List — Lily Design System</title>
  <meta name="description" content="TourList is a headless guided walkthrough overlay that presents step-by-step instructions to users. It renders as a modal dialog when active and can be dismissed with the Escape key, providing a st..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

