<script lang="ts">
  const html: string = "<h1>Kanban Table Head</h1>\n\n<p>A Kanban table head provides a header row for a Kanban board, containing column headings that label each workflow stage (e.g., \"To Do\", \"In Progress\", \"Done\"). It renders as a <code>&lt;div&gt;</code> with appropriate ARIA attributes and is designed to be used as a child of a KanbanTable component. The header establishes the visual and semantic structure for the columns below it.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders as a <code>&lt;div&gt;</code> for the Kanban board header area</li>\n<li>Contains column heading elements that label each workflow stage</li>\n<li>Consumer provides heading content through the children slot, typically a row of heading elements</li>\n<li>Spreads <code>...restProps</code> onto the <code>&lt;div&gt;</code> element for consumer customization</li>\n<li>No internal state -- purely a structural wrapper</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>children</code>: slot (required) -- column heading content for each workflow stage</li>\n<li><code>...restProps</code>: any -- additional HTML attributes spread onto the <code>&lt;div&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Kanban board header with three workflow stages:</p>\n\n<pre><code class=\"language-html\">&lt;KanbanTable label=\"Sprint board\"&gt;\n    &lt;KanbanTableHead&gt;\n        &lt;KanbanTableRow&gt;\n            &lt;KanbanTableTD&gt;To Do&lt;/KanbanTableTD&gt;\n            &lt;KanbanTableTD&gt;In Progress&lt;/KanbanTableTD&gt;\n            &lt;KanbanTableTD&gt;Done&lt;/KanbanTableTD&gt;\n        &lt;/KanbanTableRow&gt;\n    &lt;/KanbanTableHead&gt;\n    &lt;KanbanTableBody&gt;\n        ...\n    &lt;/KanbanTableBody&gt;\n&lt;/KanbanTable&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<p>None -- passive container for heading elements. Keyboard interaction depends on interactive elements within the header content provided by the consumer.</p>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>No explicit ARIA roles required -- the headings within provide their own semantic structure</li>\n<li>Column headings should use appropriate heading levels (e.g., <code>&lt;h3&gt;</code>) for proper document outline</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside KanbanTable to provide column headings that label each workflow stage.</li>\n<li>Use when users need clear, visible labels for board columns such as \"To Do\", \"In Progress\", \"Done\".</li>\n<li>Use to establish the visual and semantic structure for the columns below it.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside of a KanbanTable parent -- use TableHead or DataTableHead for general tables.</li>\n<li>Do not use for footer summaries -- use KanbanTableFoot instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a structural header wrapper for Kanban board column headings. It outputs a <code>&lt;div&gt;</code> container where the consumer places heading elements. The consumer provides all visual styling, including heading typography, alignment, and column width coordination.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.kanban-table-head</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;thead&gt;</code> element with class <code>kanban-table-head</code></li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Align column headings with their corresponding body columns and use consistent typography so the board structure is immediately clear.</li>\n<li><strong>Developers</strong>: Use appropriate heading levels (e.g., <code>&lt;h3&gt;</code>) inside the header to maintain proper document outline and screen reader navigation.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>KanbanTableHead is a child of KanbanTable, appearing before KanbanTableBody. It is part of the KanbanTable compound component pattern.</p>\n\n<pre><code>KanbanTable → KanbanTableHead + KanbanTableBody + KanbanTableFoot\n                └→ column headings</code></pre>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>kanban-table</code> — a kanban board table interactive grid for organizing items by status &lt;table&gt;</li>\n<li><code>kanban-table-body</code> — a kanban board table interactive grid tbody for organizing items by status &lt;tbody&gt;</li>\n<li><code>kanban-table-foot</code> — a kanban board table interactive grid tfoot for organizing items by status &lt;tfoot&gt;</li>\n<li><code>kanban-table-row</code> — a kanban board table interactive grid row for organizing items by status &lt;tr&gt;</li>\n<li><code>kanban-table-td</code> — kanban board table interactive grid data cell for organizing items by status &lt;td&gt;</li>\n<li><code>kanban-table-th</code> — a kanban board table interactive grid header cell for organizing items by status &lt;th&gt;</li>\n<li><code>table</code> — a table with rows and columns &lt;table&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA region role: https://www.w3.org/TR/wai-aria-1.2/#region</li>\n</ul>";
</script>

<svelte:head>
  <title>Kanban Table Head — Lily Design System</title>
  <meta name="description" content="A Kanban table head provides a header row for a Kanban board, containing column headings that label each workflow stage (e.g., &quot;To Do&quot;, &quot;In Progress&quot;, &quot;Done&quot;). It renders as a &lt;div&gt; with appropriat..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
