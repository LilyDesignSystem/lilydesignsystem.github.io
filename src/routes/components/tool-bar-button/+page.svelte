<script lang="ts">
  const html: string = "<h1>Tool Bar Button</h1>\n\n<p>A tool bar button is a single button within a toolbar, rendered as a native <code>&lt;button&gt;</code> element for proper keyboard and screen reader support. It is designed to be placed inside a ToolBar container that manages roving focus keyboard navigation.</p>\n\n<p>The component uses <code>&lt;button type=\"button\"&gt;</code> to avoid unintended form submissions and supports the <code>disabled</code> attribute for disabling interaction. Content is provided through the children slot, allowing full flexibility in button content.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;button&gt;</code> element with class <code>tool-bar-button</code></li>\n<li>Spreads <code>restProps</code> onto the root element for consumer customization</li>\n</ul>\n\n<h2>Help</h2>\n\n<p>Use ToolBarButton for individual action buttons within a ToolBar. Common scenarios include text formatting buttons (Bold, Italic, Underline), drawing tool selectors, or media controls. The parent ToolBar manages keyboard navigation between buttons.</p>\n\n<h2>Syntax</h2>\n\n<pre><code class=\"language-html\">&lt;ToolBarButton&gt;Label&lt;/ToolBarButton&gt;</code></pre>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;ToolBar label=\"Text formatting\"&gt;\n  &lt;ToolBarButton&gt;Bold&lt;/ToolBarButton&gt;\n  &lt;ToolBarButton&gt;Italic&lt;/ToolBarButton&gt;\n  &lt;ToolBarButton disabled&gt;Strikethrough&lt;/ToolBarButton&gt;\n&lt;/ToolBar&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;ToolBar label=\"Drawing tools\"&gt;\n  &lt;ToolBarButton aria-label=\"Pen tool\"&gt;Pen&lt;/ToolBarButton&gt;\n  &lt;ToolBarButton aria-label=\"Eraser tool\"&gt;Eraser&lt;/ToolBarButton&gt;\n  &lt;ToolBarButton aria-label=\"Fill tool\" disabled&gt;Fill&lt;/ToolBarButton&gt;\n&lt;/ToolBar&gt;</code></pre>\n\n<h2>Props</h2>\n\n<table>\n<thead>\n<tr><th>Prop</th><th>Type</th><th>Default</th><th>Description</th></tr>\n</thead>\n<tbody>\n<tr><td><code>disabled</code></td><td><code>boolean</code></td><td><code>false</code></td><td>Whether the button is disabled</td></tr>\n<tr><td><code>children</code></td><td><code>slot</code></td><td>(required)</td><td>Button content</td></tr>\n<tr><td><code>...restProps</code></td><td>HTML attributes</td><td></td><td>Additional attributes passed to the <code>&lt;button&gt;</code> element</td></tr>\n</tbody>\n</table>\n\n<h2>Examples</h2>\n\n<p>Inside a ToolBar:</p>\n\n<pre><code class=\"language-html\">&lt;ToolBar label=\"Text formatting\"&gt;\n  &lt;ToolBarButton&gt;Bold&lt;/ToolBarButton&gt;\n  &lt;ToolBarButton&gt;Italic&lt;/ToolBarButton&gt;\n  &lt;ToolBarButton disabled&gt;Strikethrough&lt;/ToolBarButton&gt;\n&lt;/ToolBar&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus the button (when not inside a ToolBar managing roving focus)</li>\n<li>Enter / Space: Activate the button (browser default)</li>\n<li>Arrow keys: Handled by parent ToolBar for roving focus navigation</li>\n<li>Button is skipped in tab order when <code>disabled</code> is true</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Implicit <code>button</code> role from the <code>&lt;button&gt;</code> element</li>\n<li><code>disabled</code> attribute communicates disabled state to assistive technology</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use inside ToolBar to represent one tool action such as formatting controls, drawing tools, or media actions.</li>\n<li>Use when you want a consistent button component that integrates with the parent ToolBar's roving focus navigation.</li>\n<li>Use for icon-only buttons with <code>aria-label</code> for accessible labelling.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use outside ToolBar -- use Button for standalone actions.</li>\n<li>Do not use for task shortcuts -- use TaskBarButton inside a TaskBar instead.</li>\n<li>Do not use for navigation links -- use ActionLink instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a native <code>&lt;button type=\"button\"&gt;</code> element with <code>disabled</code> support. It relies on the parent ToolBar for roving focus keyboard navigation. The consumer provides all content (text, icons) and all visual styling.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.tool-bar-button</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;button&gt;</code> element with class <code>tool-bar-button</code></li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Maintain consistent button sizing within the toolbar. For icon-only buttons, ensure there is an accessible label or tooltip.</li>\n<li><strong>Developers</strong>: Use <code>type=\"button\"</code> (already set) to prevent unintended form submissions. Pass additional attributes like <code>aria-label</code> via restProps for icon-only buttons.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>ToolBarButton is a child of ToolBar, following the Bar/BarButton pattern. The parent ToolBar manages roving focus navigation between ToolBarButton children.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>tool-bar</code> — a horizontal bar of tool actions</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Toolbar Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/toolbar/</li>\n</ul>";
</script>

<svelte:head>
  <title>Tool Bar Button — Lily Design System</title>
  <meta name="description" content="A tool bar button is a single button within a toolbar, rendered as a native &lt;button&gt; element for proper keyboard and screen reader support. It is designed to be placed inside a ToolBar container th..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
