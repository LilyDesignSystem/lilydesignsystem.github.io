<script lang="ts">
  const html: string = "<h1>Feature Card</h1>\n\n<p>A feature card is a large content card that pairs a prominent image with a heading, optional description, and optional calls-to-action. The image can be positioned at the start, end, or top of the card to create varied editorial layouts.</p>\n\n<p>Use it for marketing pages, product feature highlights, blog post previews, program announcements, and other compositions that benefit from a strong visual paired with rich text.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Uses an <code>&lt;article&gt;</code> element to declare a self-contained composition</li>\n<li><code>heading</code> is <strong>required</strong> and renders inside <code>&lt;header&gt;</code> as an <code>&lt;h3&gt;</code></li>\n<li><code>imagePosition</code> is exposed as <code>data-image-position</code> so consumer CSS can lay out start/end/top variants</li>\n<li><code>imageUrl</code> and <code>imageAlt</code> are optional; the image renders only when both are useful</li>\n<li><code>label</code> overrides the implicit accessible name; defaults to <code>heading</code></li>\n<li><code>children</code> render after the description for arbitrary additional content (CTAs, badges)</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>heading</code>: string (<strong>required</strong>) -- card heading</li>\n<li><code>imagePosition</code>: \"start\" | \"end\" | \"top\" (default: \"start\") -- image placement</li>\n<li><code>imageUrl</code>: string (optional) -- image source</li>\n<li><code>imageAlt</code>: string (optional) -- image alt text</li>\n<li><code>description</code>: string (optional) -- body text</li>\n<li><code>label</code>: string (optional) -- aria-label override; defaults to heading</li>\n<li><code>children</code>: slot (optional) -- additional content (e.g., CTAs)</li>\n<li><code>...restProps</code>: any additional HTML attributes</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Basic feature card with image start:</p>\n\n<pre><code class=\"language-html\">&lt;FeatureCard\n  heading=\"Privacy first\"\n  description=\"We protect your data with end-to-end encryption.\"\n  imageUrl=\"/images/privacy.jpg\"\n  imageAlt=\"Padlock illustration\"\n&gt;\n  &lt;a href=\"/privacy\"&gt;Learn more&lt;/a&gt;\n&lt;/FeatureCard&gt;</code></pre>\n\n<p>Image at the top:</p>\n\n<pre><code class=\"language-html\">&lt;FeatureCard\n  heading=\"Open by design\"\n  imagePosition=\"top\"\n  imageUrl=\"/images/open.jpg\"\n  imageAlt=\"Open lock illustration\"\n  description=\"Built on open standards from day one.\"\n/&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>None on the card itself (passive container); interactive children manage their own keyboard behavior.</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Implicit <code>article</code> semantics from the <code>&lt;article&gt;</code> element</li>\n<li><code>aria-label</code> provides the accessible name (defaults to <code>heading</code>)</li>\n<li>The image's <code>alt</code> attribute provides its accessible name; decorative images can pass an empty <code>alt</code></li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to highlight a single feature, story, or product capability with image and text together.</li>\n<li>Use in marketing layouts, pricing pages, and program landing pages.</li>\n<li>Use when the image is informative or strongly enhances the message.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for compact list items — use Card or Tile.</li>\n<li>Do not use for navigation tiles — consider a tile with a single link inside.</li>\n<li>Do not omit <code>imageAlt</code> for non-decorative images.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>Renders an <code>&lt;article&gt;</code> with optional <code>&lt;img&gt;</code>, a <code>&lt;header&gt;</code> containing the heading, an optional <code>&lt;p&gt;</code> description, and any children. Layout (image side, image-on-top, responsive flow) is the consumer's responsibility, with <code>data-image-position</code> available as a hook.</p>\n\n<h2>Styles</h2>\n\n<p>Consumer CSS targets the <code>feature-card</code> class. Use the <code>data-image-position=\"start\"</code>, <code>data-image-position=\"end\"</code>, and <code>data-image-position=\"top\"</code> attribute selectors to lay out variants. Style the inner <code>feature-card-image</code>, <code>feature-card-header</code>, <code>feature-card-heading</code>, and <code>feature-card-description</code> classes as needed.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders an <code>&lt;article&gt;</code> element with class <code>feature-card</code></li>\n<li>Verify <code>data-image-position</code> reflects the <code>imagePosition</code> prop (defaulting to <code>start</code>)</li>\n<li>Verify the heading is rendered inside <code>&lt;header&gt;</code> as an <code>&lt;h3&gt;</code> with class <code>feature-card-heading</code></li>\n<li>Verify the <code>&lt;img&gt;</code> renders only when <code>imageUrl</code> is provided</li>\n<li>Verify the description is rendered inside <code>&lt;p class=\"feature-card-description\"&gt;</code> when provided</li>\n<li>Verify <code>aria-label</code> defaults to <code>heading</code> and can be overridden by <code>label</code></li>\n<li>Verify children render after the description</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Keep the image meaningful and high-contrast. Match the heading hierarchy to surrounding content (most pages should use <code>&lt;h3&gt;</code> for cards inside an <code>&lt;h2&gt;</code> section).</li>\n<li><strong>Developers</strong>: Always provide <code>imageAlt</code> (use an empty string for decorative imagery). Render CTAs as children so the consumer can swap link/button styles per surface.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>card</code> — a grouped content container with header, body, and footer areas</li>\n<li><code>hero</code> — a large box or image with a title and description</li>\n<li><code>call-to-action</code> — a prominent prompt encouraging user action</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN <code>&lt;article&gt;</code>: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/article</li>\n<li>WAI-ARIA <code>article</code> role: https://www.w3.org/TR/wai-aria-1.2/#article</li>\n<li>Mozilla Protocol Design System: https://protocol.mozilla.org/</li>\n</ul>";
</script>

<svelte:head>
  <title>Feature Card — Lily Design System</title>
  <meta name="description" content="A feature card is a large content card that pairs a prominent image with a heading, optional description, and optional calls-to-action. The image can be positioned at the start, end, or top of the ..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
