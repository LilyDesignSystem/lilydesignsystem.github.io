<script lang="ts">
  const html: string = "<h1>Error Summary</h1>\n\n<p>An error summary displays a consolidated list of form validation errors at the top of a form. It alerts users to problems that need correction and provides links to the specific fields with errors, following the pattern used by GOV.UK and NHS England design systems.</p>\n\n<p>This component uses <code>role=\"alert\"</code> to ensure screen readers announce the error summary immediately when it appears. The <code>tabindex=\"-1\"</code> attribute allows programmatic focus (e.g., via JavaScript <code>focus()</code>) so the user's attention is directed to the error list after form submission. A heading identifies the summary, and children typically contain a list of linked error messages.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> with <code>role=\"alert\"</code> for live-region announcement</li>\n<li>Uses <code>aria-labelledby</code> to associate the container with its heading</li>\n<li>Contains an <code>&lt;h2&gt;</code> element with a randomly generated ID for the <code>aria-labelledby</code> reference</li>\n<li><code>tabindex=\"-1\"</code> allows programmatic focus but does not place the element in the tab order</li>\n<li>Children slot is rendered after the heading for the error list content</li>\n<li>Spreads <code>restProps</code> on the outer <code>&lt;div&gt;</code> element</li>\n<li>Heading ID is generated with <code>Math.random().toString(36).slice(2, 9)</code> to avoid collisions</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>title</code>: string (required) -- heading text for the error summary (e.g., \"There is a problem\")</li>\n<li><code>children</code>: slot (required) -- error list content, typically a <code>&lt;ul&gt;</code> with <code>&lt;li&gt;</code> items containing anchor links to error fields</li>\n<li><code>...restProps</code>: any additional HTML attributes spread onto the outer <code>&lt;div&gt;</code></li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;ErrorSummary heading=\"There is a problem\"&gt;\n  &lt;ul&gt;\n    &lt;li&gt;&lt;a href=\"#date-of-birth\"&gt;Enter your date of birth&lt;/a&gt;&lt;/li&gt;\n    &lt;li&gt;&lt;a href=\"#email\"&gt;Enter an email address in the correct format, like name@example.com&lt;/a&gt;&lt;/li&gt;\n  &lt;/ul&gt;\n&lt;/ErrorSummary&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;ErrorSummary heading=\"There is a problem\"&gt;\n  &lt;ul&gt;\n    &lt;li&gt;&lt;a href=\"#full-name\"&gt;Enter your full name&lt;/a&gt;&lt;/li&gt;\n    &lt;li&gt;&lt;a href=\"#nhs-number\"&gt;Enter your NHS number&lt;/a&gt;&lt;/li&gt;\n    &lt;li&gt;&lt;a href=\"#password\"&gt;Password must be at least 8 characters&lt;/a&gt;&lt;/li&gt;\n  &lt;/ul&gt;\n&lt;/ErrorSummary&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li><strong>Tab</strong>: Moves focus through the error links within the summary</li>\n<li>The container itself can receive programmatic focus via <code>element.focus()</code> due to <code>tabindex=\"-1\"</code></li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"alert\"</code> -- creates a live region that announces content to screen readers when the summary appears</li>\n<li><code>aria-labelledby={titleId}</code> -- associates the alert region with its heading for context</li>\n<li><code>tabindex=\"-1\"</code> -- allows the error summary to receive programmatic focus without being in the natural tab order</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use at the top of a page when a form submission has validation errors, to summarise all errors in one place.</li>\n<li>Use even when there is only a single error -- users need the summary to understand what went wrong.</li>\n<li>Use with linked error descriptions that take users directly to the field with the problem.</li>\n<li>Use alongside individual ErrorMessage components next to each field -- the summary supplements, not replaces, field-level errors.</li>\n<li>Use the heading \"There is a problem\" as the default heading text.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for warnings or informational messages -- use Alert or Banner instead.</li>\n<li>Do not use before the user has attempted to submit the form -- errors should appear on submission, not while typing.</li>\n<li>Do not use without individual ErrorMessage components next to each field -- the summary and field-level errors work together.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component provides a <code>&lt;div&gt;</code> with <code>role=\"alert\"</code>, an <code>&lt;h2&gt;</code> heading linked via <code>aria-labelledby</code>, and <code>tabindex=\"-1\"</code> for programmatic focus. It announces the error summary immediately when rendered. The consumer provides all visual styling, error list content, and anchor links to error fields.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.error-summary</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>error-summary</code></li>\n<li>Verify role=\"alert\"` -- creates a live region that announces content to screen readers when the summary appears</li>\n<li>Verify aria-labelledby={titleId}` -- associates the alert region with its heading for context</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Place the error summary prominently at the top of the form with a distinct visual treatment (e.g., red border, error icon). Each error should be a clickable link that scrolls to the relevant field.</li>\n<li><strong>Developers</strong>: Call <code>element.focus()</code> on the error summary after form submission fails to direct the user's attention. Populate each list item with an anchor link targeting the input's ID.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>error-message</code> — an error message associated with a form field</li>\n<li><code>alert</code> — a status message for important information or feedback</li>\n<li><code>field</code> — a form field wrapper with label, input, and error message</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>GOV.UK Error Summary: https://design-system.service.gov.uk/components/error-summary/</li>\n<li>WAI-ARIA Alert Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/alert/</li>\n</ul>";
</script>

<svelte:head>
  <title>Error Summary — Lily Design System</title>
  <meta name="description" content="An error summary displays a consolidated list of form validation errors at the top of a form. It alerts users to problems that need correction and provides links to the specific fields with errors,..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
