<script lang="ts">
  const html: string = "<h1>Chat Message</h1>\n\n<p>A chat message shows one chat conversation message entry and all its data, including the author avatar, author name, time, and message content. It represents a single message in a chat interface, providing semantic structure for messaging applications, customer support tools, and collaborative platforms.</p>\n\n<p>This headless component uses the native HTML <code>&lt;article&gt;</code> element to represent a self-contained composition of a chat message. It is typically placed inside a ChatListItem component as part of the ChatNav composition pattern.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders an <code>&lt;article&gt;</code> element for self-contained message semantics</li>\n<li>Consumer provides author avatar, author name, timestamp, and message content as children</li>\n<li><code>aria-label</code> provides an accessible description of the message (e.g. \"Message from Alice at 10:30 AM\")</li>\n<li>Supports a <code>&lt;time&gt;</code> element within the content for the message timestamp</li>\n<li>Spreads <code>restProps</code> onto the <code>&lt;article&gt;</code> for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (optional) -- accessible label override for the message</li>\n<li><code>children</code>: slot (required) -- message content including avatar, author name, time, and text</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;article&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>A patient messaging conversation in a healthcare portal:</p>\n\n<pre><code class=\"language-html\">&lt;ChatNav label=\"Patient messages\"&gt;\n  &lt;ChatList&gt;\n    &lt;ChatListItem&gt;\n      &lt;ChatMessage label=\"Message from Dr Sarah Mitchell at 10:30 AM\"&gt;\n        &lt;Avatar&gt;\n          &lt;AvatarImage src=\"/photos/sm.jpg\" alt=\"Dr Sarah Mitchell\" /&gt;\n        &lt;/Avatar&gt;\n        &lt;span&gt;Dr Sarah Mitchell&lt;/span&gt;\n        &lt;time datetime=\"2025-03-15T10:30:00\"&gt;10:30 AM&lt;/time&gt;\n        &lt;p&gt;Your blood test results are back. Everything looks normal.&lt;/p&gt;\n      &lt;/ChatMessage&gt;\n    &lt;/ChatListItem&gt;\n    &lt;ChatListItem current&gt;\n      &lt;ChatMessage label=\"Message from Karen Francis at 10:45 AM\"&gt;\n        &lt;Avatar&gt;\n          &lt;AvatarImage src=\"/photos/kf.jpg\" alt=\"Karen Francis\" /&gt;\n        &lt;/Avatar&gt;\n        &lt;span&gt;Karen Francis&lt;/span&gt;\n        &lt;time datetime=\"2025-03-15T10:45:00\"&gt;10:45 AM&lt;/time&gt;\n        &lt;p&gt;Thank you, doctor. Should I book a follow-up?&lt;/p&gt;\n      &lt;/ChatMessage&gt;\n    &lt;/ChatListItem&gt;\n  &lt;/ChatList&gt;\n&lt;/ChatNav&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus moves to interactive elements within the message (links, buttons)</li>\n<li>(The article element itself is not typically focusable)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li>Implicit <code>article</code> role from <code>&lt;article&gt;</code> element</li>\n<li><code>aria-label</code> provides an accessible description of the message</li>\n<li><code>&lt;time&gt;</code> element with <code>datetime</code> attribute for machine-readable timestamps</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to display one message in a chat conversation, including author avatar, name, timestamp, and message content.</li>\n<li>Use inside a ChatListItem as part of the ChatNav composition pattern for conversation lists.</li>\n<li>Use in standalone chat thread views for displaying message history.</li>\n<li>Use when building real-time or asynchronous messaging interfaces.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for comments on content such as articles or clinical notes -- use Comment instead.</li>\n<li>Do not use for system notifications or alerts -- use Alert or Notification.</li>\n<li>Do not use for log entries or event history -- use TimelineList.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides an <code>&lt;article&gt;</code> element with optional <code>aria-label</code> for message description, with zero visual styling. The consumer is responsible for all CSS including avatar positioning, message bubble styling, timestamp formatting, author name layout, and sent/received message alignment.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.chat-message</code> class for targeting. No default styles are included -- this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders an <code>&lt;article&gt;</code> element with class <code>chat-message</code></li>\n<li>Verify <code>aria-label</code> for accessible message description</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Visually distinguish sent and received messages with different alignment or bubble colors. Show avatars, author names, and timestamps consistently. Ensure message text has sufficient contrast and readable font size.</li>\n<li><strong>Developers</strong>: Always provide a descriptive <code>label</code> prop (e.g., \"Message from Alice at 10:30 AM\") for screen reader context. Use semantic <code>&lt;time&gt;</code> elements with <code>datetime</code> attributes for timestamps.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>ChatMessage is part of the ChatNav composition pattern:</p>\n\n<ul>\n<li><strong>ChatNav</strong> -- outer <code>&lt;nav&gt;</code> container providing the navigation landmark and accessible label.</li>\n<li><strong>ChatList</strong> -- <code>&lt;ol&gt;</code> list conveying the ordered relationship of chat entries.</li>\n<li><strong>ChatListItem</strong> -- individual <code>&lt;li&gt;</code> items, each typically containing a ChatMessage component.</li>\n<li><strong>ChatMessage</strong> -- <code>&lt;article&gt;</code> element displaying one chat message with author, time, and content.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>chat-nav</code> — a navigation container for chat information</li>\n<li><code>chat-list</code> — an ordered list of chat list item components</li>\n<li><code>chat-list-item</code> — one chat list item component, typically containing one chat message component</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>MDN HTML article element: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/article</li>\n</ul>";
</script>

<svelte:head>
  <title>Chat Message — Lily Design System</title>
  <meta name="description" content="A chat message shows one chat conversation message entry and all its data, including the author avatar, author name, time, and message content. It represents a single message in a chat interface, p..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
