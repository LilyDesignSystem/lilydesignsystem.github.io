<script lang="ts">
  const html: string = "<h1>Segment Group</h1>\n\n<p>SegmentGroup is a headless container for segmented controls that uses the ARIA <code>radiogroup</code> role. It groups a set of mutually exclusive options, such as view mode toggles (grid/list), filter selections, or display density controls.</p>\n\n<p>Use SegmentGroup when you need a set of visually distinct toggle buttons where only one option can be active at a time. Unlike RadioGroup which wraps native radio inputs in a fieldset, SegmentGroup uses a <code>&lt;div&gt;</code> with <code>role=\"radiogroup\"</code> and expects children to be buttons with <code>role=\"radio\"</code> and <code>aria-checked</code> attributes. This makes it suitable for custom segmented control UIs that go beyond standard radio button appearance.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> element with <code>role=\"radiogroup\"</code> for ARIA semantics</li>\n<li>The <code>aria-label</code> attribute provides an accessible name for the group</li>\n<li>Consumer provides segment button children, each with <code>role=\"radio\"</code> and <code>aria-checked</code></li>\n<li>Consumer is responsible for managing selection state (toggling <code>aria-checked</code> on children)</li>\n<li>Spreads <code>restProps</code> onto the div for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible label for the segment group, applied via <code>aria-label</code></li>\n<li><code>children</code>: slot (required) -- segment button elements to render inside the group</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;div&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>View mode toggle between Grid and List:</p>\n\n<pre><code class=\"language-html\">&lt;SegmentGroup label=\"View mode\"&gt;\n  &lt;SegmentGroupItem checked={mode === 'grid'} value=\"grid\" onclick={() =&gt; mode = 'grid'}&gt;Grid&lt;/SegmentGroupItem&gt;\n  &lt;SegmentGroupItem checked={mode === 'list'} value=\"list\" onclick={() =&gt; mode = 'list'}&gt;List&lt;/SegmentGroupItem&gt;\n&lt;/SegmentGroup&gt;</code></pre>\n\n<p>Task status filter:</p>\n\n<pre><code class=\"language-html\">&lt;SegmentGroup label=\"Task filter\"&gt;\n  &lt;SegmentGroupItem checked={filter === 'all'} value=\"all\" onclick={() =&gt; filter = 'all'}&gt;All&lt;/SegmentGroupItem&gt;\n  &lt;SegmentGroupItem checked={filter === 'active'} value=\"active\" onclick={() =&gt; filter = 'active'}&gt;Active&lt;/SegmentGroupItem&gt;\n  &lt;SegmentGroupItem checked={filter === 'completed'} value=\"completed\" onclick={() =&gt; filter = 'completed'}&gt;Completed&lt;/SegmentGroupItem&gt;\n&lt;/SegmentGroup&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Moves focus into the segment group</li>\n<li>Arrow Left / Arrow Right: Consumer should implement arrow key navigation between segments (following the radiogroup pattern)</li>\n<li>Space / Enter: Consumer should implement activation of the focused segment</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"radiogroup\"</code> -- identifies the container as a group of mutually exclusive radio-like options</li>\n<li><code>aria-label={label}</code> -- provides an accessible name for the group so screen readers announce its purpose</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use SegmentGroup for mutually exclusive single-select options displayed as connected segments, such as view mode toggles (List/Grid).</li>\n<li>Use SegmentGroup for filter toggles like \"All / Active / Completed\" in a task manager.</li>\n<li>Use SegmentGroup for display density controls such as \"Compact / Normal / Spacious\".</li>\n<li>Use SegmentGroup when you want a custom segmented control appearance beyond standard radio buttons.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use SegmentGroup for multi-select options -- use ToggleGroup instead.</li>\n<li>Do not use SegmentGroup for form choices that require submission with standard form semantics -- use RadioGroup instead.</li>\n<li>Do not use SegmentGroup for navigation between pages -- use TabBar instead.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>The SegmentGroup headless component provides a <code>&lt;div&gt;</code> with <code>role=\"radiogroup\"</code> and <code>aria-label</code> for accessible group naming. It expects children with <code>role=\"radio\"</code> and <code>aria-checked</code> attributes. The consumer provides all visual styling, selection state management, and keyboard navigation between segments.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.segment-group</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>segment-group</code></li>\n<li>Verify role=\"radiogroup\"` -- identifies the container as a group of mutually exclusive radio-like options</li>\n<li>Verify aria-label={label}` -- provides an accessible name for the group so screen readers announce its purpose</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Give the segmented control a clear visual boundary and ensure the active segment has a prominent selected state (e.g., filled background vs. outlined).</li>\n<li><strong>Developers</strong>: Implement arrow key navigation between segments following the WAI-ARIA radio group pattern, and manage roving tabindex so only the selected segment receives focus via Tab.</li>\n</ul>\n\n<h2>Composition</h2>\n\n<p>SegmentGroup is the container for SegmentGroupItem children. Use <code>&lt;SegmentGroup&gt;</code> to wrap <code>&lt;SegmentGroupItem&gt;</code> elements, providing the group-level accessible label while each item carries its own value and checked state.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>segment-group-item</code> — one selectable segment in a segment group</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Radio Group Pattern: https://www.w3.org/WAI/ARIA/apg/patterns/radio/</li>\n<li>WAI-ARIA Authoring Practices - Toolbar: https://www.w3.org/WAI/ARIA/apg/patterns/toolbar/</li>\n</ul>";
  // BEGIN auto-generated component example consts
  const demoHtml: string = "<div class=\"segment-group\" role=\"radiogroup\" aria-label=\"View\"><button class=\"segment-group-item\" role=\"radio\" aria-checked=\"true\">Grid</button><button class=\"segment-group-item\" role=\"radio\">List</button></div>";
  const svelteSource: string = "// In your Svelte component:\nimport SegmentGroup from \"lily-design-system-svelte-headless/components/SegmentGroup/SegmentGroup.svelte\";\n\n<SegmentGroup>\n  <!-- list children -->\n</SegmentGroup>\n";
  // END auto-generated component example consts
</script>

<svelte:head>
  <title>Segment Group — Lily Design System</title>
  <meta name="description" content="SegmentGroup is a headless container for segmented controls that uses the ARIA radiogroup role. It groups a set of mutually exclusive options, such as view mode toggles (grid/list), filter selectio..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>

<!-- BEGIN auto-generated component example -->
<section class="component-example" aria-labelledby="example-heading" style="max-width: 64rem; margin: 0 auto; padding: 0 1rem 2rem;">
  <h2 id="example-heading">Example</h2>
  <div class="component-example-rendered" style="padding: 1rem; border: 1px solid #d8dde0; border-radius: 0.5rem; background: #ffffff;">{@html demoHtml}</div>
  <details style="margin-top: 1rem;">
    <summary style="cursor: pointer; font-weight: 600;">Show Svelte source</summary>
    <pre style="overflow-x: auto; padding: 1rem; background: #f0f4f5; border-radius: 0.5rem;"><code>{svelteSource}</code></pre>
  </details>
</section>
<!-- END auto-generated component example -->

