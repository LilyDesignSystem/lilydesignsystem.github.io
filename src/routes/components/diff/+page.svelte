<script lang="ts">
  const html: string = "<h1>Diff</h1>\n\n<p>A diff is a side-by-side comparison of two items, allowing users to visually identify differences between two versions of content. It is commonly used in code review tools, document comparison interfaces, version control systems, and before/after image comparisons.</p>\n\n<p>This headless component uses a <code>&lt;div&gt;</code> element as the container for the two comparison panels. The consumer provides the two items to compare as children.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a <code>&lt;div&gt;</code> element as the comparison container</li>\n<li><code>role=\"group\"</code> with <code>aria-label</code> identifies the comparison region</li>\n<li>Consumer provides two child elements representing the items to compare</li>\n<li>Supports labeling each side (e.g., \"Before\" and \"After\", \"Original\" and \"Modified\")</li>\n<li>Spreads <code>restProps</code> onto the <code>&lt;div&gt;</code> for consumer customization</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>label</code>: string (required) -- accessible name describing the comparison (e.g., \"Code comparison\")</li>\n<li><code>children</code>: slot (required) -- two child elements representing the items to compare</li>\n<li><code>...restProps</code>: unknown -- additional attributes spread onto the <code>&lt;div&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<p>Basic side-by-side comparison:</p>\n\n<pre><code class=\"language-html\">&lt;Diff label=\"Code comparison\"&gt;\n  &lt;div&gt;Original content here&lt;/div&gt;\n  &lt;div&gt;Modified content here&lt;/div&gt;\n&lt;/Diff&gt;</code></pre>\n\n<p>Before/after image comparison:</p>\n\n<pre><code class=\"language-html\">&lt;Diff label=\"Image comparison\"&gt;\n  &lt;figure&gt;\n    &lt;img src=\"before.jpg\" alt=\"Before redesign\" /&gt;\n    &lt;figcaption&gt;Before&lt;/figcaption&gt;\n  &lt;/figure&gt;\n  &lt;figure&gt;\n    &lt;img src=\"after.jpg\" alt=\"After redesign\" /&gt;\n    &lt;figcaption&gt;After&lt;/figcaption&gt;\n  &lt;/figure&gt;\n&lt;/Diff&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab: Focus moves between interactive elements within the comparison panels</li>\n<li>(Keyboard interactions depend on the content placed within the diff panels)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"group\"</code> with <code>aria-label</code> identifies the comparison region</li>\n<li>Each side can use <code>aria-label</code> to describe its role (e.g., \"Original\", \"Modified\")</li>\n<li>Screen readers announce the group label to provide context for the comparison</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use for comparing two versions of text, code, images, or other content side by side.</li>\n<li>Use in code review interfaces to show changes between file versions.</li>\n<li>Use for before/after comparisons in design tools or content editing.</li>\n<li>Avoid for displaying a single item -- use a standard content container instead.</li>\n<li>Consider DataTable instead when comparing structured data across multiple rows and columns.</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use to display a single item — use a standard content container instead.</li>\n<li>Do not use when comparing structured data across many rows and columns — use <code>DataTable</code> so users can sort and scan.</li>\n<li>Do not use for displaying merged or already-resolved content — <code>Diff</code> is for showing the comparison itself, not the result.</li>\n<li>Do not use as a navigation pattern between two pages — use <code>BreadcrumbNav</code> or page links.</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This component provides a <code>&lt;div&gt;</code> with <code>role=\"group\"</code> and <code>aria-label</code> for the comparison container, with zero visual styling. The consumer is responsible for all CSS including side-by-side layout, panel widths, divider styling, highlighting of differences, scroll synchronization, and responsive behavior.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.diff</code> class for targeting. No default styles are included -- this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>diff</code></li>\n<li>Verify <code>role=\"group\"</code> with <code>aria-label</code> for the comparison region</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use clear visual separation between the two panels (e.g., a vertical divider). Highlight additions, deletions, and modifications with distinct colors. Ensure responsive behavior for narrow viewports, such as stacking panels vertically.</li>\n<li><strong>Developers</strong>: Always provide a descriptive <code>label</code> prop for the comparison group. Label each side clearly so screen reader users understand which content is the original and which is the modified version.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>code-block</code> — a block of formatted code with optional line numbers and line highlighting</li>\n<li><code>table</code> — a table with rows and columns &lt;table&gt;</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>WAI-ARIA Group Role: https://www.w3.org/TR/wai-aria-1.2/#group</li>\n</ul>";
</script>

<svelte:head>
  <title>Diff — Lily Design System</title>
  <meta name="description" content="A diff is a side-by-side comparison of two items, allowing users to visually identify differences between two versions of content. It is commonly used in code review tools, document comparison inte..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
