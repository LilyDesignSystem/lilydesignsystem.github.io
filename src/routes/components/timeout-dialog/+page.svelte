<script lang="ts">
  const html: string = "<h1>TimeoutDialog</h1>\n\n<p>A modal dialog warning users before session timeout, giving them a clear chance to keep working before unsaved data is lost or they are signed out.</p>\n\n<p>The component is built on the native <code>&lt;dialog&gt;</code> element opened via <code>showModal()</code> so that focus is trapped and the rest of the page becomes inert. It uses <code>role=\"alertdialog\"</code> so screen readers announce the warning as an interruption that requires immediate attention. The consumer drives the countdown — the component takes a live <code>remainingSeconds</code> prop and re-renders the visible time, but does not start, stop, or reset any timer of its own.</p>\n\n<p>Common in services that hold sensitive data (banking, government, healthcare) where automatic sign-out is a regulatory requirement, but where the user still deserves an opportunity to extend the session before being kicked out.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>Renders a native <code>&lt;dialog&gt;</code> opened with <code>showModal()</code> so the dialog is modal and focus-trapped automatically</li>\n<li><code>role=\"alertdialog\"</code> ensures assistive technology announces it as an interruption</li>\n<li>Provides two slots: a body for the warning message (and remaining time) and a footer for action buttons</li>\n<li>Common buttons are \"Stay signed in\" (extends the session) and \"Sign out\" (ends the session immediately)</li>\n<li>Consumer drives the countdown clock; the component re-renders when <code>remainingSeconds</code> changes</li>\n<li>Closes via <code>close()</code> on the dialog element, returning focus to the previously focused element</li>\n</ul>\n\n<h2>Props</h2>\n\n<table>\n<thead>\n<tr><th>Prop</th><th>Type</th><th>Default</th><th>Description</th></tr>\n</thead>\n<tbody>\n<tr><td><code>title</code></td><td>string (required)</td><td>—</td><td>Dialog heading</td></tr>\n<tr><td><code>open</code></td><td>boolean</td><td>false</td><td>Whether the dialog is open; bindable</td></tr>\n<tr><td><code>remainingSeconds</code></td><td>number (required)</td><td>—</td><td>Seconds remaining before timeout</td></tr>\n<tr><td><code>onExtend</code></td><td>callback</td><td>—</td><td>Fired when the user chooses to stay signed in</td></tr>\n<tr><td><code>onSignOut</code></td><td>callback</td><td>—</td><td>Fired when the user chooses to sign out now</td></tr>\n<tr><td><code>...restProps</code></td><td>HTML attributes</td><td>—</td><td>Spread onto the root <code>&lt;dialog&gt;</code></td></tr>\n</tbody>\n</table>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;TimeoutDialog title=\"You are about to be signed out\" open={isOpen} remainingSeconds={120}\n  onExtend={extendSession} onSignOut={signOutNow} /&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>Tab / Shift+Tab cycles between focusable elements inside the dialog (focus trap)</li>\n<li>Escape: triggers <code>cancel</code> (consumer typically wires this to \"Sign out\" or \"Stay signed in\")</li>\n<li>Enter: activates the focused button (native browser behaviour)</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"alertdialog\"</code> identifies this as an interruption requiring user attention</li>\n<li><code>aria-labelledby</code> references the heading element</li>\n<li><code>aria-describedby</code> references the body content</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Warning users before an inactive session ends, giving them a chance to extend it</li>\n<li>Any context where automatic timeout is a security or data-integrity requirement</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Use <code>AlertDialog</code> for non-timeout urgent confirmations</li>\n<li>Use <code>Dialog</code> for general modal content without alert semantics</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless component renders semantic HTML with appropriate ARIA wiring. The consumer provides all visual styling — no CSS, animations, or layout assumptions are baked in.</p>\n\n<h2>Styles</h2>\n\n<p>The component renders with <code>.timeout-dialog</code> as the root class. No default styles are included.</p>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>dialog</code> — generic modal dialog</li>\n<li><code>alert-dialog</code> — modal dialog for urgent confirmations</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/HTML/Element/dialog\">MDN dialog element</a></li>\n<li><a href=\"https://www.w3.org/TR/wai-aria-1.2/#alertdialog\">WAI-ARIA alertdialog role</a></li>\n</ul>";
</script>

<svelte:head>
  <title>TimeoutDialog — Lily Design System</title>
  <meta name="description" content="A modal dialog warning users before session timeout, giving them a clear chance to keep working before unsaved data is lost or they are signed out." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
