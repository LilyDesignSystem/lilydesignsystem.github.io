<script lang="ts">
  const html: string = "<h1>Care Card</h1>\n\n<p>A care card is a healthcare-oriented component for displaying medical guidance with varying levels of urgency. Inspired by the NHS England design system, care cards communicate health advice in three tiers: non-urgent (speak to a GP), urgent (visit A&amp;E or call 111), and immediate (call 999).</p>\n\n<p>Each care card has a heading that describes the action and a body containing the relevant advice or conditions. The <code>data-type</code> attribute allows consumers to apply urgency-appropriate styling (e.g., blue for non-urgent, red for immediate). The card is rendered as a <code>&lt;section&gt;</code> landmark with an accessible label derived from the heading.</p>\n\n<h2>Implementation Notes</h2>\n\n<ul>\n<li>NHS England inspired pattern for healthcare guidance</li>\n<li>Three urgency levels: non-urgent, urgent, immediate</li>\n<li>Renders as <code>&lt;section role=\"region\"&gt;</code> with heading for landmark navigation</li>\n<li><code>aria-label</code> defaults to the heading text, overridable via the <code>label</code> prop</li>\n<li>Uses <code>&lt;h2&gt;</code> for the heading element</li>\n<li><code>data-type</code> attribute exposes the urgency level for consumer CSS styling</li>\n<li>Content is provided through child elements</li>\n</ul>\n\n<h2>Props</h2>\n\n<ul>\n<li><code>type</code>: \"non-urgent\" | \"urgent\" | \"immediate\" (default: \"non-urgent\") -- urgency level</li>\n<li><code>heading</code>: string (required) -- heading text for the care card</li>\n<li><code>label</code>: string (optional) -- <code>aria-label</code> override; defaults to heading text</li>\n<li><code>children</code>: slot (required) -- the care card body content</li>\n<li><code>...restProps</code>: Any additional HTML attributes passed to the <code>&lt;section&gt;</code> element</li>\n</ul>\n\n<h2>Usage</h2>\n\n<pre><code class=\"language-html\">&lt;CareCard type=\"non-urgent\" heading=\"See a GP if:\"&gt;\n  &lt;ul&gt;\n    &lt;li&gt;your symptoms do not improve after 2 weeks&lt;/li&gt;\n    &lt;li&gt;you have a high temperature for more than 3 days&lt;/li&gt;\n  &lt;/ul&gt;\n&lt;/CareCard&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;CareCard type=\"urgent\" heading=\"Ask for an urgent GP appointment or call 111 if:\"&gt;\n  &lt;ul&gt;\n    &lt;li&gt;you have difficulty breathing&lt;/li&gt;\n    &lt;li&gt;you have chest pain&lt;/li&gt;\n  &lt;/ul&gt;\n&lt;/CareCard&gt;</code></pre>\n\n<pre><code class=\"language-html\">&lt;CareCard type=\"immediate\" heading=\"Call 999 if:\"&gt;\n  &lt;ul&gt;\n    &lt;li&gt;someone is unconscious&lt;/li&gt;\n    &lt;li&gt;someone is having a seizure for the first time&lt;/li&gt;\n  &lt;/ul&gt;\n&lt;/CareCard&gt;</code></pre>\n\n<h2>Keyboard Interactions</h2>\n\n<ul>\n<li>None -- this component is a passive content container</li>\n</ul>\n\n<h2>ARIA</h2>\n\n<ul>\n<li><code>role=\"region\"</code> -- identifies the card as a named landmark</li>\n<li><code>aria-label</code> -- set from <code>label</code> prop or <code>heading</code> prop for screen reader identification</li>\n<li><code>data-type</code> -- exposes urgency level for consumer styling hooks</li>\n</ul>\n\n<h2>When to Use</h2>\n\n<ul>\n<li>Use to provide medical care instructions with urgency levels (non-urgent, urgent, emergency)</li>\n<li>Use when directing users to seek medical help, such as \"See a GP\" or \"Call 999\"</li>\n<li>Use to communicate the appropriate level of medical response for symptoms described on the page</li>\n<li>Use when medical guidance needs to be visually distinct from surrounding content with clear urgency indicators</li>\n<li>Use on health information pages where users need actionable next steps based on their symptoms</li>\n</ul>\n\n<h2>When Not to Use</h2>\n\n<ul>\n<li>Do not use for general warnings without medical context -- use WarningCallout instead</li>\n<li>Do not use for informational callouts or supplementary notes -- use InformationCallout or InsetText</li>\n<li>Do not use for system alerts or application-level messages -- use Alert or Banner</li>\n<li>Do not use for patient record banners -- use MedicalBanner with MedicalBannerBoxForDanger</li>\n</ul>\n\n<h2>Headless</h2>\n\n<p>This headless CareCard component provides the semantic <code>&lt;section&gt;</code> with <code>role=\"region\"</code>, accessible labeling from the heading, and a <code>data-type</code> attribute exposing the urgency level. The consumer provides all visual styling including urgency-specific colors (e.g., blue for non-urgent, red for immediate), card borders, spacing, and typography.</p>\n\n<h2>Styles</h2>\n\n<p>The consumer provides all CSS styling. The component renders with a <code>.care-card</code> class for targeting. No default styles are included — this is a fully headless component.</p>\n\n<h2>Testing</h2>\n\n<ul>\n<li>Verify the component renders a <code>&lt;div&gt;</code> element with class <code>care-card</code></li>\n<li>Verify role=\"region\"` -- identifies the card as a named landmark</li>\n<li>Verify aria-label<code> -- set from </code>label<code> prop or </code>heading` prop for screen reader identification</li>\n<li>Verify keyboard interactions work correctly</li>\n<li>Verify pass-through attributes are applied</li>\n</ul>\n\n<h2>Advice</h2>\n\n<ul>\n<li><strong>Designers</strong>: Use distinct, high-contrast colors for each urgency level so users can quickly identify severity. Follow NHS design patterns for familiarity in healthcare contexts.</li>\n<li><strong>Developers</strong>: Always set the <code>type</code> prop explicitly to match the clinical urgency. Use the <code>data-type</code> attribute in your CSS selectors to apply urgency-specific styling.</li>\n</ul>\n\n<h2>Related components</h2>\n\n<ul>\n<li><code>card</code> — a grouped content container with header, body, and footer areas</li>\n<li><code>medical-banner</code> — a prominent message bar across the top of a page, with medical information</li>\n<li><code>alert</code> — a status message for important information or feedback</li>\n</ul>\n\n<h2>References</h2>\n\n<ul>\n<li>NHS England Care Cards: https://service-manual.nhs.uk/design-system/components/care-cards</li>\n</ul>";
</script>

<svelte:head>
  <title>Care Card — Lily Design System</title>
  <meta name="description" content="A care card is a healthcare-oriented component for displaying medical guidance with varying levels of urgency. Inspired by the NHS England design system, care cards communicate health advice in thr..." />
</svelte:head>

<nav class="component-page-back" aria-label="Breadcrumb" style="max-width: 64rem; margin: 0 auto; padding: 1.5rem 1rem 0;">
  <a href="/components/">← All components</a>
</nav>

<article class="component-page prose" style="max-width: 64rem; margin: 0 auto; padding: 1rem 1rem 2rem;">
  {@html html}
</article>
