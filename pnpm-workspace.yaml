allowBuilds:
  esbuild: true
# @sveltejs/kit pins cookie@^0.6.0 directly (not yet updated upstream to
# the 0.7.0 fix for GHSA-pxg6-pf52-xh8x). Found while remediating
# Dependabot alerts; same fix applied across the other affected Svelte
# subprojects.
overrides:
  cookie: "0.7.2"
